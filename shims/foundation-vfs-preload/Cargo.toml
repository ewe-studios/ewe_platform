[package]
name = "foundation-vfs-preload"
version = "0.0.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "LD_PRELOAD/DYLD_INSERT_LIBRARIES shim — libc interception redirecting configured paths to a VFS daemon over IPC"

[lib]
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"
once_cell = "1"

[lints]
workspace = true
