[package]
name = "foundation_theme"
version = "0.0.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Design-token theme model + the single CSS generator shared by the theme! macro, the ThemeTokens derive, and the runtime Theme builder. no_std + alloc."

[lints]
workspace = true
