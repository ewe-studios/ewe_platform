[package]
name = "foundation_connectrpc"
version = "0.0.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "ConnectRPC for the EWE platform — Connect/gRPC/gRPC-Web on foundation crates"
keywords = ["connectrpc", "grpc", "rpc", "foundation"]

[dependencies]
foundation_errstacks = { path = "../foundation_errstacks", version = "0.0.1", features = ["to_structured"] }
foundation_netio = { path = "../foundation_netio", version = "0.0.1" }
foundation_http = { path = "../foundation_http", version = "0.0.1" }
foundation_arrow = { path = "../foundation_arrow", version = "0.0.1", optional = true }
buffa = { version = "0.8.1", features = ["json"] }
bytes = "1.5"
serde = { workspace = true }
serde_json = { workspace = true }
base64 = "0.22"
concurrent-queue = "2.5"
flate2 = "1.0"
zstd = { version = "0.13", optional = true }
brotli = { version = "8", optional = true }

[dev-dependencies]
foundation_errstacks = { path = "../foundation_errstacks", version = "0.0.1", features = ["to_structured"] }
serde_json = { workspace = true }
serde = { workspace = true }
buffa = { version = "0.8.1", features = ["json"] }
bytes = "1.5"
foundation_arrow = { path = "../foundation_arrow", version = "0.0.1" }

[features]
default = []
# Arrow columnar codec (platform extension) — pulls foundation_arrow.
arrow = ["dep:foundation_arrow"]
# Additional compressors (gzip is always available via flate2).
zstd = ["dep:zstd"]
brotli = ["dep:brotli"]
