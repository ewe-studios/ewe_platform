//! Auto-generated API module for docker logs.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `docker_logs `

#![cfg(feature = "docker_logs")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(clippy::missing_errors_doc, clippy::doc_markdown, clippy::useless_format)]
#![allow(unused_imports)]

use foundation_netio::{DynNetClient, PreparedRequestBuilder};
use foundation_netio::shared::client::http_client::HttpClient;
use serde::{Deserialize, Serialize};
use foundation_macros::JsonHash;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`ContainerLogs_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerLogsArgs {
    /// Path parameter: `id`.
    pub id: String,
    /// Query parameter: `follow`.
    pub follow: Option<String>,
    /// Query parameter: `stdout`.
    pub stdout: Option<String>,
    /// Query parameter: `stderr`.
    pub stderr: Option<String>,
    /// Query parameter: `since`.
    pub since: Option<String>,
    /// Query parameter: `until`.
    pub until: Option<String>,
    /// Query parameter: `timestamps`.
    pub timestamps: Option<String>,
    /// Query parameter: `tail`.
    pub tail: Option<String>,
}

/// Arguments for [`ServiceLogs_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ServiceLogsArgs {
    /// Path parameter: `id`.
    pub id: String,
    /// Query parameter: `details`.
    pub details: Option<String>,
    /// Query parameter: `follow`.
    pub follow: Option<String>,
    /// Query parameter: `stdout`.
    pub stdout: Option<String>,
    /// Query parameter: `stderr`.
    pub stderr: Option<String>,
    /// Query parameter: `since`.
    pub since: Option<String>,
    /// Query parameter: `timestamps`.
    pub timestamps: Option<String>,
    /// Query parameter: `tail`.
    pub tail: Option<String>,
}

/// Arguments for [`TaskLogs_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TaskLogsArgs {
    /// Path parameter: `id`.
    pub id: String,
    /// Query parameter: `details`.
    pub details: Option<String>,
    /// Query parameter: `follow`.
    pub follow: Option<String>,
    /// Query parameter: `stdout`.
    pub stdout: Option<String>,
    /// Query parameter: `stderr`.
    pub stderr: Option<String>,
    /// Query parameter: `since`.
    pub since: Option<String>,
    /// Query parameter: `timestamps`.
    pub timestamps: Option<String>,
    /// Query parameter: `tail`.
    pub tail: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /containers/{id}/logs
// -----------------------------------------------------------------------------

/// GET /containers/{id}/logs.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = container_logs_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn container_logs_request<F>(
    client: DynNetClient,
    args: &ContainerLogsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<serde_json::Value>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "http://localhost/v1.53/containers/{}/logs",
        args.id,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("follow", args.follow.as_deref());
    builder = builder.query("stdout", args.stdout.as_deref());
    builder = builder.query("stderr", args.stderr.as_deref());
    builder = builder.query("since", args.since.as_deref());
    builder = builder.query("until", args.until.as_deref());
    builder = builder.query("timestamps", args.timestamps.as_deref());
    builder = builder.query("tail", args.tail.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    let body_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
    let parsed: serde_json::Value = serde_json::from_slice(&body_bytes).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
    Ok(ApiResponse { status: status as u16, headers, body: parsed })
}

// -----------------------------------------------------------------------------
// GET /services/{id}/logs
// -----------------------------------------------------------------------------

/// GET /services/{id}/logs.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = service_logs_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn service_logs_request<F>(
    client: DynNetClient,
    args: &ServiceLogsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<serde_json::Value>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "http://localhost/v1.53/services/{}/logs",
        args.id,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("details", args.details.as_deref());
    builder = builder.query("follow", args.follow.as_deref());
    builder = builder.query("stdout", args.stdout.as_deref());
    builder = builder.query("stderr", args.stderr.as_deref());
    builder = builder.query("since", args.since.as_deref());
    builder = builder.query("timestamps", args.timestamps.as_deref());
    builder = builder.query("tail", args.tail.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    let body_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
    let parsed: serde_json::Value = serde_json::from_slice(&body_bytes).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
    Ok(ApiResponse { status: status as u16, headers, body: parsed })
}

// -----------------------------------------------------------------------------
// GET /tasks/{id}/logs
// -----------------------------------------------------------------------------

/// GET /tasks/{id}/logs.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = task_logs_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn task_logs_request<F>(
    client: DynNetClient,
    args: &TaskLogsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<serde_json::Value>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "http://localhost/v1.53/tasks/{}/logs",
        args.id,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("details", args.details.as_deref());
    builder = builder.query("follow", args.follow.as_deref());
    builder = builder.query("stdout", args.stdout.as_deref());
    builder = builder.query("stderr", args.stderr.as_deref());
    builder = builder.query("since", args.since.as_deref());
    builder = builder.query("timestamps", args.timestamps.as_deref());
    builder = builder.query("tail", args.tail.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    let body_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
    let parsed: serde_json::Value = serde_json::from_slice(&body_bytes).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
    Ok(ApiResponse { status: status as u16, headers, body: parsed })
}

