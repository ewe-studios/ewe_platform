//! Auto-generated API module for docker update.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `docker_update `

#![cfg(feature = "docker_update")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(clippy::missing_errors_doc, clippy::doc_markdown, clippy::useless_format)]
#![allow(unused_imports)]

use foundation_netio::{DynNetClient, PreparedRequestBuilder};
use foundation_netio::shared::client::http_client::HttpClient;
use serde::{Deserialize, Serialize};
use foundation_macros::JsonHash;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SecretSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecretSpec {
    /// Data property.
    #[serde(rename = "Data")]
    pub data: Option<String>,
    /// Driver property.
    #[serde(rename = "Driver")]
    pub driver: Option<Driver>,
    /// Labels property.
    #[serde(rename = "Labels")]
    pub labels: Option<serde_json::Value>,
    /// Name property.
    #[serde(rename = "Name")]
    pub name: Option<String>,
    /// Templating property.
    #[serde(rename = "Templating")]
    pub templating: Option<Driver>,
}

/// `NodeSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeSpec {
    /// Availability property.
    #[serde(rename = "Availability")]
    pub availability: Option<String>,
    /// Labels property.
    #[serde(rename = "Labels")]
    pub labels: Option<serde_json::Value>,
    /// Name property.
    #[serde(rename = "Name")]
    pub name: Option<String>,
    /// Role property.
    #[serde(rename = "Role")]
    pub role: Option<String>,
}

/// `ContainerUpdateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContainerUpdateResponse {
    /// Warnings property.
    #[serde(rename = "Warnings")]
    pub warnings: Option<Vec<String>>,
}

/// `Driver` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Driver {
    /// Name property.
    #[serde(rename = "Name")]
    pub name: String,
    /// Options property.
    #[serde(rename = "Options")]
    pub options: Option<serde_json::Value>,
}

/// `SwarmSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SwarmSpec {
    /// CAConfig property.
    #[serde(rename = "CAConfig")]
    pub ca_config: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// Dispatcher property.
    #[serde(rename = "Dispatcher")]
    pub dispatcher: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// EncryptionConfig property.
    #[serde(rename = "EncryptionConfig")]
    pub encryption_config: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// Labels property.
    #[serde(rename = "Labels")]
    pub labels: Option<serde_json::Value>,
    /// Name property.
    #[serde(rename = "Name")]
    pub name: Option<String>,
    /// Orchestration property.
    #[serde(rename = "Orchestration")]
    pub orchestration: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// Raft property.
    #[serde(rename = "Raft")]
    pub raft: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// TaskDefaults property.
    #[serde(rename = "TaskDefaults")]
    pub task_defaults: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ServiceUpdateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceUpdateResponse {
    /// Warnings property.
    #[serde(rename = "Warnings")]
    pub warnings: Option<Vec<String>>,
}

/// `ConfigSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConfigSpec {
    /// Data property.
    #[serde(rename = "Data")]
    pub data: Option<String>,
    /// Labels property.
    #[serde(rename = "Labels")]
    pub labels: Option<serde_json::Value>,
    /// Name property.
    #[serde(rename = "Name")]
    pub name: Option<String>,
    /// Templating property.
    #[serde(rename = "Templating")]
    pub templating: Option<Driver>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`ConfigUpdate_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConfigUpdateArgs {
    /// Path parameter: `id`.
    pub id: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
    /// Request body.
    pub body: ConfigSpec,
}

/// Arguments for [`ContainerUpdate_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerUpdateArgs {
    /// Path parameter: `id`.
    pub id: String,
}

/// Arguments for [`NodeUpdate_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct NodeUpdateArgs {
    /// Path parameter: `id`.
    pub id: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
    /// Request body.
    pub body: NodeSpec,
}

/// Arguments for [`SecretUpdate_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SecretUpdateArgs {
    /// Path parameter: `id`.
    pub id: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
    /// Request body.
    pub body: SecretSpec,
}

/// Arguments for [`ServiceUpdate_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ServiceUpdateArgs {
    /// Path parameter: `id`.
    pub id: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
    /// Query parameter: `registryAuthFrom`.
    pub registry_auth_from: Option<String>,
    /// Query parameter: `rollback`.
    pub rollback: Option<String>,
}

/// Arguments for [`SwarmUpdate_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SwarmUpdateArgs {
    /// Query parameter: `version`.
    pub version: Option<String>,
    /// Query parameter: `rotateWorkerToken`.
    pub rotate_worker_token: Option<String>,
    /// Query parameter: `rotateManagerToken`.
    pub rotate_manager_token: Option<String>,
    /// Query parameter: `rotateManagerUnlockKey`.
    pub rotate_manager_unlock_key: Option<String>,
    /// Request body.
    pub body: SwarmSpec,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST /configs/{id}/update
// -----------------------------------------------------------------------------

/// POST /configs/{id}/update.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = config_update_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn config_update_request<F>(
    client: DynNetClient,
    args: &ConfigUpdateArgs,
    base_url: &str,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let path = format!("/configs/{}/update",
        args.id,
    );
    let endpoint_url = format!("{}{}", base_url, path);

    let mut builder = PreparedRequestBuilder::post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("version", args.version.as_deref());

    builder = builder.body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// POST /containers/{id}/update
// -----------------------------------------------------------------------------

/// POST /containers/{id}/update.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = container_update_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn container_update_request<F>(
    client: DynNetClient,
    args: &ContainerUpdateArgs,
    base_url: &str,
    builder_mod: Option<F>,
) -> Result<ApiResponse<ContainerUpdateResponse>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let path = format!("/containers/{}/update",
        args.id,
    );
    let endpoint_url = format!("{}{}", base_url, path);

    let mut builder = PreparedRequestBuilder::post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    let body_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
    let parsed: ContainerUpdateResponse = serde_json::from_slice(&body_bytes).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
    Ok(ApiResponse { status: status as u16, headers, body: parsed })
}

// -----------------------------------------------------------------------------
// POST /nodes/{id}/update
// -----------------------------------------------------------------------------

/// POST /nodes/{id}/update.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = node_update_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn node_update_request<F>(
    client: DynNetClient,
    args: &NodeUpdateArgs,
    base_url: &str,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let path = format!("/nodes/{}/update",
        args.id,
    );
    let endpoint_url = format!("{}{}", base_url, path);

    let mut builder = PreparedRequestBuilder::post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("version", args.version.as_deref());

    builder = builder.body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// POST /secrets/{id}/update
// -----------------------------------------------------------------------------

/// POST /secrets/{id}/update.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = secret_update_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn secret_update_request<F>(
    client: DynNetClient,
    args: &SecretUpdateArgs,
    base_url: &str,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let path = format!("/secrets/{}/update",
        args.id,
    );
    let endpoint_url = format!("{}{}", base_url, path);

    let mut builder = PreparedRequestBuilder::post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("version", args.version.as_deref());

    builder = builder.body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// POST /services/{id}/update
// -----------------------------------------------------------------------------

/// POST /services/{id}/update.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = service_update_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn service_update_request<F>(
    client: DynNetClient,
    args: &ServiceUpdateArgs,
    base_url: &str,
    builder_mod: Option<F>,
) -> Result<ApiResponse<ServiceUpdateResponse>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let path = format!("/services/{}/update",
        args.id,
    );
    let endpoint_url = format!("{}{}", base_url, path);

    let mut builder = PreparedRequestBuilder::post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("version", args.version.as_deref());
    builder = builder.query("registryAuthFrom", args.registry_auth_from.as_deref());
    builder = builder.query("rollback", args.rollback.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    let body_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
    let parsed: ServiceUpdateResponse = serde_json::from_slice(&body_bytes).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
    Ok(ApiResponse { status: status as u16, headers, body: parsed })
}

// -----------------------------------------------------------------------------
// POST /swarm/update
// -----------------------------------------------------------------------------

/// POST /swarm/update.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = swarm_update_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn swarm_update_request<F>(
    client: DynNetClient,
    args: &SwarmUpdateArgs,
    base_url: &str,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let path = format!("/swarm/update",
    );
    let endpoint_url = format!("{}{}", base_url, path);

    let mut builder = PreparedRequestBuilder::post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("version", args.version.as_deref());
    builder = builder.query("rotateWorkerToken", args.rotate_worker_token.as_deref());
    builder = builder.query("rotateManagerToken", args.rotate_manager_token.as_deref());
    builder = builder.query("rotateManagerUnlockKey", args.rotate_manager_unlock_key.as_deref());

    builder = builder.body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

