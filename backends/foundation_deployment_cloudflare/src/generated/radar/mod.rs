//! Auto-generated API module for cloudflare radar.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `cloudflare_radar `

#![cfg(feature = "cloudflare_radar")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(clippy::missing_errors_doc, clippy::doc_markdown, clippy::useless_format)]
#![allow(unused_imports)]

use foundation_netio::{DynNetClient, PreparedRequestBuilder};
use foundation_netio::shared::client::http_client::HttpClient;
use serde::{Deserialize, Serialize};
use foundation_macros::JsonHash;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`radar-get-annotations_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetAnnotationsArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `offset`.
    pub offset: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `dataSource`.
    pub data_source: Option<String>,
    /// Query parameter: `eventType`.
    pub event_type: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `origin`.
    pub origin: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-annotations-outages_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetAnnotationsOutagesArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `offset`.
    pub offset: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `origin`.
    pub origin: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-annotations-outages-top_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetAnnotationsOutagesTopArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-dns-as112-summary_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetDnsAs112SummaryArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `queryType`.
    pub query_type: Option<String>,
    /// Query parameter: `protocol`.
    pub protocol: Option<String>,
    /// Query parameter: `responseCode`.
    pub response_code: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-dns-as112-timeseries_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetDnsAs112TimeseriesArgs {
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `queryType`.
    pub query_type: Option<String>,
    /// Query parameter: `protocol`.
    pub protocol: Option<String>,
    /// Query parameter: `responseCode`.
    pub response_code: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-dns-as112-timeseries-group_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetDnsAs112TimeseriesGroupArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `queryType`.
    pub query_type: Option<String>,
    /// Query parameter: `protocol`.
    pub protocol: Option<String>,
    /// Query parameter: `responseCode`.
    pub response_code: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-dns-as112-top-locations_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetDnsAs112TopLocationsArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-dns-as112-top-locations-by-dnssec_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetDnsAs112TopLocationsByDnssecArgs {
    /// Path parameter: `dnssec`.
    pub dnssec: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-dns-as112-top-locations-by-edns_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetDnsAs112TopLocationsByEdnsArgs {
    /// Path parameter: `edns`.
    pub edns: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-dns-as112-top-locations-by-ip-version_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetDnsAs112TopLocationsByIpVersionArgs {
    /// Path parameter: `ip_version`.
    pub ip_version: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-certificate-authorities_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetCertificateAuthoritiesArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `offset`.
    pub offset: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-certificate-authority-details_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetCertificateAuthorityDetailsArgs {
    /// Path parameter: `ca_slug`.
    pub ca_slug: String,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-certificate-logs_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetCertificateLogsArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `offset`.
    pub offset: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-certificate-log-details_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetCertificateLogDetailsArgs {
    /// Path parameter: `log_slug`.
    pub log_slug: String,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-ct-summary_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetCtSummaryArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `ca`.
    pub ca: Option<String>,
    /// Query parameter: `caOwner`.
    pub ca_owner: Option<String>,
    /// Query parameter: `duration`.
    pub duration: Option<String>,
    /// Query parameter: `entryType`.
    pub entry_type: Option<String>,
    /// Query parameter: `expirationStatus`.
    pub expiration_status: Option<String>,
    /// Query parameter: `hasIps`.
    pub has_ips: Option<String>,
    /// Query parameter: `hasWildcards`.
    pub has_wildcards: Option<String>,
    /// Query parameter: `log`.
    pub log: Option<String>,
    /// Query parameter: `logApi`.
    pub log_api: Option<String>,
    /// Query parameter: `logOperator`.
    pub log_operator: Option<String>,
    /// Query parameter: `publicKeyAlgorithm`.
    pub public_key_algorithm: Option<String>,
    /// Query parameter: `signatureAlgorithm`.
    pub signature_algorithm: Option<String>,
    /// Query parameter: `tld`.
    pub tld: Option<String>,
    /// Query parameter: `validationLevel`.
    pub validation_level: Option<String>,
    /// Query parameter: `uniqueEntries`.
    pub unique_entries: Option<String>,
    /// Query parameter: `normalization`.
    pub normalization: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-ct-timeseries_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetCtTimeseriesArgs {
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `ca`.
    pub ca: Option<String>,
    /// Query parameter: `caOwner`.
    pub ca_owner: Option<String>,
    /// Query parameter: `duration`.
    pub duration: Option<String>,
    /// Query parameter: `entryType`.
    pub entry_type: Option<String>,
    /// Query parameter: `expirationStatus`.
    pub expiration_status: Option<String>,
    /// Query parameter: `hasIps`.
    pub has_ips: Option<String>,
    /// Query parameter: `hasWildcards`.
    pub has_wildcards: Option<String>,
    /// Query parameter: `log`.
    pub log: Option<String>,
    /// Query parameter: `logApi`.
    pub log_api: Option<String>,
    /// Query parameter: `logOperator`.
    pub log_operator: Option<String>,
    /// Query parameter: `publicKeyAlgorithm`.
    pub public_key_algorithm: Option<String>,
    /// Query parameter: `signatureAlgorithm`.
    pub signature_algorithm: Option<String>,
    /// Query parameter: `tld`.
    pub tld: Option<String>,
    /// Query parameter: `validationLevel`.
    pub validation_level: Option<String>,
    /// Query parameter: `uniqueEntries`.
    pub unique_entries: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-ct-timeseries-group_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetCtTimeseriesGroupArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `ca`.
    pub ca: Option<String>,
    /// Query parameter: `caOwner`.
    pub ca_owner: Option<String>,
    /// Query parameter: `duration`.
    pub duration: Option<String>,
    /// Query parameter: `entryType`.
    pub entry_type: Option<String>,
    /// Query parameter: `expirationStatus`.
    pub expiration_status: Option<String>,
    /// Query parameter: `hasIps`.
    pub has_ips: Option<String>,
    /// Query parameter: `hasWildcards`.
    pub has_wildcards: Option<String>,
    /// Query parameter: `log`.
    pub log: Option<String>,
    /// Query parameter: `logApi`.
    pub log_api: Option<String>,
    /// Query parameter: `logOperator`.
    pub log_operator: Option<String>,
    /// Query parameter: `publicKeyAlgorithm`.
    pub public_key_algorithm: Option<String>,
    /// Query parameter: `signatureAlgorithm`.
    pub signature_algorithm: Option<String>,
    /// Query parameter: `validationLevel`.
    pub validation_level: Option<String>,
    /// Query parameter: `tld`.
    pub tld: Option<String>,
    /// Query parameter: `normalization`.
    pub normalization: Option<String>,
    /// Query parameter: `uniqueEntries`.
    pub unique_entries: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-reports-datasets_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetReportsDatasetsArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `offset`.
    pub offset: Option<String>,
    /// Query parameter: `datasetType`.
    pub dataset_type: Option<String>,
    /// Query parameter: `date`.
    pub date: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-post-reports-dataset-download-url_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarPostReportsDatasetDownloadUrlArgs {
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-reports-dataset-download_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetReportsDatasetDownloadArgs {
    /// Path parameter: `alias`.
    pub alias: String,
}

/// Arguments for [`radar-get-entities-asn-list_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetEntitiesAsnListArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `offset`.
    pub offset: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-entities-asn-by-ip_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetEntitiesAsnByIpArgs {
    /// Query parameter: `ip`.
    pub ip: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-entities-asn-by-id_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetEntitiesAsnByIdArgs {
    /// Path parameter: `asn`.
    pub asn: String,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-asns-as-set_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetAsnsAsSetArgs {
    /// Path parameter: `asn`.
    pub asn: String,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-asns-rel_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetAsnsRelArgs {
    /// Path parameter: `asn`.
    pub asn: String,
    /// Query parameter: `asn2`.
    pub asn2: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-entities-ip_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetEntitiesIpArgs {
    /// Query parameter: `ip`.
    pub ip: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-entities-locations_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetEntitiesLocationsArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `offset`.
    pub offset: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `region`.
    pub region: Option<String>,
    /// Query parameter: `subregion`.
    pub subregion: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-entities-location-by-alpha2_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetEntitiesLocationByAlpha2Args {
    /// Path parameter: `location`.
    pub location: String,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-geolocations_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetGeolocationsArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `offset`.
    pub offset: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-geolocation-details_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetGeolocationDetailsArgs {
    /// Path parameter: `geo_id`.
    pub geo_id: String,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-summary_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpSummaryArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-timeseries_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTimeseriesArgs {
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `normalization`.
    pub normalization: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-timeseries-group_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTimeseriesGroupArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `normalization`.
    pub normalization: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-ases-by-http-requests_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopAsesByHttpRequestsArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-ases-by-browser-family_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopAsesByBrowserFamilyArgs {
    /// Path parameter: `browser_family`.
    pub browser_family: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-ases-by-device-type_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopAsesByDeviceTypeArgs {
    /// Path parameter: `device_type`.
    pub device_type: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-ases-by-http-protocol_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopAsesByHttpProtocolArgs {
    /// Path parameter: `http_protocol`.
    pub http_protocol: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-ases-by-http-version_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopAsesByHttpVersionArgs {
    /// Path parameter: `http_version`.
    pub http_version: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-ases-by-ip-version_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopAsesByIpVersionArgs {
    /// Path parameter: `ip_version`.
    pub ip_version: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-ases-by-operating-system_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopAsesByOperatingSystemArgs {
    /// Path parameter: `os`.
    pub os: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-ases-by-tls-version_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopAsesByTlsVersionArgs {
    /// Path parameter: `tls_version`.
    pub tls_version: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-locations-by-http-requests_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopLocationsByHttpRequestsArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-locations-by-browser-family_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopLocationsByBrowserFamilyArgs {
    /// Path parameter: `browser_family`.
    pub browser_family: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-locations-by-device-type_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopLocationsByDeviceTypeArgs {
    /// Path parameter: `device_type`.
    pub device_type: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-locations-by-http-protocol_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopLocationsByHttpProtocolArgs {
    /// Path parameter: `http_protocol`.
    pub http_protocol: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-locations-by-http-version_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopLocationsByHttpVersionArgs {
    /// Path parameter: `http_version`.
    pub http_version: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-locations-by-ip-version_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopLocationsByIpVersionArgs {
    /// Path parameter: `ip_version`.
    pub ip_version: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-locations-by-operating-system_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopLocationsByOperatingSystemArgs {
    /// Path parameter: `os`.
    pub os: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `tlsVersion`.
    pub tls_version: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-http-top-locations-by-tls-version_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetHttpTopLocationsByTlsVersionArgs {
    /// Path parameter: `tls_version`.
    pub tls_version: String,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `httpProtocol`.
    pub http_protocol: Option<String>,
    /// Query parameter: `httpVersion`.
    pub http_version: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `os`.
    pub os: Option<String>,
    /// Query parameter: `browserFamily`.
    pub browser_family: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-leaked-credential-checks-summary_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetLeakedCredentialChecksSummaryArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `compromised`.
    pub compromised: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-leaked-credential-checks-timeseries-group_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetLeakedCredentialChecksTimeseriesGroupArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `botClass`.
    pub bot_class: Option<String>,
    /// Query parameter: `compromised`.
    pub compromised: Option<String>,
    /// Query parameter: `checkResult`.
    pub check_result: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `normalization`.
    pub normalization: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-netflows-summary_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetNetflowsSummaryArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `product`.
    pub product: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-netflows-timeseries_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetNetflowsTimeseriesArgs {
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `product`.
    pub product: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `normalization`.
    pub normalization: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-netflows-timeseries-group_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetNetflowsTimeseriesGroupArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `normalization`.
    pub normalization: Option<String>,
    /// Query parameter: `product`.
    pub product: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-netflows-top-ases_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetNetflowsTopAsesArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-netflows-top-locations_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetNetflowsTopLocationsArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `geoId`.
    pub geo_id: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-origins_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetOriginsArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `offset`.
    pub offset: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-origins-summary_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetOriginsSummaryArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `origin`.
    pub origin: Option<String>,
    /// Query parameter: `metric`.
    pub metric: Option<String>,
    /// Query parameter: `region`.
    pub region: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-origins-timeseries_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetOriginsTimeseriesArgs {
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `origin`.
    pub origin: Option<String>,
    /// Query parameter: `metric`.
    pub metric: Option<String>,
    /// Query parameter: `region`.
    pub region: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-origins-timeseries-group_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetOriginsTimeseriesGroupArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `origin`.
    pub origin: Option<String>,
    /// Query parameter: `metric`.
    pub metric: Option<String>,
    /// Query parameter: `region`.
    pub region: Option<String>,
    /// Query parameter: `normalization`.
    pub normalization: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-origin-details_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetOriginDetailsArgs {
    /// Path parameter: `slug`.
    pub slug: String,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-origin-post-quantum-summary_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetOriginPostQuantumSummaryArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-origin-post-quantum-timeseries-groups_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetOriginPostQuantumTimeseriesGroupsArgs {
    /// Path parameter: `dimension`.
    pub dimension: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-post-quantum-tls-support_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetPostQuantumTlsSupportArgs {
    /// Query parameter: `host`.
    pub host: Option<String>,
}

/// Arguments for [`radar-get-quality-speed-histogram_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetQualitySpeedHistogramArgs {
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `bucketSize`.
    pub bucket_size: Option<String>,
    /// Query parameter: `metricGroup`.
    pub metric_group: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-quality-speed-summary_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetQualitySpeedSummaryArgs {
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-quality-speed-top-ases_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetQualitySpeedTopAsesArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `reverse`.
    pub reverse: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-quality-speed-top-locations_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetQualitySpeedTopLocationsArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `reverse`.
    pub reverse: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-ranking-internet-services-categories_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetRankingInternetServicesCategoriesArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `date`.
    pub date: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-ranking-internet-services-timeseries_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetRankingInternetServicesTimeseriesArgs {
    /// Query parameter: `serviceCategory`.
    pub service_category: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-ranking-top-internet-services_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetRankingTopInternetServicesArgs {
    /// Query parameter: `serviceCategory`.
    pub service_category: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `date`.
    pub date: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-search-global_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetSearchGlobalArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `limitPerGroup`.
    pub limit_per_group: Option<String>,
    /// Query parameter: `query`.
    pub query: Option<String>,
    /// Query parameter: `include`.
    pub include: Option<String>,
    /// Query parameter: `exclude`.
    pub exclude: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-tcp-resets-timeouts-summary_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetTcpResetsTimeoutsSummaryArgs {
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-tcp-resets-timeouts-timeseries-group_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetTcpResetsTimeoutsTimeseriesGroupArgs {
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-traffic-anomalies_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetTrafficAnomaliesArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `offset`.
    pub offset: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `status`.
    pub status: Option<String>,
    /// Query parameter: `type`.
    pub r#type: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `origin`.
    pub origin: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-traffic-anomalies-top_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetTrafficAnomaliesTopArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `status`.
    pub status: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /radar/annotations
// -----------------------------------------------------------------------------

/// GET /radar/annotations.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_annotations_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_annotations_request<F>(
    client: DynNetClient,
    args: &RadarGetAnnotationsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/annotations",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("offset", args.offset.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("dataSource", args.data_source.as_deref());
    builder = builder.query("eventType", args.event_type.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("origin", args.origin.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/annotations/outages
// -----------------------------------------------------------------------------

/// GET /radar/annotations/outages.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_annotations_outages_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_annotations_outages_request<F>(
    client: DynNetClient,
    args: &RadarGetAnnotationsOutagesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/annotations/outages",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("offset", args.offset.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("origin", args.origin.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/annotations/outages/locations
// -----------------------------------------------------------------------------

/// GET /radar/annotations/outages/locations.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_annotations_outages_top_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_annotations_outages_top_request<F>(
    client: DynNetClient,
    args: &RadarGetAnnotationsOutagesTopArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/annotations/outages/locations",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/as112/summary/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/as112/summary/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_dns_as112_summary_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_dns_as112_summary_request<F>(
    client: DynNetClient,
    args: &RadarGetDnsAs112SummaryArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/as112/summary/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("queryType", args.query_type.as_deref());
    builder = builder.query("protocol", args.protocol.as_deref());
    builder = builder.query("responseCode", args.response_code.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/as112/timeseries
// -----------------------------------------------------------------------------

/// GET /radar/as112/timeseries.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_dns_as112_timeseries_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_dns_as112_timeseries_request<F>(
    client: DynNetClient,
    args: &RadarGetDnsAs112TimeseriesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/as112/timeseries",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("queryType", args.query_type.as_deref());
    builder = builder.query("protocol", args.protocol.as_deref());
    builder = builder.query("responseCode", args.response_code.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/as112/timeseries_groups/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/as112/timeseries_groups/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_dns_as112_timeseries_group_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_dns_as112_timeseries_group_request<F>(
    client: DynNetClient,
    args: &RadarGetDnsAs112TimeseriesGroupArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/as112/timeseries_groups/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("queryType", args.query_type.as_deref());
    builder = builder.query("protocol", args.protocol.as_deref());
    builder = builder.query("responseCode", args.response_code.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/as112/top/locations
// -----------------------------------------------------------------------------

/// GET /radar/as112/top/locations.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_dns_as112_top_locations_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_dns_as112_top_locations_request<F>(
    client: DynNetClient,
    args: &RadarGetDnsAs112TopLocationsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/as112/top/locations",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/as112/top/locations/dnssec/{dnssec}
// -----------------------------------------------------------------------------

/// GET /radar/as112/top/locations/dnssec/{dnssec}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_dns_as112_top_locations_by_dnssec_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_dns_as112_top_locations_by_dnssec_request<F>(
    client: DynNetClient,
    args: &RadarGetDnsAs112TopLocationsByDnssecArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/as112/top/locations/dnssec/{}",
        args.dnssec,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/as112/top/locations/edns/{edns}
// -----------------------------------------------------------------------------

/// GET /radar/as112/top/locations/edns/{edns}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_dns_as112_top_locations_by_edns_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_dns_as112_top_locations_by_edns_request<F>(
    client: DynNetClient,
    args: &RadarGetDnsAs112TopLocationsByEdnsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/as112/top/locations/edns/{}",
        args.edns,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/as112/top/locations/ip_version/{ip_version}
// -----------------------------------------------------------------------------

/// GET /radar/as112/top/locations/ip_version/{ip_version}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_dns_as112_top_locations_by_ip_version_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_dns_as112_top_locations_by_ip_version_request<F>(
    client: DynNetClient,
    args: &RadarGetDnsAs112TopLocationsByIpVersionArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/as112/top/locations/ip_version/{}",
        args.ip_version,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/ct/authorities
// -----------------------------------------------------------------------------

/// GET /radar/ct/authorities.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_certificate_authorities_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_certificate_authorities_request<F>(
    client: DynNetClient,
    args: &RadarGetCertificateAuthoritiesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/ct/authorities",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("offset", args.offset.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/ct/authorities/{ca_slug}
// -----------------------------------------------------------------------------

/// GET /radar/ct/authorities/{ca_slug}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_certificate_authority_details_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_certificate_authority_details_request<F>(
    client: DynNetClient,
    args: &RadarGetCertificateAuthorityDetailsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/ct/authorities/{}",
        args.ca_slug,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/ct/logs
// -----------------------------------------------------------------------------

/// GET /radar/ct/logs.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_certificate_logs_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_certificate_logs_request<F>(
    client: DynNetClient,
    args: &RadarGetCertificateLogsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/ct/logs",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("offset", args.offset.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/ct/logs/{log_slug}
// -----------------------------------------------------------------------------

/// GET /radar/ct/logs/{log_slug}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_certificate_log_details_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_certificate_log_details_request<F>(
    client: DynNetClient,
    args: &RadarGetCertificateLogDetailsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/ct/logs/{}",
        args.log_slug,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/ct/summary/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/ct/summary/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_ct_summary_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_ct_summary_request<F>(
    client: DynNetClient,
    args: &RadarGetCtSummaryArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/ct/summary/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("ca", args.ca.as_deref());
    builder = builder.query("caOwner", args.ca_owner.as_deref());
    builder = builder.query("duration", args.duration.as_deref());
    builder = builder.query("entryType", args.entry_type.as_deref());
    builder = builder.query("expirationStatus", args.expiration_status.as_deref());
    builder = builder.query("hasIps", args.has_ips.as_deref());
    builder = builder.query("hasWildcards", args.has_wildcards.as_deref());
    builder = builder.query("log", args.log.as_deref());
    builder = builder.query("logApi", args.log_api.as_deref());
    builder = builder.query("logOperator", args.log_operator.as_deref());
    builder = builder.query("publicKeyAlgorithm", args.public_key_algorithm.as_deref());
    builder = builder.query("signatureAlgorithm", args.signature_algorithm.as_deref());
    builder = builder.query("tld", args.tld.as_deref());
    builder = builder.query("validationLevel", args.validation_level.as_deref());
    builder = builder.query("uniqueEntries", args.unique_entries.as_deref());
    builder = builder.query("normalization", args.normalization.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/ct/timeseries
// -----------------------------------------------------------------------------

/// GET /radar/ct/timeseries.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_ct_timeseries_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_ct_timeseries_request<F>(
    client: DynNetClient,
    args: &RadarGetCtTimeseriesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/ct/timeseries",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("ca", args.ca.as_deref());
    builder = builder.query("caOwner", args.ca_owner.as_deref());
    builder = builder.query("duration", args.duration.as_deref());
    builder = builder.query("entryType", args.entry_type.as_deref());
    builder = builder.query("expirationStatus", args.expiration_status.as_deref());
    builder = builder.query("hasIps", args.has_ips.as_deref());
    builder = builder.query("hasWildcards", args.has_wildcards.as_deref());
    builder = builder.query("log", args.log.as_deref());
    builder = builder.query("logApi", args.log_api.as_deref());
    builder = builder.query("logOperator", args.log_operator.as_deref());
    builder = builder.query("publicKeyAlgorithm", args.public_key_algorithm.as_deref());
    builder = builder.query("signatureAlgorithm", args.signature_algorithm.as_deref());
    builder = builder.query("tld", args.tld.as_deref());
    builder = builder.query("validationLevel", args.validation_level.as_deref());
    builder = builder.query("uniqueEntries", args.unique_entries.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/ct/timeseries_groups/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/ct/timeseries_groups/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_ct_timeseries_group_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_ct_timeseries_group_request<F>(
    client: DynNetClient,
    args: &RadarGetCtTimeseriesGroupArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/ct/timeseries_groups/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("ca", args.ca.as_deref());
    builder = builder.query("caOwner", args.ca_owner.as_deref());
    builder = builder.query("duration", args.duration.as_deref());
    builder = builder.query("entryType", args.entry_type.as_deref());
    builder = builder.query("expirationStatus", args.expiration_status.as_deref());
    builder = builder.query("hasIps", args.has_ips.as_deref());
    builder = builder.query("hasWildcards", args.has_wildcards.as_deref());
    builder = builder.query("log", args.log.as_deref());
    builder = builder.query("logApi", args.log_api.as_deref());
    builder = builder.query("logOperator", args.log_operator.as_deref());
    builder = builder.query("publicKeyAlgorithm", args.public_key_algorithm.as_deref());
    builder = builder.query("signatureAlgorithm", args.signature_algorithm.as_deref());
    builder = builder.query("validationLevel", args.validation_level.as_deref());
    builder = builder.query("tld", args.tld.as_deref());
    builder = builder.query("normalization", args.normalization.as_deref());
    builder = builder.query("uniqueEntries", args.unique_entries.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/datasets
// -----------------------------------------------------------------------------

/// GET /radar/datasets.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_reports_datasets_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_reports_datasets_request<F>(
    client: DynNetClient,
    args: &RadarGetReportsDatasetsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/datasets",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("offset", args.offset.as_deref());
    builder = builder.query("datasetType", args.dataset_type.as_deref());
    builder = builder.query("date", args.date.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// POST /radar/datasets/download
// -----------------------------------------------------------------------------

/// POST /radar/datasets/download.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_post_reports_dataset_download_url_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_post_reports_dataset_download_url_request<F>(
    client: DynNetClient,
    args: &RadarPostReportsDatasetDownloadUrlArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/datasets/download",
    );

    let mut builder = PreparedRequestBuilder::post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/datasets/{alias}
// -----------------------------------------------------------------------------

/// GET /radar/datasets/{alias}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_reports_dataset_download_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_reports_dataset_download_request<F>(
    client: DynNetClient,
    args: &RadarGetReportsDatasetDownloadArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/datasets/{}",
        args.alias,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/entities/asns
// -----------------------------------------------------------------------------

/// GET /radar/entities/asns.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_entities_asn_list_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_entities_asn_list_request<F>(
    client: DynNetClient,
    args: &RadarGetEntitiesAsnListArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/entities/asns",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("offset", args.offset.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("orderBy", args.order_by.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/entities/asns/ip
// -----------------------------------------------------------------------------

/// GET /radar/entities/asns/ip.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_entities_asn_by_ip_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_entities_asn_by_ip_request<F>(
    client: DynNetClient,
    args: &RadarGetEntitiesAsnByIpArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/entities/asns/ip",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("ip", args.ip.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/entities/asns/{asn}
// -----------------------------------------------------------------------------

/// GET /radar/entities/asns/{asn}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_entities_asn_by_id_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_entities_asn_by_id_request<F>(
    client: DynNetClient,
    args: &RadarGetEntitiesAsnByIdArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/entities/asns/{}",
        args.asn,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/entities/asns/{asn}/as_set
// -----------------------------------------------------------------------------

/// GET /radar/entities/asns/{asn}/as_set.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_asns_as_set_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_asns_as_set_request<F>(
    client: DynNetClient,
    args: &RadarGetAsnsAsSetArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/entities/asns/{}/as_set",
        args.asn,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/entities/asns/{asn}/rel
// -----------------------------------------------------------------------------

/// GET /radar/entities/asns/{asn}/rel.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_asns_rel_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_asns_rel_request<F>(
    client: DynNetClient,
    args: &RadarGetAsnsRelArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/entities/asns/{}/rel",
        args.asn,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("asn2", args.asn2.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/entities/ip
// -----------------------------------------------------------------------------

/// GET /radar/entities/ip.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_entities_ip_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_entities_ip_request<F>(
    client: DynNetClient,
    args: &RadarGetEntitiesIpArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/entities/ip",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("ip", args.ip.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/entities/locations
// -----------------------------------------------------------------------------

/// GET /radar/entities/locations.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_entities_locations_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_entities_locations_request<F>(
    client: DynNetClient,
    args: &RadarGetEntitiesLocationsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/entities/locations",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("offset", args.offset.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("region", args.region.as_deref());
    builder = builder.query("subregion", args.subregion.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/entities/locations/{location}
// -----------------------------------------------------------------------------

/// GET /radar/entities/locations/{location}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_entities_location_by_alpha2_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_entities_location_by_alpha2_request<F>(
    client: DynNetClient,
    args: &RadarGetEntitiesLocationByAlpha2Args,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/entities/locations/{}",
        args.location,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/geolocations
// -----------------------------------------------------------------------------

/// GET /radar/geolocations.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_geolocations_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_geolocations_request<F>(
    client: DynNetClient,
    args: &RadarGetGeolocationsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/geolocations",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("offset", args.offset.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/geolocations/{geo_id}
// -----------------------------------------------------------------------------

/// GET /radar/geolocations/{geo_id}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_geolocation_details_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_geolocation_details_request<F>(
    client: DynNetClient,
    args: &RadarGetGeolocationDetailsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/geolocations/{}",
        args.geo_id,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/summary/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/http/summary/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_summary_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_summary_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpSummaryArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/summary/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/timeseries
// -----------------------------------------------------------------------------

/// GET /radar/http/timeseries.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_timeseries_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_timeseries_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTimeseriesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/timeseries",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("normalization", args.normalization.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/timeseries_groups/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/http/timeseries_groups/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_timeseries_group_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_timeseries_group_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTimeseriesGroupArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/timeseries_groups/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("normalization", args.normalization.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/ases
// -----------------------------------------------------------------------------

/// GET /radar/http/top/ases.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_ases_by_http_requests_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_ases_by_http_requests_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopAsesByHttpRequestsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/ases",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/ases/browser_family/{browser_family}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/ases/browser_family/{browser_family}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_ases_by_browser_family_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_ases_by_browser_family_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopAsesByBrowserFamilyArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/ases/browser_family/{}",
        args.browser_family,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/ases/device_type/{device_type}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/ases/device_type/{device_type}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_ases_by_device_type_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_ases_by_device_type_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopAsesByDeviceTypeArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/ases/device_type/{}",
        args.device_type,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/ases/http_protocol/{http_protocol}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/ases/http_protocol/{http_protocol}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_ases_by_http_protocol_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_ases_by_http_protocol_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopAsesByHttpProtocolArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/ases/http_protocol/{}",
        args.http_protocol,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/ases/http_version/{http_version}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/ases/http_version/{http_version}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_ases_by_http_version_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_ases_by_http_version_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopAsesByHttpVersionArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/ases/http_version/{}",
        args.http_version,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/ases/ip_version/{ip_version}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/ases/ip_version/{ip_version}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_ases_by_ip_version_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_ases_by_ip_version_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopAsesByIpVersionArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/ases/ip_version/{}",
        args.ip_version,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/ases/os/{os}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/ases/os/{os}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_ases_by_operating_system_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_ases_by_operating_system_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopAsesByOperatingSystemArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/ases/os/{}",
        args.os,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/ases/tls_version/{tls_version}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/ases/tls_version/{tls_version}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_ases_by_tls_version_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_ases_by_tls_version_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopAsesByTlsVersionArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/ases/tls_version/{}",
        args.tls_version,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/locations
// -----------------------------------------------------------------------------

/// GET /radar/http/top/locations.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_locations_by_http_requests_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_locations_by_http_requests_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopLocationsByHttpRequestsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/locations",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/locations/browser_family/{browser_family}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/locations/browser_family/{browser_family}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_locations_by_browser_family_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_locations_by_browser_family_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopLocationsByBrowserFamilyArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/locations/browser_family/{}",
        args.browser_family,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/locations/device_type/{device_type}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/locations/device_type/{device_type}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_locations_by_device_type_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_locations_by_device_type_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopLocationsByDeviceTypeArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/locations/device_type/{}",
        args.device_type,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/locations/http_protocol/{http_protocol}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/locations/http_protocol/{http_protocol}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_locations_by_http_protocol_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_locations_by_http_protocol_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopLocationsByHttpProtocolArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/locations/http_protocol/{}",
        args.http_protocol,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/locations/http_version/{http_version}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/locations/http_version/{http_version}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_locations_by_http_version_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_locations_by_http_version_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopLocationsByHttpVersionArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/locations/http_version/{}",
        args.http_version,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/locations/ip_version/{ip_version}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/locations/ip_version/{ip_version}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_locations_by_ip_version_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_locations_by_ip_version_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopLocationsByIpVersionArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/locations/ip_version/{}",
        args.ip_version,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/locations/os/{os}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/locations/os/{os}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_locations_by_operating_system_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_locations_by_operating_system_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopLocationsByOperatingSystemArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/locations/os/{}",
        args.os,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("tlsVersion", args.tls_version.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/http/top/locations/tls_version/{tls_version}
// -----------------------------------------------------------------------------

/// GET /radar/http/top/locations/tls_version/{tls_version}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_http_top_locations_by_tls_version_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_http_top_locations_by_tls_version_request<F>(
    client: DynNetClient,
    args: &RadarGetHttpTopLocationsByTlsVersionArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/http/top/locations/tls_version/{}",
        args.tls_version,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("deviceType", args.device_type.as_deref());
    builder = builder.query("httpProtocol", args.http_protocol.as_deref());
    builder = builder.query("httpVersion", args.http_version.as_deref());
    builder = builder.query("ipVersion", args.ip_version.as_deref());
    builder = builder.query("os", args.os.as_deref());
    builder = builder.query("browserFamily", args.browser_family.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/leaked_credential_checks/summary/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/leaked_credential_checks/summary/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_leaked_credential_checks_summary_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_leaked_credential_checks_summary_request<F>(
    client: DynNetClient,
    args: &RadarGetLeakedCredentialChecksSummaryArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/leaked_credential_checks/summary/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("compromised", args.compromised.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/leaked_credential_checks/timeseries_groups/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/leaked_credential_checks/timeseries_groups/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_leaked_credential_checks_timeseries_group_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_leaked_credential_checks_timeseries_group_request<F>(
    client: DynNetClient,
    args: &RadarGetLeakedCredentialChecksTimeseriesGroupArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/leaked_credential_checks/timeseries_groups/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("botClass", args.bot_class.as_deref());
    builder = builder.query("compromised", args.compromised.as_deref());
    builder = builder.query("checkResult", args.check_result.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("normalization", args.normalization.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/netflows/summary/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/netflows/summary/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_netflows_summary_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_netflows_summary_request<F>(
    client: DynNetClient,
    args: &RadarGetNetflowsSummaryArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/netflows/summary/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("product", args.product.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/netflows/timeseries
// -----------------------------------------------------------------------------

/// GET /radar/netflows/timeseries.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_netflows_timeseries_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_netflows_timeseries_request<F>(
    client: DynNetClient,
    args: &RadarGetNetflowsTimeseriesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/netflows/timeseries",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("product", args.product.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("normalization", args.normalization.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/netflows/timeseries_groups/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/netflows/timeseries_groups/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_netflows_timeseries_group_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_netflows_timeseries_group_request<F>(
    client: DynNetClient,
    args: &RadarGetNetflowsTimeseriesGroupArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/netflows/timeseries_groups/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("normalization", args.normalization.as_deref());
    builder = builder.query("product", args.product.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/netflows/top/ases
// -----------------------------------------------------------------------------

/// GET /radar/netflows/top/ases.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_netflows_top_ases_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_netflows_top_ases_request<F>(
    client: DynNetClient,
    args: &RadarGetNetflowsTopAsesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/netflows/top/ases",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/netflows/top/locations
// -----------------------------------------------------------------------------

/// GET /radar/netflows/top/locations.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_netflows_top_locations_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_netflows_top_locations_request<F>(
    client: DynNetClient,
    args: &RadarGetNetflowsTopLocationsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/netflows/top/locations",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("geoId", args.geo_id.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/origins
// -----------------------------------------------------------------------------

/// GET /radar/origins.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_origins_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_origins_request<F>(
    client: DynNetClient,
    args: &RadarGetOriginsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/origins",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("offset", args.offset.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/origins/summary/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/origins/summary/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_origins_summary_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_origins_summary_request<F>(
    client: DynNetClient,
    args: &RadarGetOriginsSummaryArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/origins/summary/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("origin", args.origin.as_deref());
    builder = builder.query("metric", args.metric.as_deref());
    builder = builder.query("region", args.region.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/origins/timeseries
// -----------------------------------------------------------------------------

/// GET /radar/origins/timeseries.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_origins_timeseries_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_origins_timeseries_request<F>(
    client: DynNetClient,
    args: &RadarGetOriginsTimeseriesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/origins/timeseries",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("origin", args.origin.as_deref());
    builder = builder.query("metric", args.metric.as_deref());
    builder = builder.query("region", args.region.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/origins/timeseries_groups/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/origins/timeseries_groups/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_origins_timeseries_group_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_origins_timeseries_group_request<F>(
    client: DynNetClient,
    args: &RadarGetOriginsTimeseriesGroupArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/origins/timeseries_groups/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("origin", args.origin.as_deref());
    builder = builder.query("metric", args.metric.as_deref());
    builder = builder.query("region", args.region.as_deref());
    builder = builder.query("normalization", args.normalization.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/origins/{slug}
// -----------------------------------------------------------------------------

/// GET /radar/origins/{slug}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_origin_details_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_origin_details_request<F>(
    client: DynNetClient,
    args: &RadarGetOriginDetailsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/origins/{}",
        args.slug,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/post_quantum/origin/summary/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/post_quantum/origin/summary/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_origin_post_quantum_summary_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_origin_post_quantum_summary_request<F>(
    client: DynNetClient,
    args: &RadarGetOriginPostQuantumSummaryArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/post_quantum/origin/summary/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/post_quantum/origin/timeseries_groups/{dimension}
// -----------------------------------------------------------------------------

/// GET /radar/post_quantum/origin/timeseries_groups/{dimension}.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_origin_post_quantum_timeseries_groups_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_origin_post_quantum_timeseries_groups_request<F>(
    client: DynNetClient,
    args: &RadarGetOriginPostQuantumTimeseriesGroupsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/post_quantum/origin/timeseries_groups/{}",
        args.dimension,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/post_quantum/tls/support
// -----------------------------------------------------------------------------

/// GET /radar/post_quantum/tls/support.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_post_quantum_tls_support_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_post_quantum_tls_support_request<F>(
    client: DynNetClient,
    args: &RadarGetPostQuantumTlsSupportArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/post_quantum/tls/support",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("host", args.host.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/quality/speed/histogram
// -----------------------------------------------------------------------------

/// GET /radar/quality/speed/histogram.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_quality_speed_histogram_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_quality_speed_histogram_request<F>(
    client: DynNetClient,
    args: &RadarGetQualitySpeedHistogramArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/quality/speed/histogram",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("bucketSize", args.bucket_size.as_deref());
    builder = builder.query("metricGroup", args.metric_group.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/quality/speed/summary
// -----------------------------------------------------------------------------

/// GET /radar/quality/speed/summary.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_quality_speed_summary_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_quality_speed_summary_request<F>(
    client: DynNetClient,
    args: &RadarGetQualitySpeedSummaryArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/quality/speed/summary",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/quality/speed/top/ases
// -----------------------------------------------------------------------------

/// GET /radar/quality/speed/top/ases.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_quality_speed_top_ases_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_quality_speed_top_ases_request<F>(
    client: DynNetClient,
    args: &RadarGetQualitySpeedTopAsesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/quality/speed/top/ases",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("orderBy", args.order_by.as_deref());
    builder = builder.query("reverse", args.reverse.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/quality/speed/top/locations
// -----------------------------------------------------------------------------

/// GET /radar/quality/speed/top/locations.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_quality_speed_top_locations_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_quality_speed_top_locations_request<F>(
    client: DynNetClient,
    args: &RadarGetQualitySpeedTopLocationsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/quality/speed/top/locations",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("orderBy", args.order_by.as_deref());
    builder = builder.query("reverse", args.reverse.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/ranking/internet_services/categories
// -----------------------------------------------------------------------------

/// GET /radar/ranking/internet_services/categories.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_ranking_internet_services_categories_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_ranking_internet_services_categories_request<F>(
    client: DynNetClient,
    args: &RadarGetRankingInternetServicesCategoriesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/ranking/internet_services/categories",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("date", args.date.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/ranking/internet_services/timeseries_groups
// -----------------------------------------------------------------------------

/// GET /radar/ranking/internet_services/timeseries_groups.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_ranking_internet_services_timeseries_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_ranking_internet_services_timeseries_request<F>(
    client: DynNetClient,
    args: &RadarGetRankingInternetServicesTimeseriesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/ranking/internet_services/timeseries_groups",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("serviceCategory", args.service_category.as_deref());
    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/ranking/internet_services/top
// -----------------------------------------------------------------------------

/// GET /radar/ranking/internet_services/top.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_ranking_top_internet_services_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_ranking_top_internet_services_request<F>(
    client: DynNetClient,
    args: &RadarGetRankingTopInternetServicesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/ranking/internet_services/top",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("serviceCategory", args.service_category.as_deref());
    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("date", args.date.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/search/global
// -----------------------------------------------------------------------------

/// GET /radar/search/global.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_search_global_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_search_global_request<F>(
    client: DynNetClient,
    args: &RadarGetSearchGlobalArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/search/global",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("limitPerGroup", args.limit_per_group.as_deref());
    builder = builder.query("query", args.query.as_deref());
    builder = builder.query("include", args.include.as_deref());
    builder = builder.query("exclude", args.exclude.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/tcp_resets_timeouts/summary
// -----------------------------------------------------------------------------

/// GET /radar/tcp_resets_timeouts/summary.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_tcp_resets_timeouts_summary_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_tcp_resets_timeouts_summary_request<F>(
    client: DynNetClient,
    args: &RadarGetTcpResetsTimeoutsSummaryArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/tcp_resets_timeouts/summary",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/tcp_resets_timeouts/timeseries_groups
// -----------------------------------------------------------------------------

/// GET /radar/tcp_resets_timeouts/timeseries_groups.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_tcp_resets_timeouts_timeseries_group_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_tcp_resets_timeouts_timeseries_group_request<F>(
    client: DynNetClient,
    args: &RadarGetTcpResetsTimeoutsTimeseriesGroupArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/tcp_resets_timeouts/timeseries_groups",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("aggInterval", args.agg_interval.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("continent", args.continent.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/traffic_anomalies
// -----------------------------------------------------------------------------

/// GET /radar/traffic_anomalies.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_traffic_anomalies_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_traffic_anomalies_request<F>(
    client: DynNetClient,
    args: &RadarGetTrafficAnomaliesArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/traffic_anomalies",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("offset", args.offset.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("status", args.status.as_deref());
    builder = builder.query("type", args.r#type.as_deref());
    builder = builder.query("asn", args.asn.as_deref());
    builder = builder.query("location", args.location.as_deref());
    builder = builder.query("origin", args.origin.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /radar/traffic_anomalies/locations
// -----------------------------------------------------------------------------

/// GET /radar/traffic_anomalies/locations.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = radar_get_traffic_anomalies_top_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn radar_get_traffic_anomalies_top_request<F>(
    client: DynNetClient,
    args: &RadarGetTrafficAnomaliesTopArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/radar/traffic_anomalies/locations",
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("limit", args.limit.as_deref());
    builder = builder.query("dateRange", args.date_range.as_deref());
    builder = builder.query("dateStart", args.date_start.as_deref());
    builder = builder.query("dateEnd", args.date_end.as_deref());
    builder = builder.query("status", args.status.as_deref());
    builder = builder.query("format", args.format.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

