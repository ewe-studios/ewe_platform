//! Auto-generated API module for cloudflare network.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `cloudflare_network `

#![cfg(feature = "cloudflare_network")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_macros::JsonHash;
use foundation_netio::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::AddressingApiResponseCommon;
use super::shared::TunnelSubnetResponseSingle;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `MagicComponentsSchemasTunnelsCollectionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicComponentsSchemasTunnelsCollectionResponse {
    /// `result` property.
    pub result: Option<serde_json::Value>,
}

/// `AddressingResponseCollectionBgp` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AddressingResponseCollectionBgp {
    /// `result` property.
    pub result: Option<Vec<AddressingIpamBgpPrefixes>>,
}

/// `DigitalExperienceMonitoringDexSingleResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DigitalExperienceMonitoringDexSingleResponse {
    /// `result` property.
    pub result: Option<DigitalExperienceMonitoringDeviceDexTestSchemasHttp>,
}

/// `MagicTunnelModifiedResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicTunnelModifiedResponse {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `MagicSchemasModifiedTunnelsCollectionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicSchemasModifiedTunnelsCollectionResponse {
    /// `result` property.
    pub result: Option<serde_json::Value>,
}

/// `MagicSchemasTunnelModifiedResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicSchemasTunnelModifiedResponse {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `MagicSchemasTunnelDeletedResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicSchemasTunnelDeletedResponse {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TunnelRouteResponseSingle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelRouteResponseSingle {
    /// `result` property.
    pub result: Option<TunnelRoute>,
}

/// `DigitalExperienceMonitoringFleetStatusLiveResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DigitalExperienceMonitoringFleetStatusLiveResponse {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `NscSlotList` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NscSlotList {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `TunnelVnetResponseSingle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelVnetResponseSingle {
    /// `result` property.
    pub result: Option<TunnelVirtualNetwork>,
}

/// `TunnelZeroTrustConnectivitySettingsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelZeroTrustConnectivitySettingsResponse {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `MagicCreateGreTunnelResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicCreateGreTunnelResponse {
    /// `result` property.
    pub result: Option<MagicGreTunnel>,
}

/// `TunnelTeamnetResponseSingle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelTeamnetResponseSingle {
    /// `result` property.
    pub result: Option<TunnelTeamnet>,
}

/// `DigitalExperienceMonitoringDexResponseCollection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DigitalExperienceMonitoringDexResponseCollection {
    /// `result` property.
    pub result: Option<Vec<DigitalExperienceMonitoringDeviceDexTestSchemasHttp>>,
}

/// `MagicTunnelsCollectionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicTunnelsCollectionResponse {
    /// `result` property.
    pub result: Option<serde_json::Value>,
}

/// `NscStatusInfo` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NscStatusInfo {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `DigitalExperienceMonitoringDevice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DigitalExperienceMonitoringDevice {
    /// alwaysOn property.
    pub always_on: Option<bool>,
    /// batteryCharging property.
    pub battery_charging: Option<bool>,
    /// batteryCycles property.
    pub battery_cycles: Option<i64>,
    /// batteryPct property.
    pub battery_pct: Option<f64>,
    /// colo property.
    pub colo: DigitalExperienceMonitoringColo,
    /// connectionType property.
    pub connection_type: Option<String>,
    /// cpuPct property.
    pub cpu_pct: Option<f64>,
    /// cpuPctByApp property.
    pub cpu_pct_by_app: Option<Vec<DigitalExperienceMonitoringCpuPctByApp>>,
    /// deviceId property.
    pub device_id: String,
    /// deviceIpv4 property.
    pub device_ipv4: Option<DigitalExperienceMonitoringIpInfo>,
    /// deviceIpv6 property.
    pub device_ipv6: Option<DigitalExperienceMonitoringIpInfo>,
    /// deviceName property.
    pub device_name: Option<String>,
    /// diskReadBps property.
    pub disk_read_bps: Option<i64>,
    /// diskUsagePct property.
    pub disk_usage_pct: Option<f64>,
    /// diskWriteBps property.
    pub disk_write_bps: Option<i64>,
    /// dohSubdomain property.
    pub doh_subdomain: Option<String>,
    /// estimatedLossPct property.
    pub estimated_loss_pct: Option<f64>,
    /// firewallEnabled property.
    pub firewall_enabled: Option<bool>,
    /// gatewayIpv4 property.
    pub gateway_ipv4: Option<DigitalExperienceMonitoringIpInfo>,
    /// gatewayIpv6 property.
    pub gateway_ipv6: Option<DigitalExperienceMonitoringIpInfo>,
    /// handshakeLatencyMs property.
    pub handshake_latency_ms: Option<f64>,
    /// ispIpv4 property.
    pub isp_ipv4: Option<DigitalExperienceMonitoringIpInfo>,
    /// ispIpv6 property.
    pub isp_ipv6: Option<DigitalExperienceMonitoringIpInfo>,
    /// metal property.
    pub metal: Option<String>,
    /// mode property.
    pub mode: DigitalExperienceMonitoringMode,
    /// networkRcvdBps property.
    pub network_rcvd_bps: Option<i64>,
    /// networkSentBps property.
    pub network_sent_bps: Option<i64>,
    /// networkSsid property.
    pub network_ssid: Option<String>,
    /// personEmail property.
    pub person_email: Option<DigitalExperienceMonitoringPersonEmail>,
    /// platform property.
    pub platform: DigitalExperienceMonitoringPlatform,
    /// ramAvailableKb property.
    pub ram_available_kb: Option<i64>,
    /// ramUsedPct property.
    pub ram_used_pct: Option<f64>,
    /// ramUsedPctByApp property.
    pub ram_used_pct_by_app: Option<Vec<DigitalExperienceMonitoringRamUsedPctByApp>>,
    /// status property.
    pub status: DigitalExperienceMonitoringStatus,
    /// switchLocked property.
    pub switch_locked: Option<bool>,
    /// timestamp property.
    pub timestamp: DigitalExperienceMonitoringTimestamp,
    /// version property.
    pub version: DigitalExperienceMonitoringVersion,
    /// wifiStrengthDbm property.
    pub wifi_strength_dbm: Option<i64>,
}

/// `MagicComponentsSchemasModifiedTunnelsCollectionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicComponentsSchemasModifiedTunnelsCollectionResponse {
    /// `result` property.
    pub result: Option<serde_json::Value>,
}

/// `TunnelCfdTunnelResponseSingle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelCfdTunnelResponseSingle {
    /// `result` property.
    pub result: Option<TunnelCfdTunnel>,
}

/// `TeamsDevicesGlobalWarpOverrideResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TeamsDevicesGlobalWarpOverrideResponse {
    /// `result` property.
    pub result: Option<TeamsDevicesGlobalWarpOverride>,
}

/// `TunnelEmptyResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelEmptyResponse {
    /// `result` property.
    pub result: Option<serde_json::Value>,
}

/// `TunnelTunnelConnectionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelTunnelConnectionsResponse {
    /// `result` property.
    pub result: Option<Vec<TunnelTunnelClient>>,
}

/// `TunnelTunnelResponseToken` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelTunnelResponseToken {
    /// `result` property.
    pub result: Option<TunnelTunnelToken>,
}

/// `NscSlotInfo` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NscSlotInfo {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `DigitalExperienceMonitoringDexDeleteResponseCollection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DigitalExperienceMonitoringDexDeleteResponseCollection {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TunnelTunnelClientResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelTunnelClientResponse {
    /// `result` property.
    pub result: Option<TunnelTunnelClient>,
}

/// `MagicTunnelDeletedResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicTunnelDeletedResponse {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `MagicSchemasCreateIpsecTunnelResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicSchemasCreateIpsecTunnelResponse {
    /// `result` property.
    pub result: Option<MagicIpsecTunnel>,
}

/// `AddressingSingleResponseBgp` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AddressingSingleResponseBgp {
    /// `result` property.
    pub result: Option<AddressingIpamBgpPrefixes>,
}

/// `NscInterconnectList` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NscInterconnectList {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `MagicSchemasTunnelSingleResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicSchemasTunnelSingleResponse {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TunnelTeamnetResponseCollection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelTeamnetResponseCollection {
    /// `result` property.
    pub result: Option<Vec<TunnelTeamnet>>,
}

/// `TunnelConfigurationResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelConfigurationResponse {
    /// `result` property.
    pub result: Option<TunnelConfiguration>,
}

/// `NscSettings` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NscSettings {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `TunnelWarpConnectorResponseSingle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelWarpConnectorResponseSingle {
    /// `result` property.
    pub result: Option<TunnelWarpConnectorTunnel>,
}

/// `TunnelTunnelWarpConnectorConnectionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelTunnelWarpConnectorConnectionsResponse {
    /// `result` property.
    pub result: Option<Vec<TunnelTunnelWarpConnectorClient>>,
}

/// `NscCni` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NscCni {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `TunnelVnetResponseCollection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelVnetResponseCollection {
    /// `result` property.
    pub result: Option<Vec<TunnelVirtualNetwork>>,
}

/// `TunnelTunnelWarpConnectorClientResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelTunnelWarpConnectorClientResponse {
    /// `result` property.
    pub result: Option<TunnelTunnelWarpConnectorClient>,
}

/// `NscCniList` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NscCniList {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `MagicModifiedTunnelsCollectionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicModifiedTunnelsCollectionResponse {
    /// `result` property.
    pub result: Option<serde_json::Value>,
}

/// `TunnelSubnetResponseSingleNullable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelSubnetResponseSingleNullable {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `MagicPskGenerationResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicPskGenerationResponse {
    /// `result` property.
    pub result: Option<serde_json::Value>,
}

/// `SpectrumAnalyticsQueryResponseAggregate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpectrumAnalyticsQueryResponseAggregate {
    /// `result` property.
    pub result: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

/// `DigitalExperienceMonitoringFleetStatusDevicesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DigitalExperienceMonitoringFleetStatusDevicesResponse {
    /// `result` property.
    pub result: Option<Vec<DigitalExperienceMonitoringDevice>>,
}

/// `MagicSchemasTunnelsCollectionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicSchemasTunnelsCollectionResponse {
    /// `result` property.
    pub result: Option<serde_json::Value>,
}

/// `TunnelWarpConnectorResponseCollection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelWarpConnectorResponseCollection {
    /// `result` property.
    pub result: Option<Vec<TunnelWarpConnectorTunnel>>,
}

/// `NscInterconnect` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NscInterconnect {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `DigitalExperienceMonitoringFleetStatusOverTimeResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DigitalExperienceMonitoringFleetStatusOverTimeResponse {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `TunnelCfdTunnelResponseCollection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TunnelCfdTunnelResponseCollection {
    /// `result` property.
    pub result: Option<Vec<TunnelCfdTunnel>>,
}

/// `MagicComponentsSchemasTunnelSingleResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicComponentsSchemasTunnelSingleResponse {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `MagicComponentsSchemasTunnelModifiedResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicComponentsSchemasTunnelModifiedResponse {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `MagicTunnelSingleResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MagicTunnelSingleResponse {
    /// `result` property.
    pub result: Option<std::collections::HashMap<String, serde_json::Value>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`ip-address-management-prefixes-list-bgp-prefixes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IpAddressManagementPrefixesListBgpPrefixesArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `prefix_id`.
    pub prefix_id: String,
}

/// Arguments for [`ip-address-management-prefixes-create-bgp-prefix_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IpAddressManagementPrefixesCreateBgpPrefixArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `prefix_id`.
    pub prefix_id: String,
}

/// Arguments for [`ip-address-management-prefixes-fetch-bgp-prefix_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IpAddressManagementPrefixesFetchBgpPrefixArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `prefix_id`.
    pub prefix_id: String,
    /// Path parameter: `bgp_prefix_id`.
    pub bgp_prefix_id: String,
}

/// Arguments for [`ip-address-management-prefixes-update-bgp-prefix_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IpAddressManagementPrefixesUpdateBgpPrefixArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `prefix_id`.
    pub prefix_id: String,
    /// Path parameter: `bgp_prefix_id`.
    pub bgp_prefix_id: String,
}

/// Arguments for [`ip-address-management-prefixes-delete-bgp-prefix_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct IpAddressManagementPrefixesDeleteBgpPrefixArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `prefix_id`.
    pub prefix_id: String,
    /// Path parameter: `bgp_prefix_id`.
    pub bgp_prefix_id: String,
}

/// Arguments for [`cloudflare-tunnel-list-cloudflare-tunnels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelListCloudflareTunnelsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `is_deleted`.
    pub is_deleted: Option<String>,
    /// Query parameter: `existed_at`.
    pub existed_at: Option<String>,
    /// Query parameter: `uuid`.
    pub uuid: Option<String>,
    /// Query parameter: `was_active_at`.
    pub was_active_at: Option<String>,
    /// Query parameter: `was_inactive_at`.
    pub was_inactive_at: Option<String>,
    /// Query parameter: `include_prefix`.
    pub include_prefix: Option<String>,
    /// Query parameter: `exclude_prefix`.
    pub exclude_prefix: Option<String>,
    /// Query parameter: `status`.
    pub status: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `page`.
    pub page: Option<String>,
}

/// Arguments for [`cloudflare-tunnel-create-a-cloudflare-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelCreateACloudflareTunnelArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`cloudflare-tunnel-get-a-cloudflare-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelGetACloudflareTunnelArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`cloudflare-tunnel-update-a-cloudflare-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelUpdateACloudflareTunnelArgs {
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`cloudflare-tunnel-delete-a-cloudflare-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelDeleteACloudflareTunnelArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`cloudflare-tunnel-configuration-get-configuration_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelConfigurationGetConfigurationArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`cloudflare-tunnel-configuration-put-configuration_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelConfigurationPutConfigurationArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`cloudflare-tunnel-list-cloudflare-tunnel-connections_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelListCloudflareTunnelConnectionsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`cloudflare-tunnel-clean-up-cloudflare-tunnel-connections_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelCleanUpCloudflareTunnelConnectionsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
    /// Query parameter: `client_id`.
    pub client_id: Option<String>,
}

/// Arguments for [`cloudflare-tunnel-get-cloudflare-tunnel-connector_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelGetCloudflareTunnelConnectorArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
    /// Path parameter: `connector_id`.
    pub connector_id: String,
}

/// Arguments for [`cloudflare-tunnel-get-a-cloudflare-tunnel-management-token_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelGetACloudflareTunnelManagementTokenArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`cloudflare-tunnel-get-a-cloudflare-tunnel-token_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelGetACloudflareTunnelTokenArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`get_EventAggregate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetEventAggregateArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `aggregateBy`.
    pub aggregate_by: Option<String>,
    /// Query parameter: `datasetId`.
    pub dataset_id: Option<String>,
    /// Query parameter: `startDate`.
    pub start_date: Option<String>,
    /// Query parameter: `endDate`.
    pub end_date: Option<String>,
    /// Query parameter: `groupByDate`.
    pub group_by_date: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
}

/// Arguments for [`list_cnis_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ListCnisArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `slot`.
    pub slot: Option<String>,
    /// Query parameter: `tunnel_id`.
    pub tunnel_id: Option<String>,
    /// Query parameter: `cursor`.
    pub cursor: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
}

/// Arguments for [`create_cni_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CreateCniArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`get_cni_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetCniArgs {
    /// Path parameter: `cni`.
    pub cni: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`update_cni_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct UpdateCniArgs {
    /// Path parameter: `cni`.
    pub cni: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`delete_cni_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeleteCniArgs {
    /// Path parameter: `cni`.
    pub cni: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`list_interconnects_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ListInterconnectsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `site`.
    pub site: Option<String>,
    /// Query parameter: `type`.
    pub r#type: Option<String>,
    /// Query parameter: `cursor`.
    pub cursor: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
}

/// Arguments for [`create_interconnect_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CreateInterconnectArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`get_interconnect_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetInterconnectArgs {
    /// Path parameter: `icon`.
    pub icon: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`delete_interconnect_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeleteInterconnectArgs {
    /// Path parameter: `icon`.
    pub icon: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`get_interconnect_loa_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetInterconnectLoaArgs {
    /// Path parameter: `icon`.
    pub icon: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`get_interconnect_status_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetInterconnectStatusArgs {
    /// Path parameter: `icon`.
    pub icon: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`get_settings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetSettingsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`update_settings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct UpdateSettingsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`list_slots_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ListSlotsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `address_contains`.
    pub address_contains: Option<String>,
    /// Query parameter: `site`.
    pub site: Option<String>,
    /// Query parameter: `speed`.
    pub speed: Option<String>,
    /// Query parameter: `occupied`.
    pub occupied: Option<String>,
    /// Query parameter: `cursor`.
    pub cursor: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
}

/// Arguments for [`get_slot_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetSlotArgs {
    /// Path parameter: `slot`.
    pub slot: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`connectivity-services-list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectivityServicesListArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `type`.
    pub r#type: Option<String>,
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
}

/// Arguments for [`connectivity-services-post_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectivityServicesPostArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`connectivity-services-get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectivityServicesGetArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `service_id`.
    pub service_id: String,
}

/// Arguments for [`connectivity-services-put_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectivityServicesPutArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `service_id`.
    pub service_id: String,
}

/// Arguments for [`connectivity-services-delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectivityServicesDeleteArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `service_id`.
    pub service_id: String,
}

/// Arguments for [`devices-resilience-retrieve-global-warp-override_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DevicesResilienceRetrieveGlobalWarpOverrideArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`devices-resilience-set-global-warp-override_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DevicesResilienceSetGlobalWarpOverrideArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`dex-endpoints-list-colos_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexEndpointsListColosArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `from`.
    pub from: Option<String>,
    /// Query parameter: `to`.
    pub to: Option<String>,
    /// Query parameter: `sortBy`.
    pub sort_by: Option<String>,
}

/// Arguments for [`get-commands_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetCommandsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `from`.
    pub from: Option<String>,
    /// Query parameter: `to`.
    pub to: Option<String>,
    /// Query parameter: `device_id`.
    pub device_id: Option<String>,
    /// Query parameter: `user_email`.
    pub user_email: Option<String>,
    /// Query parameter: `command_type`.
    pub command_type: Option<String>,
    /// Query parameter: `status`.
    pub status: Option<String>,
}

/// Arguments for [`post-commands_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PostCommandsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`get-commands-eligible-devices_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetCommandsEligibleDevicesArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `search`.
    pub search: Option<String>,
}

/// Arguments for [`get-commands-quota_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetCommandsQuotaArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`get-commands-command-id-downloads-filename_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetCommandsCommandIdDownloadsFilenameArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `command_id`.
    pub command_id: String,
    /// Path parameter: `filename`.
    pub filename: String,
}

/// Arguments for [`device-dex-test-details_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeviceDexTestDetailsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `testName`.
    pub test_name: Option<String>,
    /// Query parameter: `kind`.
    pub kind: Option<String>,
}

/// Arguments for [`device-dex-test-create-device-dex-test_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeviceDexTestCreateDeviceDexTestArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`device-dex-test-get-device-dex-test_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeviceDexTestGetDeviceDexTestArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `dex_test_id`.
    pub dex_test_id: String,
}

/// Arguments for [`device-dex-test-update-device-dex-test_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeviceDexTestUpdateDeviceDexTestArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `dex_test_id`.
    pub dex_test_id: String,
}

/// Arguments for [`device-dex-test-delete-device-dex-test_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeviceDexTestDeleteDeviceDexTestArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `dex_test_id`.
    pub dex_test_id: String,
}

/// Arguments for [`devices-live-status_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DevicesLiveStatusArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `device_id`.
    pub device_id: String,
    /// Query parameter: `since_minutes`.
    pub since_minutes: Option<String>,
    /// Query parameter: `time_now`.
    pub time_now: Option<String>,
    /// Query parameter: `colo`.
    pub colo: Option<String>,
}

/// Arguments for [`dex-fleet-status-devices_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexFleetStatusDevicesArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `to`.
    pub to: Option<String>,
    /// Query parameter: `from`.
    pub from: Option<String>,
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `sort_by`.
    pub sort_by: Option<String>,
    /// Query parameter: `colo`.
    pub colo: Option<String>,
    /// Query parameter: `device_id`.
    pub device_id: Option<String>,
    /// Query parameter: `mode`.
    pub mode: Option<String>,
    /// Query parameter: `status`.
    pub status: Option<String>,
    /// Query parameter: `platform`.
    pub platform: Option<String>,
    /// Query parameter: `version`.
    pub version: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`dex-fleet-status-live_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexFleetStatusLiveArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `since_minutes`.
    pub since_minutes: Option<String>,
}

/// Arguments for [`dex-fleet-status-over-time_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexFleetStatusOverTimeArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `to`.
    pub to: Option<String>,
    /// Query parameter: `from`.
    pub from: Option<String>,
    /// Query parameter: `colo`.
    pub colo: Option<String>,
    /// Query parameter: `device_id`.
    pub device_id: Option<String>,
}

/// Arguments for [`dex-endpoints-http-test-details_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexEndpointsHttpTestDetailsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `test_id`.
    pub test_id: String,
    /// Query parameter: `deviceId`.
    pub device_id: Option<String>,
    /// Query parameter: `from`.
    pub from: Option<String>,
    /// Query parameter: `to`.
    pub to: Option<String>,
    /// Query parameter: `interval`.
    pub interval: Option<String>,
    /// Query parameter: `colo`.
    pub colo: Option<String>,
}

/// Arguments for [`dex-endpoints-http-test-percentiles_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexEndpointsHttpTestPercentilesArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `test_id`.
    pub test_id: String,
    /// Query parameter: `deviceId`.
    pub device_id: Option<String>,
    /// Query parameter: `from`.
    pub from: Option<String>,
    /// Query parameter: `to`.
    pub to: Option<String>,
    /// Query parameter: `colo`.
    pub colo: Option<String>,
}

/// Arguments for [`list-dex-rules_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ListDexRulesArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `sort_order`.
    pub sort_order: Option<String>,
    /// Query parameter: `sort_by`.
    pub sort_by: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`create-dex-rule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CreateDexRuleArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`get-dex-rule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetDexRuleArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `rule_id`.
    pub rule_id: String,
}

/// Arguments for [`update-dex-rule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct UpdateDexRuleArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `rule_id`.
    pub rule_id: String,
}

/// Arguments for [`delete-dex-rule_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeleteDexRuleArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `rule_id`.
    pub rule_id: String,
}

/// Arguments for [`dex-endpoints-list-tests-overview_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexEndpointsListTestsOverviewArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `colo`.
    pub colo: Option<String>,
    /// Query parameter: `testName`.
    pub test_name: Option<String>,
    /// Query parameter: `deviceId`.
    pub device_id: Option<String>,
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `kind`.
    pub kind: Option<String>,
}

/// Arguments for [`dex-endpoints-tests-unique-devices_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexEndpointsTestsUniqueDevicesArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `testName`.
    pub test_name: Option<String>,
    /// Query parameter: `deviceId`.
    pub device_id: Option<String>,
}

/// Arguments for [`dex-endpoints-traceroute-test-result-network-path_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexEndpointsTracerouteTestResultNetworkPathArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `test_result_id`.
    pub test_result_id: String,
}

/// Arguments for [`dex-endpoints-traceroute-test-details_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexEndpointsTracerouteTestDetailsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `test_id`.
    pub test_id: String,
    /// Query parameter: `deviceId`.
    pub device_id: Option<String>,
    /// Query parameter: `from`.
    pub from: Option<String>,
    /// Query parameter: `to`.
    pub to: Option<String>,
    /// Query parameter: `interval`.
    pub interval: Option<String>,
    /// Query parameter: `colo`.
    pub colo: Option<String>,
}

/// Arguments for [`dex-endpoints-traceroute-test-network-path_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexEndpointsTracerouteTestNetworkPathArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `test_id`.
    pub test_id: String,
    /// Query parameter: `deviceId`.
    pub device_id: Option<String>,
    /// Query parameter: `from`.
    pub from: Option<String>,
    /// Query parameter: `to`.
    pub to: Option<String>,
    /// Query parameter: `interval`.
    pub interval: Option<String>,
}

/// Arguments for [`dex-endpoints-traceroute-test-percentiles_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DexEndpointsTracerouteTestPercentilesArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `test_id`.
    pub test_id: String,
    /// Query parameter: `deviceId`.
    pub device_id: Option<String>,
    /// Query parameter: `from`.
    pub from: Option<String>,
    /// Query parameter: `to`.
    pub to: Option<String>,
    /// Query parameter: `colo`.
    pub colo: Option<String>,
}

/// Arguments for [`list-warp-change-events_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ListWarpChangeEventsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `from`.
    pub from: Option<String>,
    /// Query parameter: `to`.
    pub to: Option<String>,
    /// Query parameter: `type`.
    pub r#type: Option<String>,
    /// Query parameter: `toggle`.
    pub toggle: Option<String>,
    /// Query parameter: `config_name`.
    pub config_name: Option<String>,
    /// Query parameter: `account_name`.
    pub account_name: Option<String>,
    /// Query parameter: `sort_order`.
    pub sort_order: Option<String>,
}

/// Arguments for [`magic-interconnects-list-interconnects_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicInterconnectsListInterconnectsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-interconnects-update-multiple-interconnects_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicInterconnectsUpdateMultipleInterconnectsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-interconnects-list-interconnect-details_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicInterconnectsListInterconnectDetailsArgs {
    /// Path parameter: `cf_interconnect_id`.
    pub cf_interconnect_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-interconnects-update-interconnect_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicInterconnectsUpdateInterconnectArgs {
    /// Path parameter: `cf_interconnect_id`.
    pub cf_interconnect_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-gre-tunnels-list-gre-tunnels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicGreTunnelsListGreTunnelsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-gre-tunnels-create-gre-tunnels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicGreTunnelsCreateGreTunnelsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-gre-tunnels-update-multiple-gre-tunnels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicGreTunnelsUpdateMultipleGreTunnelsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-gre-tunnels-list-gre-tunnel-details_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicGreTunnelsListGreTunnelDetailsArgs {
    /// Path parameter: `gre_tunnel_id`.
    pub gre_tunnel_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-gre-tunnels-update-gre-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicGreTunnelsUpdateGreTunnelArgs {
    /// Path parameter: `gre_tunnel_id`.
    pub gre_tunnel_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-gre-tunnels-delete-gre-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicGreTunnelsDeleteGreTunnelArgs {
    /// Path parameter: `gre_tunnel_id`.
    pub gre_tunnel_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-ipsec-tunnels-list-ipsec-tunnels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicIpsecTunnelsListIpsecTunnelsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-ipsec-tunnels-create-ipsec-tunnels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicIpsecTunnelsCreateIpsecTunnelsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-ipsec-tunnels-update-multiple-ipsec-tunnels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicIpsecTunnelsUpdateMultipleIpsecTunnelsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-ipsec-tunnels-list-ipsec-tunnel-details_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicIpsecTunnelsListIpsecTunnelDetailsArgs {
    /// Path parameter: `ipsec_tunnel_id`.
    pub ipsec_tunnel_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-ipsec-tunnels-update-ipsec-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicIpsecTunnelsUpdateIpsecTunnelArgs {
    /// Path parameter: `ipsec_tunnel_id`.
    pub ipsec_tunnel_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-ipsec-tunnels-delete-ipsec-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicIpsecTunnelsDeleteIpsecTunnelArgs {
    /// Path parameter: `ipsec_tunnel_id`.
    pub ipsec_tunnel_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`magic-ipsec-tunnels-generate-pre-shared-key-(-psk)-for-ipsec-tunnels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct MagicIpsecTunnelsGeneratePreSharedKeyPskForIpsecTunnelsArgs {
    /// Path parameter: `ipsec_tunnel_id`.
    pub ipsec_tunnel_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`slurper-get-job-progress_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SlurperGetJobProgressArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `job_id`.
    pub job_id: String,
}

/// Arguments for [`slurper-check-source-connectivity_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SlurperCheckSourceConnectivityArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`slurper-check-target-connectivity_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SlurperCheckTargetConnectivityArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`tunnel-route-list-tunnel-routes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TunnelRouteListTunnelRoutesArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `comment`.
    pub comment: Option<String>,
    /// Query parameter: `is_deleted`.
    pub is_deleted: Option<String>,
    /// Query parameter: `network_subset`.
    pub network_subset: Option<String>,
    /// Query parameter: `network_superset`.
    pub network_superset: Option<String>,
    /// Query parameter: `existed_at`.
    pub existed_at: Option<String>,
    /// Query parameter: `tunnel_id`.
    pub tunnel_id: Option<String>,
    /// Query parameter: `route_id`.
    pub route_id: Option<String>,
    /// Query parameter: `tun_types`.
    pub tun_types: Option<String>,
    /// Query parameter: `virtual_network_id`.
    pub virtual_network_id: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `page`.
    pub page: Option<String>,
}

/// Arguments for [`tunnel-route-create-a-tunnel-route_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TunnelRouteCreateATunnelRouteArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`tunnel-route-get-tunnel-route-by-ip_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TunnelRouteGetTunnelRouteByIpArgs {
    /// Path parameter: `ip`.
    pub ip: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `virtual_network_id`.
    pub virtual_network_id: Option<String>,
    /// Query parameter: `default_virtual_network_fallback`.
    pub default_virtual_network_fallback: Option<String>,
}

/// Arguments for [`tunnel-route-get-tunnel-route_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TunnelRouteGetTunnelRouteArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `route_id`.
    pub route_id: String,
}

/// Arguments for [`tunnel-route-update-a-tunnel-route_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TunnelRouteUpdateATunnelRouteArgs {
    /// Path parameter: `route_id`.
    pub route_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`tunnel-route-delete-a-tunnel-route_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TunnelRouteDeleteATunnelRouteArgs {
    /// Path parameter: `route_id`.
    pub route_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`tunnel-virtual-network-list-virtual-networks_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TunnelVirtualNetworkListVirtualNetworksArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `id`.
    pub id: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `is_default`.
    pub is_default: Option<String>,
    /// Query parameter: `is_default_network`.
    pub is_default_network: Option<String>,
    /// Query parameter: `is_deleted`.
    pub is_deleted: Option<String>,
}

/// Arguments for [`tunnel-virtual-network-create-a-virtual-network_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TunnelVirtualNetworkCreateAVirtualNetworkArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`tunnel-virtual-network-get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TunnelVirtualNetworkGetArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `virtual_network_id`.
    pub virtual_network_id: String,
}

/// Arguments for [`tunnel-virtual-network-update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TunnelVirtualNetworkUpdateArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `virtual_network_id`.
    pub virtual_network_id: String,
}

/// Arguments for [`tunnel-virtual-network-delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct TunnelVirtualNetworkDeleteArgs {
    /// Path parameter: `virtual_network_id`.
    pub virtual_network_id: String,
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`cloudflare-tunnel-list-warp-connector-tunnels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelListWarpConnectorTunnelsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `is_deleted`.
    pub is_deleted: Option<String>,
    /// Query parameter: `existed_at`.
    pub existed_at: Option<String>,
    /// Query parameter: `uuid`.
    pub uuid: Option<String>,
    /// Query parameter: `was_active_at`.
    pub was_active_at: Option<String>,
    /// Query parameter: `was_inactive_at`.
    pub was_inactive_at: Option<String>,
    /// Query parameter: `include_prefix`.
    pub include_prefix: Option<String>,
    /// Query parameter: `exclude_prefix`.
    pub exclude_prefix: Option<String>,
    /// Query parameter: `status`.
    pub status: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `page`.
    pub page: Option<String>,
}

/// Arguments for [`cloudflare-tunnel-create-a-warp-connector-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelCreateAWarpConnectorTunnelArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`cloudflare-tunnel-get-a-warp-connector-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelGetAWarpConnectorTunnelArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`cloudflare-tunnel-update-a-warp-connector-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelUpdateAWarpConnectorTunnelArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`cloudflare-tunnel-delete-a-warp-connector-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelDeleteAWarpConnectorTunnelArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`cloudflare-tunnel-list-warp-connector-tunnel-connections_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelListWarpConnectorTunnelConnectionsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`cloudflare-tunnel-get-warp-connector-tunnel-connector_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelGetWarpConnectorTunnelConnectorArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
    /// Path parameter: `connector_id`.
    pub connector_id: String,
}

/// Arguments for [`cloudflare-tunnel-manual-failover-warp-connector-tunnel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelManualFailoverWarpConnectorTunnelArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`cloudflare-tunnel-get-a-warp-connector-tunnel-token_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudflareTunnelGetAWarpConnectorTunnelTokenArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `tunnel_id`.
    pub tunnel_id: String,
}

/// Arguments for [`zero-trust-accounts-get-connectivity-settings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ZeroTrustAccountsGetConnectivitySettingsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`zero-trust-accounts-patch-connectivity-settings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ZeroTrustAccountsPatchConnectivitySettingsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`zero-trust-networks-subnet-create-warp_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ZeroTrustNetworksSubnetCreateWarpArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
}

/// Arguments for [`zero-trust-networks-subnet-get-warp_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ZeroTrustNetworksSubnetGetWarpArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `subnet_id`.
    pub subnet_id: String,
}

/// Arguments for [`zero-trust-networks-subnet-update-warp_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ZeroTrustNetworksSubnetUpdateWarpArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `subnet_id`.
    pub subnet_id: String,
}

/// Arguments for [`zero-trust-networks-subnet-delete-warp_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ZeroTrustNetworksSubnetDeleteWarpArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `subnet_id`.
    pub subnet_id: String,
}

/// Arguments for [`radar-get-bgp-hijacks-events_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpHijacksEventsArgs {
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `eventId`.
    pub event_id: Option<String>,
    /// Query parameter: `hijackerAsn`.
    pub hijacker_asn: Option<String>,
    /// Query parameter: `victimAsn`.
    pub victim_asn: Option<String>,
    /// Query parameter: `involvedAsn`.
    pub involved_asn: Option<String>,
    /// Query parameter: `involvedCountry`.
    pub involved_country: Option<String>,
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
    /// Query parameter: `minConfidence`.
    pub min_confidence: Option<String>,
    /// Query parameter: `maxConfidence`.
    pub max_confidence: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `sortBy`.
    pub sort_by: Option<String>,
    /// Query parameter: `sortOrder`.
    pub sort_order: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-ips-timeseries_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpIpsTimeseriesArgs {
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `ipVersion`.
    pub ip_version: Option<String>,
    /// Query parameter: `includeDelay`.
    pub include_delay: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-route-leak-events_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpRouteLeakEventsArgs {
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
    /// Query parameter: `eventId`.
    pub event_id: Option<String>,
    /// Query parameter: `leakAsn`.
    pub leak_asn: Option<String>,
    /// Query parameter: `involvedAsn`.
    pub involved_asn: Option<String>,
    /// Query parameter: `involvedCountry`.
    pub involved_country: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `sortBy`.
    pub sort_by: Option<String>,
    /// Query parameter: `sortOrder`.
    pub sort_order: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-routes-asns_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpRoutesAsnsArgs {
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `sortBy`.
    pub sort_by: Option<String>,
    /// Query parameter: `sortOrder`.
    pub sort_order: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-pfx2as-moas_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpPfx2AsMoasArgs {
    /// Query parameter: `origin`.
    pub origin: Option<String>,
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
    /// Query parameter: `invalid_only`.
    pub invalid_only: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-pfx2as_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpPfx2AsArgs {
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
    /// Query parameter: `origin`.
    pub origin: Option<String>,
    /// Query parameter: `rpkiStatus`.
    pub rpki_status: Option<String>,
    /// Query parameter: `longestPrefixMatch`.
    pub longest_prefix_match: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-routes-stats_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpRoutesStatsArgs {
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-rpki-aspa-changes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpRpkiAspaChangesArgs {
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `includeAsnInfo`.
    pub include_asn_info: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-rpki-aspa-snapshot_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpRpkiAspaSnapshotArgs {
    /// Query parameter: `customerAsn`.
    pub customer_asn: Option<String>,
    /// Query parameter: `providerAsn`.
    pub provider_asn: Option<String>,
    /// Query parameter: `date`.
    pub date: Option<String>,
    /// Query parameter: `includeAsnInfo`.
    pub include_asn_info: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-rpki-aspa-timeseries_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpRpkiAspaTimeseriesArgs {
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `rir`.
    pub rir: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-timeseries_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpTimeseriesArgs {
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
    /// Query parameter: `updateType`.
    pub update_type: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-top-ases_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpTopAsesArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
    /// Query parameter: `updateType`.
    pub update_type: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-top-asns-by-prefixes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpTopAsnsByPrefixesArgs {
    /// Query parameter: `country`.
    pub country: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-bgp-top-prefixes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetBgpTopPrefixesArgs {
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `updateType`.
    pub update_type: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-quality-index-summary_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetQualityIndexSummaryArgs {
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `metric`.
    pub metric: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`radar-get-quality-index-timeseries-group_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RadarGetQualityIndexTimeseriesGroupArgs {
    /// Query parameter: `aggInterval`.
    pub agg_interval: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `dateRange`.
    pub date_range: Option<String>,
    /// Query parameter: `dateStart`.
    pub date_start: Option<String>,
    /// Query parameter: `dateEnd`.
    pub date_end: Option<String>,
    /// Query parameter: `asn`.
    pub asn: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `continent`.
    pub continent: Option<String>,
    /// Query parameter: `interpolation`.
    pub interpolation: Option<String>,
    /// Query parameter: `metric`.
    pub metric: Option<String>,
    /// Query parameter: `format`.
    pub format: Option<String>,
}

/// Arguments for [`spectrum-aggregate-analytics-get-current-aggregated-analytics_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SpectrumAggregateAnalyticsGetCurrentAggregatedAnalyticsArgs {
    /// Path parameter: `zone_id`.
    pub zone_id: String,
    /// Query parameter: `appID`.
    pub app_id: Option<String>,
    /// Query parameter: `colo_name`.
    pub colo_name: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = ip_address_management_prefixes_list_bgp_prefixes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn ip_address_management_prefixes_list_bgp_prefixes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IpAddressManagementPrefixesListBgpPrefixesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressingResponseCollectionBgp>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/addressing/prefixes/{}/bgp/prefixes",
        args.account_id, args.prefix_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: AddressingResponseCollectionBgp =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = ip_address_management_prefixes_create_bgp_prefix_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn ip_address_management_prefixes_create_bgp_prefix_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IpAddressManagementPrefixesCreateBgpPrefixArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressingSingleResponseBgp>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/addressing/prefixes/{}/bgp/prefixes",
        args.account_id, args.prefix_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: AddressingSingleResponseBgp =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes/{bgp_prefix_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes/{bgp_prefix_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = ip_address_management_prefixes_fetch_bgp_prefix_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn ip_address_management_prefixes_fetch_bgp_prefix_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IpAddressManagementPrefixesFetchBgpPrefixArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressingSingleResponseBgp>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/addressing/prefixes/{}/bgp/prefixes/{}",
        args.account_id, args.prefix_id, args.bgp_prefix_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: AddressingSingleResponseBgp =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes/{bgp_prefix_id}
// -----------------------------------------------------------------------------

/// PATCH /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes/{bgp_prefix_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = ip_address_management_prefixes_update_bgp_prefix_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn ip_address_management_prefixes_update_bgp_prefix_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IpAddressManagementPrefixesUpdateBgpPrefixArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressingSingleResponseBgp>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/addressing/prefixes/{}/bgp/prefixes/{}",
        args.account_id, args.prefix_id, args.bgp_prefix_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: AddressingSingleResponseBgp =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes/{bgp_prefix_id}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/addressing/prefixes/{prefix_id}/bgp/prefixes/{bgp_prefix_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = ip_address_management_prefixes_delete_bgp_prefix_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn ip_address_management_prefixes_delete_bgp_prefix_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &IpAddressManagementPrefixesDeleteBgpPrefixArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AddressingApiResponseCommon>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/addressing/prefixes/{}/bgp/prefixes/{}",
        args.account_id, args.prefix_id, args.bgp_prefix_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: AddressingApiResponseCommon =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cfd_tunnel
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cfd_tunnel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_list_cloudflare_tunnels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_list_cloudflare_tunnels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelListCloudflareTunnelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelCfdTunnelResponseCollection>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.is_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("is_deleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.existed_at {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("existed_at=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.uuid {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("uuid=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.was_active_at {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("was_active_at=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.was_inactive_at {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("was_inactive_at=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("include_prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.exclude_prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("exclude_prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("status=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelCfdTunnelResponseCollection = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/cfd_tunnel
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/cfd_tunnel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_create_a_cloudflare_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_create_a_cloudflare_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelCreateACloudflareTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelCfdTunnelResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelCfdTunnelResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_get_a_cloudflare_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_get_a_cloudflare_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelGetACloudflareTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelCfdTunnelResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel/{}",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelCfdTunnelResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH /accounts/{account_id}/cfd_tunnel/{tunnel_id}
// -----------------------------------------------------------------------------

/// PATCH /accounts/{account_id}/cfd_tunnel/{tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_update_a_cloudflare_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_update_a_cloudflare_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelUpdateACloudflareTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelCfdTunnelResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel/{}",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelCfdTunnelResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/cfd_tunnel/{tunnel_id}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/cfd_tunnel/{tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_delete_a_cloudflare_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_delete_a_cloudflare_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelDeleteACloudflareTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelCfdTunnelResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel/{}",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelCfdTunnelResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}/configurations
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}/configurations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_configuration_get_configuration_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_configuration_get_configuration_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelConfigurationGetConfigurationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelConfigurationResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel/{}/configurations",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelConfigurationResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/cfd_tunnel/{tunnel_id}/configurations
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/cfd_tunnel/{tunnel_id}/configurations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_configuration_put_configuration_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_configuration_put_configuration_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelConfigurationPutConfigurationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelConfigurationResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel/{}/configurations",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelConfigurationResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}/connections
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}/connections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_list_cloudflare_tunnel_connections_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_list_cloudflare_tunnel_connections_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelListCloudflareTunnelConnectionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelTunnelConnectionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel/{}/connections",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelTunnelConnectionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/cfd_tunnel/{tunnel_id}/connections
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/cfd_tunnel/{tunnel_id}/connections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_clean_up_cloudflare_tunnel_connections_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_clean_up_cloudflare_tunnel_connections_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelCleanUpCloudflareTunnelConnectionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelEmptyResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel/{}/connections",
        args.account_id, args.tunnel_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.client_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("client_id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelEmptyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}/connectors/{connector_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}/connectors/{connector_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_get_cloudflare_tunnel_connector_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_get_cloudflare_tunnel_connector_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelGetCloudflareTunnelConnectorArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelTunnelClientResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel/{}/connectors/{}",
        args.account_id, args.tunnel_id, args.connector_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelTunnelClientResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/cfd_tunnel/{tunnel_id}/management
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/cfd_tunnel/{tunnel_id}/management.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_get_a_cloudflare_tunnel_management_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_get_a_cloudflare_tunnel_management_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelGetACloudflareTunnelManagementTokenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelTunnelResponseToken>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel/{}/management",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelTunnelResponseToken =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}/token
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cfd_tunnel/{tunnel_id}/token.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_get_a_cloudflare_tunnel_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_get_a_cloudflare_tunnel_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelGetACloudflareTunnelTokenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelTunnelResponseToken>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cfd_tunnel/{}/token",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelTunnelResponseToken =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cloudforce-one/events/aggregate
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cloudforce-one/events/aggregate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_event_aggregate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_event_aggregate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetEventAggregateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cloudforce-one/events/aggregate",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.aggregate_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("aggregateBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.dataset_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("datasetId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.group_by_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("groupByDate=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("limit=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cni/cnis
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cni/cnis.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = list_cnis_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn list_cnis_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ListCnisArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscCniList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/cnis",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.slot {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("slot=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.tunnel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("tunnel_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.cursor {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("cursor=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("limit=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscCniList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/cni/cnis
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/cni/cnis.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = create_cni_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn create_cni_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CreateCniArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscCni>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/cnis",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscCni =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cni/cnis/{cni}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cni/cnis/{cni}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_cni_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_cni_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetCniArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscCni>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/cnis/{}",
        args.account_id, args.cni,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscCni =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/cni/cnis/{cni}
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/cni/cnis/{cni}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = update_cni_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn update_cni_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &UpdateCniArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscCni>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/cnis/{}",
        args.account_id, args.cni,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscCni =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/cni/cnis/{cni}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/cni/cnis/{cni}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = delete_cni_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn delete_cni_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeleteCniArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/cnis/{}",
        args.account_id, args.cni,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cni/interconnects
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cni/interconnects.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = list_interconnects_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn list_interconnects_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ListInterconnectsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscInterconnectList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/interconnects",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.site {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("site=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.r#type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("type=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.cursor {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("cursor=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("limit=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscInterconnectList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/cni/interconnects
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/cni/interconnects.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = create_interconnect_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn create_interconnect_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CreateInterconnectArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscInterconnect>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/interconnects",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscInterconnect =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cni/interconnects/{icon}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cni/interconnects/{icon}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_interconnect_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_interconnect_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetInterconnectArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscInterconnect>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/interconnects/{}",
        args.account_id, args.icon,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscInterconnect =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/cni/interconnects/{icon}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/cni/interconnects/{icon}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = delete_interconnect_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn delete_interconnect_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeleteInterconnectArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/interconnects/{}",
        args.account_id, args.icon,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cni/interconnects/{icon}/loa
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cni/interconnects/{icon}/loa.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_interconnect_loa_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_interconnect_loa_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetInterconnectLoaArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/interconnects/{}/loa",
        args.account_id, args.icon,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cni/interconnects/{icon}/status
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cni/interconnects/{icon}/status.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_interconnect_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_interconnect_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetInterconnectStatusArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscStatusInfo>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/interconnects/{}/status",
        args.account_id, args.icon,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscStatusInfo =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cni/settings
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cni/settings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/settings",
        args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/cni/settings
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/cni/settings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = update_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn update_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &UpdateSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/settings",
        args.account_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cni/slots
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cni/slots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = list_slots_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn list_slots_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ListSlotsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscSlotList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/slots",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.address_contains {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("address_contains=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.site {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("site=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.speed {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("speed=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.occupied {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("occupied=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.cursor {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("cursor=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("limit=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscSlotList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/cni/slots/{slot}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/cni/slots/{slot}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_slot_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_slot_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetSlotArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NscSlotInfo>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/cni/slots/{}",
        args.account_id, args.slot,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: NscSlotInfo =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/connectivity/directory/services
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/connectivity/directory/services.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectivity_services_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectivity_services_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectivityServicesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/connectivity/directory/services",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.r#type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("type=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/connectivity/directory/services
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/connectivity/directory/services.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectivity_services_post_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectivity_services_post_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectivityServicesPostArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/connectivity/directory/services",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/connectivity/directory/services/{service_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/connectivity/directory/services/{service_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectivity_services_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectivity_services_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectivityServicesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/connectivity/directory/services/{}",
        args.account_id, args.service_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/connectivity/directory/services/{service_id}
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/connectivity/directory/services/{service_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectivity_services_put_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectivity_services_put_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectivityServicesPutArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/connectivity/directory/services/{}",
        args.account_id, args.service_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/connectivity/directory/services/{service_id}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/connectivity/directory/services/{service_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectivity_services_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectivity_services_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectivityServicesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/connectivity/directory/services/{}",
        args.account_id, args.service_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/devices/resilience/disconnect
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/devices/resilience/disconnect.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = devices_resilience_retrieve_global_warp_override_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn devices_resilience_retrieve_global_warp_override_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DevicesResilienceRetrieveGlobalWarpOverrideArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<TeamsDevicesGlobalWarpOverrideResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/devices/resilience/disconnect",
        args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TeamsDevicesGlobalWarpOverrideResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/devices/resilience/disconnect
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/devices/resilience/disconnect.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = devices_resilience_set_global_warp_override_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn devices_resilience_set_global_warp_override_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DevicesResilienceSetGlobalWarpOverrideArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<TeamsDevicesGlobalWarpOverrideResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/devices/resilience/disconnect",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TeamsDevicesGlobalWarpOverrideResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/colos
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/colos.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_endpoints_list_colos_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_endpoints_list_colos_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexEndpointsListColosArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/colos",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.from {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("from=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("to=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sortBy=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/commands
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/commands.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_commands_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_commands_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetCommandsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/commands",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("from=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("to=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.device_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("device_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_email {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("user_email=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.command_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("command_type=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("status=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/dex/commands
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/dex/commands.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = post_commands_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn post_commands_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PostCommandsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/commands",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/commands/devices
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/commands/devices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_commands_eligible_devices_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_commands_eligible_devices_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetCommandsEligibleDevicesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/commands/devices",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.search {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("search=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/commands/quota
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/commands/quota.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_commands_quota_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_commands_quota_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetCommandsQuotaArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/commands/quota",
        args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/commands/{command_id}/downloads/{filename}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/commands/{command_id}/downloads/{filename}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_commands_command_id_downloads_filename_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_commands_command_id_downloads_filename_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetCommandsCommandIdDownloadsFilenameArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/commands/{}/downloads/{}",
        args.account_id, args.command_id, args.filename,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/devices/dex_tests
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/devices/dex_tests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = device_dex_test_details_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn device_dex_test_details_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeviceDexTestDetailsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<DigitalExperienceMonitoringDexResponseCollection>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/devices/dex_tests",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.test_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("testName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.kind {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("kind=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: DigitalExperienceMonitoringDexResponseCollection =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/dex/devices/dex_tests
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/dex/devices/dex_tests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = device_dex_test_create_device_dex_test_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn device_dex_test_create_device_dex_test_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeviceDexTestCreateDeviceDexTestArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<DigitalExperienceMonitoringDexSingleResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/devices/dex_tests",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: DigitalExperienceMonitoringDexSingleResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/devices/dex_tests/{dex_test_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/devices/dex_tests/{dex_test_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = device_dex_test_get_device_dex_test_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn device_dex_test_get_device_dex_test_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeviceDexTestGetDeviceDexTestArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<DigitalExperienceMonitoringDexSingleResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/devices/dex_tests/{}",
        args.account_id, args.dex_test_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: DigitalExperienceMonitoringDexSingleResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/dex/devices/dex_tests/{dex_test_id}
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/dex/devices/dex_tests/{dex_test_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = device_dex_test_update_device_dex_test_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn device_dex_test_update_device_dex_test_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeviceDexTestUpdateDeviceDexTestArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<DigitalExperienceMonitoringDexSingleResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/devices/dex_tests/{}",
        args.account_id, args.dex_test_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: DigitalExperienceMonitoringDexSingleResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/dex/devices/dex_tests/{dex_test_id}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/dex/devices/dex_tests/{dex_test_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = device_dex_test_delete_device_dex_test_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn device_dex_test_delete_device_dex_test_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeviceDexTestDeleteDeviceDexTestArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<DigitalExperienceMonitoringDexDeleteResponseCollection>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/devices/dex_tests/{}",
        args.account_id, args.dex_test_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: DigitalExperienceMonitoringDexDeleteResponseCollection =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/devices/{device_id}/fleet-status/live
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/devices/{device_id}/fleet-status/live.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = devices_live_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn devices_live_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DevicesLiveStatusArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DigitalExperienceMonitoringDevice>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/devices/{}/fleet-status/live",
        args.account_id, args.device_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.since_minutes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("since_minutes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.time_now {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("time_now=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.colo {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("colo=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: DigitalExperienceMonitoringDevice = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/fleet-status/devices
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/fleet-status/devices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_fleet_status_devices_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_fleet_status_devices_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexFleetStatusDevicesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<DigitalExperienceMonitoringFleetStatusDevicesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/fleet-status/devices",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.to {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("to=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("from=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sort_by=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.colo {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("colo=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.device_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("device_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.mode {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("status=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.platform {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("platform=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: DigitalExperienceMonitoringFleetStatusDevicesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/fleet-status/live
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/fleet-status/live.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_fleet_status_live_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_fleet_status_live_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexFleetStatusLiveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<DigitalExperienceMonitoringFleetStatusLiveResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/fleet-status/live",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.since_minutes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("since_minutes=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: DigitalExperienceMonitoringFleetStatusLiveResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/fleet-status/over-time
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/fleet-status/over-time.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_fleet_status_over_time_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_fleet_status_over_time_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexFleetStatusOverTimeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<DigitalExperienceMonitoringFleetStatusOverTimeResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/fleet-status/over-time",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.to {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("to=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("from=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.colo {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("colo=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.device_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("device_id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: DigitalExperienceMonitoringFleetStatusOverTimeResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/http-tests/{test_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/http-tests/{test_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_endpoints_http_test_details_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_endpoints_http_test_details_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexEndpointsHttpTestDetailsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/http-tests/{}",
        args.account_id, args.test_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.device_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deviceId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("from=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("to=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.colo {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("colo=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/http-tests/{test_id}/percentiles
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/http-tests/{test_id}/percentiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_endpoints_http_test_percentiles_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_endpoints_http_test_percentiles_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexEndpointsHttpTestPercentilesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/http-tests/{}/percentiles",
        args.account_id, args.test_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.device_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deviceId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("from=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("to=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.colo {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("colo=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/rules
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/rules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = list_dex_rules_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn list_dex_rules_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ListDexRulesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/rules",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_order {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sort_order=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sort_by=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/dex/rules
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/dex/rules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = create_dex_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn create_dex_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CreateDexRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/rules",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/rules/{rule_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/rules/{rule_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = get_dex_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn get_dex_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GetDexRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/rules/{}",
        args.account_id, args.rule_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH /accounts/{account_id}/dex/rules/{rule_id}
// -----------------------------------------------------------------------------

/// PATCH /accounts/{account_id}/dex/rules/{rule_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = update_dex_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn update_dex_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &UpdateDexRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/rules/{}",
        args.account_id, args.rule_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/dex/rules/{rule_id}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/dex/rules/{rule_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = delete_dex_rule_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn delete_dex_rule_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeleteDexRuleArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/rules/{}",
        args.account_id, args.rule_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/tests/overview
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/tests/overview.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_endpoints_list_tests_overview_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_endpoints_list_tests_overview_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexEndpointsListTestsOverviewArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/tests/overview",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.colo {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("colo=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.test_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("testName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.device_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deviceId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.kind {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("kind=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/tests/unique-devices
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/tests/unique-devices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_endpoints_tests_unique_devices_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_endpoints_tests_unique_devices_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexEndpointsTestsUniqueDevicesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/tests/unique-devices",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.test_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("testName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.device_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deviceId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/traceroute-test-results/{test_result_id}/network-path
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/traceroute-test-results/{test_result_id}/network-path.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_endpoints_traceroute_test_result_network_path_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_endpoints_traceroute_test_result_network_path_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexEndpointsTracerouteTestResultNetworkPathArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/traceroute-test-results/{}/network-path",
        args.account_id,
        args.test_result_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/traceroute-tests/{test_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/traceroute-tests/{test_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_endpoints_traceroute_test_details_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_endpoints_traceroute_test_details_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexEndpointsTracerouteTestDetailsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/traceroute-tests/{}",
        args.account_id, args.test_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.device_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deviceId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("from=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("to=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.colo {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("colo=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/traceroute-tests/{test_id}/network-path
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/traceroute-tests/{test_id}/network-path.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_endpoints_traceroute_test_network_path_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_endpoints_traceroute_test_network_path_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexEndpointsTracerouteTestNetworkPathArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/traceroute-tests/{}/network-path",
        args.account_id, args.test_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.device_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deviceId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("from=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("to=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/traceroute-tests/{test_id}/percentiles
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/traceroute-tests/{test_id}/percentiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dex_endpoints_traceroute_test_percentiles_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dex_endpoints_traceroute_test_percentiles_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DexEndpointsTracerouteTestPercentilesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/traceroute-tests/{}/percentiles",
        args.account_id, args.test_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.device_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deviceId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("from=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("to=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.colo {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("colo=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/dex/warp-change-events
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/dex/warp-change-events.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = list_warp_change_events_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn list_warp_change_events_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ListWarpChangeEventsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/dex/warp-change-events",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("from=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("to=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.r#type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("type=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.toggle {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toggle=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.config_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("config_name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.account_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("account_name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_order {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sort_order=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/magic/cf_interconnects
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/magic/cf_interconnects.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_interconnects_list_interconnects_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_interconnects_list_interconnects_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicInterconnectsListInterconnectsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<MagicComponentsSchemasTunnelsCollectionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/cf_interconnects",
        args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicComponentsSchemasTunnelsCollectionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/magic/cf_interconnects
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/magic/cf_interconnects.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_interconnects_update_multiple_interconnects_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_interconnects_update_multiple_interconnects_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicInterconnectsUpdateMultipleInterconnectsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<MagicComponentsSchemasModifiedTunnelsCollectionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/cf_interconnects",
        args.account_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicComponentsSchemasModifiedTunnelsCollectionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/magic/cf_interconnects/{cf_interconnect_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/magic/cf_interconnects/{cf_interconnect_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_interconnects_list_interconnect_details_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_interconnects_list_interconnect_details_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicInterconnectsListInterconnectDetailsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<MagicComponentsSchemasTunnelSingleResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/cf_interconnects/{}",
        args.account_id, args.cf_interconnect_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicComponentsSchemasTunnelSingleResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/magic/cf_interconnects/{cf_interconnect_id}
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/magic/cf_interconnects/{cf_interconnect_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_interconnects_update_interconnect_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_interconnects_update_interconnect_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicInterconnectsUpdateInterconnectArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<MagicComponentsSchemasTunnelModifiedResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/cf_interconnects/{}",
        args.account_id, args.cf_interconnect_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicComponentsSchemasTunnelModifiedResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/magic/gre_tunnels
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/magic/gre_tunnels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_gre_tunnels_list_gre_tunnels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_gre_tunnels_list_gre_tunnels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicGreTunnelsListGreTunnelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MagicTunnelsCollectionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/gre_tunnels",
        args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicTunnelsCollectionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/magic/gre_tunnels
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/magic/gre_tunnels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_gre_tunnels_create_gre_tunnels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_gre_tunnels_create_gre_tunnels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicGreTunnelsCreateGreTunnelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MagicCreateGreTunnelResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/gre_tunnels",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicCreateGreTunnelResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/magic/gre_tunnels
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/magic/gre_tunnels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_gre_tunnels_update_multiple_gre_tunnels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_gre_tunnels_update_multiple_gre_tunnels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicGreTunnelsUpdateMultipleGreTunnelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<MagicModifiedTunnelsCollectionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/gre_tunnels",
        args.account_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicModifiedTunnelsCollectionResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/magic/gre_tunnels/{gre_tunnel_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/magic/gre_tunnels/{gre_tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_gre_tunnels_list_gre_tunnel_details_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_gre_tunnels_list_gre_tunnel_details_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicGreTunnelsListGreTunnelDetailsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MagicTunnelSingleResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/gre_tunnels/{}",
        args.account_id, args.gre_tunnel_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicTunnelSingleResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/magic/gre_tunnels/{gre_tunnel_id}
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/magic/gre_tunnels/{gre_tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_gre_tunnels_update_gre_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_gre_tunnels_update_gre_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicGreTunnelsUpdateGreTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MagicTunnelModifiedResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/gre_tunnels/{}",
        args.account_id, args.gre_tunnel_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicTunnelModifiedResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/magic/gre_tunnels/{gre_tunnel_id}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/magic/gre_tunnels/{gre_tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_gre_tunnels_delete_gre_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_gre_tunnels_delete_gre_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicGreTunnelsDeleteGreTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MagicTunnelDeletedResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/gre_tunnels/{}",
        args.account_id, args.gre_tunnel_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicTunnelDeletedResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/magic/ipsec_tunnels
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/magic/ipsec_tunnels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_ipsec_tunnels_list_ipsec_tunnels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_ipsec_tunnels_list_ipsec_tunnels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicIpsecTunnelsListIpsecTunnelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<MagicSchemasTunnelsCollectionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/ipsec_tunnels",
        args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicSchemasTunnelsCollectionResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/magic/ipsec_tunnels
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/magic/ipsec_tunnels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_ipsec_tunnels_create_ipsec_tunnels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_ipsec_tunnels_create_ipsec_tunnels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicIpsecTunnelsCreateIpsecTunnelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<MagicSchemasCreateIpsecTunnelResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/ipsec_tunnels",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicSchemasCreateIpsecTunnelResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/magic/ipsec_tunnels
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/magic/ipsec_tunnels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_ipsec_tunnels_update_multiple_ipsec_tunnels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_ipsec_tunnels_update_multiple_ipsec_tunnels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicIpsecTunnelsUpdateMultipleIpsecTunnelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<MagicSchemasModifiedTunnelsCollectionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/ipsec_tunnels",
        args.account_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicSchemasModifiedTunnelsCollectionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/magic/ipsec_tunnels/{ipsec_tunnel_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/magic/ipsec_tunnels/{ipsec_tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_ipsec_tunnels_list_ipsec_tunnel_details_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_ipsec_tunnels_list_ipsec_tunnel_details_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicIpsecTunnelsListIpsecTunnelDetailsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MagicSchemasTunnelSingleResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/ipsec_tunnels/{}",
        args.account_id, args.ipsec_tunnel_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicSchemasTunnelSingleResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/magic/ipsec_tunnels/{ipsec_tunnel_id}
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/magic/ipsec_tunnels/{ipsec_tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_ipsec_tunnels_update_ipsec_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_ipsec_tunnels_update_ipsec_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicIpsecTunnelsUpdateIpsecTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<MagicSchemasTunnelModifiedResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/ipsec_tunnels/{}",
        args.account_id, args.ipsec_tunnel_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicSchemasTunnelModifiedResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/magic/ipsec_tunnels/{ipsec_tunnel_id}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/magic/ipsec_tunnels/{ipsec_tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_ipsec_tunnels_delete_ipsec_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_ipsec_tunnels_delete_ipsec_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicIpsecTunnelsDeleteIpsecTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MagicSchemasTunnelDeletedResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/ipsec_tunnels/{}",
        args.account_id, args.ipsec_tunnel_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicSchemasTunnelDeletedResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/magic/ipsec_tunnels/{ipsec_tunnel_id}/psk_generate
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/magic/ipsec_tunnels/{ipsec_tunnel_id}/psk_generate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = magic_ipsec_tunnels_generate_pre_shared_key_psk_for_ipsec_tunnels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn magic_ipsec_tunnels_generate_pre_shared_key_psk_for_ipsec_tunnels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &MagicIpsecTunnelsGeneratePreSharedKeyPskForIpsecTunnelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MagicPskGenerationResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/magic/ipsec_tunnels/{}/psk_generate",
        args.account_id, args.ipsec_tunnel_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: MagicPskGenerationResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/slurper/jobs/{job_id}/progress
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/slurper/jobs/{job_id}/progress.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = slurper_get_job_progress_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn slurper_get_job_progress_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SlurperGetJobProgressArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/slurper/jobs/{}/progress",
        args.account_id, args.job_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/slurper/source/connectivity-precheck
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/slurper/source/connectivity-precheck.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = slurper_check_source_connectivity_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn slurper_check_source_connectivity_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SlurperCheckSourceConnectivityArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/slurper/source/connectivity-precheck",
        args.account_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/slurper/target/connectivity-precheck
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/slurper/target/connectivity-precheck.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = slurper_check_target_connectivity_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn slurper_check_target_connectivity_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SlurperCheckTargetConnectivityArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/slurper/target/connectivity-precheck",
        args.account_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/teamnet/routes
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/teamnet/routes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = tunnel_route_list_tunnel_routes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn tunnel_route_list_tunnel_routes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TunnelRouteListTunnelRoutesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelTeamnetResponseCollection>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/teamnet/routes",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.comment {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("comment=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.is_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("is_deleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.network_subset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("network_subset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.network_superset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("network_superset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.existed_at {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("existed_at=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.tunnel_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("tunnel_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.route_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("route_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.tun_types {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("tun_types=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.virtual_network_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("virtual_network_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelTeamnetResponseCollection =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/teamnet/routes
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/teamnet/routes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = tunnel_route_create_a_tunnel_route_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn tunnel_route_create_a_tunnel_route_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TunnelRouteCreateATunnelRouteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelRouteResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/teamnet/routes",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelRouteResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/teamnet/routes/ip/{ip}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/teamnet/routes/ip/{ip}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = tunnel_route_get_tunnel_route_by_ip_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn tunnel_route_get_tunnel_route_by_ip_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TunnelRouteGetTunnelRouteByIpArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelTeamnetResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/teamnet/routes/ip/{}",
        args.account_id, args.ip,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.virtual_network_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("virtual_network_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.default_virtual_network_fallback {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("default_virtual_network_fallback=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelTeamnetResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/teamnet/routes/{route_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/teamnet/routes/{route_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = tunnel_route_get_tunnel_route_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn tunnel_route_get_tunnel_route_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TunnelRouteGetTunnelRouteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelRouteResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/teamnet/routes/{}",
        args.account_id, args.route_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelRouteResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH /accounts/{account_id}/teamnet/routes/{route_id}
// -----------------------------------------------------------------------------

/// PATCH /accounts/{account_id}/teamnet/routes/{route_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = tunnel_route_update_a_tunnel_route_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn tunnel_route_update_a_tunnel_route_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TunnelRouteUpdateATunnelRouteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelRouteResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/teamnet/routes/{}",
        args.account_id, args.route_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelRouteResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/teamnet/routes/{route_id}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/teamnet/routes/{route_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = tunnel_route_delete_a_tunnel_route_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn tunnel_route_delete_a_tunnel_route_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TunnelRouteDeleteATunnelRouteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelRouteResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/teamnet/routes/{}",
        args.account_id, args.route_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelRouteResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/teamnet/virtual_networks
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/teamnet/virtual_networks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = tunnel_virtual_network_list_virtual_networks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn tunnel_virtual_network_list_virtual_networks_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TunnelVirtualNetworkListVirtualNetworksArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelVnetResponseCollection>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/teamnet/virtual_networks",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.is_default {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("is_default=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.is_default_network {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("is_default_network=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.is_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("is_deleted=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelVnetResponseCollection =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/teamnet/virtual_networks
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/teamnet/virtual_networks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = tunnel_virtual_network_create_a_virtual_network_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn tunnel_virtual_network_create_a_virtual_network_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TunnelVirtualNetworkCreateAVirtualNetworkArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelVnetResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/teamnet/virtual_networks",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelVnetResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/teamnet/virtual_networks/{virtual_network_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/teamnet/virtual_networks/{virtual_network_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = tunnel_virtual_network_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn tunnel_virtual_network_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TunnelVirtualNetworkGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelVnetResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/teamnet/virtual_networks/{}",
        args.account_id, args.virtual_network_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelVnetResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH /accounts/{account_id}/teamnet/virtual_networks/{virtual_network_id}
// -----------------------------------------------------------------------------

/// PATCH /accounts/{account_id}/teamnet/virtual_networks/{virtual_network_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = tunnel_virtual_network_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn tunnel_virtual_network_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TunnelVirtualNetworkUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelVnetResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/teamnet/virtual_networks/{}",
        args.account_id, args.virtual_network_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelVnetResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/teamnet/virtual_networks/{virtual_network_id}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/teamnet/virtual_networks/{virtual_network_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = tunnel_virtual_network_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn tunnel_virtual_network_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &TunnelVirtualNetworkDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelVnetResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/teamnet/virtual_networks/{}",
        args.account_id, args.virtual_network_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelVnetResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/warp_connector
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/warp_connector.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_list_warp_connector_tunnels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_list_warp_connector_tunnels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelListWarpConnectorTunnelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<TunnelWarpConnectorResponseCollection>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/warp_connector",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.is_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("is_deleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.existed_at {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("existed_at=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.uuid {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("uuid=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.was_active_at {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("was_active_at=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.was_inactive_at {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("was_inactive_at=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("include_prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.exclude_prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("exclude_prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("status=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelWarpConnectorResponseCollection = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/warp_connector
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/warp_connector.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_create_a_warp_connector_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_create_a_warp_connector_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelCreateAWarpConnectorTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelWarpConnectorResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/warp_connector",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelWarpConnectorResponseSingle = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/warp_connector/{tunnel_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/warp_connector/{tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_get_a_warp_connector_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_get_a_warp_connector_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelGetAWarpConnectorTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelWarpConnectorResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/warp_connector/{}",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelWarpConnectorResponseSingle = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH /accounts/{account_id}/warp_connector/{tunnel_id}
// -----------------------------------------------------------------------------

/// PATCH /accounts/{account_id}/warp_connector/{tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_update_a_warp_connector_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_update_a_warp_connector_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelUpdateAWarpConnectorTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelWarpConnectorResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/warp_connector/{}",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelWarpConnectorResponseSingle = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/warp_connector/{tunnel_id}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/warp_connector/{tunnel_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_delete_a_warp_connector_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_delete_a_warp_connector_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelDeleteAWarpConnectorTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelWarpConnectorResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/warp_connector/{}",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelWarpConnectorResponseSingle = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/warp_connector/{tunnel_id}/connections
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/warp_connector/{tunnel_id}/connections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_list_warp_connector_tunnel_connections_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_list_warp_connector_tunnel_connections_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelListWarpConnectorTunnelConnectionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<TunnelTunnelWarpConnectorConnectionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/warp_connector/{}/connections",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelTunnelWarpConnectorConnectionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/warp_connector/{tunnel_id}/connectors/{connector_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/warp_connector/{tunnel_id}/connectors/{connector_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_get_warp_connector_tunnel_connector_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_get_warp_connector_tunnel_connector_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelGetWarpConnectorTunnelConnectorArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<TunnelTunnelWarpConnectorClientResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/warp_connector/{}/connectors/{}",
        args.account_id, args.tunnel_id, args.connector_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelTunnelWarpConnectorClientResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/warp_connector/{tunnel_id}/failover
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/warp_connector/{tunnel_id}/failover.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_manual_failover_warp_connector_tunnel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_manual_failover_warp_connector_tunnel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelManualFailoverWarpConnectorTunnelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelEmptyResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/warp_connector/{}/failover",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelEmptyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/warp_connector/{tunnel_id}/token
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/warp_connector/{tunnel_id}/token.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudflare_tunnel_get_a_warp_connector_tunnel_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudflare_tunnel_get_a_warp_connector_tunnel_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudflareTunnelGetAWarpConnectorTunnelTokenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelTunnelResponseToken>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/warp_connector/{}/token",
        args.account_id, args.tunnel_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelTunnelResponseToken =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/zerotrust/connectivity_settings
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/zerotrust/connectivity_settings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = zero_trust_accounts_get_connectivity_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn zero_trust_accounts_get_connectivity_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ZeroTrustAccountsGetConnectivitySettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<TunnelZeroTrustConnectivitySettingsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/zerotrust/connectivity_settings",
        args.account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelZeroTrustConnectivitySettingsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH /accounts/{account_id}/zerotrust/connectivity_settings
// -----------------------------------------------------------------------------

/// PATCH /accounts/{account_id}/zerotrust/connectivity_settings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = zero_trust_accounts_patch_connectivity_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn zero_trust_accounts_patch_connectivity_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ZeroTrustAccountsPatchConnectivitySettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<TunnelZeroTrustConnectivitySettingsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/zerotrust/connectivity_settings",
        args.account_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelZeroTrustConnectivitySettingsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST /accounts/{account_id}/zerotrust/subnets/warp
// -----------------------------------------------------------------------------

/// POST /accounts/{account_id}/zerotrust/subnets/warp.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = zero_trust_networks_subnet_create_warp_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn zero_trust_networks_subnet_create_warp_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ZeroTrustNetworksSubnetCreateWarpArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelSubnetResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/zerotrust/subnets/warp",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelSubnetResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/zerotrust/subnets/warp/{subnet_id}
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/zerotrust/subnets/warp/{subnet_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = zero_trust_networks_subnet_get_warp_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn zero_trust_networks_subnet_get_warp_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ZeroTrustNetworksSubnetGetWarpArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelSubnetResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/zerotrust/subnets/warp/{}",
        args.account_id, args.subnet_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelSubnetResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH /accounts/{account_id}/zerotrust/subnets/warp/{subnet_id}
// -----------------------------------------------------------------------------

/// PATCH /accounts/{account_id}/zerotrust/subnets/warp/{subnet_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = zero_trust_networks_subnet_update_warp_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn zero_trust_networks_subnet_update_warp_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ZeroTrustNetworksSubnetUpdateWarpArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TunnelSubnetResponseSingle>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/zerotrust/subnets/warp/{}",
        args.account_id, args.subnet_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelSubnetResponseSingle =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE /accounts/{account_id}/zerotrust/subnets/warp/{subnet_id}
// -----------------------------------------------------------------------------

/// DELETE /accounts/{account_id}/zerotrust/subnets/warp/{subnet_id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = zero_trust_networks_subnet_delete_warp_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn zero_trust_networks_subnet_delete_warp_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ZeroTrustNetworksSubnetDeleteWarpArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<TunnelSubnetResponseSingleNullable>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/zerotrust/subnets/warp/{}",
        args.account_id, args.subnet_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: TunnelSubnetResponseSingleNullable = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/hijacks/events
// -----------------------------------------------------------------------------

/// GET /radar/bgp/hijacks/events.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_hijacks_events_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_hijacks_events_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpHijacksEventsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/hijacks/events",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.event_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("eventId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.hijacker_asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("hijackerAsn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.victim_asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("victimAsn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.involved_asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("involvedAsn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.involved_country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("involvedCountry=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.min_confidence {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minConfidence=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_confidence {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxConfidence=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_range {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateRange=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_start {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateStart=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_end {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateEnd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sortBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_order {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sortOrder=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/ips/timeseries
// -----------------------------------------------------------------------------

/// GET /radar/bgp/ips/timeseries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_ips_timeseries_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_ips_timeseries_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpIpsTimeseriesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/ips/timeseries",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_range {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateRange=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_start {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateStart=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_end {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateEnd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("asn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.ip_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ipVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_delay {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeDelay=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/leaks/events
// -----------------------------------------------------------------------------

/// GET /radar/bgp/leaks/events.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_route_leak_events_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_route_leak_events_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpRouteLeakEventsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/leaks/events",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.per_page {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("per_page=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.event_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("eventId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.leak_asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("leakAsn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.involved_asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("involvedAsn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.involved_country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("involvedCountry=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_range {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateRange=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_start {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateStart=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_end {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateEnd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sortBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_order {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sortOrder=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/routes/ases
// -----------------------------------------------------------------------------

/// GET /radar/bgp/routes/ases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_routes_asns_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_routes_asns_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpRoutesAsnsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/routes/ases",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("limit=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sortBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_order {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sortOrder=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/routes/moas
// -----------------------------------------------------------------------------

/// GET /radar/bgp/routes/moas.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_pfx2as_moas_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_pfx2as_moas_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpPfx2AsMoasArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/routes/moas",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.origin {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("origin=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.invalid_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("invalid_only=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/routes/pfx2as
// -----------------------------------------------------------------------------

/// GET /radar/bgp/routes/pfx2as.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_pfx2as_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_pfx2as_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpPfx2AsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/routes/pfx2as",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.origin {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("origin=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.rpki_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("rpkiStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.longest_prefix_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("longestPrefixMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/routes/stats
// -----------------------------------------------------------------------------

/// GET /radar/bgp/routes/stats.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_routes_stats_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_routes_stats_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpRoutesStatsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/routes/stats",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("asn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/rpki/aspa/changes
// -----------------------------------------------------------------------------

/// GET /radar/bgp/rpki/aspa/changes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_rpki_aspa_changes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_rpki_aspa_changes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpRpkiAspaChangesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/rpki/aspa/changes",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.date_start {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateStart=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_end {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateEnd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("asn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_asn_info {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAsnInfo=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/rpki/aspa/snapshot
// -----------------------------------------------------------------------------

/// GET /radar/bgp/rpki/aspa/snapshot.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_rpki_aspa_snapshot_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_rpki_aspa_snapshot_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpRpkiAspaSnapshotArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://api.cloudflare.com/client/v4/radar/bgp/rpki/aspa/snapshot",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.customer_asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("customerAsn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.provider_asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("providerAsn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("date=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_asn_info {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeAsnInfo=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/rpki/aspa/timeseries
// -----------------------------------------------------------------------------

/// GET /radar/bgp/rpki/aspa/timeseries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_rpki_aspa_timeseries_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_rpki_aspa_timeseries_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpRpkiAspaTimeseriesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://api.cloudflare.com/client/v4/radar/bgp/rpki/aspa/timeseries",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.date_start {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateStart=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_end {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateEnd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.rir {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("rir=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/timeseries
// -----------------------------------------------------------------------------

/// GET /radar/bgp/timeseries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_timeseries_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_timeseries_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpTimeseriesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/timeseries",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.agg_interval {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("aggInterval=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_range {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateRange=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_start {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateStart=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_end {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateEnd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("asn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/top/ases
// -----------------------------------------------------------------------------

/// GET /radar/bgp/top/ases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_top_ases_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_top_ases_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpTopAsesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/top/ases",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("limit=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_range {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateRange=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_start {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateStart=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_end {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateEnd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("asn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/top/ases/prefixes
// -----------------------------------------------------------------------------

/// GET /radar/bgp/top/ases/prefixes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_top_asns_by_prefixes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_top_asns_by_prefixes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpTopAsnsByPrefixesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/top/ases/prefixes",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("country=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("limit=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/bgp/top/prefixes
// -----------------------------------------------------------------------------

/// GET /radar/bgp/top/prefixes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_bgp_top_prefixes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_bgp_top_prefixes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetBgpTopPrefixesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/bgp/top/prefixes",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("limit=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_range {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateRange=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_start {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateStart=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_end {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateEnd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("asn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/quality/iqi/summary
// -----------------------------------------------------------------------------

/// GET /radar/quality/iqi/summary.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_quality_index_summary_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_quality_index_summary_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetQualityIndexSummaryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://api.cloudflare.com/client/v4/radar/quality/iqi/summary",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_range {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateRange=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_start {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateStart=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_end {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateEnd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("asn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.continent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("continent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.metric {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("metric=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /radar/quality/iqi/timeseries_groups
// -----------------------------------------------------------------------------

/// GET /radar/quality/iqi/timeseries_groups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = radar_get_quality_index_timeseries_group_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn radar_get_quality_index_timeseries_group_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RadarGetQualityIndexTimeseriesGroupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://api.cloudflare.com/client/v4/radar/quality/iqi/timeseries_groups",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.agg_interval {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("aggInterval=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_range {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateRange=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_start {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateStart=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_end {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dateEnd=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.asn {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("asn=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.continent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("continent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interpolation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interpolation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.metric {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("metric=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.format {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("format=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET /zones/{zone_id}/spectrum/analytics/aggregate/current
// -----------------------------------------------------------------------------

/// GET /zones/{zone_id}/spectrum/analytics/aggregate/current.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = spectrum_aggregate_analytics_get_current_aggregated_analytics_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn spectrum_aggregate_analytics_get_current_aggregated_analytics_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SpectrumAggregateAnalyticsGetCurrentAggregatedAnalyticsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SpectrumAnalyticsQueryResponseAggregate>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_netio::simple_http::client::shared::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/zones/{}/spectrum/analytics/aggregate/current",
        args.zone_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.app_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("appID=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.colo_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("colo_name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(
            |e: foundation_netio::simple_http::shared::HttpClientError| {
                super::shared::ApiError::RequestBuildFailed(e.to_string())
            },
        )?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_netio::simple_http::client::shared::body_reader::collect_string(
                        stream,
                    );
                let parsed: SpectrumAnalyticsQueryResponseAggregate = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
