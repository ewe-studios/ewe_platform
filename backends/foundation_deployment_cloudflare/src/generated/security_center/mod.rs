//! Auto-generated API module for cloudflare security_center.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `cloudflare_security_center `

#![cfg(feature = "cloudflare_security_center")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(clippy::missing_errors_doc, clippy::doc_markdown, clippy::useless_format)]
#![allow(unused_imports)]

use foundation_netio::{DynNetClient, PreparedRequestBuilder};
use foundation_netio::shared::client::http_client::HttpClient;
use serde::{Deserialize, Serialize};
use foundation_macros::JsonHash;

// Import shared types used by this module
use super::shared::SecurityCenterValueCountsResponse;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`get-security-center-insights_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetSecurityCenterInsightsArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `dismissed`.
    pub dismissed: Option<String>,
    /// Query parameter: `issue_class`.
    pub issue_class: Option<String>,
    /// Query parameter: `issue_type`.
    pub issue_type: Option<String>,
    /// Query parameter: `product`.
    pub product: Option<String>,
    /// Query parameter: `severity`.
    pub severity: Option<String>,
    /// Query parameter: `subject`.
    pub subject: Option<String>,
    /// Query parameter: `issue_class~neq`.
    pub issue_class_neq: Option<String>,
    /// Query parameter: `issue_type~neq`.
    pub issue_type_neq: Option<String>,
    /// Query parameter: `product~neq`.
    pub product_neq: Option<String>,
    /// Query parameter: `severity~neq`.
    pub severity_neq: Option<String>,
    /// Query parameter: `subject~neq`.
    pub subject_neq: Option<String>,
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
}

/// Arguments for [`get-security-center-insight-counts-by-class_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetSecurityCenterInsightCountsByClassArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `dismissed`.
    pub dismissed: Option<String>,
    /// Query parameter: `issue_class`.
    pub issue_class: Option<String>,
    /// Query parameter: `issue_type`.
    pub issue_type: Option<String>,
    /// Query parameter: `product`.
    pub product: Option<String>,
    /// Query parameter: `severity`.
    pub severity: Option<String>,
    /// Query parameter: `subject`.
    pub subject: Option<String>,
    /// Query parameter: `issue_class~neq`.
    pub issue_class_neq: Option<String>,
    /// Query parameter: `issue_type~neq`.
    pub issue_type_neq: Option<String>,
    /// Query parameter: `product~neq`.
    pub product_neq: Option<String>,
    /// Query parameter: `severity~neq`.
    pub severity_neq: Option<String>,
    /// Query parameter: `subject~neq`.
    pub subject_neq: Option<String>,
}

/// Arguments for [`get-security-center-insight-counts-by-severity_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetSecurityCenterInsightCountsBySeverityArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `dismissed`.
    pub dismissed: Option<String>,
    /// Query parameter: `issue_class`.
    pub issue_class: Option<String>,
    /// Query parameter: `issue_type`.
    pub issue_type: Option<String>,
    /// Query parameter: `product`.
    pub product: Option<String>,
    /// Query parameter: `severity`.
    pub severity: Option<String>,
    /// Query parameter: `subject`.
    pub subject: Option<String>,
    /// Query parameter: `issue_class~neq`.
    pub issue_class_neq: Option<String>,
    /// Query parameter: `issue_type~neq`.
    pub issue_type_neq: Option<String>,
    /// Query parameter: `product~neq`.
    pub product_neq: Option<String>,
    /// Query parameter: `severity~neq`.
    pub severity_neq: Option<String>,
    /// Query parameter: `subject~neq`.
    pub subject_neq: Option<String>,
}

/// Arguments for [`get-security-center-insight-counts-by-type_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetSecurityCenterInsightCountsByTypeArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Query parameter: `dismissed`.
    pub dismissed: Option<String>,
    /// Query parameter: `issue_class`.
    pub issue_class: Option<String>,
    /// Query parameter: `issue_type`.
    pub issue_type: Option<String>,
    /// Query parameter: `product`.
    pub product: Option<String>,
    /// Query parameter: `severity`.
    pub severity: Option<String>,
    /// Query parameter: `subject`.
    pub subject: Option<String>,
    /// Query parameter: `issue_class~neq`.
    pub issue_class_neq: Option<String>,
    /// Query parameter: `issue_type~neq`.
    pub issue_type_neq: Option<String>,
    /// Query parameter: `product~neq`.
    pub product_neq: Option<String>,
    /// Query parameter: `severity~neq`.
    pub severity_neq: Option<String>,
    /// Query parameter: `subject~neq`.
    pub subject_neq: Option<String>,
}

/// Arguments for [`get-security-center-insight-context_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct GetSecurityCenterInsightContextArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `issue_id`.
    pub issue_id: String,
}

/// Arguments for [`archive-security-center-insight_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ArchiveSecurityCenterInsightArgs {
    /// Path parameter: `account_id`.
    pub account_id: String,
    /// Path parameter: `issue_id`.
    pub issue_id: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/security-center/insights
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/security-center/insights.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = get_security_center_insights_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn get_security_center_insights_request<F>(
    client: DynNetClient,
    args: &GetSecurityCenterInsightsArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/security-center/insights",
        args.account_id,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("dismissed", args.dismissed.as_deref());
    builder = builder.query("issue_class", args.issue_class.as_deref());
    builder = builder.query("issue_type", args.issue_type.as_deref());
    builder = builder.query("product", args.product.as_deref());
    builder = builder.query("severity", args.severity.as_deref());
    builder = builder.query("subject", args.subject.as_deref());
    builder = builder.query("issue_class~neq", args.issue_class_neq.as_deref());
    builder = builder.query("issue_type~neq", args.issue_type_neq.as_deref());
    builder = builder.query("product~neq", args.product_neq.as_deref());
    builder = builder.query("severity~neq", args.severity_neq.as_deref());
    builder = builder.query("subject~neq", args.subject_neq.as_deref());
    builder = builder.query("page", args.page.as_deref());
    builder = builder.query("per_page", args.per_page.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/security-center/insights/class
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/security-center/insights/class.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = get_security_center_insight_counts_by_class_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn get_security_center_insight_counts_by_class_request<F>(
    client: DynNetClient,
    args: &GetSecurityCenterInsightCountsByClassArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<SecurityCenterValueCountsResponse>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/security-center/insights/class",
        args.account_id,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("dismissed", args.dismissed.as_deref());
    builder = builder.query("issue_class", args.issue_class.as_deref());
    builder = builder.query("issue_type", args.issue_type.as_deref());
    builder = builder.query("product", args.product.as_deref());
    builder = builder.query("severity", args.severity.as_deref());
    builder = builder.query("subject", args.subject.as_deref());
    builder = builder.query("issue_class~neq", args.issue_class_neq.as_deref());
    builder = builder.query("issue_type~neq", args.issue_type_neq.as_deref());
    builder = builder.query("product~neq", args.product_neq.as_deref());
    builder = builder.query("severity~neq", args.severity_neq.as_deref());
    builder = builder.query("subject~neq", args.subject_neq.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    let body_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
    let parsed: SecurityCenterValueCountsResponse = serde_json::from_slice(&body_bytes).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
    Ok(ApiResponse { status: status as u16, headers, body: parsed })
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/security-center/insights/severity
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/security-center/insights/severity.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = get_security_center_insight_counts_by_severity_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn get_security_center_insight_counts_by_severity_request<F>(
    client: DynNetClient,
    args: &GetSecurityCenterInsightCountsBySeverityArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<SecurityCenterValueCountsResponse>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/security-center/insights/severity",
        args.account_id,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("dismissed", args.dismissed.as_deref());
    builder = builder.query("issue_class", args.issue_class.as_deref());
    builder = builder.query("issue_type", args.issue_type.as_deref());
    builder = builder.query("product", args.product.as_deref());
    builder = builder.query("severity", args.severity.as_deref());
    builder = builder.query("subject", args.subject.as_deref());
    builder = builder.query("issue_class~neq", args.issue_class_neq.as_deref());
    builder = builder.query("issue_type~neq", args.issue_type_neq.as_deref());
    builder = builder.query("product~neq", args.product_neq.as_deref());
    builder = builder.query("severity~neq", args.severity_neq.as_deref());
    builder = builder.query("subject~neq", args.subject_neq.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    let body_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
    let parsed: SecurityCenterValueCountsResponse = serde_json::from_slice(&body_bytes).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
    Ok(ApiResponse { status: status as u16, headers, body: parsed })
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/security-center/insights/type
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/security-center/insights/type.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = get_security_center_insight_counts_by_type_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn get_security_center_insight_counts_by_type_request<F>(
    client: DynNetClient,
    args: &GetSecurityCenterInsightCountsByTypeArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<SecurityCenterValueCountsResponse>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/security-center/insights/type",
        args.account_id,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("dismissed", args.dismissed.as_deref());
    builder = builder.query("issue_class", args.issue_class.as_deref());
    builder = builder.query("issue_type", args.issue_type.as_deref());
    builder = builder.query("product", args.product.as_deref());
    builder = builder.query("severity", args.severity.as_deref());
    builder = builder.query("subject", args.subject.as_deref());
    builder = builder.query("issue_class~neq", args.issue_class_neq.as_deref());
    builder = builder.query("issue_type~neq", args.issue_type_neq.as_deref());
    builder = builder.query("product~neq", args.product_neq.as_deref());
    builder = builder.query("severity~neq", args.severity_neq.as_deref());
    builder = builder.query("subject~neq", args.subject_neq.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    let body_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
    let parsed: SecurityCenterValueCountsResponse = serde_json::from_slice(&body_bytes).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
    Ok(ApiResponse { status: status as u16, headers, body: parsed })
}

// -----------------------------------------------------------------------------
// GET /accounts/{account_id}/security-center/insights/{issue_id}/context
// -----------------------------------------------------------------------------

/// GET /accounts/{account_id}/security-center/insights/{issue_id}/context.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = get_security_center_insight_context_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn get_security_center_insight_context_request<F>(
    client: DynNetClient,
    args: &GetSecurityCenterInsightContextArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/security-center/insights/{}/context",
        args.account_id,
        args.issue_id,
    );

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

// -----------------------------------------------------------------------------
// PUT /accounts/{account_id}/security-center/insights/{issue_id}/dismiss
// -----------------------------------------------------------------------------

/// PUT /accounts/{account_id}/security-center/insights/{issue_id}/dismiss.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = archive_security_center_insight_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn archive_security_center_insight_request<F>(
    client: DynNetClient,
    args: &ArchiveSecurityCenterInsightArgs,
    builder_mod: Option<F>,
) -> Result<ApiResponse<()>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let endpoint_url = format!(
        "https://api.cloudflare.com/client/v4/accounts/{}/security-center/insights/{}/dismiss",
        args.account_id,
        args.issue_id,
    );

    let mut builder = PreparedRequestBuilder::put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body: None });
    }
    Ok(ApiResponse { status: status as u16, headers, body: () })
}

