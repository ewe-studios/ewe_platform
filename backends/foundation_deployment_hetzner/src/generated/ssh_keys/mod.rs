//! Auto-generated API module for hetzner ssh_keys.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `hetzner_ssh_keys `

#![cfg(feature = "hetzner_ssh_keys")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(clippy::missing_errors_doc, clippy::doc_markdown, clippy::useless_format)]
#![allow(unused_imports)]

use foundation_netio::{DynNetClient, PreparedRequestBuilder};
use foundation_netio::shared::client::http_client::HttpClient;
use serde::{Deserialize, Serialize};
use foundation_macros::JsonHash;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ListSshKeysResponseMetaPagination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListSshKeysResponseMetaPagination {
    /// last_page property.
    pub last_page: i64,
    /// next_page property.
    pub next_page: i64,
    /// page property.
    pub page: i64,
    /// per_page property.
    pub per_page: i64,
    /// previous_page property.
    pub previous_page: i64,
    /// total_entries property.
    pub total_entries: i64,
}

/// `ListSshKeysResponseMeta` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListSshKeysResponseMeta {
    /// pagination property.
    pub pagination: ListSshKeysResponseMetaPagination,
}

/// `ListSshKeysResponseSshKeysItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListSshKeysResponseSshKeysItem {
    /// created property.
    pub created: String,
    /// fingerprint property.
    pub fingerprint: String,
    /// id property.
    pub id: i64,
    /// labels property.
    pub labels: serde_json::Value,
    /// name property.
    pub name: String,
    /// public_key property.
    pub public_key: String,
}

/// `CreateSshKeyResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreateSshKeyResponse {
    /// ssh_key property.
    pub ssh_key: CreateSshKeyResponseSshKey,
}

/// `CreateSshKeyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreateSshKeyRequest {
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: String,
    /// public_key property.
    pub public_key: String,
}

/// `CreateSshKeyResponseSshKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreateSshKeyResponseSshKey {
    /// created property.
    pub created: String,
    /// fingerprint property.
    pub fingerprint: String,
    /// id property.
    pub id: i64,
    /// labels property.
    pub labels: serde_json::Value,
    /// name property.
    pub name: String,
    /// public_key property.
    pub public_key: String,
}

/// `ListSshKeysResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListSshKeysResponse {
    /// meta property.
    pub meta: ListSshKeysResponseMeta,
    /// ssh_keys property.
    pub ssh_keys: Vec<ListSshKeysResponseSshKeysItem>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`list_ssh_keys_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ListSshKeysArgs {
    /// Query parameter: `sort`.
    pub sort: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `fingerprint`.
    pub fingerprint: Option<String>,
    /// Query parameter: `label_selector`.
    pub label_selector: Option<String>,
    /// Query parameter: `page`.
    pub page: Option<String>,
    /// Query parameter: `per_page`.
    pub per_page: Option<String>,
}

/// Arguments for [`create_ssh_key_request`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CreateSshKeyArgs {
    /// Request body.
    pub body: CreateSshKeyRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET /ssh_keys
// -----------------------------------------------------------------------------

/// GET /ssh_keys.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = list_ssh_keys_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn list_ssh_keys_request<F>(
    client: DynNetClient,
    args: &ListSshKeysArgs,
    base_url: &str,
    builder_mod: Option<F>,
) -> Result<ApiResponse<ListSshKeysResponse>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let path = format!("/ssh_keys",
    );
    let endpoint_url = format!("{}{}", base_url, path);

    let mut builder = PreparedRequestBuilder::get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.query("sort", args.sort.as_deref());
    builder = builder.query("name", args.name.as_deref());
    builder = builder.query("fingerprint", args.fingerprint.as_deref());
    builder = builder.query("label_selector", args.label_selector.as_deref());
    builder = builder.query("page", args.page.as_deref());
    builder = builder.query("per_page", args.per_page.as_deref());

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        let error_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
        let body = (!error_bytes.is_empty())
            .then(|| String::from_utf8_lossy(&error_bytes).into_owned());
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body });
    }
    let body_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
    let parsed: ListSshKeysResponse = serde_json::from_slice(&body_bytes).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
    Ok(ApiResponse { status: status as u16, headers, body: parsed })
}

// -----------------------------------------------------------------------------
// POST /ssh_keys
// -----------------------------------------------------------------------------

/// POST /ssh_keys.
///
/// Takes a `DynNetClient` and args, builds the request, optionally applies
/// modifications, then sends it via `send_async().await`.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let response = create_ssh_key_request(client.clone(), &args, Some(|b: &mut PreparedRequestBuilder| {
///     b.header("X-Custom-Header", "value");
/// })).await?;
/// ```
pub async fn create_ssh_key_request<F>(
    client: DynNetClient,
    args: &CreateSshKeyArgs,
    base_url: &str,
    builder_mod: Option<F>,
) -> Result<ApiResponse<CreateSshKeyResponse>, super::shared::ApiError>
where
    F: FnOnce(&mut PreparedRequestBuilder),
{
    let path = format!("/ssh_keys",
    );
    let endpoint_url = format!("{}{}", base_url, path);

    let mut builder = PreparedRequestBuilder::post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder.body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    let response = client.send_async(builder.build()).await
        .map_err(|e| super::shared::ApiError::RequestSendFailed(e.to_string()))?;

    let status: usize = response.get_status().into();
    let headers = response.get_headers_ref().clone();
    if status < 200 || status >= 300 {
        let error_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
        let body = (!error_bytes.is_empty())
            .then(|| String::from_utf8_lossy(&error_bytes).into_owned());
        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers, body });
    }
    let body_bytes = foundation_netio::shared::client::body_reader::collect_bytes_from_send_safe(response.take_body());
    let parsed: CreateSshKeyResponse = serde_json::from_slice(&body_bytes).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
    Ok(ApiResponse { status: status as u16, headers, body: parsed })
}

