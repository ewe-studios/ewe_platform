//! Auto-generated API module for gcp/sqladmin sqladmin.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_sqladmin_sqladmin `

#![cfg(feature = "gcp_sqladmin_sqladmin")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SqlInstancesVerifyExternalSyncSettingsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlInstancesVerifyExternalSyncSettingsResponse {
    /// errors property.
    pub errors: Option<Vec<SqlExternalSyncSettingError>>,
    /// kind property.
    pub kind: Option<String>,
    /// warnings property.
    pub warnings: Option<Vec<SqlExternalSyncSettingError>>,
}

/// `TargetMetric` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetMetric {
    /// metric property.
    pub metric: Option<String>,
    /// targetValue property.
    pub target_value: Option<f64>,
}

/// `InstancesCloneRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesCloneRequest {
    /// cloneContext property.
    pub clone_context: Option<CloneContext>,
}

/// `SqlInstancesResetReplicaSizeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlInstancesResetReplicaSizeRequest {}

/// `MySqlSyncConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MySqlSyncConfig {
    /// initialSyncFlags property.
    pub initial_sync_flags: Option<Vec<SyncFlags>>,
}

/// `DatabaseInstance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatabaseInstance {
    /// availableMaintenanceVersions property.
    pub available_maintenance_versions: Option<Vec<String>>,
    /// backendType property.
    pub backend_type: Option<String>,
    /// connectionName property.
    pub connection_name: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// currentDiskSize property.
    pub current_disk_size: Option<String>,
    /// databaseInstalledVersion property.
    pub database_installed_version: Option<String>,
    /// databaseVersion property.
    pub database_version: Option<String>,
    /// diskEncryptionConfiguration property.
    pub disk_encryption_configuration: Option<DiskEncryptionConfiguration>,
    /// diskEncryptionStatus property.
    pub disk_encryption_status: Option<DiskEncryptionStatus>,
    /// dnsName property.
    pub dns_name: Option<String>,
    /// dnsNames property.
    pub dns_names: Option<Vec<DnsNameMapping>>,
    /// etag property.
    pub etag: Option<String>,
    /// failoverReplica property.
    pub failover_replica: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// gceZone property.
    pub gce_zone: Option<String>,
    /// geminiConfig property.
    pub gemini_config: Option<GeminiInstanceConfig>,
    /// includeReplicasForMajorVersionUpgrade property.
    pub include_replicas_for_major_version_upgrade: Option<bool>,
    /// instanceType property.
    pub instance_type: Option<String>,
    /// ipAddresses property.
    pub ip_addresses: Option<Vec<IpMapping>>,
    /// ipv6Address property.
    pub ipv6_address: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// maintenanceVersion property.
    pub maintenance_version: Option<String>,
    /// masterInstanceName property.
    pub master_instance_name: Option<String>,
    /// maxDiskSize property.
    pub max_disk_size: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nodeCount property.
    pub node_count: Option<i64>,
    /// nodes property.
    pub nodes: Option<Vec<PoolNodeConfig>>,
    /// onPremisesConfiguration property.
    pub on_premises_configuration: Option<OnPremisesConfiguration>,
    /// outOfDiskReport property.
    pub out_of_disk_report: Option<SqlOutOfDiskReport>,
    /// primaryDnsName property.
    pub primary_dns_name: Option<String>,
    /// project property.
    pub project: Option<String>,
    /// pscServiceAttachmentLink property.
    pub psc_service_attachment_link: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// replicaConfiguration property.
    pub replica_configuration: Option<ReplicaConfiguration>,
    /// replicaNames property.
    pub replica_names: Option<Vec<String>>,
    /// replicationCluster property.
    pub replication_cluster: Option<ReplicationCluster>,
    /// rootPassword property.
    pub root_password: Option<String>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// scheduledMaintenance property.
    pub scheduled_maintenance: Option<SqlScheduledMaintenance>,
    /// secondaryGceZone property.
    pub secondary_gce_zone: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// serverCaCert property.
    pub server_ca_cert: Option<SslCert>,
    /// serviceAccountEmailAddress property.
    pub service_account_email_address: Option<String>,
    /// settings property.
    pub settings: Option<Settings>,
    /// sqlNetworkArchitecture property.
    pub sql_network_architecture: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// suspensionReason property.
    pub suspension_reason: Option<Vec<String>>,
    /// switchTransactionLogsToCloudStorageEnabled property.
    pub switch_transaction_logs_to_cloud_storage_enabled: Option<bool>,
    /// tags property.
    pub tags: Option<serde_json::Value>,
    /// upgradableDatabaseVersions property.
    pub upgradable_database_versions: Option<Vec<AvailableDatabaseVersion>>,
    /// writeEndpoint property.
    pub write_endpoint: Option<String>,
}

/// `DiskEncryptionStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskEncryptionStatus {
    /// kind property.
    pub kind: Option<String>,
    /// kmsKeyVersionName property.
    pub kms_key_version_name: Option<String>,
}

/// `Value` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Value {
    /// nullValue property.
    pub null_value: Option<bool>,
    /// value property.
    pub value: Option<String>,
}

/// `DataCacheConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataCacheConfig {
    /// dataCacheEnabled property.
    pub data_cache_enabled: Option<bool>,
}

/// `SqlSubOperationType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlSubOperationType {
    /// maintenanceType property.
    pub maintenance_type: Option<String>,
}

/// `InstancesRotateServerCertificateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesRotateServerCertificateRequest {
    /// rotateServerCertificateContext property.
    pub rotate_server_certificate_context: Option<RotateServerCertificateContext>,
}

/// `AdvancedMachineFeatures` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvancedMachineFeatures {
    /// threadsPerCore property.
    pub threads_per_core: Option<i64>,
}

/// `InstancesExportRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesExportRequest {
    /// exportContext property.
    pub export_context: Option<ExportContext>,
}

/// `ListBackupsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListBackupsResponse {
    /// backups property.
    pub backups: Option<Vec<Backup>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// warnings property.
    pub warnings: Option<Vec<ApiWarning>>,
}

/// `SqlInstancesExecuteSqlResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlInstancesExecuteSqlResponse {
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// metadata property.
    pub metadata: Option<Metadata>,
    /// results property.
    pub results: Option<Vec<QueryResult>>,
    /// status property.
    pub status: Option<Status>,
}

/// `Row` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Row {
    /// values property.
    pub values: Option<Vec<Value>>,
}

/// `MySqlReplicaConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MySqlReplicaConfiguration {
    /// caCertificate property.
    pub ca_certificate: Option<String>,
    /// clientCertificate property.
    pub client_certificate: Option<String>,
    /// clientKey property.
    pub client_key: Option<String>,
    /// connectRetryInterval property.
    pub connect_retry_interval: Option<i64>,
    /// dumpFilePath property.
    pub dump_file_path: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// masterHeartbeatPeriod property.
    pub master_heartbeat_period: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// sslCipher property.
    pub ssl_cipher: Option<String>,
    /// username property.
    pub username: Option<String>,
    /// verifyServerCertificate property.
    pub verify_server_certificate: Option<bool>,
}

/// `SqlInstancesGetDiskShrinkConfigResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlInstancesGetDiskShrinkConfigResponse {
    /// kind property.
    pub kind: Option<String>,
    /// message property.
    pub message: Option<String>,
    /// minimalTargetSizeGb property.
    pub minimal_target_size_gb: Option<String>,
}

/// `ApiWarning` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApiWarning {
    /// code property.
    pub code: Option<String>,
    /// message property.
    pub message: Option<String>,
    /// region property.
    pub region: Option<String>,
}

/// `Reschedule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Reschedule {
    /// rescheduleType property.
    pub reschedule_type: Option<String>,
    /// scheduleTime property.
    pub schedule_time: Option<String>,
}

/// `GenerateEphemeralCertResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateEphemeralCertResponse {
    /// ephemeralCert property.
    pub ephemeral_cert: Option<SslCert>,
}

/// `ImportContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ImportContext {
    /// bakImportOptions property.
    pub bak_import_options: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// csvImportOptions property.
    pub csv_import_options: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// database property.
    pub database: Option<String>,
    /// fileType property.
    pub file_type: Option<String>,
    /// importUser property.
    pub import_user: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// sqlImportOptions property.
    pub sql_import_options: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// tdeImportOptions property.
    pub tde_import_options: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// uri property.
    pub uri: Option<String>,
}

/// `GeminiInstanceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GeminiInstanceConfig {
    /// activeQueryEnabled property.
    pub active_query_enabled: Option<bool>,
    /// entitled property.
    pub entitled: Option<bool>,
    /// flagRecommenderEnabled property.
    pub flag_recommender_enabled: Option<bool>,
    /// googleVacuumMgmtEnabled property.
    pub google_vacuum_mgmt_enabled: Option<bool>,
    /// indexAdvisorEnabled property.
    pub index_advisor_enabled: Option<bool>,
    /// oomSessionCancelEnabled property.
    pub oom_session_cancel_enabled: Option<bool>,
}

/// `RotateEntraIdCertificateContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RotateEntraIdCertificateContext {
    /// kind property.
    pub kind: Option<String>,
    /// nextVersion property.
    pub next_version: Option<String>,
}

/// `ConnectPoolNodeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConnectPoolNodeConfig {
    /// dnsName property.
    pub dns_name: Option<String>,
    /// dnsNames property.
    pub dns_names: Option<Vec<DnsNameMapping>>,
    /// ipAddresses property.
    pub ip_addresses: Option<Vec<IpMapping>>,
    /// name property.
    pub name: Option<String>,
}

/// `InstancesListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesListResponse {
    /// items property.
    pub items: Option<Vec<DatabaseInstance>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// warnings property.
    pub warnings: Option<Vec<ApiWarning>>,
}

/// `SslCert` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslCert {
    /// cert property.
    pub cert: Option<String>,
    /// certSerialNumber property.
    pub cert_serial_number: Option<String>,
    /// commonName property.
    pub common_name: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// expirationTime property.
    pub expiration_time: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// sha1Fingerprint property.
    pub sha1_fingerprint: Option<String>,
}

/// `DnsNameMapping` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DnsNameMapping {
    /// connectionType property.
    pub connection_type: Option<String>,
    /// dnsScope property.
    pub dns_scope: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// recordManager property.
    pub record_manager: Option<String>,
}

/// `TiersListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TiersListResponse {
    /// items property.
    pub items: Option<Vec<Tier>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `OnPremisesConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OnPremisesConfiguration {
    /// caCertificate property.
    pub ca_certificate: Option<String>,
    /// clientCertificate property.
    pub client_certificate: Option<String>,
    /// clientKey property.
    pub client_key: Option<String>,
    /// dumpFilePath property.
    pub dump_file_path: Option<String>,
    /// hostPort property.
    pub host_port: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// selectedObjects property.
    pub selected_objects: Option<Vec<SelectedObjects>>,
    /// sourceInstance property.
    pub source_instance: Option<InstanceReference>,
    /// sslOption property.
    pub ssl_option: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `ReplicaConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReplicaConfiguration {
    /// cascadableReplica property.
    pub cascadable_replica: Option<bool>,
    /// failoverTarget property.
    pub failover_target: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// mysqlReplicaConfiguration property.
    pub mysql_replica_configuration: Option<MySqlReplicaConfiguration>,
}

/// `ConnectSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConnectSettings {
    /// backendType property.
    pub backend_type: Option<String>,
    /// customSubjectAlternativeNames property.
    pub custom_subject_alternative_names: Option<Vec<String>>,
    /// databaseVersion property.
    pub database_version: Option<String>,
    /// dnsName property.
    pub dns_name: Option<String>,
    /// dnsNames property.
    pub dns_names: Option<Vec<DnsNameMapping>>,
    /// ipAddresses property.
    pub ip_addresses: Option<Vec<IpMapping>>,
    /// kind property.
    pub kind: Option<String>,
    /// mdxProtocolSupport property.
    pub mdx_protocol_support: Option<Vec<String>>,
    /// nodeCount property.
    pub node_count: Option<i64>,
    /// nodes property.
    pub nodes: Option<Vec<ConnectPoolNodeConfig>>,
    /// pscEnabled property.
    pub psc_enabled: Option<bool>,
    /// region property.
    pub region: Option<String>,
    /// serverCaCert property.
    pub server_ca_cert: Option<SslCert>,
    /// serverCaMode property.
    pub server_ca_mode: Option<String>,
}

/// `BackupRunsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackupRunsListResponse {
    /// items property.
    pub items: Option<Vec<BackupRun>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Flag` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Flag {
    /// allowedIntValues property.
    pub allowed_int_values: Option<Vec<String>>,
    /// allowedStringValues property.
    pub allowed_string_values: Option<Vec<String>>,
    /// appliesTo property.
    pub applies_to: Option<Vec<String>>,
    /// flagScope property.
    pub flag_scope: Option<String>,
    /// inBeta property.
    pub in_beta: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// maxValue property.
    pub max_value: Option<String>,
    /// minValue property.
    pub min_value: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// recommendedIntValue property.
    pub recommended_int_value: Option<String>,
    /// recommendedStringValue property.
    pub recommended_string_value: Option<String>,
    /// requiresRestart property.
    pub requires_restart: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SqlInstancesStartExternalSyncRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlInstancesStartExternalSyncRequest {
    /// migrationType property.
    pub migration_type: Option<String>,
    /// mysqlSyncConfig property.
    pub mysql_sync_config: Option<MySqlSyncConfig>,
    /// replicaOverwriteEnabled property.
    pub replica_overwrite_enabled: Option<bool>,
    /// skipVerification property.
    pub skip_verification: Option<bool>,
    /// syncMode property.
    pub sync_mode: Option<String>,
    /// syncParallelLevel property.
    pub sync_parallel_level: Option<String>,
}

/// `BackupRun` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackupRun {
    /// backupKind property.
    pub backup_kind: Option<String>,
    /// databaseVersion property.
    pub database_version: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// diskEncryptionConfiguration property.
    pub disk_encryption_configuration: Option<DiskEncryptionConfiguration>,
    /// diskEncryptionStatus property.
    pub disk_encryption_status: Option<DiskEncryptionStatus>,
    /// endTime property.
    pub end_time: Option<String>,
    /// enqueuedTime property.
    pub enqueued_time: Option<String>,
    /// error property.
    pub error: Option<OperationError>,
    /// id property.
    pub id: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// maxChargeableBytes property.
    pub max_chargeable_bytes: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// windowStartTime property.
    pub window_start_time: Option<String>,
}

/// `DiskEncryptionConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskEncryptionConfiguration {
    /// kind property.
    pub kind: Option<String>,
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
}

/// `SslCertDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslCertDetail {
    /// certInfo property.
    pub cert_info: Option<SslCert>,
    /// certPrivateKey property.
    pub cert_private_key: Option<String>,
}

/// `SslCertsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslCertsListResponse {
    /// items property.
    pub items: Option<Vec<SslCert>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Metadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metadata {
    /// sqlStatementExecutionTime property.
    pub sql_statement_execution_time: Option<String>,
}

/// `IpConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IpConfiguration {
    /// allocatedIpRange property.
    pub allocated_ip_range: Option<String>,
    /// authorizedNetworks property.
    pub authorized_networks: Option<Vec<AclEntry>>,
    /// customSubjectAlternativeNames property.
    pub custom_subject_alternative_names: Option<Vec<String>>,
    /// enablePrivatePathForGoogleCloudServices property.
    pub enable_private_path_for_google_cloud_services: Option<bool>,
    /// ipv4Enabled property.
    pub ipv4_enabled: Option<bool>,
    /// privateNetwork property.
    pub private_network: Option<String>,
    /// pscConfig property.
    pub psc_config: Option<PscConfig>,
    /// requireSsl property.
    pub require_ssl: Option<bool>,
    /// serverCaMode property.
    pub server_ca_mode: Option<String>,
    /// serverCaPool property.
    pub server_ca_pool: Option<String>,
    /// serverCertificateRotationMode property.
    pub server_certificate_rotation_mode: Option<String>,
    /// sslMode property.
    pub ssl_mode: Option<String>,
}

/// `LocationPreference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocationPreference {
    /// followGaeApplication property.
    pub follow_gae_application: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// secondaryZone property.
    pub secondary_zone: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `ConnectionPoolFlags` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConnectionPoolFlags {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `SelectedObjects` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SelectedObjects {
    /// database property.
    pub database: Option<String>,
}

/// `PoolNodeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PoolNodeConfig {
    /// dnsName property.
    pub dns_name: Option<String>,
    /// dnsNames property.
    pub dns_names: Option<Vec<DnsNameMapping>>,
    /// gceZone property.
    pub gce_zone: Option<String>,
    /// ipAddresses property.
    pub ip_addresses: Option<Vec<IpMapping>>,
    /// name property.
    pub name: Option<String>,
    /// pscAutoConnections property.
    pub psc_auto_connections: Option<Vec<PscAutoConnectionConfig>>,
    /// pscServiceAttachmentLink property.
    pub psc_service_attachment_link: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `IpMapping` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IpMapping {
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// timeToRetire property.
    pub time_to_retire: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `InstancesFailoverRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesFailoverRequest {
    /// failoverContext property.
    pub failover_context: Option<FailoverContext>,
}

/// `SyncFlags` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SyncFlags {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `SqlOutOfDiskReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlOutOfDiskReport {
    /// sqlMinRecommendedIncreaseSizeGb property.
    pub sql_min_recommended_increase_size_gb: Option<i64>,
    /// sqlOutOfDiskState property.
    pub sql_out_of_disk_state: Option<String>,
}

/// `Backup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Backup {
    /// backupInterval property.
    pub backup_interval: Option<Interval>,
    /// backupKind property.
    pub backup_kind: Option<String>,
    /// backupRun property.
    pub backup_run: Option<String>,
    /// databaseVersion property.
    pub database_version: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// error property.
    pub error: Option<OperationError>,
    /// expiryTime property.
    pub expiry_time: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// instanceDeletionTime property.
    pub instance_deletion_time: Option<String>,
    /// instanceSettings property.
    pub instance_settings: Option<DatabaseInstance>,
    /// kind property.
    pub kind: Option<String>,
    /// kmsKey property.
    pub kms_key: Option<String>,
    /// kmsKeyVersion property.
    pub kms_key_version: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// maxChargeableBytes property.
    pub max_chargeable_bytes: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// ttlDays property.
    pub ttl_days: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `InstancesImportRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesImportRequest {
    /// importContext property.
    pub import_context: Option<ImportContext>,
}

/// `InstancesListEntraIdCertificatesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesListEntraIdCertificatesResponse {
    /// activeVersion property.
    pub active_version: Option<String>,
    /// certs property.
    pub certs: Option<Vec<SslCert>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Database` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Database {
    /// charset property.
    pub charset: Option<String>,
    /// collation property.
    pub collation: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// project property.
    pub project: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// sqlserverDatabaseDetails property.
    pub sqlserver_database_details: Option<SqlServerDatabaseDetails>,
}

/// `Interval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Interval {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `FailoverContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FailoverContext {
    /// kind property.
    pub kind: Option<String>,
    /// settingsVersion property.
    pub settings_version: Option<String>,
}

/// `RotateServerCaContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RotateServerCaContext {
    /// kind property.
    pub kind: Option<String>,
    /// nextVersion property.
    pub next_version: Option<String>,
}

/// `PasswordValidationPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PasswordValidationPolicy {
    /// complexity property.
    pub complexity: Option<String>,
    /// disallowCompromisedCredentials property.
    pub disallow_compromised_credentials: Option<bool>,
    /// disallowUsernameSubstring property.
    pub disallow_username_substring: Option<bool>,
    /// enablePasswordPolicy property.
    pub enable_password_policy: Option<bool>,
    /// minLength property.
    pub min_length: Option<i64>,
    /// passwordChangeInterval property.
    pub password_change_interval: Option<String>,
    /// reuseInterval property.
    pub reuse_interval: Option<i64>,
}

/// `InstancesListServerCertificatesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesListServerCertificatesResponse {
    /// activeVersion property.
    pub active_version: Option<String>,
    /// caCerts property.
    pub ca_certs: Option<Vec<SslCert>>,
    /// kind property.
    pub kind: Option<String>,
    /// serverCerts property.
    pub server_certs: Option<Vec<SslCert>>,
}

/// `PasswordStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PasswordStatus {
    /// locked property.
    pub locked: Option<bool>,
    /// passwordExpirationTime property.
    pub password_expiration_time: Option<String>,
}

/// `CloneContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CloneContext {
    /// allocatedIpRange property.
    pub allocated_ip_range: Option<String>,
    /// binLogCoordinates property.
    pub bin_log_coordinates: Option<BinLogCoordinates>,
    /// databaseNames property.
    pub database_names: Option<Vec<String>>,
    /// destinationInstanceName property.
    pub destination_instance_name: Option<String>,
    /// destinationNetwork property.
    pub destination_network: Option<String>,
    /// destinationProject property.
    pub destination_project: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// pitrTimestampMs property.
    pub pitr_timestamp_ms: Option<String>,
    /// pointInTime property.
    pub point_in_time: Option<String>,
    /// preferredSecondaryZone property.
    pub preferred_secondary_zone: Option<String>,
    /// preferredZone property.
    pub preferred_zone: Option<String>,
    /// sourceInstanceDeletionTime property.
    pub source_instance_deletion_time: Option<String>,
}

/// `RestoreBackupContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RestoreBackupContext {
    /// backupRunId property.
    pub backup_run_id: Option<String>,
    /// instanceId property.
    pub instance_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// project property.
    pub project: Option<String>,
}

/// `SqlServerEntraIdConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerEntraIdConfig {
    /// applicationId property.
    pub application_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// tenantId property.
    pub tenant_id: Option<String>,
}

/// `OperationsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OperationsListResponse {
    /// items property.
    pub items: Option<Vec<Operation>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `InstanceReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceReference {
    /// name property.
    pub name: Option<String>,
    /// project property.
    pub project: Option<String>,
    /// region property.
    pub region: Option<String>,
}

/// `TruncateLogContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TruncateLogContext {
    /// kind property.
    pub kind: Option<String>,
    /// logType property.
    pub log_type: Option<String>,
}

/// `ReadPoolAutoScaleConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReadPoolAutoScaleConfig {
    /// disableScaleIn property.
    pub disable_scale_in: Option<bool>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// maxNodeCount property.
    pub max_node_count: Option<i64>,
    /// minNodeCount property.
    pub min_node_count: Option<i64>,
    /// scaleInCooldownSeconds property.
    pub scale_in_cooldown_seconds: Option<i64>,
    /// scaleOutCooldownSeconds property.
    pub scale_out_cooldown_seconds: Option<i64>,
    /// targetMetrics property.
    pub target_metrics: Option<Vec<TargetMetric>>,
}

/// `ExecuteSqlPayload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecuteSqlPayload {
    /// application property.
    pub application: Option<String>,
    /// autoIamAuthn property.
    pub auto_iam_authn: Option<bool>,
    /// database property.
    pub database: Option<String>,
    /// partialResultMode property.
    pub partial_result_mode: Option<String>,
    /// rowLimit property.
    pub row_limit: Option<String>,
    /// sqlStatement property.
    pub sql_statement: Option<String>,
    /// user property.
    pub user: Option<String>,
}

/// `SqlInstancesGetLatestRecoveryTimeResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlInstancesGetLatestRecoveryTimeResponse {
    /// earliestRecoveryTime property.
    pub earliest_recovery_time: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// latestRecoveryTime property.
    pub latest_recovery_time: Option<String>,
}

/// `SslCertsCreateEphemeralRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslCertsCreateEphemeralRequest {
    /// access_token property.
    pub access_token: Option<String>,
    /// public_key property.
    pub public_key: Option<String>,
}

/// `PscAutoConnectionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PscAutoConnectionConfig {
    /// consumerNetwork property.
    pub consumer_network: Option<String>,
    /// consumerNetworkStatus property.
    pub consumer_network_status: Option<String>,
    /// consumerProject property.
    pub consumer_project: Option<String>,
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `AvailableDatabaseVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AvailableDatabaseVersion {
    /// displayName property.
    pub display_name: Option<String>,
    /// majorVersion property.
    pub major_version: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `GenerateEphemeralCertRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateEphemeralCertRequest {
    /// access_token property.
    pub access_token: Option<String>,
    /// public_key property.
    pub public_key: Option<String>,
    /// readTime property.
    pub read_time: Option<String>,
    /// validDuration property.
    pub valid_duration: Option<String>,
}

/// `FinalBackupConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FinalBackupConfig {
    /// enabled property.
    pub enabled: Option<bool>,
    /// retentionDays property.
    pub retention_days: Option<i64>,
}

/// `InstancesDemoteRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesDemoteRequest {
    /// demoteContext property.
    pub demote_context: Option<DemoteContext>,
}

/// `InstancesRotateEntraIdCertificateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesRotateEntraIdCertificateRequest {
    /// rotateEntraIdCertificateContext property.
    pub rotate_entra_id_certificate_context: Option<RotateEntraIdCertificateContext>,
}

/// `FlagsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlagsListResponse {
    /// items property.
    pub items: Option<Vec<Flag>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `SqlScheduledMaintenance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlScheduledMaintenance {
    /// canDefer property.
    pub can_defer: Option<bool>,
    /// canReschedule property.
    pub can_reschedule: Option<bool>,
    /// scheduleDeadlineTime property.
    pub schedule_deadline_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `BackupRetentionSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackupRetentionSettings {
    /// retainedBackups property.
    pub retained_backups: Option<i64>,
    /// retentionUnit property.
    pub retention_unit: Option<String>,
}

/// `InsightsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InsightsConfig {
    /// enhancedQueryInsightsEnabled property.
    pub enhanced_query_insights_enabled: Option<bool>,
    /// queryInsightsEnabled property.
    pub query_insights_enabled: Option<bool>,
    /// queryPlansPerMinute property.
    pub query_plans_per_minute: Option<i64>,
    /// queryStringLength property.
    pub query_string_length: Option<i64>,
    /// recordApplicationTags property.
    pub record_application_tags: Option<bool>,
    /// recordClientAddress property.
    pub record_client_address: Option<bool>,
}

/// `PreCheckMajorVersionUpgradeContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreCheckMajorVersionUpgradeContext {
    /// kind property.
    pub kind: Option<String>,
    /// preCheckResponse property.
    pub pre_check_response: Option<Vec<PreCheckResponse>>,
    /// targetDatabaseVersion property.
    pub target_database_version: Option<String>,
}

/// `DemoteMasterContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DemoteMasterContext {
    /// kind property.
    pub kind: Option<String>,
    /// masterInstanceName property.
    pub master_instance_name: Option<String>,
    /// replicaConfiguration property.
    pub replica_configuration: Option<DemoteMasterConfiguration>,
    /// skipReplicationSetup property.
    pub skip_replication_setup: Option<bool>,
    /// verifyGtidConsistency property.
    pub verify_gtid_consistency: Option<bool>,
}

/// `InstancesTruncateLogRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesTruncateLogRequest {
    /// truncateLogContext property.
    pub truncate_log_context: Option<TruncateLogContext>,
}

/// `User` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct User {
    /// databaseRoles property.
    pub database_roles: Option<Vec<String>>,
    /// dualPasswordType property.
    pub dual_password_type: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// host property.
    pub host: Option<String>,
    /// iamEmail property.
    pub iam_email: Option<String>,
    /// iamStatus property.
    pub iam_status: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// passwordPolicy property.
    pub password_policy: Option<UserPasswordValidationPolicy>,
    /// project property.
    pub project: Option<String>,
    /// sqlserverUserDetails property.
    pub sqlserver_user_details: Option<SqlServerUserDetails>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Message` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Message {
    /// message property.
    pub message: Option<String>,
    /// severity property.
    pub severity: Option<String>,
}

/// `InstancesReencryptRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesReencryptRequest {
    /// backupReencryptionConfig property.
    pub backup_reencryption_config: Option<BackupReencryptionConfig>,
}

/// `SqlInstancesRescheduleMaintenanceRequestBody` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlInstancesRescheduleMaintenanceRequestBody {
    /// reschedule property.
    pub reschedule: Option<Reschedule>,
}

/// `SslCertsInsertRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslCertsInsertRequest {
    /// commonName property.
    pub common_name: Option<String>,
}

/// `InstancesRestoreBackupRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesRestoreBackupRequest {
    /// backup property.
    pub backup: Option<String>,
    /// backupdrBackup property.
    pub backupdr_backup: Option<String>,
    /// restoreBackupContext property.
    pub restore_backup_context: Option<RestoreBackupContext>,
    /// restoreInstanceClearOverridesFieldNames property.
    pub restore_instance_clear_overrides_field_names: Option<Vec<String>>,
    /// restoreInstanceSettings property.
    pub restore_instance_settings: Option<DatabaseInstance>,
}

/// `ReplicationCluster` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReplicationCluster {
    /// drReplica property.
    pub dr_replica: Option<bool>,
    /// failoverDrReplicaName property.
    pub failover_dr_replica_name: Option<String>,
    /// psaWriteEndpoint property.
    pub psa_write_endpoint: Option<String>,
}

/// `PerformanceCaptureConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PerformanceCaptureConfig {
    /// enabled property.
    pub enabled: Option<bool>,
    /// probeThreshold property.
    pub probe_threshold: Option<i64>,
    /// probingIntervalSeconds property.
    pub probing_interval_seconds: Option<i64>,
    /// runningThreadsThreshold property.
    pub running_threads_threshold: Option<i64>,
    /// secondsBehindSourceThreshold property.
    pub seconds_behind_source_threshold: Option<i64>,
    /// transactionDurationThreshold property.
    pub transaction_duration_threshold: Option<i64>,
}

/// `BackupConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackupConfiguration {
    /// backupRetentionSettings property.
    pub backup_retention_settings: Option<BackupRetentionSettings>,
    /// backupTier property.
    pub backup_tier: Option<String>,
    /// binaryLogEnabled property.
    pub binary_log_enabled: Option<bool>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// pointInTimeRecoveryEnabled property.
    pub point_in_time_recovery_enabled: Option<bool>,
    /// replicationLogArchivingEnabled property.
    pub replication_log_archiving_enabled: Option<bool>,
    /// startTime property.
    pub start_time: Option<String>,
    /// transactionLogRetentionDays property.
    pub transaction_log_retention_days: Option<i64>,
    /// transactionalLogStorageState property.
    pub transactional_log_storage_state: Option<String>,
}

/// `SslCertsInsertResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SslCertsInsertResponse {
    /// clientCert property.
    pub client_cert: Option<SslCertDetail>,
    /// kind property.
    pub kind: Option<String>,
    /// operation property.
    pub operation: Option<Operation>,
    /// serverCaCert property.
    pub server_ca_cert: Option<SslCert>,
}

/// `SqlServerUserDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerUserDetails {
    /// disabled property.
    pub disabled: Option<bool>,
    /// serverRoles property.
    pub server_roles: Option<Vec<String>>,
}

/// `OperationError` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OperationError {
    /// code property.
    pub code: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// message property.
    pub message: Option<String>,
}

/// `DemoteContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DemoteContext {
    /// kind property.
    pub kind: Option<String>,
    /// sourceRepresentativeInstanceName property.
    pub source_representative_instance_name: Option<String>,
}

/// `PerformDiskShrinkContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PerformDiskShrinkContext {
    /// targetSizeGb property.
    pub target_size_gb: Option<String>,
}

/// `InstancesDemoteMasterRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesDemoteMasterRequest {
    /// demoteMasterContext property.
    pub demote_master_context: Option<DemoteMasterContext>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `OperationErrors` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OperationErrors {
    /// errors property.
    pub errors: Option<Vec<OperationError>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `SqlExternalSyncSettingError` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlExternalSyncSettingError {
    /// detail property.
    pub detail: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `DatabaseFlags` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatabaseFlags {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `BackupContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackupContext {
    /// backupId property.
    pub backup_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `DemoteMasterMySqlReplicaConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DemoteMasterMySqlReplicaConfiguration {
    /// caCertificate property.
    pub ca_certificate: Option<String>,
    /// clientCertificate property.
    pub client_certificate: Option<String>,
    /// clientKey property.
    pub client_key: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `InstancesAcquireSsrsLeaseRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesAcquireSsrsLeaseRequest {
    /// acquireSsrsLeaseContext property.
    pub acquire_ssrs_lease_context: Option<AcquireSsrsLeaseContext>,
}

/// `PreCheckResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreCheckResponse {
    /// actionsRequired property.
    pub actions_required: Option<Vec<String>>,
    /// message property.
    pub message: Option<String>,
    /// messageType property.
    pub message_type: Option<String>,
}

/// `DenyMaintenancePeriod` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DenyMaintenancePeriod {
    /// endDate property.
    pub end_date: Option<String>,
    /// startDate property.
    pub start_date: Option<String>,
    /// time property.
    pub time: Option<String>,
}

/// `ConnectionPoolConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConnectionPoolConfig {
    /// connectionPoolingEnabled property.
    pub connection_pooling_enabled: Option<bool>,
    /// flags property.
    pub flags: Option<Vec<ConnectionPoolFlags>>,
    /// poolerCount property.
    pub pooler_count: Option<i64>,
}

/// `InstancesRotateServerCaRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesRotateServerCaRequest {
    /// rotateServerCaContext property.
    pub rotate_server_ca_context: Option<RotateServerCaContext>,
}

/// `Settings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Settings {
    /// acceleratedReplicaMode property.
    pub accelerated_replica_mode: Option<bool>,
    /// activationPolicy property.
    pub activation_policy: Option<String>,
    /// activeDirectoryConfig property.
    pub active_directory_config: Option<SqlActiveDirectoryConfig>,
    /// advancedMachineFeatures property.
    pub advanced_machine_features: Option<AdvancedMachineFeatures>,
    /// authorizedGaeApplications property.
    pub authorized_gae_applications: Option<Vec<String>>,
    /// autoUpgradeEnabled property.
    pub auto_upgrade_enabled: Option<bool>,
    /// availabilityType property.
    pub availability_type: Option<String>,
    /// backupConfiguration property.
    pub backup_configuration: Option<BackupConfiguration>,
    /// collation property.
    pub collation: Option<String>,
    /// connectionPoolConfig property.
    pub connection_pool_config: Option<ConnectionPoolConfig>,
    /// connectorEnforcement property.
    pub connector_enforcement: Option<String>,
    /// crashSafeReplicationEnabled property.
    pub crash_safe_replication_enabled: Option<bool>,
    /// dataApiAccess property.
    pub data_api_access: Option<String>,
    /// dataCacheConfig property.
    pub data_cache_config: Option<DataCacheConfig>,
    /// dataDiskProvisionedIops property.
    pub data_disk_provisioned_iops: Option<String>,
    /// dataDiskProvisionedThroughput property.
    pub data_disk_provisioned_throughput: Option<String>,
    /// dataDiskSizeGb property.
    pub data_disk_size_gb: Option<String>,
    /// dataDiskType property.
    pub data_disk_type: Option<String>,
    /// databaseFlags property.
    pub database_flags: Option<Vec<DatabaseFlags>>,
    /// databaseReplicationEnabled property.
    pub database_replication_enabled: Option<bool>,
    /// deletionProtectionEnabled property.
    pub deletion_protection_enabled: Option<bool>,
    /// denyMaintenancePeriods property.
    pub deny_maintenance_periods: Option<Vec<DenyMaintenancePeriod>>,
    /// edition property.
    pub edition: Option<String>,
    /// enableDataplexIntegration property.
    pub enable_dataplex_integration: Option<bool>,
    /// enableGoogleMlIntegration property.
    pub enable_google_ml_integration: Option<bool>,
    /// entraidConfig property.
    pub entraid_config: Option<SqlServerEntraIdConfig>,
    /// finalBackupConfig property.
    pub final_backup_config: Option<FinalBackupConfig>,
    /// insightsConfig property.
    pub insights_config: Option<InsightsConfig>,
    /// ipConfiguration property.
    pub ip_configuration: Option<IpConfiguration>,
    /// kind property.
    pub kind: Option<String>,
    /// locationPreference property.
    pub location_preference: Option<LocationPreference>,
    /// maintenanceWindow property.
    pub maintenance_window: Option<MaintenanceWindow>,
    /// passwordValidationPolicy property.
    pub password_validation_policy: Option<PasswordValidationPolicy>,
    /// performanceCaptureConfig property.
    pub performance_capture_config: Option<PerformanceCaptureConfig>,
    /// pricingPlan property.
    pub pricing_plan: Option<String>,
    /// readPoolAutoScaleConfig property.
    pub read_pool_auto_scale_config: Option<ReadPoolAutoScaleConfig>,
    /// replicationLagMaxSeconds property.
    pub replication_lag_max_seconds: Option<i64>,
    /// replicationType property.
    pub replication_type: Option<String>,
    /// retainBackupsOnDelete property.
    pub retain_backups_on_delete: Option<bool>,
    /// settingsVersion property.
    pub settings_version: Option<String>,
    /// sqlServerAuditConfig property.
    pub sql_server_audit_config: Option<SqlServerAuditConfig>,
    /// storageAutoResize property.
    pub storage_auto_resize: Option<bool>,
    /// storageAutoResizeLimit property.
    pub storage_auto_resize_limit: Option<String>,
    /// tier property.
    pub tier: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// userLabels property.
    pub user_labels: Option<serde_json::Value>,
}

/// `SqlServerAuditConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerAuditConfig {
    /// bucket property.
    pub bucket: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// retentionInterval property.
    pub retention_interval: Option<String>,
    /// uploadInterval property.
    pub upload_interval: Option<String>,
}

/// `RotateServerCertificateContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RotateServerCertificateContext {
    /// kind property.
    pub kind: Option<String>,
    /// nextVersion property.
    pub next_version: Option<String>,
}

/// `SqlServerDatabaseDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerDatabaseDetails {
    /// compatibilityLevel property.
    pub compatibility_level: Option<i64>,
    /// recoveryModel property.
    pub recovery_model: Option<String>,
}

/// `InstancesPreCheckMajorVersionUpgradeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesPreCheckMajorVersionUpgradeRequest {
    /// preCheckMajorVersionUpgradeContext property.
    pub pre_check_major_version_upgrade_context: Option<PreCheckMajorVersionUpgradeContext>,
}

/// `AcquireSsrsLeaseContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AcquireSsrsLeaseContext {
    /// duration property.
    pub duration: Option<String>,
    /// reportDatabase property.
    pub report_database: Option<String>,
    /// serviceLogin property.
    pub service_login: Option<String>,
    /// setupLogin property.
    pub setup_login: Option<String>,
}

/// `SqlInstancesVerifyExternalSyncSettingsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlInstancesVerifyExternalSyncSettingsRequest {
    /// migrationType property.
    pub migration_type: Option<String>,
    /// mysqlSyncConfig property.
    pub mysql_sync_config: Option<MySqlSyncConfig>,
    /// selectedObjects property.
    pub selected_objects: Option<Vec<ExternalSyncSelectedObject>>,
    /// syncMode property.
    pub sync_mode: Option<String>,
    /// syncParallelLevel property.
    pub sync_parallel_level: Option<String>,
    /// verifyConnectionOnly property.
    pub verify_connection_only: Option<bool>,
    /// verifyReplicationOnly property.
    pub verify_replication_only: Option<bool>,
}

/// `UsersListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsersListResponse {
    /// items property.
    pub items: Option<Vec<User>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `BinLogCoordinates` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BinLogCoordinates {
    /// binLogFileName property.
    pub bin_log_file_name: Option<String>,
    /// binLogPosition property.
    pub bin_log_position: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `InstancesListServerCasResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesListServerCasResponse {
    /// activeVersion property.
    pub active_version: Option<String>,
    /// certs property.
    pub certs: Option<Vec<SslCert>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Tier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Tier {
    /// DiskQuota property.
    pub disk_quota: Option<String>,
    /// RAM property.
    pub ram: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// region property.
    pub region: Option<Vec<String>>,
    /// tier property.
    pub tier: Option<String>,
}

/// `SqlActiveDirectoryConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlActiveDirectoryConfig {
    /// adminCredentialSecretName property.
    pub admin_credential_secret_name: Option<String>,
    /// dnsServers property.
    pub dns_servers: Option<Vec<String>>,
    /// domain property.
    pub domain: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// mode property.
    pub mode: Option<String>,
    /// organizationalUnit property.
    pub organizational_unit: Option<String>,
}

/// `DemoteMasterConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DemoteMasterConfiguration {
    /// kind property.
    pub kind: Option<String>,
    /// mysqlReplicaConfiguration property.
    pub mysql_replica_configuration: Option<DemoteMasterMySqlReplicaConfiguration>,
}

/// `PointInTimeRestoreContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PointInTimeRestoreContext {
    /// allocatedIpRange property.
    pub allocated_ip_range: Option<String>,
    /// datasource property.
    pub datasource: Option<String>,
    /// pointInTime property.
    pub point_in_time: Option<String>,
    /// preferredSecondaryZone property.
    pub preferred_secondary_zone: Option<String>,
    /// preferredZone property.
    pub preferred_zone: Option<String>,
    /// privateNetwork property.
    pub private_network: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// targetInstance property.
    pub target_instance: Option<String>,
    /// targetInstanceClearSettingsFieldNames property.
    pub target_instance_clear_settings_field_names: Option<Vec<String>>,
    /// targetInstanceSettings property.
    pub target_instance_settings: Option<DatabaseInstance>,
}

/// `UserPasswordValidationPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserPasswordValidationPolicy {
    /// allowedFailedAttempts property.
    pub allowed_failed_attempts: Option<i64>,
    /// enableFailedAttemptsCheck property.
    pub enable_failed_attempts_check: Option<bool>,
    /// enablePasswordVerification property.
    pub enable_password_verification: Option<bool>,
    /// passwordExpirationDuration property.
    pub password_expiration_duration: Option<String>,
    /// status property.
    pub status: Option<PasswordStatus>,
}

/// `MaintenanceWindow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MaintenanceWindow {
    /// day property.
    pub day: Option<i64>,
    /// hour property.
    pub hour: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// updateTrack property.
    pub update_track: Option<String>,
}

/// `SqlInstancesAcquireSsrsLeaseResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlInstancesAcquireSsrsLeaseResponse {
    /// operationId property.
    pub operation_id: Option<String>,
}

/// `QueryResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryResult {
    /// columns property.
    pub columns: Option<Vec<Column>>,
    /// message property.
    pub message: Option<String>,
    /// partialResult property.
    pub partial_result: Option<bool>,
    /// rows property.
    pub rows: Option<Vec<Row>>,
    /// status property.
    pub status: Option<Status>,
}

/// `Column` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Column {
    /// name property.
    pub name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ExportContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExportContext {
    /// bakExportOptions property.
    pub bak_export_options: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// csvExportOptions property.
    pub csv_export_options: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// databases property.
    pub databases: Option<Vec<String>>,
    /// fileType property.
    pub file_type: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// offload property.
    pub offload: Option<bool>,
    /// sqlExportOptions property.
    pub sql_export_options: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// tdeExportOptions property.
    pub tde_export_options: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// uri property.
    pub uri: Option<String>,
}

/// `PscConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PscConfig {
    /// allowedConsumerProjects property.
    pub allowed_consumer_projects: Option<Vec<String>>,
    /// networkAttachmentUri property.
    pub network_attachment_uri: Option<String>,
    /// pscAutoConnections property.
    pub psc_auto_connections: Option<Vec<PscAutoConnectionConfig>>,
    /// pscAutoDnsEnabled property.
    pub psc_auto_dns_enabled: Option<bool>,
    /// pscEnabled property.
    pub psc_enabled: Option<bool>,
    /// pscWriteEndpointDnsEnabled property.
    pub psc_write_endpoint_dns_enabled: Option<bool>,
}

/// `BackupReencryptionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackupReencryptionConfig {
    /// backupLimit property.
    pub backup_limit: Option<i64>,
    /// backupType property.
    pub backup_type: Option<String>,
}

/// `SqlInstancesReleaseSsrsLeaseResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlInstancesReleaseSsrsLeaseResponse {
    /// operationId property.
    pub operation_id: Option<String>,
}

/// `ExternalSyncSelectedObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExternalSyncSelectedObject {
    /// database property.
    pub database: Option<String>,
}

/// `DatabasesListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatabasesListResponse {
    /// items property.
    pub items: Option<Vec<Database>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `AclEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AclEntry {
    /// expirationTime property.
    pub expiration_time: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`sql.Backups.CreateBackup_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlBackupsCreateBackupArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: Backup,
}

/// Arguments for [`sql.Backups.DeleteBackup_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlBackupsDeleteBackupArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`sql.Backups.GetBackup_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlBackupsGetBackupArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`sql.Backups.ListBackups_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlBackupsListBackupsArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`sql.Backups.UpdateBackup_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlBackupsUpdateBackupArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Backup,
}

/// Arguments for [`sql.backupRuns.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlBackupRunsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Path parameter: `id`.
    pub id: String,
}

/// Arguments for [`sql.backupRuns.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlBackupRunsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Path parameter: `id`.
    pub id: String,
}

/// Arguments for [`sql.backupRuns.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlBackupRunsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: BackupRun,
}

/// Arguments for [`sql.backupRuns.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlBackupRunsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`sql.connect.generateEphemeral_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlConnectGenerateEphemeralArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: GenerateEphemeralCertRequest,
}

/// Arguments for [`sql.connect.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlConnectGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `readTime`.
    pub read_time: Option<String>,
}

/// Arguments for [`sql.databases.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlDatabasesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Path parameter: `database`.
    pub database: String,
}

/// Arguments for [`sql.databases.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlDatabasesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Path parameter: `database`.
    pub database: String,
}

/// Arguments for [`sql.databases.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlDatabasesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: Database,
}

/// Arguments for [`sql.databases.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlDatabasesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.databases.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlDatabasesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Path parameter: `database`.
    pub database: String,
    /// Request body.
    pub body: Database,
}

/// Arguments for [`sql.databases.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlDatabasesUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Path parameter: `database`.
    pub database: String,
    /// Request body.
    pub body: Database,
}

/// Arguments for [`sql.flags.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlFlagsListArgs {
    /// Query parameter: `databaseVersion`.
    pub database_version: Option<String>,
    /// Query parameter: `flagScope`.
    pub flag_scope: Option<String>,
}

/// Arguments for [`sql.instances.ListEntraIdCertificates_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesListEntraIdCertificatesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.instances.ListServerCertificates_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesListServerCertificatesArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.instances.RotateEntraIdCertificate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesRotateEntraIdCertificateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesRotateEntraIdCertificateRequest,
}

/// Arguments for [`sql.instances.RotateServerCertificate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesRotateServerCertificateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesRotateServerCertificateRequest,
}

/// Arguments for [`sql.instances.acquireSsrsLease_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesAcquireSsrsLeaseArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesAcquireSsrsLeaseRequest,
}

/// Arguments for [`sql.instances.addEntraIdCertificate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesAddEntraIdCertificateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.instances.addServerCa_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesAddServerCaArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.instances.addServerCertificate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesAddServerCertificateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.instances.clone_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesCloneArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesCloneRequest,
}

/// Arguments for [`sql.instances.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `enableFinalBackup`.
    pub enable_final_backup: Option<String>,
    /// Query parameter: `finalBackupDescription`.
    pub final_backup_description: Option<String>,
    /// Query parameter: `finalBackupExpiryTime`.
    pub final_backup_expiry_time: Option<String>,
    /// Query parameter: `finalBackupTtlDays`.
    pub final_backup_ttl_days: Option<String>,
}

/// Arguments for [`sql.instances.demote_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesDemoteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesDemoteRequest,
}

/// Arguments for [`sql.instances.demoteMaster_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesDemoteMasterArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesDemoteMasterRequest,
}

/// Arguments for [`sql.instances.executeSql_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesExecuteSqlArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: ExecuteSqlPayload,
}

/// Arguments for [`sql.instances.export_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesExportArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesExportRequest,
}

/// Arguments for [`sql.instances.failover_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesFailoverArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesFailoverRequest,
}

/// Arguments for [`sql.instances.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.instances.import_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesImportArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesImportRequest,
}

/// Arguments for [`sql.instances.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Request body.
    pub body: DatabaseInstance,
}

/// Arguments for [`sql.instances.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`sql.instances.listServerCas_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesListServerCasArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.instances.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: DatabaseInstance,
}

/// Arguments for [`sql.instances.pointInTimeRestore_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesPointInTimeRestoreArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: PointInTimeRestoreContext,
}

/// Arguments for [`sql.instances.preCheckMajorVersionUpgrade_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesPreCheckMajorVersionUpgradeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesPreCheckMajorVersionUpgradeRequest,
}

/// Arguments for [`sql.instances.promoteReplica_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesPromoteReplicaArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `failover`.
    pub failover: Option<String>,
}

/// Arguments for [`sql.instances.reencrypt_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesReencryptArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesReencryptRequest,
}

/// Arguments for [`sql.instances.releaseSsrsLease_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesReleaseSsrsLeaseArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.instances.resetSslConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesResetSslConfigArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `mode`.
    pub mode: Option<String>,
}

/// Arguments for [`sql.instances.restart_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesRestartArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.instances.restoreBackup_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesRestoreBackupArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesRestoreBackupRequest,
}

/// Arguments for [`sql.instances.rotateServerCa_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesRotateServerCaArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesRotateServerCaRequest,
}

/// Arguments for [`sql.instances.startReplica_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesStartReplicaArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.instances.stopReplica_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesStopReplicaArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.instances.switchover_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesSwitchoverArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `dbTimeout`.
    pub db_timeout: Option<String>,
}

/// Arguments for [`sql.instances.truncateLog_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesTruncateLogArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: InstancesTruncateLogRequest,
}

/// Arguments for [`sql.instances.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlInstancesUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: DatabaseInstance,
}

/// Arguments for [`sql.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlOperationsCancelArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `operation`.
    pub operation: String,
}

/// Arguments for [`sql.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlOperationsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `operation`.
    pub operation: String,
}

/// Arguments for [`sql.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlOperationsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `instance`.
    pub instance: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`sql.projects.instances.getDiskShrinkConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlProjectsInstancesGetDiskShrinkConfigArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.projects.instances.getLatestRecoveryTime_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlProjectsInstancesGetLatestRecoveryTimeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `sourceInstanceDeletionTime`.
    pub source_instance_deletion_time: Option<String>,
}

/// Arguments for [`sql.projects.instances.performDiskShrink_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlProjectsInstancesPerformDiskShrinkArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: PerformDiskShrinkContext,
}

/// Arguments for [`sql.projects.instances.rescheduleMaintenance_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlProjectsInstancesRescheduleMaintenanceArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: SqlInstancesRescheduleMaintenanceRequestBody,
}

/// Arguments for [`sql.projects.instances.resetReplicaSize_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlProjectsInstancesResetReplicaSizeArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: SqlInstancesResetReplicaSizeRequest,
}

/// Arguments for [`sql.projects.instances.startExternalSync_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlProjectsInstancesStartExternalSyncArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: SqlInstancesStartExternalSyncRequest,
}

/// Arguments for [`sql.projects.instances.verifyExternalSyncSettings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlProjectsInstancesVerifyExternalSyncSettingsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: SqlInstancesVerifyExternalSyncSettingsRequest,
}

/// Arguments for [`sql.sslCerts.createEphemeral_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlSslCertsCreateEphemeralArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: SslCertsCreateEphemeralRequest,
}

/// Arguments for [`sql.sslCerts.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlSslCertsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Path parameter: `sha1Fingerprint`.
    pub sha1_fingerprint: String,
}

/// Arguments for [`sql.sslCerts.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlSslCertsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Path parameter: `sha1Fingerprint`.
    pub sha1_fingerprint: String,
}

/// Arguments for [`sql.sslCerts.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlSslCertsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: SslCertsInsertRequest,
}

/// Arguments for [`sql.sslCerts.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlSslCertsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.tiers.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlTiersListArgs {
    /// Path parameter: `project`.
    pub project: String,
}

/// Arguments for [`sql.users.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlUsersDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `host`.
    pub host: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`sql.users.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlUsersGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `host`.
    pub host: Option<String>,
}

/// Arguments for [`sql.users.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlUsersInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Request body.
    pub body: User,
}

/// Arguments for [`sql.users.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlUsersListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
}

/// Arguments for [`sql.users.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SqlUsersUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `instance`.
    pub instance: String,
    /// Query parameter: `databaseRoles`.
    pub database_roles: Option<String>,
    /// Query parameter: `host`.
    pub host: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `revokeExistingRoles`.
    pub revoke_existing_roles: Option<String>,
    /// Request body.
    pub body: User,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/{+parent}/backups
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/backups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_backups_create_backup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_backups_create_backup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlBackupsCreateBackupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://sqladmin.googleapis.com/v1/{}/backups", args.parent,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_backups_delete_backup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_backups_delete_backup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlBackupsDeleteBackupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://sqladmin.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_backups_get_backup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_backups_get_backup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlBackupsGetBackupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Backup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://sqladmin.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Backup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/backups
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/backups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_backups_list_backups_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_backups_list_backups_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlBackupsListBackupsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBackupsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://sqladmin.googleapis.com/v1/{}/backups", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBackupsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_backups_update_backup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_backups_update_backup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlBackupsUpdateBackupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://sqladmin.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{project}/instances/{instance}/backupRuns/{id}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{project}/instances/{instance}/backupRuns/{id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_backup_runs_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_backup_runs_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlBackupRunsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns/{}",
        args.project, args.instance, args.id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_backup_runs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_backup_runs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlBackupRunsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackupRun>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns/{}",
        args.project, args.instance, args.id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackupRun =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/backupRuns
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/backupRuns.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_backup_runs_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_backup_runs_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlBackupRunsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/backupRuns
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/backupRuns.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_backup_runs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_backup_runs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlBackupRunsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackupRunsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns",
        args.project, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackupRunsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}:generateEphemeralCert
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}:generateEphemeralCert.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_connect_generate_ephemeral_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_connect_generate_ephemeral_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlConnectGenerateEphemeralArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateEphemeralCertResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}:generateEphemeralCert",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenerateEphemeralCertResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/connectSettings
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/connectSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_connect_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_connect_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlConnectGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConnectSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/connectSettings",
        args.project, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.read_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("readTime=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConnectSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{project}/instances/{instance}/databases/{database}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{project}/instances/{instance}/databases/{database}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_databases_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_databases_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlDatabasesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        args.project, args.instance, args.database,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/databases/{database}
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/databases/{database}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_databases_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_databases_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlDatabasesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Database>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        args.project, args.instance, args.database,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Database =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/databases
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/databases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_databases_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_databases_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlDatabasesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/databases
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/databases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_databases_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_databases_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlDatabasesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabasesListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases",
        args.project, args.instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatabasesListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{project}/instances/{instance}/databases/{database}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{project}/instances/{instance}/databases/{database}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_databases_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_databases_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlDatabasesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        args.project, args.instance, args.database,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/projects/{project}/instances/{instance}/databases/{database}
// -----------------------------------------------------------------------------

/// PUT v1/projects/{project}/instances/{instance}/databases/{database}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_databases_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_databases_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlDatabasesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        args.project, args.instance, args.database,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/flags
// -----------------------------------------------------------------------------

/// GET v1/flags.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_flags_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_flags_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlFlagsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlagsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://sqladmin.googleapis.com/v1/flags",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.database_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("databaseVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.flag_scope {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("flagScope=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlagsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/listEntraIdCertificates
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/listEntraIdCertificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_list_entra_id_certificates_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_list_entra_id_certificates_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesListEntraIdCertificatesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InstancesListEntraIdCertificatesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/listEntraIdCertificates",
        args.project, args.instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstancesListEntraIdCertificatesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/listServerCertificates
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/listServerCertificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_list_server_certificates_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_list_server_certificates_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesListServerCertificatesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<InstancesListServerCertificatesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/listServerCertificates",
        args.project, args.instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstancesListServerCertificatesResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/rotateEntraIdCertificate
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/rotateEntraIdCertificate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_rotate_entra_id_certificate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_rotate_entra_id_certificate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesRotateEntraIdCertificateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rotateEntraIdCertificate",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/rotateServerCertificate
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/rotateServerCertificate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_rotate_server_certificate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_rotate_server_certificate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesRotateServerCertificateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rotateServerCertificate",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/acquireSsrsLease
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/acquireSsrsLease.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_acquire_ssrs_lease_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_acquire_ssrs_lease_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesAcquireSsrsLeaseArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SqlInstancesAcquireSsrsLeaseResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/acquireSsrsLease",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SqlInstancesAcquireSsrsLeaseResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/addEntraIdCertificate
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/addEntraIdCertificate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_add_entra_id_certificate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_add_entra_id_certificate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesAddEntraIdCertificateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/addEntraIdCertificate",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/addServerCa
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/addServerCa.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_add_server_ca_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_add_server_ca_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesAddServerCaArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/addServerCa",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/addServerCertificate
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/addServerCertificate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_add_server_certificate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_add_server_certificate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesAddServerCertificateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/addServerCertificate",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/clone
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/clone.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_clone_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_clone_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesCloneArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/clone",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{project}/instances/{instance}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{project}/instances/{instance}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        args.project, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.enable_final_backup {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enableFinalBackup=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.final_backup_description {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("finalBackupDescription=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.final_backup_expiry_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("finalBackupExpiryTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.final_backup_ttl_days {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("finalBackupTtlDays=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/demote
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/demote.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_demote_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_demote_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesDemoteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/demote",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/demoteMaster
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/demoteMaster.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_demote_master_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_demote_master_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesDemoteMasterArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/demoteMaster",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/executeSql
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/executeSql.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_execute_sql_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_execute_sql_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesExecuteSqlArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SqlInstancesExecuteSqlResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/executeSql",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SqlInstancesExecuteSqlResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/export
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/export.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_export_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_export_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesExportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/export",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/failover
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/failover.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_failover_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_failover_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesFailoverArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/failover",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabaseInstance>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        args.project, args.instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DatabaseInstance =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/import
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesImportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/import",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances",
        args.project,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstancesListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstancesListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/listServerCas
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/listServerCas.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_list_server_cas_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_list_server_cas_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesListServerCasArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstancesListServerCasResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/listServerCas",
        args.project, args.instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: InstancesListServerCasResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{project}/instances/{instance}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{project}/instances/{instance}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        args.project, args.instance,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}:pointInTimeRestore
// -----------------------------------------------------------------------------

/// POST v1/{+parent}:pointInTimeRestore.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_point_in_time_restore_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_point_in_time_restore_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesPointInTimeRestoreArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/{}:pointInTimeRestore",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/preCheckMajorVersionUpgrade
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/preCheckMajorVersionUpgrade.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_pre_check_major_version_upgrade_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_pre_check_major_version_upgrade_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesPreCheckMajorVersionUpgradeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/preCheckMajorVersionUpgrade",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/promoteReplica
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/promoteReplica.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_promote_replica_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_promote_replica_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesPromoteReplicaArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/promoteReplica",
        args.project, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.failover {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("failover=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/reencrypt
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/reencrypt.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_reencrypt_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_reencrypt_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesReencryptArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/reencrypt",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/releaseSsrsLease
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/releaseSsrsLease.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_release_ssrs_lease_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_release_ssrs_lease_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesReleaseSsrsLeaseArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SqlInstancesReleaseSsrsLeaseResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/releaseSsrsLease",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SqlInstancesReleaseSsrsLeaseResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/resetSslConfig
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/resetSslConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_reset_ssl_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_reset_ssl_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesResetSslConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/resetSslConfig",
        args.project, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.mode {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/restart
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/restart.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_restart_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_restart_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesRestartArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/restart",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/restoreBackup
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/restoreBackup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_restore_backup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_restore_backup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesRestoreBackupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/restoreBackup",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/rotateServerCa
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/rotateServerCa.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_rotate_server_ca_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_rotate_server_ca_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesRotateServerCaArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rotateServerCa",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/startReplica
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/startReplica.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_start_replica_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_start_replica_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesStartReplicaArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/startReplica",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/stopReplica
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/stopReplica.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_stop_replica_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_stop_replica_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesStopReplicaArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/stopReplica",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/switchover
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/switchover.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_switchover_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_switchover_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesSwitchoverArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/switchover",
        args.project, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.db_timeout {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dbTimeout=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/truncateLog
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/truncateLog.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_truncate_log_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_truncate_log_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesTruncateLogArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/truncateLog",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/projects/{project}/instances/{instance}
// -----------------------------------------------------------------------------

/// PUT v1/projects/{project}/instances/{instance}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_instances_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_instances_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlInstancesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        args.project, args.instance,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/operations/{operation}/cancel
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/operations/{operation}/cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/operations/{}/cancel",
        args.project, args.operation,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/operations/{operation}
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/operations/{operation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/operations/{}",
        args.project, args.operation,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/operations
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/operations",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.instance {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("instance=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OperationsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/getDiskShrinkConfig
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/getDiskShrinkConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_projects_instances_get_disk_shrink_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_projects_instances_get_disk_shrink_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlProjectsInstancesGetDiskShrinkConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SqlInstancesGetDiskShrinkConfigResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/getDiskShrinkConfig",
        args.project, args.instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SqlInstancesGetDiskShrinkConfigResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/getLatestRecoveryTime
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/getLatestRecoveryTime.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_projects_instances_get_latest_recovery_time_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_projects_instances_get_latest_recovery_time_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlProjectsInstancesGetLatestRecoveryTimeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SqlInstancesGetLatestRecoveryTimeResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/getLatestRecoveryTime",
        args.project, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.source_instance_deletion_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceInstanceDeletionTime=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SqlInstancesGetLatestRecoveryTimeResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/performDiskShrink
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/performDiskShrink.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_projects_instances_perform_disk_shrink_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_projects_instances_perform_disk_shrink_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlProjectsInstancesPerformDiskShrinkArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/performDiskShrink",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/rescheduleMaintenance
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/rescheduleMaintenance.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_projects_instances_reschedule_maintenance_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_projects_instances_reschedule_maintenance_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlProjectsInstancesRescheduleMaintenanceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rescheduleMaintenance",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/resetReplicaSize
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/resetReplicaSize.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_projects_instances_reset_replica_size_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_projects_instances_reset_replica_size_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlProjectsInstancesResetReplicaSizeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/resetReplicaSize",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/startExternalSync
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/startExternalSync.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_projects_instances_start_external_sync_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_projects_instances_start_external_sync_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlProjectsInstancesStartExternalSyncArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/startExternalSync",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/verifyExternalSyncSettings
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/verifyExternalSyncSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_projects_instances_verify_external_sync_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_projects_instances_verify_external_sync_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlProjectsInstancesVerifyExternalSyncSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SqlInstancesVerifyExternalSyncSettingsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/verifyExternalSyncSettings",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SqlInstancesVerifyExternalSyncSettingsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/createEphemeral
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/createEphemeral.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_ssl_certs_create_ephemeral_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_ssl_certs_create_ephemeral_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlSslCertsCreateEphemeralArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCert>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/createEphemeral",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslCert =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_ssl_certs_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_ssl_certs_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlSslCertsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts/{}",
        args.project, args.instance, args.sha1_fingerprint,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_ssl_certs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_ssl_certs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlSslCertsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCert>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts/{}",
        args.project, args.instance, args.sha1_fingerprint,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslCert =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/sslCerts
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/sslCerts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_ssl_certs_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_ssl_certs_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlSslCertsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertsInsertResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslCertsInsertResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/sslCerts
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/sslCerts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_ssl_certs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_ssl_certs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlSslCertsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts",
        args.project, args.instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SslCertsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/tiers
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/tiers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_tiers_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_tiers_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlTiersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TiersListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/tiers",
        args.project,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TiersListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{project}/instances/{instance}/users
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{project}/instances/{instance}/users.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_users_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_users_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlUsersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        args.project, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.host {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("host=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/users/{name}
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/users/{name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_users_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_users_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlUsersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<User>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users/{}",
        args.project, args.instance, args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.host {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("host=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: User =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{project}/instances/{instance}/users
// -----------------------------------------------------------------------------

/// POST v1/projects/{project}/instances/{instance}/users.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_users_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_users_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlUsersInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        args.project, args.instance,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{project}/instances/{instance}/users
// -----------------------------------------------------------------------------

/// GET v1/projects/{project}/instances/{instance}/users.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_users_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_users_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlUsersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UsersListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        args.project, args.instance,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UsersListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/projects/{project}/instances/{instance}/users
// -----------------------------------------------------------------------------

/// PUT v1/projects/{project}/instances/{instance}/users.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sql_users_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sql_users_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SqlUsersUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        args.project, args.instance,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.database_roles {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("databaseRoles=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.host {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("host=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.revoke_existing_roles {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("revokeExistingRoles=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
