//! Auto-generated API module for gcp/storagebatchoperations bucketOperations.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_storagebatchoperations_bucket_operations `

#![cfg(feature = "gcp_storagebatchoperations_bucket_operations")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Manifest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Manifest {
    /// manifestLocation property.
    pub manifest_location: Option<String>,
}

/// `ListBucketOperationsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListBucketOperationsResponse {
    /// bucketOperations property.
    pub bucket_operations: Option<Vec<BucketOperation>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `RewriteObject` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RewriteObject {
    /// kmsKey property.
    pub kms_key: Option<String>,
}

/// `PrefixList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PrefixList {
    /// includedObjectPrefixes property.
    pub included_object_prefixes: Option<Vec<String>>,
}

/// `CustomContextUpdates` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomContextUpdates {
    /// keysToClear property.
    pub keys_to_clear: Option<Vec<String>>,
    /// updates property.
    pub updates: Option<serde_json::Value>,
}

/// `PutMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PutMetadata {
    /// cacheControl property.
    pub cache_control: Option<String>,
    /// contentDisposition property.
    pub content_disposition: Option<String>,
    /// contentEncoding property.
    pub content_encoding: Option<String>,
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// contentType property.
    pub content_type: Option<String>,
    /// customMetadata property.
    pub custom_metadata: Option<serde_json::Value>,
    /// customTime property.
    pub custom_time: Option<String>,
    /// objectRetention property.
    pub object_retention: Option<ObjectRetention>,
}

/// `Counters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Counters {
    /// failedObjectCount property.
    pub failed_object_count: Option<String>,
    /// objectCustomContextsCreated property.
    pub object_custom_contexts_created: Option<String>,
    /// objectCustomContextsDeleted property.
    pub object_custom_contexts_deleted: Option<String>,
    /// objectCustomContextsUpdated property.
    pub object_custom_contexts_updated: Option<String>,
    /// succeededObjectCount property.
    pub succeeded_object_count: Option<String>,
    /// totalBytesFound property.
    pub total_bytes_found: Option<String>,
    /// totalObjectCount property.
    pub total_object_count: Option<String>,
}

/// `DeleteObject` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeleteObject {
    /// permanentObjectDeletionEnabled property.
    pub permanent_object_deletion_enabled: Option<bool>,
}

/// `PutObjectHold` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PutObjectHold {
    /// eventBasedHold property.
    pub event_based_hold: Option<String>,
    /// temporaryHold property.
    pub temporary_hold: Option<String>,
}

/// `ObjectRetention` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ObjectRetention {
    /// retainUntilTime property.
    pub retain_until_time: Option<String>,
    /// retentionMode property.
    pub retention_mode: Option<String>,
}

/// `BucketOperation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BucketOperation {
    /// bucketName property.
    pub bucket_name: Option<String>,
    /// completeTime property.
    pub complete_time: Option<String>,
    /// counters property.
    pub counters: Option<Counters>,
    /// createTime property.
    pub create_time: Option<String>,
    /// deleteObject property.
    pub delete_object: Option<DeleteObject>,
    /// errorSummaries property.
    pub error_summaries: Option<Vec<ErrorSummary>>,
    /// manifest property.
    pub manifest: Option<Manifest>,
    /// name property.
    pub name: Option<String>,
    /// prefixList property.
    pub prefix_list: Option<PrefixList>,
    /// putMetadata property.
    pub put_metadata: Option<PutMetadata>,
    /// putObjectHold property.
    pub put_object_hold: Option<PutObjectHold>,
    /// rewriteObject property.
    pub rewrite_object: Option<RewriteObject>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// updateObjectCustomContext property.
    pub update_object_custom_context: Option<UpdateObjectCustomContext>,
}

/// `ErrorSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ErrorSummary {
    /// errorCode property.
    pub error_code: Option<String>,
    /// errorCount property.
    pub error_count: Option<String>,
    /// errorLogEntries property.
    pub error_log_entries: Option<Vec<ErrorLogEntry>>,
}

/// `ErrorLogEntry` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ErrorLogEntry {
    /// errorDetails property.
    pub error_details: Option<Vec<String>>,
    /// objectUri property.
    pub object_uri: Option<String>,
}

/// `UpdateObjectCustomContext` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpdateObjectCustomContext {
    /// clearAll property.
    pub clear_all: Option<bool>,
    /// customContextUpdates property.
    pub custom_context_updates: Option<CustomContextUpdates>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`storagebatchoperations.projects.locations.jobs.bucketOperations.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct StoragebatchoperationsProjectsLocationsJobsBucketOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`storagebatchoperations.projects.locations.jobs.bucketOperations.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct StoragebatchoperationsProjectsLocationsJobsBucketOperationsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}/bucketOperations/{bucketOperationsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}/bucketOperations/{bucketOperationsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storagebatchoperations_projects_locations_jobs_bucket_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storagebatchoperations_projects_locations_jobs_bucket_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BucketOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storagebatchoperations.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs/{{jobsId}}/bucketOperations/{{bucketOperationsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BucketOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}/bucketOperations
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}/bucketOperations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storagebatchoperations_projects_locations_jobs_bucket_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storagebatchoperations_projects_locations_jobs_bucket_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBucketOperationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storagebatchoperations.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs/{{jobsId}}/bucketOperations",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBucketOperationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
