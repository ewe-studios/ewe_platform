//! Auto-generated API module for gcp/storagebatchoperations jobs.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_storagebatchoperations_jobs `

#![cfg(feature = "gcp_storagebatchoperations_jobs")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::Operation;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `PutMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PutMetadata {
    /// cacheControl property.
    pub cache_control: Option<String>,
    /// contentDisposition property.
    pub content_disposition: Option<String>,
    /// contentEncoding property.
    pub content_encoding: Option<String>,
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// contentType property.
    pub content_type: Option<String>,
    /// customMetadata property.
    pub custom_metadata: Option<serde_json::Value>,
    /// customTime property.
    pub custom_time: Option<String>,
    /// objectRetention property.
    pub object_retention: Option<ObjectRetention>,
}

/// `ObjectRetention` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ObjectRetention {
    /// retainUntilTime property.
    pub retain_until_time: Option<String>,
    /// retentionMode property.
    pub retention_mode: Option<String>,
}

/// `Bucket` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Bucket {
    /// bucket property.
    pub bucket: Option<String>,
    /// manifest property.
    pub manifest: Option<Manifest>,
    /// prefixList property.
    pub prefix_list: Option<PrefixList>,
}

/// `Manifest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Manifest {
    /// manifestLocation property.
    pub manifest_location: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `DeleteObject` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeleteObject {
    /// permanentObjectDeletionEnabled property.
    pub permanent_object_deletion_enabled: Option<bool>,
}

/// `RewriteObject` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RewriteObject {
    /// kmsKey property.
    pub kms_key: Option<String>,
}

/// `Job` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Job {
    /// bucketList property.
    pub bucket_list: Option<BucketList>,
    /// completeTime property.
    pub complete_time: Option<String>,
    /// counters property.
    pub counters: Option<Counters>,
    /// createTime property.
    pub create_time: Option<String>,
    /// deleteObject property.
    pub delete_object: Option<DeleteObject>,
    /// description property.
    pub description: Option<String>,
    /// dryRun property.
    pub dry_run: Option<bool>,
    /// errorSummaries property.
    pub error_summaries: Option<Vec<ErrorSummary>>,
    /// isMultiBucketJob property.
    pub is_multi_bucket_job: Option<bool>,
    /// loggingConfig property.
    pub logging_config: Option<LoggingConfig>,
    /// name property.
    pub name: Option<String>,
    /// putMetadata property.
    pub put_metadata: Option<PutMetadata>,
    /// putObjectHold property.
    pub put_object_hold: Option<PutObjectHold>,
    /// rewriteObject property.
    pub rewrite_object: Option<RewriteObject>,
    /// scheduleTime property.
    pub schedule_time: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// updateObjectCustomContext property.
    pub update_object_custom_context: Option<UpdateObjectCustomContext>,
}

/// `BucketList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BucketList {
    /// buckets property.
    pub buckets: Option<Vec<Bucket>>,
}

/// `PrefixList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PrefixList {
    /// includedObjectPrefixes property.
    pub included_object_prefixes: Option<Vec<String>>,
}

/// `ErrorSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ErrorSummary {
    /// errorCode property.
    pub error_code: Option<String>,
    /// errorCount property.
    pub error_count: Option<String>,
    /// errorLogEntries property.
    pub error_log_entries: Option<Vec<ErrorLogEntry>>,
}

/// `Counters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Counters {
    /// failedObjectCount property.
    pub failed_object_count: Option<String>,
    /// objectCustomContextsCreated property.
    pub object_custom_contexts_created: Option<String>,
    /// objectCustomContextsDeleted property.
    pub object_custom_contexts_deleted: Option<String>,
    /// objectCustomContextsUpdated property.
    pub object_custom_contexts_updated: Option<String>,
    /// succeededObjectCount property.
    pub succeeded_object_count: Option<String>,
    /// totalBytesFound property.
    pub total_bytes_found: Option<String>,
    /// totalObjectCount property.
    pub total_object_count: Option<String>,
}

/// `UpdateObjectCustomContext` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpdateObjectCustomContext {
    /// clearAll property.
    pub clear_all: Option<bool>,
    /// customContextUpdates property.
    pub custom_context_updates: Option<CustomContextUpdates>,
}

/// `CancelJobResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CancelJobResponse {}

/// `CustomContextUpdates` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomContextUpdates {
    /// keysToClear property.
    pub keys_to_clear: Option<Vec<String>>,
    /// updates property.
    pub updates: Option<serde_json::Value>,
}

/// `ErrorLogEntry` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ErrorLogEntry {
    /// errorDetails property.
    pub error_details: Option<Vec<String>>,
    /// objectUri property.
    pub object_uri: Option<String>,
}

/// `LoggingConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LoggingConfig {
    /// logActionStates property.
    pub log_action_states: Option<Vec<String>>,
    /// logActions property.
    pub log_actions: Option<Vec<String>>,
}

/// `PutObjectHold` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PutObjectHold {
    /// eventBasedHold property.
    pub event_based_hold: Option<String>,
    /// temporaryHold property.
    pub temporary_hold: Option<String>,
}

/// `ListJobsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListJobsResponse {
    /// jobs property.
    pub jobs: Option<Vec<Job>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`storagebatchoperations.projects.locations.jobs.cancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct StoragebatchoperationsProjectsLocationsJobsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`storagebatchoperations.projects.locations.jobs.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct StoragebatchoperationsProjectsLocationsJobsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `jobId`.
    pub job_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`storagebatchoperations.projects.locations.jobs.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct StoragebatchoperationsProjectsLocationsJobsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`storagebatchoperations.projects.locations.jobs.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct StoragebatchoperationsProjectsLocationsJobsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`storagebatchoperations.projects.locations.jobs.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct StoragebatchoperationsProjectsLocationsJobsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}:cancel
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storagebatchoperations_projects_locations_jobs_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storagebatchoperations_projects_locations_jobs_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelJobResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storagebatchoperations.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs/{{jobsId}}:cancel",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CancelJobResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/jobs
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storagebatchoperations_projects_locations_jobs_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storagebatchoperations_projects_locations_jobs_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storagebatchoperations.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storagebatchoperations_projects_locations_jobs_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storagebatchoperations_projects_locations_jobs_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storagebatchoperations.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs/{{jobsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storagebatchoperations_projects_locations_jobs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storagebatchoperations_projects_locations_jobs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storagebatchoperations.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs/{{jobsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storagebatchoperations_projects_locations_jobs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storagebatchoperations_projects_locations_jobs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storagebatchoperations.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
