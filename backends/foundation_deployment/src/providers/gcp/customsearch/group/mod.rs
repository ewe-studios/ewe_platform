//! Auto-generated API module for gcp/customsearch v1.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_customsearch_group `

#![cfg(feature = "gcp_customsearch_group")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Search;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Promotion` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Promotion {
    /// bodyLines property.
    pub body_lines: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// displayLink property.
    pub display_link: Option<String>,
    /// htmlTitle property.
    pub html_title: Option<String>,
    /// image property.
    pub image: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// link property.
    pub link: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `ResultType` response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ResultType {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`search.cse.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SearchCseListArgs {
    /// Query parameter: `c2coff`.
    pub c2coff: Option<String>,
    /// Query parameter: `cr`.
    pub cr: Option<String>,
    /// Query parameter: `cx`.
    pub cx: Option<String>,
    /// Query parameter: `dateRestrict`.
    pub date_restrict: Option<String>,
    /// Query parameter: `enableAlternateSearchHandler`.
    pub enable_alternate_search_handler: Option<String>,
    /// Query parameter: `exactTerms`.
    pub exact_terms: Option<String>,
    /// Query parameter: `excludeTerms`.
    pub exclude_terms: Option<String>,
    /// Query parameter: `fileType`.
    pub file_type: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `gl`.
    pub gl: Option<String>,
    /// Query parameter: `googlehost`.
    pub googlehost: Option<String>,
    /// Query parameter: `highRange`.
    pub high_range: Option<String>,
    /// Query parameter: `hl`.
    pub hl: Option<String>,
    /// Query parameter: `hq`.
    pub hq: Option<String>,
    /// Query parameter: `imgColorType`.
    pub img_color_type: Option<String>,
    /// Query parameter: `imgDominantColor`.
    pub img_dominant_color: Option<String>,
    /// Query parameter: `imgSize`.
    pub img_size: Option<String>,
    /// Query parameter: `imgType`.
    pub img_type: Option<String>,
    /// Query parameter: `linkSite`.
    pub link_site: Option<String>,
    /// Query parameter: `lowRange`.
    pub low_range: Option<String>,
    /// Query parameter: `lr`.
    pub lr: Option<String>,
    /// Query parameter: `num`.
    pub num: Option<String>,
    /// Query parameter: `orTerms`.
    pub or_terms: Option<String>,
    /// Query parameter: `q`.
    pub q: Option<String>,
    /// Query parameter: `relatedSite`.
    pub related_site: Option<String>,
    /// Query parameter: `rights`.
    pub rights: Option<String>,
    /// Query parameter: `safe`.
    pub safe: Option<String>,
    /// Query parameter: `searchType`.
    pub search_type: Option<String>,
    /// Query parameter: `siteSearch`.
    pub site_search: Option<String>,
    /// Query parameter: `siteSearchFilter`.
    pub site_search_filter: Option<String>,
    /// Query parameter: `snippetLength`.
    pub snippet_length: Option<String>,
    /// Query parameter: `sort`.
    pub sort: Option<String>,
    /// Query parameter: `start`.
    pub start: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET customsearch/v1
// -----------------------------------------------------------------------------

/// GET customsearch/v1.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = search_cse_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn search_cse_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Search>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://customsearch.googleapis.com/customsearch/v1",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Search =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
