//! Auto-generated API module for gcp/playdeveloperreporting playdeveloperreporting.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_playdeveloperreporting_playdeveloperreporting `

#![cfg(feature = "gcp_playdeveloperreporting_playdeveloperreporting")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GooglePlayDeveloperReportingV1Beta1FreshnessInfo` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1FreshnessInfo {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1Beta1OsVersion` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1OsVersion {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1Beta1DeviceId` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1DeviceId {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse {
    /// apps property.
    pub apps: Option<Vec<GooglePlayDeveloperReportingV1Beta1App>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetRequest {
    /// dimensions property.
    pub dimensions: Option<Vec<String>>,
    /// filter property.
    pub filter: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// timelineSpec property.
    pub timeline_spec: Option<GooglePlayDeveloperReportingV1Beta1TimelineSpec>,
    /// userCohort property.
    pub user_cohort: Option<String>,
}

/// `GooglePlayDeveloperReportingV1Beta1DeviceModelSummary` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1DeviceModelSummary {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1Beta1AppVersion` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1AppVersion {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet {
    /// freshnessInfo property.
    pub freshness_info: Option<GooglePlayDeveloperReportingV1Beta1FreshnessInfo>,
    /// name property.
    pub name: Option<String>,
}

/// `GooglePlayDeveloperReportingV1Beta1TimelineSpec` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1TimelineSpec {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1Beta1Track` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1Track {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse {
    /// anomalies property.
    pub anomalies: Option<Vec<GooglePlayDeveloperReportingV1Beta1Anomaly>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// rows property.
    pub rows: Option<Vec<GooglePlayDeveloperReportingV1Beta1MetricsRow>>,
}

/// `GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions {
    /// tracks property.
    pub tracks: Option<Vec<GooglePlayDeveloperReportingV1Beta1Track>>,
}

/// `GoogleTypeTimeZone` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeTimeZone {
    /// id property.
    pub id: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet {
    /// freshnessInfo property.
    pub freshness_info: Option<GooglePlayDeveloperReportingV1Beta1FreshnessInfo>,
    /// name property.
    pub name: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// rows property.
    pub rows: Option<Vec<GooglePlayDeveloperReportingV1Beta1MetricsRow>>,
}

/// `GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse {
    /// errorReports property.
    pub error_reports: Option<Vec<GooglePlayDeveloperReportingV1Beta1ErrorReport>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1LmkRateMetricSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1LmkRateMetricSet {
    /// freshnessInfo property.
    pub freshness_info: Option<GooglePlayDeveloperReportingV1Beta1FreshnessInfo>,
    /// name property.
    pub name: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// rows property.
    pub rows: Option<Vec<GooglePlayDeveloperReportingV1Beta1MetricsRow>>,
}

/// `GooglePlayDeveloperReportingV1Beta1DecimalConfidenceInterval` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1DecimalConfidenceInterval {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1beta1CrashRateMetricSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1CrashRateMetricSet {
    /// freshnessInfo property.
    pub freshness_info: Option<GooglePlayDeveloperReportingV1Beta1FreshnessInfo>,
    /// name property.
    pub name: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetRequest {
    /// dimensions property.
    pub dimensions: Option<Vec<String>>,
    /// filter property.
    pub filter: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// timelineSpec property.
    pub timeline_spec: Option<GooglePlayDeveloperReportingV1Beta1TimelineSpec>,
    /// userCohort property.
    pub user_cohort: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// rows property.
    pub rows: Option<Vec<GooglePlayDeveloperReportingV1Beta1MetricsRow>>,
}

/// `GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetRequest {
    /// dimensions property.
    pub dimensions: Option<Vec<String>>,
    /// filter property.
    pub filter: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// timelineSpec property.
    pub timeline_spec: Option<GooglePlayDeveloperReportingV1Beta1TimelineSpec>,
    /// userCohort property.
    pub user_cohort: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetRequest {
    /// dimensions property.
    pub dimensions: Option<Vec<String>>,
    /// filter property.
    pub filter: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// timelineSpec property.
    pub timeline_spec: Option<GooglePlayDeveloperReportingV1Beta1TimelineSpec>,
    /// userCohort property.
    pub user_cohort: Option<String>,
}

/// `GooglePlayDeveloperReportingV1Beta1DimensionValue` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1DimensionValue {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1Beta1App` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1App {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetRequest {
    /// dimensions property.
    pub dimensions: Option<Vec<String>>,
    /// filter property.
    pub filter: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// timelineSpec property.
    pub timeline_spec: Option<GooglePlayDeveloperReportingV1Beta1TimelineSpec>,
    /// userCohort property.
    pub user_cohort: Option<String>,
}

/// `GooglePlayDeveloperReportingV1Beta1FreshnessInfoFreshness` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1FreshnessInfoFreshness {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetRequest {
    /// dimensions property.
    pub dimensions: Option<Vec<String>>,
    /// filter property.
    pub filter: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// timelineSpec property.
    pub timeline_spec: Option<GooglePlayDeveloperReportingV1Beta1TimelineSpec>,
    /// userCohort property.
    pub user_cohort: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// rows property.
    pub rows: Option<Vec<GooglePlayDeveloperReportingV1Beta1MetricsRow>>,
}

/// `GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet {
    /// freshnessInfo property.
    pub freshness_info: Option<GooglePlayDeveloperReportingV1Beta1FreshnessInfo>,
    /// name property.
    pub name: Option<String>,
}

/// `GooglePlayDeveloperReportingV1Beta1Release` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1Release {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GoogleTypeDateTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeDateTime {
    /// day property.
    pub day: Option<i64>,
    /// hours property.
    pub hours: Option<i64>,
    /// minutes property.
    pub minutes: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<i64>,
    /// timeZone property.
    pub time_zone: Option<GoogleTypeTimeZone>,
    /// utcOffset property.
    pub utc_offset: Option<String>,
    /// year property.
    pub year: Option<i64>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// rows property.
    pub rows: Option<Vec<GooglePlayDeveloperReportingV1Beta1MetricsRow>>,
}

/// `GooglePlayDeveloperReportingV1Beta1ErrorIssue` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1ErrorIssue {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// rows property.
    pub rows: Option<Vec<GooglePlayDeveloperReportingV1Beta1MetricsRow>>,
}

/// `GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse {
    /// errorIssues property.
    pub error_issues: Option<Vec<GooglePlayDeveloperReportingV1Beta1ErrorIssue>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleTypeDecimal` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeDecimal {
    /// value property.
    pub value: Option<String>,
}

/// `GooglePlayDeveloperReportingV1Beta1MetricsRow` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1MetricsRow {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1Beta1ErrorReport` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1ErrorReport {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1Beta1Anomaly` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1Anomaly {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetRequest {
    /// dimensions property.
    pub dimensions: Option<Vec<String>>,
    /// filter property.
    pub filter: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// timelineSpec property.
    pub timeline_spec: Option<GooglePlayDeveloperReportingV1Beta1TimelineSpec>,
}

/// `GooglePlayDeveloperReportingV1beta1AnrRateMetricSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1AnrRateMetricSet {
    /// freshnessInfo property.
    pub freshness_info: Option<GooglePlayDeveloperReportingV1Beta1FreshnessInfo>,
    /// name property.
    pub name: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetRequest {
    /// dimensions property.
    pub dimensions: Option<Vec<String>>,
    /// filter property.
    pub filter: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// timelineSpec property.
    pub timeline_spec: Option<GooglePlayDeveloperReportingV1Beta1TimelineSpec>,
    /// userCohort property.
    pub user_cohort: Option<String>,
}

/// `GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// rows property.
    pub rows: Option<Vec<GooglePlayDeveloperReportingV1Beta1MetricsRow>>,
}

/// `GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet {
    /// freshnessInfo property.
    pub freshness_info: Option<GooglePlayDeveloperReportingV1Beta1FreshnessInfo>,
    /// name property.
    pub name: Option<String>,
}

/// `GooglePlayDeveloperReportingV1Beta1MetricValue` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1MetricValue {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1Beta1IssueAnnotation` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1Beta1IssueAnnotation {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet {
    /// freshnessInfo property.
    pub freshness_info: Option<GooglePlayDeveloperReportingV1Beta1FreshnessInfo>,
    /// name property.
    pub name: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`playdeveloperreporting.anomalies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingAnomaliesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`playdeveloperreporting.apps.fetchReleaseFilterOptions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingAppsFetchReleaseFilterOptionsArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`playdeveloperreporting.apps.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingAppsSearchArgs {
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`playdeveloperreporting.vitals.anrrate.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsAnrrateGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`playdeveloperreporting.vitals.anrrate.query_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsAnrrateQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetRequest,
}

/// Arguments for [`playdeveloperreporting.vitals.crashrate.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsCrashrateGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`playdeveloperreporting.vitals.crashrate.query_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsCrashrateQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetRequest,
}

/// Arguments for [`playdeveloperreporting.vitals.errors.counts.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsErrorsCountsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`playdeveloperreporting.vitals.errors.counts.query_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsErrorsCountsQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetRequest,
}

/// Arguments for [`playdeveloperreporting.vitals.errors.issues.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsErrorsIssuesSearchArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `interval.endTime.day`.
    pub interval_end_time_day: Option<String>,
    /// Query parameter: `interval.endTime.hours`.
    pub interval_end_time_hours: Option<String>,
    /// Query parameter: `interval.endTime.minutes`.
    pub interval_end_time_minutes: Option<String>,
    /// Query parameter: `interval.endTime.month`.
    pub interval_end_time_month: Option<String>,
    /// Query parameter: `interval.endTime.nanos`.
    pub interval_end_time_nanos: Option<String>,
    /// Query parameter: `interval.endTime.seconds`.
    pub interval_end_time_seconds: Option<String>,
    /// Query parameter: `interval.endTime.timeZone.id`.
    pub interval_end_time_time_zone_id: Option<String>,
    /// Query parameter: `interval.endTime.timeZone.version`.
    pub interval_end_time_time_zone_version: Option<String>,
    /// Query parameter: `interval.endTime.utcOffset`.
    pub interval_end_time_utc_offset: Option<String>,
    /// Query parameter: `interval.endTime.year`.
    pub interval_end_time_year: Option<String>,
    /// Query parameter: `interval.startTime.day`.
    pub interval_start_time_day: Option<String>,
    /// Query parameter: `interval.startTime.hours`.
    pub interval_start_time_hours: Option<String>,
    /// Query parameter: `interval.startTime.minutes`.
    pub interval_start_time_minutes: Option<String>,
    /// Query parameter: `interval.startTime.month`.
    pub interval_start_time_month: Option<String>,
    /// Query parameter: `interval.startTime.nanos`.
    pub interval_start_time_nanos: Option<String>,
    /// Query parameter: `interval.startTime.seconds`.
    pub interval_start_time_seconds: Option<String>,
    /// Query parameter: `interval.startTime.timeZone.id`.
    pub interval_start_time_time_zone_id: Option<String>,
    /// Query parameter: `interval.startTime.timeZone.version`.
    pub interval_start_time_time_zone_version: Option<String>,
    /// Query parameter: `interval.startTime.utcOffset`.
    pub interval_start_time_utc_offset: Option<String>,
    /// Query parameter: `interval.startTime.year`.
    pub interval_start_time_year: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `sampleErrorReportLimit`.
    pub sample_error_report_limit: Option<String>,
}

/// Arguments for [`playdeveloperreporting.vitals.errors.reports.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsErrorsReportsSearchArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `interval.endTime.day`.
    pub interval_end_time_day: Option<String>,
    /// Query parameter: `interval.endTime.hours`.
    pub interval_end_time_hours: Option<String>,
    /// Query parameter: `interval.endTime.minutes`.
    pub interval_end_time_minutes: Option<String>,
    /// Query parameter: `interval.endTime.month`.
    pub interval_end_time_month: Option<String>,
    /// Query parameter: `interval.endTime.nanos`.
    pub interval_end_time_nanos: Option<String>,
    /// Query parameter: `interval.endTime.seconds`.
    pub interval_end_time_seconds: Option<String>,
    /// Query parameter: `interval.endTime.timeZone.id`.
    pub interval_end_time_time_zone_id: Option<String>,
    /// Query parameter: `interval.endTime.timeZone.version`.
    pub interval_end_time_time_zone_version: Option<String>,
    /// Query parameter: `interval.endTime.utcOffset`.
    pub interval_end_time_utc_offset: Option<String>,
    /// Query parameter: `interval.endTime.year`.
    pub interval_end_time_year: Option<String>,
    /// Query parameter: `interval.startTime.day`.
    pub interval_start_time_day: Option<String>,
    /// Query parameter: `interval.startTime.hours`.
    pub interval_start_time_hours: Option<String>,
    /// Query parameter: `interval.startTime.minutes`.
    pub interval_start_time_minutes: Option<String>,
    /// Query parameter: `interval.startTime.month`.
    pub interval_start_time_month: Option<String>,
    /// Query parameter: `interval.startTime.nanos`.
    pub interval_start_time_nanos: Option<String>,
    /// Query parameter: `interval.startTime.seconds`.
    pub interval_start_time_seconds: Option<String>,
    /// Query parameter: `interval.startTime.timeZone.id`.
    pub interval_start_time_time_zone_id: Option<String>,
    /// Query parameter: `interval.startTime.timeZone.version`.
    pub interval_start_time_time_zone_version: Option<String>,
    /// Query parameter: `interval.startTime.utcOffset`.
    pub interval_start_time_utc_offset: Option<String>,
    /// Query parameter: `interval.startTime.year`.
    pub interval_start_time_year: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`playdeveloperreporting.vitals.excessivewakeuprate.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsExcessivewakeuprateGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`playdeveloperreporting.vitals.excessivewakeuprate.query_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsExcessivewakeuprateQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetRequest,
}

/// Arguments for [`playdeveloperreporting.vitals.lmkrate.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsLmkrateGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`playdeveloperreporting.vitals.lmkrate.query_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsLmkrateQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetRequest,
}

/// Arguments for [`playdeveloperreporting.vitals.slowrenderingrate.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsSlowrenderingrateGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`playdeveloperreporting.vitals.slowrenderingrate.query_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsSlowrenderingrateQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetRequest,
}

/// Arguments for [`playdeveloperreporting.vitals.slowstartrate.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsSlowstartrateGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`playdeveloperreporting.vitals.slowstartrate.query_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsSlowstartrateQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetRequest,
}

/// Arguments for [`playdeveloperreporting.vitals.stuckbackgroundwakelockrate.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsStuckbackgroundwakelockrateGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`playdeveloperreporting.vitals.stuckbackgroundwakelockrate.query_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct PlaydeveloperreportingVitalsStuckbackgroundwakelockrateQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1beta1/{+parent}/anomalies
// -----------------------------------------------------------------------------

/// GET v1beta1/{+parent}/anomalies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_anomalies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_anomalies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingAnomaliesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}/anomalies",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ListAnomaliesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1beta1/{+name}:fetchReleaseFilterOptions
// -----------------------------------------------------------------------------

/// GET v1beta1/{+name}:fetchReleaseFilterOptions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_apps_fetch_release_filter_options_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_apps_fetch_release_filter_options_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingAppsFetchReleaseFilterOptionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}:fetchReleaseFilterOptions",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ReleaseFilterOptions =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1beta1/apps:search
// -----------------------------------------------------------------------------

/// GET v1beta1/apps:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_apps_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_apps_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingAppsSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://playdeveloperreporting.googleapis.com/v1beta1/apps:search",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SearchAccessibleAppsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1beta1/{+name}
// -----------------------------------------------------------------------------

/// GET v1beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_anrrate_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_anrrate_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsAnrrateGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1AnrRateMetricSet>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1AnrRateMetricSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta1/{+name}:query
// -----------------------------------------------------------------------------

/// POST v1beta1/{+name}:query.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_anrrate_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_anrrate_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsAnrrateQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}:query",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryAnrRateMetricSetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1beta1/{+name}
// -----------------------------------------------------------------------------

/// GET v1beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_crashrate_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_crashrate_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsCrashrateGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1CrashRateMetricSet>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1CrashRateMetricSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta1/{+name}:query
// -----------------------------------------------------------------------------

/// POST v1beta1/{+name}:query.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_crashrate_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_crashrate_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsCrashrateQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}:query",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryCrashRateMetricSetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1beta1/{+name}
// -----------------------------------------------------------------------------

/// GET v1beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_errors_counts_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_errors_counts_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsErrorsCountsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ErrorCountMetricSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta1/{+name}:query
// -----------------------------------------------------------------------------

/// POST v1beta1/{+name}:query.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_errors_counts_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_errors_counts_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsErrorsCountsQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}:query",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryErrorCountMetricSetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1beta1/{+parent}/errorIssues:search
// -----------------------------------------------------------------------------

/// GET v1beta1/{+parent}/errorIssues:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_errors_issues_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_errors_issues_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsErrorsIssuesSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}/errorIssues:search",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_hours {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_hours=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_minutes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_minutes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_nanos {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_nanos=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_seconds {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_seconds=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_time_zone_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_timeZone_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_time_zone_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_timeZone_version=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_utc_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_utcOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_hours {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_hours=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_minutes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_minutes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_nanos {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_nanos=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_seconds {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_seconds=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_time_zone_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_timeZone_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_time_zone_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_timeZone_version=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_utc_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_utcOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sample_error_report_limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sampleErrorReportLimit=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SearchErrorIssuesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1beta1/{+parent}/errorReports:search
// -----------------------------------------------------------------------------

/// GET v1beta1/{+parent}/errorReports:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_errors_reports_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_errors_reports_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsErrorsReportsSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}/errorReports:search",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_hours {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_hours=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_minutes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_minutes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_nanos {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_nanos=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_seconds {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_seconds=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_time_zone_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_timeZone_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_time_zone_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_timeZone_version=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_utc_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_utcOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_end_time_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_endTime_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_hours {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_hours=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_minutes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_minutes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_nanos {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_nanos=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_seconds {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_seconds=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_time_zone_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_timeZone_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_time_zone_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_timeZone_version=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_utc_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_utcOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.interval_start_time_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("interval_startTime_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SearchErrorReportsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1beta1/{+name}
// -----------------------------------------------------------------------------

/// GET v1beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_excessivewakeuprate_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_excessivewakeuprate_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsExcessivewakeuprateGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1ExcessiveWakeupRateMetricSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta1/{+name}:query
// -----------------------------------------------------------------------------

/// POST v1beta1/{+name}:query.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_excessivewakeuprate_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_excessivewakeuprate_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsExcessivewakeuprateQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse,
                >,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}:query",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(
        builder
            .build_send_request()
            .map_err(|e: foundation_core::wire::simple_http::HttpClientError| super::shared::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                super::shared::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                    let parsed: GooglePlayDeveloperReportingV1beta1QueryExcessiveWakeupRateMetricSetResponse = serde_json::from_str(&body).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                super::shared::RequestIntro::Failed(e) => Err(super::shared::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| super::shared::ApiPending::Sending)
    )
}

// -----------------------------------------------------------------------------
// GET v1beta1/{+name}
// -----------------------------------------------------------------------------

/// GET v1beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_lmkrate_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_lmkrate_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsLmkrateGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1LmkRateMetricSet>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1LmkRateMetricSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta1/{+name}:query
// -----------------------------------------------------------------------------

/// POST v1beta1/{+name}:query.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_lmkrate_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_lmkrate_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsLmkrateQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}:query",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QueryLmkRateMetricSetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1beta1/{+name}
// -----------------------------------------------------------------------------

/// GET v1beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_slowrenderingrate_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_slowrenderingrate_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsSlowrenderingrateGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SlowRenderingRateMetricSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta1/{+name}:query
// -----------------------------------------------------------------------------

/// POST v1beta1/{+name}:query.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_slowrenderingrate_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_slowrenderingrate_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsSlowrenderingrateQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse,
                >,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}:query",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(
        builder
            .build_send_request()
            .map_err(|e: foundation_core::wire::simple_http::HttpClientError| super::shared::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                super::shared::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                    let parsed: GooglePlayDeveloperReportingV1beta1QuerySlowRenderingRateMetricSetResponse = serde_json::from_str(&body).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                super::shared::RequestIntro::Failed(e) => Err(super::shared::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| super::shared::ApiPending::Sending)
    )
}

// -----------------------------------------------------------------------------
// GET v1beta1/{+name}
// -----------------------------------------------------------------------------

/// GET v1beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_slowstartrate_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_slowstartrate_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsSlowstartrateGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1SlowStartRateMetricSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1beta1/{+name}:query
// -----------------------------------------------------------------------------

/// POST v1beta1/{+name}:query.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_slowstartrate_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_slowstartrate_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsSlowstartrateQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}:query",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GooglePlayDeveloperReportingV1beta1QuerySlowStartRateMetricSetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1beta1/{+name}
// -----------------------------------------------------------------------------

/// GET v1beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsStuckbackgroundwakelockrateGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet,
                >,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(
        builder
            .build_send_request()
            .map_err(|e: foundation_core::wire::simple_http::HttpClientError| super::shared::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                super::shared::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                    let parsed: GooglePlayDeveloperReportingV1beta1StuckBackgroundWakelockRateMetricSet = serde_json::from_str(&body).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                super::shared::RequestIntro::Failed(e) => Err(super::shared::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| super::shared::ApiPending::Sending)
    )
}

// -----------------------------------------------------------------------------
// POST v1beta1/{+name}:query
// -----------------------------------------------------------------------------

/// POST v1beta1/{+name}:query.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn playdeveloperreporting_vitals_stuckbackgroundwakelockrate_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &PlaydeveloperreportingVitalsStuckbackgroundwakelockrateQueryArgs,
    builder_mod: Option<F>,
) -> Result<impl TaskIterator<Ready = Result<ApiResponse<GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse>, super::shared::ApiError>, Pending = super::shared::ApiPending, Spawner = super::shared::BoxedSendExecutionAction> + Send + 'static, super::shared::ApiError>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://playdeveloperreporting.googleapis.com/v1beta1/{}:query",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(
        builder
            .build_send_request()
            .map_err(|e: foundation_core::wire::simple_http::HttpClientError| super::shared::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                super::shared::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                    let parsed: GooglePlayDeveloperReportingV1beta1QueryStuckBackgroundWakelockRateMetricSetResponse = serde_json::from_str(&body).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                super::shared::RequestIntro::Failed(e) => Err(super::shared::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| super::shared::ApiPending::Sending)
    )
}
