//! Auto-generated API module for gcp/gamesManagement hidden.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_gamesManagement_hidden `

#![cfg(feature = "gcp_gamesManagement_hidden")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `HiddenPlayerList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HiddenPlayerList {
    /// items property.
    pub items: Option<Vec<HiddenPlayer>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Player` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Player {
    /// avatarImageUrl property.
    pub avatar_image_url: Option<String>,
    /// bannerUrlLandscape property.
    pub banner_url_landscape: Option<String>,
    /// bannerUrlPortrait property.
    pub banner_url_portrait: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// experienceInfo property.
    pub experience_info: Option<GamesPlayerExperienceInfoResource>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// originalPlayerId property.
    pub original_player_id: Option<String>,
    /// playerId property.
    pub player_id: Option<String>,
    /// profileSettings property.
    pub profile_settings: Option<ProfileSettings>,
    /// title property.
    pub title: Option<String>,
}

/// `GamesPlayerExperienceInfoResource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GamesPlayerExperienceInfoResource {
    /// currentExperiencePoints property.
    pub current_experience_points: Option<String>,
    /// currentLevel property.
    pub current_level: Option<GamesPlayerLevelResource>,
    /// lastLevelUpTimestampMillis property.
    pub last_level_up_timestamp_millis: Option<String>,
    /// nextLevel property.
    pub next_level: Option<GamesPlayerLevelResource>,
}

/// `GamesPlayerLevelResource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GamesPlayerLevelResource {
    /// level property.
    pub level: Option<i64>,
    /// maxExperiencePoints property.
    pub max_experience_points: Option<String>,
    /// minExperiencePoints property.
    pub min_experience_points: Option<String>,
}

/// `HiddenPlayer` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HiddenPlayer {
    /// hiddenTimeMillis property.
    pub hidden_time_millis: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// player property.
    pub player: Option<Player>,
}

/// `ProfileSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProfileSettings {
    /// kind property.
    pub kind: Option<String>,
    /// profileVisible property.
    pub profile_visible: Option<bool>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`gamesManagement.applications.listHidden_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementApplicationsListHiddenArgs {
    /// Path parameter: `applicationId`.
    pub application_id: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`gamesManagement.players.hide_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementPlayersHideArgs {
    /// Path parameter: `applicationId`.
    pub application_id: String,
    /// Path parameter: `playerId`.
    pub player_id: String,
}

/// Arguments for [`gamesManagement.players.unhide_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementPlayersUnhideArgs {
    /// Path parameter: `applicationId`.
    pub application_id: String,
    /// Path parameter: `playerId`.
    pub player_id: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET games/v1management/applications/{applicationId}/players/hidden
// -----------------------------------------------------------------------------

/// GET games/v1management/applications/{applicationId}/players/hidden.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = games_management_applications_list_hidden_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn games_management_applications_list_hidden_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GamesManagementApplicationsListHiddenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HiddenPlayerList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/applications/{}/players/hidden",
        args.application_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: HiddenPlayerList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST games/v1management/applications/{applicationId}/players/hidden/{playerId}
// -----------------------------------------------------------------------------

/// POST games/v1management/applications/{applicationId}/players/hidden/{playerId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = games_management_players_hide_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn games_management_players_hide_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GamesManagementPlayersHideArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/applications/{}/players/hidden/{}",
        args.application_id,
        args.player_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE games/v1management/applications/{applicationId}/players/hidden/{playerId}
// -----------------------------------------------------------------------------

/// DELETE games/v1management/applications/{applicationId}/players/hidden/{playerId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = games_management_players_unhide_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn games_management_players_unhide_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GamesManagementPlayersUnhideArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/applications/{}/players/hidden/{}",
        args.application_id,
        args.player_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
