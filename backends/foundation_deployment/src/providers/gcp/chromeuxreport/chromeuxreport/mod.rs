//! Auto-generated API module for gcp/chromeuxreport chromeuxreport.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_chromeuxreport_chromeuxreport `

#![cfg(feature = "gcp_chromeuxreport_chromeuxreport")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `UrlNormalization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlNormalization {
    /// normalizedUrl property.
    pub normalized_url: Option<String>,
    /// originalUrl property.
    pub original_url: Option<String>,
}

/// `QueryHistoryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryHistoryResponse {
    /// record property.
    pub record: Option<HistoryRecord>,
    /// urlNormalizationDetails property.
    pub url_normalization_details: Option<UrlNormalization>,
}

/// `QueryRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryRequest {
    /// effectiveConnectionType property.
    pub effective_connection_type: Option<String>,
    /// formFactor property.
    pub form_factor: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// origin property.
    pub origin: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `Date` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `Record` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Record {
    /// collectionPeriod property.
    pub collection_period: Option<CollectionPeriod>,
    /// key property.
    pub key: Option<Key>,
    /// metrics property.
    pub metrics: Option<serde_json::Value>,
}

/// `QueryHistoryRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryHistoryRequest {
    /// collectionPeriodCount property.
    pub collection_period_count: Option<i64>,
    /// formFactor property.
    pub form_factor: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// origin property.
    pub origin: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `HistoryKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HistoryKey {
    /// formFactor property.
    pub form_factor: Option<String>,
    /// origin property.
    pub origin: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `CollectionPeriod` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CollectionPeriod {
    /// firstDate property.
    pub first_date: Option<Date>,
    /// lastDate property.
    pub last_date: Option<Date>,
}

/// `QueryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryResponse {
    /// record property.
    pub record: Option<Record>,
    /// urlNormalizationDetails property.
    pub url_normalization_details: Option<UrlNormalization>,
}

/// `HistoryRecord` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HistoryRecord {
    /// collectionPeriods property.
    pub collection_periods: Option<Vec<CollectionPeriod>>,
    /// key property.
    pub key: Option<HistoryKey>,
    /// metrics property.
    pub metrics: Option<serde_json::Value>,
}

/// `Key` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Key {
    /// effectiveConnectionType property.
    pub effective_connection_type: Option<String>,
    /// formFactor property.
    pub form_factor: Option<String>,
    /// origin property.
    pub origin: Option<String>,
    /// url property.
    pub url: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`chromeuxreport.records.queryHistoryRecord_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromeuxreportRecordsQueryHistoryRecordArgs {
    /// Request body.
    pub body: QueryHistoryRequest,
}

/// Arguments for [`chromeuxreport.records.queryRecord_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromeuxreportRecordsQueryRecordArgs {
    /// Request body.
    pub body: QueryRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/records:queryHistoryRecord
// -----------------------------------------------------------------------------

/// POST v1/records:queryHistoryRecord.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromeuxreport_records_query_history_record_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromeuxreport_records_query_history_record_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromeuxreportRecordsQueryHistoryRecordArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryHistoryResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://chromeuxreport.googleapis.com/v1/records:queryHistoryRecord",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: QueryHistoryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/records:queryRecord
// -----------------------------------------------------------------------------

/// POST v1/records:queryRecord.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromeuxreport_records_query_record_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromeuxreport_records_query_record_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromeuxreportRecordsQueryRecordArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromeuxreport.googleapis.com/v1/records:queryRecord",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: QueryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
