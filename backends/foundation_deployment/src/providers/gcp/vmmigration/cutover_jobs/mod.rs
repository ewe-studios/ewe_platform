//! Auto-generated API module for gcp/vmmigration cutoverJobs.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_vmmigration_cutover_jobs `

#![cfg(feature = "gcp_vmmigration_cutover_jobs")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ComputeEngineTargetDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComputeEngineTargetDetails {
    /// adaptationModifiers property.
    pub adaptation_modifiers: Option<Vec<AdaptationModifier>>,
    /// additionalLicenses property.
    pub additional_licenses: Option<Vec<String>>,
    /// appliedLicense property.
    pub applied_license: Option<AppliedLicense>,
    /// bootConversion property.
    pub boot_conversion: Option<String>,
    /// bootOption property.
    pub boot_option: Option<String>,
    /// computeScheduling property.
    pub compute_scheduling: Option<ComputeScheduling>,
    /// diskReplicaZones property.
    pub disk_replica_zones: Option<Vec<String>>,
    /// diskType property.
    pub disk_type: Option<String>,
    /// enableIntegrityMonitoring property.
    pub enable_integrity_monitoring: Option<bool>,
    /// enableVtpm property.
    pub enable_vtpm: Option<bool>,
    /// encryption property.
    pub encryption: Option<Encryption>,
    /// hostname property.
    pub hostname: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// licenseType property.
    pub license_type: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// machineTypeSeries property.
    pub machine_type_series: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// networkInterfaces property.
    pub network_interfaces: Option<Vec<NetworkInterface>>,
    /// networkTags property.
    pub network_tags: Option<Vec<String>>,
    /// project property.
    pub project: Option<String>,
    /// secureBoot property.
    pub secure_boot: Option<bool>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// storagePool property.
    pub storage_pool: Option<String>,
    /// vmName property.
    pub vm_name: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `ComputeEngineDisksTargetDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComputeEngineDisksTargetDetails {
    /// disks property.
    pub disks: Option<Vec<PersistentDisk>>,
    /// disksTargetDetails property.
    pub disks_target_details: Option<DisksMigrationDisksTargetDetails>,
    /// vmTargetDetails property.
    pub vm_target_details: Option<DisksMigrationVmTargetDetails>,
}

/// `MigrationWarning` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MigrationWarning {
    /// actionItem property.
    pub action_item: Option<LocalizedMessage>,
    /// code property.
    pub code: Option<String>,
    /// helpLinks property.
    pub help_links: Option<Vec<Link>>,
    /// warningMessage property.
    pub warning_message: Option<LocalizedMessage>,
    /// warningTime property.
    pub warning_time: Option<String>,
}

/// `PreparingVMDisksStep` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PreparingVMDisksStep {}

/// `PostProcessingStep` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PostProcessingStep {}

/// `AppliedLicense` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AppliedLicense {
    /// osLicense property.
    pub os_license: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Encryption` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Encryption {
    /// kmsKey property.
    pub kms_key: Option<String>,
}

/// `ListCutoverJobsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListCutoverJobsResponse {
    /// cutoverJobs property.
    pub cutover_jobs: Option<Vec<CutoverJob>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `InstantiatingMigratedVMStep` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstantiatingMigratedVMStep {}

/// `ShuttingDownSourceVMStep` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ShuttingDownSourceVMStep {}

/// `CutoverStep` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CutoverStep {
    /// endTime property.
    pub end_time: Option<String>,
    /// finalSync property.
    pub final_sync: Option<ReplicationCycle>,
    /// instantiatingMigratedVm property.
    pub instantiating_migrated_vm: Option<InstantiatingMigratedVMStep>,
    /// preparingVmDisks property.
    pub preparing_vm_disks: Option<PreparingVMDisksStep>,
    /// previousReplicationCycle property.
    pub previous_replication_cycle: Option<ReplicationCycle>,
    /// shuttingDownSourceVm property.
    pub shutting_down_source_vm: Option<ShuttingDownSourceVMStep>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `ReplicationCycle` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReplicationCycle {
    /// cycleNumber property.
    pub cycle_number: Option<i64>,
    /// endTime property.
    pub end_time: Option<String>,
    /// error property.
    pub error: Option<Status>,
    /// name property.
    pub name: Option<String>,
    /// progressPercent property.
    pub progress_percent: Option<i64>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// steps property.
    pub steps: Option<Vec<CycleStep>>,
    /// totalPauseDuration property.
    pub total_pause_duration: Option<String>,
    /// warnings property.
    pub warnings: Option<Vec<MigrationWarning>>,
}

/// `AdaptationModifier` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AdaptationModifier {
    /// modifier property.
    pub modifier: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `SchedulingNodeAffinity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SchedulingNodeAffinity {
    /// key property.
    pub key: Option<String>,
    /// operator property.
    pub operator: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `InitializingReplicationStep` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InitializingReplicationStep {}

/// `PersistentDisk` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PersistentDisk {
    /// diskUri property.
    pub disk_uri: Option<String>,
    /// sourceDiskNumber property.
    pub source_disk_number: Option<i64>,
}

/// `DisksMigrationDisksTargetDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DisksMigrationDisksTargetDetails {}

/// `CycleStep` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CycleStep {
    /// endTime property.
    pub end_time: Option<String>,
    /// initializingReplication property.
    pub initializing_replication: Option<InitializingReplicationStep>,
    /// postProcessing property.
    pub post_processing: Option<PostProcessingStep>,
    /// replicating property.
    pub replicating: Option<ReplicatingStep>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `LocalizedMessage` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LocalizedMessage {
    /// locale property.
    pub locale: Option<String>,
    /// message property.
    pub message: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `CutoverJob` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CutoverJob {
    /// computeEngineDisksTargetDetails property.
    pub compute_engine_disks_target_details: Option<ComputeEngineDisksTargetDetails>,
    /// computeEngineTargetDetails property.
    pub compute_engine_target_details: Option<ComputeEngineTargetDetails>,
    /// createTime property.
    pub create_time: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// error property.
    pub error: Option<Status>,
    /// name property.
    pub name: Option<String>,
    /// progressPercent property.
    pub progress_percent: Option<i64>,
    /// state property.
    pub state: Option<String>,
    /// stateMessage property.
    pub state_message: Option<String>,
    /// stateTime property.
    pub state_time: Option<String>,
    /// steps property.
    pub steps: Option<Vec<CutoverStep>>,
}

/// `NetworkInterface` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NetworkInterface {
    /// externalIp property.
    pub external_ip: Option<String>,
    /// internalIp property.
    pub internal_ip: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// networkTier property.
    pub network_tier: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
}

/// `DisksMigrationVmTargetDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DisksMigrationVmTargetDetails {
    /// vmUri property.
    pub vm_uri: Option<String>,
}

/// `ReplicatingStep` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReplicatingStep {
    /// lastThirtyMinutesAverageBytesPerSecond property.
    pub last_thirty_minutes_average_bytes_per_second: Option<String>,
    /// lastTwoMinutesAverageBytesPerSecond property.
    pub last_two_minutes_average_bytes_per_second: Option<String>,
    /// replicatedBytes property.
    pub replicated_bytes: Option<String>,
    /// totalBytes property.
    pub total_bytes: Option<String>,
}

/// `Link` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Link {
    /// description property.
    pub description: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `ComputeScheduling` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComputeScheduling {
    /// minNodeCpus property.
    pub min_node_cpus: Option<i64>,
    /// nodeAffinities property.
    pub node_affinities: Option<Vec<SchedulingNodeAffinity>>,
    /// onHostMaintenance property.
    pub on_host_maintenance: Option<String>,
    /// restartType property.
    pub restart_type: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`vmmigration.projects.locations.sources.migratingVms.cutoverJobs.cancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`vmmigration.projects.locations.sources.migratingVms.cutoverJobs.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `cutoverJobId`.
    pub cutover_job_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`vmmigration.projects.locations.sources.migratingVms.cutoverJobs.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`vmmigration.projects.locations.sources.migratingVms.cutoverJobs.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VmmigrationProjectsLocationsSourcesMigratingVmsCutoverJobsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}:cancel
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sources/{{sourcesId}}/migratingVms/{{migratingVmsId}}/cutoverJobs/{{cutoverJobsId}}:cancel",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sources/{{sourcesId}}/migratingVms/{{migratingVmsId}}/cutoverJobs",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs/{cutoverJobsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CutoverJob>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sources/{{sourcesId}}/migratingVms/{{migratingVmsId}}/cutoverJobs/{{cutoverJobsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CutoverJob =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sources/{sourcesId}/migratingVms/{migratingVmsId}/cutoverJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn vmmigration_projects_locations_sources_migrating_vms_cutover_jobs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCutoverJobsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://vmmigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sources/{{sourcesId}}/migratingVms/{{migratingVmsId}}/cutoverJobs",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCutoverJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
