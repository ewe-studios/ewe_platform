//! Auto-generated API module for gcp/dataproc sparkApplications.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dataproc_spark_applications `

#![cfg(feature = "gcp_dataproc_spark_applications")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SearchSessionSparkApplicationSqlQueriesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationSqlQueriesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationSqlQueries property.
    pub spark_application_sql_queries: Option<Vec<SqlExecutionUiData>>,
}

/// `JobsSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct JobsSummary {
    /// activeJobs property.
    pub active_jobs: Option<i64>,
    /// applicationId property.
    pub application_id: Option<String>,
    /// attempts property.
    pub attempts: Option<Vec<ApplicationAttemptInfo>>,
    /// completedJobs property.
    pub completed_jobs: Option<i64>,
    /// failedJobs property.
    pub failed_jobs: Option<i64>,
    /// schedulingMode property.
    pub scheduling_mode: Option<String>,
}

/// `AccessSessionSparkApplicationJobResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationJobResponse {
    /// jobData property.
    pub job_data: Option<JobData>,
}

/// `InputQuantileMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InputQuantileMetrics {
    /// bytesRead property.
    pub bytes_read: Option<Quantiles>,
    /// recordsRead property.
    pub records_read: Option<Quantiles>,
}

/// `ApplicationAttemptInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApplicationAttemptInfo {
    /// appSparkVersion property.
    pub app_spark_version: Option<String>,
    /// attemptId property.
    pub attempt_id: Option<String>,
    /// completed property.
    pub completed: Option<bool>,
    /// durationMillis property.
    pub duration_millis: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// lastUpdated property.
    pub last_updated: Option<String>,
    /// sparkUser property.
    pub spark_user: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `SearchSparkApplicationStageAttemptsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationStageAttemptsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageAttempts property.
    pub spark_application_stage_attempts: Option<Vec<StageData>>,
}

/// `AccessSessionSparkApplicationEnvironmentInfoResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationEnvironmentInfoResponse {
    /// applicationEnvironmentInfo property.
    pub application_environment_info: Option<ApplicationEnvironmentInfo>,
}

/// `WriteSessionSparkApplicationContextResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WriteSessionSparkApplicationContextResponse {}

/// `TaskMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TaskMetrics {
    /// diskBytesSpilled property.
    pub disk_bytes_spilled: Option<String>,
    /// executorCpuTimeNanos property.
    pub executor_cpu_time_nanos: Option<String>,
    /// executorDeserializeCpuTimeNanos property.
    pub executor_deserialize_cpu_time_nanos: Option<String>,
    /// executorDeserializeTimeMillis property.
    pub executor_deserialize_time_millis: Option<String>,
    /// executorRunTimeMillis property.
    pub executor_run_time_millis: Option<String>,
    /// inputMetrics property.
    pub input_metrics: Option<InputMetrics>,
    /// jvmGcTimeMillis property.
    pub jvm_gc_time_millis: Option<String>,
    /// memoryBytesSpilled property.
    pub memory_bytes_spilled: Option<String>,
    /// outputMetrics property.
    pub output_metrics: Option<OutputMetrics>,
    /// peakExecutionMemoryBytes property.
    pub peak_execution_memory_bytes: Option<String>,
    /// resultSerializationTimeMillis property.
    pub result_serialization_time_millis: Option<String>,
    /// resultSize property.
    pub result_size: Option<String>,
    /// shuffleReadMetrics property.
    pub shuffle_read_metrics: Option<ShuffleReadMetrics>,
    /// shuffleWriteMetrics property.
    pub shuffle_write_metrics: Option<ShuffleWriteMetrics>,
}

/// `AccessSparkApplicationStageRddOperationGraphResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationStageRddOperationGraphResponse {
    /// rddOperationGraph property.
    pub rdd_operation_graph: Option<RddOperationGraph>,
}

/// `WriteSparkApplicationContextResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WriteSparkApplicationContextResponse {}

/// `SummarizeSessionSparkApplicationExecutorsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSessionSparkApplicationExecutorsResponse {
    /// activeExecutorSummary property.
    pub active_executor_summary: Option<ConsolidatedExecutorSummary>,
    /// applicationId property.
    pub application_id: Option<String>,
    /// deadExecutorSummary property.
    pub dead_executor_summary: Option<ConsolidatedExecutorSummary>,
    /// totalExecutorSummary property.
    pub total_executor_summary: Option<ConsolidatedExecutorSummary>,
}

/// `OutputMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OutputMetrics {
    /// bytesWritten property.
    pub bytes_written: Option<String>,
    /// recordsWritten property.
    pub records_written: Option<String>,
}

/// `TaskData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TaskData {
    /// accumulatorUpdates property.
    pub accumulator_updates: Option<Vec<AccumulableInfo>>,
    /// attempt property.
    pub attempt: Option<i64>,
    /// durationMillis property.
    pub duration_millis: Option<String>,
    /// errorMessage property.
    pub error_message: Option<String>,
    /// executorId property.
    pub executor_id: Option<String>,
    /// executorLogs property.
    pub executor_logs: Option<serde_json::Value>,
    /// gettingResultTimeMillis property.
    pub getting_result_time_millis: Option<String>,
    /// hasMetrics property.
    pub has_metrics: Option<bool>,
    /// host property.
    pub host: Option<String>,
    /// index property.
    pub index: Option<i64>,
    /// launchTime property.
    pub launch_time: Option<String>,
    /// partitionId property.
    pub partition_id: Option<i64>,
    /// resultFetchStart property.
    pub result_fetch_start: Option<String>,
    /// schedulerDelayMillis property.
    pub scheduler_delay_millis: Option<String>,
    /// speculative property.
    pub speculative: Option<bool>,
    /// stageAttemptId property.
    pub stage_attempt_id: Option<i64>,
    /// stageId property.
    pub stage_id: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// taskId property.
    pub task_id: Option<String>,
    /// taskLocality property.
    pub task_locality: Option<String>,
    /// taskMetrics property.
    pub task_metrics: Option<TaskMetrics>,
}

/// `AccessSparkApplicationSqlSparkPlanGraphResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationSqlSparkPlanGraphResponse {
    /// sparkPlanGraph property.
    pub spark_plan_graph: Option<SparkPlanGraph>,
}

/// `SearchSparkApplicationExecutorStageSummaryResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationExecutorStageSummaryResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageExecutors property.
    pub spark_application_stage_executors: Option<Vec<ExecutorStageSummary>>,
}

/// `AccessSessionSparkApplicationStageAttemptResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationStageAttemptResponse {
    /// stageData property.
    pub stage_data: Option<StageData>,
}

/// `AccumulableInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccumulableInfo {
    /// accumullableInfoId property.
    pub accumullable_info_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// update property.
    pub update: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `StageShuffleReadMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StageShuffleReadMetrics {
    /// bytesRead property.
    pub bytes_read: Option<String>,
    /// fetchWaitTimeMillis property.
    pub fetch_wait_time_millis: Option<String>,
    /// localBlocksFetched property.
    pub local_blocks_fetched: Option<String>,
    /// localBytesRead property.
    pub local_bytes_read: Option<String>,
    /// recordsRead property.
    pub records_read: Option<String>,
    /// remoteBlocksFetched property.
    pub remote_blocks_fetched: Option<String>,
    /// remoteBytesRead property.
    pub remote_bytes_read: Option<String>,
    /// remoteBytesReadToDisk property.
    pub remote_bytes_read_to_disk: Option<String>,
    /// remoteReqsDuration property.
    pub remote_reqs_duration: Option<String>,
    /// stageShufflePushReadMetrics property.
    pub stage_shuffle_push_read_metrics: Option<StageShufflePushReadMetrics>,
}

/// `ShuffleReadMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ShuffleReadMetrics {
    /// fetchWaitTimeMillis property.
    pub fetch_wait_time_millis: Option<String>,
    /// localBlocksFetched property.
    pub local_blocks_fetched: Option<String>,
    /// localBytesRead property.
    pub local_bytes_read: Option<String>,
    /// recordsRead property.
    pub records_read: Option<String>,
    /// remoteBlocksFetched property.
    pub remote_blocks_fetched: Option<String>,
    /// remoteBytesRead property.
    pub remote_bytes_read: Option<String>,
    /// remoteBytesReadToDisk property.
    pub remote_bytes_read_to_disk: Option<String>,
    /// remoteReqsDuration property.
    pub remote_reqs_duration: Option<String>,
    /// shufflePushReadMetrics property.
    pub shuffle_push_read_metrics: Option<ShufflePushReadMetrics>,
}

/// `SparkRuntimeInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SparkRuntimeInfo {
    /// javaHome property.
    pub java_home: Option<String>,
    /// javaVersion property.
    pub java_version: Option<String>,
    /// scalaVersion property.
    pub scala_version: Option<String>,
}

/// `ConsolidatedExecutorSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConsolidatedExecutorSummary {
    /// activeTasks property.
    pub active_tasks: Option<i64>,
    /// completedTasks property.
    pub completed_tasks: Option<i64>,
    /// count property.
    pub count: Option<i64>,
    /// diskUsed property.
    pub disk_used: Option<String>,
    /// failedTasks property.
    pub failed_tasks: Option<i64>,
    /// isExcluded property.
    pub is_excluded: Option<i64>,
    /// maxMemory property.
    pub max_memory: Option<String>,
    /// memoryMetrics property.
    pub memory_metrics: Option<MemoryMetrics>,
    /// memoryUsed property.
    pub memory_used: Option<String>,
    /// rddBlocks property.
    pub rdd_blocks: Option<i64>,
    /// totalCores property.
    pub total_cores: Option<i64>,
    /// totalDurationMillis property.
    pub total_duration_millis: Option<String>,
    /// totalGcTimeMillis property.
    pub total_gc_time_millis: Option<String>,
    /// totalInputBytes property.
    pub total_input_bytes: Option<String>,
    /// totalShuffleRead property.
    pub total_shuffle_read: Option<String>,
    /// totalShuffleWrite property.
    pub total_shuffle_write: Option<String>,
    /// totalTasks property.
    pub total_tasks: Option<i64>,
}

/// `ExecutorMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExecutorMetrics {
    /// metrics property.
    pub metrics: Option<serde_json::Value>,
}

/// `StageInputMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StageInputMetrics {
    /// bytesRead property.
    pub bytes_read: Option<String>,
    /// recordsRead property.
    pub records_read: Option<String>,
}

/// `ResourceProfileInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ResourceProfileInfo {
    /// executorResources property.
    pub executor_resources: Option<serde_json::Value>,
    /// resourceProfileId property.
    pub resource_profile_id: Option<i64>,
    /// taskResources property.
    pub task_resources: Option<serde_json::Value>,
}

/// `ShuffleWriteQuantileMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ShuffleWriteQuantileMetrics {
    /// writeBytes property.
    pub write_bytes: Option<Quantiles>,
    /// writeRecords property.
    pub write_records: Option<Quantiles>,
    /// writeTimeNanos property.
    pub write_time_nanos: Option<Quantiles>,
}

/// `MemoryMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MemoryMetrics {
    /// totalOffHeapStorageMemory property.
    pub total_off_heap_storage_memory: Option<String>,
    /// totalOnHeapStorageMemory property.
    pub total_on_heap_storage_memory: Option<String>,
    /// usedOffHeapStorageMemory property.
    pub used_off_heap_storage_memory: Option<String>,
    /// usedOnHeapStorageMemory property.
    pub used_on_heap_storage_memory: Option<String>,
}

/// `SearchSessionSparkApplicationExecutorsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationExecutorsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationExecutors property.
    pub spark_application_executors: Option<Vec<ExecutorSummary>>,
}

/// `SummarizeSparkApplicationStageAttemptTasksResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSparkApplicationStageAttemptTasksResponse {
    /// stageAttemptTasksSummary property.
    pub stage_attempt_tasks_summary: Option<StageAttemptTasksSummary>,
}

/// `AccessSessionSparkApplicationResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationResponse {
    /// application property.
    pub application: Option<ApplicationInfo>,
}

/// `SearchSessionSparkApplicationStageAttemptTasksResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationStageAttemptTasksResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageAttemptTasks property.
    pub spark_application_stage_attempt_tasks: Option<Vec<TaskData>>,
}

/// `OutputQuantileMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OutputQuantileMetrics {
    /// bytesWritten property.
    pub bytes_written: Option<Quantiles>,
    /// recordsWritten property.
    pub records_written: Option<Quantiles>,
}

/// `SqlPlanMetric` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SqlPlanMetric {
    /// accumulatorId property.
    pub accumulator_id: Option<String>,
    /// metricType property.
    pub metric_type: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `SparkPlanGraph` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SparkPlanGraph {
    /// edges property.
    pub edges: Option<Vec<SparkPlanGraphEdge>>,
    /// executionId property.
    pub execution_id: Option<String>,
    /// nodes property.
    pub nodes: Option<Vec<Box<SparkPlanGraphNodeWrapper>>>,
}

/// `StageShuffleWriteMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StageShuffleWriteMetrics {
    /// bytesWritten property.
    pub bytes_written: Option<String>,
    /// recordsWritten property.
    pub records_written: Option<String>,
    /// writeTimeNanos property.
    pub write_time_nanos: Option<String>,
}

/// `StageShufflePushReadMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StageShufflePushReadMetrics {
    /// corruptMergedBlockChunks property.
    pub corrupt_merged_block_chunks: Option<String>,
    /// localMergedBlocksFetched property.
    pub local_merged_blocks_fetched: Option<String>,
    /// localMergedBytesRead property.
    pub local_merged_bytes_read: Option<String>,
    /// localMergedChunksFetched property.
    pub local_merged_chunks_fetched: Option<String>,
    /// mergedFetchFallbackCount property.
    pub merged_fetch_fallback_count: Option<String>,
    /// remoteMergedBlocksFetched property.
    pub remote_merged_blocks_fetched: Option<String>,
    /// remoteMergedBytesRead property.
    pub remote_merged_bytes_read: Option<String>,
    /// remoteMergedChunksFetched property.
    pub remote_merged_chunks_fetched: Option<String>,
    /// remoteMergedReqsDuration property.
    pub remote_merged_reqs_duration: Option<String>,
}

/// `SparkPlanGraphCluster` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SparkPlanGraphCluster {
    /// desc property.
    pub desc: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// metrics property.
    pub metrics: Option<Vec<SqlPlanMetric>>,
    /// name property.
    pub name: Option<String>,
    /// nodes property.
    pub nodes: Option<Vec<Box<SparkPlanGraphNodeWrapper>>>,
    /// sparkPlanGraphClusterId property.
    pub spark_plan_graph_cluster_id: Option<String>,
}

/// `TaskQuantileMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TaskQuantileMetrics {
    /// diskBytesSpilled property.
    pub disk_bytes_spilled: Option<Quantiles>,
    /// durationMillis property.
    pub duration_millis: Option<Quantiles>,
    /// executorCpuTimeNanos property.
    pub executor_cpu_time_nanos: Option<Quantiles>,
    /// executorDeserializeCpuTimeNanos property.
    pub executor_deserialize_cpu_time_nanos: Option<Quantiles>,
    /// executorDeserializeTimeMillis property.
    pub executor_deserialize_time_millis: Option<Quantiles>,
    /// executorRunTimeMillis property.
    pub executor_run_time_millis: Option<Quantiles>,
    /// gettingResultTimeMillis property.
    pub getting_result_time_millis: Option<Quantiles>,
    /// inputMetrics property.
    pub input_metrics: Option<InputQuantileMetrics>,
    /// jvmGcTimeMillis property.
    pub jvm_gc_time_millis: Option<Quantiles>,
    /// memoryBytesSpilled property.
    pub memory_bytes_spilled: Option<Quantiles>,
    /// outputMetrics property.
    pub output_metrics: Option<OutputQuantileMetrics>,
    /// peakExecutionMemoryBytes property.
    pub peak_execution_memory_bytes: Option<Quantiles>,
    /// resultSerializationTimeMillis property.
    pub result_serialization_time_millis: Option<Quantiles>,
    /// resultSize property.
    pub result_size: Option<Quantiles>,
    /// schedulerDelayMillis property.
    pub scheduler_delay_millis: Option<Quantiles>,
    /// shuffleReadMetrics property.
    pub shuffle_read_metrics: Option<ShuffleReadQuantileMetrics>,
    /// shuffleWriteMetrics property.
    pub shuffle_write_metrics: Option<ShuffleWriteQuantileMetrics>,
}

/// `SparkPlanGraphEdge` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SparkPlanGraphEdge {
    /// fromId property.
    pub from_id: Option<String>,
    /// toId property.
    pub to_id: Option<String>,
}

/// `ShuffleWriteMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ShuffleWriteMetrics {
    /// bytesWritten property.
    pub bytes_written: Option<String>,
    /// recordsWritten property.
    pub records_written: Option<String>,
    /// writeTimeNanos property.
    pub write_time_nanos: Option<String>,
}

/// `AccessSparkApplicationStageAttemptResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationStageAttemptResponse {
    /// stageData property.
    pub stage_data: Option<StageData>,
}

/// `SearchSparkApplicationJobsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationJobsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationJobs property.
    pub spark_application_jobs: Option<Vec<JobData>>,
}

/// `JobData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct JobData {
    /// completionTime property.
    pub completion_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// jobGroup property.
    pub job_group: Option<String>,
    /// jobId property.
    pub job_id: Option<String>,
    /// killTasksSummary property.
    pub kill_tasks_summary: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// numActiveStages property.
    pub num_active_stages: Option<i64>,
    /// numActiveTasks property.
    pub num_active_tasks: Option<i64>,
    /// numCompletedIndices property.
    pub num_completed_indices: Option<i64>,
    /// numCompletedStages property.
    pub num_completed_stages: Option<i64>,
    /// numCompletedTasks property.
    pub num_completed_tasks: Option<i64>,
    /// numFailedStages property.
    pub num_failed_stages: Option<i64>,
    /// numFailedTasks property.
    pub num_failed_tasks: Option<i64>,
    /// numKilledTasks property.
    pub num_killed_tasks: Option<i64>,
    /// numSkippedStages property.
    pub num_skipped_stages: Option<i64>,
    /// numSkippedTasks property.
    pub num_skipped_tasks: Option<i64>,
    /// numTasks property.
    pub num_tasks: Option<i64>,
    /// skippedStages property.
    pub skipped_stages: Option<Vec<i64>>,
    /// sqlExecutionId property.
    pub sql_execution_id: Option<String>,
    /// stageIds property.
    pub stage_ids: Option<Vec<String>>,
    /// status property.
    pub status: Option<String>,
    /// submissionTime property.
    pub submission_time: Option<String>,
}

/// `SearchSessionSparkApplicationExecutorStageSummaryResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationExecutorStageSummaryResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageExecutors property.
    pub spark_application_stage_executors: Option<Vec<ExecutorStageSummary>>,
}

/// `SummarizeSessionSparkApplicationStageAttemptTasksResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSessionSparkApplicationStageAttemptTasksResponse {
    /// stageAttemptTasksSummary property.
    pub stage_attempt_tasks_summary: Option<StageAttemptTasksSummary>,
}

/// `StageMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StageMetrics {
    /// diskBytesSpilled property.
    pub disk_bytes_spilled: Option<String>,
    /// executorCpuTimeNanos property.
    pub executor_cpu_time_nanos: Option<String>,
    /// executorDeserializeCpuTimeNanos property.
    pub executor_deserialize_cpu_time_nanos: Option<String>,
    /// executorDeserializeTimeMillis property.
    pub executor_deserialize_time_millis: Option<String>,
    /// executorRunTimeMillis property.
    pub executor_run_time_millis: Option<String>,
    /// jvmGcTimeMillis property.
    pub jvm_gc_time_millis: Option<String>,
    /// memoryBytesSpilled property.
    pub memory_bytes_spilled: Option<String>,
    /// peakExecutionMemoryBytes property.
    pub peak_execution_memory_bytes: Option<String>,
    /// resultSerializationTimeMillis property.
    pub result_serialization_time_millis: Option<String>,
    /// resultSize property.
    pub result_size: Option<String>,
    /// stageInputMetrics property.
    pub stage_input_metrics: Option<StageInputMetrics>,
    /// stageOutputMetrics property.
    pub stage_output_metrics: Option<StageOutputMetrics>,
    /// stageShuffleReadMetrics property.
    pub stage_shuffle_read_metrics: Option<StageShuffleReadMetrics>,
    /// stageShuffleWriteMetrics property.
    pub stage_shuffle_write_metrics: Option<StageShuffleWriteMetrics>,
}

/// `ApplicationEnvironmentInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApplicationEnvironmentInfo {
    /// classpathEntries property.
    pub classpath_entries: Option<serde_json::Value>,
    /// hadoopProperties property.
    pub hadoop_properties: Option<serde_json::Value>,
    /// metricsProperties property.
    pub metrics_properties: Option<serde_json::Value>,
    /// resourceProfiles property.
    pub resource_profiles: Option<Vec<ResourceProfileInfo>>,
    /// runtime property.
    pub runtime: Option<SparkRuntimeInfo>,
    /// sparkProperties property.
    pub spark_properties: Option<serde_json::Value>,
    /// systemProperties property.
    pub system_properties: Option<serde_json::Value>,
}

/// `ShuffleReadQuantileMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ShuffleReadQuantileMetrics {
    /// fetchWaitTimeMillis property.
    pub fetch_wait_time_millis: Option<Quantiles>,
    /// localBlocksFetched property.
    pub local_blocks_fetched: Option<Quantiles>,
    /// readBytes property.
    pub read_bytes: Option<Quantiles>,
    /// readRecords property.
    pub read_records: Option<Quantiles>,
    /// remoteBlocksFetched property.
    pub remote_blocks_fetched: Option<Quantiles>,
    /// remoteBytesRead property.
    pub remote_bytes_read: Option<Quantiles>,
    /// remoteBytesReadToDisk property.
    pub remote_bytes_read_to_disk: Option<Quantiles>,
    /// remoteReqsDuration property.
    pub remote_reqs_duration: Option<Quantiles>,
    /// shufflePushReadMetrics property.
    pub shuffle_push_read_metrics: Option<ShufflePushReadQuantileMetrics>,
    /// totalBlocksFetched property.
    pub total_blocks_fetched: Option<Quantiles>,
}

/// `AccessSessionSparkApplicationSqlQueryResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationSqlQueryResponse {
    /// executionData property.
    pub execution_data: Option<SqlExecutionUiData>,
}

/// `SearchSparkApplicationStagesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationStagesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStages property.
    pub spark_application_stages: Option<Vec<StageData>>,
}

/// `StagesSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StagesSummary {
    /// applicationId property.
    pub application_id: Option<String>,
    /// numActiveStages property.
    pub num_active_stages: Option<i64>,
    /// numCompletedStages property.
    pub num_completed_stages: Option<i64>,
    /// numFailedStages property.
    pub num_failed_stages: Option<i64>,
    /// numPendingStages property.
    pub num_pending_stages: Option<i64>,
    /// numSkippedStages property.
    pub num_skipped_stages: Option<i64>,
}

/// `RddOperationNode` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RddOperationNode {
    /// barrier property.
    pub barrier: Option<bool>,
    /// cached property.
    pub cached: Option<bool>,
    /// callsite property.
    pub callsite: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nodeId property.
    pub node_id: Option<i64>,
    /// outputDeterministicLevel property.
    pub output_deterministic_level: Option<String>,
}

/// `ExecutorPeakMetricsDistributions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExecutorPeakMetricsDistributions {
    /// executorMetrics property.
    pub executor_metrics: Option<Vec<ExecutorMetrics>>,
    /// quantiles property.
    pub quantiles: Option<Vec<f64>>,
}

/// `SearchSparkApplicationExecutorsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationExecutorsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationExecutors property.
    pub spark_application_executors: Option<Vec<ExecutorSummary>>,
}

/// `SearchSessionSparkApplicationJobsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationJobsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationJobs property.
    pub spark_application_jobs: Option<Vec<JobData>>,
}

/// `SearchSessionSparkApplicationStageAttemptsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationStageAttemptsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageAttempts property.
    pub spark_application_stage_attempts: Option<Vec<StageData>>,
}

/// `ShufflePushReadMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ShufflePushReadMetrics {
    /// corruptMergedBlockChunks property.
    pub corrupt_merged_block_chunks: Option<String>,
    /// localMergedBlocksFetched property.
    pub local_merged_blocks_fetched: Option<String>,
    /// localMergedBytesRead property.
    pub local_merged_bytes_read: Option<String>,
    /// localMergedChunksFetched property.
    pub local_merged_chunks_fetched: Option<String>,
    /// mergedFetchFallbackCount property.
    pub merged_fetch_fallback_count: Option<String>,
    /// remoteMergedBlocksFetched property.
    pub remote_merged_blocks_fetched: Option<String>,
    /// remoteMergedBytesRead property.
    pub remote_merged_bytes_read: Option<String>,
    /// remoteMergedChunksFetched property.
    pub remote_merged_chunks_fetched: Option<String>,
    /// remoteMergedReqsDuration property.
    pub remote_merged_reqs_duration: Option<String>,
}

/// `ExecutorStageSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExecutorStageSummary {
    /// diskBytesSpilled property.
    pub disk_bytes_spilled: Option<String>,
    /// executorId property.
    pub executor_id: Option<String>,
    /// failedTasks property.
    pub failed_tasks: Option<i64>,
    /// inputBytes property.
    pub input_bytes: Option<String>,
    /// inputRecords property.
    pub input_records: Option<String>,
    /// isExcludedForStage property.
    pub is_excluded_for_stage: Option<bool>,
    /// killedTasks property.
    pub killed_tasks: Option<i64>,
    /// memoryBytesSpilled property.
    pub memory_bytes_spilled: Option<String>,
    /// outputBytes property.
    pub output_bytes: Option<String>,
    /// outputRecords property.
    pub output_records: Option<String>,
    /// peakMemoryMetrics property.
    pub peak_memory_metrics: Option<ExecutorMetrics>,
    /// shuffleRead property.
    pub shuffle_read: Option<String>,
    /// shuffleReadRecords property.
    pub shuffle_read_records: Option<String>,
    /// shuffleWrite property.
    pub shuffle_write: Option<String>,
    /// shuffleWriteRecords property.
    pub shuffle_write_records: Option<String>,
    /// stageAttemptId property.
    pub stage_attempt_id: Option<i64>,
    /// stageId property.
    pub stage_id: Option<String>,
    /// succeededTasks property.
    pub succeeded_tasks: Option<i64>,
    /// taskTimeMillis property.
    pub task_time_millis: Option<String>,
}

/// `SparkPlanGraphNodeWrapper` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SparkPlanGraphNodeWrapper {
    /// cluster property.
    pub cluster: Option<Box<SparkPlanGraphCluster>>,
    /// node property.
    pub node: Option<SparkPlanGraphNode>,
}

/// `Quantiles` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Quantiles {
    /// count property.
    pub count: Option<String>,
    /// maximum property.
    pub maximum: Option<String>,
    /// minimum property.
    pub minimum: Option<String>,
    /// percentile25 property.
    pub percentile25: Option<String>,
    /// percentile50 property.
    pub percentile50: Option<String>,
    /// percentile75 property.
    pub percentile75: Option<String>,
    /// sum property.
    pub sum: Option<String>,
}

/// `SummarizeSparkApplicationJobsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSparkApplicationJobsResponse {
    /// jobsSummary property.
    pub jobs_summary: Option<JobsSummary>,
}

/// `SpeculationStageSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SpeculationStageSummary {
    /// numActiveTasks property.
    pub num_active_tasks: Option<i64>,
    /// numCompletedTasks property.
    pub num_completed_tasks: Option<i64>,
    /// numFailedTasks property.
    pub num_failed_tasks: Option<i64>,
    /// numKilledTasks property.
    pub num_killed_tasks: Option<i64>,
    /// numTasks property.
    pub num_tasks: Option<i64>,
    /// stageAttemptId property.
    pub stage_attempt_id: Option<i64>,
    /// stageId property.
    pub stage_id: Option<String>,
}

/// `AccessSessionSparkApplicationStageRddOperationGraphResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationStageRddOperationGraphResponse {
    /// rddOperationGraph property.
    pub rdd_operation_graph: Option<RddOperationGraph>,
}

/// `SearchSparkApplicationSqlQueriesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationSqlQueriesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationSqlQueries property.
    pub spark_application_sql_queries: Option<Vec<SqlExecutionUiData>>,
}

/// `SummarizeSessionSparkApplicationStagesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSessionSparkApplicationStagesResponse {
    /// stagesSummary property.
    pub stages_summary: Option<StagesSummary>,
}

/// `AccessSparkApplicationSqlQueryResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationSqlQueryResponse {
    /// executionData property.
    pub execution_data: Option<SqlExecutionUiData>,
}

/// `ApplicationInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApplicationInfo {
    /// applicationContextIngestionStatus property.
    pub application_context_ingestion_status: Option<String>,
    /// applicationId property.
    pub application_id: Option<String>,
    /// attempts property.
    pub attempts: Option<Vec<ApplicationAttemptInfo>>,
    /// coresGranted property.
    pub cores_granted: Option<i64>,
    /// coresPerExecutor property.
    pub cores_per_executor: Option<i64>,
    /// maxCores property.
    pub max_cores: Option<i64>,
    /// memoryPerExecutorMb property.
    pub memory_per_executor_mb: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// quantileDataStatus property.
    pub quantile_data_status: Option<String>,
}

/// `SummarizeSparkApplicationStagesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSparkApplicationStagesResponse {
    /// stagesSummary property.
    pub stages_summary: Option<StagesSummary>,
}

/// `StageData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StageData {
    /// accumulatorUpdates property.
    pub accumulator_updates: Option<Vec<AccumulableInfo>>,
    /// completionTime property.
    pub completion_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// details property.
    pub details: Option<String>,
    /// executorMetricsDistributions property.
    pub executor_metrics_distributions: Option<ExecutorMetricsDistributions>,
    /// executorSummary property.
    pub executor_summary: Option<serde_json::Value>,
    /// failureReason property.
    pub failure_reason: Option<String>,
    /// firstTaskLaunchedTime property.
    pub first_task_launched_time: Option<String>,
    /// isShufflePushEnabled property.
    pub is_shuffle_push_enabled: Option<bool>,
    /// jobIds property.
    pub job_ids: Option<Vec<String>>,
    /// killedTasksSummary property.
    pub killed_tasks_summary: Option<serde_json::Value>,
    /// locality property.
    pub locality: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// numActiveTasks property.
    pub num_active_tasks: Option<i64>,
    /// numCompleteTasks property.
    pub num_complete_tasks: Option<i64>,
    /// numCompletedIndices property.
    pub num_completed_indices: Option<i64>,
    /// numFailedTasks property.
    pub num_failed_tasks: Option<i64>,
    /// numKilledTasks property.
    pub num_killed_tasks: Option<i64>,
    /// numTasks property.
    pub num_tasks: Option<i64>,
    /// parentStageIds property.
    pub parent_stage_ids: Option<Vec<String>>,
    /// peakExecutorMetrics property.
    pub peak_executor_metrics: Option<ExecutorMetrics>,
    /// rddIds property.
    pub rdd_ids: Option<Vec<String>>,
    /// resourceProfileId property.
    pub resource_profile_id: Option<i64>,
    /// schedulingPool property.
    pub scheduling_pool: Option<String>,
    /// shuffleMergersCount property.
    pub shuffle_mergers_count: Option<i64>,
    /// speculationSummary property.
    pub speculation_summary: Option<SpeculationStageSummary>,
    /// stageAttemptId property.
    pub stage_attempt_id: Option<i64>,
    /// stageId property.
    pub stage_id: Option<String>,
    /// stageMetrics property.
    pub stage_metrics: Option<StageMetrics>,
    /// status property.
    pub status: Option<String>,
    /// submissionTime property.
    pub submission_time: Option<String>,
    /// taskQuantileMetrics property.
    pub task_quantile_metrics: Option<TaskQuantileMetrics>,
    /// tasks property.
    pub tasks: Option<serde_json::Value>,
}

/// `StageOutputMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StageOutputMetrics {
    /// bytesWritten property.
    pub bytes_written: Option<String>,
    /// recordsWritten property.
    pub records_written: Option<String>,
}

/// `ExecutorSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExecutorSummary {
    /// activeTasks property.
    pub active_tasks: Option<i64>,
    /// addTime property.
    pub add_time: Option<String>,
    /// attributes property.
    pub attributes: Option<serde_json::Value>,
    /// completedTasks property.
    pub completed_tasks: Option<i64>,
    /// diskUsed property.
    pub disk_used: Option<String>,
    /// excludedInStages property.
    pub excluded_in_stages: Option<Vec<String>>,
    /// executorId property.
    pub executor_id: Option<String>,
    /// executorLogs property.
    pub executor_logs: Option<serde_json::Value>,
    /// failedTasks property.
    pub failed_tasks: Option<i64>,
    /// hostPort property.
    pub host_port: Option<String>,
    /// isActive property.
    pub is_active: Option<bool>,
    /// isExcluded property.
    pub is_excluded: Option<bool>,
    /// maxMemory property.
    pub max_memory: Option<String>,
    /// maxTasks property.
    pub max_tasks: Option<i64>,
    /// memoryMetrics property.
    pub memory_metrics: Option<MemoryMetrics>,
    /// memoryUsed property.
    pub memory_used: Option<String>,
    /// peakMemoryMetrics property.
    pub peak_memory_metrics: Option<ExecutorMetrics>,
    /// rddBlocks property.
    pub rdd_blocks: Option<i64>,
    /// removeReason property.
    pub remove_reason: Option<String>,
    /// removeTime property.
    pub remove_time: Option<String>,
    /// resourceProfileId property.
    pub resource_profile_id: Option<i64>,
    /// resources property.
    pub resources: Option<serde_json::Value>,
    /// totalCores property.
    pub total_cores: Option<i64>,
    /// totalDurationMillis property.
    pub total_duration_millis: Option<String>,
    /// totalGcTimeMillis property.
    pub total_gc_time_millis: Option<String>,
    /// totalInputBytes property.
    pub total_input_bytes: Option<String>,
    /// totalShuffleRead property.
    pub total_shuffle_read: Option<String>,
    /// totalShuffleWrite property.
    pub total_shuffle_write: Option<String>,
    /// totalTasks property.
    pub total_tasks: Option<i64>,
}

/// `SummarizeSparkApplicationExecutorsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSparkApplicationExecutorsResponse {
    /// activeExecutorSummary property.
    pub active_executor_summary: Option<ConsolidatedExecutorSummary>,
    /// applicationId property.
    pub application_id: Option<String>,
    /// deadExecutorSummary property.
    pub dead_executor_summary: Option<ConsolidatedExecutorSummary>,
    /// totalExecutorSummary property.
    pub total_executor_summary: Option<ConsolidatedExecutorSummary>,
}

/// `ShufflePushReadQuantileMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ShufflePushReadQuantileMetrics {
    /// corruptMergedBlockChunks property.
    pub corrupt_merged_block_chunks: Option<Quantiles>,
    /// localMergedBlocksFetched property.
    pub local_merged_blocks_fetched: Option<Quantiles>,
    /// localMergedBytesRead property.
    pub local_merged_bytes_read: Option<Quantiles>,
    /// localMergedChunksFetched property.
    pub local_merged_chunks_fetched: Option<Quantiles>,
    /// mergedFetchFallbackCount property.
    pub merged_fetch_fallback_count: Option<Quantiles>,
    /// remoteMergedBlocksFetched property.
    pub remote_merged_blocks_fetched: Option<Quantiles>,
    /// remoteMergedBytesRead property.
    pub remote_merged_bytes_read: Option<Quantiles>,
    /// remoteMergedChunksFetched property.
    pub remote_merged_chunks_fetched: Option<Quantiles>,
    /// remoteMergedReqsDuration property.
    pub remote_merged_reqs_duration: Option<Quantiles>,
}

/// `AccessSparkApplicationEnvironmentInfoResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationEnvironmentInfoResponse {
    /// applicationEnvironmentInfo property.
    pub application_environment_info: Option<ApplicationEnvironmentInfo>,
}

/// `SqlExecutionUiData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SqlExecutionUiData {
    /// completionTime property.
    pub completion_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// details property.
    pub details: Option<String>,
    /// errorMessage property.
    pub error_message: Option<String>,
    /// executionId property.
    pub execution_id: Option<String>,
    /// jobs property.
    pub jobs: Option<serde_json::Value>,
    /// metricValues property.
    pub metric_values: Option<serde_json::Value>,
    /// metricValuesIsNull property.
    pub metric_values_is_null: Option<bool>,
    /// metrics property.
    pub metrics: Option<Vec<SqlPlanMetric>>,
    /// modifiedConfigs property.
    pub modified_configs: Option<serde_json::Value>,
    /// physicalPlanDescription property.
    pub physical_plan_description: Option<String>,
    /// rootExecutionId property.
    pub root_execution_id: Option<String>,
    /// stages property.
    pub stages: Option<Vec<String>>,
    /// submissionTime property.
    pub submission_time: Option<String>,
}

/// `RddOperationCluster` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RddOperationCluster {
    /// childClusters property.
    pub child_clusters: Option<Vec<Box<RddOperationCluster>>>,
    /// childNodes property.
    pub child_nodes: Option<Vec<RddOperationNode>>,
    /// name property.
    pub name: Option<String>,
    /// rddClusterId property.
    pub rdd_cluster_id: Option<String>,
}

/// `ExecutorMetricsDistributions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExecutorMetricsDistributions {
    /// diskBytesSpilled property.
    pub disk_bytes_spilled: Option<Vec<f64>>,
    /// failedTasks property.
    pub failed_tasks: Option<Vec<f64>>,
    /// inputBytes property.
    pub input_bytes: Option<Vec<f64>>,
    /// inputRecords property.
    pub input_records: Option<Vec<f64>>,
    /// killedTasks property.
    pub killed_tasks: Option<Vec<f64>>,
    /// memoryBytesSpilled property.
    pub memory_bytes_spilled: Option<Vec<f64>>,
    /// outputBytes property.
    pub output_bytes: Option<Vec<f64>>,
    /// outputRecords property.
    pub output_records: Option<Vec<f64>>,
    /// peakMemoryMetrics property.
    pub peak_memory_metrics: Option<ExecutorPeakMetricsDistributions>,
    /// quantiles property.
    pub quantiles: Option<Vec<f64>>,
    /// shuffleRead property.
    pub shuffle_read: Option<Vec<f64>>,
    /// shuffleReadRecords property.
    pub shuffle_read_records: Option<Vec<f64>>,
    /// shuffleWrite property.
    pub shuffle_write: Option<Vec<f64>>,
    /// shuffleWriteRecords property.
    pub shuffle_write_records: Option<Vec<f64>>,
    /// succeededTasks property.
    pub succeeded_tasks: Option<Vec<f64>>,
    /// taskTimeMillis property.
    pub task_time_millis: Option<Vec<f64>>,
}

/// `InputMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InputMetrics {
    /// bytesRead property.
    pub bytes_read: Option<String>,
    /// recordsRead property.
    pub records_read: Option<String>,
}

/// `AccessSparkApplicationJobResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationJobResponse {
    /// jobData property.
    pub job_data: Option<JobData>,
}

/// `AccessSessionSparkApplicationSqlSparkPlanGraphResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationSqlSparkPlanGraphResponse {
    /// sparkPlanGraph property.
    pub spark_plan_graph: Option<SparkPlanGraph>,
}

/// `SummarizeSessionSparkApplicationJobsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSessionSparkApplicationJobsResponse {
    /// jobsSummary property.
    pub jobs_summary: Option<JobsSummary>,
}

/// `RddOperationGraph` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RddOperationGraph {
    /// edges property.
    pub edges: Option<Vec<RddOperationEdge>>,
    /// incomingEdges property.
    pub incoming_edges: Option<Vec<RddOperationEdge>>,
    /// outgoingEdges property.
    pub outgoing_edges: Option<Vec<RddOperationEdge>>,
    /// rootCluster property.
    pub root_cluster: Option<Box<RddOperationCluster>>,
    /// stageId property.
    pub stage_id: Option<String>,
}

/// `SearchSessionSparkApplicationStagesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationStagesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStages property.
    pub spark_application_stages: Option<Vec<StageData>>,
}

/// `SparkPlanGraphNode` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SparkPlanGraphNode {
    /// desc property.
    pub desc: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// metrics property.
    pub metrics: Option<Vec<SqlPlanMetric>>,
    /// name property.
    pub name: Option<String>,
    /// sparkPlanGraphNodeId property.
    pub spark_plan_graph_node_id: Option<String>,
}

/// `AccessSparkApplicationResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationResponse {
    /// application property.
    pub application: Option<ApplicationInfo>,
}

/// `StageAttemptTasksSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StageAttemptTasksSummary {
    /// applicationId property.
    pub application_id: Option<String>,
    /// numFailedTasks property.
    pub num_failed_tasks: Option<i64>,
    /// numKilledTasks property.
    pub num_killed_tasks: Option<i64>,
    /// numPendingTasks property.
    pub num_pending_tasks: Option<i64>,
    /// numRunningTasks property.
    pub num_running_tasks: Option<i64>,
    /// numSuccessTasks property.
    pub num_success_tasks: Option<i64>,
    /// numTasks property.
    pub num_tasks: Option<i64>,
    /// stageAttemptId property.
    pub stage_attempt_id: Option<i64>,
    /// stageId property.
    pub stage_id: Option<String>,
}

/// `RddOperationEdge` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RddOperationEdge {
    /// fromId property.
    pub from_id: Option<i64>,
    /// toId property.
    pub to_id: Option<i64>,
}

/// `SearchSparkApplicationStageAttemptTasksResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationStageAttemptTasksResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageAttemptTasks property.
    pub spark_application_stage_attempt_tasks: Option<Vec<TaskData>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.access_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessEnvironmentInfo_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessEnvironmentInfoArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessJob_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessJobArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `jobId`.
    pub job_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessSqlPlan_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessSqlPlanArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executionId`.
    pub execution_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessSqlQuery_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessSqlQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `details`.
    pub details: Option<String>,
    /// Query parameter: `executionId`.
    pub execution_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `planDescription`.
    pub plan_description: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessStageAttempt_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessStageAttemptArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessStageRddGraph_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessStageRddGraphArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchExecutorStageSummary_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchExecutorStageSummaryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchExecutors_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchExecutorsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executorStatus`.
    pub executor_status: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchJobs_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchJobsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `jobStatus`.
    pub job_status: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchSqlQueries_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchSqlQueriesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `details`.
    pub details: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `planDescription`.
    pub plan_description: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchStageAttemptTasks_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchStageAttemptTasksArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `sortRuntime`.
    pub sort_runtime: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `taskStatus`.
    pub task_status: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchStageAttempts_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchStageAttemptsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchStages_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchStagesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageStatus`.
    pub stage_status: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.summarizeExecutors_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeExecutorsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.summarizeJobs_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeJobsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.summarizeStageAttemptTasks_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeStageAttemptTasksArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.summarizeStages_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeStagesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.write_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsWriteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.access_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessEnvironmentInfo_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessEnvironmentInfoArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessJob_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessJobArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `jobId`.
    pub job_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessSqlPlan_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessSqlPlanArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executionId`.
    pub execution_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessSqlQuery_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessSqlQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `details`.
    pub details: Option<String>,
    /// Query parameter: `executionId`.
    pub execution_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `planDescription`.
    pub plan_description: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessStageAttempt_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessStageAttemptArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessStageRddGraph_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessStageRddGraphArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchExecutorStageSummary_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchExecutorStageSummaryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchExecutors_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchExecutorsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executorStatus`.
    pub executor_status: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchJobs_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchJobsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `jobIds`.
    pub job_ids: Option<String>,
    /// Query parameter: `jobStatus`.
    pub job_status: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchSqlQueries_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchSqlQueriesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `details`.
    pub details: Option<String>,
    /// Query parameter: `operationIds`.
    pub operation_ids: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `planDescription`.
    pub plan_description: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchStageAttemptTasks_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchStageAttemptTasksArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `sortRuntime`.
    pub sort_runtime: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `taskStatus`.
    pub task_status: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchStageAttempts_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchStageAttemptsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchStages_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchStagesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageIds`.
    pub stage_ids: Option<String>,
    /// Query parameter: `stageStatus`.
    pub stage_status: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.summarizeExecutors_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeExecutorsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.summarizeJobs_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeJobsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `jobIds`.
    pub job_ids: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.summarizeStageAttemptTasks_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeStageAttemptTasksArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.summarizeStages_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeStagesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageIds`.
    pub stage_ids: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.write_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsWriteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:access
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:access.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccessSparkApplicationResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:access",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_environment_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_environment_info_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSparkApplicationEnvironmentInfoResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:accessEnvironmentInfo",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationEnvironmentInfoResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessJob
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessJob.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_job_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_job_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccessSparkApplicationJobResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:accessJob",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationJobResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_sql_plan_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_sql_plan_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSparkApplicationSqlSparkPlanGraphResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:accessSqlPlan",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationSqlSparkPlanGraphResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_sql_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_sql_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSparkApplicationSqlQueryResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:accessSqlQuery",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationSqlQueryResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_stage_attempt_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_stage_attempt_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSparkApplicationStageAttemptResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:accessStageAttempt",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationStageAttemptResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSparkApplicationStageRddOperationGraphResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:accessStageRddGraph",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationStageRddOperationGraphResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationExecutorStageSummaryResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:searchExecutorStageSummary",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationExecutorStageSummaryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutors
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchExecutors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_executors_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_executors_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationExecutorsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:searchExecutors",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationExecutorsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchJobs
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_jobs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_jobs_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationJobsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:searchJobs",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_sql_queries_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_sql_queries_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationSqlQueriesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:searchSqlQueries",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationSqlQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationStageAttemptTasksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:searchStageAttemptTasks",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_stage_attempts_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempts_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationStageAttemptsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:searchStageAttempts",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStageAttemptsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStages
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:searchStages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_stages_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_stages_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationStagesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:searchStages",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStagesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_summarize_executors_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_summarize_executors_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSparkApplicationExecutorsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:summarizeExecutors",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeJobs
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_summarize_jobs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_summarize_jobs_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSparkApplicationJobsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:summarizeJobs",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSparkApplicationStageAttemptTasksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:summarizeStageAttemptTasks",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStages
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:summarizeStages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_summarize_stages_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_summarize_stages_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSparkApplicationStagesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:summarizeStages",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationStagesResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:write
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications/{sparkApplicationsId}:write.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_write_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_write_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<WriteSparkApplicationContextResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications/{{sparkApplicationsId}}:write",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WriteSparkApplicationContextResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:access
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:access.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:access",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessEnvironmentInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_environment_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_environment_info_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationEnvironmentInfoResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:accessEnvironmentInfo",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationEnvironmentInfoResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessJob
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessJob.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_job_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_job_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationJobResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:accessJob",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationJobResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlPlan.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_sql_plan_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_plan_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationSqlSparkPlanGraphResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:accessSqlPlan",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationSqlSparkPlanGraphResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessSqlQuery.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_sql_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationSqlQueryResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:accessSqlQuery",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationSqlQueryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageAttempt.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationStageAttemptResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:accessStageAttempt",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationStageAttemptResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:accessStageRddGraph.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationStageRddOperationGraphResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:accessStageRddGraph",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationStageRddOperationGraphResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutorStageSummary.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationExecutorStageSummaryResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:searchExecutorStageSummary",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationExecutorStageSummaryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutors
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchExecutors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_executors_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_executors_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationExecutorsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:searchExecutors",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchJobs
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_jobs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_jobs_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationJobsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:searchJobs",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchSqlQueries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_sql_queries_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_sql_queries_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationSqlQueriesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:searchSqlQueries",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationSqlQueriesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttemptTasks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationStageAttemptTasksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:searchStageAttemptTasks",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStageAttempts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationStageAttemptsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:searchStageAttempts",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStageAttemptsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStages
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:searchStages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_stages_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_stages_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationStagesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:searchStages",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeExecutors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_summarize_executors_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_summarize_executors_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSessionSparkApplicationExecutorsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:summarizeExecutors",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeJobs
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_summarize_jobs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_summarize_jobs_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSessionSparkApplicationJobsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:summarizeJobs",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStageAttemptTasks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSessionSparkApplicationStageAttemptTasksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:summarizeStageAttemptTasks",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStages
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:summarizeStages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_summarize_stages_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stages_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSessionSparkApplicationStagesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:summarizeStages",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationStagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:write
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications/{sparkApplicationsId}:write.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_write_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_write_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<WriteSessionSparkApplicationContextResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications/{{sparkApplicationsId}}:write",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WriteSessionSparkApplicationContextResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
