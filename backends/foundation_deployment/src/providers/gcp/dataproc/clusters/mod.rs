//! Auto-generated API module for gcp/dataproc clusters.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dataproc_clusters `

#![cfg(feature = "gcp_dataproc_clusters")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;
use super::shared::Policy;
use super::shared::TestIamPermissionsResponse;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SoftwareConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SoftwareConfig {
    /// imageVersion property.
    pub image_version: Option<String>,
    /// optionalComponents property.
    pub optional_components: Option<Vec<String>>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
}

/// `Expr` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `KubernetesSoftwareConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KubernetesSoftwareConfig {
    /// componentVersion property.
    pub component_version: Option<serde_json::Value>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
}

/// `GceClusterConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GceClusterConfig {
    /// autoZoneExcludeZoneUris property.
    pub auto_zone_exclude_zone_uris: Option<Vec<String>>,
    /// confidentialInstanceConfig property.
    pub confidential_instance_config: Option<ConfidentialInstanceConfig>,
    /// internalIpOnly property.
    pub internal_ip_only: Option<bool>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// networkUri property.
    pub network_uri: Option<String>,
    /// nodeGroupAffinity property.
    pub node_group_affinity: Option<NodeGroupAffinity>,
    /// privateIpv6GoogleAccess property.
    pub private_ipv6_google_access: Option<String>,
    /// reservationAffinity property.
    pub reservation_affinity: Option<ReservationAffinity>,
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// serviceAccountScopes property.
    pub service_account_scopes: Option<Vec<String>>,
    /// shieldedInstanceConfig property.
    pub shielded_instance_config: Option<ShieldedInstanceConfig>,
    /// subnetworkUri property.
    pub subnetwork_uri: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// zoneUri property.
    pub zone_uri: Option<String>,
}

/// `ClusterStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClusterStatus {
    /// detail property.
    pub detail: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// stateStartTime property.
    pub state_start_time: Option<String>,
    /// substate property.
    pub substate: Option<String>,
}

/// `InstanceFlexibilityPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstanceFlexibilityPolicy {
    /// instanceMachineTypes property.
    pub instance_machine_types: Option<serde_json::Value>,
    /// instanceSelectionList property.
    pub instance_selection_list: Option<Vec<InstanceSelection>>,
    /// instanceSelectionResults property.
    pub instance_selection_results: Option<Vec<InstanceSelectionResult>>,
    /// provisioningModelMix property.
    pub provisioning_model_mix: Option<ProvisioningModelMix>,
}

/// `InstanceReference` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstanceReference {
    /// instanceId property.
    pub instance_id: Option<String>,
    /// instanceName property.
    pub instance_name: Option<String>,
    /// publicEciesKey property.
    pub public_ecies_key: Option<String>,
    /// publicKey property.
    pub public_key: Option<String>,
}

/// `StartupConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StartupConfig {
    /// requiredRegistrationFraction property.
    pub required_registration_fraction: Option<f64>,
}

/// `KubernetesClusterConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KubernetesClusterConfig {
    /// gkeClusterConfig property.
    pub gke_cluster_config: Option<GkeClusterConfig>,
    /// kubernetesNamespace property.
    pub kubernetes_namespace: Option<String>,
    /// kubernetesSoftwareConfig property.
    pub kubernetes_software_config: Option<KubernetesSoftwareConfig>,
}

/// `ClusterMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClusterMetrics {
    /// hdfsMetrics property.
    pub hdfs_metrics: Option<serde_json::Value>,
    /// yarnMetrics property.
    pub yarn_metrics: Option<serde_json::Value>,
}

/// `GkeNodePoolConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GkeNodePoolConfig {
    /// autoscaling property.
    pub autoscaling: Option<GkeNodePoolAutoscalingConfig>,
    /// config property.
    pub config: Option<GkeNodeConfig>,
    /// locations property.
    pub locations: Option<Vec<String>>,
}

/// `VirtualClusterConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VirtualClusterConfig {
    /// auxiliaryServicesConfig property.
    pub auxiliary_services_config: Option<AuxiliaryServicesConfig>,
    /// kubernetesClusterConfig property.
    pub kubernetes_cluster_config: Option<KubernetesClusterConfig>,
    /// stagingBucket property.
    pub staging_bucket: Option<String>,
}

/// `GkeNodePoolAutoscalingConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GkeNodePoolAutoscalingConfig {
    /// maxNodeCount property.
    pub max_node_count: Option<i64>,
    /// minNodeCount property.
    pub min_node_count: Option<i64>,
}

/// `NodeInitializationAction` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NodeInitializationAction {
    /// executableFile property.
    pub executable_file: Option<String>,
    /// executionTimeout property.
    pub execution_timeout: Option<String>,
}

/// `Binding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `EndpointConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EndpointConfig {
    /// enableHttpPortAccess property.
    pub enable_http_port_access: Option<bool>,
    /// httpPorts property.
    pub http_ports: Option<serde_json::Value>,
}

/// `AcceleratorConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AcceleratorConfig {
    /// acceleratorCount property.
    pub accelerator_count: Option<i64>,
    /// acceleratorTypeUri property.
    pub accelerator_type_uri: Option<String>,
}

/// `ManagedGroupConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedGroupConfig {
    /// instanceGroupManagerName property.
    pub instance_group_manager_name: Option<String>,
    /// instanceGroupManagerUri property.
    pub instance_group_manager_uri: Option<String>,
    /// instanceTemplateName property.
    pub instance_template_name: Option<String>,
}

/// `NodeGroupAffinity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupAffinity {
    /// nodeGroupUri property.
    pub node_group_uri: Option<String>,
}

/// `ShieldedInstanceConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ShieldedInstanceConfig {
    /// enableIntegrityMonitoring property.
    pub enable_integrity_monitoring: Option<bool>,
    /// enableSecureBoot property.
    pub enable_secure_boot: Option<bool>,
    /// enableVtpm property.
    pub enable_vtpm: Option<bool>,
}

/// `Metric` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Metric {
    /// metricOverrides property.
    pub metric_overrides: Option<Vec<String>>,
    /// metricSource property.
    pub metric_source: Option<String>,
}

/// `GkeNodePoolTarget` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GkeNodePoolTarget {
    /// nodePool property.
    pub node_pool: Option<String>,
    /// nodePoolConfig property.
    pub node_pool_config: Option<GkeNodePoolConfig>,
    /// roles property.
    pub roles: Option<Vec<String>>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `GkeClusterConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GkeClusterConfig {
    /// gkeClusterTarget property.
    pub gke_cluster_target: Option<String>,
    /// namespacedGkeDeploymentTarget property.
    pub namespaced_gke_deployment_target: Option<NamespacedGkeDeploymentTarget>,
    /// nodePoolTarget property.
    pub node_pool_target: Option<Vec<GkeNodePoolTarget>>,
}

/// `GkeNodePoolAcceleratorConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GkeNodePoolAcceleratorConfig {
    /// acceleratorCount property.
    pub accelerator_count: Option<String>,
    /// acceleratorType property.
    pub accelerator_type: Option<String>,
    /// gpuPartitionSize property.
    pub gpu_partition_size: Option<String>,
}

/// `AttachedDiskConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AttachedDiskConfig {
    /// diskSizeGb property.
    pub disk_size_gb: Option<i64>,
    /// diskType property.
    pub disk_type: Option<String>,
    /// provisionedIops property.
    pub provisioned_iops: Option<String>,
    /// provisionedThroughput property.
    pub provisioned_throughput: Option<String>,
}

/// `NodeGroup` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NodeGroup {
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// nodeGroupConfig property.
    pub node_group_config: Option<InstanceGroupConfig>,
    /// roles property.
    pub roles: Option<Vec<String>>,
}

/// `ProvisioningModelMix` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProvisioningModelMix {
    /// standardCapacityBase property.
    pub standard_capacity_base: Option<i64>,
    /// standardCapacityPercentAboveBase property.
    pub standard_capacity_percent_above_base: Option<i64>,
}

/// `InstanceSelection` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstanceSelection {
    /// machineTypes property.
    pub machine_types: Option<Vec<String>>,
    /// rank property.
    pub rank: Option<i64>,
}

/// `GkeNodeConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GkeNodeConfig {
    /// accelerators property.
    pub accelerators: Option<Vec<GkeNodePoolAcceleratorConfig>>,
    /// bootDiskKmsKey property.
    pub boot_disk_kms_key: Option<String>,
    /// localSsdCount property.
    pub local_ssd_count: Option<i64>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
    /// preemptible property.
    pub preemptible: Option<bool>,
    /// spot property.
    pub spot: Option<bool>,
}

/// `InstanceGroupConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupConfig {
    /// accelerators property.
    pub accelerators: Option<Vec<AcceleratorConfig>>,
    /// diskConfig property.
    pub disk_config: Option<DiskConfig>,
    /// imageUri property.
    pub image_uri: Option<String>,
    /// instanceFlexibilityPolicy property.
    pub instance_flexibility_policy: Option<InstanceFlexibilityPolicy>,
    /// instanceNames property.
    pub instance_names: Option<Vec<String>>,
    /// instanceReferences property.
    pub instance_references: Option<Vec<InstanceReference>>,
    /// isPreemptible property.
    pub is_preemptible: Option<bool>,
    /// machineTypeUri property.
    pub machine_type_uri: Option<String>,
    /// managedGroupConfig property.
    pub managed_group_config: Option<ManagedGroupConfig>,
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
    /// minNumInstances property.
    pub min_num_instances: Option<i64>,
    /// numInstances property.
    pub num_instances: Option<i64>,
    /// preemptibility property.
    pub preemptibility: Option<String>,
    /// startupConfig property.
    pub startup_config: Option<StartupConfig>,
}

/// `AuxiliaryNodeGroup` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuxiliaryNodeGroup {
    /// nodeGroup property.
    pub node_group: Option<NodeGroup>,
    /// nodeGroupId property.
    pub node_group_id: Option<String>,
}

/// `ConfidentialInstanceConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConfidentialInstanceConfig {
    /// enableConfidentialCompute property.
    pub enable_confidential_compute: Option<bool>,
}

/// `EncryptionConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EncryptionConfig {
    /// gcePdKmsKeyName property.
    pub gce_pd_kms_key_name: Option<String>,
    /// kmsKey property.
    pub kms_key: Option<String>,
}

/// `ReservationAffinity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReservationAffinity {
    /// consumeReservationType property.
    pub consume_reservation_type: Option<String>,
    /// key property.
    pub key: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `AutoscalingConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AutoscalingConfig {
    /// policyUri property.
    pub policy_uri: Option<String>,
}

/// `DiskConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DiskConfig {
    /// attachedDiskConfigs property.
    pub attached_disk_configs: Option<Vec<AttachedDiskConfig>>,
    /// bootDiskProvisionedIops property.
    pub boot_disk_provisioned_iops: Option<String>,
    /// bootDiskProvisionedThroughput property.
    pub boot_disk_provisioned_throughput: Option<String>,
    /// bootDiskSizeGb property.
    pub boot_disk_size_gb: Option<i64>,
    /// bootDiskType property.
    pub boot_disk_type: Option<String>,
    /// localSsdInterface property.
    pub local_ssd_interface: Option<String>,
    /// numLocalSsds property.
    pub num_local_ssds: Option<i64>,
}

/// `MetastoreConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MetastoreConfig {
    /// dataprocMetastoreService property.
    pub dataproc_metastore_service: Option<String>,
}

/// `InstanceSelectionResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstanceSelectionResult {
    /// machineType property.
    pub machine_type: Option<String>,
    /// vmCount property.
    pub vm_count: Option<i64>,
}

/// `AuxiliaryServicesConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuxiliaryServicesConfig {
    /// metastoreConfig property.
    pub metastore_config: Option<MetastoreConfig>,
    /// sparkHistoryServerConfig property.
    pub spark_history_server_config: Option<SparkHistoryServerConfig>,
}

/// `LifecycleConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LifecycleConfig {
    /// autoDeleteTime property.
    pub auto_delete_time: Option<String>,
    /// autoDeleteTtl property.
    pub auto_delete_ttl: Option<String>,
    /// autoStopTime property.
    pub auto_stop_time: Option<String>,
    /// autoStopTtl property.
    pub auto_stop_ttl: Option<String>,
    /// idleDeleteTtl property.
    pub idle_delete_ttl: Option<String>,
    /// idleStartTime property.
    pub idle_start_time: Option<String>,
    /// idleStopTtl property.
    pub idle_stop_ttl: Option<String>,
}

/// `SecurityConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SecurityConfig {
    /// identityConfig property.
    pub identity_config: Option<IdentityConfig>,
    /// kerberosConfig property.
    pub kerberos_config: Option<KerberosConfig>,
}

/// `Cluster` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Cluster {
    /// clusterName property.
    pub cluster_name: Option<String>,
    /// clusterUuid property.
    pub cluster_uuid: Option<String>,
    /// config property.
    pub config: Option<ClusterConfig>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// metrics property.
    pub metrics: Option<ClusterMetrics>,
    /// projectId property.
    pub project_id: Option<String>,
    /// status property.
    pub status: Option<ClusterStatus>,
    /// statusHistory property.
    pub status_history: Option<Vec<ClusterStatus>>,
    /// virtualClusterConfig property.
    pub virtual_cluster_config: Option<VirtualClusterConfig>,
}

/// `IdentityConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IdentityConfig {
    /// userServiceAccountMapping property.
    pub user_service_account_mapping: Option<serde_json::Value>,
}

/// `DataprocMetricConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DataprocMetricConfig {
    /// metrics property.
    pub metrics: Option<Vec<Metric>>,
}

/// `KerberosConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KerberosConfig {
    /// crossRealmTrustAdminServer property.
    pub cross_realm_trust_admin_server: Option<String>,
    /// crossRealmTrustKdc property.
    pub cross_realm_trust_kdc: Option<String>,
    /// crossRealmTrustRealm property.
    pub cross_realm_trust_realm: Option<String>,
    /// crossRealmTrustSharedPasswordUri property.
    pub cross_realm_trust_shared_password_uri: Option<String>,
    /// enableKerberos property.
    pub enable_kerberos: Option<bool>,
    /// kdcDbKeyUri property.
    pub kdc_db_key_uri: Option<String>,
    /// keyPasswordUri property.
    pub key_password_uri: Option<String>,
    /// keystorePasswordUri property.
    pub keystore_password_uri: Option<String>,
    /// keystoreUri property.
    pub keystore_uri: Option<String>,
    /// kmsKeyUri property.
    pub kms_key_uri: Option<String>,
    /// realm property.
    pub realm: Option<String>,
    /// rootPrincipalPasswordUri property.
    pub root_principal_password_uri: Option<String>,
    /// tgtLifetimeHours property.
    pub tgt_lifetime_hours: Option<i64>,
    /// truststorePasswordUri property.
    pub truststore_password_uri: Option<String>,
    /// truststoreUri property.
    pub truststore_uri: Option<String>,
}

/// `ListClustersResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListClustersResponse {
    /// clusters property.
    pub clusters: Option<Vec<Cluster>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `NamespacedGkeDeploymentTarget` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NamespacedGkeDeploymentTarget {
    /// clusterNamespace property.
    pub cluster_namespace: Option<String>,
    /// targetGkeCluster property.
    pub target_gke_cluster: Option<String>,
}

/// `SparkHistoryServerConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SparkHistoryServerConfig {
    /// dataprocCluster property.
    pub dataproc_cluster: Option<String>,
}

/// `ClusterConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClusterConfig {
    /// autoscalingConfig property.
    pub autoscaling_config: Option<AutoscalingConfig>,
    /// auxiliaryNodeGroups property.
    pub auxiliary_node_groups: Option<Vec<AuxiliaryNodeGroup>>,
    /// clusterTier property.
    pub cluster_tier: Option<String>,
    /// clusterType property.
    pub cluster_type: Option<String>,
    /// configBucket property.
    pub config_bucket: Option<String>,
    /// dataprocMetricConfig property.
    pub dataproc_metric_config: Option<DataprocMetricConfig>,
    /// diagnosticBucket property.
    pub diagnostic_bucket: Option<String>,
    /// encryptionConfig property.
    pub encryption_config: Option<EncryptionConfig>,
    /// endpointConfig property.
    pub endpoint_config: Option<EndpointConfig>,
    /// engine property.
    pub engine: Option<String>,
    /// gceClusterConfig property.
    pub gce_cluster_config: Option<GceClusterConfig>,
    /// gkeClusterConfig property.
    pub gke_cluster_config: Option<GkeClusterConfig>,
    /// initializationActions property.
    pub initialization_actions: Option<Vec<NodeInitializationAction>>,
    /// lifecycleConfig property.
    pub lifecycle_config: Option<LifecycleConfig>,
    /// masterConfig property.
    pub master_config: Option<InstanceGroupConfig>,
    /// metastoreConfig property.
    pub metastore_config: Option<MetastoreConfig>,
    /// secondaryWorkerConfig property.
    pub secondary_worker_config: Option<InstanceGroupConfig>,
    /// securityConfig property.
    pub security_config: Option<SecurityConfig>,
    /// softwareConfig property.
    pub software_config: Option<SoftwareConfig>,
    /// tempBucket property.
    pub temp_bucket: Option<String>,
    /// workerConfig property.
    pub worker_config: Option<InstanceGroupConfig>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dataproc.projects.regions.clusters.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `actionOnFailedPrimaryWorkers`.
    pub action_on_failed_primary_workers: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.clusters.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersDeleteArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
    /// Query parameter: `clusterUuid`.
    pub cluster_uuid: Option<String>,
    /// Query parameter: `gracefulTerminationTimeout`.
    pub graceful_termination_timeout: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.clusters.diagnose_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersDiagnoseArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
}

/// Arguments for [`dataproc.projects.regions.clusters.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
}

/// Arguments for [`dataproc.projects.regions.clusters.getIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`dataproc.projects.regions.clusters.injectCredentials_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersInjectCredentialsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `cluster`.
    pub cluster: String,
}

/// Arguments for [`dataproc.projects.regions.clusters.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.clusters.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersPatchArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
    /// Query parameter: `gracefulDecommissionTimeout`.
    pub graceful_decommission_timeout: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.clusters.repair_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersRepairArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
}

/// Arguments for [`dataproc.projects.regions.clusters.setIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`dataproc.projects.regions.clusters.start_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersStartArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
}

/// Arguments for [`dataproc.projects.regions.clusters.stop_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersStopArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
}

/// Arguments for [`dataproc.projects.regions.clusters.testIamPermissions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/clusters
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/clusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters",
        args.project_id, args.region,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectId}/regions/{region}/clusters/{clusterName}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:diagnose
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:diagnose.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_diagnose_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_diagnose_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersDiagnoseArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:diagnose",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Cluster>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Cluster =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/regions/{{regionsId}}/clusters/{{clustersId}}:getIamPolicy",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:injectCredentials
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:injectCredentials.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_inject_credentials_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_inject_credentials_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/regions/{{regionsId}}/clusters/{{clustersId}}:injectCredentials",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/regions/{region}/clusters
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/regions/{region}/clusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClustersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters",
        args.project_id, args.region,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListClustersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectId}/regions/{region}/clusters/{clusterName}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:repair
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:repair.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_repair_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_repair_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersRepairArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:repair",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/regions/{{regionsId}}/clusters/{{clustersId}}:setIamPolicy",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:start
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:start.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_start_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_start_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersStartArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:start",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:stop
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:stop.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_stop_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_stop_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersStopArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:stop",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/regions/{regionsId}/clusters/{clustersId}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/regions/{{regionsId}}/clusters/{{clustersId}}:testIamPermissions",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
