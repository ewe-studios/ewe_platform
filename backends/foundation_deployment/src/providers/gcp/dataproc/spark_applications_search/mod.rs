//! Auto-generated API module for gcp/dataproc sparkApplications:search.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dataproc_spark_applications_search `

#![cfg(feature = "gcp_dataproc_spark_applications_search")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ApplicationInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApplicationInfo {
    /// applicationContextIngestionStatus property.
    pub application_context_ingestion_status: Option<String>,
    /// applicationId property.
    pub application_id: Option<String>,
    /// attempts property.
    pub attempts: Option<Vec<ApplicationAttemptInfo>>,
    /// coresGranted property.
    pub cores_granted: Option<i64>,
    /// coresPerExecutor property.
    pub cores_per_executor: Option<i64>,
    /// maxCores property.
    pub max_cores: Option<i64>,
    /// memoryPerExecutorMb property.
    pub memory_per_executor_mb: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// quantileDataStatus property.
    pub quantile_data_status: Option<String>,
}

/// `ApplicationAttemptInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApplicationAttemptInfo {
    /// appSparkVersion property.
    pub app_spark_version: Option<String>,
    /// attemptId property.
    pub attempt_id: Option<String>,
    /// completed property.
    pub completed: Option<bool>,
    /// durationMillis property.
    pub duration_millis: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// lastUpdated property.
    pub last_updated: Option<String>,
    /// sparkUser property.
    pub spark_user: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `SparkApplication` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SparkApplication {
    /// application property.
    pub application: Option<ApplicationInfo>,
    /// name property.
    pub name: Option<String>,
}

/// `SearchSessionSparkApplicationsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplications property.
    pub spark_applications: Option<Vec<SparkApplication>>,
}

/// `SearchSparkApplicationsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplications property.
    pub spark_applications: Option<Vec<SparkApplication>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.search_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `applicationStatus`.
    pub application_status: Option<String>,
    /// Query parameter: `maxEndTime`.
    pub max_end_time: Option<String>,
    /// Query parameter: `maxTime`.
    pub max_time: Option<String>,
    /// Query parameter: `minEndTime`.
    pub min_end_time: Option<String>,
    /// Query parameter: `minTime`.
    pub min_time: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.search_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `applicationStatus`.
    pub application_status: Option<String>,
    /// Query parameter: `maxEndTime`.
    pub max_end_time: Option<String>,
    /// Query parameter: `maxTime`.
    pub max_time: Option<String>,
    /// Query parameter: `minEndTime`.
    pub min_end_time: Option<String>,
    /// Query parameter: `minTime`.
    pub min_time: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications:search
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/batches/{batchesId}/sparkApplications:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchSparkApplicationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/batches/{{batchesId}}/sparkApplications:search",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications:search
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/sessions/{sessionsId}/sparkApplications:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/sessions/{{sessionsId}}/sparkApplications:search",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
