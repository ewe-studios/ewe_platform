//! Auto-generated API module for gcp/dataproc dataproc.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dataproc_dataproc `

#![cfg(feature = "gcp_dataproc_dataproc")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ApplicationAttemptInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApplicationAttemptInfo {
    /// appSparkVersion property.
    pub app_spark_version: Option<String>,
    /// attemptId property.
    pub attempt_id: Option<String>,
    /// completed property.
    pub completed: Option<bool>,
    /// durationMillis property.
    pub duration_millis: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// lastUpdated property.
    pub last_updated: Option<String>,
    /// sparkUser property.
    pub spark_user: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `ListBatchesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListBatchesResponse {
    /// batches property.
    pub batches: Option<Vec<Batch>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `GkeNodePoolAutoscalingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GkeNodePoolAutoscalingConfig {
    /// maxNodeCount property.
    pub max_node_count: Option<i64>,
    /// minNodeCount property.
    pub min_node_count: Option<i64>,
}

/// `SparkPlanGraph` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkPlanGraph {
    /// edges property.
    pub edges: Option<Vec<SparkPlanGraphEdge>>,
    /// executionId property.
    pub execution_id: Option<String>,
    /// nodes property.
    pub nodes: Option<Vec<Box<SparkPlanGraphNodeWrapper>>>,
}

/// `DataprocMetricConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataprocMetricConfig {
    /// metrics property.
    pub metrics: Option<Vec<Metric>>,
}

/// `SparkConnectConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkConnectConfig {}

/// `InstanceGroupConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupConfig {
    /// accelerators property.
    pub accelerators: Option<Vec<AcceleratorConfig>>,
    /// diskConfig property.
    pub disk_config: Option<DiskConfig>,
    /// imageUri property.
    pub image_uri: Option<String>,
    /// instanceFlexibilityPolicy property.
    pub instance_flexibility_policy: Option<InstanceFlexibilityPolicy>,
    /// instanceNames property.
    pub instance_names: Option<Vec<String>>,
    /// instanceReferences property.
    pub instance_references: Option<Vec<InstanceReference>>,
    /// isPreemptible property.
    pub is_preemptible: Option<bool>,
    /// machineTypeUri property.
    pub machine_type_uri: Option<String>,
    /// managedGroupConfig property.
    pub managed_group_config: Option<ManagedGroupConfig>,
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
    /// minNumInstances property.
    pub min_num_instances: Option<i64>,
    /// numInstances property.
    pub num_instances: Option<i64>,
    /// preemptibility property.
    pub preemptibility: Option<String>,
    /// startupConfig property.
    pub startup_config: Option<StartupConfig>,
}

/// `StartClusterRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StartClusterRequest {
    /// clusterUuid property.
    pub cluster_uuid: Option<String>,
    /// requestId property.
    pub request_id: Option<String>,
}

/// `JobStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobStatus {
    /// details property.
    pub details: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// stateStartTime property.
    pub state_start_time: Option<String>,
    /// substate property.
    pub substate: Option<String>,
}

/// `KubernetesClusterConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct KubernetesClusterConfig {
    /// gkeClusterConfig property.
    pub gke_cluster_config: Option<GkeClusterConfig>,
    /// kubernetesNamespace property.
    pub kubernetes_namespace: Option<String>,
    /// kubernetesSoftwareConfig property.
    pub kubernetes_software_config: Option<KubernetesSoftwareConfig>,
}

/// `SqlPlanMetric` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlPlanMetric {
    /// accumulatorId property.
    pub accumulator_id: Option<String>,
    /// metricType property.
    pub metric_type: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `OutputMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OutputMetrics {
    /// bytesWritten property.
    pub bytes_written: Option<String>,
    /// recordsWritten property.
    pub records_written: Option<String>,
}

/// `StageMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageMetrics {
    /// diskBytesSpilled property.
    pub disk_bytes_spilled: Option<String>,
    /// executorCpuTimeNanos property.
    pub executor_cpu_time_nanos: Option<String>,
    /// executorDeserializeCpuTimeNanos property.
    pub executor_deserialize_cpu_time_nanos: Option<String>,
    /// executorDeserializeTimeMillis property.
    pub executor_deserialize_time_millis: Option<String>,
    /// executorRunTimeMillis property.
    pub executor_run_time_millis: Option<String>,
    /// jvmGcTimeMillis property.
    pub jvm_gc_time_millis: Option<String>,
    /// memoryBytesSpilled property.
    pub memory_bytes_spilled: Option<String>,
    /// peakExecutionMemoryBytes property.
    pub peak_execution_memory_bytes: Option<String>,
    /// resultSerializationTimeMillis property.
    pub result_serialization_time_millis: Option<String>,
    /// resultSize property.
    pub result_size: Option<String>,
    /// stageInputMetrics property.
    pub stage_input_metrics: Option<StageInputMetrics>,
    /// stageOutputMetrics property.
    pub stage_output_metrics: Option<StageOutputMetrics>,
    /// stageShuffleReadMetrics property.
    pub stage_shuffle_read_metrics: Option<StageShuffleReadMetrics>,
    /// stageShuffleWriteMetrics property.
    pub stage_shuffle_write_metrics: Option<StageShuffleWriteMetrics>,
}

/// `InstanceFlexibilityPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceFlexibilityPolicy {
    /// instanceMachineTypes property.
    pub instance_machine_types: Option<serde_json::Value>,
    /// instanceSelectionList property.
    pub instance_selection_list: Option<Vec<InstanceSelection>>,
    /// instanceSelectionResults property.
    pub instance_selection_results: Option<Vec<InstanceSelectionResult>>,
    /// provisioningModelMix property.
    pub provisioning_model_mix: Option<ProvisioningModelMix>,
}

/// `ClusterSelector` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClusterSelector {
    /// clusterLabels property.
    pub cluster_labels: Option<serde_json::Value>,
    /// zone property.
    pub zone: Option<String>,
}

/// `Quantiles` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Quantiles {
    /// count property.
    pub count: Option<String>,
    /// maximum property.
    pub maximum: Option<String>,
    /// minimum property.
    pub minimum: Option<String>,
    /// percentile25 property.
    pub percentile25: Option<String>,
    /// percentile50 property.
    pub percentile50: Option<String>,
    /// percentile75 property.
    pub percentile75: Option<String>,
    /// sum property.
    pub sum: Option<String>,
}

/// `Session` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Session {
    /// createTime property.
    pub create_time: Option<String>,
    /// creator property.
    pub creator: Option<String>,
    /// environmentConfig property.
    pub environment_config: Option<EnvironmentConfig>,
    /// jupyterSession property.
    pub jupyter_session: Option<JupyterConfig>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// runtimeConfig property.
    pub runtime_config: Option<RuntimeConfig>,
    /// runtimeInfo property.
    pub runtime_info: Option<RuntimeInfo>,
    /// sessionTemplate property.
    pub session_template: Option<String>,
    /// sparkConnectSession property.
    pub spark_connect_session: Option<SparkConnectConfig>,
    /// state property.
    pub state: Option<String>,
    /// stateHistory property.
    pub state_history: Option<Vec<SessionStateHistory>>,
    /// stateMessage property.
    pub state_message: Option<String>,
    /// stateTime property.
    pub state_time: Option<String>,
    /// user property.
    pub user: Option<String>,
    /// uuid property.
    pub uuid: Option<String>,
}

/// `ListSessionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListSessionsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sessions property.
    pub sessions: Option<Vec<Session>>,
}

/// `RddOperationEdge` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RddOperationEdge {
    /// fromId property.
    pub from_id: Option<i64>,
    /// toId property.
    pub to_id: Option<i64>,
}

/// `BasicAutoscalingAlgorithm` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BasicAutoscalingAlgorithm {
    /// cooldownPeriod property.
    pub cooldown_period: Option<String>,
    /// sparkStandaloneConfig property.
    pub spark_standalone_config: Option<SparkStandaloneAutoscalingConfig>,
    /// yarnConfig property.
    pub yarn_config: Option<BasicYarnAutoscalingConfig>,
}

/// `SearchSparkApplicationStageAttemptsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationStageAttemptsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageAttempts property.
    pub spark_application_stage_attempts: Option<Vec<StageData>>,
}

/// `NodeInitializationAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeInitializationAction {
    /// executableFile property.
    pub executable_file: Option<String>,
    /// executionTimeout property.
    pub execution_timeout: Option<String>,
}

/// `ReservationAffinity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationAffinity {
    /// consumeReservationType property.
    pub consume_reservation_type: Option<String>,
    /// key property.
    pub key: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `SearchSessionSparkApplicationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplications property.
    pub spark_applications: Option<Vec<SparkApplication>>,
}

/// `Interval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Interval {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `SparkSqlJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkSqlJob {
    /// jarFileUris property.
    pub jar_file_uris: Option<Vec<String>>,
    /// loggingConfig property.
    pub logging_config: Option<LoggingConfig>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
    /// queryFileUri property.
    pub query_file_uri: Option<String>,
    /// queryList property.
    pub query_list: Option<QueryList>,
    /// scriptVariables property.
    pub script_variables: Option<serde_json::Value>,
}

/// `AccessSessionSparkApplicationStageAttemptResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationStageAttemptResponse {
    /// stageData property.
    pub stage_data: Option<StageData>,
}

/// `GetPolicyOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetPolicyOptions {
    /// requestedPolicyVersion property.
    pub requested_policy_version: Option<i64>,
}

/// `DriverSchedulingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DriverSchedulingConfig {
    /// memoryMb property.
    pub memory_mb: Option<i64>,
    /// vcores property.
    pub vcores: Option<i64>,
}

/// `ExecutorMetricsDistributions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecutorMetricsDistributions {
    /// diskBytesSpilled property.
    pub disk_bytes_spilled: Option<Vec<f64>>,
    /// failedTasks property.
    pub failed_tasks: Option<Vec<f64>>,
    /// inputBytes property.
    pub input_bytes: Option<Vec<f64>>,
    /// inputRecords property.
    pub input_records: Option<Vec<f64>>,
    /// killedTasks property.
    pub killed_tasks: Option<Vec<f64>>,
    /// memoryBytesSpilled property.
    pub memory_bytes_spilled: Option<Vec<f64>>,
    /// outputBytes property.
    pub output_bytes: Option<Vec<f64>>,
    /// outputRecords property.
    pub output_records: Option<Vec<f64>>,
    /// peakMemoryMetrics property.
    pub peak_memory_metrics: Option<ExecutorPeakMetricsDistributions>,
    /// quantiles property.
    pub quantiles: Option<Vec<f64>>,
    /// shuffleRead property.
    pub shuffle_read: Option<Vec<f64>>,
    /// shuffleReadRecords property.
    pub shuffle_read_records: Option<Vec<f64>>,
    /// shuffleWrite property.
    pub shuffle_write: Option<Vec<f64>>,
    /// shuffleWriteRecords property.
    pub shuffle_write_records: Option<Vec<f64>>,
    /// succeededTasks property.
    pub succeeded_tasks: Option<Vec<f64>>,
    /// taskTimeMillis property.
    pub task_time_millis: Option<Vec<f64>>,
}

/// `SearchSparkApplicationStagesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationStagesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStages property.
    pub spark_application_stages: Option<Vec<StageData>>,
}

/// `AccessSessionSparkApplicationJobResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationJobResponse {
    /// jobData property.
    pub job_data: Option<JobData>,
}

/// `StageData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageData {
    /// accumulatorUpdates property.
    pub accumulator_updates: Option<Vec<AccumulableInfo>>,
    /// completionTime property.
    pub completion_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// details property.
    pub details: Option<String>,
    /// executorMetricsDistributions property.
    pub executor_metrics_distributions: Option<ExecutorMetricsDistributions>,
    /// executorSummary property.
    pub executor_summary: Option<serde_json::Value>,
    /// failureReason property.
    pub failure_reason: Option<String>,
    /// firstTaskLaunchedTime property.
    pub first_task_launched_time: Option<String>,
    /// isShufflePushEnabled property.
    pub is_shuffle_push_enabled: Option<bool>,
    /// jobIds property.
    pub job_ids: Option<Vec<String>>,
    /// killedTasksSummary property.
    pub killed_tasks_summary: Option<serde_json::Value>,
    /// locality property.
    pub locality: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// numActiveTasks property.
    pub num_active_tasks: Option<i64>,
    /// numCompleteTasks property.
    pub num_complete_tasks: Option<i64>,
    /// numCompletedIndices property.
    pub num_completed_indices: Option<i64>,
    /// numFailedTasks property.
    pub num_failed_tasks: Option<i64>,
    /// numKilledTasks property.
    pub num_killed_tasks: Option<i64>,
    /// numTasks property.
    pub num_tasks: Option<i64>,
    /// parentStageIds property.
    pub parent_stage_ids: Option<Vec<String>>,
    /// peakExecutorMetrics property.
    pub peak_executor_metrics: Option<ExecutorMetrics>,
    /// rddIds property.
    pub rdd_ids: Option<Vec<String>>,
    /// resourceProfileId property.
    pub resource_profile_id: Option<i64>,
    /// schedulingPool property.
    pub scheduling_pool: Option<String>,
    /// shuffleMergersCount property.
    pub shuffle_mergers_count: Option<i64>,
    /// speculationSummary property.
    pub speculation_summary: Option<SpeculationStageSummary>,
    /// stageAttemptId property.
    pub stage_attempt_id: Option<i64>,
    /// stageId property.
    pub stage_id: Option<String>,
    /// stageMetrics property.
    pub stage_metrics: Option<StageMetrics>,
    /// status property.
    pub status: Option<String>,
    /// submissionTime property.
    pub submission_time: Option<String>,
    /// taskQuantileMetrics property.
    pub task_quantile_metrics: Option<TaskQuantileMetrics>,
    /// tasks property.
    pub tasks: Option<serde_json::Value>,
}

/// `SetIamPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetIamPolicyRequest {
    /// policy property.
    pub policy: Option<Policy>,
}

/// `PySparkJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PySparkJob {
    /// archiveUris property.
    pub archive_uris: Option<Vec<String>>,
    /// args property.
    pub args: Option<Vec<String>>,
    /// fileUris property.
    pub file_uris: Option<Vec<String>>,
    /// jarFileUris property.
    pub jar_file_uris: Option<Vec<String>>,
    /// loggingConfig property.
    pub logging_config: Option<LoggingConfig>,
    /// mainPythonFileUri property.
    pub main_python_file_uri: Option<String>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
    /// pythonFileUris property.
    pub python_file_uris: Option<Vec<String>>,
}

/// `WriteSessionSparkApplicationContextResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WriteSessionSparkApplicationContextResponse {}

/// `EnvironmentConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EnvironmentConfig {
    /// executionConfig property.
    pub execution_config: Option<ExecutionConfig>,
    /// peripheralsConfig property.
    pub peripherals_config: Option<PeripheralsConfig>,
}

/// `ManagedCluster` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedCluster {
    /// clusterName property.
    pub cluster_name: Option<String>,
    /// config property.
    pub config: Option<ClusterConfig>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `BasicYarnAutoscalingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BasicYarnAutoscalingConfig {
    /// gracefulDecommissionTimeout property.
    pub graceful_decommission_timeout: Option<String>,
    /// scaleDownFactor property.
    pub scale_down_factor: Option<f64>,
    /// scaleDownMinWorkerFraction property.
    pub scale_down_min_worker_fraction: Option<f64>,
    /// scaleUpFactor property.
    pub scale_up_factor: Option<f64>,
    /// scaleUpMinWorkerFraction property.
    pub scale_up_min_worker_fraction: Option<f64>,
}

/// `SearchSparkApplicationStageAttemptTasksResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationStageAttemptTasksResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageAttemptTasks property.
    pub spark_application_stage_attempt_tasks: Option<Vec<TaskData>>,
}

/// `GkeNodePoolTarget` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GkeNodePoolTarget {
    /// nodePool property.
    pub node_pool: Option<String>,
    /// nodePoolConfig property.
    pub node_pool_config: Option<GkeNodePoolConfig>,
    /// roles property.
    pub roles: Option<Vec<String>>,
}

/// `NodeGroupAffinity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroupAffinity {
    /// nodeGroupUri property.
    pub node_group_uri: Option<String>,
}

/// `WriteSparkApplicationContextResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WriteSparkApplicationContextResponse {}

/// `StageShufflePushReadMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageShufflePushReadMetrics {
    /// corruptMergedBlockChunks property.
    pub corrupt_merged_block_chunks: Option<String>,
    /// localMergedBlocksFetched property.
    pub local_merged_blocks_fetched: Option<String>,
    /// localMergedBytesRead property.
    pub local_merged_bytes_read: Option<String>,
    /// localMergedChunksFetched property.
    pub local_merged_chunks_fetched: Option<String>,
    /// mergedFetchFallbackCount property.
    pub merged_fetch_fallback_count: Option<String>,
    /// remoteMergedBlocksFetched property.
    pub remote_merged_blocks_fetched: Option<String>,
    /// remoteMergedBytesRead property.
    pub remote_merged_bytes_read: Option<String>,
    /// remoteMergedChunksFetched property.
    pub remote_merged_chunks_fetched: Option<String>,
    /// remoteMergedReqsDuration property.
    pub remote_merged_reqs_duration: Option<String>,
}

/// `SparkStandaloneAutoscalingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkStandaloneAutoscalingConfig {
    /// gracefulDecommissionTimeout property.
    pub graceful_decommission_timeout: Option<String>,
    /// removeOnlyIdleWorkers property.
    pub remove_only_idle_workers: Option<bool>,
    /// scaleDownFactor property.
    pub scale_down_factor: Option<f64>,
    /// scaleDownMinWorkerFraction property.
    pub scale_down_min_worker_fraction: Option<f64>,
    /// scaleUpFactor property.
    pub scale_up_factor: Option<f64>,
    /// scaleUpMinWorkerFraction property.
    pub scale_up_min_worker_fraction: Option<f64>,
}

/// `ExecutionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecutionConfig {
    /// authenticationConfig property.
    pub authentication_config: Option<AuthenticationConfig>,
    /// idleTtl property.
    pub idle_ttl: Option<String>,
    /// kmsKey property.
    pub kms_key: Option<String>,
    /// networkTags property.
    pub network_tags: Option<Vec<String>>,
    /// networkUri property.
    pub network_uri: Option<String>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// stagingBucket property.
    pub staging_bucket: Option<String>,
    /// subnetworkUri property.
    pub subnetwork_uri: Option<String>,
    /// ttl property.
    pub ttl: Option<String>,
}

/// `AuthenticationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuthenticationConfig {
    /// userWorkloadAuthenticationType property.
    pub user_workload_authentication_type: Option<String>,
}

/// `StreamingQueryProgress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingQueryProgress {
    /// batchDuration property.
    pub batch_duration: Option<String>,
    /// batchId property.
    pub batch_id: Option<String>,
    /// durationMillis property.
    pub duration_millis: Option<serde_json::Value>,
    /// eventTime property.
    pub event_time: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// observedMetrics property.
    pub observed_metrics: Option<serde_json::Value>,
    /// runId property.
    pub run_id: Option<String>,
    /// sink property.
    pub sink: Option<SinkProgress>,
    /// sources property.
    pub sources: Option<Vec<SourceProgress>>,
    /// stateOperators property.
    pub state_operators: Option<Vec<StateOperatorProgress>>,
    /// streamingQueryProgressId property.
    pub streaming_query_progress_id: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `NodeGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeGroup {
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// nodeGroupConfig property.
    pub node_group_config: Option<InstanceGroupConfig>,
    /// roles property.
    pub roles: Option<Vec<String>>,
}

/// `SummarizeSessionSparkApplicationStagesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSessionSparkApplicationStagesResponse {
    /// stagesSummary property.
    pub stages_summary: Option<StagesSummary>,
}

/// `PigJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PigJob {
    /// continueOnFailure property.
    pub continue_on_failure: Option<bool>,
    /// jarFileUris property.
    pub jar_file_uris: Option<Vec<String>>,
    /// loggingConfig property.
    pub logging_config: Option<LoggingConfig>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
    /// queryFileUri property.
    pub query_file_uri: Option<String>,
    /// queryList property.
    pub query_list: Option<QueryList>,
    /// scriptVariables property.
    pub script_variables: Option<serde_json::Value>,
}

/// `StopClusterRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StopClusterRequest {
    /// clusterUuid property.
    pub cluster_uuid: Option<String>,
    /// requestId property.
    pub request_id: Option<String>,
}

/// `InputMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputMetrics {
    /// bytesRead property.
    pub bytes_read: Option<String>,
    /// recordsRead property.
    pub records_read: Option<String>,
}

/// `StageShuffleWriteMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageShuffleWriteMetrics {
    /// bytesWritten property.
    pub bytes_written: Option<String>,
    /// recordsWritten property.
    pub records_written: Option<String>,
    /// writeTimeNanos property.
    pub write_time_nanos: Option<String>,
}

/// `ResourceProfileInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceProfileInfo {
    /// executorResources property.
    pub executor_resources: Option<serde_json::Value>,
    /// resourceProfileId property.
    pub resource_profile_id: Option<i64>,
    /// taskResources property.
    pub task_resources: Option<serde_json::Value>,
}

/// `SpeculationStageSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpeculationStageSummary {
    /// numActiveTasks property.
    pub num_active_tasks: Option<i64>,
    /// numCompletedTasks property.
    pub num_completed_tasks: Option<i64>,
    /// numFailedTasks property.
    pub num_failed_tasks: Option<i64>,
    /// numKilledTasks property.
    pub num_killed_tasks: Option<i64>,
    /// numTasks property.
    pub num_tasks: Option<i64>,
    /// stageAttemptId property.
    pub stage_attempt_id: Option<i64>,
    /// stageId property.
    pub stage_id: Option<String>,
}

/// `SummarizeSparkApplicationStagesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSparkApplicationStagesResponse {
    /// stagesSummary property.
    pub stages_summary: Option<StagesSummary>,
}

/// `ShuffleReadQuantileMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShuffleReadQuantileMetrics {
    /// fetchWaitTimeMillis property.
    pub fetch_wait_time_millis: Option<Quantiles>,
    /// localBlocksFetched property.
    pub local_blocks_fetched: Option<Quantiles>,
    /// readBytes property.
    pub read_bytes: Option<Quantiles>,
    /// readRecords property.
    pub read_records: Option<Quantiles>,
    /// remoteBlocksFetched property.
    pub remote_blocks_fetched: Option<Quantiles>,
    /// remoteBytesRead property.
    pub remote_bytes_read: Option<Quantiles>,
    /// remoteBytesReadToDisk property.
    pub remote_bytes_read_to_disk: Option<Quantiles>,
    /// remoteReqsDuration property.
    pub remote_reqs_duration: Option<Quantiles>,
    /// shufflePushReadMetrics property.
    pub shuffle_push_read_metrics: Option<ShufflePushReadQuantileMetrics>,
    /// totalBlocksFetched property.
    pub total_blocks_fetched: Option<Quantiles>,
}

/// `SearchSessionSparkApplicationStageAttemptsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationStageAttemptsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageAttempts property.
    pub spark_application_stage_attempts: Option<Vec<StageData>>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `JupyterConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JupyterConfig {
    /// displayName property.
    pub display_name: Option<String>,
    /// kernel property.
    pub kernel: Option<String>,
}

/// `NativeSqlExecutionUiData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NativeSqlExecutionUiData {
    /// description property.
    pub description: Option<String>,
    /// executionId property.
    pub execution_id: Option<String>,
    /// fallbackDescription property.
    pub fallback_description: Option<String>,
    /// fallbackNodeToReason property.
    pub fallback_node_to_reason: Option<Vec<FallbackReason>>,
    /// numFallbackNodes property.
    pub num_fallback_nodes: Option<i64>,
    /// numNativeNodes property.
    pub num_native_nodes: Option<i64>,
}

/// `SparkSqlBatch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkSqlBatch {
    /// jarFileUris property.
    pub jar_file_uris: Option<Vec<String>>,
    /// queryFileUri property.
    pub query_file_uri: Option<String>,
    /// queryVariables property.
    pub query_variables: Option<serde_json::Value>,
}

/// `RddOperationCluster` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RddOperationCluster {
    /// childClusters property.
    pub child_clusters: Option<Vec<Box<RddOperationCluster>>>,
    /// childNodes property.
    pub child_nodes: Option<Vec<RddOperationNode>>,
    /// name property.
    pub name: Option<String>,
    /// rddClusterId property.
    pub rdd_cluster_id: Option<String>,
}

/// `ClusterStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClusterStatus {
    /// detail property.
    pub detail: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// stateStartTime property.
    pub state_start_time: Option<String>,
    /// substate property.
    pub substate: Option<String>,
}

/// `ApplicationInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApplicationInfo {
    /// applicationContextIngestionStatus property.
    pub application_context_ingestion_status: Option<String>,
    /// applicationId property.
    pub application_id: Option<String>,
    /// attempts property.
    pub attempts: Option<Vec<ApplicationAttemptInfo>>,
    /// coresGranted property.
    pub cores_granted: Option<i64>,
    /// coresPerExecutor property.
    pub cores_per_executor: Option<i64>,
    /// maxCores property.
    pub max_cores: Option<i64>,
    /// memoryPerExecutorMb property.
    pub memory_per_executor_mb: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// quantileDataStatus property.
    pub quantile_data_status: Option<String>,
}

/// `JobData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobData {
    /// completionTime property.
    pub completion_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// jobGroup property.
    pub job_group: Option<String>,
    /// jobId property.
    pub job_id: Option<String>,
    /// killTasksSummary property.
    pub kill_tasks_summary: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// numActiveStages property.
    pub num_active_stages: Option<i64>,
    /// numActiveTasks property.
    pub num_active_tasks: Option<i64>,
    /// numCompletedIndices property.
    pub num_completed_indices: Option<i64>,
    /// numCompletedStages property.
    pub num_completed_stages: Option<i64>,
    /// numCompletedTasks property.
    pub num_completed_tasks: Option<i64>,
    /// numFailedStages property.
    pub num_failed_stages: Option<i64>,
    /// numFailedTasks property.
    pub num_failed_tasks: Option<i64>,
    /// numKilledTasks property.
    pub num_killed_tasks: Option<i64>,
    /// numSkippedStages property.
    pub num_skipped_stages: Option<i64>,
    /// numSkippedTasks property.
    pub num_skipped_tasks: Option<i64>,
    /// numTasks property.
    pub num_tasks: Option<i64>,
    /// skippedStages property.
    pub skipped_stages: Option<Vec<i64>>,
    /// sqlExecutionId property.
    pub sql_execution_id: Option<String>,
    /// stageIds property.
    pub stage_ids: Option<Vec<String>>,
    /// status property.
    pub status: Option<String>,
    /// submissionTime property.
    pub submission_time: Option<String>,
}

/// `RepairClusterRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RepairClusterRequest {
    /// cluster property.
    pub cluster: Option<ClusterToRepair>,
    /// clusterUuid property.
    pub cluster_uuid: Option<String>,
    /// dataprocSuperUser property.
    pub dataproc_super_user: Option<bool>,
    /// gracefulDecommissionTimeout property.
    pub graceful_decommission_timeout: Option<String>,
    /// nodePools property.
    pub node_pools: Option<Vec<NodePool>>,
    /// parentOperationId property.
    pub parent_operation_id: Option<String>,
    /// requestId property.
    pub request_id: Option<String>,
}

/// `SummarizeSparkApplicationJobsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSparkApplicationJobsResponse {
    /// jobsSummary property.
    pub jobs_summary: Option<JobsSummary>,
}

/// `SummarizeSessionSparkApplicationStageAttemptTasksResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSessionSparkApplicationStageAttemptTasksResponse {
    /// stageAttemptTasksSummary property.
    pub stage_attempt_tasks_summary: Option<StageAttemptTasksSummary>,
}

/// `DiskConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiskConfig {
    /// attachedDiskConfigs property.
    pub attached_disk_configs: Option<Vec<AttachedDiskConfig>>,
    /// bootDiskProvisionedIops property.
    pub boot_disk_provisioned_iops: Option<String>,
    /// bootDiskProvisionedThroughput property.
    pub boot_disk_provisioned_throughput: Option<String>,
    /// bootDiskSizeGb property.
    pub boot_disk_size_gb: Option<i64>,
    /// bootDiskType property.
    pub boot_disk_type: Option<String>,
    /// localSsdInterface property.
    pub local_ssd_interface: Option<String>,
    /// numLocalSsds property.
    pub num_local_ssds: Option<i64>,
}

/// `ShuffleReadMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShuffleReadMetrics {
    /// fetchWaitTimeMillis property.
    pub fetch_wait_time_millis: Option<String>,
    /// localBlocksFetched property.
    pub local_blocks_fetched: Option<String>,
    /// localBytesRead property.
    pub local_bytes_read: Option<String>,
    /// recordsRead property.
    pub records_read: Option<String>,
    /// remoteBlocksFetched property.
    pub remote_blocks_fetched: Option<String>,
    /// remoteBytesRead property.
    pub remote_bytes_read: Option<String>,
    /// remoteBytesReadToDisk property.
    pub remote_bytes_read_to_disk: Option<String>,
    /// remoteReqsDuration property.
    pub remote_reqs_duration: Option<String>,
    /// shufflePushReadMetrics property.
    pub shuffle_push_read_metrics: Option<ShufflePushReadMetrics>,
}

/// `SparkRBatch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkRBatch {
    /// archiveUris property.
    pub archive_uris: Option<Vec<String>>,
    /// args property.
    pub args: Option<Vec<String>>,
    /// fileUris property.
    pub file_uris: Option<Vec<String>>,
    /// mainRFileUri property.
    pub main_r_file_uri: Option<String>,
}

/// `SearchSessionSparkApplicationStageAttemptTasksResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationStageAttemptTasksResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageAttemptTasks property.
    pub spark_application_stage_attempt_tasks: Option<Vec<TaskData>>,
}

/// `Metric` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metric {
    /// metricOverrides property.
    pub metric_overrides: Option<Vec<String>>,
    /// metricSource property.
    pub metric_source: Option<String>,
}

/// `ApplicationEnvironmentInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApplicationEnvironmentInfo {
    /// classpathEntries property.
    pub classpath_entries: Option<serde_json::Value>,
    /// hadoopProperties property.
    pub hadoop_properties: Option<serde_json::Value>,
    /// metricsProperties property.
    pub metrics_properties: Option<serde_json::Value>,
    /// resourceProfiles property.
    pub resource_profiles: Option<Vec<ResourceProfileInfo>>,
    /// runtime property.
    pub runtime: Option<SparkRuntimeInfo>,
    /// sparkProperties property.
    pub spark_properties: Option<serde_json::Value>,
    /// systemProperties property.
    pub system_properties: Option<serde_json::Value>,
}

/// `JobPlacement` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobPlacement {
    /// clusterLabels property.
    pub cluster_labels: Option<serde_json::Value>,
    /// clusterName property.
    pub cluster_name: Option<String>,
    /// clusterUuid property.
    pub cluster_uuid: Option<String>,
}

/// `RuntimeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RuntimeConfig {
    /// autotuningConfig property.
    pub autotuning_config: Option<AutotuningConfig>,
    /// cohort property.
    pub cohort: Option<String>,
    /// containerImage property.
    pub container_image: Option<String>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
    /// repositoryConfig property.
    pub repository_config: Option<RepositoryConfig>,
    /// version property.
    pub version: Option<String>,
}

/// `ExecutorStageSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecutorStageSummary {
    /// diskBytesSpilled property.
    pub disk_bytes_spilled: Option<String>,
    /// executorId property.
    pub executor_id: Option<String>,
    /// failedTasks property.
    pub failed_tasks: Option<i64>,
    /// inputBytes property.
    pub input_bytes: Option<String>,
    /// inputRecords property.
    pub input_records: Option<String>,
    /// isExcludedForStage property.
    pub is_excluded_for_stage: Option<bool>,
    /// killedTasks property.
    pub killed_tasks: Option<i64>,
    /// memoryBytesSpilled property.
    pub memory_bytes_spilled: Option<String>,
    /// outputBytes property.
    pub output_bytes: Option<String>,
    /// outputRecords property.
    pub output_records: Option<String>,
    /// peakMemoryMetrics property.
    pub peak_memory_metrics: Option<ExecutorMetrics>,
    /// shuffleRead property.
    pub shuffle_read: Option<String>,
    /// shuffleReadRecords property.
    pub shuffle_read_records: Option<String>,
    /// shuffleWrite property.
    pub shuffle_write: Option<String>,
    /// shuffleWriteRecords property.
    pub shuffle_write_records: Option<String>,
    /// stageAttemptId property.
    pub stage_attempt_id: Option<i64>,
    /// stageId property.
    pub stage_id: Option<String>,
    /// succeededTasks property.
    pub succeeded_tasks: Option<i64>,
    /// taskTimeMillis property.
    pub task_time_millis: Option<String>,
}

/// `SparkConnectExecutionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkConnectExecutionInfo {
    /// closeTimestamp property.
    pub close_timestamp: Option<String>,
    /// detail property.
    pub detail: Option<String>,
    /// finishTimestamp property.
    pub finish_timestamp: Option<String>,
    /// jobIds property.
    pub job_ids: Option<Vec<String>>,
    /// jobTag property.
    pub job_tag: Option<String>,
    /// operationId property.
    pub operation_id: Option<String>,
    /// sessionId property.
    pub session_id: Option<String>,
    /// sparkSessionTags property.
    pub spark_session_tags: Option<Vec<String>>,
    /// sqlExecIds property.
    pub sql_exec_ids: Option<Vec<String>>,
    /// startTimestamp property.
    pub start_timestamp: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// statement property.
    pub statement: Option<String>,
    /// userId property.
    pub user_id: Option<String>,
}

/// `AccessSparkApplicationEnvironmentInfoResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationEnvironmentInfoResponse {
    /// applicationEnvironmentInfo property.
    pub application_environment_info: Option<ApplicationEnvironmentInfo>,
}

/// `RddPartitionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RddPartitionInfo {
    /// blockName property.
    pub block_name: Option<String>,
    /// diskUsed property.
    pub disk_used: Option<String>,
    /// executors property.
    pub executors: Option<Vec<String>>,
    /// memoryUsed property.
    pub memory_used: Option<String>,
    /// storageLevel property.
    pub storage_level: Option<String>,
}

/// `SearchSessionSparkApplicationSqlQueriesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationSqlQueriesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationSqlQueries property.
    pub spark_application_sql_queries: Option<Vec<SqlExecutionUiData>>,
}

/// `DiagnoseClusterRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiagnoseClusterRequest {
    /// diagnosisInterval property.
    pub diagnosis_interval: Option<Interval>,
    /// job property.
    pub job: Option<String>,
    /// jobs property.
    pub jobs: Option<Vec<String>>,
    /// tarballAccess property.
    pub tarball_access: Option<String>,
    /// tarballGcsDir property.
    pub tarball_gcs_dir: Option<String>,
    /// yarnApplicationId property.
    pub yarn_application_id: Option<String>,
    /// yarnApplicationIds property.
    pub yarn_application_ids: Option<Vec<String>>,
}

/// `ListSessionTemplatesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListSessionTemplatesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sessionTemplates property.
    pub session_templates: Option<Vec<SessionTemplate>>,
}

/// `AnalyzeBatchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnalyzeBatchRequest {
    /// requestId property.
    pub request_id: Option<String>,
    /// requestorId property.
    pub requestor_id: Option<String>,
}

/// `ConsolidatedExecutorSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConsolidatedExecutorSummary {
    /// activeTasks property.
    pub active_tasks: Option<i64>,
    /// completedTasks property.
    pub completed_tasks: Option<i64>,
    /// count property.
    pub count: Option<i64>,
    /// diskUsed property.
    pub disk_used: Option<String>,
    /// failedTasks property.
    pub failed_tasks: Option<i64>,
    /// isExcluded property.
    pub is_excluded: Option<i64>,
    /// maxMemory property.
    pub max_memory: Option<String>,
    /// memoryMetrics property.
    pub memory_metrics: Option<MemoryMetrics>,
    /// memoryUsed property.
    pub memory_used: Option<String>,
    /// rddBlocks property.
    pub rdd_blocks: Option<i64>,
    /// totalCores property.
    pub total_cores: Option<i64>,
    /// totalDurationMillis property.
    pub total_duration_millis: Option<String>,
    /// totalGcTimeMillis property.
    pub total_gc_time_millis: Option<String>,
    /// totalInputBytes property.
    pub total_input_bytes: Option<String>,
    /// totalShuffleRead property.
    pub total_shuffle_read: Option<String>,
    /// totalShuffleWrite property.
    pub total_shuffle_write: Option<String>,
    /// totalTasks property.
    pub total_tasks: Option<i64>,
}

/// `PropertiesInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PropertiesInfo {
    /// autotuningProperties property.
    pub autotuning_properties: Option<serde_json::Value>,
}

/// `ShieldedInstanceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShieldedInstanceConfig {
    /// enableIntegrityMonitoring property.
    pub enable_integrity_monitoring: Option<bool>,
    /// enableSecureBoot property.
    pub enable_secure_boot: Option<bool>,
    /// enableVtpm property.
    pub enable_vtpm: Option<bool>,
}

/// `ExecutorPeakMetricsDistributions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecutorPeakMetricsDistributions {
    /// executorMetrics property.
    pub executor_metrics: Option<Vec<ExecutorMetrics>>,
    /// quantiles property.
    pub quantiles: Option<Vec<f64>>,
}

/// `SummarizeSparkApplicationExecutorsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSparkApplicationExecutorsResponse {
    /// activeExecutorSummary property.
    pub active_executor_summary: Option<ConsolidatedExecutorSummary>,
    /// applicationId property.
    pub application_id: Option<String>,
    /// deadExecutorSummary property.
    pub dead_executor_summary: Option<ConsolidatedExecutorSummary>,
    /// totalExecutorSummary property.
    pub total_executor_summary: Option<ConsolidatedExecutorSummary>,
}

/// `Cluster` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Cluster {
    /// clusterName property.
    pub cluster_name: Option<String>,
    /// clusterUuid property.
    pub cluster_uuid: Option<String>,
    /// config property.
    pub config: Option<ClusterConfig>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// metrics property.
    pub metrics: Option<ClusterMetrics>,
    /// projectId property.
    pub project_id: Option<String>,
    /// status property.
    pub status: Option<ClusterStatus>,
    /// statusHistory property.
    pub status_history: Option<Vec<ClusterStatus>>,
    /// virtualClusterConfig property.
    pub virtual_cluster_config: Option<VirtualClusterConfig>,
}

/// `LifecycleConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LifecycleConfig {
    /// autoDeleteTime property.
    pub auto_delete_time: Option<String>,
    /// autoDeleteTtl property.
    pub auto_delete_ttl: Option<String>,
    /// autoStopTime property.
    pub auto_stop_time: Option<String>,
    /// autoStopTtl property.
    pub auto_stop_ttl: Option<String>,
    /// idleDeleteTtl property.
    pub idle_delete_ttl: Option<String>,
    /// idleStartTime property.
    pub idle_start_time: Option<String>,
    /// idleStopTtl property.
    pub idle_stop_ttl: Option<String>,
}

/// `AccessSparkApplicationSqlSparkPlanGraphResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationSqlSparkPlanGraphResponse {
    /// sparkPlanGraph property.
    pub spark_plan_graph: Option<SparkPlanGraph>,
}

/// `TrinoJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TrinoJob {
    /// clientTags property.
    pub client_tags: Option<Vec<String>>,
    /// continueOnFailure property.
    pub continue_on_failure: Option<bool>,
    /// loggingConfig property.
    pub logging_config: Option<LoggingConfig>,
    /// outputFormat property.
    pub output_format: Option<String>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
    /// queryFileUri property.
    pub query_file_uri: Option<String>,
    /// queryList property.
    pub query_list: Option<QueryList>,
}

/// `SecurityConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityConfig {
    /// identityConfig property.
    pub identity_config: Option<IdentityConfig>,
    /// kerberosConfig property.
    pub kerberos_config: Option<KerberosConfig>,
}

/// `ExecutorMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecutorMetrics {
    /// metrics property.
    pub metrics: Option<serde_json::Value>,
}

/// `InstanceSelectionResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceSelectionResult {
    /// machineType property.
    pub machine_type: Option<String>,
    /// vmCount property.
    pub vm_count: Option<i64>,
}

/// `AccessSessionSparkApplicationSqlQueryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationSqlQueryResponse {
    /// executionData property.
    pub execution_data: Option<SqlExecutionUiData>,
}

/// `StartupConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StartupConfig {
    /// requiredRegistrationFraction property.
    pub required_registration_fraction: Option<f64>,
}

/// `SearchSparkApplicationExecutorStageSummaryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationExecutorStageSummaryResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageExecutors property.
    pub spark_application_stage_executors: Option<Vec<ExecutorStageSummary>>,
}

/// `RepairNodeGroupRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RepairNodeGroupRequest {
    /// instanceNames property.
    pub instance_names: Option<Vec<String>>,
    /// repairAction property.
    pub repair_action: Option<String>,
    /// requestId property.
    pub request_id: Option<String>,
}

/// `ListJobsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListJobsResponse {
    /// jobs property.
    pub jobs: Option<Vec<Job>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `GkeClusterConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GkeClusterConfig {
    /// gkeClusterTarget property.
    pub gke_cluster_target: Option<String>,
    /// namespacedGkeDeploymentTarget property.
    pub namespaced_gke_deployment_target: Option<NamespacedGkeDeploymentTarget>,
    /// nodePoolTarget property.
    pub node_pool_target: Option<Vec<GkeNodePoolTarget>>,
}

/// `Expr` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `StreamingQueryData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingQueryData {
    /// endTimestamp property.
    pub end_timestamp: Option<String>,
    /// exception property.
    pub exception: Option<String>,
    /// isActive property.
    pub is_active: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// runId property.
    pub run_id: Option<String>,
    /// startTimestamp property.
    pub start_timestamp: Option<String>,
    /// streamingQueryId property.
    pub streaming_query_id: Option<String>,
}

/// `SearchSparkApplicationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplications property.
    pub spark_applications: Option<Vec<SparkApplication>>,
}

/// `AccessSessionSparkApplicationSqlSparkPlanGraphResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationSqlSparkPlanGraphResponse {
    /// sparkPlanGraph property.
    pub spark_plan_graph: Option<SparkPlanGraph>,
}

/// `SearchSessionSparkApplicationExecutorsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationExecutorsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationExecutors property.
    pub spark_application_executors: Option<Vec<ExecutorSummary>>,
}

/// `SparkJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkJob {
    /// archiveUris property.
    pub archive_uris: Option<Vec<String>>,
    /// args property.
    pub args: Option<Vec<String>>,
    /// fileUris property.
    pub file_uris: Option<Vec<String>>,
    /// jarFileUris property.
    pub jar_file_uris: Option<Vec<String>>,
    /// loggingConfig property.
    pub logging_config: Option<LoggingConfig>,
    /// mainClass property.
    pub main_class: Option<String>,
    /// mainJarFileUri property.
    pub main_jar_file_uri: Option<String>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
}

/// `TestIamPermissionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestIamPermissionsResponse {
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `TaskMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TaskMetrics {
    /// diskBytesSpilled property.
    pub disk_bytes_spilled: Option<String>,
    /// executorCpuTimeNanos property.
    pub executor_cpu_time_nanos: Option<String>,
    /// executorDeserializeCpuTimeNanos property.
    pub executor_deserialize_cpu_time_nanos: Option<String>,
    /// executorDeserializeTimeMillis property.
    pub executor_deserialize_time_millis: Option<String>,
    /// executorRunTimeMillis property.
    pub executor_run_time_millis: Option<String>,
    /// inputMetrics property.
    pub input_metrics: Option<InputMetrics>,
    /// jvmGcTimeMillis property.
    pub jvm_gc_time_millis: Option<String>,
    /// memoryBytesSpilled property.
    pub memory_bytes_spilled: Option<String>,
    /// outputMetrics property.
    pub output_metrics: Option<OutputMetrics>,
    /// peakExecutionMemoryBytes property.
    pub peak_execution_memory_bytes: Option<String>,
    /// resultSerializationTimeMillis property.
    pub result_serialization_time_millis: Option<String>,
    /// resultSize property.
    pub result_size: Option<String>,
    /// shuffleReadMetrics property.
    pub shuffle_read_metrics: Option<ShuffleReadMetrics>,
    /// shuffleWriteMetrics property.
    pub shuffle_write_metrics: Option<ShuffleWriteMetrics>,
}

/// `SparkHistoryServerConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkHistoryServerConfig {
    /// dataprocCluster property.
    pub dataproc_cluster: Option<String>,
}

/// `UsageMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsageMetrics {
    /// acceleratorType property.
    pub accelerator_type: Option<String>,
    /// milliAcceleratorSeconds property.
    pub milli_accelerator_seconds: Option<String>,
    /// milliDcuSeconds property.
    pub milli_dcu_seconds: Option<String>,
    /// shuffleStorageGbSeconds property.
    pub shuffle_storage_gb_seconds: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `NodePool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodePool {
    /// id property.
    pub id: Option<String>,
    /// instanceNames property.
    pub instance_names: Option<Vec<String>>,
    /// repairAction property.
    pub repair_action: Option<String>,
}

/// `InstanceReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceReference {
    /// instanceId property.
    pub instance_id: Option<String>,
    /// instanceName property.
    pub instance_name: Option<String>,
    /// publicEciesKey property.
    pub public_ecies_key: Option<String>,
    /// publicKey property.
    pub public_key: Option<String>,
}

/// `PoolData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PoolData {
    /// name property.
    pub name: Option<String>,
    /// stageIds property.
    pub stage_ids: Option<Vec<String>>,
}

/// `RuntimeInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RuntimeInfo {
    /// approximateUsage property.
    pub approximate_usage: Option<UsageMetrics>,
    /// cohortInfo property.
    pub cohort_info: Option<CohortInfo>,
    /// currentUsage property.
    pub current_usage: Option<UsageSnapshot>,
    /// diagnosticOutputUri property.
    pub diagnostic_output_uri: Option<String>,
    /// endpoints property.
    pub endpoints: Option<serde_json::Value>,
    /// outputUri property.
    pub output_uri: Option<String>,
    /// propertiesInfo property.
    pub properties_info: Option<PropertiesInfo>,
}

/// `StateHistory` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StateHistory {
    /// state property.
    pub state: Option<String>,
    /// stateMessage property.
    pub state_message: Option<String>,
    /// stateStartTime property.
    pub state_start_time: Option<String>,
}

/// `AccessSparkApplicationResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationResponse {
    /// application property.
    pub application: Option<ApplicationInfo>,
}

/// `StageAttemptTasksSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageAttemptTasksSummary {
    /// applicationId property.
    pub application_id: Option<String>,
    /// numFailedTasks property.
    pub num_failed_tasks: Option<i64>,
    /// numKilledTasks property.
    pub num_killed_tasks: Option<i64>,
    /// numPendingTasks property.
    pub num_pending_tasks: Option<i64>,
    /// numRunningTasks property.
    pub num_running_tasks: Option<i64>,
    /// numSuccessTasks property.
    pub num_success_tasks: Option<i64>,
    /// numTasks property.
    pub num_tasks: Option<i64>,
    /// stageAttemptId property.
    pub stage_attempt_id: Option<i64>,
    /// stageId property.
    pub stage_id: Option<String>,
}

/// `WorkflowTemplate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkflowTemplate {
    /// createTime property.
    pub create_time: Option<String>,
    /// dagTimeout property.
    pub dag_timeout: Option<String>,
    /// encryptionConfig property.
    pub encryption_config: Option<GoogleCloudDataprocV1WorkflowTemplateEncryptionConfig>,
    /// id property.
    pub id: Option<String>,
    /// jobs property.
    pub jobs: Option<Vec<OrderedJob>>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// parameters property.
    pub parameters: Option<Vec<TemplateParameter>>,
    /// placement property.
    pub placement: Option<WorkflowTemplatePlacement>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// version property.
    pub version: Option<i64>,
}

/// `SearchSparkApplicationJobsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationJobsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationJobs property.
    pub spark_application_jobs: Option<Vec<JobData>>,
}

/// `SparkPlanGraphCluster` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkPlanGraphCluster {
    /// desc property.
    pub desc: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// metrics property.
    pub metrics: Option<Vec<SqlPlanMetric>>,
    /// name property.
    pub name: Option<String>,
    /// nodes property.
    pub nodes: Option<Vec<Box<SparkPlanGraphNodeWrapper>>>,
    /// sparkPlanGraphClusterId property.
    pub spark_plan_graph_cluster_id: Option<String>,
}

/// `FlinkJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlinkJob {
    /// args property.
    pub args: Option<Vec<String>>,
    /// jarFileUris property.
    pub jar_file_uris: Option<Vec<String>>,
    /// loggingConfig property.
    pub logging_config: Option<LoggingConfig>,
    /// mainClass property.
    pub main_class: Option<String>,
    /// mainJarFileUri property.
    pub main_jar_file_uri: Option<String>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
    /// savepointUri property.
    pub savepoint_uri: Option<String>,
}

/// `KubernetesSoftwareConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct KubernetesSoftwareConfig {
    /// componentVersion property.
    pub component_version: Option<serde_json::Value>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
}

/// `SparkRuntimeInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkRuntimeInfo {
    /// javaHome property.
    pub java_home: Option<String>,
    /// javaVersion property.
    pub java_version: Option<String>,
    /// scalaVersion property.
    pub scala_version: Option<String>,
}

/// `EncryptionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EncryptionConfig {
    /// gcePdKmsKeyName property.
    pub gce_pd_kms_key_name: Option<String>,
    /// kmsKey property.
    pub kms_key: Option<String>,
}

/// `AccumulableInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccumulableInfo {
    /// accumullableInfoId property.
    pub accumullable_info_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// update property.
    pub update: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `BuildInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuildInfo {
    /// buildKey property.
    pub build_key: Option<String>,
    /// buildValue property.
    pub build_value: Option<String>,
}

/// `SummarizeSessionSparkApplicationJobsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSessionSparkApplicationJobsResponse {
    /// jobsSummary property.
    pub jobs_summary: Option<JobsSummary>,
}

/// `WriteSessionSparkApplicationContextRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WriteSessionSparkApplicationContextRequest {
    /// parent property.
    pub parent: Option<String>,
    /// sparkWrapperObjects property.
    pub spark_wrapper_objects: Option<Vec<SparkWrapperObject>>,
}

/// `StageShuffleReadMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageShuffleReadMetrics {
    /// bytesRead property.
    pub bytes_read: Option<String>,
    /// fetchWaitTimeMillis property.
    pub fetch_wait_time_millis: Option<String>,
    /// localBlocksFetched property.
    pub local_blocks_fetched: Option<String>,
    /// localBytesRead property.
    pub local_bytes_read: Option<String>,
    /// recordsRead property.
    pub records_read: Option<String>,
    /// remoteBlocksFetched property.
    pub remote_blocks_fetched: Option<String>,
    /// remoteBytesRead property.
    pub remote_bytes_read: Option<String>,
    /// remoteBytesReadToDisk property.
    pub remote_bytes_read_to_disk: Option<String>,
    /// remoteReqsDuration property.
    pub remote_reqs_duration: Option<String>,
    /// stageShufflePushReadMetrics property.
    pub stage_shuffle_push_read_metrics: Option<StageShufflePushReadMetrics>,
}

/// `JobReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobReference {
    /// jobId property.
    pub job_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
}

/// `GkeNodePoolConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GkeNodePoolConfig {
    /// autoscaling property.
    pub autoscaling: Option<GkeNodePoolAutoscalingConfig>,
    /// config property.
    pub config: Option<GkeNodeConfig>,
    /// locations property.
    pub locations: Option<Vec<String>>,
}

/// `Policy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Policy {
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// version property.
    pub version: Option<i64>,
}

/// `AttachedDiskConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AttachedDiskConfig {
    /// diskSizeGb property.
    pub disk_size_gb: Option<i64>,
    /// diskType property.
    pub disk_type: Option<String>,
    /// provisionedIops property.
    pub provisioned_iops: Option<String>,
    /// provisionedThroughput property.
    pub provisioned_throughput: Option<String>,
}

/// `SourceProgress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceProgress {
    /// description property.
    pub description: Option<String>,
    /// endOffset property.
    pub end_offset: Option<String>,
    /// inputRowsPerSecond property.
    pub input_rows_per_second: Option<f64>,
    /// latestOffset property.
    pub latest_offset: Option<String>,
    /// metrics property.
    pub metrics: Option<serde_json::Value>,
    /// numInputRows property.
    pub num_input_rows: Option<String>,
    /// processedRowsPerSecond property.
    pub processed_rows_per_second: Option<f64>,
    /// startOffset property.
    pub start_offset: Option<String>,
}

/// `RepositoryConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RepositoryConfig {
    /// pypiRepositoryConfig property.
    pub pypi_repository_config: Option<PyPiRepositoryConfig>,
}

/// `SummarizeSparkApplicationStageAttemptTasksResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSparkApplicationStageAttemptTasksResponse {
    /// stageAttemptTasksSummary property.
    pub stage_attempt_tasks_summary: Option<StageAttemptTasksSummary>,
}

/// `InjectCredentialsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InjectCredentialsRequest {
    /// clusterUuid property.
    pub cluster_uuid: Option<String>,
    /// credentialsCiphertext property.
    pub credentials_ciphertext: Option<String>,
}

/// `PrestoJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PrestoJob {
    /// clientTags property.
    pub client_tags: Option<Vec<String>>,
    /// continueOnFailure property.
    pub continue_on_failure: Option<bool>,
    /// loggingConfig property.
    pub logging_config: Option<LoggingConfig>,
    /// outputFormat property.
    pub output_format: Option<String>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
    /// queryFileUri property.
    pub query_file_uri: Option<String>,
    /// queryList property.
    pub query_list: Option<QueryList>,
}

/// `HadoopJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HadoopJob {
    /// archiveUris property.
    pub archive_uris: Option<Vec<String>>,
    /// args property.
    pub args: Option<Vec<String>>,
    /// fileUris property.
    pub file_uris: Option<Vec<String>>,
    /// jarFileUris property.
    pub jar_file_uris: Option<Vec<String>>,
    /// loggingConfig property.
    pub logging_config: Option<LoggingConfig>,
    /// mainClass property.
    pub main_class: Option<String>,
    /// mainJarFileUri property.
    pub main_jar_file_uri: Option<String>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
}

/// `InstantiateWorkflowTemplateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstantiateWorkflowTemplateRequest {
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
    /// requestId property.
    pub request_id: Option<String>,
    /// version property.
    pub version: Option<i64>,
}

/// `AccessSparkApplicationSqlQueryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationSqlQueryResponse {
    /// executionData property.
    pub execution_data: Option<SqlExecutionUiData>,
}

/// `NamespacedGkeDeploymentTarget` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NamespacedGkeDeploymentTarget {
    /// clusterNamespace property.
    pub cluster_namespace: Option<String>,
    /// targetGkeCluster property.
    pub target_gke_cluster: Option<String>,
}

/// `MetastoreConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MetastoreConfig {
    /// dataprocMetastoreService property.
    pub dataproc_metastore_service: Option<String>,
}

/// `SessionTemplate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SessionTemplate {
    /// createTime property.
    pub create_time: Option<String>,
    /// creator property.
    pub creator: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// environmentConfig property.
    pub environment_config: Option<EnvironmentConfig>,
    /// jupyterSession property.
    pub jupyter_session: Option<JupyterConfig>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// runtimeConfig property.
    pub runtime_config: Option<RuntimeConfig>,
    /// sparkConnectSession property.
    pub spark_connect_session: Option<SparkConnectConfig>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// uuid property.
    pub uuid: Option<String>,
}

/// `SparkWrapperObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkWrapperObject {
    /// appSummary property.
    pub app_summary: Option<AppSummary>,
    /// applicationEnvironmentInfo property.
    pub application_environment_info: Option<ApplicationEnvironmentInfo>,
    /// applicationId property.
    pub application_id: Option<String>,
    /// applicationInfo property.
    pub application_info: Option<ApplicationInfo>,
    /// eventTimestamp property.
    pub event_timestamp: Option<String>,
    /// executorStageSummary property.
    pub executor_stage_summary: Option<ExecutorStageSummary>,
    /// executorSummary property.
    pub executor_summary: Option<ExecutorSummary>,
    /// jobData property.
    pub job_data: Option<JobData>,
    /// nativeBuildInfoUiData property.
    pub native_build_info_ui_data: Option<NativeBuildInfoUiData>,
    /// nativeSqlExecutionUiData property.
    pub native_sql_execution_ui_data: Option<NativeSqlExecutionUiData>,
    /// poolData property.
    pub pool_data: Option<PoolData>,
    /// processSummary property.
    pub process_summary: Option<ProcessSummary>,
    /// rddOperationGraph property.
    pub rdd_operation_graph: Option<RddOperationGraph>,
    /// rddStorageInfo property.
    pub rdd_storage_info: Option<RddStorageInfo>,
    /// resourceProfileInfo property.
    pub resource_profile_info: Option<ResourceProfileInfo>,
    /// sparkConnectExecutionInfo property.
    pub spark_connect_execution_info: Option<SparkConnectExecutionInfo>,
    /// sparkConnectSessionInfo property.
    pub spark_connect_session_info: Option<SparkConnectSessionInfo>,
    /// sparkPlanGraph property.
    pub spark_plan_graph: Option<SparkPlanGraph>,
    /// speculationStageSummary property.
    pub speculation_stage_summary: Option<SpeculationStageSummary>,
    /// sqlExecutionUiData property.
    pub sql_execution_ui_data: Option<SqlExecutionUiData>,
    /// stageData property.
    pub stage_data: Option<StageData>,
    /// streamBlockData property.
    pub stream_block_data: Option<StreamBlockData>,
    /// streamingQueryData property.
    pub streaming_query_data: Option<StreamingQueryData>,
    /// streamingQueryProgress property.
    pub streaming_query_progress: Option<StreamingQueryProgress>,
    /// taskData property.
    pub task_data: Option<TaskData>,
}

/// `ValueValidation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ValueValidation {
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `AutoscalingPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalingPolicy {
    /// basicAlgorithm property.
    pub basic_algorithm: Option<BasicAutoscalingAlgorithm>,
    /// clusterType property.
    pub cluster_type: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// secondaryWorkerConfig property.
    pub secondary_worker_config: Option<InstanceGroupAutoscalingPolicyConfig>,
    /// workerConfig property.
    pub worker_config: Option<InstanceGroupAutoscalingPolicyConfig>,
}

/// `AutotuningConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutotuningConfig {
    /// scenarios property.
    pub scenarios: Option<Vec<String>>,
}

/// `ShuffleWriteQuantileMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShuffleWriteQuantileMetrics {
    /// writeBytes property.
    pub write_bytes: Option<Quantiles>,
    /// writeRecords property.
    pub write_records: Option<Quantiles>,
    /// writeTimeNanos property.
    pub write_time_nanos: Option<Quantiles>,
}

/// `ProcessSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProcessSummary {
    /// addTime property.
    pub add_time: Option<String>,
    /// hostPort property.
    pub host_port: Option<String>,
    /// isActive property.
    pub is_active: Option<bool>,
    /// processId property.
    pub process_id: Option<String>,
    /// processLogs property.
    pub process_logs: Option<serde_json::Value>,
    /// removeTime property.
    pub remove_time: Option<String>,
    /// totalCores property.
    pub total_cores: Option<i64>,
}

/// `SessionStateHistory` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SessionStateHistory {
    /// state property.
    pub state: Option<String>,
    /// stateMessage property.
    pub state_message: Option<String>,
    /// stateStartTime property.
    pub state_start_time: Option<String>,
}

/// `ParameterValidation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ParameterValidation {
    /// regex property.
    pub regex: Option<RegexValidation>,
    /// values property.
    pub values: Option<ValueValidation>,
}

/// `AccessSessionSparkApplicationStageRddOperationGraphResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationStageRddOperationGraphResponse {
    /// rddOperationGraph property.
    pub rdd_operation_graph: Option<RddOperationGraph>,
}

/// `StageOutputMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageOutputMetrics {
    /// bytesWritten property.
    pub bytes_written: Option<String>,
    /// recordsWritten property.
    pub records_written: Option<String>,
}

/// `MemoryMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MemoryMetrics {
    /// totalOffHeapStorageMemory property.
    pub total_off_heap_storage_memory: Option<String>,
    /// totalOnHeapStorageMemory property.
    pub total_on_heap_storage_memory: Option<String>,
    /// usedOffHeapStorageMemory property.
    pub used_off_heap_storage_memory: Option<String>,
    /// usedOnHeapStorageMemory property.
    pub used_on_heap_storage_memory: Option<String>,
}

/// `StageInputMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageInputMetrics {
    /// bytesRead property.
    pub bytes_read: Option<String>,
    /// recordsRead property.
    pub records_read: Option<String>,
}

/// `SearchSessionSparkApplicationExecutorStageSummaryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationExecutorStageSummaryResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStageExecutors property.
    pub spark_application_stage_executors: Option<Vec<ExecutorStageSummary>>,
}

/// `AccessSparkApplicationStageAttemptResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationStageAttemptResponse {
    /// stageData property.
    pub stage_data: Option<StageData>,
}

/// `WriteSparkApplicationContextRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WriteSparkApplicationContextRequest {
    /// parent property.
    pub parent: Option<String>,
    /// sparkWrapperObjects property.
    pub spark_wrapper_objects: Option<Vec<SparkWrapperObject>>,
}

/// `ResizeNodeGroupRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResizeNodeGroupRequest {
    /// gracefulDecommissionTimeout property.
    pub graceful_decommission_timeout: Option<String>,
    /// parentOperationId property.
    pub parent_operation_id: Option<String>,
    /// requestId property.
    pub request_id: Option<String>,
    /// size property.
    pub size: Option<i64>,
}

/// `RddStorageInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RddStorageInfo {
    /// dataDistribution property.
    pub data_distribution: Option<Vec<RddDataDistribution>>,
    /// diskUsed property.
    pub disk_used: Option<String>,
    /// memoryUsed property.
    pub memory_used: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// numCachedPartitions property.
    pub num_cached_partitions: Option<i64>,
    /// numPartitions property.
    pub num_partitions: Option<i64>,
    /// partitions property.
    pub partitions: Option<Vec<RddPartitionInfo>>,
    /// rddStorageId property.
    pub rdd_storage_id: Option<i64>,
    /// storageLevel property.
    pub storage_level: Option<String>,
}

/// `PyPiRepositoryConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PyPiRepositoryConfig {
    /// pypiRepository property.
    pub pypi_repository: Option<String>,
}

/// `ClusterToRepair` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClusterToRepair {
    /// clusterRepairAction property.
    pub cluster_repair_action: Option<String>,
}

/// `OrderedJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OrderedJob {
    /// flinkJob property.
    pub flink_job: Option<FlinkJob>,
    /// hadoopJob property.
    pub hadoop_job: Option<HadoopJob>,
    /// hiveJob property.
    pub hive_job: Option<HiveJob>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// pigJob property.
    pub pig_job: Option<PigJob>,
    /// prerequisiteStepIds property.
    pub prerequisite_step_ids: Option<Vec<String>>,
    /// prestoJob property.
    pub presto_job: Option<PrestoJob>,
    /// pysparkJob property.
    pub pyspark_job: Option<PySparkJob>,
    /// scheduling property.
    pub scheduling: Option<JobScheduling>,
    /// sparkJob property.
    pub spark_job: Option<SparkJob>,
    /// sparkRJob property.
    pub spark_r_job: Option<SparkRJob>,
    /// sparkSqlJob property.
    pub spark_sql_job: Option<SparkSqlJob>,
    /// stepId property.
    pub step_id: Option<String>,
    /// trinoJob property.
    pub trino_job: Option<TrinoJob>,
}

/// `SearchSessionSparkApplicationJobsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationJobsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationJobs property.
    pub spark_application_jobs: Option<Vec<JobData>>,
}

/// `Job` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Job {
    /// done property.
    pub done: Option<bool>,
    /// driverControlFilesUri property.
    pub driver_control_files_uri: Option<String>,
    /// driverOutputResourceUri property.
    pub driver_output_resource_uri: Option<String>,
    /// driverSchedulingConfig property.
    pub driver_scheduling_config: Option<DriverSchedulingConfig>,
    /// flinkJob property.
    pub flink_job: Option<FlinkJob>,
    /// hadoopJob property.
    pub hadoop_job: Option<HadoopJob>,
    /// hiveJob property.
    pub hive_job: Option<HiveJob>,
    /// jobUuid property.
    pub job_uuid: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// pigJob property.
    pub pig_job: Option<PigJob>,
    /// placement property.
    pub placement: Option<JobPlacement>,
    /// prestoJob property.
    pub presto_job: Option<PrestoJob>,
    /// pysparkJob property.
    pub pyspark_job: Option<PySparkJob>,
    /// reference property.
    pub reference: Option<JobReference>,
    /// scheduling property.
    pub scheduling: Option<JobScheduling>,
    /// sparkJob property.
    pub spark_job: Option<SparkJob>,
    /// sparkRJob property.
    pub spark_r_job: Option<SparkRJob>,
    /// sparkSqlJob property.
    pub spark_sql_job: Option<SparkSqlJob>,
    /// status property.
    pub status: Option<JobStatus>,
    /// statusHistory property.
    pub status_history: Option<Vec<JobStatus>>,
    /// trinoJob property.
    pub trino_job: Option<TrinoJob>,
    /// yarnApplications property.
    pub yarn_applications: Option<Vec<YarnApplication>>,
}

/// `InputQuantileMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputQuantileMetrics {
    /// bytesRead property.
    pub bytes_read: Option<Quantiles>,
    /// recordsRead property.
    pub records_read: Option<Quantiles>,
}

/// `ConfidentialInstanceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConfidentialInstanceConfig {
    /// enableConfidentialCompute property.
    pub enable_confidential_compute: Option<bool>,
}

/// `RegexValidation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegexValidation {
    /// regexes property.
    pub regexes: Option<Vec<String>>,
}

/// `AppSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppSummary {
    /// numCompletedJobs property.
    pub num_completed_jobs: Option<i64>,
    /// numCompletedStages property.
    pub num_completed_stages: Option<i64>,
}

/// `SqlExecutionUiData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlExecutionUiData {
    /// completionTime property.
    pub completion_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// details property.
    pub details: Option<String>,
    /// errorMessage property.
    pub error_message: Option<String>,
    /// executionId property.
    pub execution_id: Option<String>,
    /// jobs property.
    pub jobs: Option<serde_json::Value>,
    /// metricValues property.
    pub metric_values: Option<serde_json::Value>,
    /// metricValuesIsNull property.
    pub metric_values_is_null: Option<bool>,
    /// metrics property.
    pub metrics: Option<Vec<SqlPlanMetric>>,
    /// modifiedConfigs property.
    pub modified_configs: Option<serde_json::Value>,
    /// physicalPlanDescription property.
    pub physical_plan_description: Option<String>,
    /// rootExecutionId property.
    pub root_execution_id: Option<String>,
    /// stages property.
    pub stages: Option<Vec<String>>,
    /// submissionTime property.
    pub submission_time: Option<String>,
}

/// `RddOperationNode` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RddOperationNode {
    /// barrier property.
    pub barrier: Option<bool>,
    /// cached property.
    pub cached: Option<bool>,
    /// callsite property.
    pub callsite: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nodeId property.
    pub node_id: Option<i64>,
    /// outputDeterministicLevel property.
    pub output_deterministic_level: Option<String>,
}

/// `SearchSessionSparkApplicationStagesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSessionSparkApplicationStagesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationStages property.
    pub spark_application_stages: Option<Vec<StageData>>,
}

/// `ListClustersResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListClustersResponse {
    /// clusters property.
    pub clusters: Option<Vec<Cluster>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `RddOperationGraph` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RddOperationGraph {
    /// edges property.
    pub edges: Option<Vec<RddOperationEdge>>,
    /// incomingEdges property.
    pub incoming_edges: Option<Vec<RddOperationEdge>>,
    /// outgoingEdges property.
    pub outgoing_edges: Option<Vec<RddOperationEdge>>,
    /// rootCluster property.
    pub root_cluster: Option<Box<RddOperationCluster>>,
    /// stageId property.
    pub stage_id: Option<String>,
}

/// `IdentityConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentityConfig {
    /// userServiceAccountMapping property.
    pub user_service_account_mapping: Option<serde_json::Value>,
}

/// `TerminateSessionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TerminateSessionRequest {
    /// requestId property.
    pub request_id: Option<String>,
}

/// `Binding` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `InstanceGroupAutoscalingPolicyConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceGroupAutoscalingPolicyConfig {
    /// maxInstances property.
    pub max_instances: Option<i64>,
    /// minInstances property.
    pub min_instances: Option<i64>,
    /// weight property.
    pub weight: Option<i64>,
}

/// `GkeNodeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GkeNodeConfig {
    /// accelerators property.
    pub accelerators: Option<Vec<GkeNodePoolAcceleratorConfig>>,
    /// bootDiskKmsKey property.
    pub boot_disk_kms_key: Option<String>,
    /// localSsdCount property.
    pub local_ssd_count: Option<i64>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
    /// preemptible property.
    pub preemptible: Option<bool>,
    /// spot property.
    pub spot: Option<bool>,
}

/// `WorkflowTemplatePlacement` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkflowTemplatePlacement {
    /// clusterSelector property.
    pub cluster_selector: Option<ClusterSelector>,
    /// managedCluster property.
    pub managed_cluster: Option<ManagedCluster>,
}

/// `SearchSparkApplicationExecutorsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationExecutorsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationExecutors property.
    pub spark_application_executors: Option<Vec<ExecutorSummary>>,
}

/// `JobScheduling` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobScheduling {
    /// maxFailuresPerHour property.
    pub max_failures_per_hour: Option<i64>,
    /// maxFailuresTotal property.
    pub max_failures_total: Option<i64>,
}

/// `PeripheralsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PeripheralsConfig {
    /// metastoreService property.
    pub metastore_service: Option<String>,
    /// sparkHistoryServerConfig property.
    pub spark_history_server_config: Option<SparkHistoryServerConfig>,
}

/// `GoogleCloudDataprocV1WorkflowTemplateEncryptionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDataprocV1WorkflowTemplateEncryptionConfig {
    /// kmsKey property.
    pub kms_key: Option<String>,
}

/// `InstanceSelection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceSelection {
    /// machineTypes property.
    pub machine_types: Option<Vec<String>>,
    /// rank property.
    pub rank: Option<i64>,
}

/// `ProvisioningModelMix` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProvisioningModelMix {
    /// standardCapacityBase property.
    pub standard_capacity_base: Option<i64>,
    /// standardCapacityPercentAboveBase property.
    pub standard_capacity_percent_above_base: Option<i64>,
}

/// `AutoscalingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalingConfig {
    /// policyUri property.
    pub policy_uri: Option<String>,
}

/// `SoftwareConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SoftwareConfig {
    /// imageVersion property.
    pub image_version: Option<String>,
    /// optionalComponents property.
    pub optional_components: Option<Vec<String>>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
}

/// `LoggingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoggingConfig {
    /// driverLogLevels property.
    pub driver_log_levels: Option<serde_json::Value>,
}

/// `SparkPlanGraphEdge` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkPlanGraphEdge {
    /// fromId property.
    pub from_id: Option<String>,
    /// toId property.
    pub to_id: Option<String>,
}

/// `GkeNodePoolAcceleratorConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GkeNodePoolAcceleratorConfig {
    /// acceleratorCount property.
    pub accelerator_count: Option<String>,
    /// acceleratorType property.
    pub accelerator_type: Option<String>,
    /// gpuPartitionSize property.
    pub gpu_partition_size: Option<String>,
}

/// `SparkBatch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkBatch {
    /// archiveUris property.
    pub archive_uris: Option<Vec<String>>,
    /// args property.
    pub args: Option<Vec<String>>,
    /// fileUris property.
    pub file_uris: Option<Vec<String>>,
    /// jarFileUris property.
    pub jar_file_uris: Option<Vec<String>>,
    /// mainClass property.
    pub main_class: Option<String>,
    /// mainJarFileUri property.
    pub main_jar_file_uri: Option<String>,
}

/// `Batch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Batch {
    /// createTime property.
    pub create_time: Option<String>,
    /// creator property.
    pub creator: Option<String>,
    /// environmentConfig property.
    pub environment_config: Option<EnvironmentConfig>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// operation property.
    pub operation: Option<String>,
    /// pysparkBatch property.
    pub pyspark_batch: Option<PySparkBatch>,
    /// runtimeConfig property.
    pub runtime_config: Option<RuntimeConfig>,
    /// runtimeInfo property.
    pub runtime_info: Option<RuntimeInfo>,
    /// sparkBatch property.
    pub spark_batch: Option<SparkBatch>,
    /// sparkRBatch property.
    pub spark_r_batch: Option<SparkRBatch>,
    /// sparkSqlBatch property.
    pub spark_sql_batch: Option<SparkSqlBatch>,
    /// state property.
    pub state: Option<String>,
    /// stateHistory property.
    pub state_history: Option<Vec<StateHistory>>,
    /// stateMessage property.
    pub state_message: Option<String>,
    /// stateTime property.
    pub state_time: Option<String>,
    /// uuid property.
    pub uuid: Option<String>,
}

/// `SinkProgress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SinkProgress {
    /// description property.
    pub description: Option<String>,
    /// metrics property.
    pub metrics: Option<serde_json::Value>,
    /// numOutputRows property.
    pub num_output_rows: Option<String>,
}

/// `SparkApplication` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkApplication {
    /// application property.
    pub application: Option<ApplicationInfo>,
    /// name property.
    pub name: Option<String>,
}

/// `JobsSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobsSummary {
    /// activeJobs property.
    pub active_jobs: Option<i64>,
    /// applicationId property.
    pub application_id: Option<String>,
    /// attempts property.
    pub attempts: Option<Vec<ApplicationAttemptInfo>>,
    /// completedJobs property.
    pub completed_jobs: Option<i64>,
    /// failedJobs property.
    pub failed_jobs: Option<i64>,
    /// schedulingMode property.
    pub scheduling_mode: Option<String>,
}

/// `StateOperatorProgress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StateOperatorProgress {
    /// allRemovalsTimeMs property.
    pub all_removals_time_ms: Option<String>,
    /// allUpdatesTimeMs property.
    pub all_updates_time_ms: Option<String>,
    /// commitTimeMs property.
    pub commit_time_ms: Option<String>,
    /// customMetrics property.
    pub custom_metrics: Option<serde_json::Value>,
    /// memoryUsedBytes property.
    pub memory_used_bytes: Option<String>,
    /// numRowsDroppedByWatermark property.
    pub num_rows_dropped_by_watermark: Option<String>,
    /// numRowsRemoved property.
    pub num_rows_removed: Option<String>,
    /// numRowsTotal property.
    pub num_rows_total: Option<String>,
    /// numRowsUpdated property.
    pub num_rows_updated: Option<String>,
    /// numShufflePartitions property.
    pub num_shuffle_partitions: Option<String>,
    /// numStateStoreInstances property.
    pub num_state_store_instances: Option<String>,
    /// operatorName property.
    pub operator_name: Option<String>,
}

/// `ShuffleWriteMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShuffleWriteMetrics {
    /// bytesWritten property.
    pub bytes_written: Option<String>,
    /// recordsWritten property.
    pub records_written: Option<String>,
    /// writeTimeNanos property.
    pub write_time_nanos: Option<String>,
}

/// `YarnApplication` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct YarnApplication {
    /// memoryMbSeconds property.
    pub memory_mb_seconds: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// progress property.
    pub progress: Option<f64>,
    /// state property.
    pub state: Option<String>,
    /// trackingUrl property.
    pub tracking_url: Option<String>,
    /// vcoreSeconds property.
    pub vcore_seconds: Option<String>,
}

/// `TaskQuantileMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TaskQuantileMetrics {
    /// diskBytesSpilled property.
    pub disk_bytes_spilled: Option<Quantiles>,
    /// durationMillis property.
    pub duration_millis: Option<Quantiles>,
    /// executorCpuTimeNanos property.
    pub executor_cpu_time_nanos: Option<Quantiles>,
    /// executorDeserializeCpuTimeNanos property.
    pub executor_deserialize_cpu_time_nanos: Option<Quantiles>,
    /// executorDeserializeTimeMillis property.
    pub executor_deserialize_time_millis: Option<Quantiles>,
    /// executorRunTimeMillis property.
    pub executor_run_time_millis: Option<Quantiles>,
    /// gettingResultTimeMillis property.
    pub getting_result_time_millis: Option<Quantiles>,
    /// inputMetrics property.
    pub input_metrics: Option<InputQuantileMetrics>,
    /// jvmGcTimeMillis property.
    pub jvm_gc_time_millis: Option<Quantiles>,
    /// memoryBytesSpilled property.
    pub memory_bytes_spilled: Option<Quantiles>,
    /// outputMetrics property.
    pub output_metrics: Option<OutputQuantileMetrics>,
    /// peakExecutionMemoryBytes property.
    pub peak_execution_memory_bytes: Option<Quantiles>,
    /// resultSerializationTimeMillis property.
    pub result_serialization_time_millis: Option<Quantiles>,
    /// resultSize property.
    pub result_size: Option<Quantiles>,
    /// schedulerDelayMillis property.
    pub scheduler_delay_millis: Option<Quantiles>,
    /// shuffleReadMetrics property.
    pub shuffle_read_metrics: Option<ShuffleReadQuantileMetrics>,
    /// shuffleWriteMetrics property.
    pub shuffle_write_metrics: Option<ShuffleWriteQuantileMetrics>,
}

/// `FallbackReason` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FallbackReason {
    /// fallbackNode property.
    pub fallback_node: Option<String>,
    /// fallbackReason property.
    pub fallback_reason: Option<String>,
}

/// `TaskData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TaskData {
    /// accumulatorUpdates property.
    pub accumulator_updates: Option<Vec<AccumulableInfo>>,
    /// attempt property.
    pub attempt: Option<i64>,
    /// durationMillis property.
    pub duration_millis: Option<String>,
    /// errorMessage property.
    pub error_message: Option<String>,
    /// executorId property.
    pub executor_id: Option<String>,
    /// executorLogs property.
    pub executor_logs: Option<serde_json::Value>,
    /// gettingResultTimeMillis property.
    pub getting_result_time_millis: Option<String>,
    /// hasMetrics property.
    pub has_metrics: Option<bool>,
    /// host property.
    pub host: Option<String>,
    /// index property.
    pub index: Option<i64>,
    /// launchTime property.
    pub launch_time: Option<String>,
    /// partitionId property.
    pub partition_id: Option<i64>,
    /// resultFetchStart property.
    pub result_fetch_start: Option<String>,
    /// schedulerDelayMillis property.
    pub scheduler_delay_millis: Option<String>,
    /// speculative property.
    pub speculative: Option<bool>,
    /// stageAttemptId property.
    pub stage_attempt_id: Option<i64>,
    /// stageId property.
    pub stage_id: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// taskId property.
    pub task_id: Option<String>,
    /// taskLocality property.
    pub task_locality: Option<String>,
    /// taskMetrics property.
    pub task_metrics: Option<TaskMetrics>,
}

/// `SparkRJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkRJob {
    /// archiveUris property.
    pub archive_uris: Option<Vec<String>>,
    /// args property.
    pub args: Option<Vec<String>>,
    /// fileUris property.
    pub file_uris: Option<Vec<String>>,
    /// loggingConfig property.
    pub logging_config: Option<LoggingConfig>,
    /// mainRFileUri property.
    pub main_r_file_uri: Option<String>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
}

/// `ClusterConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClusterConfig {
    /// autoscalingConfig property.
    pub autoscaling_config: Option<AutoscalingConfig>,
    /// auxiliaryNodeGroups property.
    pub auxiliary_node_groups: Option<Vec<AuxiliaryNodeGroup>>,
    /// clusterTier property.
    pub cluster_tier: Option<String>,
    /// clusterType property.
    pub cluster_type: Option<String>,
    /// configBucket property.
    pub config_bucket: Option<String>,
    /// dataprocMetricConfig property.
    pub dataproc_metric_config: Option<DataprocMetricConfig>,
    /// diagnosticBucket property.
    pub diagnostic_bucket: Option<String>,
    /// encryptionConfig property.
    pub encryption_config: Option<EncryptionConfig>,
    /// endpointConfig property.
    pub endpoint_config: Option<EndpointConfig>,
    /// engine property.
    pub engine: Option<String>,
    /// gceClusterConfig property.
    pub gce_cluster_config: Option<GceClusterConfig>,
    /// gkeClusterConfig property.
    pub gke_cluster_config: Option<GkeClusterConfig>,
    /// initializationActions property.
    pub initialization_actions: Option<Vec<NodeInitializationAction>>,
    /// lifecycleConfig property.
    pub lifecycle_config: Option<LifecycleConfig>,
    /// masterConfig property.
    pub master_config: Option<InstanceGroupConfig>,
    /// metastoreConfig property.
    pub metastore_config: Option<MetastoreConfig>,
    /// secondaryWorkerConfig property.
    pub secondary_worker_config: Option<InstanceGroupConfig>,
    /// securityConfig property.
    pub security_config: Option<SecurityConfig>,
    /// softwareConfig property.
    pub software_config: Option<SoftwareConfig>,
    /// tempBucket property.
    pub temp_bucket: Option<String>,
    /// workerConfig property.
    pub worker_config: Option<InstanceGroupConfig>,
}

/// `TestIamPermissionsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestIamPermissionsRequest {
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `ClusterMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClusterMetrics {
    /// hdfsMetrics property.
    pub hdfs_metrics: Option<serde_json::Value>,
    /// yarnMetrics property.
    pub yarn_metrics: Option<serde_json::Value>,
}

/// `ManagedGroupConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedGroupConfig {
    /// instanceGroupManagerName property.
    pub instance_group_manager_name: Option<String>,
    /// instanceGroupManagerUri property.
    pub instance_group_manager_uri: Option<String>,
    /// instanceTemplateName property.
    pub instance_template_name: Option<String>,
}

/// `AccessSessionSparkApplicationResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationResponse {
    /// application property.
    pub application: Option<ApplicationInfo>,
}

/// `StagesSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StagesSummary {
    /// applicationId property.
    pub application_id: Option<String>,
    /// numActiveStages property.
    pub num_active_stages: Option<i64>,
    /// numCompletedStages property.
    pub num_completed_stages: Option<i64>,
    /// numFailedStages property.
    pub num_failed_stages: Option<i64>,
    /// numPendingStages property.
    pub num_pending_stages: Option<i64>,
    /// numSkippedStages property.
    pub num_skipped_stages: Option<i64>,
}

/// `UsageSnapshot` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsageSnapshot {
    /// acceleratorType property.
    pub accelerator_type: Option<String>,
    /// milliAccelerator property.
    pub milli_accelerator: Option<String>,
    /// milliDcu property.
    pub milli_dcu: Option<String>,
    /// milliDcuPremium property.
    pub milli_dcu_premium: Option<String>,
    /// shuffleStorageGb property.
    pub shuffle_storage_gb: Option<String>,
    /// shuffleStorageGbPremium property.
    pub shuffle_storage_gb_premium: Option<String>,
    /// snapshotTime property.
    pub snapshot_time: Option<String>,
}

/// `GetIamPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetIamPolicyRequest {
    /// options property.
    pub options: Option<GetPolicyOptions>,
}

/// `AuxiliaryNodeGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuxiliaryNodeGroup {
    /// nodeGroup property.
    pub node_group: Option<NodeGroup>,
    /// nodeGroupId property.
    pub node_group_id: Option<String>,
}

/// `ShufflePushReadQuantileMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShufflePushReadQuantileMetrics {
    /// corruptMergedBlockChunks property.
    pub corrupt_merged_block_chunks: Option<Quantiles>,
    /// localMergedBlocksFetched property.
    pub local_merged_blocks_fetched: Option<Quantiles>,
    /// localMergedBytesRead property.
    pub local_merged_bytes_read: Option<Quantiles>,
    /// localMergedChunksFetched property.
    pub local_merged_chunks_fetched: Option<Quantiles>,
    /// mergedFetchFallbackCount property.
    pub merged_fetch_fallback_count: Option<Quantiles>,
    /// remoteMergedBlocksFetched property.
    pub remote_merged_blocks_fetched: Option<Quantiles>,
    /// remoteMergedBytesRead property.
    pub remote_merged_bytes_read: Option<Quantiles>,
    /// remoteMergedChunksFetched property.
    pub remote_merged_chunks_fetched: Option<Quantiles>,
    /// remoteMergedReqsDuration property.
    pub remote_merged_reqs_duration: Option<Quantiles>,
}

/// `AuxiliaryServicesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuxiliaryServicesConfig {
    /// metastoreConfig property.
    pub metastore_config: Option<MetastoreConfig>,
    /// sparkHistoryServerConfig property.
    pub spark_history_server_config: Option<SparkHistoryServerConfig>,
}

/// `OutputQuantileMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OutputQuantileMetrics {
    /// bytesWritten property.
    pub bytes_written: Option<Quantiles>,
    /// recordsWritten property.
    pub records_written: Option<Quantiles>,
}

/// `QueryList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryList {
    /// queries property.
    pub queries: Option<Vec<String>>,
}

/// `ShufflePushReadMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShufflePushReadMetrics {
    /// corruptMergedBlockChunks property.
    pub corrupt_merged_block_chunks: Option<String>,
    /// localMergedBlocksFetched property.
    pub local_merged_blocks_fetched: Option<String>,
    /// localMergedBytesRead property.
    pub local_merged_bytes_read: Option<String>,
    /// localMergedChunksFetched property.
    pub local_merged_chunks_fetched: Option<String>,
    /// mergedFetchFallbackCount property.
    pub merged_fetch_fallback_count: Option<String>,
    /// remoteMergedBlocksFetched property.
    pub remote_merged_blocks_fetched: Option<String>,
    /// remoteMergedBytesRead property.
    pub remote_merged_bytes_read: Option<String>,
    /// remoteMergedChunksFetched property.
    pub remote_merged_chunks_fetched: Option<String>,
    /// remoteMergedReqsDuration property.
    pub remote_merged_reqs_duration: Option<String>,
}

/// `NativeBuildInfoUiData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NativeBuildInfoUiData {
    /// buildClass property.
    pub build_class: Option<String>,
    /// buildInfo property.
    pub build_info: Option<Vec<BuildInfo>>,
}

/// `ListAutoscalingPoliciesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListAutoscalingPoliciesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// policies property.
    pub policies: Option<Vec<AutoscalingPolicy>>,
}

/// `EndpointConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EndpointConfig {
    /// enableHttpPortAccess property.
    pub enable_http_port_access: Option<bool>,
    /// httpPorts property.
    pub http_ports: Option<serde_json::Value>,
}

/// `SparkPlanGraphNodeWrapper` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkPlanGraphNodeWrapper {
    /// cluster property.
    pub cluster: Option<Box<SparkPlanGraphCluster>>,
    /// node property.
    pub node: Option<SparkPlanGraphNode>,
}

/// `AccessSparkApplicationStageRddOperationGraphResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationStageRddOperationGraphResponse {
    /// rddOperationGraph property.
    pub rdd_operation_graph: Option<RddOperationGraph>,
}

/// `ListWorkflowTemplatesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListWorkflowTemplatesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// templates property.
    pub templates: Option<Vec<WorkflowTemplate>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `ListOperationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListOperationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<Operation>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `SparkPlanGraphNode` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkPlanGraphNode {
    /// desc property.
    pub desc: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// metrics property.
    pub metrics: Option<Vec<SqlPlanMetric>>,
    /// name property.
    pub name: Option<String>,
    /// sparkPlanGraphNodeId property.
    pub spark_plan_graph_node_id: Option<String>,
}

/// `AccessSessionSparkApplicationEnvironmentInfoResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSessionSparkApplicationEnvironmentInfoResponse {
    /// applicationEnvironmentInfo property.
    pub application_environment_info: Option<ApplicationEnvironmentInfo>,
}

/// `SummarizeSessionSparkApplicationExecutorsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SummarizeSessionSparkApplicationExecutorsResponse {
    /// activeExecutorSummary property.
    pub active_executor_summary: Option<ConsolidatedExecutorSummary>,
    /// applicationId property.
    pub application_id: Option<String>,
    /// deadExecutorSummary property.
    pub dead_executor_summary: Option<ConsolidatedExecutorSummary>,
    /// totalExecutorSummary property.
    pub total_executor_summary: Option<ConsolidatedExecutorSummary>,
}

/// `VirtualClusterConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VirtualClusterConfig {
    /// auxiliaryServicesConfig property.
    pub auxiliary_services_config: Option<AuxiliaryServicesConfig>,
    /// kubernetesClusterConfig property.
    pub kubernetes_cluster_config: Option<KubernetesClusterConfig>,
    /// stagingBucket property.
    pub staging_bucket: Option<String>,
}

/// `RddDataDistribution` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RddDataDistribution {
    /// address property.
    pub address: Option<String>,
    /// diskUsed property.
    pub disk_used: Option<String>,
    /// memoryRemaining property.
    pub memory_remaining: Option<String>,
    /// memoryUsed property.
    pub memory_used: Option<String>,
    /// offHeapMemoryRemaining property.
    pub off_heap_memory_remaining: Option<String>,
    /// offHeapMemoryUsed property.
    pub off_heap_memory_used: Option<String>,
    /// onHeapMemoryRemaining property.
    pub on_heap_memory_remaining: Option<String>,
    /// onHeapMemoryUsed property.
    pub on_heap_memory_used: Option<String>,
}

/// `SubmitJobRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SubmitJobRequest {
    /// job property.
    pub job: Option<Job>,
    /// requestId property.
    pub request_id: Option<String>,
}

/// `SearchSparkApplicationSqlQueriesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchSparkApplicationSqlQueriesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sparkApplicationSqlQueries property.
    pub spark_application_sql_queries: Option<Vec<SqlExecutionUiData>>,
}

/// `AccessSparkApplicationJobResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessSparkApplicationJobResponse {
    /// jobData property.
    pub job_data: Option<JobData>,
}

/// `AcceleratorConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AcceleratorConfig {
    /// acceleratorCount property.
    pub accelerator_count: Option<i64>,
    /// acceleratorTypeUri property.
    pub accelerator_type_uri: Option<String>,
}

/// `KerberosConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct KerberosConfig {
    /// crossRealmTrustAdminServer property.
    pub cross_realm_trust_admin_server: Option<String>,
    /// crossRealmTrustKdc property.
    pub cross_realm_trust_kdc: Option<String>,
    /// crossRealmTrustRealm property.
    pub cross_realm_trust_realm: Option<String>,
    /// crossRealmTrustSharedPasswordUri property.
    pub cross_realm_trust_shared_password_uri: Option<String>,
    /// enableKerberos property.
    pub enable_kerberos: Option<bool>,
    /// kdcDbKeyUri property.
    pub kdc_db_key_uri: Option<String>,
    /// keyPasswordUri property.
    pub key_password_uri: Option<String>,
    /// keystorePasswordUri property.
    pub keystore_password_uri: Option<String>,
    /// keystoreUri property.
    pub keystore_uri: Option<String>,
    /// kmsKeyUri property.
    pub kms_key_uri: Option<String>,
    /// realm property.
    pub realm: Option<String>,
    /// rootPrincipalPasswordUri property.
    pub root_principal_password_uri: Option<String>,
    /// tgtLifetimeHours property.
    pub tgt_lifetime_hours: Option<i64>,
    /// truststorePasswordUri property.
    pub truststore_password_uri: Option<String>,
    /// truststoreUri property.
    pub truststore_uri: Option<String>,
}

/// `SparkConnectSessionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SparkConnectSessionInfo {
    /// finishTimestamp property.
    pub finish_timestamp: Option<String>,
    /// sessionId property.
    pub session_id: Option<String>,
    /// startTimestamp property.
    pub start_timestamp: Option<String>,
    /// totalExecution property.
    pub total_execution: Option<String>,
    /// userId property.
    pub user_id: Option<String>,
}

/// `CohortInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CohortInfo {
    /// cohort property.
    pub cohort: Option<String>,
    /// cohortSource property.
    pub cohort_source: Option<String>,
}

/// `HiveJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HiveJob {
    /// continueOnFailure property.
    pub continue_on_failure: Option<bool>,
    /// jarFileUris property.
    pub jar_file_uris: Option<Vec<String>>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
    /// queryFileUri property.
    pub query_file_uri: Option<String>,
    /// queryList property.
    pub query_list: Option<QueryList>,
    /// scriptVariables property.
    pub script_variables: Option<serde_json::Value>,
}

/// `TemplateParameter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TemplateParameter {
    /// description property.
    pub description: Option<String>,
    /// fields property.
    pub fields: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// validation property.
    pub validation: Option<ParameterValidation>,
}

/// `ExecutorSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecutorSummary {
    /// activeTasks property.
    pub active_tasks: Option<i64>,
    /// addTime property.
    pub add_time: Option<String>,
    /// attributes property.
    pub attributes: Option<serde_json::Value>,
    /// completedTasks property.
    pub completed_tasks: Option<i64>,
    /// diskUsed property.
    pub disk_used: Option<String>,
    /// excludedInStages property.
    pub excluded_in_stages: Option<Vec<String>>,
    /// executorId property.
    pub executor_id: Option<String>,
    /// executorLogs property.
    pub executor_logs: Option<serde_json::Value>,
    /// failedTasks property.
    pub failed_tasks: Option<i64>,
    /// hostPort property.
    pub host_port: Option<String>,
    /// isActive property.
    pub is_active: Option<bool>,
    /// isExcluded property.
    pub is_excluded: Option<bool>,
    /// maxMemory property.
    pub max_memory: Option<String>,
    /// maxTasks property.
    pub max_tasks: Option<i64>,
    /// memoryMetrics property.
    pub memory_metrics: Option<MemoryMetrics>,
    /// memoryUsed property.
    pub memory_used: Option<String>,
    /// peakMemoryMetrics property.
    pub peak_memory_metrics: Option<ExecutorMetrics>,
    /// rddBlocks property.
    pub rdd_blocks: Option<i64>,
    /// removeReason property.
    pub remove_reason: Option<String>,
    /// removeTime property.
    pub remove_time: Option<String>,
    /// resourceProfileId property.
    pub resource_profile_id: Option<i64>,
    /// resources property.
    pub resources: Option<serde_json::Value>,
    /// totalCores property.
    pub total_cores: Option<i64>,
    /// totalDurationMillis property.
    pub total_duration_millis: Option<String>,
    /// totalGcTimeMillis property.
    pub total_gc_time_millis: Option<String>,
    /// totalInputBytes property.
    pub total_input_bytes: Option<String>,
    /// totalShuffleRead property.
    pub total_shuffle_read: Option<String>,
    /// totalShuffleWrite property.
    pub total_shuffle_write: Option<String>,
    /// totalTasks property.
    pub total_tasks: Option<i64>,
}

/// `GceClusterConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GceClusterConfig {
    /// autoZoneExcludeZoneUris property.
    pub auto_zone_exclude_zone_uris: Option<Vec<String>>,
    /// confidentialInstanceConfig property.
    pub confidential_instance_config: Option<ConfidentialInstanceConfig>,
    /// internalIpOnly property.
    pub internal_ip_only: Option<bool>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// networkUri property.
    pub network_uri: Option<String>,
    /// nodeGroupAffinity property.
    pub node_group_affinity: Option<NodeGroupAffinity>,
    /// privateIpv6GoogleAccess property.
    pub private_ipv6_google_access: Option<String>,
    /// reservationAffinity property.
    pub reservation_affinity: Option<ReservationAffinity>,
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<serde_json::Value>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// serviceAccountScopes property.
    pub service_account_scopes: Option<Vec<String>>,
    /// shieldedInstanceConfig property.
    pub shielded_instance_config: Option<ShieldedInstanceConfig>,
    /// subnetworkUri property.
    pub subnetwork_uri: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// zoneUri property.
    pub zone_uri: Option<String>,
}

/// `PySparkBatch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PySparkBatch {
    /// archiveUris property.
    pub archive_uris: Option<Vec<String>>,
    /// args property.
    pub args: Option<Vec<String>>,
    /// fileUris property.
    pub file_uris: Option<Vec<String>>,
    /// jarFileUris property.
    pub jar_file_uris: Option<Vec<String>>,
    /// mainPythonFileUri property.
    pub main_python_file_uri: Option<String>,
    /// pythonFileUris property.
    pub python_file_uris: Option<Vec<String>>,
}

/// `CancelJobRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelJobRequest {}

/// `StreamBlockData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamBlockData {
    /// deserialized property.
    pub deserialized: Option<bool>,
    /// diskSize property.
    pub disk_size: Option<String>,
    /// executorId property.
    pub executor_id: Option<String>,
    /// hostPort property.
    pub host_port: Option<String>,
    /// memSize property.
    pub mem_size: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// storageLevel property.
    pub storage_level: Option<String>,
    /// useDisk property.
    pub use_disk: Option<bool>,
    /// useMemory property.
    pub use_memory: Option<bool>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dataproc.projects.locations.autoscalingPolicies.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: AutoscalingPolicy,
}

/// Arguments for [`dataproc.projects.locations.autoscalingPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.locations.autoscalingPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.locations.autoscalingPolicies.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.locations.autoscalingPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.autoscalingPolicies.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.locations.autoscalingPolicies.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`dataproc.projects.locations.autoscalingPolicies.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsAutoscalingPoliciesUpdateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: AutoscalingPolicy,
}

/// Arguments for [`dataproc.projects.locations.batches.analyze_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesAnalyzeArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: AnalyzeBatchRequest,
}

/// Arguments for [`dataproc.projects.locations.batches.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `batchId`.
    pub batch_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Batch,
}

/// Arguments for [`dataproc.projects.locations.batches.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.locations.batches.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.locations.batches.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.access_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessEnvironmentInfo_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessEnvironmentInfoArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessJob_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessJobArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `jobId`.
    pub job_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessSqlPlan_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessSqlPlanArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executionId`.
    pub execution_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessSqlQuery_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessSqlQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `details`.
    pub details: Option<String>,
    /// Query parameter: `executionId`.
    pub execution_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `planDescription`.
    pub plan_description: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessStageAttempt_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessStageAttemptArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.accessStageRddGraph_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsAccessStageRddGraphArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `applicationStatus`.
    pub application_status: Option<String>,
    /// Query parameter: `maxEndTime`.
    pub max_end_time: Option<String>,
    /// Query parameter: `maxTime`.
    pub max_time: Option<String>,
    /// Query parameter: `minEndTime`.
    pub min_end_time: Option<String>,
    /// Query parameter: `minTime`.
    pub min_time: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchExecutorStageSummary_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchExecutorStageSummaryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchExecutors_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchExecutorsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executorStatus`.
    pub executor_status: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchJobs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchJobsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `jobStatus`.
    pub job_status: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchSqlQueries_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchSqlQueriesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `details`.
    pub details: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `planDescription`.
    pub plan_description: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchStageAttemptTasks_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchStageAttemptTasksArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `sortRuntime`.
    pub sort_runtime: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `taskStatus`.
    pub task_status: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchStageAttempts_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchStageAttemptsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.searchStages_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSearchStagesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageStatus`.
    pub stage_status: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.summarizeExecutors_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeExecutorsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.summarizeJobs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeJobsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.summarizeStageAttemptTasks_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeStageAttemptTasksArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.summarizeStages_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsSummarizeStagesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.batches.sparkApplications.write_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsBatchesSparkApplicationsWriteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: WriteSparkApplicationContextRequest,
}

/// Arguments for [`dataproc.projects.locations.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsOperationsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.locations.operations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.locations.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.locations.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessionTemplates.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionTemplatesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: SessionTemplate,
}

/// Arguments for [`dataproc.projects.locations.sessionTemplates.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionTemplatesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.locations.sessionTemplates.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionTemplatesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.locations.sessionTemplates.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionTemplatesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessionTemplates.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionTemplatesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SessionTemplate,
}

/// Arguments for [`dataproc.projects.locations.sessions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `sessionId`.
    pub session_id: Option<String>,
    /// Request body.
    pub body: Session,
}

/// Arguments for [`dataproc.projects.locations.sessions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.locations.sessions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.terminate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsTerminateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: TerminateSessionRequest,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.access_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessEnvironmentInfo_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessEnvironmentInfoArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessJob_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessJobArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `jobId`.
    pub job_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessSqlPlan_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessSqlPlanArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executionId`.
    pub execution_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessSqlQuery_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessSqlQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `details`.
    pub details: Option<String>,
    /// Query parameter: `executionId`.
    pub execution_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `planDescription`.
    pub plan_description: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessStageAttempt_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessStageAttemptArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.accessStageRddGraph_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsAccessStageRddGraphArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `applicationStatus`.
    pub application_status: Option<String>,
    /// Query parameter: `maxEndTime`.
    pub max_end_time: Option<String>,
    /// Query parameter: `maxTime`.
    pub max_time: Option<String>,
    /// Query parameter: `minEndTime`.
    pub min_end_time: Option<String>,
    /// Query parameter: `minTime`.
    pub min_time: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchExecutorStageSummary_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchExecutorStageSummaryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchExecutors_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchExecutorsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executorStatus`.
    pub executor_status: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchJobs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchJobsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `jobIds`.
    pub job_ids: Option<String>,
    /// Query parameter: `jobStatus`.
    pub job_status: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchSqlQueries_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchSqlQueriesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `details`.
    pub details: Option<String>,
    /// Query parameter: `operationIds`.
    pub operation_ids: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `planDescription`.
    pub plan_description: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchStageAttemptTasks_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchStageAttemptTasksArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `sortRuntime`.
    pub sort_runtime: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `taskStatus`.
    pub task_status: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchStageAttempts_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchStageAttemptsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.searchStages_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSearchStagesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageIds`.
    pub stage_ids: Option<String>,
    /// Query parameter: `stageStatus`.
    pub stage_status: Option<String>,
    /// Query parameter: `summaryMetricsMask`.
    pub summary_metrics_mask: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.summarizeExecutors_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeExecutorsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.summarizeJobs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeJobsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `jobIds`.
    pub job_ids: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.summarizeStageAttemptTasks_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeStageAttemptTasksArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageAttemptId`.
    pub stage_attempt_id: Option<String>,
    /// Query parameter: `stageId`.
    pub stage_id: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.summarizeStages_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsSummarizeStagesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
    /// Query parameter: `stageIds`.
    pub stage_ids: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.sessions.sparkApplications.write_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsSessionsSparkApplicationsWriteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: WriteSessionSparkApplicationContextRequest,
}

/// Arguments for [`dataproc.projects.locations.workflowTemplates.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: WorkflowTemplate,
}

/// Arguments for [`dataproc.projects.locations.workflowTemplates.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.workflowTemplates.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.workflowTemplates.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.locations.workflowTemplates.instantiate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesInstantiateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: InstantiateWorkflowTemplateRequest,
}

/// Arguments for [`dataproc.projects.locations.workflowTemplates.instantiateInline_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesInstantiateInlineArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: WorkflowTemplate,
}

/// Arguments for [`dataproc.projects.locations.workflowTemplates.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.locations.workflowTemplates.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.locations.workflowTemplates.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`dataproc.projects.locations.workflowTemplates.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsLocationsWorkflowTemplatesUpdateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: WorkflowTemplate,
}

/// Arguments for [`dataproc.projects.regions.autoscalingPolicies.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: AutoscalingPolicy,
}

/// Arguments for [`dataproc.projects.regions.autoscalingPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.regions.autoscalingPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.regions.autoscalingPolicies.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.regions.autoscalingPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.autoscalingPolicies.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.regions.autoscalingPolicies.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`dataproc.projects.regions.autoscalingPolicies.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsAutoscalingPoliciesUpdateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: AutoscalingPolicy,
}

/// Arguments for [`dataproc.projects.regions.clusters.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `actionOnFailedPrimaryWorkers`.
    pub action_on_failed_primary_workers: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Cluster,
}

/// Arguments for [`dataproc.projects.regions.clusters.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersDeleteArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
    /// Query parameter: `clusterUuid`.
    pub cluster_uuid: Option<String>,
    /// Query parameter: `gracefulTerminationTimeout`.
    pub graceful_termination_timeout: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.clusters.diagnose_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersDiagnoseArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
    /// Request body.
    pub body: DiagnoseClusterRequest,
}

/// Arguments for [`dataproc.projects.regions.clusters.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
}

/// Arguments for [`dataproc.projects.regions.clusters.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.regions.clusters.injectCredentials_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersInjectCredentialsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `cluster`.
    pub cluster: String,
    /// Request body.
    pub body: InjectCredentialsRequest,
}

/// Arguments for [`dataproc.projects.regions.clusters.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.clusters.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersPatchArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
    /// Query parameter: `gracefulDecommissionTimeout`.
    pub graceful_decommission_timeout: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Cluster,
}

/// Arguments for [`dataproc.projects.regions.clusters.repair_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersRepairArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
    /// Request body.
    pub body: RepairClusterRequest,
}

/// Arguments for [`dataproc.projects.regions.clusters.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.regions.clusters.start_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersStartArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
    /// Request body.
    pub body: StartClusterRequest,
}

/// Arguments for [`dataproc.projects.regions.clusters.stop_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersStopArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `clusterName`.
    pub cluster_name: String,
    /// Request body.
    pub body: StopClusterRequest,
}

/// Arguments for [`dataproc.projects.regions.clusters.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`dataproc.projects.regions.clusters.nodeGroups.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersNodeGroupsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `nodeGroupId`.
    pub node_group_id: Option<String>,
    /// Query parameter: `parentOperationId`.
    pub parent_operation_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: NodeGroup,
}

/// Arguments for [`dataproc.projects.regions.clusters.nodeGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersNodeGroupsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.regions.clusters.nodeGroups.repair_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersNodeGroupsRepairArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: RepairNodeGroupRequest,
}

/// Arguments for [`dataproc.projects.regions.clusters.nodeGroups.resize_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsClustersNodeGroupsResizeArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: ResizeNodeGroupRequest,
}

/// Arguments for [`dataproc.projects.regions.jobs.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsCancelArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: CancelJobRequest,
}

/// Arguments for [`dataproc.projects.regions.jobs.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsDeleteArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
}

/// Arguments for [`dataproc.projects.regions.jobs.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
}

/// Arguments for [`dataproc.projects.regions.jobs.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.regions.jobs.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Query parameter: `clusterName`.
    pub cluster_name: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `jobStateMatcher`.
    pub job_state_matcher: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.jobs.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsPatchArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Job,
}

/// Arguments for [`dataproc.projects.regions.jobs.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.regions.jobs.submit_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsSubmitArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Request body.
    pub body: SubmitJobRequest,
}

/// Arguments for [`dataproc.projects.regions.jobs.submitAsOperation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsSubmitAsOperationArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Request body.
    pub body: SubmitJobRequest,
}

/// Arguments for [`dataproc.projects.regions.jobs.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsJobsTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`dataproc.projects.regions.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.regions.operations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.regions.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataproc.projects.regions.operations.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.regions.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.operations.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.regions.operations.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsOperationsTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`dataproc.projects.regions.workflowTemplates.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: WorkflowTemplate,
}

/// Arguments for [`dataproc.projects.regions.workflowTemplates.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.workflowTemplates.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.workflowTemplates.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.regions.workflowTemplates.instantiate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesInstantiateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: InstantiateWorkflowTemplateRequest,
}

/// Arguments for [`dataproc.projects.regions.workflowTemplates.instantiateInline_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesInstantiateInlineArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: WorkflowTemplate,
}

/// Arguments for [`dataproc.projects.regions.workflowTemplates.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataproc.projects.regions.workflowTemplates.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`dataproc.projects.regions.workflowTemplates.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`dataproc.projects.regions.workflowTemplates.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataprocProjectsRegionsWorkflowTemplatesUpdateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: WorkflowTemplate,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/{+parent}/autoscalingPolicies
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/autoscalingPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_autoscaling_policies_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_autoscaling_policies_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsAutoscalingPoliciesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutoscalingPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/autoscalingPolicies",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_autoscaling_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_autoscaling_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsAutoscalingPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_autoscaling_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_autoscaling_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsAutoscalingPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutoscalingPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_autoscaling_policies_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_autoscaling_policies_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsAutoscalingPoliciesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/autoscalingPolicies
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/autoscalingPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_autoscaling_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_autoscaling_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsAutoscalingPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAutoscalingPoliciesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/autoscalingPolicies",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListAutoscalingPoliciesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_autoscaling_policies_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_autoscaling_policies_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsAutoscalingPoliciesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_autoscaling_policies_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_autoscaling_policies_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsAutoscalingPoliciesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/{+name}
// -----------------------------------------------------------------------------

/// PUT v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_autoscaling_policies_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_autoscaling_policies_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsAutoscalingPoliciesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutoscalingPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:analyze
// -----------------------------------------------------------------------------

/// POST v1/{+name}:analyze.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_analyze_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_analyze_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesAnalyzeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:analyze", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/batches
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/batches.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}/batches", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.batch_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("batchId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Batch>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Batch =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/batches
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/batches.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBatchesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}/batches", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBatchesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:access
// -----------------------------------------------------------------------------

/// GET v1/{+name}:access.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccessSparkApplicationResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:access", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessEnvironmentInfo
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessEnvironmentInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_environment_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_environment_info_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessEnvironmentInfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSparkApplicationEnvironmentInfoResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:accessEnvironmentInfo",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationEnvironmentInfoResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessJob
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessJob.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_job_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_job_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessJobArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccessSparkApplicationJobResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:accessJob", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.job_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("jobId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationJobResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessSqlPlan
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessSqlPlan.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_sql_plan_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_sql_plan_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessSqlPlanArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSparkApplicationSqlSparkPlanGraphResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:accessSqlPlan",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationSqlSparkPlanGraphResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessSqlQuery
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessSqlQuery.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_sql_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_sql_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessSqlQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSparkApplicationSqlQueryResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:accessSqlQuery",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.details {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("details=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.execution_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.plan_description {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("planDescription=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationSqlQueryResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessStageAttempt
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessStageAttempt.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_stage_attempt_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_stage_attempt_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessStageAttemptArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSparkApplicationStageAttemptResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:accessStageAttempt",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_attempt_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageAttemptId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.summary_metrics_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("summaryMetricsMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationStageAttemptResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessStageRddGraph
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessStageRddGraph.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_access_stage_rdd_graph_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsAccessStageRddGraphArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSparkApplicationStageRddOperationGraphResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:accessStageRddGraph",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSparkApplicationStageRddOperationGraphResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/sparkApplications:search
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/sparkApplications:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchSparkApplicationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/sparkApplications:search",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.application_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("applicationStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_end_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxEndTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.min_end_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minEndTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.min_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchExecutorStageSummary
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchExecutorStageSummary.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_executor_stage_summary_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchExecutorStageSummaryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationExecutorStageSummaryResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchExecutorStageSummary",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_attempt_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageAttemptId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationExecutorStageSummaryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchExecutors
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchExecutors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_executors_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_executors_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchExecutorsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationExecutorsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchExecutors",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.executor_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executorStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationExecutorsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchJobs
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_jobs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_jobs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchJobsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationJobsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchJobs",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.job_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("jobStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchSqlQueries
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchSqlQueries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_sql_queries_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_sql_queries_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchSqlQueriesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationSqlQueriesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchSqlQueries",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.details {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("details=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.plan_description {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("planDescription=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationSqlQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchStageAttemptTasks
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchStageAttemptTasks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempt_tasks_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchStageAttemptTasksArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationStageAttemptTasksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchStageAttemptTasks",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_runtime {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sortRuntime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_attempt_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageAttemptId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.task_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("taskStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchStageAttempts
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchStageAttempts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_stage_attempts_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_stage_attempts_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchStageAttemptsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationStageAttemptsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchStageAttempts",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.summary_metrics_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("summaryMetricsMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStageAttemptsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchStages
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchStages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_search_stages_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_search_stages_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSearchStagesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSparkApplicationStagesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchStages",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.summary_metrics_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("summaryMetricsMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSparkApplicationStagesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:summarizeExecutors
// -----------------------------------------------------------------------------

/// GET v1/{+name}:summarizeExecutors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_summarize_executors_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_summarize_executors_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSummarizeExecutorsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSparkApplicationExecutorsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:summarizeExecutors",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:summarizeJobs
// -----------------------------------------------------------------------------

/// GET v1/{+name}:summarizeJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_summarize_jobs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_summarize_jobs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSummarizeJobsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSparkApplicationJobsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:summarizeJobs",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:summarizeStageAttemptTasks
// -----------------------------------------------------------------------------

/// GET v1/{+name}:summarizeStageAttemptTasks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_summarize_stage_attempt_tasks_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSummarizeStageAttemptTasksArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSparkApplicationStageAttemptTasksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:summarizeStageAttemptTasks",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_attempt_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageAttemptId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:summarizeStages
// -----------------------------------------------------------------------------

/// GET v1/{+name}:summarizeStages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_summarize_stages_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_summarize_stages_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsSummarizeStagesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSparkApplicationStagesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:summarizeStages",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSparkApplicationStagesResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:write
// -----------------------------------------------------------------------------

/// POST v1/{+name}:write.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_batches_spark_applications_write_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_batches_spark_applications_write_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsBatchesSparkApplicationsWriteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<WriteSparkApplicationContextResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:write", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WriteSparkApplicationContextResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:cancel", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_operations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_operations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsOperationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOperationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/sessionTemplates
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/sessionTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_session_templates_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_session_templates_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionTemplatesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SessionTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/sessionTemplates",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SessionTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_session_templates_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_session_templates_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionTemplatesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_session_templates_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_session_templates_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionTemplatesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SessionTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SessionTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/sessionTemplates
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/sessionTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_session_templates_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_session_templates_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionTemplatesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSessionTemplatesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/sessionTemplates",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListSessionTemplatesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_session_templates_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_session_templates_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionTemplatesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SessionTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SessionTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/sessions
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/sessions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/sessions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.session_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sessionId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Session>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Session =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/sessions
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/sessions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSessionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/sessions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListSessionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:terminate
// -----------------------------------------------------------------------------

/// POST v1/{+name}:terminate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_terminate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_terminate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsTerminateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:terminate", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:access
// -----------------------------------------------------------------------------

/// GET v1/{+name}:access.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:access", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessEnvironmentInfo
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessEnvironmentInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_environment_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_environment_info_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessEnvironmentInfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationEnvironmentInfoResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:accessEnvironmentInfo",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationEnvironmentInfoResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessJob
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessJob.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_job_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_job_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessJobArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationJobResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:accessJob", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.job_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("jobId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationJobResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessSqlPlan
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessSqlPlan.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_sql_plan_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_plan_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessSqlPlanArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationSqlSparkPlanGraphResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:accessSqlPlan",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationSqlSparkPlanGraphResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessSqlQuery
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessSqlQuery.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_sql_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_sql_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessSqlQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationSqlQueryResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:accessSqlQuery",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.details {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("details=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.execution_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.plan_description {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("planDescription=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationSqlQueryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessStageAttempt
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessStageAttempt.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_attempt_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessStageAttemptArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationStageAttemptResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:accessStageAttempt",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_attempt_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageAttemptId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.summary_metrics_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("summaryMetricsMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationStageAttemptResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:accessStageRddGraph
// -----------------------------------------------------------------------------

/// GET v1/{+name}:accessStageRddGraph.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_access_stage_rdd_graph_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsAccessStageRddGraphArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<AccessSessionSparkApplicationStageRddOperationGraphResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:accessStageRddGraph",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessSessionSparkApplicationStageRddOperationGraphResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/sparkApplications:search
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/sparkApplications:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/sparkApplications:search",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.application_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("applicationStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_end_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxEndTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.min_end_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minEndTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.min_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchExecutorStageSummary
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchExecutorStageSummary.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_executor_stage_summary_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchExecutorStageSummaryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationExecutorStageSummaryResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchExecutorStageSummary",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_attempt_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageAttemptId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationExecutorStageSummaryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchExecutors
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchExecutors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_executors_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_executors_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchExecutorsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationExecutorsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchExecutors",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.executor_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executorStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchJobs
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_jobs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_jobs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchJobsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationJobsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchJobs",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.job_ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("jobIds=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.job_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("jobStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationJobsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchSqlQueries
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchSqlQueries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_sql_queries_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_sql_queries_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchSqlQueriesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationSqlQueriesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchSqlQueries",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.details {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("details=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.operation_ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("operationIds=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.plan_description {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("planDescription=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationSqlQueriesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchStageAttemptTasks
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchStageAttemptTasks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempt_tasks_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchStageAttemptTasksArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationStageAttemptTasksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchStageAttemptTasks",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_runtime {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sortRuntime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_attempt_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageAttemptId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.task_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("taskStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchStageAttempts
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchStageAttempts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_stage_attempts_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchStageAttemptsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationStageAttemptsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchStageAttempts",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.summary_metrics_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("summaryMetricsMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStageAttemptsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:searchStages
// -----------------------------------------------------------------------------

/// GET v1/{+name}:searchStages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_search_stages_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_search_stages_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSearchStagesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SearchSessionSparkApplicationStagesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:searchStages",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageIds=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_status {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageStatus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.summary_metrics_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("summaryMetricsMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchSessionSparkApplicationStagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:summarizeExecutors
// -----------------------------------------------------------------------------

/// GET v1/{+name}:summarizeExecutors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_summarize_executors_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_summarize_executors_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSummarizeExecutorsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSessionSparkApplicationExecutorsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:summarizeExecutors",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationExecutorsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:summarizeJobs
// -----------------------------------------------------------------------------

/// GET v1/{+name}:summarizeJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_summarize_jobs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_summarize_jobs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSummarizeJobsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSessionSparkApplicationJobsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:summarizeJobs",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.job_ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("jobIds=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:summarizeStageAttemptTasks
// -----------------------------------------------------------------------------

/// GET v1/{+name}:summarizeStageAttemptTasks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stage_attempt_tasks_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSummarizeStageAttemptTasksArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSessionSparkApplicationStageAttemptTasksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:summarizeStageAttemptTasks",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_attempt_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageAttemptId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationStageAttemptTasksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:summarizeStages
// -----------------------------------------------------------------------------

/// GET v1/{+name}:summarizeStages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_summarize_stages_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_summarize_stages_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsSummarizeStagesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<SummarizeSessionSparkApplicationStagesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:summarizeStages",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stage_ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("stageIds=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SummarizeSessionSparkApplicationStagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:write
// -----------------------------------------------------------------------------

/// POST v1/{+name}:write.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_sessions_spark_applications_write_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_sessions_spark_applications_write_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsSessionsSparkApplicationsWriteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<WriteSessionSparkApplicationContextResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:write", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WriteSessionSparkApplicationContextResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/workflowTemplates
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/workflowTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_workflow_templates_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_workflow_templates_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsWorkflowTemplatesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkflowTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/workflowTemplates",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WorkflowTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_workflow_templates_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_workflow_templates_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsWorkflowTemplatesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_workflow_templates_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_workflow_templates_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsWorkflowTemplatesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkflowTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WorkflowTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_workflow_templates_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_workflow_templates_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsWorkflowTemplatesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:instantiate
// -----------------------------------------------------------------------------

/// POST v1/{+name}:instantiate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_workflow_templates_instantiate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_workflow_templates_instantiate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsWorkflowTemplatesInstantiateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:instantiate",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/workflowTemplates:instantiateInline
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/workflowTemplates:instantiateInline.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_workflow_templates_instantiate_inline_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_workflow_templates_instantiate_inline_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsWorkflowTemplatesInstantiateInlineArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/workflowTemplates:instantiateInline",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/workflowTemplates
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/workflowTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_workflow_templates_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_workflow_templates_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsWorkflowTemplatesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListWorkflowTemplatesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/workflowTemplates",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListWorkflowTemplatesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_workflow_templates_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_workflow_templates_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsWorkflowTemplatesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_workflow_templates_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_workflow_templates_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsWorkflowTemplatesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/{+name}
// -----------------------------------------------------------------------------

/// PUT v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_locations_workflow_templates_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_locations_workflow_templates_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsLocationsWorkflowTemplatesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkflowTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WorkflowTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/autoscalingPolicies
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/autoscalingPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_autoscaling_policies_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_autoscaling_policies_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsAutoscalingPoliciesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutoscalingPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/autoscalingPolicies",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_autoscaling_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_autoscaling_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsAutoscalingPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_autoscaling_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_autoscaling_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsAutoscalingPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutoscalingPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_autoscaling_policies_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_autoscaling_policies_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsAutoscalingPoliciesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/autoscalingPolicies
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/autoscalingPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_autoscaling_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_autoscaling_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsAutoscalingPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAutoscalingPoliciesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/autoscalingPolicies",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListAutoscalingPoliciesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_autoscaling_policies_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_autoscaling_policies_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsAutoscalingPoliciesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_autoscaling_policies_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_autoscaling_policies_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsAutoscalingPoliciesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/{+name}
// -----------------------------------------------------------------------------

/// PUT v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_autoscaling_policies_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_autoscaling_policies_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsAutoscalingPoliciesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutoscalingPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AutoscalingPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/clusters
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/clusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters",
        args.project_id, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.action_on_failed_primary_workers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("actionOnFailedPrimaryWorkers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectId}/regions/{region}/clusters/{clusterName}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}",
        args.project_id, args.region, args.cluster_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cluster_uuid {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("clusterUuid=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.graceful_termination_timeout {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("gracefulTerminationTimeout=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:diagnose
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:diagnose.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_diagnose_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_diagnose_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersDiagnoseArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:diagnose",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/regions/{region}/clusters/{clusterName}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Cluster>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Cluster =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+project}/{+region}/{+cluster}:injectCredentials
// -----------------------------------------------------------------------------

/// POST v1/{+project}/{+region}/{+cluster}:injectCredentials.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_inject_credentials_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_inject_credentials_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersInjectCredentialsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/{}/{}:injectCredentials",
        args.project, args.region, args.cluster,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/regions/{region}/clusters
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/regions/{region}/clusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClustersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters",
        args.project_id, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListClustersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectId}/regions/{region}/clusters/{clusterName}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectId}/regions/{region}/clusters/{clusterName}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}",
        args.project_id, args.region, args.cluster_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.graceful_decommission_timeout {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("gracefulDecommissionTimeout=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:repair
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:repair.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_repair_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_repair_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersRepairArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:repair",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:start
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:start.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_start_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_start_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersStartArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:start",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:stop
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/clusters/{clusterName}:stop.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_stop_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_stop_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersStopArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/clusters/{}:stop",
        args.project_id, args.region, args.cluster_name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/nodeGroups
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/nodeGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_node_groups_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_node_groups_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersNodeGroupsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/nodeGroups",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.node_group_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("nodeGroupId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent_operation_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parentOperationId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_node_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_node_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersNodeGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodeGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodeGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:repair
// -----------------------------------------------------------------------------

/// POST v1/{+name}:repair.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_node_groups_repair_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_node_groups_repair_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersNodeGroupsRepairArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:repair", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:resize
// -----------------------------------------------------------------------------

/// POST v1/{+name}:resize.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_clusters_node_groups_resize_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_clusters_node_groups_resize_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsClustersNodeGroupsResizeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:resize", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/jobs/{jobId}:cancel
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/jobs/{jobId}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_jobs_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_jobs_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsJobsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs/{}:cancel",
        args.project_id, args.region, args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectId}/regions/{region}/jobs/{jobId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectId}/regions/{region}/jobs/{jobId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_jobs_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_jobs_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsJobsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs/{}",
        args.project_id, args.region, args.job_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/regions/{region}/jobs/{jobId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_jobs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_jobs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsJobsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs/{}",
        args.project_id, args.region, args.job_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_jobs_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_jobs_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsJobsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/regions/{region}/jobs
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/regions/{region}/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_jobs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_jobs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsJobsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs",
        args.project_id, args.region,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cluster_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("clusterName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.job_state_matcher {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("jobStateMatcher=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectId}/regions/{region}/jobs/{jobId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectId}/regions/{region}/jobs/{jobId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_jobs_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_jobs_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsJobsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs/{}",
        args.project_id, args.region, args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_jobs_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_jobs_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsJobsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/jobs:submit
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/jobs:submit.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_jobs_submit_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_jobs_submit_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsJobsSubmitArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs:submit",
        args.project_id, args.region,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/regions/{region}/jobs:submitAsOperation
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/regions/{region}/jobs:submitAsOperation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_jobs_submit_as_operation_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_jobs_submit_as_operation_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsJobsSubmitAsOperationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/projects/{}/regions/{}/jobs:submitAsOperation",
        args.project_id, args.region,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_jobs_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_jobs_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsJobsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}:cancel", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_operations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_operations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsOperationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_operations_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_operations_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsOperationsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOperationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_operations_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_operations_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsOperationsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_operations_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_operations_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsOperationsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/workflowTemplates
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/workflowTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_workflow_templates_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_workflow_templates_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsWorkflowTemplatesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkflowTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/workflowTemplates",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WorkflowTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_workflow_templates_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_workflow_templates_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsWorkflowTemplatesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_workflow_templates_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_workflow_templates_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsWorkflowTemplatesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkflowTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WorkflowTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_workflow_templates_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_workflow_templates_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsWorkflowTemplatesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:instantiate
// -----------------------------------------------------------------------------

/// POST v1/{+name}:instantiate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_workflow_templates_instantiate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_workflow_templates_instantiate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsWorkflowTemplatesInstantiateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:instantiate",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/workflowTemplates:instantiateInline
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/workflowTemplates:instantiateInline.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_workflow_templates_instantiate_inline_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_workflow_templates_instantiate_inline_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsWorkflowTemplatesInstantiateInlineArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/workflowTemplates:instantiateInline",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/workflowTemplates
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/workflowTemplates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_workflow_templates_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_workflow_templates_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsWorkflowTemplatesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListWorkflowTemplatesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}/workflowTemplates",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListWorkflowTemplatesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_workflow_templates_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_workflow_templates_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsWorkflowTemplatesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_workflow_templates_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_workflow_templates_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsWorkflowTemplatesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataproc.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/{+name}
// -----------------------------------------------------------------------------

/// PUT v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataproc_projects_regions_workflow_templates_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataproc_projects_regions_workflow_templates_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataprocProjectsRegionsWorkflowTemplatesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkflowTemplate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dataproc.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WorkflowTemplate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
