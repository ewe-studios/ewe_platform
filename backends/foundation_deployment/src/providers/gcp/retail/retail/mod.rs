//! Auto-generated API module for gcp/retail retail.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_retail_retail `

#![cfg(feature = "gcp_retail_retail")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleCloudRetailV2ModelModelFeaturesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ModelModelFeaturesConfig {
    /// frequentlyBoughtTogetherConfig property.
    pub frequently_bought_together_config:
        Option<GoogleCloudRetailV2ModelFrequentlyBoughtTogetherFeaturesConfig>,
}

/// `GoogleCloudRetailV2SearchRequestFacetSpecFacetKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestFacetSpecFacetKey {
    /// caseInsensitive property.
    pub case_insensitive: Option<bool>,
    /// contains property.
    pub contains: Option<Vec<String>>,
    /// intervals property.
    pub intervals: Option<Vec<GoogleCloudRetailV2Interval>>,
    /// key property.
    pub key: Option<String>,
    /// orderBy property.
    pub order_by: Option<String>,
    /// prefixes property.
    pub prefixes: Option<Vec<String>>,
    /// query property.
    pub query: Option<String>,
    /// restrictedValues property.
    pub restricted_values: Option<Vec<String>>,
    /// returnMinMax property.
    pub return_min_max: Option<bool>,
}

/// `GoogleCloudRetailV2CatalogAttributeFacetConfigMergedFacet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CatalogAttributeFacetConfigMergedFacet {
    /// mergedFacetKey property.
    pub merged_facet_key: Option<String>,
}

/// `GoogleCloudRetailV2Image` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Image {
    /// height property.
    pub height: Option<i64>,
    /// uri property.
    pub uri: Option<String>,
    /// width property.
    pub width: Option<i64>,
}

/// `GoogleCloudRetailV2PanelInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2PanelInfo {
    /// attributionToken property.
    pub attribution_token: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// panelId property.
    pub panel_id: Option<String>,
    /// panelPosition property.
    pub panel_position: Option<i64>,
    /// productDetails property.
    pub product_details: Option<Vec<GoogleCloudRetailV2ProductDetail>>,
    /// totalPanels property.
    pub total_panels: Option<i64>,
}

/// `GoogleCloudRetailV2ConversationalSearchRequestSearchParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchRequestSearchParams {
    /// boostSpec property.
    pub boost_spec: Option<GoogleCloudRetailV2SearchRequestBoostSpec>,
    /// canonicalFilter property.
    pub canonical_filter: Option<String>,
    /// filter property.
    pub filter: Option<String>,
    /// sortBy property.
    pub sort_by: Option<String>,
}

/// `GoogleCloudRetailV2ConversationalSearchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchResponse {
    /// conversationId property.
    pub conversation_id: Option<String>,
    /// conversationalFilteringResult property.
    pub conversational_filtering_result:
        Option<GoogleCloudRetailV2ConversationalSearchResponseConversationalFilteringResult>,
    /// conversationalTextResponse property.
    pub conversational_text_response: Option<String>,
    /// followupQuestion property.
    pub followup_question: Option<GoogleCloudRetailV2ConversationalSearchResponseFollowupQuestion>,
    /// refinedSearch property.
    pub refined_search: Option<Vec<GoogleCloudRetailV2ConversationalSearchResponseRefinedSearch>>,
    /// state property.
    pub state: Option<String>,
    /// userQueryTypes property.
    pub user_query_types: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2RemoveFulfillmentPlacesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RemoveFulfillmentPlacesRequest {
    /// allowMissing property.
    pub allow_missing: Option<bool>,
    /// placeIds property.
    pub place_ids: Option<Vec<String>>,
    /// removeTime property.
    pub remove_time: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleCloudRetailV2CompleteQueryResponseRecentSearchResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CompleteQueryResponseRecentSearchResult {
    /// recentSearch property.
    pub recent_search: Option<String>,
}

/// `GoogleCloudRetailV2SearchResponseConversationalSearchResultSuggestedAnswer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchResponseConversationalSearchResultSuggestedAnswer {
    /// productAttributeValue property.
    pub product_attribute_value: Option<GoogleCloudRetailV2ProductAttributeValue>,
}

/// `GoogleCloudRetailV2RuleBoostAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RuleBoostAction {
    /// boost property.
    pub boost: Option<f64>,
    /// productsFilter property.
    pub products_filter: Option<String>,
}

/// `GoogleCloudRetailV2CatalogAttributeFacetConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CatalogAttributeFacetConfig {
    /// facetIntervals property.
    pub facet_intervals: Option<Vec<GoogleCloudRetailV2Interval>>,
    /// ignoredFacetValues property.
    pub ignored_facet_values:
        Option<Vec<GoogleCloudRetailV2CatalogAttributeFacetConfigIgnoredFacetValues>>,
    /// mergedFacet property.
    pub merged_facet: Option<GoogleCloudRetailV2CatalogAttributeFacetConfigMergedFacet>,
    /// mergedFacetValues property.
    pub merged_facet_values:
        Option<Vec<GoogleCloudRetailV2CatalogAttributeFacetConfigMergedFacetValue>>,
    /// rerankConfig property.
    pub rerank_config: Option<GoogleCloudRetailV2CatalogAttributeFacetConfigRerankConfig>,
}

/// `GoogleCloudRetailV2ExperimentInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ExperimentInfo {
    /// experiment property.
    pub experiment: Option<String>,
    /// servingConfigExperiment property.
    pub serving_config_experiment: Option<GoogleCloudRetailV2ExperimentInfoServingConfigExperiment>,
}

/// `GoogleCloudRetailV2OutputConfigBigQueryDestination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2OutputConfigBigQueryDestination {
    /// datasetId property.
    pub dataset_id: Option<String>,
    /// tableIdPrefix property.
    pub table_id_prefix: Option<String>,
    /// tableType property.
    pub table_type: Option<String>,
}

/// `GoogleCloudRetailV2LocalInventory` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2LocalInventory {
    /// attributes property.
    pub attributes: Option<serde_json::Value>,
    /// fulfillmentTypes property.
    pub fulfillment_types: Option<Vec<String>>,
    /// placeId property.
    pub place_id: Option<String>,
    /// priceInfo property.
    pub price_info: Option<GoogleCloudRetailV2PriceInfo>,
}

/// `GoogleCloudRetailV2ConversationalSearchResponseRefinedSearch` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchResponseRefinedSearch {
    /// query property.
    pub query: Option<String>,
}

/// `GoogleCloudRetailV2ServingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ServingConfig {
    /// boostControlIds property.
    pub boost_control_ids: Option<Vec<String>>,
    /// displayName property.
    pub display_name: Option<String>,
    /// diversityLevel property.
    pub diversity_level: Option<String>,
    /// diversityType property.
    pub diversity_type: Option<String>,
    /// doNotAssociateControlIds property.
    pub do_not_associate_control_ids: Option<Vec<String>>,
    /// dynamicFacetSpec property.
    pub dynamic_facet_spec: Option<GoogleCloudRetailV2SearchRequestDynamicFacetSpec>,
    /// enableCategoryFilterLevel property.
    pub enable_category_filter_level: Option<String>,
    /// facetControlIds property.
    pub facet_control_ids: Option<Vec<String>>,
    /// filterControlIds property.
    pub filter_control_ids: Option<Vec<String>>,
    /// ignoreControlIds property.
    pub ignore_control_ids: Option<Vec<String>>,
    /// ignoreRecsDenylist property.
    pub ignore_recs_denylist: Option<bool>,
    /// modelId property.
    pub model_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// onewaySynonymsControlIds property.
    pub oneway_synonyms_control_ids: Option<Vec<String>>,
    /// personalizationSpec property.
    pub personalization_spec: Option<GoogleCloudRetailV2SearchRequestPersonalizationSpec>,
    /// priceRerankingLevel property.
    pub price_reranking_level: Option<String>,
    /// redirectControlIds property.
    pub redirect_control_ids: Option<Vec<String>>,
    /// replacementControlIds property.
    pub replacement_control_ids: Option<Vec<String>>,
    /// solutionTypes property.
    pub solution_types: Option<Vec<String>>,
    /// twowaySynonymsControlIds property.
    pub twoway_synonyms_control_ids: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2AddControlRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2AddControlRequest {
    /// controlId property.
    pub control_id: Option<String>,
}

/// `GoogleCloudRetailV2SearchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequest {
    /// boostSpec property.
    pub boost_spec: Option<GoogleCloudRetailV2SearchRequestBoostSpec>,
    /// branch property.
    pub branch: Option<String>,
    /// canonicalFilter property.
    pub canonical_filter: Option<String>,
    /// conversationalSearchSpec property.
    pub conversational_search_spec:
        Option<GoogleCloudRetailV2SearchRequestConversationalSearchSpec>,
    /// dynamicFacetSpec property.
    pub dynamic_facet_spec: Option<GoogleCloudRetailV2SearchRequestDynamicFacetSpec>,
    /// entity property.
    pub entity: Option<String>,
    /// facetSpecs property.
    pub facet_specs: Option<Vec<GoogleCloudRetailV2SearchRequestFacetSpec>>,
    /// filter property.
    pub filter: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// offset property.
    pub offset: Option<i64>,
    /// orderBy property.
    pub order_by: Option<String>,
    /// pageCategories property.
    pub page_categories: Option<Vec<String>>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// personalizationSpec property.
    pub personalization_spec: Option<GoogleCloudRetailV2SearchRequestPersonalizationSpec>,
    /// placeId property.
    pub place_id: Option<String>,
    /// query property.
    pub query: Option<String>,
    /// queryExpansionSpec property.
    pub query_expansion_spec: Option<GoogleCloudRetailV2SearchRequestQueryExpansionSpec>,
    /// regionCode property.
    pub region_code: Option<String>,
    /// searchMode property.
    pub search_mode: Option<String>,
    /// spellCorrectionSpec property.
    pub spell_correction_spec: Option<GoogleCloudRetailV2SearchRequestSpellCorrectionSpec>,
    /// tileNavigationSpec property.
    pub tile_navigation_spec: Option<GoogleCloudRetailV2SearchRequestTileNavigationSpec>,
    /// userAttributes property.
    pub user_attributes: Option<serde_json::Value>,
    /// userInfo property.
    pub user_info: Option<GoogleCloudRetailV2UserInfo>,
    /// variantRollupKeys property.
    pub variant_rollup_keys: Option<Vec<String>>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `GoogleCloudRetailV2RuleTwowaySynonymsAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RuleTwowaySynonymsAction {
    /// synonyms property.
    pub synonyms: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2ConditionQueryTerm` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConditionQueryTerm {
    /// fullMatch property.
    pub full_match: Option<bool>,
    /// value property.
    pub value: Option<String>,
}

/// `GoogleCloudRetailV2ExperimentInfoServingConfigExperiment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ExperimentInfoServingConfigExperiment {
    /// experimentServingConfig property.
    pub experiment_serving_config: Option<String>,
    /// originalServingConfig property.
    pub original_serving_config: Option<String>,
}

/// `GoogleCloudRetailV2Product` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Product {
    /// attributes property.
    pub attributes: Option<serde_json::Value>,
    /// audience property.
    pub audience: Option<GoogleCloudRetailV2Audience>,
    /// availability property.
    pub availability: Option<String>,
    /// availableQuantity property.
    pub available_quantity: Option<i64>,
    /// availableTime property.
    pub available_time: Option<String>,
    /// brands property.
    pub brands: Option<Vec<String>>,
    /// categories property.
    pub categories: Option<Vec<String>>,
    /// collectionMemberIds property.
    pub collection_member_ids: Option<Vec<String>>,
    /// colorInfo property.
    pub color_info: Option<GoogleCloudRetailV2ColorInfo>,
    /// conditions property.
    pub conditions: Option<Vec<String>>,
    /// description property.
    pub description: Option<String>,
    /// expireTime property.
    pub expire_time: Option<String>,
    /// fulfillmentInfo property.
    pub fulfillment_info: Option<Vec<GoogleCloudRetailV2FulfillmentInfo>>,
    /// gtin property.
    pub gtin: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// images property.
    pub images: Option<Vec<GoogleCloudRetailV2Image>>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// localInventories property.
    pub local_inventories: Option<Vec<GoogleCloudRetailV2LocalInventory>>,
    /// materials property.
    pub materials: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// patterns property.
    pub patterns: Option<Vec<String>>,
    /// priceInfo property.
    pub price_info: Option<GoogleCloudRetailV2PriceInfo>,
    /// primaryProductId property.
    pub primary_product_id: Option<String>,
    /// promotions property.
    pub promotions: Option<Vec<GoogleCloudRetailV2Promotion>>,
    /// publishTime property.
    pub publish_time: Option<String>,
    /// rating property.
    pub rating: Option<GoogleCloudRetailV2Rating>,
    /// retrievableFields property.
    pub retrievable_fields: Option<String>,
    /// sizes property.
    pub sizes: Option<Vec<String>>,
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// title property.
    pub title: Option<String>,
    /// ttl property.
    pub ttl: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// uri property.
    pub uri: Option<String>,
    /// variants property.
    pub variants: Option<Vec<Box<GoogleCloudRetailV2Product>>>,
}

/// `GoogleCloudRetailV2ImportProductsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ImportProductsRequest {
    /// errorsConfig property.
    pub errors_config: Option<GoogleCloudRetailV2ImportErrorsConfig>,
    /// inputConfig property.
    pub input_config: Option<GoogleCloudRetailV2ProductInputConfig>,
    /// notificationPubsubTopic property.
    pub notification_pubsub_topic: Option<String>,
    /// reconciliationMode property.
    pub reconciliation_mode: Option<String>,
    /// requestId property.
    pub request_id: Option<String>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `GoogleCloudRetailV2ProductInputConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ProductInputConfig {
    /// bigQuerySource property.
    pub big_query_source: Option<GoogleCloudRetailV2BigQuerySource>,
    /// gcsSource property.
    pub gcs_source: Option<GoogleCloudRetailV2GcsSource>,
    /// productInlineSource property.
    pub product_inline_source: Option<GoogleCloudRetailV2ProductInlineSource>,
}

/// `GoogleCloudRetailV2ProductAttributeInterval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ProductAttributeInterval {
    /// interval property.
    pub interval: Option<GoogleCloudRetailV2Interval>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleCloudRetailV2PurgeUserEventsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2PurgeUserEventsRequest {
    /// filter property.
    pub filter: Option<String>,
    /// force property.
    pub force: Option<bool>,
}

/// `GoogleCloudRetailV2Promotion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Promotion {
    /// promotionId property.
    pub promotion_id: Option<String>,
}

/// `GoogleCloudRetailV2Interval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Interval {
    /// exclusiveMaximum property.
    pub exclusive_maximum: Option<f64>,
    /// exclusiveMinimum property.
    pub exclusive_minimum: Option<f64>,
    /// maximum property.
    pub maximum: Option<f64>,
    /// minimum property.
    pub minimum: Option<f64>,
}

/// `GoogleCloudRetailV2RuleForceReturnFacetActionFacetPositionAdjustment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RuleForceReturnFacetActionFacetPositionAdjustment {
    /// attributeName property.
    pub attribute_name: Option<String>,
    /// position property.
    pub position: Option<i64>,
}

/// `GoogleTypeDate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeDate {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `GoogleCloudRetailV2FulfillmentInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2FulfillmentInfo {
    /// placeIds property.
    pub place_ids: Option<Vec<String>>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleLongrunningOperation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleLongrunningOperation {
    /// done property.
    pub done: Option<bool>,
    /// error property.
    pub error: Option<GoogleRpcStatus>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// response property.
    pub response: Option<serde_json::Value>,
}

/// `GoogleCloudRetailV2RuleFilterAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RuleFilterAction {
    /// filter property.
    pub filter: Option<String>,
}

/// `GoogleCloudRetailV2ConversationalSearchResponseConversationalFilteringResultAdditionalFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchResponseConversationalFilteringResultAdditionalFilter
{
    /// productAttributeValue property.
    pub product_attribute_value: Option<GoogleCloudRetailV2ProductAttributeValue>,
}

/// `GoogleCloudRetailV2SearchResponseFacet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchResponseFacet {
    /// dynamicFacet property.
    pub dynamic_facet: Option<bool>,
    /// key property.
    pub key: Option<String>,
    /// values property.
    pub values: Option<Vec<GoogleCloudRetailV2SearchResponseFacetFacetValue>>,
}

/// `GoogleCloudRetailV2AddCatalogAttributeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2AddCatalogAttributeRequest {
    /// catalogAttribute property.
    pub catalog_attribute: Option<GoogleCloudRetailV2CatalogAttribute>,
}

/// `GoogleCloudRetailV2PredictResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2PredictResponse {
    /// attributionToken property.
    pub attribution_token: Option<String>,
    /// missingIds property.
    pub missing_ids: Option<Vec<String>>,
    /// results property.
    pub results: Option<Vec<GoogleCloudRetailV2PredictResponsePredictionResult>>,
    /// validateOnly property.
    pub validate_only: Option<bool>,
}

/// `GoogleCloudRetailV2UpdateGenerativeQuestionConfigRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2UpdateGenerativeQuestionConfigRequest {
    /// generativeQuestionConfig property.
    pub generative_question_config: Option<GoogleCloudRetailV2GenerativeQuestionConfig>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `GoogleCloudRetailV2GenerativeQuestionsFeatureConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2GenerativeQuestionsFeatureConfig {
    /// catalog property.
    pub catalog: Option<String>,
    /// featureEnabled property.
    pub feature_enabled: Option<bool>,
    /// minimumProducts property.
    pub minimum_products: Option<i64>,
}

/// `GoogleCloudRetailV2PriceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2PriceInfo {
    /// cost property.
    pub cost: Option<f64>,
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// originalPrice property.
    pub original_price: Option<f64>,
    /// price property.
    pub price: Option<f64>,
    /// priceEffectiveTime property.
    pub price_effective_time: Option<String>,
    /// priceExpireTime property.
    pub price_expire_time: Option<String>,
    /// priceRange property.
    pub price_range: Option<GoogleCloudRetailV2PriceInfoPriceRange>,
}

/// `GoogleCloudRetailV2UserInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2UserInfo {
    /// directUserRequest property.
    pub direct_user_request: Option<bool>,
    /// ipAddress property.
    pub ip_address: Option<String>,
    /// userAgent property.
    pub user_agent: Option<String>,
    /// userId property.
    pub user_id: Option<String>,
}

/// `GoogleCloudRetailV2RuleRemoveFacetAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RuleRemoveFacetAction {
    /// attributeNames property.
    pub attribute_names: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2SearchRequestQueryExpansionSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestQueryExpansionSpec {
    /// condition property.
    pub condition: Option<String>,
    /// pinUnexpandedResults property.
    pub pin_unexpanded_results: Option<bool>,
}

/// `GoogleCloudRetailV2SearchRequestConversationalSearchSpecUserAnswer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestConversationalSearchSpecUserAnswer {
    /// selectedAnswer property.
    pub selected_answer:
        Option<GoogleCloudRetailV2SearchRequestConversationalSearchSpecUserAnswerSelectedAnswer>,
    /// textAnswer property.
    pub text_answer: Option<String>,
}

/// `GoogleCloudRetailV2ListProductsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ListProductsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// products property.
    pub products: Option<Vec<Box<GoogleCloudRetailV2Product>>>,
}

/// `GoogleLongrunningListOperationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleLongrunningListOperationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<GoogleLongrunningOperation>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2ProductLevelConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ProductLevelConfig {
    /// ingestionProductType property.
    pub ingestion_product_type: Option<String>,
    /// merchantCenterProductIdField property.
    pub merchant_center_product_id_field: Option<String>,
}

/// `GoogleCloudRetailV2SearchResponseQueryExpansionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchResponseQueryExpansionInfo {
    /// expandedQuery property.
    pub expanded_query: Option<bool>,
    /// pinnedResultCount property.
    pub pinned_result_count: Option<String>,
}

/// `GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse {
    /// generativeQuestionConfigs property.
    pub generative_question_configs: Option<Vec<GoogleCloudRetailV2GenerativeQuestionConfig>>,
}

/// `GoogleCloudRetailV2RuleForceReturnFacetAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RuleForceReturnFacetAction {
    /// facetPositionAdjustments property.
    pub facet_position_adjustments:
        Option<Vec<GoogleCloudRetailV2RuleForceReturnFacetActionFacetPositionAdjustment>>,
}

/// `GoogleCloudRetailV2ConversationalSearchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchRequest {
    /// branch property.
    pub branch: Option<String>,
    /// conversationId property.
    pub conversation_id: Option<String>,
    /// conversationalFilteringSpec property.
    pub conversational_filtering_spec:
        Option<GoogleCloudRetailV2ConversationalSearchRequestConversationalFilteringSpec>,
    /// pageCategories property.
    pub page_categories: Option<Vec<String>>,
    /// query property.
    pub query: Option<String>,
    /// safetySettings property.
    pub safety_settings: Option<Vec<GoogleCloudRetailV2SafetySetting>>,
    /// searchParams property.
    pub search_params: Option<GoogleCloudRetailV2ConversationalSearchRequestSearchParams>,
    /// userInfo property.
    pub user_info: Option<GoogleCloudRetailV2UserInfo>,
    /// userLabels property.
    pub user_labels: Option<serde_json::Value>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `GoogleCloudRetailV2Rating` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Rating {
    /// averageRating property.
    pub average_rating: Option<f64>,
    /// ratingCount property.
    pub rating_count: Option<i64>,
    /// ratingHistogram property.
    pub rating_histogram: Option<Vec<i64>>,
}

/// `GoogleCloudRetailV2ConversationalSearchRequestConversationalFilteringSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchRequestConversationalFilteringSpec {
    /// conversationalFilteringMode property.
    pub conversational_filtering_mode: Option<String>,
    /// enableConversationalFiltering property.
    pub enable_conversational_filtering: Option<bool>,
    /// userAnswer property.
    pub user_answer: Option<GoogleCloudRetailV2ConversationalSearchRequestUserAnswer>,
}

/// `GoogleCloudRetailV2ModelServingConfigList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ModelServingConfigList {
    /// servingConfigIds property.
    pub serving_config_ids: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2CompletionDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CompletionDetail {
    /// completionAttributionToken property.
    pub completion_attribution_token: Option<String>,
    /// selectedPosition property.
    pub selected_position: Option<i64>,
    /// selectedSuggestion property.
    pub selected_suggestion: Option<String>,
}

/// `GoogleCloudRetailV2ConversationalSearchResponseFollowupQuestion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchResponseFollowupQuestion {
    /// followupQuestion property.
    pub followup_question: Option<String>,
    /// suggestedAnswers property.
    pub suggested_answers:
        Option<Vec<GoogleCloudRetailV2ConversationalSearchResponseFollowupQuestionSuggestedAnswer>>,
}

/// `GoogleCloudRetailV2RejoinUserEventsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RejoinUserEventsRequest {
    /// userEventRejoinScope property.
    pub user_event_rejoin_scope: Option<String>,
}

/// `GoogleCloudRetailV2Rule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Rule {
    /// boostAction property.
    pub boost_action: Option<GoogleCloudRetailV2RuleBoostAction>,
    /// condition property.
    pub condition: Option<GoogleCloudRetailV2Condition>,
    /// doNotAssociateAction property.
    pub do_not_associate_action: Option<GoogleCloudRetailV2RuleDoNotAssociateAction>,
    /// filterAction property.
    pub filter_action: Option<GoogleCloudRetailV2RuleFilterAction>,
    /// forceReturnFacetAction property.
    pub force_return_facet_action: Option<GoogleCloudRetailV2RuleForceReturnFacetAction>,
    /// ignoreAction property.
    pub ignore_action: Option<GoogleCloudRetailV2RuleIgnoreAction>,
    /// onewaySynonymsAction property.
    pub oneway_synonyms_action: Option<GoogleCloudRetailV2RuleOnewaySynonymsAction>,
    /// pinAction property.
    pub pin_action: Option<GoogleCloudRetailV2RulePinAction>,
    /// redirectAction property.
    pub redirect_action: Option<GoogleCloudRetailV2RuleRedirectAction>,
    /// removeFacetAction property.
    pub remove_facet_action: Option<GoogleCloudRetailV2RuleRemoveFacetAction>,
    /// replacementAction property.
    pub replacement_action: Option<GoogleCloudRetailV2RuleReplacementAction>,
    /// twowaySynonymsAction property.
    pub twoway_synonyms_action: Option<GoogleCloudRetailV2RuleTwowaySynonymsAction>,
}

/// `GoogleCloudRetailV2ResumeModelRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ResumeModelRequest {}

/// `GoogleCloudRetailV2CompletionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CompletionConfig {
    /// allowlistInputConfig property.
    pub allowlist_input_config: Option<GoogleCloudRetailV2CompletionDataInputConfig>,
    /// autoLearning property.
    pub auto_learning: Option<bool>,
    /// denylistInputConfig property.
    pub denylist_input_config: Option<GoogleCloudRetailV2CompletionDataInputConfig>,
    /// lastAllowlistImportOperation property.
    pub last_allowlist_import_operation: Option<String>,
    /// lastDenylistImportOperation property.
    pub last_denylist_import_operation: Option<String>,
    /// lastSuggestionsImportOperation property.
    pub last_suggestions_import_operation: Option<String>,
    /// matchingOrder property.
    pub matching_order: Option<String>,
    /// maxSuggestions property.
    pub max_suggestions: Option<i64>,
    /// minPrefixLength property.
    pub min_prefix_length: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// suggestionsInputConfig property.
    pub suggestions_input_config: Option<GoogleCloudRetailV2CompletionDataInputConfig>,
}

/// `GoogleCloudRetailV2ReplaceCatalogAttributeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ReplaceCatalogAttributeRequest {
    /// catalogAttribute property.
    pub catalog_attribute: Option<GoogleCloudRetailV2CatalogAttribute>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `GoogleCloudRetailV2ProductAttributeValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ProductAttributeValue {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `GoogleCloudRetailV2ImportUserEventsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ImportUserEventsRequest {
    /// errorsConfig property.
    pub errors_config: Option<GoogleCloudRetailV2ImportErrorsConfig>,
    /// inputConfig property.
    pub input_config: Option<GoogleCloudRetailV2UserEventInputConfig>,
}

/// `GoogleCloudRetailV2CollectUserEventRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CollectUserEventRequest {
    /// ets property.
    pub ets: Option<String>,
    /// prebuiltRule property.
    pub prebuilt_rule: Option<String>,
    /// rawJson property.
    pub raw_json: Option<String>,
    /// uri property.
    pub uri: Option<String>,
    /// userEvent property.
    pub user_event: Option<String>,
}

/// `GoogleCloudRetailV2SearchRequestBoostSpecConditionBoostSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestBoostSpecConditionBoostSpec {
    /// boost property.
    pub boost: Option<f64>,
    /// condition property.
    pub condition: Option<String>,
}

/// `GoogleApiHttpBody` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleApiHttpBody {
    /// contentType property.
    pub content_type: Option<String>,
    /// data property.
    pub data: Option<String>,
    /// extensions property.
    pub extensions: Option<Vec<serde_json::Value>>,
}

/// `GoogleCloudRetailV2RuleReplacementAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RuleReplacementAction {
    /// queryTerms property.
    pub query_terms: Option<Vec<String>>,
    /// replacementTerm property.
    pub replacement_term: Option<String>,
    /// term property.
    pub term: Option<String>,
}

/// `GoogleCloudRetailV2ListModelsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ListModelsResponse {
    /// models property.
    pub models: Option<Vec<GoogleCloudRetailV2Model>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudRetailV2GcsSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2GcsSource {
    /// dataSchema property.
    pub data_schema: Option<String>,
    /// inputUris property.
    pub input_uris: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2CatalogAttributeFacetConfigMergedFacetValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CatalogAttributeFacetConfigMergedFacetValue {
    /// mergedValue property.
    pub merged_value: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2CatalogAttribute` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CatalogAttribute {
    /// dynamicFacetableOption property.
    pub dynamic_facetable_option: Option<String>,
    /// exactSearchableOption property.
    pub exact_searchable_option: Option<String>,
    /// facetConfig property.
    pub facet_config: Option<GoogleCloudRetailV2CatalogAttributeFacetConfig>,
    /// inUse property.
    pub in_use: Option<bool>,
    /// indexableOption property.
    pub indexable_option: Option<String>,
    /// key property.
    pub key: Option<String>,
    /// retrievableOption property.
    pub retrievable_option: Option<String>,
    /// searchableOption property.
    pub searchable_option: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleCloudRetailV2GetDefaultBranchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2GetDefaultBranchResponse {
    /// branch property.
    pub branch: Option<String>,
    /// note property.
    pub note: Option<String>,
    /// setTime property.
    pub set_time: Option<String>,
}

/// `GoogleCloudRetailV2UserEventInlineSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2UserEventInlineSource {
    /// userEvents property.
    pub user_events: Option<Vec<GoogleCloudRetailV2UserEvent>>,
}

/// `GoogleCloudRetailV2PurgeProductsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2PurgeProductsRequest {
    /// filter property.
    pub filter: Option<String>,
    /// force property.
    pub force: Option<bool>,
}

/// `GoogleCloudRetailV2PinControlMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2PinControlMetadata {
    /// allMatchedPins property.
    pub all_matched_pins: Option<serde_json::Value>,
    /// droppedPins property.
    pub dropped_pins: Option<serde_json::Value>,
}

/// `GoogleCloudRetailV2SearchRequestTileNavigationSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestTileNavigationSpec {
    /// appliedTiles property.
    pub applied_tiles: Option<Vec<GoogleCloudRetailV2Tile>>,
    /// tileNavigationRequested property.
    pub tile_navigation_requested: Option<bool>,
}

/// `GoogleCloudRetailV2CompleteQueryResponseCompletionResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CompleteQueryResponseCompletionResult {
    /// attributes property.
    pub attributes: Option<serde_json::Value>,
    /// suggestion property.
    pub suggestion: Option<String>,
}

/// `GoogleCloudRetailV2PredictRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2PredictRequest {
    /// filter property.
    pub filter: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// params property.
    pub params: Option<serde_json::Value>,
    /// userEvent property.
    pub user_event: Option<GoogleCloudRetailV2UserEvent>,
    /// validateOnly property.
    pub validate_only: Option<bool>,
}

/// `GoogleCloudRetailV2SearchRequestDynamicFacetSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestDynamicFacetSpec {
    /// mode property.
    pub mode: Option<String>,
}

/// `GoogleCloudRetailV2PredictResponsePredictionResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2PredictResponsePredictionResult {
    /// id property.
    pub id: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
}

/// `GoogleCloudRetailV2CatalogAttributeFacetConfigIgnoredFacetValues` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CatalogAttributeFacetConfigIgnoredFacetValues {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2IntentClassificationConfigInlineSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2IntentClassificationConfigInlineSource {
    /// inlineForceIntents property.
    pub inline_force_intents:
        Option<Vec<GoogleCloudRetailV2IntentClassificationConfigInlineForceIntent>>,
}

/// `GoogleCloudRetailV2Audience` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Audience {
    /// ageGroups property.
    pub age_groups: Option<Vec<String>>,
    /// genders property.
    pub genders: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2ListServingConfigsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ListServingConfigsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// servingConfigs property.
    pub serving_configs: Option<Vec<GoogleCloudRetailV2ServingConfig>>,
}

/// `GoogleCloudRetailV2SearchResponseSearchResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchResponseSearchResult {
    /// id property.
    pub id: Option<String>,
    /// matchingVariantCount property.
    pub matching_variant_count: Option<i64>,
    /// matchingVariantFields property.
    pub matching_variant_fields: Option<serde_json::Value>,
    /// modelScores property.
    pub model_scores: Option<serde_json::Value>,
    /// personalLabels property.
    pub personal_labels: Option<Vec<String>>,
    /// product property.
    pub product: Option<Box<GoogleCloudRetailV2Product>>,
    /// variantRollupValues property.
    pub variant_rollup_values: Option<serde_json::Value>,
}

/// `GoogleCloudRetailV2Catalog` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Catalog {
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// productLevelConfig property.
    pub product_level_config: Option<GoogleCloudRetailV2ProductLevelConfig>,
}

/// `GoogleCloudRetailV2TuneModelRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2TuneModelRequest {}

/// `GoogleCloudRetailV2ColorInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ColorInfo {
    /// colorFamilies property.
    pub color_families: Option<Vec<String>>,
    /// colors property.
    pub colors: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2CompletionDataInputConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CompletionDataInputConfig {
    /// bigQuerySource property.
    pub big_query_source: Option<GoogleCloudRetailV2BigQuerySource>,
}

/// `GoogleCloudRetailV2SearchResponseFacetFacetValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchResponseFacetFacetValue {
    /// count property.
    pub count: Option<String>,
    /// interval property.
    pub interval: Option<GoogleCloudRetailV2Interval>,
    /// maxValue property.
    pub max_value: Option<f64>,
    /// minValue property.
    pub min_value: Option<f64>,
    /// value property.
    pub value: Option<String>,
}

/// `GoogleCloudRetailV2GenerativeQuestionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2GenerativeQuestionConfig {
    /// allowedInConversation property.
    pub allowed_in_conversation: Option<bool>,
    /// catalog property.
    pub catalog: Option<String>,
    /// exampleValues property.
    pub example_values: Option<Vec<String>>,
    /// facet property.
    pub facet: Option<String>,
    /// finalQuestion property.
    pub final_question: Option<String>,
    /// frequency property.
    pub frequency: Option<f64>,
    /// generatedQuestion property.
    pub generated_question: Option<String>,
}

/// `GoogleCloudRetailV2Model` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Model {
    /// createTime property.
    pub create_time: Option<String>,
    /// dataState property.
    pub data_state: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// filteringOption property.
    pub filtering_option: Option<String>,
    /// lastTuneTime property.
    pub last_tune_time: Option<String>,
    /// modelFeaturesConfig property.
    pub model_features_config: Option<GoogleCloudRetailV2ModelModelFeaturesConfig>,
    /// name property.
    pub name: Option<String>,
    /// optimizationObjective property.
    pub optimization_objective: Option<String>,
    /// periodicTuningState property.
    pub periodic_tuning_state: Option<String>,
    /// servingConfigLists property.
    pub serving_config_lists: Option<Vec<GoogleCloudRetailV2ModelServingConfigList>>,
    /// servingState property.
    pub serving_state: Option<String>,
    /// trainingState property.
    pub training_state: Option<String>,
    /// tuningOperation property.
    pub tuning_operation: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `GoogleCloudRetailV2ConversationalSearchCustomizationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchCustomizationConfig {
    /// catalog property.
    pub catalog: Option<String>,
    /// intentClassificationConfig property.
    pub intent_classification_config: Option<GoogleCloudRetailV2IntentClassificationConfig>,
    /// retailerDisplayName property.
    pub retailer_display_name: Option<String>,
}

/// `GoogleCloudRetailV2PurchaseTransaction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2PurchaseTransaction {
    /// cost property.
    pub cost: Option<f64>,
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// revenue property.
    pub revenue: Option<f64>,
    /// tax property.
    pub tax: Option<f64>,
}

/// `GoogleCloudRetailV2SearchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchResponse {
    /// appliedControls property.
    pub applied_controls: Option<Vec<String>>,
    /// attributionToken property.
    pub attribution_token: Option<String>,
    /// conversationalSearchResult property.
    pub conversational_search_result:
        Option<GoogleCloudRetailV2SearchResponseConversationalSearchResult>,
    /// correctedQuery property.
    pub corrected_query: Option<String>,
    /// experimentInfo property.
    pub experiment_info: Option<Vec<GoogleCloudRetailV2ExperimentInfo>>,
    /// facets property.
    pub facets: Option<Vec<GoogleCloudRetailV2SearchResponseFacet>>,
    /// invalidConditionBoostSpecs property.
    pub invalid_condition_boost_specs:
        Option<Vec<GoogleCloudRetailV2SearchRequestBoostSpecConditionBoostSpec>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pinControlMetadata property.
    pub pin_control_metadata: Option<GoogleCloudRetailV2PinControlMetadata>,
    /// queryExpansionInfo property.
    pub query_expansion_info: Option<GoogleCloudRetailV2SearchResponseQueryExpansionInfo>,
    /// redirectUri property.
    pub redirect_uri: Option<String>,
    /// results property.
    pub results: Option<Vec<GoogleCloudRetailV2SearchResponseSearchResult>>,
    /// tileNavigationResult property.
    pub tile_navigation_result: Option<GoogleCloudRetailV2SearchResponseTileNavigationResult>,
    /// totalSize property.
    pub total_size: Option<i64>,
}

/// `GoogleCloudRetailV2BigQuerySource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2BigQuerySource {
    /// dataSchema property.
    pub data_schema: Option<String>,
    /// datasetId property.
    pub dataset_id: Option<String>,
    /// gcsStagingDir property.
    pub gcs_staging_dir: Option<String>,
    /// partitionDate property.
    pub partition_date: Option<GoogleTypeDate>,
    /// projectId property.
    pub project_id: Option<String>,
    /// tableId property.
    pub table_id: Option<String>,
}

/// `GoogleCloudRetailV2Tile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Tile {
    /// productAttributeInterval property.
    pub product_attribute_interval: Option<GoogleCloudRetailV2ProductAttributeInterval>,
    /// productAttributeValue property.
    pub product_attribute_value: Option<GoogleCloudRetailV2ProductAttributeValue>,
    /// representativeProductId property.
    pub representative_product_id: Option<String>,
}

/// `GoogleCloudRetailV2ConversationalSearchResponseConversationalFilteringResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchResponseConversationalFilteringResult {
    /// additionalFilter property.
    pub additional_filter: Option<GoogleCloudRetailV2ConversationalSearchResponseConversationalFilteringResultAdditionalFilter>,
    /// followupQuestion property.
    pub followup_question: Option<GoogleCloudRetailV2ConversationalSearchResponseFollowupQuestion>,
}

/// `GoogleCloudRetailV2SafetySetting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SafetySetting {
    /// category property.
    pub category: Option<String>,
    /// method property.
    pub method: Option<String>,
    /// threshold property.
    pub threshold: Option<String>,
}

/// `GoogleCloudRetailV2SearchRequestConversationalSearchSpecUserAnswerSelectedAnswer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestConversationalSearchSpecUserAnswerSelectedAnswer {
    /// productAttributeValue property.
    pub product_attribute_value: Option<GoogleCloudRetailV2ProductAttributeValue>,
    /// productAttributeValues property.
    pub product_attribute_values: Option<Vec<GoogleCloudRetailV2ProductAttributeValue>>,
}

/// `GoogleCloudRetailV2RemoveCatalogAttributeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RemoveCatalogAttributeRequest {
    /// key property.
    pub key: Option<String>,
}

/// `GoogleCloudRetailV2AddFulfillmentPlacesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2AddFulfillmentPlacesRequest {
    /// addTime property.
    pub add_time: Option<String>,
    /// allowMissing property.
    pub allow_missing: Option<bool>,
    /// placeIds property.
    pub place_ids: Option<Vec<String>>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleProtobufEmpty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleProtobufEmpty {}

/// `GoogleCloudRetailV2IntentClassificationConfigInlineForceIntent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2IntentClassificationConfigInlineForceIntent {
    /// intentType property.
    pub intent_type: Option<String>,
    /// operation property.
    pub operation: Option<String>,
    /// query property.
    pub query: Option<String>,
}

/// `GoogleCloudRetailV2SearchRequestBoostSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestBoostSpec {
    /// conditionBoostSpecs property.
    pub condition_boost_specs:
        Option<Vec<GoogleCloudRetailV2SearchRequestBoostSpecConditionBoostSpec>>,
    /// skipBoostSpecValidation property.
    pub skip_boost_spec_validation: Option<bool>,
}

/// `GoogleCloudRetailV2SearchRequestConversationalSearchSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestConversationalSearchSpec {
    /// conversationId property.
    pub conversation_id: Option<String>,
    /// followupConversationRequested property.
    pub followup_conversation_requested: Option<bool>,
    /// userAnswer property.
    pub user_answer: Option<GoogleCloudRetailV2SearchRequestConversationalSearchSpecUserAnswer>,
}

/// `GoogleCloudRetailV2RuleRedirectAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RuleRedirectAction {
    /// redirectUri property.
    pub redirect_uri: Option<String>,
}

/// `GoogleCloudRetailV2PauseModelRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2PauseModelRequest {}

/// `GoogleCloudRetailV2SearchRequestPersonalizationSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestPersonalizationSpec {
    /// mode property.
    pub mode: Option<String>,
}

/// `GoogleCloudRetailV2SetInventoryRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SetInventoryRequest {
    /// allowMissing property.
    pub allow_missing: Option<bool>,
    /// inventory property.
    pub inventory: Option<Box<GoogleCloudRetailV2Product>>,
    /// setMask property.
    pub set_mask: Option<String>,
    /// setTime property.
    pub set_time: Option<String>,
}

/// `GoogleCloudRetailV2SearchResponseConversationalSearchResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchResponseConversationalSearchResult {
    /// additionalFilter property.
    pub additional_filter:
        Option<GoogleCloudRetailV2SearchResponseConversationalSearchResultAdditionalFilter>,
    /// additionalFilters property.
    pub additional_filters:
        Option<Vec<GoogleCloudRetailV2SearchResponseConversationalSearchResultAdditionalFilter>>,
    /// conversationId property.
    pub conversation_id: Option<String>,
    /// followupQuestion property.
    pub followup_question: Option<String>,
    /// refinedQuery property.
    pub refined_query: Option<String>,
    /// suggestedAnswers property.
    pub suggested_answers:
        Option<Vec<GoogleCloudRetailV2SearchResponseConversationalSearchResultSuggestedAnswer>>,
}

/// `GoogleCloudRetailV2ProductInlineSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ProductInlineSource {
    /// products property.
    pub products: Option<Vec<Box<GoogleCloudRetailV2Product>>>,
}

/// `GoogleCloudRetailV2IntentClassificationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2IntentClassificationConfig {
    /// blocklistKeywords property.
    pub blocklist_keywords: Option<Vec<String>>,
    /// disabledIntentTypes property.
    pub disabled_intent_types: Option<Vec<String>>,
    /// example property.
    pub example: Option<Vec<GoogleCloudRetailV2IntentClassificationConfigExample>>,
    /// inlineSource property.
    pub inline_source: Option<GoogleCloudRetailV2IntentClassificationConfigInlineSource>,
    /// modelPreamble property.
    pub model_preamble: Option<String>,
}

/// `GoogleCloudRetailV2IntentClassificationConfigExample` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2IntentClassificationConfigExample {
    /// classifiedPositive property.
    pub classified_positive: Option<bool>,
    /// intentType property.
    pub intent_type: Option<String>,
    /// query property.
    pub query: Option<String>,
    /// reason property.
    pub reason: Option<String>,
}

/// `GoogleCloudRetailV2ListCatalogsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ListCatalogsResponse {
    /// catalogs property.
    pub catalogs: Option<Vec<GoogleCloudRetailV2Catalog>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudRetailV2ModelFrequentlyBoughtTogetherFeaturesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ModelFrequentlyBoughtTogetherFeaturesConfig {
    /// contextProductsType property.
    pub context_products_type: Option<String>,
}

/// `GoogleCloudRetailV2ProductDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ProductDetail {
    /// product property.
    pub product: Option<Box<GoogleCloudRetailV2Product>>,
    /// quantity property.
    pub quantity: Option<i64>,
}

/// `GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsRequest {
    /// requests property.
    pub requests: Option<Vec<GoogleCloudRetailV2UpdateGenerativeQuestionConfigRequest>>,
}

/// `GoogleCloudRetailV2RuleIgnoreAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RuleIgnoreAction {
    /// ignoreTerms property.
    pub ignore_terms: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2OutputConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2OutputConfig {
    /// bigqueryDestination property.
    pub bigquery_destination: Option<GoogleCloudRetailV2OutputConfigBigQueryDestination>,
    /// gcsDestination property.
    pub gcs_destination: Option<GoogleCloudRetailV2OutputConfigGcsDestination>,
}

/// `GoogleCloudRetailV2SearchRequestFacetSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestFacetSpec {
    /// enableDynamicPosition property.
    pub enable_dynamic_position: Option<bool>,
    /// excludedFilterKeys property.
    pub excluded_filter_keys: Option<Vec<String>>,
    /// facetKey property.
    pub facet_key: Option<GoogleCloudRetailV2SearchRequestFacetSpecFacetKey>,
    /// limit property.
    pub limit: Option<i64>,
}

/// `GoogleCloudRetailV2CatalogAttributeFacetConfigRerankConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CatalogAttributeFacetConfigRerankConfig {
    /// facetValues property.
    pub facet_values: Option<Vec<String>>,
    /// rerankFacet property.
    pub rerank_facet: Option<bool>,
}

/// `GoogleCloudRetailV2RemoveControlRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RemoveControlRequest {
    /// controlId property.
    pub control_id: Option<String>,
}

/// `GoogleCloudRetailV2ImportErrorsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ImportErrorsConfig {
    /// gcsPrefix property.
    pub gcs_prefix: Option<String>,
}

/// `GoogleCloudRetailV2ConditionTimeRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConditionTimeRange {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `GoogleCloudRetailV2ConversationalSearchRequestUserAnswerSelectedAnswer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchRequestUserAnswerSelectedAnswer {
    /// productAttributeValue property.
    pub product_attribute_value: Option<GoogleCloudRetailV2ProductAttributeValue>,
}

/// `GoogleCloudRetailV2RuleOnewaySynonymsAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RuleOnewaySynonymsAction {
    /// onewayTerms property.
    pub oneway_terms: Option<Vec<String>>,
    /// queryTerms property.
    pub query_terms: Option<Vec<String>>,
    /// synonyms property.
    pub synonyms: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2SearchRequestSpellCorrectionSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchRequestSpellCorrectionSpec {
    /// mode property.
    pub mode: Option<String>,
}

/// `GoogleCloudRetailV2SetDefaultBranchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SetDefaultBranchRequest {
    /// branchId property.
    pub branch_id: Option<String>,
    /// force property.
    pub force: Option<bool>,
    /// note property.
    pub note: Option<String>,
}

/// `GoogleCloudRetailV2ListControlsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ListControlsResponse {
    /// controls property.
    pub controls: Option<Vec<GoogleCloudRetailV2Control>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudRetailV2SearchResponseConversationalSearchResultAdditionalFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchResponseConversationalSearchResultAdditionalFilter {
    /// productAttributeValue property.
    pub product_attribute_value: Option<GoogleCloudRetailV2ProductAttributeValue>,
}

/// `GoogleCloudRetailV2ImportCompletionDataRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ImportCompletionDataRequest {
    /// inputConfig property.
    pub input_config: Option<GoogleCloudRetailV2CompletionDataInputConfig>,
    /// notificationPubsubTopic property.
    pub notification_pubsub_topic: Option<String>,
}

/// `GoogleCloudRetailV2RemoveLocalInventoriesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RemoveLocalInventoriesRequest {
    /// allowMissing property.
    pub allow_missing: Option<bool>,
    /// placeIds property.
    pub place_ids: Option<Vec<String>>,
    /// removeTime property.
    pub remove_time: Option<String>,
}

/// `GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse {
    /// generativeQuestionConfigs property.
    pub generative_question_configs: Option<Vec<GoogleCloudRetailV2GenerativeQuestionConfig>>,
}

/// `GoogleCloudRetailV2Condition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Condition {
    /// activeTimeRange property.
    pub active_time_range: Option<Vec<GoogleCloudRetailV2ConditionTimeRange>>,
    /// pageCategories property.
    pub page_categories: Option<Vec<String>>,
    /// queryTerms property.
    pub query_terms: Option<Vec<GoogleCloudRetailV2ConditionQueryTerm>>,
}

/// `GoogleCloudRetailV2ConversationalSearchResponseFollowupQuestionSuggestedAnswer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchResponseFollowupQuestionSuggestedAnswer {
    /// productAttributeValue property.
    pub product_attribute_value: Option<GoogleCloudRetailV2ProductAttributeValue>,
}

/// `GoogleCloudRetailV2PriceInfoPriceRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2PriceInfoPriceRange {
    /// originalPrice property.
    pub original_price: Option<GoogleCloudRetailV2Interval>,
    /// price property.
    pub price: Option<GoogleCloudRetailV2Interval>,
}

/// `GoogleCloudRetailV2RulePinAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RulePinAction {
    /// pinMap property.
    pub pin_map: Option<serde_json::Value>,
}

/// `GoogleCloudRetailV2AttributesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2AttributesConfig {
    /// attributeConfigLevel property.
    pub attribute_config_level: Option<String>,
    /// catalogAttributes property.
    pub catalog_attributes: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleCloudRetailV2ExportAnalyticsMetricsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ExportAnalyticsMetricsRequest {
    /// filter property.
    pub filter: Option<String>,
    /// outputConfig property.
    pub output_config: Option<GoogleCloudRetailV2OutputConfig>,
}

/// `GoogleCloudRetailV2RuleDoNotAssociateAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2RuleDoNotAssociateAction {
    /// doNotAssociateTerms property.
    pub do_not_associate_terms: Option<Vec<String>>,
    /// queryTerms property.
    pub query_terms: Option<Vec<String>>,
    /// terms property.
    pub terms: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2UserEventInputConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2UserEventInputConfig {
    /// bigQuerySource property.
    pub big_query_source: Option<GoogleCloudRetailV2BigQuerySource>,
    /// gcsSource property.
    pub gcs_source: Option<GoogleCloudRetailV2GcsSource>,
    /// userEventInlineSource property.
    pub user_event_inline_source: Option<GoogleCloudRetailV2UserEventInlineSource>,
}

/// `GoogleCloudRetailV2OutputConfigGcsDestination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2OutputConfigGcsDestination {
    /// outputUriPrefix property.
    pub output_uri_prefix: Option<String>,
}

/// `GoogleRpcStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleRpcStatus {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `GoogleCloudRetailV2UserEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2UserEvent {
    /// attributes property.
    pub attributes: Option<serde_json::Value>,
    /// attributionToken property.
    pub attribution_token: Option<String>,
    /// cartId property.
    pub cart_id: Option<String>,
    /// completionDetail property.
    pub completion_detail: Option<GoogleCloudRetailV2CompletionDetail>,
    /// entity property.
    pub entity: Option<String>,
    /// eventTime property.
    pub event_time: Option<String>,
    /// eventType property.
    pub event_type: Option<String>,
    /// experimentIds property.
    pub experiment_ids: Option<Vec<String>>,
    /// filter property.
    pub filter: Option<String>,
    /// offset property.
    pub offset: Option<i64>,
    /// orderBy property.
    pub order_by: Option<String>,
    /// pageCategories property.
    pub page_categories: Option<Vec<String>>,
    /// pageViewId property.
    pub page_view_id: Option<String>,
    /// panels property.
    pub panels: Option<Vec<GoogleCloudRetailV2PanelInfo>>,
    /// productDetails property.
    pub product_details: Option<Vec<GoogleCloudRetailV2ProductDetail>>,
    /// purchaseTransaction property.
    pub purchase_transaction: Option<GoogleCloudRetailV2PurchaseTransaction>,
    /// referrerUri property.
    pub referrer_uri: Option<String>,
    /// searchQuery property.
    pub search_query: Option<String>,
    /// sessionId property.
    pub session_id: Option<String>,
    /// uri property.
    pub uri: Option<String>,
    /// userInfo property.
    pub user_info: Option<GoogleCloudRetailV2UserInfo>,
    /// visitorId property.
    pub visitor_id: Option<String>,
}

/// `GoogleCloudRetailV2CompleteQueryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2CompleteQueryResponse {
    /// attributeResults property.
    pub attribute_results: Option<serde_json::Value>,
    /// attributionToken property.
    pub attribution_token: Option<String>,
    /// completionResults property.
    pub completion_results: Option<Vec<GoogleCloudRetailV2CompleteQueryResponseCompletionResult>>,
    /// recentSearchResults property.
    pub recent_search_results:
        Option<Vec<GoogleCloudRetailV2CompleteQueryResponseRecentSearchResult>>,
}

/// `GoogleCloudRetailV2Control` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2Control {
    /// associatedServingConfigIds property.
    pub associated_serving_config_ids: Option<Vec<String>>,
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// rule property.
    pub rule: Option<GoogleCloudRetailV2Rule>,
    /// searchSolutionUseCase property.
    pub search_solution_use_case: Option<Vec<String>>,
    /// solutionTypes property.
    pub solution_types: Option<Vec<String>>,
}

/// `GoogleCloudRetailV2SearchResponseTileNavigationResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2SearchResponseTileNavigationResult {
    /// tiles property.
    pub tiles: Option<Vec<GoogleCloudRetailV2Tile>>,
}

/// `GoogleCloudRetailV2ConversationalSearchRequestUserAnswer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2ConversationalSearchRequestUserAnswer {
    /// selectedAnswer property.
    pub selected_answer:
        Option<GoogleCloudRetailV2ConversationalSearchRequestUserAnswerSelectedAnswer>,
    /// textAnswer property.
    pub text_answer: Option<String>,
}

/// `GoogleCloudRetailV2AddLocalInventoriesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRetailV2AddLocalInventoriesRequest {
    /// addMask property.
    pub add_mask: Option<String>,
    /// addTime property.
    pub add_time: Option<String>,
    /// allowMissing property.
    pub allow_missing: Option<bool>,
    /// localInventories property.
    pub local_inventories: Option<Vec<GoogleCloudRetailV2LocalInventory>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`retail.projects.locations.catalogs.completeQuery_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsCompleteQueryArgs {
    /// Path parameter: `catalog`.
    pub catalog: String,
    /// Query parameter: `dataset`.
    pub dataset: Option<String>,
    /// Query parameter: `deviceType`.
    pub device_type: Option<String>,
    /// Query parameter: `enableAttributeSuggestions`.
    pub enable_attribute_suggestions: Option<String>,
    /// Query parameter: `entity`.
    pub entity: Option<String>,
    /// Query parameter: `languageCodes`.
    pub language_codes: Option<String>,
    /// Query parameter: `maxSuggestions`.
    pub max_suggestions: Option<String>,
    /// Query parameter: `query`.
    pub query: Option<String>,
    /// Query parameter: `visitorId`.
    pub visitor_id: Option<String>,
}

/// Arguments for [`retail.projects.locations.catalogs.exportAnalyticsMetrics_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsExportAnalyticsMetricsArgs {
    /// Path parameter: `catalog`.
    pub catalog: String,
    /// Request body.
    pub body: GoogleCloudRetailV2ExportAnalyticsMetricsRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.getAttributesConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGetAttributesConfigArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.getCompletionConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGetCompletionConfigArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.getConversationalSearchCustomizationConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGetConversationalSearchCustomizationConfigArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.getDefaultBranch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGetDefaultBranchArgs {
    /// Path parameter: `catalog`.
    pub catalog: String,
}

/// Arguments for [`retail.projects.locations.catalogs.getGenerativeQuestionFeature_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGetGenerativeQuestionFeatureArgs {
    /// Path parameter: `catalog`.
    pub catalog: String,
}

/// Arguments for [`retail.projects.locations.catalogs.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`retail.projects.locations.catalogs.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2Catalog,
}

/// Arguments for [`retail.projects.locations.catalogs.setDefaultBranch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsSetDefaultBranchArgs {
    /// Path parameter: `catalog`.
    pub catalog: String,
    /// Request body.
    pub body: GoogleCloudRetailV2SetDefaultBranchRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.updateAttributesConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUpdateAttributesConfigArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2AttributesConfig,
}

/// Arguments for [`retail.projects.locations.catalogs.updateCompletionConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUpdateCompletionConfigArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2CompletionConfig,
}

/// Arguments for [`retail.projects.locations.catalogs.updateConversationalSearchCustomizationConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUpdateConversationalSearchCustomizationConfigArgs {
    /// Path parameter: `catalog`.
    pub catalog: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2ConversationalSearchCustomizationConfig,
}

/// Arguments for [`retail.projects.locations.catalogs.updateGenerativeQuestion_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUpdateGenerativeQuestionArgs {
    /// Path parameter: `catalog`.
    pub catalog: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2GenerativeQuestionConfig,
}

/// Arguments for [`retail.projects.locations.catalogs.updateGenerativeQuestionFeature_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUpdateGenerativeQuestionFeatureArgs {
    /// Path parameter: `catalog`.
    pub catalog: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2GenerativeQuestionsFeatureConfig,
}

/// Arguments for [`retail.projects.locations.catalogs.attributesConfig.addCatalogAttribute_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsAttributesConfigAddCatalogAttributeArgs {
    /// Path parameter: `attributesConfig`.
    pub attributes_config: String,
    /// Request body.
    pub body: GoogleCloudRetailV2AddCatalogAttributeRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.attributesConfig.removeCatalogAttribute_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsAttributesConfigRemoveCatalogAttributeArgs {
    /// Path parameter: `attributesConfig`.
    pub attributes_config: String,
    /// Request body.
    pub body: GoogleCloudRetailV2RemoveCatalogAttributeRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.attributesConfig.replaceCatalogAttribute_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsAttributesConfigReplaceCatalogAttributeArgs {
    /// Path parameter: `attributesConfig`.
    pub attributes_config: String,
    /// Request body.
    pub body: GoogleCloudRetailV2ReplaceCatalogAttributeRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.addFulfillmentPlaces_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsAddFulfillmentPlacesArgs {
    /// Path parameter: `product`.
    pub product: String,
    /// Request body.
    pub body: GoogleCloudRetailV2AddFulfillmentPlacesRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.addLocalInventories_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsAddLocalInventoriesArgs {
    /// Path parameter: `product`.
    pub product: String,
    /// Request body.
    pub body: GoogleCloudRetailV2AddLocalInventoriesRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `productId`.
    pub product_id: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2Product,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.import_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsImportArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRetailV2ImportProductsRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `readMask`.
    pub read_mask: Option<String>,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2Product,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.purge_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsPurgeArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRetailV2PurgeProductsRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.removeFulfillmentPlaces_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsRemoveFulfillmentPlacesArgs {
    /// Path parameter: `product`.
    pub product: String,
    /// Request body.
    pub body: GoogleCloudRetailV2RemoveFulfillmentPlacesRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.removeLocalInventories_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsRemoveLocalInventoriesArgs {
    /// Path parameter: `product`.
    pub product: String,
    /// Request body.
    pub body: GoogleCloudRetailV2RemoveLocalInventoriesRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.branches.products.setInventory_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsBranchesProductsSetInventoryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudRetailV2SetInventoryRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.completionData.import_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsCompletionDataImportArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRetailV2ImportCompletionDataRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.controls.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsControlsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `controlId`.
    pub control_id: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2Control,
}

/// Arguments for [`retail.projects.locations.catalogs.controls.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsControlsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.controls.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsControlsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.controls.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsControlsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`retail.projects.locations.catalogs.controls.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsControlsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2Control,
}

/// Arguments for [`retail.projects.locations.catalogs.generativeQuestion.batchUpdate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGenerativeQuestionBatchUpdateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.generativeQuestions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsGenerativeQuestionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`retail.projects.locations.catalogs.models.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `dryRun`.
    pub dry_run: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2Model,
}

/// Arguments for [`retail.projects.locations.catalogs.models.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.models.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.models.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`retail.projects.locations.catalogs.models.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2Model,
}

/// Arguments for [`retail.projects.locations.catalogs.models.pause_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsPauseArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudRetailV2PauseModelRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.models.resume_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsResumeArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudRetailV2ResumeModelRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.models.tune_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsModelsTuneArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudRetailV2TuneModelRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`retail.projects.locations.catalogs.placements.conversationalSearch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsPlacementsConversationalSearchArgs {
    /// Path parameter: `placement`.
    pub placement: String,
    /// Request body.
    pub body: GoogleCloudRetailV2ConversationalSearchRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.placements.predict_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsPlacementsPredictArgs {
    /// Path parameter: `placement`.
    pub placement: String,
    /// Request body.
    pub body: GoogleCloudRetailV2PredictRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.placements.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsPlacementsSearchArgs {
    /// Path parameter: `placement`.
    pub placement: String,
    /// Request body.
    pub body: GoogleCloudRetailV2SearchRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.servingConfigs.addControl_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsAddControlArgs {
    /// Path parameter: `servingConfig`.
    pub serving_config: String,
    /// Request body.
    pub body: GoogleCloudRetailV2AddControlRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.servingConfigs.conversationalSearch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsConversationalSearchArgs {
    /// Path parameter: `placement`.
    pub placement: String,
    /// Request body.
    pub body: GoogleCloudRetailV2ConversationalSearchRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.servingConfigs.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `servingConfigId`.
    pub serving_config_id: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2ServingConfig,
}

/// Arguments for [`retail.projects.locations.catalogs.servingConfigs.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.servingConfigs.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.catalogs.servingConfigs.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`retail.projects.locations.catalogs.servingConfigs.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2ServingConfig,
}

/// Arguments for [`retail.projects.locations.catalogs.servingConfigs.predict_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsPredictArgs {
    /// Path parameter: `placement`.
    pub placement: String,
    /// Request body.
    pub body: GoogleCloudRetailV2PredictRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.servingConfigs.removeControl_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsRemoveControlArgs {
    /// Path parameter: `servingConfig`.
    pub serving_config: String,
    /// Request body.
    pub body: GoogleCloudRetailV2RemoveControlRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.servingConfigs.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsServingConfigsSearchArgs {
    /// Path parameter: `placement`.
    pub placement: String,
    /// Request body.
    pub body: GoogleCloudRetailV2SearchRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.userEvents.collect_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUserEventsCollectArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRetailV2CollectUserEventRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.userEvents.import_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUserEventsImportArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRetailV2ImportUserEventsRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.userEvents.purge_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUserEventsPurgeArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRetailV2PurgeUserEventsRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.userEvents.rejoin_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUserEventsRejoinArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRetailV2RejoinUserEventsRequest,
}

/// Arguments for [`retail.projects.locations.catalogs.userEvents.write_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsCatalogsUserEventsWriteArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `writeAsync`.
    pub write_async: Option<String>,
    /// Request body.
    pub body: GoogleCloudRetailV2UserEvent,
}

/// Arguments for [`retail.projects.locations.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.locations.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsLocationsOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`retail.projects.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`retail.projects.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RetailProjectsOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v2/{+catalog}:completeQuery
// -----------------------------------------------------------------------------

/// GET v2/{+catalog}:completeQuery.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_complete_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_complete_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsCompleteQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2CompleteQueryResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:completeQuery",
        args.catalog,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.dataset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dataset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.device_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deviceType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.enable_attribute_suggestions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enableAttributeSuggestions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.entity {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("entity=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.language_codes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCodes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_suggestions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxSuggestions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.query {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("query=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.visitor_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("visitorId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2CompleteQueryResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+catalog}:exportAnalyticsMetrics
// -----------------------------------------------------------------------------

/// POST v2/{+catalog}:exportAnalyticsMetrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_export_analytics_metrics_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_export_analytics_metrics_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsExportAnalyticsMetricsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:exportAnalyticsMetrics",
        args.catalog,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_get_attributes_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_get_attributes_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsGetAttributesConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2AttributesConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2AttributesConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_get_completion_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_get_completion_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsGetCompletionConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2CompletionConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2CompletionConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}/conversationalSearchCustomizationConfig
// -----------------------------------------------------------------------------

/// GET v2/{+name}/conversationalSearchCustomizationConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_get_conversational_search_customization_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_get_conversational_search_customization_config_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsGetConversationalSearchCustomizationConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ConversationalSearchCustomizationConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/conversationalSearchCustomizationConfig",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ConversationalSearchCustomizationConfig =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+catalog}:getDefaultBranch
// -----------------------------------------------------------------------------

/// GET v2/{+catalog}:getDefaultBranch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_get_default_branch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_get_default_branch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsGetDefaultBranchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2GetDefaultBranchResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:getDefaultBranch",
        args.catalog,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2GetDefaultBranchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+catalog}/generativeQuestionFeature
// -----------------------------------------------------------------------------

/// GET v2/{+catalog}/generativeQuestionFeature.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_get_generative_question_feature_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_get_generative_question_feature_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsGetGenerativeQuestionFeatureArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2GenerativeQuestionsFeatureConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/generativeQuestionFeature",
        args.catalog,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2GenerativeQuestionsFeatureConfig =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/catalogs
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/catalogs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ListCatalogsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}/catalogs", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListCatalogsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+name}
// -----------------------------------------------------------------------------

/// PATCH v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Catalog>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Catalog =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+catalog}:setDefaultBranch
// -----------------------------------------------------------------------------

/// POST v2/{+catalog}:setDefaultBranch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_set_default_branch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_set_default_branch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsSetDefaultBranchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:setDefaultBranch",
        args.catalog,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+name}
// -----------------------------------------------------------------------------

/// PATCH v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_update_attributes_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_update_attributes_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsUpdateAttributesConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2AttributesConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2AttributesConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+name}
// -----------------------------------------------------------------------------

/// PATCH v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_update_completion_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_update_completion_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsUpdateCompletionConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2CompletionConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2CompletionConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+catalog}/conversationalSearchCustomizationConfig
// -----------------------------------------------------------------------------

/// PATCH v2/{+catalog}/conversationalSearchCustomizationConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_update_conversational_search_customization_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_update_conversational_search_customization_config_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsUpdateConversationalSearchCustomizationConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ConversationalSearchCustomizationConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/conversationalSearchCustomizationConfig",
        args.catalog,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ConversationalSearchCustomizationConfig =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+catalog}/generativeQuestion
// -----------------------------------------------------------------------------

/// PATCH v2/{+catalog}/generativeQuestion.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_update_generative_question_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_update_generative_question_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsUpdateGenerativeQuestionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2GenerativeQuestionConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/generativeQuestion",
        args.catalog,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2GenerativeQuestionConfig =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+catalog}/generativeQuestionFeature
// -----------------------------------------------------------------------------

/// PATCH v2/{+catalog}/generativeQuestionFeature.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_update_generative_question_feature_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_update_generative_question_feature_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsUpdateGenerativeQuestionFeatureArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2GenerativeQuestionsFeatureConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/generativeQuestionFeature",
        args.catalog,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2GenerativeQuestionsFeatureConfig =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+attributesConfig}:addCatalogAttribute
// -----------------------------------------------------------------------------

/// POST v2/{+attributesConfig}:addCatalogAttribute.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_attributes_config_add_catalog_attribute_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsAttributesConfigAddCatalogAttributeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2AttributesConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:addCatalogAttribute",
        args.attributes_config,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2AttributesConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+attributesConfig}:removeCatalogAttribute
// -----------------------------------------------------------------------------

/// POST v2/{+attributesConfig}:removeCatalogAttribute.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_attributes_config_remove_catalog_attribute_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsAttributesConfigRemoveCatalogAttributeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2AttributesConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:removeCatalogAttribute",
        args.attributes_config,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2AttributesConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+attributesConfig}:replaceCatalogAttribute
// -----------------------------------------------------------------------------

/// POST v2/{+attributesConfig}:replaceCatalogAttribute.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_attributes_config_replace_catalog_attribute_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsAttributesConfigReplaceCatalogAttributeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2AttributesConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:replaceCatalogAttribute",
        args.attributes_config,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2AttributesConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+product}:addFulfillmentPlaces
// -----------------------------------------------------------------------------

/// POST v2/{+product}:addFulfillmentPlaces.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_add_fulfillment_places_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_add_fulfillment_places_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsAddFulfillmentPlacesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:addFulfillmentPlaces",
        args.product,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+product}:addLocalInventories
// -----------------------------------------------------------------------------

/// POST v2/{+product}:addLocalInventories.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_add_local_inventories_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_add_local_inventories_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsAddLocalInventoriesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:addLocalInventories",
        args.product,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/products
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/products.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Product>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}/products", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.product_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("productId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Product =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2/{+name}
// -----------------------------------------------------------------------------

/// DELETE v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Product>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Product =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/products:import
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/products:import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsImportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/products:import",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/products
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/products.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ListProductsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}/products", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.read_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("readMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListProductsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+name}
// -----------------------------------------------------------------------------

/// PATCH v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Product>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_missing {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowMissing=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Product =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/products:purge
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/products:purge.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_purge_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_purge_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsPurgeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/products:purge",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+product}:removeFulfillmentPlaces
// -----------------------------------------------------------------------------

/// POST v2/{+product}:removeFulfillmentPlaces.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_remove_fulfillment_places_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsRemoveFulfillmentPlacesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:removeFulfillmentPlaces",
        args.product,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+product}:removeLocalInventories
// -----------------------------------------------------------------------------

/// POST v2/{+product}:removeLocalInventories.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_remove_local_inventories_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_remove_local_inventories_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsRemoveLocalInventoriesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:removeLocalInventories",
        args.product,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:setInventory
// -----------------------------------------------------------------------------

/// POST v2/{+name}:setInventory.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_branches_products_set_inventory_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_branches_products_set_inventory_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsBranchesProductsSetInventoryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:setInventory",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/completionData:import
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/completionData:import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_completion_data_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_completion_data_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsCompletionDataImportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/completionData:import",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/controls
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/controls.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_controls_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_controls_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsControlsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Control>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}/controls", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.control_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("controlId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Control =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2/{+name}
// -----------------------------------------------------------------------------

/// DELETE v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_controls_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_controls_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsControlsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_controls_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_controls_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsControlsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Control>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Control =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/controls
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/controls.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_controls_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_controls_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsControlsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ListControlsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}/controls", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListControlsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+name}
// -----------------------------------------------------------------------------

/// PATCH v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_controls_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_controls_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsControlsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Control>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Control =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/generativeQuestion:batchUpdate
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/generativeQuestion:batchUpdate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_generative_question_batch_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_generative_question_batch_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsGenerativeQuestionBatchUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/generativeQuestion:batchUpdate",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2BatchUpdateGenerativeQuestionConfigsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/generativeQuestions
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/generativeQuestions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_generative_questions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_generative_questions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsGenerativeQuestionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/generativeQuestions",
        args.parent,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListGenerativeQuestionConfigsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/models
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/models.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_models_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_models_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsModelsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}/models", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.dry_run {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dryRun=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2/{+name}
// -----------------------------------------------------------------------------

/// DELETE v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_models_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_models_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsModelsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_models_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_models_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsModelsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Model>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Model =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/models
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/models.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_models_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_models_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsModelsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ListModelsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}/models", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListModelsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+name}
// -----------------------------------------------------------------------------

/// PATCH v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_models_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_models_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsModelsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Model>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Model =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:pause
// -----------------------------------------------------------------------------

/// POST v2/{+name}:pause.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_models_pause_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_models_pause_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsModelsPauseArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Model>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}:pause", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Model =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:resume
// -----------------------------------------------------------------------------

/// POST v2/{+name}:resume.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_models_resume_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_models_resume_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsModelsResumeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2Model>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}:resume", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2Model =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:tune
// -----------------------------------------------------------------------------

/// POST v2/{+name}:tune.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_models_tune_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_models_tune_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsModelsTuneArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}:tune", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}/operations
// -----------------------------------------------------------------------------

/// GET v2/{+name}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleLongrunningListOperationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}/operations", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+placement}:conversationalSearch
// -----------------------------------------------------------------------------

/// POST v2/{+placement}:conversationalSearch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_placements_conversational_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_placements_conversational_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsPlacementsConversationalSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ConversationalSearchResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:conversationalSearch",
        args.placement,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ConversationalSearchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+placement}:predict
// -----------------------------------------------------------------------------

/// POST v2/{+placement}:predict.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_placements_predict_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_placements_predict_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsPlacementsPredictArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2PredictResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:predict",
        args.placement,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2PredictResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+placement}:search
// -----------------------------------------------------------------------------

/// POST v2/{+placement}:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_placements_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_placements_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsPlacementsSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2SearchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}:search", args.placement,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2SearchResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+servingConfig}:addControl
// -----------------------------------------------------------------------------

/// POST v2/{+servingConfig}:addControl.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_serving_configs_add_control_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_serving_configs_add_control_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsServingConfigsAddControlArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:addControl",
        args.serving_config,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ServingConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+placement}:conversationalSearch
// -----------------------------------------------------------------------------

/// POST v2/{+placement}:conversationalSearch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_serving_configs_conversational_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_serving_configs_conversational_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsServingConfigsConversationalSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ConversationalSearchResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:conversationalSearch",
        args.placement,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ConversationalSearchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/servingConfigs
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/servingConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_serving_configs_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_serving_configs_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsServingConfigsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/servingConfigs",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.serving_config_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("servingConfigId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ServingConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2/{+name}
// -----------------------------------------------------------------------------

/// DELETE v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_serving_configs_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_serving_configs_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsServingConfigsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_serving_configs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_serving_configs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsServingConfigsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ServingConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/servingConfigs
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/servingConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_serving_configs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_serving_configs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsServingConfigsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2ListServingConfigsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/servingConfigs",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ListServingConfigsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+name}
// -----------------------------------------------------------------------------

/// PATCH v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_serving_configs_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_serving_configs_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsServingConfigsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ServingConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+placement}:predict
// -----------------------------------------------------------------------------

/// POST v2/{+placement}:predict.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_serving_configs_predict_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_serving_configs_predict_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsServingConfigsPredictArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRetailV2PredictResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:predict",
        args.placement,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2PredictResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+servingConfig}:removeControl
// -----------------------------------------------------------------------------

/// POST v2/{+servingConfig}:removeControl.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_serving_configs_remove_control_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_serving_configs_remove_control_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsServingConfigsRemoveControlArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2ServingConfig>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}:removeControl",
        args.serving_config,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2ServingConfig = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+placement}:search
// -----------------------------------------------------------------------------

/// POST v2/{+placement}:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_serving_configs_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_serving_configs_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsServingConfigsSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2SearchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}:search", args.placement,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2SearchResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/userEvents:collect
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/userEvents:collect.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_user_events_collect_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_user_events_collect_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsUserEventsCollectArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleApiHttpBody>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/userEvents:collect",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/userEvents:import
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/userEvents:import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_user_events_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_user_events_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsUserEventsImportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/userEvents:import",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/userEvents:purge
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/userEvents:purge.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_user_events_purge_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_user_events_purge_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsUserEventsPurgeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/userEvents:purge",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/userEvents:rejoin
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/userEvents:rejoin.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_user_events_rejoin_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_user_events_rejoin_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsUserEventsRejoinArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/userEvents:rejoin",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/userEvents:write
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/userEvents:write.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_catalogs_user_events_write_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_catalogs_user_events_write_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsCatalogsUserEventsWriteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRetailV2UserEvent>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://retail.googleapis.com/v2/{}/userEvents:write",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.write_async {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("writeAsync=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRetailV2UserEvent =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}/operations
// -----------------------------------------------------------------------------

/// GET v2/{+name}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_locations_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_locations_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsLocationsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleLongrunningListOperationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}/operations", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}/operations
// -----------------------------------------------------------------------------

/// GET v2/{+name}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = retail_projects_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn retail_projects_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RetailProjectsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleLongrunningListOperationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://retail.googleapis.com/v2/{}/operations", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
