//! Auto-generated API module for gcp/containeranalysis containeranalysis.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_containeranalysis_containeranalysis `

#![cfg(feature = "gcp_containeranalysis_containeranalysis")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `DSSEHint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DSSEHint {
    /// humanReadableName property.
    pub human_readable_name: Option<String>,
}

/// `KnowledgeBase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct KnowledgeBase {
    /// name property.
    pub name: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `CloudRepoSourceContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CloudRepoSourceContext {
    /// aliasContext property.
    pub alias_context: Option<AliasContext>,
    /// repoId property.
    pub repo_id: Option<RepoId>,
    /// revisionId property.
    pub revision_id: Option<String>,
}

/// `ExportSBOMRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExportSBOMRequest {
    /// cloudStorageLocation property.
    pub cloud_storage_location: Option<CloudStorageLocation>,
}

/// `Source` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Source {
    /// additionalContexts property.
    pub additional_contexts: Option<Vec<SourceContext>>,
    /// artifactStorageSourceUri property.
    pub artifact_storage_source_uri: Option<String>,
    /// context property.
    pub context: Option<SourceContext>,
    /// fileHashes property.
    pub file_hashes: Option<serde_json::Value>,
}

/// `Binding` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `DeploymentNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeploymentNote {
    /// resourceUri property.
    pub resource_uri: Option<Vec<String>>,
}

/// `VulnerabilityAssessmentNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VulnerabilityAssessmentNote {
    /// assessment property.
    pub assessment: Option<Assessment>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// longDescription property.
    pub long_description: Option<String>,
    /// product property.
    pub product: Option<Product>,
    /// publisher property.
    pub publisher: Option<Publisher>,
    /// shortDescription property.
    pub short_description: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `WindowsDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WindowsDetail {
    /// cpeUri property.
    pub cpe_uri: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fixingKbs property.
    pub fixing_kbs: Option<Vec<KnowledgeBase>>,
    /// name property.
    pub name: Option<String>,
}

/// `BuildNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuildNote {
    /// builderVersion property.
    pub builder_version: Option<String>,
}

/// `Material` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Material {
    /// digest property.
    pub digest: Option<serde_json::Value>,
    /// uri property.
    pub uri: Option<String>,
}

/// `Version` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Version {
    /// epoch property.
    pub epoch: Option<i64>,
    /// fullName property.
    pub full_name: Option<String>,
    /// inclusive property.
    pub inclusive: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// revision property.
    pub revision: Option<String>,
}

/// `BatchCreateOccurrencesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BatchCreateOccurrencesResponse {
    /// occurrences property.
    pub occurrences: Option<Vec<Occurrence>>,
}

/// `ImageOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ImageOccurrence {
    /// baseResourceUrl property.
    pub base_resource_url: Option<String>,
    /// distance property.
    pub distance: Option<i64>,
    /// fingerprint property.
    pub fingerprint: Option<Fingerprint>,
    /// layerInfo property.
    pub layer_info: Option<Vec<Layer>>,
}

/// `DeploymentOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeploymentOccurrence {
    /// address property.
    pub address: Option<String>,
    /// config property.
    pub config: Option<String>,
    /// deployTime property.
    pub deploy_time: Option<String>,
    /// platform property.
    pub platform: Option<String>,
    /// resourceUri property.
    pub resource_uri: Option<Vec<String>>,
    /// undeployTime property.
    pub undeploy_time: Option<String>,
    /// userEmail property.
    pub user_email: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `GerritSourceContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GerritSourceContext {
    /// aliasContext property.
    pub alias_context: Option<AliasContext>,
    /// gerritProject property.
    pub gerrit_project: Option<String>,
    /// hostUri property.
    pub host_uri: Option<String>,
    /// revisionId property.
    pub revision_id: Option<String>,
}

/// `ProjectRepoId` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProjectRepoId {
    /// projectId property.
    pub project_id: Option<String>,
    /// repoName property.
    pub repo_name: Option<String>,
}

/// `ResourceDescriptor` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceDescriptor {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// content property.
    pub content: Option<String>,
    /// digest property.
    pub digest: Option<serde_json::Value>,
    /// downloadLocation property.
    pub download_location: Option<String>,
    /// mediaType property.
    pub media_type: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// uri property.
    pub uri: Option<String>,
}

/// `DiscoveryNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiscoveryNote {
    /// analysisKind property.
    pub analysis_kind: Option<String>,
}

/// `SourceContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceContext {
    /// cloudRepo property.
    pub cloud_repo: Option<CloudRepoSourceContext>,
    /// gerrit property.
    pub gerrit: Option<GerritSourceContext>,
    /// git property.
    pub git: Option<GitSourceContext>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `SlsaBuilder` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SlsaBuilder {
    /// id property.
    pub id: Option<String>,
}

/// `ComplianceOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComplianceOccurrence {
    /// nonComplianceReason property.
    pub non_compliance_reason: Option<String>,
    /// nonCompliantFiles property.
    pub non_compliant_files: Option<Vec<NonCompliantFile>>,
    /// version property.
    pub version: Option<ComplianceVersion>,
}

/// `SlsaProvenance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SlsaProvenance {
    /// builder property.
    pub builder: Option<SlsaBuilder>,
    /// materials property.
    pub materials: Option<Vec<Material>>,
    /// metadata property.
    pub metadata: Option<SlsaMetadata>,
    /// recipe property.
    pub recipe: Option<SlsaRecipe>,
}

/// `SlsaProvenanceV1` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SlsaProvenanceV1 {
    /// buildDefinition property.
    pub build_definition: Option<BuildDefinition>,
    /// runDetails property.
    pub run_details: Option<RunDetails>,
}

/// `DiscoveryOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiscoveryOccurrence {
    /// analysisCompleted property.
    pub analysis_completed: Option<AnalysisCompleted>,
    /// analysisError property.
    pub analysis_error: Option<Vec<Status>>,
    /// analysisStatus property.
    pub analysis_status: Option<String>,
    /// analysisStatusError property.
    pub analysis_status_error: Option<Status>,
    /// archiveTime property.
    pub archive_time: Option<String>,
    /// continuousAnalysis property.
    pub continuous_analysis: Option<String>,
    /// cpe property.
    pub cpe: Option<String>,
    /// files property.
    pub files: Option<Vec<File>>,
    /// lastScanTime property.
    pub last_scan_time: Option<String>,
    /// lastVulnerabilityUpdateTime property.
    pub last_vulnerability_update_time: Option<String>,
    /// sbomStatus property.
    pub sbom_status: Option<SBOMStatus>,
}

/// `CVSS` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CVSS {
    /// attackComplexity property.
    pub attack_complexity: Option<String>,
    /// attackVector property.
    pub attack_vector: Option<String>,
    /// authentication property.
    pub authentication: Option<String>,
    /// availabilityImpact property.
    pub availability_impact: Option<String>,
    /// baseScore property.
    pub base_score: Option<f64>,
    /// confidentialityImpact property.
    pub confidentiality_impact: Option<String>,
    /// exploitabilityScore property.
    pub exploitability_score: Option<f64>,
    /// impactScore property.
    pub impact_score: Option<f64>,
    /// integrityImpact property.
    pub integrity_impact: Option<String>,
    /// privilegesRequired property.
    pub privileges_required: Option<String>,
    /// scope property.
    pub scope: Option<String>,
    /// userInteraction property.
    pub user_interaction: Option<String>,
}

/// `DSSEAttestationOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DSSEAttestationOccurrence {
    /// envelope property.
    pub envelope: Option<Envelope>,
    /// statement property.
    pub statement: Option<InTotoStatement>,
}

/// `InTotoStatement` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InTotoStatement {
    /// _type property.
    pub r#type: Option<String>,
    /// predicateType property.
    pub predicate_type: Option<String>,
    /// provenance property.
    pub provenance: Option<InTotoProvenance>,
    /// slsaProvenance property.
    pub slsa_provenance: Option<SlsaProvenance>,
    /// slsaProvenanceZeroTwo property.
    pub slsa_provenance_zero_two: Option<SlsaProvenanceZeroTwo>,
    /// subject property.
    pub subject: Option<Vec<Subject>>,
}

/// `Identity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Identity {
    /// revision property.
    pub revision: Option<i64>,
    /// updateId property.
    pub update_id: Option<String>,
}

/// `SBOMReferenceOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SBOMReferenceOccurrence {
    /// payload property.
    pub payload: Option<SbomReferenceIntotoPayload>,
    /// payloadType property.
    pub payload_type: Option<String>,
    /// signatures property.
    pub signatures: Option<Vec<EnvelopeSignature>>,
}

/// `SlsaRecipe` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SlsaRecipe {
    /// arguments property.
    pub arguments: Option<serde_json::Value>,
    /// definedInMaterial property.
    pub defined_in_material: Option<String>,
    /// entryPoint property.
    pub entry_point: Option<String>,
    /// environment property.
    pub environment: Option<serde_json::Value>,
    /// type property.
    pub r#type: Option<String>,
}

/// `RunDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RunDetails {
    /// builder property.
    pub builder: Option<ProvenanceBuilder>,
    /// byproducts property.
    pub byproducts: Option<Vec<ResourceDescriptor>>,
    /// metadata property.
    pub metadata: Option<BuildMetadata>,
}

/// `SlsaMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SlsaMetadata {
    /// buildFinishedOn property.
    pub build_finished_on: Option<String>,
    /// buildInvocationId property.
    pub build_invocation_id: Option<String>,
    /// buildStartedOn property.
    pub build_started_on: Option<String>,
    /// completeness property.
    pub completeness: Option<SlsaCompleteness>,
    /// reproducible property.
    pub reproducible: Option<bool>,
}

/// `CloudStorageLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CloudStorageLocation {}

/// `GetPolicyOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetPolicyOptions {
    /// requestedPolicyVersion property.
    pub requested_policy_version: Option<i64>,
}

/// `PackageIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PackageIssue {
    /// affectedCpeUri property.
    pub affected_cpe_uri: Option<String>,
    /// affectedPackage property.
    pub affected_package: Option<String>,
    /// affectedVersion property.
    pub affected_version: Option<Version>,
    /// effectiveSeverity property.
    pub effective_severity: Option<String>,
    /// fileLocation property.
    pub file_location: Option<Vec<GrafeasV1FileLocation>>,
    /// fixAvailable property.
    pub fix_available: Option<bool>,
    /// fixedCpeUri property.
    pub fixed_cpe_uri: Option<String>,
    /// fixedPackage property.
    pub fixed_package: Option<String>,
    /// fixedVersion property.
    pub fixed_version: Option<Version>,
    /// packageType property.
    pub package_type: Option<String>,
}

/// `WindowsUpdate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WindowsUpdate {
    /// categories property.
    pub categories: Option<Vec<Category>>,
    /// description property.
    pub description: Option<String>,
    /// identity property.
    pub identity: Option<Identity>,
    /// kbArticleIds property.
    pub kb_article_ids: Option<Vec<String>>,
    /// lastPublishedTimestamp property.
    pub last_published_timestamp: Option<String>,
    /// supportUrl property.
    pub support_url: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `AnalysisCompleted` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnalysisCompleted {
    /// analysisType property.
    pub analysis_type: Option<Vec<String>>,
}

/// `GrafeasV1SlsaProvenanceZeroTwoSlsaBuilder` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GrafeasV1SlsaProvenanceZeroTwoSlsaBuilder {
    /// id property.
    pub id: Option<String>,
}

/// `GrafeasV1SlsaProvenanceZeroTwoSlsaMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GrafeasV1SlsaProvenanceZeroTwoSlsaMetadata {
    /// buildFinishedOn property.
    pub build_finished_on: Option<String>,
    /// buildInvocationId property.
    pub build_invocation_id: Option<String>,
    /// buildStartedOn property.
    pub build_started_on: Option<String>,
    /// completeness property.
    pub completeness: Option<GrafeasV1SlsaProvenanceZeroTwoSlsaCompleteness>,
    /// reproducible property.
    pub reproducible: Option<bool>,
}

/// `File` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct File {
    /// digest property.
    pub digest: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
}

/// `BatchCreateNotesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BatchCreateNotesRequest {
    /// notes property.
    pub notes: Option<serde_json::Value>,
}

/// `GrafeasV1SlsaProvenanceZeroTwoSlsaInvocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GrafeasV1SlsaProvenanceZeroTwoSlsaInvocation {
    /// configSource property.
    pub config_source: Option<GrafeasV1SlsaProvenanceZeroTwoSlsaConfigSource>,
    /// environment property.
    pub environment: Option<serde_json::Value>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
}

/// `GrafeasV1FileLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GrafeasV1FileLocation {
    /// filePath property.
    pub file_path: Option<String>,
    /// layerDetails property.
    pub layer_details: Option<LayerDetails>,
    /// lineNumber property.
    pub line_number: Option<i64>,
}

/// `AttestationNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AttestationNote {
    /// hint property.
    pub hint: Option<Hint>,
}

/// `UpgradeNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpgradeNote {
    /// distributions property.
    pub distributions: Option<Vec<UpgradeDistribution>>,
    /// package property.
    pub package: Option<String>,
    /// version property.
    pub version: Option<Version>,
    /// windowsUpdate property.
    pub windows_update: Option<WindowsUpdate>,
}

/// `SecretStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecretStatus {
    /// message property.
    pub message: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `GrafeasV1SlsaProvenanceZeroTwoSlsaMaterial` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GrafeasV1SlsaProvenanceZeroTwoSlsaMaterial {
    /// digest property.
    pub digest: Option<serde_json::Value>,
    /// uri property.
    pub uri: Option<String>,
}

/// `Signature` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Signature {
    /// publicKeyId property.
    pub public_key_id: Option<String>,
    /// signature property.
    pub signature: Option<String>,
}

/// `SecretOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecretOccurrence {
    /// kind property.
    pub kind: Option<String>,
    /// locations property.
    pub locations: Option<Vec<SecretLocation>>,
    /// statuses property.
    pub statuses: Option<Vec<SecretStatus>>,
}

/// `VexAssessment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VexAssessment {
    /// cve property.
    pub cve: Option<String>,
    /// impacts property.
    pub impacts: Option<Vec<String>>,
    /// justification property.
    pub justification: Option<Justification>,
    /// noteName property.
    pub note_name: Option<String>,
    /// relatedUris property.
    pub related_uris: Option<Vec<RelatedUrl>>,
    /// remediations property.
    pub remediations: Option<Vec<Remediation>>,
    /// state property.
    pub state: Option<String>,
    /// vulnerabilityId property.
    pub vulnerability_id: Option<String>,
}

/// `InTotoSlsaProvenanceV1` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InTotoSlsaProvenanceV1 {
    /// _type property.
    pub r#type: Option<String>,
    /// predicate property.
    pub predicate: Option<SlsaProvenanceV1>,
    /// predicateType property.
    pub predicate_type: Option<String>,
    /// subject property.
    pub subject: Option<Vec<Subject>>,
}

/// `EnvelopeSignature` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EnvelopeSignature {
    /// keyid property.
    pub keyid: Option<String>,
    /// sig property.
    pub sig: Option<String>,
}

/// `Occurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Occurrence {
    /// advisoryPublishTime property.
    pub advisory_publish_time: Option<String>,
    /// attestation property.
    pub attestation: Option<AttestationOccurrence>,
    /// build property.
    pub build: Option<BuildOccurrence>,
    /// compliance property.
    pub compliance: Option<ComplianceOccurrence>,
    /// createTime property.
    pub create_time: Option<String>,
    /// deployment property.
    pub deployment: Option<DeploymentOccurrence>,
    /// discovery property.
    pub discovery: Option<DiscoveryOccurrence>,
    /// dsseAttestation property.
    pub dsse_attestation: Option<DSSEAttestationOccurrence>,
    /// envelope property.
    pub envelope: Option<Envelope>,
    /// image property.
    pub image: Option<ImageOccurrence>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// noteName property.
    pub note_name: Option<String>,
    /// package property.
    pub package: Option<PackageOccurrence>,
    /// remediation property.
    pub remediation: Option<String>,
    /// resourceUri property.
    pub resource_uri: Option<String>,
    /// sbomReference property.
    pub sbom_reference: Option<SBOMReferenceOccurrence>,
    /// secret property.
    pub secret: Option<SecretOccurrence>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// upgrade property.
    pub upgrade: Option<UpgradeOccurrence>,
    /// vulnerability property.
    pub vulnerability: Option<VulnerabilityOccurrence>,
}

/// `BuildOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuildOccurrence {
    /// inTotoSlsaProvenanceV1 property.
    pub in_toto_slsa_provenance_v1: Option<InTotoSlsaProvenanceV1>,
    /// intotoProvenance property.
    pub intoto_provenance: Option<InTotoProvenance>,
    /// intotoStatement property.
    pub intoto_statement: Option<InTotoStatement>,
    /// provenance property.
    pub provenance: Option<BuildProvenance>,
    /// provenanceBytes property.
    pub provenance_bytes: Option<String>,
}

/// `GrafeasV1SlsaProvenanceZeroTwoSlsaCompleteness` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GrafeasV1SlsaProvenanceZeroTwoSlsaCompleteness {
    /// environment property.
    pub environment: Option<bool>,
    /// materials property.
    pub materials: Option<bool>,
    /// parameters property.
    pub parameters: Option<bool>,
}

/// `GrafeasV1SlsaProvenanceZeroTwoSlsaConfigSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GrafeasV1SlsaProvenanceZeroTwoSlsaConfigSource {
    /// digest property.
    pub digest: Option<serde_json::Value>,
    /// entryPoint property.
    pub entry_point: Option<String>,
    /// uri property.
    pub uri: Option<String>,
}

/// `BatchCreateNotesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BatchCreateNotesResponse {
    /// notes property.
    pub notes: Option<Vec<Note>>,
}

/// `Policy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Policy {
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// version property.
    pub version: Option<i64>,
}

/// `ComplianceNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComplianceNote {
    /// cisBenchmark property.
    pub cis_benchmark: Option<CisBenchmark>,
    /// description property.
    pub description: Option<String>,
    /// impact property.
    pub impact: Option<String>,
    /// rationale property.
    pub rationale: Option<String>,
    /// remediation property.
    pub remediation: Option<String>,
    /// scanInstructions property.
    pub scan_instructions: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// version property.
    pub version: Option<Vec<ComplianceVersion>>,
}

/// `Command` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Command {
    /// args property.
    pub args: Option<Vec<String>>,
    /// dir property.
    pub dir: Option<String>,
    /// env property.
    pub env: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// waitFor property.
    pub wait_for: Option<Vec<String>>,
}

/// `UpgradeDistribution` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpgradeDistribution {
    /// classification property.
    pub classification: Option<String>,
    /// cpeUri property.
    pub cpe_uri: Option<String>,
    /// cve property.
    pub cve: Option<Vec<String>>,
    /// severity property.
    pub severity: Option<String>,
}

/// `SBOMReferenceNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SBOMReferenceNote {
    /// format property.
    pub format: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `FixableTotalByDigest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FixableTotalByDigest {
    /// fixableCount property.
    pub fixable_count: Option<String>,
    /// resourceUri property.
    pub resource_uri: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// totalCount property.
    pub total_count: Option<String>,
}

/// `BaseImage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BaseImage {
    /// layerCount property.
    pub layer_count: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// registry property.
    pub registry: Option<String>,
    /// repository property.
    pub repository: Option<String>,
}

/// `BuildProvenance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuildProvenance {
    /// buildOptions property.
    pub build_options: Option<serde_json::Value>,
    /// builderVersion property.
    pub builder_version: Option<String>,
    /// builtArtifacts property.
    pub built_artifacts: Option<Vec<Artifact>>,
    /// commands property.
    pub commands: Option<Vec<Command>>,
    /// createTime property.
    pub create_time: Option<String>,
    /// creator property.
    pub creator: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// logsUri property.
    pub logs_uri: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// sourceProvenance property.
    pub source_provenance: Option<Source>,
    /// startTime property.
    pub start_time: Option<String>,
    /// triggerId property.
    pub trigger_id: Option<String>,
}

/// `SlsaCompleteness` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SlsaCompleteness {
    /// arguments property.
    pub arguments: Option<bool>,
    /// environment property.
    pub environment: Option<bool>,
    /// materials property.
    pub materials: Option<bool>,
}

/// `Completeness` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Completeness {
    /// arguments property.
    pub arguments: Option<bool>,
    /// environment property.
    pub environment: Option<bool>,
    /// materials property.
    pub materials: Option<bool>,
}

/// `CVSSv3` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CVSSv3 {
    /// attackComplexity property.
    pub attack_complexity: Option<String>,
    /// attackVector property.
    pub attack_vector: Option<String>,
    /// availabilityImpact property.
    pub availability_impact: Option<String>,
    /// baseScore property.
    pub base_score: Option<f64>,
    /// confidentialityImpact property.
    pub confidentiality_impact: Option<String>,
    /// exploitabilityScore property.
    pub exploitability_score: Option<f64>,
    /// impactScore property.
    pub impact_score: Option<f64>,
    /// integrityImpact property.
    pub integrity_impact: Option<String>,
    /// privilegesRequired property.
    pub privileges_required: Option<String>,
    /// scope property.
    pub scope: Option<String>,
    /// userInteraction property.
    pub user_interaction: Option<String>,
}

/// `SbomReferenceIntotoPayload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SbomReferenceIntotoPayload {
    /// _type property.
    pub r#type: Option<String>,
    /// predicate property.
    pub predicate: Option<SbomReferenceIntotoPredicate>,
    /// predicateType property.
    pub predicate_type: Option<String>,
    /// subject property.
    pub subject: Option<Vec<Subject>>,
}

/// `ComplianceVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComplianceVersion {
    /// benchmarkDocument property.
    pub benchmark_document: Option<String>,
    /// cpeUri property.
    pub cpe_uri: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `ListNotesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListNotesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// notes property.
    pub notes: Option<Vec<Note>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `LayerDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LayerDetails {
    /// baseImages property.
    pub base_images: Option<Vec<BaseImage>>,
    /// chainId property.
    pub chain_id: Option<String>,
    /// command property.
    pub command: Option<String>,
    /// diffId property.
    pub diff_id: Option<String>,
    /// index property.
    pub index: Option<i64>,
}

/// `Distribution` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Distribution {
    /// architecture property.
    pub architecture: Option<String>,
    /// cpeUri property.
    pub cpe_uri: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// latestVersion property.
    pub latest_version: Option<Version>,
    /// maintainer property.
    pub maintainer: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `SetIamPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetIamPolicyRequest {
    /// policy property.
    pub policy: Option<Policy>,
}

/// `ExploitPredictionScoringSystem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExploitPredictionScoringSystem {
    /// percentile property.
    pub percentile: Option<f64>,
    /// score property.
    pub score: Option<f64>,
}

/// `Category` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Category {
    /// categoryId property.
    pub category_id: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `InTotoProvenance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InTotoProvenance {
    /// builderConfig property.
    pub builder_config: Option<BuilderConfig>,
    /// materials property.
    pub materials: Option<Vec<String>>,
    /// metadata property.
    pub metadata: Option<Metadata>,
    /// recipe property.
    pub recipe: Option<Recipe>,
}

/// `Expr` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `SecretLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecretLocation {
    /// fileLocation property.
    pub file_location: Option<GrafeasV1FileLocation>,
}

/// `BatchCreateOccurrencesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BatchCreateOccurrencesRequest {
    /// occurrences property.
    pub occurrences: Option<Vec<Occurrence>>,
}

/// `Detail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Detail {
    /// affectedCpeUri property.
    pub affected_cpe_uri: Option<String>,
    /// affectedPackage property.
    pub affected_package: Option<String>,
    /// affectedVersionEnd property.
    pub affected_version_end: Option<Version>,
    /// affectedVersionStart property.
    pub affected_version_start: Option<Version>,
    /// description property.
    pub description: Option<String>,
    /// fixedCpeUri property.
    pub fixed_cpe_uri: Option<String>,
    /// fixedPackage property.
    pub fixed_package: Option<String>,
    /// fixedVersion property.
    pub fixed_version: Option<Version>,
    /// isObsolete property.
    pub is_obsolete: Option<bool>,
    /// packageType property.
    pub package_type: Option<String>,
    /// severityName property.
    pub severity_name: Option<String>,
    /// source property.
    pub source: Option<String>,
    /// sourceUpdateTime property.
    pub source_update_time: Option<String>,
    /// vendor property.
    pub vendor: Option<String>,
}

/// `License` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct License {
    /// comments property.
    pub comments: Option<String>,
    /// expression property.
    pub expression: Option<String>,
}

/// `Justification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Justification {
    /// details property.
    pub details: Option<String>,
    /// justificationType property.
    pub justification_type: Option<String>,
}

/// `ProvenanceBuilder` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProvenanceBuilder {
    /// builderDependencies property.
    pub builder_dependencies: Option<Vec<ResourceDescriptor>>,
    /// id property.
    pub id: Option<String>,
    /// version property.
    pub version: Option<serde_json::Value>,
}

/// `SecretNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecretNote {}

/// `CisBenchmark` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CisBenchmark {
    /// profileLevel property.
    pub profile_level: Option<i64>,
    /// severity property.
    pub severity: Option<String>,
}

/// `Hint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Hint {
    /// humanReadableName property.
    pub human_readable_name: Option<String>,
}

/// `TestIamPermissionsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestIamPermissionsRequest {
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `DSSEAttestationNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DSSEAttestationNote {
    /// hint property.
    pub hint: Option<DSSEHint>,
}

/// `Subject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Subject {
    /// digest property.
    pub digest: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
}

/// `SlsaProvenanceZeroTwo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SlsaProvenanceZeroTwo {
    /// buildConfig property.
    pub build_config: Option<serde_json::Value>,
    /// buildType property.
    pub build_type: Option<String>,
    /// builder property.
    pub builder: Option<GrafeasV1SlsaProvenanceZeroTwoSlsaBuilder>,
    /// invocation property.
    pub invocation: Option<GrafeasV1SlsaProvenanceZeroTwoSlsaInvocation>,
    /// materials property.
    pub materials: Option<Vec<GrafeasV1SlsaProvenanceZeroTwoSlsaMaterial>>,
    /// metadata property.
    pub metadata: Option<GrafeasV1SlsaProvenanceZeroTwoSlsaMetadata>,
}

/// `Metadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metadata {
    /// buildFinishedOn property.
    pub build_finished_on: Option<String>,
    /// buildInvocationId property.
    pub build_invocation_id: Option<String>,
    /// buildStartedOn property.
    pub build_started_on: Option<String>,
    /// completeness property.
    pub completeness: Option<Completeness>,
    /// reproducible property.
    pub reproducible: Option<bool>,
}

/// `ImageNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ImageNote {
    /// fingerprint property.
    pub fingerprint: Option<Fingerprint>,
    /// resourceUrl property.
    pub resource_url: Option<String>,
}

/// `PackageNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PackageNote {
    /// architecture property.
    pub architecture: Option<String>,
    /// cpeUri property.
    pub cpe_uri: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// digest property.
    pub digest: Option<Vec<Digest>>,
    /// distribution property.
    pub distribution: Option<Vec<Distribution>>,
    /// license property.
    pub license: Option<License>,
    /// maintainer property.
    pub maintainer: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// packageType property.
    pub package_type: Option<String>,
    /// url property.
    pub url: Option<String>,
    /// version property.
    pub version: Option<Version>,
}

/// `GetIamPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetIamPolicyRequest {
    /// options property.
    pub options: Option<GetPolicyOptions>,
}

/// `VulnerabilityOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VulnerabilityOccurrence {
    /// cvssScore property.
    pub cvss_score: Option<f64>,
    /// cvssV2 property.
    pub cvss_v2: Option<CVSS>,
    /// cvssVersion property.
    pub cvss_version: Option<String>,
    /// cvssv3 property.
    pub cvssv3: Option<CVSS>,
    /// effectiveSeverity property.
    pub effective_severity: Option<String>,
    /// extraDetails property.
    pub extra_details: Option<String>,
    /// fixAvailable property.
    pub fix_available: Option<bool>,
    /// longDescription property.
    pub long_description: Option<String>,
    /// packageIssue property.
    pub package_issue: Option<Vec<PackageIssue>>,
    /// relatedUrls property.
    pub related_urls: Option<Vec<RelatedUrl>>,
    /// risk property.
    pub risk: Option<Risk>,
    /// severity property.
    pub severity: Option<String>,
    /// shortDescription property.
    pub short_description: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// vexAssessment property.
    pub vex_assessment: Option<VexAssessment>,
}

/// `Envelope` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Envelope {
    /// payload property.
    pub payload: Option<String>,
    /// payloadType property.
    pub payload_type: Option<String>,
    /// signatures property.
    pub signatures: Option<Vec<EnvelopeSignature>>,
}

/// `CISAKnownExploitedVulnerabilities` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CISAKnownExploitedVulnerabilities {
    /// knownRansomwareCampaignUse property.
    pub known_ransomware_campaign_use: Option<String>,
}

/// `VulnerabilityNote` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VulnerabilityNote {
    /// advisoryPublishTime property.
    pub advisory_publish_time: Option<String>,
    /// cvssScore property.
    pub cvss_score: Option<f64>,
    /// cvssV2 property.
    pub cvss_v2: Option<CVSS>,
    /// cvssV3 property.
    pub cvss_v3: Option<CVSSv3>,
    /// cvssVersion property.
    pub cvss_version: Option<String>,
    /// details property.
    pub details: Option<Vec<Detail>>,
    /// severity property.
    pub severity: Option<String>,
    /// sourceUpdateTime property.
    pub source_update_time: Option<String>,
    /// windowsDetails property.
    pub windows_details: Option<Vec<WindowsDetail>>,
}

/// `RelatedUrl` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RelatedUrl {
    /// label property.
    pub label: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `BuildMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuildMetadata {
    /// finishedOn property.
    pub finished_on: Option<String>,
    /// invocationId property.
    pub invocation_id: Option<String>,
    /// startedOn property.
    pub started_on: Option<String>,
}

/// `Publisher` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Publisher {
    /// issuingAuthority property.
    pub issuing_authority: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// publisherNamespace property.
    pub publisher_namespace: Option<String>,
}

/// `Product` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Product {
    /// genericUri property.
    pub generic_uri: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `VulnerabilityOccurrencesSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VulnerabilityOccurrencesSummary {
    /// counts property.
    pub counts: Option<Vec<FixableTotalByDigest>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `Artifact` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Artifact {
    /// checksum property.
    pub checksum: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// names property.
    pub names: Option<Vec<String>>,
}

/// `PackageOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PackageOccurrence {
    /// architecture property.
    pub architecture: Option<String>,
    /// cpeUri property.
    pub cpe_uri: Option<String>,
    /// license property.
    pub license: Option<License>,
    /// location property.
    pub location: Option<Vec<Location>>,
    /// name property.
    pub name: Option<String>,
    /// packageType property.
    pub package_type: Option<String>,
    /// version property.
    pub version: Option<Version>,
}

/// `GitSourceContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GitSourceContext {
    /// revisionId property.
    pub revision_id: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `ListNoteOccurrencesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListNoteOccurrencesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// occurrences property.
    pub occurrences: Option<Vec<Occurrence>>,
}

/// `Fingerprint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Fingerprint {
    /// v1Name property.
    pub v1_name: Option<String>,
    /// v2Blob property.
    pub v2_blob: Option<Vec<String>>,
    /// v2Name property.
    pub v2_name: Option<String>,
}

/// `BuildDefinition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuildDefinition {
    /// buildType property.
    pub build_type: Option<String>,
    /// externalParameters property.
    pub external_parameters: Option<serde_json::Value>,
    /// internalParameters property.
    pub internal_parameters: Option<serde_json::Value>,
    /// resolvedDependencies property.
    pub resolved_dependencies: Option<Vec<ResourceDescriptor>>,
}

/// `Digest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Digest {
    /// algo property.
    pub algo: Option<String>,
    /// digestBytes property.
    pub digest_bytes: Option<String>,
}

/// `Remediation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Remediation {
    /// details property.
    pub details: Option<String>,
    /// remediationType property.
    pub remediation_type: Option<String>,
    /// remediationUri property.
    pub remediation_uri: Option<RelatedUrl>,
}

/// `NonCompliantFile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NonCompliantFile {
    /// displayCommand property.
    pub display_command: Option<String>,
    /// path property.
    pub path: Option<String>,
    /// reason property.
    pub reason: Option<String>,
}

/// `Jwt` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Jwt {
    /// compactJwt property.
    pub compact_jwt: Option<String>,
}

/// `ExportSBOMResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExportSBOMResponse {
    /// discoveryOccurrence property.
    pub discovery_occurrence: Option<String>,
}

/// `SBOMStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SBOMStatus {
    /// error property.
    pub error: Option<String>,
    /// sbomState property.
    pub sbom_state: Option<String>,
}

/// `Location` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Location {
    /// cpeUri property.
    pub cpe_uri: Option<String>,
    /// path property.
    pub path: Option<String>,
    /// version property.
    pub version: Option<Version>,
}

/// `AttestationOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AttestationOccurrence {
    /// jwts property.
    pub jwts: Option<Vec<Jwt>>,
    /// serializedPayload property.
    pub serialized_payload: Option<String>,
    /// signatures property.
    pub signatures: Option<Vec<Signature>>,
}

/// `TestIamPermissionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestIamPermissionsResponse {
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `Assessment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Assessment {
    /// cve property.
    pub cve: Option<String>,
    /// impacts property.
    pub impacts: Option<Vec<String>>,
    /// justification property.
    pub justification: Option<Justification>,
    /// longDescription property.
    pub long_description: Option<String>,
    /// relatedUris property.
    pub related_uris: Option<Vec<RelatedUrl>>,
    /// remediations property.
    pub remediations: Option<Vec<Remediation>>,
    /// shortDescription property.
    pub short_description: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// vulnerabilityId property.
    pub vulnerability_id: Option<String>,
}

/// `Note` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Note {
    /// attestation property.
    pub attestation: Option<AttestationNote>,
    /// build property.
    pub build: Option<BuildNote>,
    /// compliance property.
    pub compliance: Option<ComplianceNote>,
    /// createTime property.
    pub create_time: Option<String>,
    /// deployment property.
    pub deployment: Option<DeploymentNote>,
    /// discovery property.
    pub discovery: Option<DiscoveryNote>,
    /// dsseAttestation property.
    pub dsse_attestation: Option<DSSEAttestationNote>,
    /// expirationTime property.
    pub expiration_time: Option<String>,
    /// image property.
    pub image: Option<ImageNote>,
    /// kind property.
    pub kind: Option<String>,
    /// longDescription property.
    pub long_description: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// package property.
    pub package: Option<PackageNote>,
    /// relatedNoteNames property.
    pub related_note_names: Option<Vec<String>>,
    /// relatedUrl property.
    pub related_url: Option<Vec<RelatedUrl>>,
    /// sbomReference property.
    pub sbom_reference: Option<SBOMReferenceNote>,
    /// secret property.
    pub secret: Option<SecretNote>,
    /// shortDescription property.
    pub short_description: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// upgrade property.
    pub upgrade: Option<UpgradeNote>,
    /// vulnerability property.
    pub vulnerability: Option<VulnerabilityNote>,
    /// vulnerabilityAssessment property.
    pub vulnerability_assessment: Option<VulnerabilityAssessmentNote>,
}

/// `Layer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Layer {
    /// arguments property.
    pub arguments: Option<String>,
    /// directive property.
    pub directive: Option<String>,
}

/// `AliasContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AliasContext {
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `SbomReferenceIntotoPredicate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SbomReferenceIntotoPredicate {
    /// digest property.
    pub digest: Option<serde_json::Value>,
    /// location property.
    pub location: Option<String>,
    /// mimeType property.
    pub mime_type: Option<String>,
    /// referrerId property.
    pub referrer_id: Option<String>,
}

/// `ListOccurrencesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListOccurrencesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// occurrences property.
    pub occurrences: Option<Vec<Occurrence>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `Risk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Risk {
    /// cisaKev property.
    pub cisa_kev: Option<CISAKnownExploitedVulnerabilities>,
    /// epss property.
    pub epss: Option<ExploitPredictionScoringSystem>,
}

/// `RepoId` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RepoId {
    /// projectRepoId property.
    pub project_repo_id: Option<ProjectRepoId>,
    /// uid property.
    pub uid: Option<String>,
}

/// `Recipe` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Recipe {
    /// arguments property.
    pub arguments: Option<Vec<serde_json::Value>>,
    /// definedInMaterial property.
    pub defined_in_material: Option<String>,
    /// entryPoint property.
    pub entry_point: Option<String>,
    /// environment property.
    pub environment: Option<Vec<serde_json::Value>>,
    /// type property.
    pub r#type: Option<String>,
}

/// `BuilderConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuilderConfig {
    /// id property.
    pub id: Option<String>,
}

/// `UpgradeOccurrence` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpgradeOccurrence {
    /// distribution property.
    pub distribution: Option<UpgradeDistribution>,
    /// package property.
    pub package: Option<String>,
    /// parsedVersion property.
    pub parsed_version: Option<Version>,
    /// windowsUpdate property.
    pub windows_update: Option<WindowsUpdate>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`containeranalysis.projects.locations.notes.batchCreate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsNotesBatchCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: BatchCreateNotesRequest,
}

/// Arguments for [`containeranalysis.projects.locations.notes.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsNotesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `noteId`.
    pub note_id: Option<String>,
    /// Request body.
    pub body: Note,
}

/// Arguments for [`containeranalysis.projects.locations.notes.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsNotesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`containeranalysis.projects.locations.notes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsNotesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`containeranalysis.projects.locations.notes.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsNotesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// Arguments for [`containeranalysis.projects.locations.notes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsNotesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`containeranalysis.projects.locations.notes.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsNotesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Note,
}

/// Arguments for [`containeranalysis.projects.locations.notes.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsNotesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`containeranalysis.projects.locations.notes.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsNotesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`containeranalysis.projects.locations.notes.occurrences.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsNotesOccurrencesListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`containeranalysis.projects.locations.occurrences.batchCreate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsOccurrencesBatchCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: BatchCreateOccurrencesRequest,
}

/// Arguments for [`containeranalysis.projects.locations.occurrences.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsOccurrencesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: Occurrence,
}

/// Arguments for [`containeranalysis.projects.locations.occurrences.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsOccurrencesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`containeranalysis.projects.locations.occurrences.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsOccurrencesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`containeranalysis.projects.locations.occurrences.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsOccurrencesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// Arguments for [`containeranalysis.projects.locations.occurrences.getNotes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsOccurrencesGetNotesArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`containeranalysis.projects.locations.occurrences.getVulnerabilitySummary_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsOccurrencesGetVulnerabilitySummaryArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`containeranalysis.projects.locations.occurrences.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsOccurrencesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`containeranalysis.projects.locations.occurrences.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsOccurrencesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Occurrence,
}

/// Arguments for [`containeranalysis.projects.locations.occurrences.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsOccurrencesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`containeranalysis.projects.locations.occurrences.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsOccurrencesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`containeranalysis.projects.locations.resources.exportSBOM_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsLocationsResourcesExportSBOMArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: ExportSBOMRequest,
}

/// Arguments for [`containeranalysis.projects.notes.batchCreate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesBatchCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: BatchCreateNotesRequest,
}

/// Arguments for [`containeranalysis.projects.notes.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `noteId`.
    pub note_id: Option<String>,
    /// Request body.
    pub body: Note,
}

/// Arguments for [`containeranalysis.projects.notes.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`containeranalysis.projects.notes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`containeranalysis.projects.notes.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// Arguments for [`containeranalysis.projects.notes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`containeranalysis.projects.notes.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Note,
}

/// Arguments for [`containeranalysis.projects.notes.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`containeranalysis.projects.notes.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`containeranalysis.projects.notes.occurrences.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsNotesOccurrencesListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`containeranalysis.projects.occurrences.batchCreate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesBatchCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: BatchCreateOccurrencesRequest,
}

/// Arguments for [`containeranalysis.projects.occurrences.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: Occurrence,
}

/// Arguments for [`containeranalysis.projects.occurrences.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`containeranalysis.projects.occurrences.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`containeranalysis.projects.occurrences.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// Arguments for [`containeranalysis.projects.occurrences.getNotes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesGetNotesArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`containeranalysis.projects.occurrences.getVulnerabilitySummary_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesGetVulnerabilitySummaryArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`containeranalysis.projects.occurrences.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`containeranalysis.projects.occurrences.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Occurrence,
}

/// Arguments for [`containeranalysis.projects.occurrences.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`containeranalysis.projects.occurrences.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsOccurrencesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`containeranalysis.projects.resources.exportSBOM_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContaineranalysisProjectsResourcesExportSBOMArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: ExportSBOMRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/{+parent}/notes:batchCreate
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/notes:batchCreate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_notes_batch_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_notes_batch_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsNotesBatchCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchCreateNotesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/notes:batchCreate",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BatchCreateNotesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/notes
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/notes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_notes_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_notes_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsNotesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Note>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/notes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.note_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("noteId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Note =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_notes_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_notes_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsNotesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_notes_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_notes_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsNotesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Note>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Note =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_notes_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_notes_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsNotesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/notes
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/notes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_notes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_notes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsNotesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNotesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/notes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListNotesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_notes_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_notes_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsNotesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Note>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Note =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_notes_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_notes_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsNotesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_notes_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_notes_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsNotesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/occurrences
// -----------------------------------------------------------------------------

/// GET v1/{+name}/occurrences.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_notes_occurrences_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_notes_occurrences_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsNotesOccurrencesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNoteOccurrencesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/occurrences",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListNoteOccurrencesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/occurrences:batchCreate
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/occurrences:batchCreate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_occurrences_batch_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_occurrences_batch_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsOccurrencesBatchCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchCreateOccurrencesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/occurrences:batchCreate",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BatchCreateOccurrencesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/occurrences
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/occurrences.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_occurrences_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_occurrences_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsOccurrencesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Occurrence>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/occurrences",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Occurrence =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_occurrences_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_occurrences_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsOccurrencesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_occurrences_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_occurrences_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsOccurrencesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Occurrence>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Occurrence =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_occurrences_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_occurrences_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsOccurrencesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/notes
// -----------------------------------------------------------------------------

/// GET v1/{+name}/notes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_occurrences_get_notes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_occurrences_get_notes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsOccurrencesGetNotesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Note>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/notes",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Note =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/occurrences:vulnerabilitySummary
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/occurrences:vulnerabilitySummary.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_occurrences_get_vulnerability_summary_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_occurrences_get_vulnerability_summary_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsOccurrencesGetVulnerabilitySummaryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VulnerabilityOccurrencesSummary>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/occurrences:vulnerabilitySummary",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VulnerabilityOccurrencesSummary =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/occurrences
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/occurrences.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_occurrences_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_occurrences_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsOccurrencesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOccurrencesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/occurrences",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOccurrencesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_occurrences_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_occurrences_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsOccurrencesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Occurrence>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Occurrence =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_occurrences_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_occurrences_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsOccurrencesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_occurrences_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_occurrences_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsOccurrencesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:exportSBOM
// -----------------------------------------------------------------------------

/// POST v1/{+name}:exportSBOM.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_locations_resources_export_sbom_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_locations_resources_export_sbom_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsLocationsResourcesExportSBOMArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExportSBOMResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:exportSBOM",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExportSBOMResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/notes:batchCreate
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/notes:batchCreate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_notes_batch_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_notes_batch_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsNotesBatchCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchCreateNotesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/notes:batchCreate",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BatchCreateNotesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/notes
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/notes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_notes_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_notes_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsNotesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Note>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/notes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.note_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("noteId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Note =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_notes_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_notes_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsNotesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_notes_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_notes_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsNotesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Note>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Note =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_notes_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_notes_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsNotesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/notes
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/notes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_notes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_notes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsNotesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNotesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/notes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListNotesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_notes_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_notes_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsNotesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Note>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Note =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_notes_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_notes_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsNotesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_notes_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_notes_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsNotesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/occurrences
// -----------------------------------------------------------------------------

/// GET v1/{+name}/occurrences.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_notes_occurrences_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_notes_occurrences_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsNotesOccurrencesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNoteOccurrencesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/occurrences",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListNoteOccurrencesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/occurrences:batchCreate
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/occurrences:batchCreate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_occurrences_batch_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_occurrences_batch_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsOccurrencesBatchCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchCreateOccurrencesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/occurrences:batchCreate",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BatchCreateOccurrencesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/occurrences
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/occurrences.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_occurrences_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_occurrences_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsOccurrencesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Occurrence>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/occurrences",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Occurrence =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_occurrences_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_occurrences_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsOccurrencesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_occurrences_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_occurrences_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsOccurrencesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Occurrence>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Occurrence =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_occurrences_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_occurrences_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsOccurrencesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/notes
// -----------------------------------------------------------------------------

/// GET v1/{+name}/notes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_occurrences_get_notes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_occurrences_get_notes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsOccurrencesGetNotesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Note>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/notes",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Note =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/occurrences:vulnerabilitySummary
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/occurrences:vulnerabilitySummary.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_occurrences_get_vulnerability_summary_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_occurrences_get_vulnerability_summary_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsOccurrencesGetVulnerabilitySummaryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VulnerabilityOccurrencesSummary>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/occurrences:vulnerabilitySummary",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VulnerabilityOccurrencesSummary =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/occurrences
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/occurrences.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_occurrences_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_occurrences_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsOccurrencesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOccurrencesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}/occurrences",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOccurrencesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_occurrences_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_occurrences_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsOccurrencesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Occurrence>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://containeranalysis.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Occurrence =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_occurrences_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_occurrences_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsOccurrencesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_occurrences_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_occurrences_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsOccurrencesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:exportSBOM
// -----------------------------------------------------------------------------

/// POST v1/{+name}:exportSBOM.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = containeranalysis_projects_resources_export_sbom_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn containeranalysis_projects_resources_export_sbom_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContaineranalysisProjectsResourcesExportSBOMArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExportSBOMResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://containeranalysis.googleapis.com/v1/{}:exportSBOM",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExportSBOMResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
