//! Auto-generated API module for gcp/displayvideo firstPartyAndPartnerAudiences.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_displayvideo_first_party_partner_audiences `

#![cfg(feature = "gcp_displayvideo_first_party_partner_audiences")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ContactInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ContactInfo {
    /// countryCode property.
    pub country_code: Option<String>,
    /// hashedEmails property.
    pub hashed_emails: Option<Vec<String>>,
    /// hashedFirstName property.
    pub hashed_first_name: Option<String>,
    /// hashedLastName property.
    pub hashed_last_name: Option<String>,
    /// hashedPhoneNumbers property.
    pub hashed_phone_numbers: Option<Vec<String>>,
    /// zipCodes property.
    pub zip_codes: Option<Vec<String>>,
}

/// `ContactInfoList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ContactInfoList {
    /// consent property.
    pub consent: Option<Consent>,
    /// contactInfos property.
    pub contact_infos: Option<Vec<ContactInfo>>,
}

/// `Consent` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Consent {
    /// adPersonalization property.
    pub ad_personalization: Option<String>,
    /// adUserData property.
    pub ad_user_data: Option<String>,
}

/// `ListFirstPartyAndPartnerAudiencesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListFirstPartyAndPartnerAudiencesResponse {
    /// firstPartyAndPartnerAudiences property.
    pub first_party_and_partner_audiences: Option<Vec<FirstPartyAndPartnerAudience>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `FirstPartyAndPartnerAudience` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FirstPartyAndPartnerAudience {
    /// activeDisplayAudienceSize property.
    pub active_display_audience_size: Option<String>,
    /// appId property.
    pub app_id: Option<String>,
    /// audienceSource property.
    pub audience_source: Option<String>,
    /// audienceType property.
    pub audience_type: Option<String>,
    /// contactInfoList property.
    pub contact_info_list: Option<ContactInfoList>,
    /// description property.
    pub description: Option<String>,
    /// displayAudienceSize property.
    pub display_audience_size: Option<String>,
    /// displayDesktopAudienceSize property.
    pub display_desktop_audience_size: Option<String>,
    /// displayMobileAppAudienceSize property.
    pub display_mobile_app_audience_size: Option<String>,
    /// displayMobileWebAudienceSize property.
    pub display_mobile_web_audience_size: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// firstPartyAndPartnerAudienceId property.
    pub first_party_and_partner_audience_id: Option<String>,
    /// firstPartyAndPartnerAudienceType property.
    pub first_party_and_partner_audience_type: Option<String>,
    /// gmailAudienceSize property.
    pub gmail_audience_size: Option<String>,
    /// membershipDurationDays property.
    pub membership_duration_days: Option<String>,
    /// mobileDeviceIdList property.
    pub mobile_device_id_list: Option<MobileDeviceIdList>,
    /// name property.
    pub name: Option<String>,
    /// youtubeAudienceSize property.
    pub youtube_audience_size: Option<String>,
}

/// `MobileDeviceIdList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MobileDeviceIdList {
    /// consent property.
    pub consent: Option<Consent>,
    /// mobileDeviceIds property.
    pub mobile_device_ids: Option<Vec<String>>,
}

/// `EditCustomerMatchMembersResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EditCustomerMatchMembersResponse {
    /// firstPartyAndPartnerAudienceId property.
    pub first_party_and_partner_audience_id: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`displayvideo.firstPartyAndPartnerAudiences.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFirstPartyAndPartnerAudiencesCreateArgs {
    /// Query parameter: `advertiserId`.
    pub advertiser_id: Option<String>,
}

/// Arguments for [`displayvideo.firstPartyAndPartnerAudiences.editCustomerMatchMembers_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFirstPartyAndPartnerAudiencesEditCustomerMatchMembersArgs {
    /// Path parameter: `firstPartyAndPartnerAudienceId`.
    pub first_party_and_partner_audience_id: String,
}

/// Arguments for [`displayvideo.firstPartyAndPartnerAudiences.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFirstPartyAndPartnerAudiencesGetArgs {
    /// Path parameter: `firstPartyAndPartnerAudienceId`.
    pub first_party_and_partner_audience_id: String,
    /// Query parameter: `advertiserId`.
    pub advertiser_id: Option<String>,
    /// Query parameter: `partnerId`.
    pub partner_id: Option<String>,
}

/// Arguments for [`displayvideo.firstPartyAndPartnerAudiences.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFirstPartyAndPartnerAudiencesListArgs {
    /// Query parameter: `advertiserId`.
    pub advertiser_id: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `partnerId`.
    pub partner_id: Option<String>,
}

/// Arguments for [`displayvideo.firstPartyAndPartnerAudiences.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFirstPartyAndPartnerAudiencesPatchArgs {
    /// Path parameter: `firstPartyAndPartnerAudienceId`.
    pub first_party_and_partner_audience_id: String,
    /// Query parameter: `advertiserId`.
    pub advertiser_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v4/firstPartyAndPartnerAudiences
// -----------------------------------------------------------------------------

/// POST v4/firstPartyAndPartnerAudiences.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_first_party_and_partner_audiences_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_first_party_and_partner_audiences_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirstPartyAndPartnerAudience>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://displayvideo.googleapis.com/v4/firstPartyAndPartnerAudiences",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirstPartyAndPartnerAudience =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}:editCustomerMatchMembers
// -----------------------------------------------------------------------------

/// POST v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}:editCustomerMatchMembers.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_first_party_and_partner_audiences_edit_customer_match_members_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_first_party_and_partner_audiences_edit_customer_match_members_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EditCustomerMatchMembersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/firstPartyAndPartnerAudiences/{{firstPartyAndPartnerAudiencesId}}:editCustomerMatchMembers",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EditCustomerMatchMembersResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}
// -----------------------------------------------------------------------------

/// GET v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_first_party_and_partner_audiences_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_first_party_and_partner_audiences_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirstPartyAndPartnerAudience>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/firstPartyAndPartnerAudiences/{{firstPartyAndPartnerAudiencesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirstPartyAndPartnerAudience =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/firstPartyAndPartnerAudiences
// -----------------------------------------------------------------------------

/// GET v4/firstPartyAndPartnerAudiences.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_first_party_and_partner_audiences_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_first_party_and_partner_audiences_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListFirstPartyAndPartnerAudiencesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://displayvideo.googleapis.com/v4/firstPartyAndPartnerAudiences",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFirstPartyAndPartnerAudiencesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}
// -----------------------------------------------------------------------------

/// PATCH v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_first_party_and_partner_audiences_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_first_party_and_partner_audiences_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirstPartyAndPartnerAudience>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/firstPartyAndPartnerAudiences/{{firstPartyAndPartnerAudiencesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FirstPartyAndPartnerAudience =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
