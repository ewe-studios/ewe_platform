//! Auto-generated API module for gcp/displayvideo insertionOrders.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_displayvideo_insertion_orders `

#![cfg(feature = "gcp_displayvideo_insertion_orders")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `MaximizeSpendBidStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MaximizeSpendBidStrategy {
    /// customBiddingAlgorithmId property.
    pub custom_bidding_algorithm_id: Option<String>,
    /// maxAverageCpmBidAmountMicros property.
    pub max_average_cpm_bid_amount_micros: Option<String>,
    /// performanceGoalType property.
    pub performance_goal_type: Option<String>,
    /// raiseBidForDeals property.
    pub raise_bid_for_deals: Option<bool>,
}

/// `Kpi` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Kpi {
    /// kpiAlgorithmId property.
    pub kpi_algorithm_id: Option<String>,
    /// kpiAmountMicros property.
    pub kpi_amount_micros: Option<String>,
    /// kpiPercentageMicros property.
    pub kpi_percentage_micros: Option<String>,
    /// kpiString property.
    pub kpi_string: Option<String>,
    /// kpiType property.
    pub kpi_type: Option<String>,
}

/// `InsertionOrderBudgetSegment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InsertionOrderBudgetSegment {
    /// budgetAmountMicros property.
    pub budget_amount_micros: Option<String>,
    /// campaignBudgetId property.
    pub campaign_budget_id: Option<String>,
    /// dateRange property.
    pub date_range: Option<DateRange>,
    /// description property.
    pub description: Option<String>,
}

/// `FixedBidStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FixedBidStrategy {
    /// bidAmountMicros property.
    pub bid_amount_micros: Option<String>,
}

/// `Pacing` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Pacing {
    /// dailyMaxImpressions property.
    pub daily_max_impressions: Option<String>,
    /// dailyMaxMicros property.
    pub daily_max_micros: Option<String>,
    /// pacingPeriod property.
    pub pacing_period: Option<String>,
    /// pacingType property.
    pub pacing_type: Option<String>,
}

/// `PerformanceGoalBidStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PerformanceGoalBidStrategy {
    /// customBiddingAlgorithmId property.
    pub custom_bidding_algorithm_id: Option<String>,
    /// maxAverageCpmBidAmountMicros property.
    pub max_average_cpm_bid_amount_micros: Option<String>,
    /// performanceGoalAmountMicros property.
    pub performance_goal_amount_micros: Option<String>,
    /// performanceGoalType property.
    pub performance_goal_type: Option<String>,
}

/// `Date` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `BiddingStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BiddingStrategy {
    /// demandGenBid property.
    pub demand_gen_bid: Option<DemandGenBiddingStrategy>,
    /// fixedBid property.
    pub fixed_bid: Option<FixedBidStrategy>,
    /// maximizeSpendAutoBid property.
    pub maximize_spend_auto_bid: Option<MaximizeSpendBidStrategy>,
    /// performanceGoalAutoBid property.
    pub performance_goal_auto_bid: Option<PerformanceGoalBidStrategy>,
    /// youtubeAndPartnersBid property.
    pub youtube_and_partners_bid: Option<YoutubeAndPartnersBiddingStrategy>,
}

/// `InsertionOrder` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InsertionOrder {
    /// advertiserId property.
    pub advertiser_id: Option<String>,
    /// bidStrategy property.
    pub bid_strategy: Option<BiddingStrategy>,
    /// budget property.
    pub budget: Option<InsertionOrderBudget>,
    /// campaignId property.
    pub campaign_id: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// entityStatus property.
    pub entity_status: Option<String>,
    /// frequencyCap property.
    pub frequency_cap: Option<FrequencyCap>,
    /// insertionOrderId property.
    pub insertion_order_id: Option<String>,
    /// insertionOrderType property.
    pub insertion_order_type: Option<String>,
    /// integrationDetails property.
    pub integration_details: Option<IntegrationDetails>,
    /// kpi property.
    pub kpi: Option<Kpi>,
    /// name property.
    pub name: Option<String>,
    /// optimizationObjective property.
    pub optimization_objective: Option<String>,
    /// pacing property.
    pub pacing: Option<Pacing>,
    /// partnerCosts property.
    pub partner_costs: Option<Vec<PartnerCost>>,
    /// reservationType property.
    pub reservation_type: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `ListInsertionOrdersResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListInsertionOrdersResponse {
    /// insertionOrders property.
    pub insertion_orders: Option<Vec<InsertionOrder>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `InsertionOrderBudget` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InsertionOrderBudget {
    /// automationType property.
    pub automation_type: Option<String>,
    /// budgetSegments property.
    pub budget_segments: Option<Vec<InsertionOrderBudgetSegment>>,
    /// budgetUnit property.
    pub budget_unit: Option<String>,
}

/// `FrequencyCap` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FrequencyCap {
    /// maxImpressions property.
    pub max_impressions: Option<i64>,
    /// maxViews property.
    pub max_views: Option<i64>,
    /// timeUnit property.
    pub time_unit: Option<String>,
    /// timeUnitCount property.
    pub time_unit_count: Option<i64>,
    /// unlimited property.
    pub unlimited: Option<bool>,
}

/// `IntegrationDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IntegrationDetails {
    /// details property.
    pub details: Option<String>,
    /// integrationCode property.
    pub integration_code: Option<String>,
}

/// `PartnerCost` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PartnerCost {
    /// costType property.
    pub cost_type: Option<String>,
    /// feeAmount property.
    pub fee_amount: Option<String>,
    /// feePercentageMillis property.
    pub fee_percentage_millis: Option<String>,
    /// feeType property.
    pub fee_type: Option<String>,
    /// invoiceType property.
    pub invoice_type: Option<String>,
}

/// `YoutubeAndPartnersBiddingStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct YoutubeAndPartnersBiddingStrategy {
    /// adGroupEffectiveTargetCpaSource property.
    pub ad_group_effective_target_cpa_source: Option<String>,
    /// adGroupEffectiveTargetCpaValue property.
    pub ad_group_effective_target_cpa_value: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `DateRange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DateRange {
    /// endDate property.
    pub end_date: Option<Date>,
    /// startDate property.
    pub start_date: Option<Date>,
}

/// `DemandGenBiddingStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DemandGenBiddingStrategy {
    /// effectiveBiddingValue property.
    pub effective_bidding_value: Option<String>,
    /// effectiveBiddingValueSource property.
    pub effective_bidding_value_source: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// value property.
    pub value: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`displayvideo.advertisers.insertionOrders.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInsertionOrdersCreateArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
}

/// Arguments for [`displayvideo.advertisers.insertionOrders.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInsertionOrdersDeleteArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `insertionOrderId`.
    pub insertion_order_id: String,
}

/// Arguments for [`displayvideo.advertisers.insertionOrders.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInsertionOrdersGetArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `insertionOrderId`.
    pub insertion_order_id: String,
}

/// Arguments for [`displayvideo.advertisers.insertionOrders.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInsertionOrdersListArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`displayvideo.advertisers.insertionOrders.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInsertionOrdersPatchArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `insertionOrderId`.
    pub insertion_order_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v4/advertisers/{advertisersId}/insertionOrders
// -----------------------------------------------------------------------------

/// POST v4/advertisers/{advertisersId}/insertionOrders.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_insertion_orders_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_insertion_orders_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InsertionOrder>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/insertionOrders",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: InsertionOrder =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
// -----------------------------------------------------------------------------

/// DELETE v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_insertion_orders_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_insertion_orders_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/insertionOrders/{{insertionOrdersId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
// -----------------------------------------------------------------------------

/// GET v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_insertion_orders_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_insertion_orders_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InsertionOrder>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/insertionOrders/{{insertionOrdersId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: InsertionOrder =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/advertisers/{advertisersId}/insertionOrders
// -----------------------------------------------------------------------------

/// GET v4/advertisers/{advertisersId}/insertionOrders.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_insertion_orders_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_insertion_orders_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInsertionOrdersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/insertionOrders",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListInsertionOrdersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
// -----------------------------------------------------------------------------

/// PATCH v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_insertion_orders_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_insertion_orders_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InsertionOrder>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/insertionOrders/{{insertionOrdersId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: InsertionOrder =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
