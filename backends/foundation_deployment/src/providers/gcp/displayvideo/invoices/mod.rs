//! Auto-generated API module for gcp/displayvideo invoices.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_displayvideo_invoices `

#![cfg(feature = "gcp_displayvideo_invoices")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ListInvoicesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListInvoicesResponse {
    /// invoices property.
    pub invoices: Option<Vec<Invoice>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Date` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `DateRange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DateRange {
    /// endDate property.
    pub end_date: Option<Date>,
    /// startDate property.
    pub start_date: Option<Date>,
}

/// `Invoice` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Invoice {
    /// budgetInvoiceGroupingId property.
    pub budget_invoice_grouping_id: Option<String>,
    /// budgetSummaries property.
    pub budget_summaries: Option<Vec<BudgetSummary>>,
    /// correctedInvoiceId property.
    pub corrected_invoice_id: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// dueDate property.
    pub due_date: Option<Date>,
    /// invoiceId property.
    pub invoice_id: Option<String>,
    /// invoiceType property.
    pub invoice_type: Option<String>,
    /// issueDate property.
    pub issue_date: Option<Date>,
    /// name property.
    pub name: Option<String>,
    /// nonBudgetMicros property.
    pub non_budget_micros: Option<String>,
    /// paymentsAccountId property.
    pub payments_account_id: Option<String>,
    /// paymentsProfileId property.
    pub payments_profile_id: Option<String>,
    /// pdfUrl property.
    pub pdf_url: Option<String>,
    /// purchaseOrderNumber property.
    pub purchase_order_number: Option<String>,
    /// replacedInvoiceIds property.
    pub replaced_invoice_ids: Option<Vec<String>>,
    /// serviceDateRange property.
    pub service_date_range: Option<DateRange>,
    /// subtotalAmountMicros property.
    pub subtotal_amount_micros: Option<String>,
    /// totalAmountMicros property.
    pub total_amount_micros: Option<String>,
    /// totalTaxAmountMicros property.
    pub total_tax_amount_micros: Option<String>,
}

/// `BudgetSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BudgetSummary {
    /// externalBudgetId property.
    pub external_budget_id: Option<String>,
    /// preTaxAmountMicros property.
    pub pre_tax_amount_micros: Option<String>,
    /// prismaCpeCode property.
    pub prisma_cpe_code: Option<PrismaCpeCode>,
    /// taxAmountMicros property.
    pub tax_amount_micros: Option<String>,
    /// totalAmountMicros property.
    pub total_amount_micros: Option<String>,
}

/// `PrismaCpeCode` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PrismaCpeCode {
    /// prismaClientCode property.
    pub prisma_client_code: Option<String>,
    /// prismaEstimateCode property.
    pub prisma_estimate_code: Option<String>,
    /// prismaProductCode property.
    pub prisma_product_code: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`displayvideo.advertisers.invoices.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInvoicesListArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Query parameter: `issueMonth`.
    pub issue_month: Option<String>,
    /// Query parameter: `loiSapinInvoiceType`.
    pub loi_sapin_invoice_type: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v4/advertisers/{advertisersId}/invoices
// -----------------------------------------------------------------------------

/// GET v4/advertisers/{advertisersId}/invoices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_invoices_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_invoices_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInvoicesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/invoices",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListInvoicesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
