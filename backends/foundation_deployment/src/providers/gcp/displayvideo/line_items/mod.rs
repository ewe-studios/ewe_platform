//! Auto-generated API module for gcp/displayvideo lineItems.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_displayvideo_line_items `

#![cfg(feature = "gcp_displayvideo_line_items")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `TrackingFloodlightActivityConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TrackingFloodlightActivityConfig {
    /// floodlightActivityId property.
    pub floodlight_activity_id: Option<String>,
    /// postClickLookbackWindowDays property.
    pub post_click_lookback_window_days: Option<i64>,
    /// postViewLookbackWindowDays property.
    pub post_view_lookback_window_days: Option<i64>,
}

/// `ListLineItemsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListLineItemsResponse {
    /// lineItems property.
    pub line_items: Option<Vec<LineItem>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `DemandGenSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DemandGenSettings {
    /// geoLanguageTargetingEnabled property.
    pub geo_language_targeting_enabled: Option<bool>,
    /// linkedMerchantId property.
    pub linked_merchant_id: Option<String>,
    /// thirdPartyMeasurementConfigs property.
    pub third_party_measurement_configs: Option<ThirdPartyMeasurementConfigs>,
}

/// `PartnerRevenueModel` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PartnerRevenueModel {
    /// markupAmount property.
    pub markup_amount: Option<String>,
    /// markupType property.
    pub markup_type: Option<String>,
}

/// `FixedBidStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FixedBidStrategy {
    /// bidAmountMicros property.
    pub bid_amount_micros: Option<String>,
}

/// `PartnerCost` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PartnerCost {
    /// costType property.
    pub cost_type: Option<String>,
    /// feeAmount property.
    pub fee_amount: Option<String>,
    /// feePercentageMillis property.
    pub fee_percentage_millis: Option<String>,
    /// feeType property.
    pub fee_type: Option<String>,
    /// invoiceType property.
    pub invoice_type: Option<String>,
}

/// `DateRange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DateRange {
    /// endDate property.
    pub end_date: Option<Date>,
    /// startDate property.
    pub start_date: Option<Date>,
}

/// `IntegrationDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IntegrationDetails {
    /// details property.
    pub details: Option<String>,
    /// integrationCode property.
    pub integration_code: Option<String>,
}

/// `YoutubeAndPartnersInventorySourceConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct YoutubeAndPartnersInventorySourceConfig {
    /// includeGoogleTv property.
    pub include_google_tv: Option<bool>,
    /// includeYoutube property.
    pub include_youtube: Option<bool>,
    /// includeYoutubeVideoPartners property.
    pub include_youtube_video_partners: Option<bool>,
}

/// `VideoAdSequenceSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VideoAdSequenceSettings {
    /// minimumDuration property.
    pub minimum_duration: Option<String>,
    /// steps property.
    pub steps: Option<Vec<VideoAdSequenceStep>>,
}

/// `FrequencyCap` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FrequencyCap {
    /// maxImpressions property.
    pub max_impressions: Option<i64>,
    /// maxViews property.
    pub max_views: Option<i64>,
    /// timeUnit property.
    pub time_unit: Option<String>,
    /// timeUnitCount property.
    pub time_unit_count: Option<i64>,
    /// unlimited property.
    pub unlimited: Option<bool>,
}

/// `MaximizeSpendBidStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MaximizeSpendBidStrategy {
    /// customBiddingAlgorithmId property.
    pub custom_bidding_algorithm_id: Option<String>,
    /// maxAverageCpmBidAmountMicros property.
    pub max_average_cpm_bid_amount_micros: Option<String>,
    /// performanceGoalType property.
    pub performance_goal_type: Option<String>,
    /// raiseBidForDeals property.
    pub raise_bid_for_deals: Option<bool>,
}

/// `TargetFrequency` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TargetFrequency {
    /// targetCount property.
    pub target_count: Option<String>,
    /// timeUnit property.
    pub time_unit: Option<String>,
    /// timeUnitCount property.
    pub time_unit_count: Option<i64>,
}

/// `Pacing` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Pacing {
    /// dailyMaxImpressions property.
    pub daily_max_impressions: Option<String>,
    /// dailyMaxMicros property.
    pub daily_max_micros: Option<String>,
    /// pacingPeriod property.
    pub pacing_period: Option<String>,
    /// pacingType property.
    pub pacing_type: Option<String>,
}

/// `BiddingStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BiddingStrategy {
    /// demandGenBid property.
    pub demand_gen_bid: Option<DemandGenBiddingStrategy>,
    /// fixedBid property.
    pub fixed_bid: Option<FixedBidStrategy>,
    /// maximizeSpendAutoBid property.
    pub maximize_spend_auto_bid: Option<MaximizeSpendBidStrategy>,
    /// performanceGoalAutoBid property.
    pub performance_goal_auto_bid: Option<PerformanceGoalBidStrategy>,
    /// youtubeAndPartnersBid property.
    pub youtube_and_partners_bid: Option<YoutubeAndPartnersBiddingStrategy>,
}

/// `YoutubeAndPartnersBiddingStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct YoutubeAndPartnersBiddingStrategy {
    /// adGroupEffectiveTargetCpaSource property.
    pub ad_group_effective_target_cpa_source: Option<String>,
    /// adGroupEffectiveTargetCpaValue property.
    pub ad_group_effective_target_cpa_value: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `MobileApp` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MobileApp {
    /// appId property.
    pub app_id: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// platform property.
    pub platform: Option<String>,
    /// publisher property.
    pub publisher: Option<String>,
}

/// `VideoAdSequenceStep` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VideoAdSequenceStep {
    /// adGroupId property.
    pub ad_group_id: Option<String>,
    /// interactionType property.
    pub interaction_type: Option<String>,
    /// previousStepId property.
    pub previous_step_id: Option<String>,
    /// stepId property.
    pub step_id: Option<String>,
}

/// `DemandGenBiddingStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DemandGenBiddingStrategy {
    /// effectiveBiddingValue property.
    pub effective_bidding_value: Option<String>,
    /// effectiveBiddingValueSource property.
    pub effective_bidding_value_source: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `TargetingExpansionConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TargetingExpansionConfig {
    /// audienceExpansionLevel property.
    pub audience_expansion_level: Option<String>,
    /// audienceExpansionSeedListExcluded property.
    pub audience_expansion_seed_list_excluded: Option<bool>,
    /// enableOptimizedTargeting property.
    pub enable_optimized_targeting: Option<bool>,
    /// excludeDemographicExpansion property.
    pub exclude_demographic_expansion: Option<bool>,
}

/// `ThirdPartyMeasurementConfigs` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ThirdPartyMeasurementConfigs {
    /// brandLiftVendorConfigs property.
    pub brand_lift_vendor_configs: Option<Vec<ThirdPartyVendorConfig>>,
    /// brandSafetyVendorConfigs property.
    pub brand_safety_vendor_configs: Option<Vec<ThirdPartyVendorConfig>>,
    /// reachVendorConfigs property.
    pub reach_vendor_configs: Option<Vec<ThirdPartyVendorConfig>>,
    /// viewabilityVendorConfigs property.
    pub viewability_vendor_configs: Option<Vec<ThirdPartyVendorConfig>>,
}

/// `LineItemBudget` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LineItemBudget {
    /// budgetAllocationType property.
    pub budget_allocation_type: Option<String>,
    /// budgetUnit property.
    pub budget_unit: Option<String>,
    /// maxAmount property.
    pub max_amount: Option<String>,
}

/// `LineItemFlight` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LineItemFlight {
    /// dateRange property.
    pub date_range: Option<DateRange>,
    /// flightDateType property.
    pub flight_date_type: Option<String>,
}

/// `ThirdPartyVendorConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ThirdPartyVendorConfig {
    /// placementId property.
    pub placement_id: Option<String>,
    /// vendor property.
    pub vendor: Option<String>,
}

/// `ConversionCountingConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConversionCountingConfig {
    /// floodlightActivityConfigs property.
    pub floodlight_activity_configs: Option<Vec<TrackingFloodlightActivityConfig>>,
    /// postViewCountPercentageMillis property.
    pub post_view_count_percentage_millis: Option<String>,
    /// primaryAttributionModelId property.
    pub primary_attribution_model_id: Option<String>,
}

/// `VideoAdInventoryControl` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VideoAdInventoryControl {
    /// allowInFeed property.
    pub allow_in_feed: Option<bool>,
    /// allowInStream property.
    pub allow_in_stream: Option<bool>,
    /// allowShorts property.
    pub allow_shorts: Option<bool>,
}

/// `DuplicateLineItemResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DuplicateLineItemResponse {
    /// duplicateLineItemId property.
    pub duplicate_line_item_id: Option<String>,
}

/// `LineItem` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LineItem {
    /// advertiserId property.
    pub advertiser_id: Option<String>,
    /// bidStrategy property.
    pub bid_strategy: Option<BiddingStrategy>,
    /// budget property.
    pub budget: Option<LineItemBudget>,
    /// campaignId property.
    pub campaign_id: Option<String>,
    /// containsEuPoliticalAds property.
    pub contains_eu_political_ads: Option<String>,
    /// conversionCounting property.
    pub conversion_counting: Option<ConversionCountingConfig>,
    /// creativeIds property.
    pub creative_ids: Option<Vec<String>>,
    /// demandGenSettings property.
    pub demand_gen_settings: Option<DemandGenSettings>,
    /// displayName property.
    pub display_name: Option<String>,
    /// entityStatus property.
    pub entity_status: Option<String>,
    /// excludeNewExchanges property.
    pub exclude_new_exchanges: Option<bool>,
    /// flight property.
    pub flight: Option<LineItemFlight>,
    /// frequencyCap property.
    pub frequency_cap: Option<FrequencyCap>,
    /// insertionOrderId property.
    pub insertion_order_id: Option<String>,
    /// integrationDetails property.
    pub integration_details: Option<IntegrationDetails>,
    /// lineItemId property.
    pub line_item_id: Option<String>,
    /// lineItemType property.
    pub line_item_type: Option<String>,
    /// mobileApp property.
    pub mobile_app: Option<MobileApp>,
    /// name property.
    pub name: Option<String>,
    /// pacing property.
    pub pacing: Option<Pacing>,
    /// partnerCosts property.
    pub partner_costs: Option<Vec<PartnerCost>>,
    /// partnerRevenueModel property.
    pub partner_revenue_model: Option<PartnerRevenueModel>,
    /// reservationType property.
    pub reservation_type: Option<String>,
    /// targetingExpansion property.
    pub targeting_expansion: Option<TargetingExpansionConfig>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// warningMessages property.
    pub warning_messages: Option<Vec<String>>,
    /// youtubeAndPartnersSettings property.
    pub youtube_and_partners_settings: Option<YoutubeAndPartnersSettings>,
}

/// `PerformanceGoalBidStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PerformanceGoalBidStrategy {
    /// customBiddingAlgorithmId property.
    pub custom_bidding_algorithm_id: Option<String>,
    /// maxAverageCpmBidAmountMicros property.
    pub max_average_cpm_bid_amount_micros: Option<String>,
    /// performanceGoalAmountMicros property.
    pub performance_goal_amount_micros: Option<String>,
    /// performanceGoalType property.
    pub performance_goal_type: Option<String>,
}

/// `Date` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `YoutubeAndPartnersSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct YoutubeAndPartnersSettings {
    /// contentCategory property.
    pub content_category: Option<String>,
    /// effectiveContentCategory property.
    pub effective_content_category: Option<String>,
    /// inventorySourceSettings property.
    pub inventory_source_settings: Option<YoutubeAndPartnersInventorySourceConfig>,
    /// leadFormId property.
    pub lead_form_id: Option<String>,
    /// linkedMerchantId property.
    pub linked_merchant_id: Option<String>,
    /// relatedVideoIds property.
    pub related_video_ids: Option<Vec<String>>,
    /// targetFrequency property.
    pub target_frequency: Option<TargetFrequency>,
    /// thirdPartyMeasurementConfigs property.
    pub third_party_measurement_configs: Option<ThirdPartyMeasurementConfigs>,
    /// videoAdInventoryControl property.
    pub video_ad_inventory_control: Option<VideoAdInventoryControl>,
    /// videoAdSequenceSettings property.
    pub video_ad_sequence_settings: Option<VideoAdSequenceSettings>,
    /// viewFrequencyCap property.
    pub view_frequency_cap: Option<FrequencyCap>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`displayvideo.advertisers.lineItems.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsCreateArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
}

/// Arguments for [`displayvideo.advertisers.lineItems.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsDeleteArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `lineItemId`.
    pub line_item_id: String,
}

/// Arguments for [`displayvideo.advertisers.lineItems.duplicate_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsDuplicateArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `lineItemId`.
    pub line_item_id: String,
}

/// Arguments for [`displayvideo.advertisers.lineItems.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsGetArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `lineItemId`.
    pub line_item_id: String,
}

/// Arguments for [`displayvideo.advertisers.lineItems.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsListArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`displayvideo.advertisers.lineItems.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsPatchArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `lineItemId`.
    pub line_item_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v4/advertisers/{advertisersId}/lineItems
// -----------------------------------------------------------------------------

/// POST v4/advertisers/{advertisersId}/lineItems.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_line_items_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_line_items_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LineItem>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/lineItems",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LineItem =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
// -----------------------------------------------------------------------------

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_line_items_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_line_items_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/lineItems/{{lineItemsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}:duplicate
// -----------------------------------------------------------------------------

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}:duplicate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_line_items_duplicate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_line_items_duplicate_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DuplicateLineItemResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/lineItems/{{lineItemsId}}:duplicate",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DuplicateLineItemResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
// -----------------------------------------------------------------------------

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_line_items_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_line_items_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LineItem>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/lineItems/{{lineItemsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LineItem =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/advertisers/{advertisersId}/lineItems
// -----------------------------------------------------------------------------

/// GET v4/advertisers/{advertisersId}/lineItems.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_line_items_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_line_items_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLineItemsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/lineItems",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListLineItemsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
// -----------------------------------------------------------------------------

/// PATCH v4/advertisers/{advertisersId}/lineItems/{lineItemsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_line_items_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_line_items_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LineItem>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/lineItems/{{lineItemsId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LineItem =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
