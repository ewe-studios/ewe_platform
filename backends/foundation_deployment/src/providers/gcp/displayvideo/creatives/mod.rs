//! Auto-generated API module for gcp/displayvideo creatives.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_displayvideo_creatives `

#![cfg(feature = "gcp_displayvideo_creatives")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ObaIcon` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ObaIcon {
    /// clickTrackingUrl property.
    pub click_tracking_url: Option<String>,
    /// dimensions property.
    pub dimensions: Option<Dimensions>,
    /// landingPageUrl property.
    pub landing_page_url: Option<String>,
    /// position property.
    pub position: Option<String>,
    /// program property.
    pub program: Option<String>,
    /// resourceMimeType property.
    pub resource_mime_type: Option<String>,
    /// resourceUrl property.
    pub resource_url: Option<String>,
    /// viewTrackingUrl property.
    pub view_tracking_url: Option<String>,
}

/// `CounterEvent` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CounterEvent {
    /// name property.
    pub name: Option<String>,
    /// reportingName property.
    pub reporting_name: Option<String>,
}

/// `ExitEvent` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExitEvent {
    /// name property.
    pub name: Option<String>,
    /// reportingName property.
    pub reporting_name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `Asset` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Asset {
    /// content property.
    pub content: Option<String>,
    /// mediaId property.
    pub media_id: Option<String>,
}

/// `ListCreativesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListCreativesResponse {
    /// creatives property.
    pub creatives: Option<Vec<Creative>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Transcode` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Transcode {
    /// audioBitRateKbps property.
    pub audio_bit_rate_kbps: Option<String>,
    /// audioSampleRateHz property.
    pub audio_sample_rate_hz: Option<String>,
    /// bitRateKbps property.
    pub bit_rate_kbps: Option<String>,
    /// dimensions property.
    pub dimensions: Option<Dimensions>,
    /// fileSizeBytes property.
    pub file_size_bytes: Option<String>,
    /// frameRate property.
    pub frame_rate: Option<f64>,
    /// mimeType property.
    pub mime_type: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// transcoded property.
    pub transcoded: Option<bool>,
}

/// `AssetAssociation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AssetAssociation {
    /// asset property.
    pub asset: Option<Asset>,
    /// role property.
    pub role: Option<String>,
}

/// `Dimensions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Dimensions {
    /// heightPixels property.
    pub height_pixels: Option<i64>,
    /// widthPixels property.
    pub width_pixels: Option<i64>,
}

/// `ExchangeReviewStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExchangeReviewStatus {
    /// exchange property.
    pub exchange: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `CmTrackingAd` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CmTrackingAd {
    /// cmAdId property.
    pub cm_ad_id: Option<String>,
    /// cmCreativeId property.
    pub cm_creative_id: Option<String>,
    /// cmPlacementId property.
    pub cm_placement_id: Option<String>,
}

/// `Creative` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Creative {
    /// additionalDimensions property.
    pub additional_dimensions: Option<Vec<Dimensions>>,
    /// advertiserId property.
    pub advertiser_id: Option<String>,
    /// appendedTag property.
    pub appended_tag: Option<String>,
    /// assets property.
    pub assets: Option<Vec<AssetAssociation>>,
    /// cmPlacementId property.
    pub cm_placement_id: Option<String>,
    /// cmTrackingAd property.
    pub cm_tracking_ad: Option<CmTrackingAd>,
    /// companionCreativeIds property.
    pub companion_creative_ids: Option<Vec<String>>,
    /// counterEvents property.
    pub counter_events: Option<Vec<CounterEvent>>,
    /// createTime property.
    pub create_time: Option<String>,
    /// creativeAttributes property.
    pub creative_attributes: Option<Vec<String>>,
    /// creativeId property.
    pub creative_id: Option<String>,
    /// creativeType property.
    pub creative_type: Option<String>,
    /// dimensions property.
    pub dimensions: Option<Dimensions>,
    /// displayName property.
    pub display_name: Option<String>,
    /// dynamic property.
    pub dynamic: Option<bool>,
    /// entityStatus property.
    pub entity_status: Option<String>,
    /// exitEvents property.
    pub exit_events: Option<Vec<ExitEvent>>,
    /// expandOnHover property.
    pub expand_on_hover: Option<bool>,
    /// expandingDirection property.
    pub expanding_direction: Option<String>,
    /// hostingSource property.
    pub hosting_source: Option<String>,
    /// html5Video property.
    pub html5_video: Option<bool>,
    /// iasCampaignMonitoring property.
    pub ias_campaign_monitoring: Option<bool>,
    /// integrationCode property.
    pub integration_code: Option<String>,
    /// jsTrackerUrl property.
    pub js_tracker_url: Option<String>,
    /// lineItemIds property.
    pub line_item_ids: Option<Vec<String>>,
    /// mediaDuration property.
    pub media_duration: Option<String>,
    /// mp3Audio property.
    pub mp3_audio: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// notes property.
    pub notes: Option<String>,
    /// obaIcon property.
    pub oba_icon: Option<ObaIcon>,
    /// oggAudio property.
    pub ogg_audio: Option<bool>,
    /// progressOffset property.
    pub progress_offset: Option<AudioVideoOffset>,
    /// requireHtml5 property.
    pub require_html5: Option<bool>,
    /// requireMraid property.
    pub require_mraid: Option<bool>,
    /// requirePingForAttribution property.
    pub require_ping_for_attribution: Option<bool>,
    /// reviewStatus property.
    pub review_status: Option<ReviewStatusInfo>,
    /// skipOffset property.
    pub skip_offset: Option<AudioVideoOffset>,
    /// skippable property.
    pub skippable: Option<bool>,
    /// thirdPartyTag property.
    pub third_party_tag: Option<String>,
    /// thirdPartyUrls property.
    pub third_party_urls: Option<Vec<ThirdPartyUrl>>,
    /// timerEvents property.
    pub timer_events: Option<Vec<TimerEvent>>,
    /// trackerUrls property.
    pub tracker_urls: Option<Vec<String>>,
    /// transcodes property.
    pub transcodes: Option<Vec<Transcode>>,
    /// universalAdId property.
    pub universal_ad_id: Option<UniversalAdId>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// vastTagUrl property.
    pub vast_tag_url: Option<String>,
    /// vpaid property.
    pub vpaid: Option<bool>,
}

/// `UniversalAdId` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UniversalAdId {
    /// id property.
    pub id: Option<String>,
    /// registry property.
    pub registry: Option<String>,
}

/// `ReviewStatusInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReviewStatusInfo {
    /// approvalStatus property.
    pub approval_status: Option<String>,
    /// contentAndPolicyReviewStatus property.
    pub content_and_policy_review_status: Option<String>,
    /// creativeAndLandingPageReviewStatus property.
    pub creative_and_landing_page_review_status: Option<String>,
    /// exchangeReviewStatuses property.
    pub exchange_review_statuses: Option<Vec<ExchangeReviewStatus>>,
}

/// `ThirdPartyUrl` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ThirdPartyUrl {
    /// type property.
    pub r#type: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `TimerEvent` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimerEvent {
    /// name property.
    pub name: Option<String>,
    /// reportingName property.
    pub reporting_name: Option<String>,
}

/// `AudioVideoOffset` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AudioVideoOffset {
    /// percentage property.
    pub percentage: Option<String>,
    /// seconds property.
    pub seconds: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`displayvideo.advertisers.creatives.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCreativesCreateArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
}

/// Arguments for [`displayvideo.advertisers.creatives.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCreativesDeleteArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `creativeId`.
    pub creative_id: String,
}

/// Arguments for [`displayvideo.advertisers.creatives.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCreativesGetArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `creativeId`.
    pub creative_id: String,
}

/// Arguments for [`displayvideo.advertisers.creatives.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCreativesListArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`displayvideo.advertisers.creatives.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCreativesPatchArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `creativeId`.
    pub creative_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v4/advertisers/{advertisersId}/creatives
// -----------------------------------------------------------------------------

/// POST v4/advertisers/{advertisersId}/creatives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_creatives_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_creatives_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/creatives",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Creative =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v4/advertisers/{advertisersId}/creatives/{creativesId}
// -----------------------------------------------------------------------------

/// DELETE v4/advertisers/{advertisersId}/creatives/{creativesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_creatives_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_creatives_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/creatives/{{creativesId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/advertisers/{advertisersId}/creatives/{creativesId}
// -----------------------------------------------------------------------------

/// GET v4/advertisers/{advertisersId}/creatives/{creativesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_creatives_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_creatives_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/creatives/{{creativesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Creative =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/advertisers/{advertisersId}/creatives
// -----------------------------------------------------------------------------

/// GET v4/advertisers/{advertisersId}/creatives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_creatives_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_creatives_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCreativesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/creatives",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCreativesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v4/advertisers/{advertisersId}/creatives/{creativesId}
// -----------------------------------------------------------------------------

/// PATCH v4/advertisers/{advertisersId}/creatives/{creativesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_creatives_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_creatives_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/creatives/{{creativesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Creative =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
