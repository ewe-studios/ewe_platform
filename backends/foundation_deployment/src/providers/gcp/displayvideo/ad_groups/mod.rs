//! Auto-generated API module for gcp/displayvideo adGroups.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_displayvideo_ad_groups `

#![cfg(feature = "gcp_displayvideo_ad_groups")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `CustomLabel` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomLabel {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `AdGroupInventoryControl` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AdGroupInventoryControl {
    /// adGroupInventoryStrategy property.
    pub ad_group_inventory_strategy: Option<String>,
    /// selectedInventories property.
    pub selected_inventories: Option<SelectedInventories>,
}

/// `YoutubeAndPartnersBiddingStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct YoutubeAndPartnersBiddingStrategy {
    /// adGroupEffectiveTargetCpaSource property.
    pub ad_group_effective_target_cpa_source: Option<String>,
    /// adGroupEffectiveTargetCpaValue property.
    pub ad_group_effective_target_cpa_value: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `MaximizeSpendBidStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MaximizeSpendBidStrategy {
    /// customBiddingAlgorithmId property.
    pub custom_bidding_algorithm_id: Option<String>,
    /// maxAverageCpmBidAmountMicros property.
    pub max_average_cpm_bid_amount_micros: Option<String>,
    /// performanceGoalType property.
    pub performance_goal_type: Option<String>,
    /// raiseBidForDeals property.
    pub raise_bid_for_deals: Option<bool>,
}

/// `AdGroup` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AdGroup {
    /// adGroupFormat property.
    pub ad_group_format: Option<String>,
    /// adGroupId property.
    pub ad_group_id: Option<String>,
    /// adGroupInventoryControl property.
    pub ad_group_inventory_control: Option<AdGroupInventoryControl>,
    /// advertiserId property.
    pub advertiser_id: Option<String>,
    /// bidStrategy property.
    pub bid_strategy: Option<BiddingStrategy>,
    /// displayName property.
    pub display_name: Option<String>,
    /// entityStatus property.
    pub entity_status: Option<String>,
    /// lineItemId property.
    pub line_item_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// productFeedData property.
    pub product_feed_data: Option<ProductFeedData>,
    /// targetingExpansion property.
    pub targeting_expansion: Option<TargetingExpansionConfig>,
}

/// `ProductFeedData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductFeedData {
    /// isFeedDisabled property.
    pub is_feed_disabled: Option<bool>,
    /// productMatchDimensions property.
    pub product_match_dimensions: Option<Vec<ProductMatchDimension>>,
    /// productMatchType property.
    pub product_match_type: Option<String>,
}

/// `FixedBidStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FixedBidStrategy {
    /// bidAmountMicros property.
    pub bid_amount_micros: Option<String>,
}

/// `ListAdGroupsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListAdGroupsResponse {
    /// adGroups property.
    pub ad_groups: Option<Vec<AdGroup>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `BiddingStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BiddingStrategy {
    /// demandGenBid property.
    pub demand_gen_bid: Option<DemandGenBiddingStrategy>,
    /// fixedBid property.
    pub fixed_bid: Option<FixedBidStrategy>,
    /// maximizeSpendAutoBid property.
    pub maximize_spend_auto_bid: Option<MaximizeSpendBidStrategy>,
    /// performanceGoalAutoBid property.
    pub performance_goal_auto_bid: Option<PerformanceGoalBidStrategy>,
    /// youtubeAndPartnersBid property.
    pub youtube_and_partners_bid: Option<YoutubeAndPartnersBiddingStrategy>,
}

/// `PerformanceGoalBidStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PerformanceGoalBidStrategy {
    /// customBiddingAlgorithmId property.
    pub custom_bidding_algorithm_id: Option<String>,
    /// maxAverageCpmBidAmountMicros property.
    pub max_average_cpm_bid_amount_micros: Option<String>,
    /// performanceGoalAmountMicros property.
    pub performance_goal_amount_micros: Option<String>,
    /// performanceGoalType property.
    pub performance_goal_type: Option<String>,
}

/// `SelectedInventories` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SelectedInventories {
    /// allowDiscover property.
    pub allow_discover: Option<bool>,
    /// allowGmail property.
    pub allow_gmail: Option<bool>,
    /// allowGoogleDisplayNetwork property.
    pub allow_google_display_network: Option<bool>,
    /// allowYoutubeFeed property.
    pub allow_youtube_feed: Option<bool>,
    /// allowYoutubeShorts property.
    pub allow_youtube_shorts: Option<bool>,
    /// allowYoutubeStream property.
    pub allow_youtube_stream: Option<bool>,
}

/// `TargetingExpansionConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TargetingExpansionConfig {
    /// audienceExpansionLevel property.
    pub audience_expansion_level: Option<String>,
    /// audienceExpansionSeedListExcluded property.
    pub audience_expansion_seed_list_excluded: Option<bool>,
    /// enableOptimizedTargeting property.
    pub enable_optimized_targeting: Option<bool>,
    /// excludeDemographicExpansion property.
    pub exclude_demographic_expansion: Option<bool>,
}

/// `DemandGenBiddingStrategy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DemandGenBiddingStrategy {
    /// effectiveBiddingValue property.
    pub effective_bidding_value: Option<String>,
    /// effectiveBiddingValueSource property.
    pub effective_bidding_value_source: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `ProductMatchDimension` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductMatchDimension {
    /// customLabel property.
    pub custom_label: Option<CustomLabel>,
    /// productOfferId property.
    pub product_offer_id: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`displayvideo.advertisers.adGroups.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsCreateArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
}

/// Arguments for [`displayvideo.advertisers.adGroups.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsDeleteArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `adGroupId`.
    pub ad_group_id: String,
}

/// Arguments for [`displayvideo.advertisers.adGroups.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsGetArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `adGroupId`.
    pub ad_group_id: String,
}

/// Arguments for [`displayvideo.advertisers.adGroups.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsListArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`displayvideo.advertisers.adGroups.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsPatchArgs {
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `adGroupId`.
    pub ad_group_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v4/advertisers/{advertisersId}/adGroups
// -----------------------------------------------------------------------------

/// POST v4/advertisers/{advertisersId}/adGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_ad_groups_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_ad_groups_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/adGroups",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AdGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
// -----------------------------------------------------------------------------

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_ad_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_ad_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/adGroups/{{adGroupsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
// -----------------------------------------------------------------------------

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_ad_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_ad_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/adGroups/{{adGroupsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AdGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v4/advertisers/{advertisersId}/adGroups
// -----------------------------------------------------------------------------

/// GET v4/advertisers/{advertisersId}/adGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_ad_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_ad_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAdGroupsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/adGroups",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListAdGroupsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
// -----------------------------------------------------------------------------

/// PATCH v4/advertisers/{advertisersId}/adGroups/{adGroupsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = displayvideo_advertisers_ad_groups_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn displayvideo_advertisers_ad_groups_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdGroup>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{{advertisersId}}/adGroups/{{adGroupsId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AdGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
