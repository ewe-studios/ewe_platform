//! Auto-generated API module for gcp/storage storage.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_storage_storage `

#![cfg(feature = "gcp_storage_storage")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `BulkRestoreObjectsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BulkRestoreObjectsRequest {
    /// allowOverwrite property.
    pub allow_overwrite: Option<bool>,
    /// copySourceAcl property.
    pub copy_source_acl: Option<bool>,
    /// createdAfterTime property.
    pub created_after_time: Option<String>,
    /// createdBeforeTime property.
    pub created_before_time: Option<String>,
    /// matchGlobs property.
    pub match_globs: Option<Vec<String>>,
    /// softDeletedAfterTime property.
    pub soft_deleted_after_time: Option<String>,
    /// softDeletedBeforeTime property.
    pub soft_deleted_before_time: Option<String>,
}

/// `HmacKeysMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HmacKeysMetadata {
    /// items property.
    pub items: Option<Vec<HmacKeyMetadata>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Notification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Notification {
    /// custom_attributes property.
    pub custom_attributes: Option<serde_json::Value>,
    /// etag property.
    pub etag: Option<String>,
    /// event_types property.
    pub event_types: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// object_name_prefix property.
    pub object_name_prefix: Option<String>,
    /// payload_format property.
    pub payload_format: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// topic property.
    pub topic: Option<String>,
}

/// `Notifications` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Notifications {
    /// items property.
    pub items: Option<Vec<Notification>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `AnywhereCaches` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnywhereCaches {
    /// items property.
    pub items: Option<Vec<AnywhereCache>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `RelocateBucketRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RelocateBucketRequest {
    /// destinationCustomPlacementConfig property.
    pub destination_custom_placement_config:
        Option<std::collections::HashMap<String, serde_json::Value>>,
    /// destinationKmsKeyName property.
    pub destination_kms_key_name: Option<String>,
    /// destinationLocation property.
    pub destination_location: Option<String>,
    /// validateOnly property.
    pub validate_only: Option<bool>,
}

/// `Folders` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Folders {
    /// items property.
    pub items: Option<Vec<Folder>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `BucketAccessControl` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BucketAccessControl {
    /// bucket property.
    pub bucket: Option<String>,
    /// domain property.
    pub domain: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// entity property.
    pub entity: Option<String>,
    /// entityId property.
    pub entity_id: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// projectTeam property.
    pub project_team: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// role property.
    pub role: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `GoogleLongrunningOperation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleLongrunningOperation {
    /// done property.
    pub done: Option<bool>,
    /// error property.
    pub error: Option<GoogleRpcStatus>,
    /// kind property.
    pub kind: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// response property.
    pub response: Option<serde_json::Value>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `Bucket` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Bucket {
    /// acl property.
    pub acl: Option<Vec<BucketAccessControl>>,
    /// autoclass property.
    pub autoclass: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// billing property.
    pub billing: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// cors property.
    pub cors: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// customPlacementConfig property.
    pub custom_placement_config: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// defaultEventBasedHold property.
    pub default_event_based_hold: Option<bool>,
    /// defaultObjectAcl property.
    pub default_object_acl: Option<Vec<ObjectAccessControl>>,
    /// encryption property.
    pub encryption: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// etag property.
    pub etag: Option<String>,
    /// generation property.
    pub generation: Option<String>,
    /// hardDeleteTime property.
    pub hard_delete_time: Option<String>,
    /// hierarchicalNamespace property.
    pub hierarchical_namespace: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// iamConfiguration property.
    pub iam_configuration: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// id property.
    pub id: Option<String>,
    /// ipFilter property.
    pub ip_filter: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// kind property.
    pub kind: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// lifecycle property.
    pub lifecycle: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// location property.
    pub location: Option<String>,
    /// locationType property.
    pub location_type: Option<String>,
    /// logging property.
    pub logging: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// metageneration property.
    pub metageneration: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// objectRetention property.
    pub object_retention: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// owner property.
    pub owner: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// projectNumber property.
    pub project_number: Option<String>,
    /// retentionPolicy property.
    pub retention_policy: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// rpo property.
    pub rpo: Option<String>,
    /// satisfiesPZI property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPZS property.
    pub satisfies_pzs: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// softDeletePolicy property.
    pub soft_delete_policy: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// softDeleteTime property.
    pub soft_delete_time: Option<String>,
    /// storageClass property.
    pub storage_class: Option<String>,
    /// timeCreated property.
    pub time_created: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// versioning property.
    pub versioning: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// website property.
    pub website: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ManagedFolders` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedFolders {
    /// items property.
    pub items: Option<Vec<ManagedFolder>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Folder` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Folder {
    /// bucket property.
    pub bucket: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// metageneration property.
    pub metageneration: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// pendingRenameInfo property.
    pub pending_rename_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `Buckets` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Buckets {
    /// items property.
    pub items: Option<Vec<Bucket>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `ManagedFolder` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedFolder {
    /// bucket property.
    pub bucket: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// metageneration property.
    pub metageneration: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `Object` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Object {
    /// acl property.
    pub acl: Option<Vec<ObjectAccessControl>>,
    /// bucket property.
    pub bucket: Option<String>,
    /// cacheControl property.
    pub cache_control: Option<String>,
    /// componentCount property.
    pub component_count: Option<i64>,
    /// contentDisposition property.
    pub content_disposition: Option<String>,
    /// contentEncoding property.
    pub content_encoding: Option<String>,
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// contentType property.
    pub content_type: Option<String>,
    /// contexts property.
    pub contexts: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// crc32c property.
    pub crc32c: Option<String>,
    /// customTime property.
    pub custom_time: Option<String>,
    /// customerEncryption property.
    pub customer_encryption: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// etag property.
    pub etag: Option<String>,
    /// eventBasedHold property.
    pub event_based_hold: Option<bool>,
    /// generation property.
    pub generation: Option<String>,
    /// hardDeleteTime property.
    pub hard_delete_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
    /// md5Hash property.
    pub md5_hash: Option<String>,
    /// mediaLink property.
    pub media_link: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// metageneration property.
    pub metageneration: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// owner property.
    pub owner: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// restoreToken property.
    pub restore_token: Option<String>,
    /// retention property.
    pub retention: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// retentionExpirationTime property.
    pub retention_expiration_time: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// size property.
    pub size: Option<String>,
    /// softDeleteTime property.
    pub soft_delete_time: Option<String>,
    /// storageClass property.
    pub storage_class: Option<String>,
    /// temporaryHold property.
    pub temporary_hold: Option<bool>,
    /// timeCreated property.
    pub time_created: Option<String>,
    /// timeDeleted property.
    pub time_deleted: Option<String>,
    /// timeFinalized property.
    pub time_finalized: Option<String>,
    /// timeStorageClassUpdated property.
    pub time_storage_class_updated: Option<String>,
    /// updated property.
    pub updated: Option<String>,
}

/// `ComposeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComposeRequest {
    /// deleteSourceObjects property.
    pub delete_source_objects: Option<bool>,
    /// destination property.
    pub destination: Option<Object>,
    /// kind property.
    pub kind: Option<String>,
    /// sourceObjects property.
    pub source_objects: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

/// `GoogleLongrunningListOperationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleLongrunningListOperationsResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<GoogleLongrunningOperation>>,
}

/// `Objects` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Objects {
    /// items property.
    pub items: Option<Vec<Object>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// prefixes property.
    pub prefixes: Option<Vec<String>>,
}

/// `HmacKeyMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HmacKeyMetadata {
    /// accessId property.
    pub access_id: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// serviceAccountEmail property.
    pub service_account_email: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// timeCreated property.
    pub time_created: Option<String>,
    /// updated property.
    pub updated: Option<String>,
}

/// `HmacKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HmacKey {
    /// kind property.
    pub kind: Option<String>,
    /// metadata property.
    pub metadata: Option<HmacKeyMetadata>,
    /// secret property.
    pub secret: Option<String>,
}

/// `Expr` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `BucketAccessControls` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BucketAccessControls {
    /// items property.
    pub items: Option<Vec<BucketAccessControl>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `RewriteResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RewriteResponse {
    /// done property.
    pub done: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// objectSize property.
    pub object_size: Option<String>,
    /// resource property.
    pub resource: Option<Object>,
    /// rewriteToken property.
    pub rewrite_token: Option<String>,
    /// totalBytesRewritten property.
    pub total_bytes_rewritten: Option<String>,
}

/// `TestIamPermissionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestIamPermissionsResponse {
    /// kind property.
    pub kind: Option<String>,
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `Channel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Channel {
    /// address property.
    pub address: Option<String>,
    /// expiration property.
    pub expiration: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// params property.
    pub params: Option<serde_json::Value>,
    /// payload property.
    pub payload: Option<bool>,
    /// resourceId property.
    pub resource_id: Option<String>,
    /// resourceUri property.
    pub resource_uri: Option<String>,
    /// token property.
    pub token: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `BucketStorageLayout` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BucketStorageLayout {
    /// bucket property.
    pub bucket: Option<String>,
    /// customPlacementConfig property.
    pub custom_placement_config: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// hierarchicalNamespace property.
    pub hierarchical_namespace: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// kind property.
    pub kind: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// locationType property.
    pub location_type: Option<String>,
}

/// `AdvanceRelocateBucketOperationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvanceRelocateBucketOperationRequest {
    /// expireTime property.
    pub expire_time: Option<String>,
    /// ttl property.
    pub ttl: Option<String>,
}

/// `Policy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Policy {
    /// bindings property.
    pub bindings: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// etag property.
    pub etag: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// resourceId property.
    pub resource_id: Option<String>,
    /// version property.
    pub version: Option<i64>,
}

/// `ObjectAccessControls` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObjectAccessControls {
    /// items property.
    pub items: Option<Vec<ObjectAccessControl>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `GoogleRpcStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleRpcStatus {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `ServiceAccount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceAccount {
    /// email_address property.
    pub email_address: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `AnywhereCache` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnywhereCache {
    /// admissionPolicy property.
    pub admission_policy: Option<String>,
    /// anywhereCacheId property.
    pub anywhere_cache_id: Option<String>,
    /// bucket property.
    pub bucket: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// pendingUpdate property.
    pub pending_update: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// ttl property.
    pub ttl: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `ObjectAccessControl` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObjectAccessControl {
    /// bucket property.
    pub bucket: Option<String>,
    /// domain property.
    pub domain: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// entity property.
    pub entity: Option<String>,
    /// entityId property.
    pub entity_id: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// generation property.
    pub generation: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// object property.
    pub object: Option<String>,
    /// projectTeam property.
    pub project_team: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// role property.
    pub role: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`storage.anywhereCaches.disable_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageAnywhereCachesDisableArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `anywhereCacheId`.
    pub anywhere_cache_id: String,
}

/// Arguments for [`storage.anywhereCaches.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageAnywhereCachesGetArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `anywhereCacheId`.
    pub anywhere_cache_id: String,
}

/// Arguments for [`storage.anywhereCaches.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageAnywhereCachesInsertArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Request body.
    pub body: AnywhereCache,
}

/// Arguments for [`storage.anywhereCaches.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageAnywhereCachesListArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`storage.anywhereCaches.pause_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageAnywhereCachesPauseArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `anywhereCacheId`.
    pub anywhere_cache_id: String,
}

/// Arguments for [`storage.anywhereCaches.resume_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageAnywhereCachesResumeArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `anywhereCacheId`.
    pub anywhere_cache_id: String,
}

/// Arguments for [`storage.anywhereCaches.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageAnywhereCachesUpdateArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `anywhereCacheId`.
    pub anywhere_cache_id: String,
    /// Request body.
    pub body: AnywhereCache,
}

/// Arguments for [`storage.bucketAccessControls.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketAccessControlsDeleteArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.bucketAccessControls.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketAccessControlsGetArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.bucketAccessControls.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketAccessControlsInsertArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: BucketAccessControl,
}

/// Arguments for [`storage.bucketAccessControls.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketAccessControlsListArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.bucketAccessControls.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketAccessControlsPatchArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: BucketAccessControl,
}

/// Arguments for [`storage.bucketAccessControls.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketAccessControlsUpdateArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: BucketAccessControl,
}

/// Arguments for [`storage.buckets.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsDeleteArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.buckets.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsGetArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `softDeleted`.
    pub soft_deleted: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.buckets.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsGetIamPolicyArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.buckets.getStorageLayout_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsGetStorageLayoutArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
}

/// Arguments for [`storage.buckets.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsInsertArgs {
    /// Query parameter: `enableObjectRetention`.
    pub enable_object_retention: Option<String>,
    /// Query parameter: `predefinedAcl`.
    pub predefined_acl: Option<String>,
    /// Query parameter: `predefinedDefaultObjectAcl`.
    pub predefined_default_object_acl: Option<String>,
    /// Query parameter: `project`.
    pub project: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Bucket,
}

/// Arguments for [`storage.buckets.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsListArgs {
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
    /// Query parameter: `project`.
    pub project: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
    /// Query parameter: `softDeleted`.
    pub soft_deleted: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.buckets.lockRetentionPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsLockRetentionPolicyArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.buckets.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsPatchArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `predefinedAcl`.
    pub predefined_acl: Option<String>,
    /// Query parameter: `predefinedDefaultObjectAcl`.
    pub predefined_default_object_acl: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Bucket,
}

/// Arguments for [`storage.buckets.relocate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsRelocateArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Request body.
    pub body: RelocateBucketRequest,
}

/// Arguments for [`storage.buckets.restore_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsRestoreArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.buckets.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsSetIamPolicyArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Policy,
}

/// Arguments for [`storage.buckets.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsTestIamPermissionsArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `permissions`.
    pub permissions: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.buckets.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsUpdateArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `predefinedAcl`.
    pub predefined_acl: Option<String>,
    /// Query parameter: `predefinedDefaultObjectAcl`.
    pub predefined_default_object_acl: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Bucket,
}

/// Arguments for [`storage.channels.stop_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageChannelsStopArgs {
    /// Request body.
    pub body: Channel,
}

/// Arguments for [`storage.defaultObjectAccessControls.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageDefaultObjectAccessControlsDeleteArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.defaultObjectAccessControls.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageDefaultObjectAccessControlsGetArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.defaultObjectAccessControls.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageDefaultObjectAccessControlsInsertArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: ObjectAccessControl,
}

/// Arguments for [`storage.defaultObjectAccessControls.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageDefaultObjectAccessControlsListArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.defaultObjectAccessControls.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageDefaultObjectAccessControlsPatchArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: ObjectAccessControl,
}

/// Arguments for [`storage.defaultObjectAccessControls.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageDefaultObjectAccessControlsUpdateArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: ObjectAccessControl,
}

/// Arguments for [`storage.folders.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageFoldersDeleteArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `folder`.
    pub folder: String,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
}

/// Arguments for [`storage.folders.deleteRecursive_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageFoldersDeleteRecursiveArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `folder`.
    pub folder: String,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
}

/// Arguments for [`storage.folders.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageFoldersGetArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `folder`.
    pub folder: String,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
}

/// Arguments for [`storage.folders.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageFoldersInsertArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `recursive`.
    pub recursive: Option<String>,
    /// Request body.
    pub body: Folder,
}

/// Arguments for [`storage.folders.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageFoldersListArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `delimiter`.
    pub delimiter: Option<String>,
    /// Query parameter: `endOffset`.
    pub end_offset: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
    /// Query parameter: `startOffset`.
    pub start_offset: Option<String>,
}

/// Arguments for [`storage.folders.rename_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageFoldersRenameArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `sourceFolder`.
    pub source_folder: String,
    /// Path parameter: `destinationFolder`.
    pub destination_folder: String,
    /// Query parameter: `ifSourceMetagenerationMatch`.
    pub if_source_metageneration_match: Option<String>,
    /// Query parameter: `ifSourceMetagenerationNotMatch`.
    pub if_source_metageneration_not_match: Option<String>,
}

/// Arguments for [`storage.managedFolders.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageManagedFoldersDeleteArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `managedFolder`.
    pub managed_folder: String,
    /// Query parameter: `allowNonEmpty`.
    pub allow_non_empty: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
}

/// Arguments for [`storage.managedFolders.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageManagedFoldersGetArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `managedFolder`.
    pub managed_folder: String,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
}

/// Arguments for [`storage.managedFolders.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageManagedFoldersGetIamPolicyArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `managedFolder`.
    pub managed_folder: String,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.managedFolders.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageManagedFoldersInsertArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Request body.
    pub body: ManagedFolder,
}

/// Arguments for [`storage.managedFolders.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageManagedFoldersListArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
}

/// Arguments for [`storage.managedFolders.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageManagedFoldersSetIamPolicyArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `managedFolder`.
    pub managed_folder: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Policy,
}

/// Arguments for [`storage.managedFolders.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageManagedFoldersTestIamPermissionsArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `managedFolder`.
    pub managed_folder: String,
    /// Query parameter: `permissions`.
    pub permissions: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.notifications.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageNotificationsDeleteArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `notification`.
    pub notification: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.notifications.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageNotificationsGetArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `notification`.
    pub notification: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.notifications.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageNotificationsInsertArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Notification,
}

/// Arguments for [`storage.notifications.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageNotificationsListArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.objectAccessControls.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectAccessControlsDeleteArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.objectAccessControls.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectAccessControlsGetArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.objectAccessControls.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectAccessControlsInsertArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: ObjectAccessControl,
}

/// Arguments for [`storage.objectAccessControls.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectAccessControlsListArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.objectAccessControls.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectAccessControlsPatchArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: ObjectAccessControl,
}

/// Arguments for [`storage.objectAccessControls.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectAccessControlsUpdateArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Path parameter: `entity`.
    pub entity: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: ObjectAccessControl,
}

/// Arguments for [`storage.objects.bulkRestore_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsBulkRestoreArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Request body.
    pub body: BulkRestoreObjectsRequest,
}

/// Arguments for [`storage.objects.compose_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsComposeArgs {
    /// Path parameter: `destinationBucket`.
    pub destination_bucket: String,
    /// Path parameter: `destinationObject`.
    pub destination_object: String,
    /// Query parameter: `destinationPredefinedAcl`.
    pub destination_predefined_acl: Option<String>,
    /// Query parameter: `dropContextGroups`.
    pub drop_context_groups: Option<String>,
    /// Query parameter: `ifGenerationMatch`.
    pub if_generation_match: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `kmsKeyName`.
    pub kms_key_name: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: ComposeRequest,
}

/// Arguments for [`storage.objects.copy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsCopyArgs {
    /// Path parameter: `sourceBucket`.
    pub source_bucket: String,
    /// Path parameter: `sourceObject`.
    pub source_object: String,
    /// Path parameter: `destinationBucket`.
    pub destination_bucket: String,
    /// Path parameter: `destinationObject`.
    pub destination_object: String,
    /// Query parameter: `destinationKmsKeyName`.
    pub destination_kms_key_name: Option<String>,
    /// Query parameter: `destinationPredefinedAcl`.
    pub destination_predefined_acl: Option<String>,
    /// Query parameter: `ifGenerationMatch`.
    pub if_generation_match: Option<String>,
    /// Query parameter: `ifGenerationNotMatch`.
    pub if_generation_not_match: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `ifSourceGenerationMatch`.
    pub if_source_generation_match: Option<String>,
    /// Query parameter: `ifSourceGenerationNotMatch`.
    pub if_source_generation_not_match: Option<String>,
    /// Query parameter: `ifSourceMetagenerationMatch`.
    pub if_source_metageneration_match: Option<String>,
    /// Query parameter: `ifSourceMetagenerationNotMatch`.
    pub if_source_metageneration_not_match: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `sourceGeneration`.
    pub source_generation: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Object,
}

/// Arguments for [`storage.objects.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsDeleteArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `ifGenerationMatch`.
    pub if_generation_match: Option<String>,
    /// Query parameter: `ifGenerationNotMatch`.
    pub if_generation_not_match: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.objects.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsGetArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `ifGenerationMatch`.
    pub if_generation_match: Option<String>,
    /// Query parameter: `ifGenerationNotMatch`.
    pub if_generation_not_match: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `restoreToken`.
    pub restore_token: Option<String>,
    /// Query parameter: `softDeleted`.
    pub soft_deleted: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.objects.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsGetIamPolicyArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.objects.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsInsertArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `contentEncoding`.
    pub content_encoding: Option<String>,
    /// Query parameter: `ifGenerationMatch`.
    pub if_generation_match: Option<String>,
    /// Query parameter: `ifGenerationNotMatch`.
    pub if_generation_not_match: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `kmsKeyName`.
    pub kms_key_name: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `predefinedAcl`.
    pub predefined_acl: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Object,
}

/// Arguments for [`storage.objects.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsListArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `delimiter`.
    pub delimiter: Option<String>,
    /// Query parameter: `endOffset`.
    pub end_offset: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `includeFoldersAsPrefixes`.
    pub include_folders_as_prefixes: Option<String>,
    /// Query parameter: `includeTrailingDelimiter`.
    pub include_trailing_delimiter: Option<String>,
    /// Query parameter: `matchGlob`.
    pub match_glob: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `softDeleted`.
    pub soft_deleted: Option<String>,
    /// Query parameter: `startOffset`.
    pub start_offset: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Query parameter: `versions`.
    pub versions: Option<String>,
}

/// Arguments for [`storage.objects.move_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsMoveArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `sourceObject`.
    pub source_object: String,
    /// Path parameter: `destinationObject`.
    pub destination_object: String,
    /// Query parameter: `ifGenerationMatch`.
    pub if_generation_match: Option<String>,
    /// Query parameter: `ifGenerationNotMatch`.
    pub if_generation_not_match: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `ifSourceGenerationMatch`.
    pub if_source_generation_match: Option<String>,
    /// Query parameter: `ifSourceGenerationNotMatch`.
    pub if_source_generation_not_match: Option<String>,
    /// Query parameter: `ifSourceMetagenerationMatch`.
    pub if_source_metageneration_match: Option<String>,
    /// Query parameter: `ifSourceMetagenerationNotMatch`.
    pub if_source_metageneration_not_match: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.objects.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsPatchArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `ifGenerationMatch`.
    pub if_generation_match: Option<String>,
    /// Query parameter: `ifGenerationNotMatch`.
    pub if_generation_not_match: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `overrideUnlockedRetention`.
    pub override_unlocked_retention: Option<String>,
    /// Query parameter: `predefinedAcl`.
    pub predefined_acl: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Object,
}

/// Arguments for [`storage.objects.restore_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsRestoreArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Query parameter: `copySourceAcl`.
    pub copy_source_acl: Option<String>,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `ifGenerationMatch`.
    pub if_generation_match: Option<String>,
    /// Query parameter: `ifGenerationNotMatch`.
    pub if_generation_not_match: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `restoreToken`.
    pub restore_token: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.objects.rewrite_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsRewriteArgs {
    /// Path parameter: `sourceBucket`.
    pub source_bucket: String,
    /// Path parameter: `sourceObject`.
    pub source_object: String,
    /// Path parameter: `destinationBucket`.
    pub destination_bucket: String,
    /// Path parameter: `destinationObject`.
    pub destination_object: String,
    /// Query parameter: `destinationKmsKeyName`.
    pub destination_kms_key_name: Option<String>,
    /// Query parameter: `destinationPredefinedAcl`.
    pub destination_predefined_acl: Option<String>,
    /// Query parameter: `dropContextGroups`.
    pub drop_context_groups: Option<String>,
    /// Query parameter: `ifGenerationMatch`.
    pub if_generation_match: Option<String>,
    /// Query parameter: `ifGenerationNotMatch`.
    pub if_generation_not_match: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `ifSourceGenerationMatch`.
    pub if_source_generation_match: Option<String>,
    /// Query parameter: `ifSourceGenerationNotMatch`.
    pub if_source_generation_not_match: Option<String>,
    /// Query parameter: `ifSourceMetagenerationMatch`.
    pub if_source_metageneration_match: Option<String>,
    /// Query parameter: `ifSourceMetagenerationNotMatch`.
    pub if_source_metageneration_not_match: Option<String>,
    /// Query parameter: `maxBytesRewrittenPerCall`.
    pub max_bytes_rewritten_per_call: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `rewriteToken`.
    pub rewrite_token: Option<String>,
    /// Query parameter: `sourceGeneration`.
    pub source_generation: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Object,
}

/// Arguments for [`storage.objects.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsSetIamPolicyArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Policy,
}

/// Arguments for [`storage.objects.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsTestIamPermissionsArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `permissions`.
    pub permissions: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.objects.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsUpdateArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `object`.
    pub object: String,
    /// Query parameter: `generation`.
    pub generation: Option<String>,
    /// Query parameter: `ifGenerationMatch`.
    pub if_generation_match: Option<String>,
    /// Query parameter: `ifGenerationNotMatch`.
    pub if_generation_not_match: Option<String>,
    /// Query parameter: `ifMetagenerationMatch`.
    pub if_metageneration_match: Option<String>,
    /// Query parameter: `ifMetagenerationNotMatch`.
    pub if_metageneration_not_match: Option<String>,
    /// Query parameter: `overrideUnlockedRetention`.
    pub override_unlocked_retention: Option<String>,
    /// Query parameter: `predefinedAcl`.
    pub predefined_acl: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: Object,
}

/// Arguments for [`storage.objects.watchAll_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageObjectsWatchAllArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `delimiter`.
    pub delimiter: Option<String>,
    /// Query parameter: `endOffset`.
    pub end_offset: Option<String>,
    /// Query parameter: `includeTrailingDelimiter`.
    pub include_trailing_delimiter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `startOffset`.
    pub start_offset: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Query parameter: `versions`.
    pub versions: Option<String>,
    /// Request body.
    pub body: Channel,
}

/// Arguments for [`storage.buckets.operations.advanceRelocateBucket_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsOperationsAdvanceRelocateBucketArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `operationId`.
    pub operation_id: String,
    /// Request body.
    pub body: AdvanceRelocateBucketOperationRequest,
}

/// Arguments for [`storage.buckets.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsOperationsCancelArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `operationId`.
    pub operation_id: String,
}

/// Arguments for [`storage.buckets.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsOperationsGetArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Path parameter: `operationId`.
    pub operation_id: String,
}

/// Arguments for [`storage.buckets.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageBucketsOperationsListArgs {
    /// Path parameter: `bucket`.
    pub bucket: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`storage.projects.hmacKeys.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageProjectsHmacKeysCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `serviceAccountEmail`.
    pub service_account_email: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.projects.hmacKeys.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageProjectsHmacKeysDeleteArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `accessId`.
    pub access_id: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.projects.hmacKeys.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageProjectsHmacKeysGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `accessId`.
    pub access_id: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.projects.hmacKeys.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageProjectsHmacKeysListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `serviceAccountEmail`.
    pub service_account_email: Option<String>,
    /// Query parameter: `showDeletedKeys`.
    pub show_deleted_keys: Option<String>,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

/// Arguments for [`storage.projects.hmacKeys.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageProjectsHmacKeysUpdateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `accessId`.
    pub access_id: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
    /// Request body.
    pub body: HmacKeyMetadata,
}

/// Arguments for [`storage.projects.serviceAccount.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct StorageProjectsServiceAccountGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `userProject`.
    pub user_project: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST b/{bucket}/anywhereCaches/{anywhereCacheId}/disable
// -----------------------------------------------------------------------------

/// POST b/{bucket}/anywhereCaches/{anywhereCacheId}/disable.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_anywhere_caches_disable_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_anywhere_caches_disable_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageAnywhereCachesDisableArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnywhereCache>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/anywhereCaches/{}/disable",
        args.bucket, args.anywhere_cache_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AnywhereCache =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/anywhereCaches/{anywhereCacheId}
// -----------------------------------------------------------------------------

/// GET b/{bucket}/anywhereCaches/{anywhereCacheId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_anywhere_caches_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_anywhere_caches_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageAnywhereCachesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnywhereCache>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/anywhereCaches/{}",
        args.bucket, args.anywhere_cache_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AnywhereCache =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/anywhereCaches
// -----------------------------------------------------------------------------

/// POST b/{bucket}/anywhereCaches.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_anywhere_caches_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_anywhere_caches_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageAnywhereCachesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/anywhereCaches",
        args.bucket,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/anywhereCaches
// -----------------------------------------------------------------------------

/// GET b/{bucket}/anywhereCaches.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_anywhere_caches_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_anywhere_caches_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageAnywhereCachesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnywhereCaches>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/anywhereCaches",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AnywhereCaches =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/anywhereCaches/{anywhereCacheId}/pause
// -----------------------------------------------------------------------------

/// POST b/{bucket}/anywhereCaches/{anywhereCacheId}/pause.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_anywhere_caches_pause_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_anywhere_caches_pause_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageAnywhereCachesPauseArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnywhereCache>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/anywhereCaches/{}/pause",
        args.bucket, args.anywhere_cache_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AnywhereCache =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/anywhereCaches/{anywhereCacheId}/resume
// -----------------------------------------------------------------------------

/// POST b/{bucket}/anywhereCaches/{anywhereCacheId}/resume.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_anywhere_caches_resume_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_anywhere_caches_resume_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageAnywhereCachesResumeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnywhereCache>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/anywhereCaches/{}/resume",
        args.bucket, args.anywhere_cache_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AnywhereCache =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH b/{bucket}/anywhereCaches/{anywhereCacheId}
// -----------------------------------------------------------------------------

/// PATCH b/{bucket}/anywhereCaches/{anywhereCacheId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_anywhere_caches_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_anywhere_caches_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageAnywhereCachesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/anywhereCaches/{}",
        args.bucket, args.anywhere_cache_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE b/{bucket}/acl/{entity}
// -----------------------------------------------------------------------------

/// DELETE b/{bucket}/acl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_bucket_access_controls_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_bucket_access_controls_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketAccessControlsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/acl/{}",
        args.bucket, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/acl/{entity}
// -----------------------------------------------------------------------------

/// GET b/{bucket}/acl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_bucket_access_controls_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_bucket_access_controls_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketAccessControlsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BucketAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/acl/{}",
        args.bucket, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BucketAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/acl
// -----------------------------------------------------------------------------

/// POST b/{bucket}/acl.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_bucket_access_controls_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_bucket_access_controls_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketAccessControlsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BucketAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/acl",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BucketAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/acl
// -----------------------------------------------------------------------------

/// GET b/{bucket}/acl.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_bucket_access_controls_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_bucket_access_controls_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketAccessControlsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BucketAccessControls>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/acl",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BucketAccessControls =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH b/{bucket}/acl/{entity}
// -----------------------------------------------------------------------------

/// PATCH b/{bucket}/acl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_bucket_access_controls_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_bucket_access_controls_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketAccessControlsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BucketAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/acl/{}",
        args.bucket, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BucketAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT b/{bucket}/acl/{entity}
// -----------------------------------------------------------------------------

/// PUT b/{bucket}/acl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_bucket_access_controls_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_bucket_access_controls_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketAccessControlsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BucketAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/acl/{}",
        args.bucket, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BucketAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE b/{bucket}
// -----------------------------------------------------------------------------

/// DELETE b/{bucket}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}
// -----------------------------------------------------------------------------

/// GET b/{bucket}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bucket>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.soft_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("softDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Bucket =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/iam
// -----------------------------------------------------------------------------

/// GET b/{bucket}/iam.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/iam",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/storageLayout
// -----------------------------------------------------------------------------

/// GET b/{bucket}/storageLayout.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_get_storage_layout_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_get_storage_layout_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsGetStorageLayoutArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BucketStorageLayout>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/storageLayout",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BucketStorageLayout =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b
// -----------------------------------------------------------------------------

/// POST b.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bucket>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://storage.googleapis.com/storage/v1/b",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.enable_object_retention {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enableObjectRetention=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.predefined_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("predefinedAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.predefined_default_object_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("predefinedDefaultObjectAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("project=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Bucket =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b
// -----------------------------------------------------------------------------

/// GET b.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Buckets>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://storage.googleapis.com/storage/v1/b",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("project=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.soft_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("softDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Buckets =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/lockRetentionPolicy
// -----------------------------------------------------------------------------

/// POST b/{bucket}/lockRetentionPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_lock_retention_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_lock_retention_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsLockRetentionPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bucket>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/lockRetentionPolicy",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Bucket =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH b/{bucket}
// -----------------------------------------------------------------------------

/// PATCH b/{bucket}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bucket>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.predefined_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("predefinedAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.predefined_default_object_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("predefinedDefaultObjectAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Bucket =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/relocate
// -----------------------------------------------------------------------------

/// POST b/{bucket}/relocate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_relocate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_relocate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsRelocateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/relocate",
        args.bucket,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/restore
// -----------------------------------------------------------------------------

/// POST b/{bucket}/restore.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_restore_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_restore_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsRestoreArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bucket>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/restore",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Bucket =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT b/{bucket}/iam
// -----------------------------------------------------------------------------

/// PUT b/{bucket}/iam.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/iam",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/iam/testPermissions
// -----------------------------------------------------------------------------

/// GET b/{bucket}/iam/testPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/iam/testPermissions",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.permissions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("permissions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT b/{bucket}
// -----------------------------------------------------------------------------

/// PUT b/{bucket}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bucket>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.predefined_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("predefinedAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.predefined_default_object_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("predefinedDefaultObjectAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Bucket =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST channels/stop
// -----------------------------------------------------------------------------

/// POST channels/stop.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_channels_stop_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_channels_stop_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageChannelsStopArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://storage.googleapis.com/storage/v1/channels/stop",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE b/{bucket}/defaultObjectAcl/{entity}
// -----------------------------------------------------------------------------

/// DELETE b/{bucket}/defaultObjectAcl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_default_object_access_controls_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_default_object_access_controls_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageDefaultObjectAccessControlsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/defaultObjectAcl/{}",
        args.bucket, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/defaultObjectAcl/{entity}
// -----------------------------------------------------------------------------

/// GET b/{bucket}/defaultObjectAcl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_default_object_access_controls_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_default_object_access_controls_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageDefaultObjectAccessControlsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObjectAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/defaultObjectAcl/{}",
        args.bucket, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ObjectAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/defaultObjectAcl
// -----------------------------------------------------------------------------

/// POST b/{bucket}/defaultObjectAcl.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_default_object_access_controls_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_default_object_access_controls_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageDefaultObjectAccessControlsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObjectAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/defaultObjectAcl",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ObjectAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/defaultObjectAcl
// -----------------------------------------------------------------------------

/// GET b/{bucket}/defaultObjectAcl.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_default_object_access_controls_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_default_object_access_controls_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageDefaultObjectAccessControlsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObjectAccessControls>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/defaultObjectAcl",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ObjectAccessControls =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH b/{bucket}/defaultObjectAcl/{entity}
// -----------------------------------------------------------------------------

/// PATCH b/{bucket}/defaultObjectAcl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_default_object_access_controls_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_default_object_access_controls_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageDefaultObjectAccessControlsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObjectAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/defaultObjectAcl/{}",
        args.bucket, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ObjectAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT b/{bucket}/defaultObjectAcl/{entity}
// -----------------------------------------------------------------------------

/// PUT b/{bucket}/defaultObjectAcl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_default_object_access_controls_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_default_object_access_controls_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageDefaultObjectAccessControlsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObjectAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/defaultObjectAcl/{}",
        args.bucket, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ObjectAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE b/{bucket}/folders/{folder}
// -----------------------------------------------------------------------------

/// DELETE b/{bucket}/folders/{folder}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_folders_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_folders_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageFoldersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/folders/{}",
        args.bucket, args.folder,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/folders/{folder}/deleteRecursive
// -----------------------------------------------------------------------------

/// POST b/{bucket}/folders/{folder}/deleteRecursive.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_folders_delete_recursive_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_folders_delete_recursive_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageFoldersDeleteRecursiveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/folders/{}/deleteRecursive",
        args.bucket, args.folder,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/folders/{folder}
// -----------------------------------------------------------------------------

/// GET b/{bucket}/folders/{folder}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_folders_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_folders_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageFoldersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Folder>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/folders/{}",
        args.bucket, args.folder,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Folder =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/folders
// -----------------------------------------------------------------------------

/// POST b/{bucket}/folders.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_folders_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_folders_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageFoldersInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Folder>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/folders",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.recursive {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("recursive=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Folder =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/folders
// -----------------------------------------------------------------------------

/// GET b/{bucket}/folders.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_folders_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_folders_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageFoldersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Folders>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/folders",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.delimiter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("delimiter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Folders =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/folders/{sourceFolder}/renameTo/folders/{destinationFolder}
// -----------------------------------------------------------------------------

/// POST b/{bucket}/folders/{sourceFolder}/renameTo/folders/{destinationFolder}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_folders_rename_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_folders_rename_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageFoldersRenameArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/folders/{}/renameTo/folders/{}",
        args.bucket, args.source_folder, args.destination_folder,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.if_source_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE b/{bucket}/managedFolders/{managedFolder}
// -----------------------------------------------------------------------------

/// DELETE b/{bucket}/managedFolders/{managedFolder}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_managed_folders_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_managed_folders_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageManagedFoldersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/managedFolders/{}",
        args.bucket, args.managed_folder,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_non_empty {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowNonEmpty=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/managedFolders/{managedFolder}
// -----------------------------------------------------------------------------

/// GET b/{bucket}/managedFolders/{managedFolder}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_managed_folders_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_managed_folders_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageManagedFoldersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManagedFolder>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/managedFolders/{}",
        args.bucket, args.managed_folder,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ManagedFolder =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/managedFolders/{managedFolder}/iam
// -----------------------------------------------------------------------------

/// GET b/{bucket}/managedFolders/{managedFolder}/iam.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_managed_folders_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_managed_folders_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageManagedFoldersGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/managedFolders/{}/iam",
        args.bucket, args.managed_folder,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/managedFolders
// -----------------------------------------------------------------------------

/// POST b/{bucket}/managedFolders.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_managed_folders_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_managed_folders_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageManagedFoldersInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManagedFolder>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/managedFolders",
        args.bucket,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ManagedFolder =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/managedFolders
// -----------------------------------------------------------------------------

/// GET b/{bucket}/managedFolders.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_managed_folders_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_managed_folders_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageManagedFoldersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManagedFolders>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/managedFolders",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ManagedFolders =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT b/{bucket}/managedFolders/{managedFolder}/iam
// -----------------------------------------------------------------------------

/// PUT b/{bucket}/managedFolders/{managedFolder}/iam.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_managed_folders_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_managed_folders_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageManagedFoldersSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/managedFolders/{}/iam",
        args.bucket, args.managed_folder,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/managedFolders/{managedFolder}/iam/testPermissions
// -----------------------------------------------------------------------------

/// GET b/{bucket}/managedFolders/{managedFolder}/iam/testPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_managed_folders_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_managed_folders_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageManagedFoldersTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/managedFolders/{}/iam/testPermissions",
        args.bucket, args.managed_folder,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.permissions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("permissions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE b/{bucket}/notificationConfigs/{notification}
// -----------------------------------------------------------------------------

/// DELETE b/{bucket}/notificationConfigs/{notification}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_notifications_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_notifications_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageNotificationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/notificationConfigs/{}",
        args.bucket, args.notification,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/notificationConfigs/{notification}
// -----------------------------------------------------------------------------

/// GET b/{bucket}/notificationConfigs/{notification}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_notifications_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_notifications_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageNotificationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Notification>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/notificationConfigs/{}",
        args.bucket, args.notification,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Notification =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/notificationConfigs
// -----------------------------------------------------------------------------

/// POST b/{bucket}/notificationConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_notifications_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_notifications_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageNotificationsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Notification>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/notificationConfigs",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Notification =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/notificationConfigs
// -----------------------------------------------------------------------------

/// GET b/{bucket}/notificationConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_notifications_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_notifications_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageNotificationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Notifications>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/notificationConfigs",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Notifications =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE b/{bucket}/o/{object}/acl/{entity}
// -----------------------------------------------------------------------------

/// DELETE b/{bucket}/o/{object}/acl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_object_access_controls_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_object_access_controls_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectAccessControlsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/acl/{}",
        args.bucket, args.object, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/o/{object}/acl/{entity}
// -----------------------------------------------------------------------------

/// GET b/{bucket}/o/{object}/acl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_object_access_controls_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_object_access_controls_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectAccessControlsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObjectAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/acl/{}",
        args.bucket, args.object, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ObjectAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/o/{object}/acl
// -----------------------------------------------------------------------------

/// POST b/{bucket}/o/{object}/acl.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_object_access_controls_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_object_access_controls_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectAccessControlsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObjectAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/acl",
        args.bucket, args.object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ObjectAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/o/{object}/acl
// -----------------------------------------------------------------------------

/// GET b/{bucket}/o/{object}/acl.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_object_access_controls_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_object_access_controls_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectAccessControlsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObjectAccessControls>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/acl",
        args.bucket, args.object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ObjectAccessControls =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH b/{bucket}/o/{object}/acl/{entity}
// -----------------------------------------------------------------------------

/// PATCH b/{bucket}/o/{object}/acl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_object_access_controls_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_object_access_controls_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectAccessControlsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObjectAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/acl/{}",
        args.bucket, args.object, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ObjectAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT b/{bucket}/o/{object}/acl/{entity}
// -----------------------------------------------------------------------------

/// PUT b/{bucket}/o/{object}/acl/{entity}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_object_access_controls_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_object_access_controls_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectAccessControlsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ObjectAccessControl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/acl/{}",
        args.bucket, args.object, args.entity,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ObjectAccessControl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/o/bulkRestore
// -----------------------------------------------------------------------------

/// POST b/{bucket}/o/bulkRestore.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_bulk_restore_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_bulk_restore_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsBulkRestoreArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/bulkRestore",
        args.bucket,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{destinationBucket}/o/{destinationObject}/compose
// -----------------------------------------------------------------------------

/// POST b/{destinationBucket}/o/{destinationObject}/compose.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_compose_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_compose_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsComposeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Object>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/compose",
        args.destination_bucket, args.destination_object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.destination_predefined_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("destinationPredefinedAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.drop_context_groups {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dropContextGroups=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.kms_key_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("kmsKeyName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Object =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{sourceBucket}/o/{sourceObject}/copyTo/b/{destinationBucket}/o/{destinationObject}
// -----------------------------------------------------------------------------

/// POST b/{sourceBucket}/o/{sourceObject}/copyTo/b/{destinationBucket}/o/{destinationObject}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_copy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_copy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsCopyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Object>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/copyTo/b/{}/o/{}",
        args.source_bucket, args.source_object, args.destination_bucket, args.destination_object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.destination_kms_key_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("destinationKmsKeyName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.destination_predefined_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("destinationPredefinedAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceGeneration=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Object =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE b/{bucket}/o/{object}
// -----------------------------------------------------------------------------

/// DELETE b/{bucket}/o/{object}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}",
        args.bucket, args.object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/o/{object}
// -----------------------------------------------------------------------------

/// GET b/{bucket}/o/{object}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Object>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}",
        args.bucket, args.object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.restore_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("restoreToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.soft_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("softDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Object =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/o/{object}/iam
// -----------------------------------------------------------------------------

/// GET b/{bucket}/o/{object}/iam.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/iam",
        args.bucket, args.object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/o
// -----------------------------------------------------------------------------

/// POST b/{bucket}/o.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Object>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.content_encoding {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contentEncoding=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.kms_key_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("kmsKeyName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.predefined_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("predefinedAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Object =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/o
// -----------------------------------------------------------------------------

/// GET b/{bucket}/o.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Objects>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.delimiter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("delimiter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_folders_as_prefixes {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeFoldersAsPrefixes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_trailing_delimiter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeTrailingDelimiter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.match_glob {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("matchGlob=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.soft_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("softDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.versions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("versions=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Objects =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/o/{sourceObject}/moveTo/o/{destinationObject}
// -----------------------------------------------------------------------------

/// POST b/{bucket}/o/{sourceObject}/moveTo/o/{destinationObject}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_move_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_move_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsMoveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Object>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/moveTo/o/{}",
        args.bucket, args.source_object, args.destination_object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.if_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Object =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH b/{bucket}/o/{object}
// -----------------------------------------------------------------------------

/// PATCH b/{bucket}/o/{object}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Object>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}",
        args.bucket, args.object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.override_unlocked_retention {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("overrideUnlockedRetention=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.predefined_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("predefinedAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Object =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/o/{object}/restore
// -----------------------------------------------------------------------------

/// POST b/{bucket}/o/{object}/restore.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_restore_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_restore_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsRestoreArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Object>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/restore",
        args.bucket, args.object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.copy_source_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("copySourceAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.restore_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("restoreToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Object =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{sourceBucket}/o/{sourceObject}/rewriteTo/b/{destinationBucket}/o/{destinationObject}
// -----------------------------------------------------------------------------

/// POST b/{sourceBucket}/o/{sourceObject}/rewriteTo/b/{destinationBucket}/o/{destinationObject}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_rewrite_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_rewrite_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsRewriteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RewriteResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/rewriteTo/b/{}/o/{}",
        args.source_bucket, args.source_object, args.destination_bucket, args.destination_object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.destination_kms_key_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("destinationKmsKeyName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.destination_predefined_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("destinationPredefinedAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.drop_context_groups {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dropContextGroups=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_source_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifSourceMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_bytes_rewritten_per_call {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxBytesRewrittenPerCall=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.rewrite_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("rewriteToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source_generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sourceGeneration=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RewriteResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT b/{bucket}/o/{object}/iam
// -----------------------------------------------------------------------------

/// PUT b/{bucket}/o/{object}/iam.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/iam",
        args.bucket, args.object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/o/{object}/iam/testPermissions
// -----------------------------------------------------------------------------

/// GET b/{bucket}/o/{object}/iam/testPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}/iam/testPermissions",
        args.bucket, args.object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.permissions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("permissions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT b/{bucket}/o/{object}
// -----------------------------------------------------------------------------

/// PUT b/{bucket}/o/{object}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Object>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/{}",
        args.bucket, args.object,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.generation {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("generation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_generation_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifGenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.if_metageneration_not_match {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ifMetagenerationNotMatch=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.override_unlocked_retention {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("overrideUnlockedRetention=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.predefined_acl {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("predefinedAcl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Object =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/o/watch
// -----------------------------------------------------------------------------

/// POST b/{bucket}/o/watch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_objects_watch_all_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_objects_watch_all_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageObjectsWatchAllArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/o/watch",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.delimiter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("delimiter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_trailing_delimiter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeTrailingDelimiter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.versions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("versions=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Channel =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/operations/{operationId}/advanceRelocateBucket
// -----------------------------------------------------------------------------

/// POST b/{bucket}/operations/{operationId}/advanceRelocateBucket.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_operations_advance_relocate_bucket_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_operations_advance_relocate_bucket_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsOperationsAdvanceRelocateBucketArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/operations/{}/advanceRelocateBucket",
        args.bucket, args.operation_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST b/{bucket}/operations/{operationId}/cancel
// -----------------------------------------------------------------------------

/// POST b/{bucket}/operations/{operationId}/cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/operations/{}/cancel",
        args.bucket, args.operation_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/operations/{operationId}
// -----------------------------------------------------------------------------

/// GET b/{bucket}/operations/{operationId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/operations/{}",
        args.bucket, args.operation_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET b/{bucket}/operations
// -----------------------------------------------------------------------------

/// GET b/{bucket}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_buckets_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_buckets_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageBucketsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleLongrunningListOperationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/b/{}/operations",
        args.bucket,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{projectId}/hmacKeys
// -----------------------------------------------------------------------------

/// POST projects/{projectId}/hmacKeys.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_projects_hmac_keys_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_projects_hmac_keys_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageProjectsHmacKeysCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HmacKey>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/projects/{}/hmacKeys",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.service_account_email {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceAccountEmail=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: HmacKey =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE projects/{projectId}/hmacKeys/{accessId}
// -----------------------------------------------------------------------------

/// DELETE projects/{projectId}/hmacKeys/{accessId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_projects_hmac_keys_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_projects_hmac_keys_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageProjectsHmacKeysDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/projects/{}/hmacKeys/{}",
        args.project_id, args.access_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{projectId}/hmacKeys/{accessId}
// -----------------------------------------------------------------------------

/// GET projects/{projectId}/hmacKeys/{accessId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_projects_hmac_keys_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_projects_hmac_keys_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageProjectsHmacKeysGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HmacKeyMetadata>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/projects/{}/hmacKeys/{}",
        args.project_id, args.access_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: HmacKeyMetadata =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{projectId}/hmacKeys
// -----------------------------------------------------------------------------

/// GET projects/{projectId}/hmacKeys.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_projects_hmac_keys_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_projects_hmac_keys_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageProjectsHmacKeysListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HmacKeysMetadata>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/projects/{}/hmacKeys",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.service_account_email {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serviceAccountEmail=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.show_deleted_keys {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("showDeletedKeys=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: HmacKeysMetadata =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{projectId}/hmacKeys/{accessId}
// -----------------------------------------------------------------------------

/// PUT projects/{projectId}/hmacKeys/{accessId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_projects_hmac_keys_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_projects_hmac_keys_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageProjectsHmacKeysUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HmacKeyMetadata>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/projects/{}/hmacKeys/{}",
        args.project_id, args.access_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: HmacKeyMetadata =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{projectId}/serviceAccount
// -----------------------------------------------------------------------------

/// GET projects/{projectId}/serviceAccount.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = storage_projects_service_account_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn storage_projects_service_account_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &StorageProjectsServiceAccountGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceAccount>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://storage.googleapis.com/storage/v1/projects/{}/serviceAccount",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.user_project {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userProject=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ServiceAccount =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
