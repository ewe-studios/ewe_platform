//! Auto-generated API module for gcp/dns operations.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dns_operations `

#![cfg(feature = "gcp_dns_operations")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ManagedZoneReverseLookupConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZoneReverseLookupConfig {
    /// kind property.
    pub kind: Option<String>,
}

/// `ManagedZonePeeringConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZonePeeringConfig {
    /// kind property.
    pub kind: Option<String>,
    /// targetNetwork property.
    pub target_network: Option<ManagedZonePeeringConfigTargetNetwork>,
}

/// `ManagedZonePeeringConfigTargetNetwork` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZonePeeringConfigTargetNetwork {
    /// deactivateTime property.
    pub deactivate_time: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// networkUrl property.
    pub network_url: Option<String>,
}

/// `ManagedZone` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZone {
    /// cloudLoggingConfig property.
    pub cloud_logging_config: Option<ManagedZoneCloudLoggingConfig>,
    /// creationTime property.
    pub creation_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// dnsName property.
    pub dns_name: Option<String>,
    /// dnssecConfig property.
    pub dnssec_config: Option<ManagedZoneDnsSecConfig>,
    /// forwardingConfig property.
    pub forwarding_config: Option<ManagedZoneForwardingConfig>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// nameServerSet property.
    pub name_server_set: Option<String>,
    /// nameServers property.
    pub name_servers: Option<Vec<String>>,
    /// peeringConfig property.
    pub peering_config: Option<ManagedZonePeeringConfig>,
    /// privateVisibilityConfig property.
    pub private_visibility_config: Option<ManagedZonePrivateVisibilityConfig>,
    /// reverseLookupConfig property.
    pub reverse_lookup_config: Option<ManagedZoneReverseLookupConfig>,
    /// serviceDirectoryConfig property.
    pub service_directory_config: Option<ManagedZoneServiceDirectoryConfig>,
    /// visibility property.
    pub visibility: Option<String>,
}

/// `ManagedZoneDnsSecConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZoneDnsSecConfig {
    /// defaultKeySpecs property.
    pub default_key_specs: Option<Vec<DnsKeySpec>>,
    /// kind property.
    pub kind: Option<String>,
    /// nonExistence property.
    pub non_existence: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `ManagedZonePrivateVisibilityConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZonePrivateVisibilityConfig {
    /// gkeClusters property.
    pub gke_clusters: Option<Vec<ManagedZonePrivateVisibilityConfigGKECluster>>,
    /// kind property.
    pub kind: Option<String>,
    /// networks property.
    pub networks: Option<Vec<ManagedZonePrivateVisibilityConfigNetwork>>,
}

/// `ManagedZonePrivateVisibilityConfigNetwork` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZonePrivateVisibilityConfigNetwork {
    /// kind property.
    pub kind: Option<String>,
    /// networkUrl property.
    pub network_url: Option<String>,
}

/// `OperationDnsKeyContext` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OperationDnsKeyContext {
    /// newValue property.
    pub new_value: Option<DnsKey>,
    /// oldValue property.
    pub old_value: Option<DnsKey>,
}

/// `ManagedZoneForwardingConfigNameServerTarget` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZoneForwardingConfigNameServerTarget {
    /// domainName property.
    pub domain_name: Option<String>,
    /// forwardingPath property.
    pub forwarding_path: Option<String>,
    /// ipv4Address property.
    pub ipv4_address: Option<String>,
    /// ipv6Address property.
    pub ipv6_address: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `ManagedZoneCloudLoggingConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZoneCloudLoggingConfig {
    /// enableLogging property.
    pub enable_logging: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
}

/// `ManagedZoneServiceDirectoryConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZoneServiceDirectoryConfig {
    /// kind property.
    pub kind: Option<String>,
    /// namespace property.
    pub namespace: Option<ManagedZoneServiceDirectoryConfigNamespace>,
}

/// `DnsKey` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DnsKey {
    /// algorithm property.
    pub algorithm: Option<String>,
    /// creationTime property.
    pub creation_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// digests property.
    pub digests: Option<Vec<DnsKeyDigest>>,
    /// id property.
    pub id: Option<String>,
    /// isActive property.
    pub is_active: Option<bool>,
    /// keyLength property.
    pub key_length: Option<i64>,
    /// keyTag property.
    pub key_tag: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// publicKey property.
    pub public_key: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `DnsKeyDigest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DnsKeyDigest {
    /// digest property.
    pub digest: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `DnsKeySpec` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DnsKeySpec {
    /// algorithm property.
    pub algorithm: Option<String>,
    /// keyLength property.
    pub key_length: Option<i64>,
    /// keyType property.
    pub key_type: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `ManagedZoneForwardingConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZoneForwardingConfig {
    /// kind property.
    pub kind: Option<String>,
    /// targetNameServers property.
    pub target_name_servers: Option<Vec<ManagedZoneForwardingConfigNameServerTarget>>,
}

/// `ManagedZoneServiceDirectoryConfigNamespace` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZoneServiceDirectoryConfigNamespace {
    /// deletionTime property.
    pub deletion_time: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// namespaceUrl property.
    pub namespace_url: Option<String>,
}

/// `ManagedZonePrivateVisibilityConfigGKECluster` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZonePrivateVisibilityConfigGKECluster {
    /// gkeClusterName property.
    pub gke_cluster_name: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `ManagedZoneOperationsListResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedZoneOperationsListResponse {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<Operation>>,
}

/// `OperationManagedZoneContext` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OperationManagedZoneContext {
    /// newValue property.
    pub new_value: Option<ManagedZone>,
    /// oldValue property.
    pub old_value: Option<ManagedZone>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dns.managedZoneOperations.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZoneOperationsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `managedZone`.
    pub managed_zone: String,
    /// Path parameter: `operation`.
    pub operation: String,
    /// Query parameter: `clientOperationId`.
    pub client_operation_id: Option<String>,
}

/// Arguments for [`dns.managedZoneOperations.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZoneOperationsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `managedZone`.
    pub managed_zone: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `sortBy`.
    pub sort_by: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET dns/v1/projects/{project}/managedZones/{managedZone}/operations/{operation}
// -----------------------------------------------------------------------------

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/operations/{operation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dns_managed_zone_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dns_managed_zone_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DnsManagedZoneOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/operations/{}",
        args.project, args.managed_zone, args.operation,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET dns/v1/projects/{project}/managedZones/{managedZone}/operations
// -----------------------------------------------------------------------------

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dns_managed_zone_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dns_managed_zone_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DnsManagedZoneOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManagedZoneOperationsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/operations",
        args.project, args.managed_zone,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ManagedZoneOperationsListResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
