//! Auto-generated API module for gcp/chromepolicy chromepolicy.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_chromepolicy_chromepolicy `

#![cfg(feature = "gcp_chromepolicy_chromepolicy")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleChromePolicyVersionsV1ListPolicySchemasResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1ListPolicySchemasResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// policySchemas property.
    pub policy_schemas: Option<Vec<GoogleChromePolicyVersionsV1PolicySchema>>,
}

/// `GoogleProtobufEmpty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleProtobufEmpty {}

/// `GoogleChromePolicyVersionsV1UploadPolicyFileResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1UploadPolicyFileResponse {
    /// downloadUri property.
    pub download_uri: Option<String>,
}

/// `GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse {
    /// groupIds property.
    pub group_ids: Option<Vec<String>>,
    /// policyNamespace property.
    pub policy_namespace: Option<String>,
    /// policySchema property.
    pub policy_schema: Option<String>,
    /// policyTargetKey property.
    pub policy_target_key: Option<GoogleChromePolicyVersionsV1PolicyTargetKey>,
}

/// `GoogleChromePolicyVersionsV1ListGroupPriorityOrderingRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1ListGroupPriorityOrderingRequest {
    /// policyNamespace property.
    pub policy_namespace: Option<String>,
    /// policySchema property.
    pub policy_schema: Option<String>,
    /// policyTargetKey property.
    pub policy_target_key: Option<GoogleChromePolicyVersionsV1PolicyTargetKey>,
}

/// `GoogleChromePolicyVersionsV1PolicySchemaRequiredItems` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1PolicySchemaRequiredItems {
    /// fieldConditions property.
    pub field_conditions: Option<Vec<String>>,
    /// requiredFields property.
    pub required_fields: Option<Vec<String>>,
}

/// `GoogleChromePolicyVersionsV1RemoveNetworkResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1RemoveNetworkResponse {}

/// `GoogleChromePolicyVersionsV1RemoveCertificateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1RemoveCertificateRequest {
    /// networkId property.
    pub network_id: Option<String>,
    /// targetResource property.
    pub target_resource: Option<String>,
}

/// `GoogleChromePolicyVersionsV1PolicySchemaNoticeDescription` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1PolicySchemaNoticeDescription {
    /// acknowledgementRequired property.
    pub acknowledgement_required: Option<bool>,
    /// field property.
    pub field: Option<String>,
    /// noticeMessage property.
    pub notice_message: Option<String>,
    /// noticeValue property.
    pub notice_value: Option<String>,
}

/// `GoogleChromePolicyVersionsV1ModifyOrgUnitPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1ModifyOrgUnitPolicyRequest {
    /// policyTargetKey property.
    pub policy_target_key: Option<GoogleChromePolicyVersionsV1PolicyTargetKey>,
    /// policyValue property.
    pub policy_value: Option<GoogleChromePolicyVersionsV1PolicyValue>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `GoogleChromePolicyVersionsV1InheritOrgUnitPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1InheritOrgUnitPolicyRequest {
    /// policySchema property.
    pub policy_schema: Option<String>,
    /// policyTargetKey property.
    pub policy_target_key: Option<GoogleChromePolicyVersionsV1PolicyTargetKey>,
}

/// `Proto2FieldDescriptorProto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Proto2FieldDescriptorProto {
    /// defaultValue property.
    pub default_value: Option<String>,
    /// jsonName property.
    pub json_name: Option<String>,
    /// label property.
    pub label: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// number property.
    pub number: Option<i64>,
    /// oneofIndex property.
    pub oneof_index: Option<i64>,
    /// proto3Optional property.
    pub proto3_optional: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
    /// typeName property.
    pub type_name: Option<String>,
}

/// `GoogleChromePolicyVersionsV1ModifyGroupPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1ModifyGroupPolicyRequest {
    /// policyTargetKey property.
    pub policy_target_key: Option<GoogleChromePolicyVersionsV1PolicyTargetKey>,
    /// policyValue property.
    pub policy_value: Option<GoogleChromePolicyVersionsV1PolicyValue>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `GoogleChromePolicyVersionsV1BatchInheritOrgUnitPoliciesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1BatchInheritOrgUnitPoliciesRequest {
    /// requests property.
    pub requests: Option<Vec<GoogleChromePolicyVersionsV1InheritOrgUnitPolicyRequest>>,
}

/// `Proto2OneofDescriptorProto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Proto2OneofDescriptorProto {
    /// name property.
    pub name: Option<String>,
}

/// `GoogleChromePolicyVersionsV1ResolvedPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1ResolvedPolicy {
    /// addedSourceKey property.
    pub added_source_key: Option<GoogleChromePolicyVersionsV1PolicyTargetKey>,
    /// sourceKey property.
    pub source_key: Option<GoogleChromePolicyVersionsV1PolicyTargetKey>,
    /// targetKey property.
    pub target_key: Option<GoogleChromePolicyVersionsV1PolicyTargetKey>,
    /// value property.
    pub value: Option<GoogleChromePolicyVersionsV1PolicyValue>,
}

/// `GoogleChromePolicyVersionsV1NumericRangeConstraint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1NumericRangeConstraint {
    /// maximum property.
    pub maximum: Option<String>,
    /// minimum property.
    pub minimum: Option<String>,
}

/// `GoogleChromePolicyVersionsV1BatchDeleteGroupPoliciesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1BatchDeleteGroupPoliciesRequest {
    /// requests property.
    pub requests: Option<Vec<GoogleChromePolicyVersionsV1DeleteGroupPolicyRequest>>,
}

/// `GoogleChromePolicyVersionsV1UpdateGroupPriorityOrderingRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1UpdateGroupPriorityOrderingRequest {
    /// groupIds property.
    pub group_ids: Option<Vec<String>>,
    /// policyNamespace property.
    pub policy_namespace: Option<String>,
    /// policySchema property.
    pub policy_schema: Option<String>,
    /// policyTargetKey property.
    pub policy_target_key: Option<GoogleChromePolicyVersionsV1PolicyTargetKey>,
}

/// `GoogleChromePolicyVersionsV1BatchModifyOrgUnitPoliciesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1BatchModifyOrgUnitPoliciesRequest {
    /// requests property.
    pub requests: Option<Vec<GoogleChromePolicyVersionsV1ModifyOrgUnitPolicyRequest>>,
}

/// `GoogleChromePolicyVersionsV1DeleteGroupPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1DeleteGroupPolicyRequest {
    /// policySchema property.
    pub policy_schema: Option<String>,
    /// policyTargetKey property.
    pub policy_target_key: Option<GoogleChromePolicyVersionsV1PolicyTargetKey>,
}

/// `GoogleChromePolicyVersionsV1PolicyApiLifecycle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1PolicyApiLifecycle {
    /// deprecatedInFavorOf property.
    pub deprecated_in_favor_of: Option<Vec<String>>,
    /// description property.
    pub description: Option<String>,
    /// endSupport property.
    pub end_support: Option<GoogleTypeDate>,
    /// policyApiLifecycleStage property.
    pub policy_api_lifecycle_stage: Option<String>,
    /// scheduledToDeprecatePolicies property.
    pub scheduled_to_deprecate_policies: Option<Vec<String>>,
}

/// `GoogleChromePolicyVersionsV1FieldConstraints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1FieldConstraints {
    /// numericRangeConstraint property.
    pub numeric_range_constraint: Option<GoogleChromePolicyVersionsV1NumericRangeConstraint>,
    /// uploadedFileConstraints property.
    pub uploaded_file_constraints: Option<GoogleChromePolicyVersionsV1UploadedFileConstraints>,
}

/// `Proto2EnumValueDescriptorProto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Proto2EnumValueDescriptorProto {
    /// name property.
    pub name: Option<String>,
    /// number property.
    pub number: Option<i64>,
}

/// `Proto2EnumDescriptorProto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Proto2EnumDescriptorProto {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<Vec<Proto2EnumValueDescriptorProto>>,
    /// visibility property.
    pub visibility: Option<String>,
}

/// `GoogleChromePolicyVersionsV1AdditionalTargetKeyName` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1AdditionalTargetKeyName {
    /// key property.
    pub key: Option<String>,
    /// keyDescription property.
    pub key_description: Option<String>,
}

/// `GoogleChromePolicyVersionsV1RemoveCertificateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1RemoveCertificateResponse {}

/// `GoogleChromePolicyVersionsV1PolicySchemaFieldDescription` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1PolicySchemaFieldDescription {
    /// defaultValue property.
    pub default_value: Option<serde_json::Value>,
    /// description property.
    pub description: Option<String>,
    /// field property.
    pub field: Option<String>,
    /// fieldConstraints property.
    pub field_constraints: Option<GoogleChromePolicyVersionsV1FieldConstraints>,
    /// fieldDependencies property.
    pub field_dependencies: Option<Vec<GoogleChromePolicyVersionsV1PolicySchemaFieldDependencies>>,
    /// fieldDescription property.
    pub field_description: Option<String>,
    /// inputConstraint property.
    pub input_constraint: Option<String>,
    /// knownValueDescriptions property.
    pub known_value_descriptions:
        Option<Vec<GoogleChromePolicyVersionsV1PolicySchemaFieldKnownValueDescription>>,
    /// name property.
    pub name: Option<String>,
    /// nestedFieldDescriptions property.
    pub nested_field_descriptions:
        Option<Vec<Box<GoogleChromePolicyVersionsV1PolicySchemaFieldDescription>>>,
    /// requiredItems property.
    pub required_items: Option<Vec<GoogleChromePolicyVersionsV1PolicySchemaRequiredItems>>,
}

/// `Proto2FileDescriptorProto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Proto2FileDescriptorProto {
    /// editionDeprecated property.
    pub edition_deprecated: Option<String>,
    /// enumType property.
    pub enum_type: Option<Vec<Proto2EnumDescriptorProto>>,
    /// messageType property.
    pub message_type: Option<Vec<Box<Proto2DescriptorProto>>>,
    /// name property.
    pub name: Option<String>,
    /// optionDependency property.
    pub option_dependency: Option<Vec<String>>,
    /// package property.
    pub package: Option<String>,
    /// syntax property.
    pub syntax: Option<String>,
}

/// `GoogleChromePolicyVersionsV1PolicyTargetKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1PolicyTargetKey {
    /// additionalTargetKeys property.
    pub additional_target_keys: Option<serde_json::Value>,
    /// targetResource property.
    pub target_resource: Option<String>,
}

/// `GoogleChromePolicyVersionsV1PolicyValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1PolicyValue {
    /// policySchema property.
    pub policy_schema: Option<String>,
    /// value property.
    pub value: Option<serde_json::Value>,
}

/// `GoogleChromePolicyVersionsV1PolicySchemaFieldDependencies` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1PolicySchemaFieldDependencies {
    /// sourceField property.
    pub source_field: Option<String>,
    /// sourceFieldValue property.
    pub source_field_value: Option<String>,
}

/// `GoogleChromePolicyVersionsV1DefineCertificateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1DefineCertificateRequest {
    /// ceritificateName property.
    pub ceritificate_name: Option<String>,
    /// certificate property.
    pub certificate: Option<String>,
    /// settings property.
    pub settings: Option<Vec<GoogleChromePolicyVersionsV1NetworkSetting>>,
    /// targetResource property.
    pub target_resource: Option<String>,
}

/// `GoogleChromePolicyVersionsV1BatchModifyGroupPoliciesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1BatchModifyGroupPoliciesRequest {
    /// requests property.
    pub requests: Option<Vec<GoogleChromePolicyVersionsV1ModifyGroupPolicyRequest>>,
}

/// `GoogleChromePolicyVersionsV1RemoveNetworkRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1RemoveNetworkRequest {
    /// networkId property.
    pub network_id: Option<String>,
    /// targetResource property.
    pub target_resource: Option<String>,
}

/// `GoogleChromePolicyVersionsV1DefineNetworkRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1DefineNetworkRequest {
    /// name property.
    pub name: Option<String>,
    /// settings property.
    pub settings: Option<Vec<GoogleChromePolicyVersionsV1NetworkSetting>>,
    /// targetResource property.
    pub target_resource: Option<String>,
}

/// `GoogleChromePolicyVersionsV1UploadedFileConstraints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1UploadedFileConstraints {
    /// sizeLimitBytes property.
    pub size_limit_bytes: Option<String>,
    /// supportedContentTypes property.
    pub supported_content_types: Option<Vec<String>>,
}

/// `GoogleChromePolicyVersionsV1DefineNetworkResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1DefineNetworkResponse {
    /// networkId property.
    pub network_id: Option<String>,
    /// settings property.
    pub settings: Option<Vec<GoogleChromePolicyVersionsV1NetworkSetting>>,
    /// targetResource property.
    pub target_resource: Option<String>,
}

/// `GoogleChromePolicyVersionsV1PolicySchemaFieldKnownValueDescription` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1PolicySchemaFieldKnownValueDescription {
    /// description property.
    pub description: Option<String>,
    /// fieldDependencies property.
    pub field_dependencies: Option<Vec<GoogleChromePolicyVersionsV1PolicySchemaFieldDependencies>>,
    /// value property.
    pub value: Option<String>,
}

/// `Proto2DescriptorProto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Proto2DescriptorProto {
    /// enumType property.
    pub enum_type: Option<Vec<Proto2EnumDescriptorProto>>,
    /// field property.
    pub field: Option<Vec<Proto2FieldDescriptorProto>>,
    /// name property.
    pub name: Option<String>,
    /// nestedType property.
    pub nested_type: Option<Vec<Box<Proto2DescriptorProto>>>,
    /// oneofDecl property.
    pub oneof_decl: Option<Vec<Proto2OneofDescriptorProto>>,
    /// visibility property.
    pub visibility: Option<String>,
}

/// `GoogleChromePolicyVersionsV1UploadPolicyFileRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1UploadPolicyFileRequest {
    /// policyField property.
    pub policy_field: Option<String>,
}

/// `GoogleChromePolicyVersionsV1ResolveResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1ResolveResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resolvedPolicies property.
    pub resolved_policies: Option<Vec<GoogleChromePolicyVersionsV1ResolvedPolicy>>,
}

/// `GoogleChromePolicyVersionsV1PolicySchema` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1PolicySchema {
    /// accessRestrictions property.
    pub access_restrictions: Option<Vec<String>>,
    /// additionalTargetKeyNames property.
    pub additional_target_key_names:
        Option<Vec<GoogleChromePolicyVersionsV1AdditionalTargetKeyName>>,
    /// categoryTitle property.
    pub category_title: Option<String>,
    /// definition property.
    pub definition: Option<Proto2FileDescriptorProto>,
    /// fieldDescriptions property.
    pub field_descriptions:
        Option<Vec<Box<GoogleChromePolicyVersionsV1PolicySchemaFieldDescription>>>,
    /// name property.
    pub name: Option<String>,
    /// notices property.
    pub notices: Option<Vec<GoogleChromePolicyVersionsV1PolicySchemaNoticeDescription>>,
    /// policyApiLifecycle property.
    pub policy_api_lifecycle: Option<GoogleChromePolicyVersionsV1PolicyApiLifecycle>,
    /// policyDescription property.
    pub policy_description: Option<String>,
    /// schemaName property.
    pub schema_name: Option<String>,
    /// supportUri property.
    pub support_uri: Option<String>,
    /// supportedPlatforms property.
    pub supported_platforms: Option<Vec<String>>,
    /// validTargetResources property.
    pub valid_target_resources: Option<Vec<String>>,
}

/// `GoogleTypeDate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeDate {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `GoogleChromePolicyVersionsV1NetworkSetting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1NetworkSetting {
    /// policySchema property.
    pub policy_schema: Option<String>,
    /// value property.
    pub value: Option<serde_json::Value>,
}

/// `GoogleChromePolicyVersionsV1ResolveRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1ResolveRequest {
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// policySchemaFilter property.
    pub policy_schema_filter: Option<String>,
    /// policyTargetKey property.
    pub policy_target_key: Option<GoogleChromePolicyVersionsV1PolicyTargetKey>,
}

/// `GoogleChromePolicyVersionsV1DefineCertificateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromePolicyVersionsV1DefineCertificateResponse {
    /// networkId property.
    pub network_id: Option<String>,
    /// settings property.
    pub settings: Option<Vec<GoogleChromePolicyVersionsV1NetworkSetting>>,
    /// targetResource property.
    pub target_resource: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`chromepolicy.customers.policies.resolve_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesResolveArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1ResolveRequest,
}

/// Arguments for [`chromepolicy.customers.policies.groups.batchDelete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesGroupsBatchDeleteArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1BatchDeleteGroupPoliciesRequest,
}

/// Arguments for [`chromepolicy.customers.policies.groups.batchModify_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesGroupsBatchModifyArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1BatchModifyGroupPoliciesRequest,
}

/// Arguments for [`chromepolicy.customers.policies.groups.listGroupPriorityOrdering_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesGroupsListGroupPriorityOrderingArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1ListGroupPriorityOrderingRequest,
}

/// Arguments for [`chromepolicy.customers.policies.groups.updateGroupPriorityOrdering_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesGroupsUpdateGroupPriorityOrderingArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1UpdateGroupPriorityOrderingRequest,
}

/// Arguments for [`chromepolicy.customers.policies.networks.defineCertificate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesNetworksDefineCertificateArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1DefineCertificateRequest,
}

/// Arguments for [`chromepolicy.customers.policies.networks.defineNetwork_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesNetworksDefineNetworkArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1DefineNetworkRequest,
}

/// Arguments for [`chromepolicy.customers.policies.networks.removeCertificate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesNetworksRemoveCertificateArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1RemoveCertificateRequest,
}

/// Arguments for [`chromepolicy.customers.policies.networks.removeNetwork_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesNetworksRemoveNetworkArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1RemoveNetworkRequest,
}

/// Arguments for [`chromepolicy.customers.policies.orgunits.batchInherit_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesOrgunitsBatchInheritArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1BatchInheritOrgUnitPoliciesRequest,
}

/// Arguments for [`chromepolicy.customers.policies.orgunits.batchModify_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesOrgunitsBatchModifyArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1BatchModifyOrgUnitPoliciesRequest,
}

/// Arguments for [`chromepolicy.customers.policySchemas.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPolicySchemasGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`chromepolicy.customers.policySchemas.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPolicySchemasListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`chromepolicy.media.upload_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromepolicyMediaUploadArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Request body.
    pub body: GoogleChromePolicyVersionsV1UploadPolicyFileRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies:resolve
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies:resolve.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policies_resolve_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policies_resolve_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPoliciesResolveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ResolveResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies:resolve",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1ResolveResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies/groups:batchDelete
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies/groups:batchDelete.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policies_groups_batch_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policies_groups_batch_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPoliciesGroupsBatchDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies/groups:batchDelete",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies/groups:batchModify
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies/groups:batchModify.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policies_groups_batch_modify_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policies_groups_batch_modify_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPoliciesGroupsBatchModifyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies/groups:batchModify",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies/groups:listGroupPriorityOrdering
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies/groups:listGroupPriorityOrdering.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policies_groups_list_group_priority_ordering_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policies_groups_list_group_priority_ordering_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPoliciesGroupsListGroupPriorityOrderingArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies/groups:listGroupPriorityOrdering",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies/groups:updateGroupPriorityOrdering
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies/groups:updateGroupPriorityOrdering.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policies_groups_update_group_priority_ordering_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policies_groups_update_group_priority_ordering_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPoliciesGroupsUpdateGroupPriorityOrderingArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies/groups:updateGroupPriorityOrdering",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies/networks:defineCertificate
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies/networks:defineCertificate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policies_networks_define_certificate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policies_networks_define_certificate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPoliciesNetworksDefineCertificateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1DefineCertificateResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies/networks:defineCertificate",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1DefineCertificateResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies/networks:defineNetwork
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies/networks:defineNetwork.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policies_networks_define_network_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policies_networks_define_network_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPoliciesNetworksDefineNetworkArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1DefineNetworkResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies/networks:defineNetwork",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1DefineNetworkResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies/networks:removeCertificate
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies/networks:removeCertificate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policies_networks_remove_certificate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policies_networks_remove_certificate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPoliciesNetworksRemoveCertificateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1RemoveCertificateResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies/networks:removeCertificate",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1RemoveCertificateResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies/networks:removeNetwork
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies/networks:removeNetwork.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policies_networks_remove_network_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policies_networks_remove_network_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPoliciesNetworksRemoveNetworkArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1RemoveNetworkResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies/networks:removeNetwork",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1RemoveNetworkResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies/orgunits:batchInherit
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies/orgunits:batchInherit.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policies_orgunits_batch_inherit_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policies_orgunits_batch_inherit_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPoliciesOrgunitsBatchInheritArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies/orgunits:batchInherit",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies/orgunits:batchModify
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies/orgunits:batchModify.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policies_orgunits_batch_modify_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policies_orgunits_batch_modify_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPoliciesOrgunitsBatchModifyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies/orgunits:batchModify",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policy_schemas_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policy_schemas_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPolicySchemasGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1PolicySchema>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromepolicy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1PolicySchema = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/policySchemas
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/policySchemas.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_customers_policy_schemas_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_customers_policy_schemas_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyCustomersPolicySchemasListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListPolicySchemasResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policySchemas",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1ListPolicySchemasResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+customer}/policies/files:uploadPolicyFile
// -----------------------------------------------------------------------------

/// POST v1/{+customer}/policies/files:uploadPolicyFile.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromepolicy_media_upload_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromepolicy_media_upload_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromepolicyMediaUploadArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1UploadPolicyFileResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/{}/policies/files:uploadPolicyFile",
        args.customer,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1UploadPolicyFileResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
