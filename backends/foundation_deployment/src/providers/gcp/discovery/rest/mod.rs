//! Auto-generated API module for gcp/discovery rest.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_discovery_rest `

#![cfg(feature = "gcp_discovery_rest")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `RestDescription` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RestDescription {
    /// auth property.
    pub auth: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// basePath property.
    pub base_path: Option<String>,
    /// baseUrl property.
    pub base_url: Option<String>,
    /// batchPath property.
    pub batch_path: Option<String>,
    /// canonicalName property.
    pub canonical_name: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// discoveryVersion property.
    pub discovery_version: Option<String>,
    /// documentationLink property.
    pub documentation_link: Option<String>,
    /// endpoints property.
    pub endpoints: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// etag property.
    pub etag: Option<String>,
    /// exponentialBackoffDefault property.
    pub exponential_backoff_default: Option<bool>,
    /// features property.
    pub features: Option<Vec<String>>,
    /// icons property.
    pub icons: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labels property.
    pub labels: Option<Vec<String>>,
    /// methods property.
    pub methods: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// ownerDomain property.
    pub owner_domain: Option<String>,
    /// ownerName property.
    pub owner_name: Option<String>,
    /// packagePath property.
    pub package_path: Option<String>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
    /// protocol property.
    pub protocol: Option<String>,
    /// resources property.
    pub resources: Option<serde_json::Value>,
    /// revision property.
    pub revision: Option<String>,
    /// rootUrl property.
    pub root_url: Option<String>,
    /// schemas property.
    pub schemas: Option<serde_json::Value>,
    /// servicePath property.
    pub service_path: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// version property.
    pub version: Option<String>,
    /// version_module property.
    pub version_module: Option<bool>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`discovery.apis.getRest_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DiscoveryApisGetRestArgs {
    /// Path parameter: `api`.
    pub api: String,
    /// Path parameter: `version`.
    pub version: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET apis/{api}/{version}/rest
// -----------------------------------------------------------------------------

/// GET apis/{api}/{version}/rest.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = discovery_apis_get_rest_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn discovery_apis_get_rest_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DiscoveryApisGetRestArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RestDescription>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/discovery/v1/apis/{}/{}/rest",
        args.api, args.version,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RestDescription =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
