//! Auto-generated API module for gcp/websecurityscanner findings.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_websecurityscanner_findings `

#![cfg(feature = "gcp_websecurityscanner_findings")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ListFindingsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListFindingsResponse {
    /// findings property.
    pub findings: Option<Vec<Finding>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `OutdatedLibrary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OutdatedLibrary {
    /// learnMoreUrls property.
    pub learn_more_urls: Option<Vec<String>>,
    /// libraryName property.
    pub library_name: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `Header` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Header {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `VulnerableHeaders` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VulnerableHeaders {
    /// headers property.
    pub headers: Option<Vec<Header>>,
    /// missingHeaders property.
    pub missing_headers: Option<Vec<Header>>,
}

/// `Finding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Finding {
    /// body property.
    pub body: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// finalUrl property.
    pub final_url: Option<String>,
    /// findingType property.
    pub finding_type: Option<String>,
    /// form property.
    pub form: Option<Form>,
    /// frameUrl property.
    pub frame_url: Option<String>,
    /// fuzzedUrl property.
    pub fuzzed_url: Option<String>,
    /// httpMethod property.
    pub http_method: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// outdatedLibrary property.
    pub outdated_library: Option<OutdatedLibrary>,
    /// reproductionUrl property.
    pub reproduction_url: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// trackingId property.
    pub tracking_id: Option<String>,
    /// violatingResource property.
    pub violating_resource: Option<ViolatingResource>,
    /// vulnerableHeaders property.
    pub vulnerable_headers: Option<VulnerableHeaders>,
    /// vulnerableParameters property.
    pub vulnerable_parameters: Option<VulnerableParameters>,
    /// xss property.
    pub xss: Option<Xss>,
    /// xxe property.
    pub xxe: Option<Xxe>,
}

/// `ViolatingResource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ViolatingResource {
    /// contentType property.
    pub content_type: Option<String>,
    /// resourceUrl property.
    pub resource_url: Option<String>,
}

/// `Xss` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Xss {
    /// attackVector property.
    pub attack_vector: Option<String>,
    /// errorMessage property.
    pub error_message: Option<String>,
    /// stackTraces property.
    pub stack_traces: Option<Vec<String>>,
    /// storedXssSeedingUrl property.
    pub stored_xss_seeding_url: Option<String>,
}

/// `VulnerableParameters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VulnerableParameters {
    /// parameterNames property.
    pub parameter_names: Option<Vec<String>>,
}

/// `Xxe` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Xxe {
    /// payloadLocation property.
    pub payload_location: Option<String>,
    /// payloadValue property.
    pub payload_value: Option<String>,
}

/// `Form` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Form {
    /// actionUri property.
    pub action_uri: Option<String>,
    /// fields property.
    pub fields: Option<Vec<String>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`websecurityscanner.projects.scanConfigs.scanRuns.findings.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebsecurityscannerProjectsScanConfigsScanRunsFindingsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`websecurityscanner.projects.scanConfigs.scanRuns.findings.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebsecurityscannerProjectsScanConfigsScanRunsFindingsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/scanConfigs/{scanConfigsId}/scanRuns/{scanRunsId}/findings/{findingsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/scanConfigs/{scanConfigsId}/scanRuns/{scanRunsId}/findings/{findingsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = websecurityscanner_projects_scan_configs_scan_runs_findings_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn websecurityscanner_projects_scan_configs_scan_runs_findings_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Finding>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://websecurityscanner.googleapis.com/v1/projects/{{projectsId}}/scanConfigs/{{scanConfigsId}}/scanRuns/{{scanRunsId}}/findings/{{findingsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Finding =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/scanConfigs/{scanConfigsId}/scanRuns/{scanRunsId}/findings
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/scanConfigs/{scanConfigsId}/scanRuns/{scanRunsId}/findings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = websecurityscanner_projects_scan_configs_scan_runs_findings_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn websecurityscanner_projects_scan_configs_scan_runs_findings_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFindingsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://websecurityscanner.googleapis.com/v1/projects/{{projectsId}}/scanConfigs/{{scanConfigsId}}/scanRuns/{{scanRunsId}}/findings",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFindingsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
