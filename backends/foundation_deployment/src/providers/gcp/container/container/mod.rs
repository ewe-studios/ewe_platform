//! Auto-generated API module for gcp/container container.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_container_container `

#![cfg(feature = "gcp_container_container")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ClusterNetworkPerformanceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClusterNetworkPerformanceConfig {
    /// totalEgressBandwidthTier property.
    pub total_egress_bandwidth_tier: Option<String>,
}

/// `ParallelstoreCsiDriverConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ParallelstoreCsiDriverConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `StatusCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StatusCondition {
    /// canonicalCode property.
    pub canonical_code: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// message property.
    pub message: Option<String>,
}

/// `DedicatedLocalSsdProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DedicatedLocalSsdProfile {
    /// diskCount property.
    pub disk_count: Option<String>,
}

/// `AutoMonitoringConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoMonitoringConfig {
    /// scope property.
    pub scope: Option<String>,
}

/// `DNSEndpointConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DNSEndpointConfig {
    /// allowExternalTraffic property.
    pub allow_external_traffic: Option<bool>,
    /// enableK8sCertsViaDns property.
    pub enable_k8s_certs_via_dns: Option<bool>,
    /// enableK8sTokensViaDns property.
    pub enable_k8s_tokens_via_dns: Option<bool>,
    /// endpoint property.
    pub endpoint: Option<String>,
}

/// `NodePool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodePool {
    /// autopilotConfig property.
    pub autopilot_config: Option<AutopilotConfig>,
    /// autoscaling property.
    pub autoscaling: Option<NodePoolAutoscaling>,
    /// bestEffortProvisioning property.
    pub best_effort_provisioning: Option<BestEffortProvisioning>,
    /// conditions property.
    pub conditions: Option<Vec<StatusCondition>>,
    /// config property.
    pub config: Option<NodeConfig>,
    /// etag property.
    pub etag: Option<String>,
    /// initialNodeCount property.
    pub initial_node_count: Option<i64>,
    /// instanceGroupUrls property.
    pub instance_group_urls: Option<Vec<String>>,
    /// locations property.
    pub locations: Option<Vec<String>>,
    /// management property.
    pub management: Option<NodeManagement>,
    /// maxPodsConstraint property.
    pub max_pods_constraint: Option<MaxPodsConstraint>,
    /// name property.
    pub name: Option<String>,
    /// networkConfig property.
    pub network_config: Option<NodeNetworkConfig>,
    /// nodeDrainConfig property.
    pub node_drain_config: Option<NodeDrainConfig>,
    /// placementPolicy property.
    pub placement_policy: Option<PlacementPolicy>,
    /// podIpv4CidrSize property.
    pub pod_ipv4_cidr_size: Option<i64>,
    /// queuedProvisioning property.
    pub queued_provisioning: Option<QueuedProvisioning>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// statusMessage property.
    pub status_message: Option<String>,
    /// updateInfo property.
    pub update_info: Option<UpdateInfo>,
    /// upgradeSettings property.
    pub upgrade_settings: Option<UpgradeSettings>,
    /// version property.
    pub version: Option<String>,
}

/// `HttpCacheControlResponseHeader` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpCacheControlResponseHeader {
    /// age property.
    pub age: Option<String>,
    /// directive property.
    pub directive: Option<String>,
    /// expires property.
    pub expires: Option<String>,
}

/// `NetworkTags` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkTags {
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `CreateClusterRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreateClusterRequest {
    /// cluster property.
    pub cluster: Option<Cluster>,
    /// parent property.
    pub parent: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `PrivateClusterMasterGlobalAccessConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PrivateClusterMasterGlobalAccessConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `CancelOperationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelOperationRequest {
    /// name property.
    pub name: Option<String>,
    /// operationId property.
    pub operation_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `SliceControllerConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SliceControllerConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `MaintenanceWindow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MaintenanceWindow {
    /// dailyMaintenanceWindow property.
    pub daily_maintenance_window: Option<DailyMaintenanceWindow>,
    /// maintenanceExclusions property.
    pub maintenance_exclusions: Option<serde_json::Value>,
    /// recurringWindow property.
    pub recurring_window: Option<RecurringTimeWindow>,
}

/// `SecretSyncConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecretSyncConfig {
    /// enabled property.
    pub enabled: Option<bool>,
    /// rotationConfig property.
    pub rotation_config: Option<SyncRotationConfig>,
}

/// `ControlPlaneEgress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ControlPlaneEgress {
    /// mode property.
    pub mode: Option<String>,
}

/// `SetLabelsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetLabelsRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// resourceLabels property.
    pub resource_labels: Option<serde_json::Value>,
    /// zone property.
    pub zone: Option<String>,
}

/// `Cluster` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Cluster {
    /// addonsConfig property.
    pub addons_config: Option<AddonsConfig>,
    /// alphaClusterFeatureGates property.
    pub alpha_cluster_feature_gates: Option<Vec<String>>,
    /// anonymousAuthenticationConfig property.
    pub anonymous_authentication_config: Option<AnonymousAuthenticationConfig>,
    /// authenticatorGroupsConfig property.
    pub authenticator_groups_config: Option<AuthenticatorGroupsConfig>,
    /// autopilot property.
    pub autopilot: Option<Autopilot>,
    /// autoscaling property.
    pub autoscaling: Option<ClusterAutoscaling>,
    /// binaryAuthorization property.
    pub binary_authorization: Option<BinaryAuthorization>,
    /// clusterIpv4Cidr property.
    pub cluster_ipv4_cidr: Option<String>,
    /// compliancePostureConfig property.
    pub compliance_posture_config: Option<CompliancePostureConfig>,
    /// conditions property.
    pub conditions: Option<Vec<StatusCondition>>,
    /// confidentialNodes property.
    pub confidential_nodes: Option<ConfidentialNodes>,
    /// controlPlaneEgress property.
    pub control_plane_egress: Option<ControlPlaneEgress>,
    /// controlPlaneEndpointsConfig property.
    pub control_plane_endpoints_config: Option<ControlPlaneEndpointsConfig>,
    /// costManagementConfig property.
    pub cost_management_config: Option<CostManagementConfig>,
    /// createTime property.
    pub create_time: Option<String>,
    /// currentMasterVersion property.
    pub current_master_version: Option<String>,
    /// currentNodeCount property.
    pub current_node_count: Option<i64>,
    /// currentNodeVersion property.
    pub current_node_version: Option<String>,
    /// databaseEncryption property.
    pub database_encryption: Option<DatabaseEncryption>,
    /// defaultMaxPodsConstraint property.
    pub default_max_pods_constraint: Option<MaxPodsConstraint>,
    /// description property.
    pub description: Option<String>,
    /// enableK8sBetaApis property.
    pub enable_k8s_beta_apis: Option<K8SBetaAPIConfig>,
    /// enableKubernetesAlpha property.
    pub enable_kubernetes_alpha: Option<bool>,
    /// enableTpu property.
    pub enable_tpu: Option<bool>,
    /// endpoint property.
    pub endpoint: Option<String>,
    /// enterpriseConfig property.
    pub enterprise_config: Option<EnterpriseConfig>,
    /// etag property.
    pub etag: Option<String>,
    /// expireTime property.
    pub expire_time: Option<String>,
    /// fleet property.
    pub fleet: Option<Fleet>,
    /// gkeAutoUpgradeConfig property.
    pub gke_auto_upgrade_config: Option<GkeAutoUpgradeConfig>,
    /// id property.
    pub id: Option<String>,
    /// identityServiceConfig property.
    pub identity_service_config: Option<IdentityServiceConfig>,
    /// initialClusterVersion property.
    pub initial_cluster_version: Option<String>,
    /// initialNodeCount property.
    pub initial_node_count: Option<i64>,
    /// instanceGroupUrls property.
    pub instance_group_urls: Option<Vec<String>>,
    /// ipAllocationPolicy property.
    pub ip_allocation_policy: Option<IPAllocationPolicy>,
    /// labelFingerprint property.
    pub label_fingerprint: Option<String>,
    /// legacyAbac property.
    pub legacy_abac: Option<LegacyAbac>,
    /// location property.
    pub location: Option<String>,
    /// locations property.
    pub locations: Option<Vec<String>>,
    /// loggingConfig property.
    pub logging_config: Option<LoggingConfig>,
    /// loggingService property.
    pub logging_service: Option<String>,
    /// maintenancePolicy property.
    pub maintenance_policy: Option<MaintenancePolicy>,
    /// managedMachineLearningDiagnosticsConfig property.
    pub managed_machine_learning_diagnostics_config:
        Option<ManagedMachineLearningDiagnosticsConfig>,
    /// managedOpentelemetryConfig property.
    pub managed_opentelemetry_config: Option<ManagedOpenTelemetryConfig>,
    /// masterAuth property.
    pub master_auth: Option<MasterAuth>,
    /// masterAuthorizedNetworksConfig property.
    pub master_authorized_networks_config: Option<MasterAuthorizedNetworksConfig>,
    /// meshCertificates property.
    pub mesh_certificates: Option<MeshCertificates>,
    /// monitoringConfig property.
    pub monitoring_config: Option<MonitoringConfig>,
    /// monitoringService property.
    pub monitoring_service: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// networkConfig property.
    pub network_config: Option<NetworkConfig>,
    /// networkPolicy property.
    pub network_policy: Option<NetworkPolicy>,
    /// nodeConfig property.
    pub node_config: Option<NodeConfig>,
    /// nodeIpv4CidrSize property.
    pub node_ipv4_cidr_size: Option<i64>,
    /// nodePoolAutoConfig property.
    pub node_pool_auto_config: Option<NodePoolAutoConfig>,
    /// nodePoolDefaults property.
    pub node_pool_defaults: Option<NodePoolDefaults>,
    /// nodePools property.
    pub node_pools: Option<Vec<NodePool>>,
    /// notificationConfig property.
    pub notification_config: Option<NotificationConfig>,
    /// parentProductConfig property.
    pub parent_product_config: Option<ParentProductConfig>,
    /// podAutoscaling property.
    pub pod_autoscaling: Option<PodAutoscaling>,
    /// privateClusterConfig property.
    pub private_cluster_config: Option<PrivateClusterConfig>,
    /// rbacBindingConfig property.
    pub rbac_binding_config: Option<RBACBindingConfig>,
    /// releaseChannel property.
    pub release_channel: Option<ReleaseChannel>,
    /// resourceLabels property.
    pub resource_labels: Option<serde_json::Value>,
    /// resourceUsageExportConfig property.
    pub resource_usage_export_config: Option<ResourceUsageExportConfig>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// scheduleUpgradeConfig property.
    pub schedule_upgrade_config: Option<ScheduleUpgradeConfig>,
    /// secretManagerConfig property.
    pub secret_manager_config: Option<SecretManagerConfig>,
    /// secretSyncConfig property.
    pub secret_sync_config: Option<SecretSyncConfig>,
    /// securityPostureConfig property.
    pub security_posture_config: Option<SecurityPostureConfig>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// servicesIpv4Cidr property.
    pub services_ipv4_cidr: Option<String>,
    /// shieldedNodes property.
    pub shielded_nodes: Option<ShieldedNodes>,
    /// status property.
    pub status: Option<String>,
    /// statusMessage property.
    pub status_message: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// tpuIpv4CidrBlock property.
    pub tpu_ipv4_cidr_block: Option<String>,
    /// userManagedKeysConfig property.
    pub user_managed_keys_config: Option<UserManagedKeysConfig>,
    /// verticalPodAutoscaling property.
    pub vertical_pod_autoscaling: Option<VerticalPodAutoscaling>,
    /// workloadIdentityConfig property.
    pub workload_identity_config: Option<WorkloadIdentityConfig>,
    /// zone property.
    pub zone: Option<String>,
}

/// `NotificationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NotificationConfig {
    /// pubsub property.
    pub pubsub: Option<PubSub>,
}

/// `CloudRunConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CloudRunConfig {
    /// disabled property.
    pub disabled: Option<bool>,
    /// loadBalancerType property.
    pub load_balancer_type: Option<String>,
}

/// `CertificateConfigPair` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CertificateConfigPair {
    /// cert property.
    pub cert: Option<CertificateConfig>,
    /// key property.
    pub key: Option<CertificateConfig>,
}

/// `Jwk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Jwk {
    /// alg property.
    pub alg: Option<String>,
    /// crv property.
    pub crv: Option<String>,
    /// e property.
    pub e: Option<String>,
    /// kid property.
    pub kid: Option<String>,
    /// kty property.
    pub kty: Option<String>,
    /// n property.
    pub n: Option<String>,
    /// use property.
    pub r#use: Option<String>,
    /// x property.
    pub x: Option<String>,
    /// y property.
    pub y: Option<String>,
}

/// `ContainerdConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContainerdConfig {
    /// privateRegistryAccessConfig property.
    pub private_registry_access_config: Option<PrivateRegistryAccessConfig>,
    /// registryHosts property.
    pub registry_hosts: Option<Vec<RegistryHostConfig>>,
    /// writableCgroups property.
    pub writable_cgroups: Option<WritableCgroups>,
}

/// `ILBSubsettingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ILBSubsettingConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `DisruptionBudget` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DisruptionBudget {
    /// lastDisruptionTime property.
    pub last_disruption_time: Option<String>,
    /// lastMinorVersionDisruptionTime property.
    pub last_minor_version_disruption_time: Option<String>,
    /// minorVersionDisruptionInterval property.
    pub minor_version_disruption_interval: Option<String>,
    /// patchVersionDisruptionInterval property.
    pub patch_version_disruption_interval: Option<String>,
}

/// `TaintConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TaintConfig {
    /// architectureTaintBehavior property.
    pub architecture_taint_behavior: Option<String>,
}

/// `AdditionalPodRangesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdditionalPodRangesConfig {
    /// podRangeInfo property.
    pub pod_range_info: Option<Vec<RangeInfo>>,
    /// podRangeNames property.
    pub pod_range_names: Option<Vec<String>>,
}

/// `ComplianceStandard` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComplianceStandard {
    /// standard property.
    pub standard: Option<String>,
}

/// `RegistryHostConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegistryHostConfig {
    /// hosts property.
    pub hosts: Option<Vec<HostConfig>>,
    /// server property.
    pub server: Option<String>,
}

/// `ManagedMachineLearningDiagnosticsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedMachineLearningDiagnosticsConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `EvictionSignals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EvictionSignals {
    /// imagefsAvailable property.
    pub imagefs_available: Option<String>,
    /// imagefsInodesFree property.
    pub imagefs_inodes_free: Option<String>,
    /// memoryAvailable property.
    pub memory_available: Option<String>,
    /// nodefsAvailable property.
    pub nodefs_available: Option<String>,
    /// nodefsInodesFree property.
    pub nodefs_inodes_free: Option<String>,
    /// pidAvailable property.
    pub pid_available: Option<String>,
}

/// `ResourceLabels` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceLabels {
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `ServiceExternalIPsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceExternalIPsConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `ManagedPrometheusConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedPrometheusConfig {
    /// autoMonitoringConfig property.
    pub auto_monitoring_config: Option<AutoMonitoringConfig>,
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `VirtualNIC` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VirtualNIC {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `NodeConfigDefaults` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeConfigDefaults {
    /// containerdConfig property.
    pub containerd_config: Option<ContainerdConfig>,
    /// gcfsConfig property.
    pub gcfs_config: Option<GcfsConfig>,
    /// loggingConfig property.
    pub logging_config: Option<NodePoolLoggingConfig>,
    /// nodeKubeletConfig property.
    pub node_kubelet_config: Option<NodeKubeletConfig>,
}

/// `LoggingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoggingConfig {
    /// componentConfig property.
    pub component_config: Option<LoggingComponentConfig>,
}

/// `NodePoolAutoConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodePoolAutoConfig {
    /// linuxNodeConfig property.
    pub linux_node_config: Option<LinuxNodeConfig>,
    /// networkTags property.
    pub network_tags: Option<NetworkTags>,
    /// nodeKubeletConfig property.
    pub node_kubelet_config: Option<NodeKubeletConfig>,
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<ResourceManagerTags>,
}

/// `StartIPRotationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StartIPRotationRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// rotateCredentials property.
    pub rotate_credentials: Option<bool>,
    /// zone property.
    pub zone: Option<String>,
}

/// `SetAddonsConfigRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetAddonsConfigRequest {
    /// addonsConfig property.
    pub addons_config: Option<AddonsConfig>,
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `CostManagementConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CostManagementConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `GCPSecretManagerCertificateConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GCPSecretManagerCertificateConfig {
    /// secretUri property.
    pub secret_uri: Option<String>,
}

/// `CompleteIPRotationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompleteIPRotationRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `MeshCertificates` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MeshCertificates {
    /// enableCertificates property.
    pub enable_certificates: Option<bool>,
}

/// `ManagedOpenTelemetryConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedOpenTelemetryConfig {
    /// scope property.
    pub scope: Option<String>,
}

/// `RollbackNodePoolUpgradeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RollbackNodePoolUpgradeRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nodePoolId property.
    pub node_pool_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// respectPdb property.
    pub respect_pdb: Option<bool>,
    /// zone property.
    pub zone: Option<String>,
}

/// `AdditionalIPRangesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdditionalIPRangesConfig {
    /// podIpv4RangeNames property.
    pub pod_ipv4_range_names: Option<Vec<String>>,
    /// status property.
    pub status: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
}

/// `AutoIpamConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoIpamConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `LoggingVariantConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoggingVariantConfig {
    /// variant property.
    pub variant: Option<String>,
}

/// `ReservationAffinity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationAffinity {
    /// consumeReservationType property.
    pub consume_reservation_type: Option<String>,
    /// key property.
    pub key: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `ServerConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServerConfig {
    /// channels property.
    pub channels: Option<Vec<ReleaseChannelConfig>>,
    /// defaultClusterVersion property.
    pub default_cluster_version: Option<String>,
    /// defaultImageType property.
    pub default_image_type: Option<String>,
    /// validImageTypes property.
    pub valid_image_types: Option<Vec<String>>,
    /// validMasterVersions property.
    pub valid_master_versions: Option<Vec<String>>,
    /// validNodeVersions property.
    pub valid_node_versions: Option<Vec<String>>,
}

/// `ParentProductConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ParentProductConfig {
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// productName property.
    pub product_name: Option<String>,
}

/// `PubSub` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PubSub {
    /// enabled property.
    pub enabled: Option<bool>,
    /// filter property.
    pub filter: Option<Filter>,
    /// topic property.
    pub topic: Option<String>,
}

/// `BlueGreenSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BlueGreenSettings {
    /// autoscaledRolloutPolicy property.
    pub autoscaled_rollout_policy: Option<AutoscaledRolloutPolicy>,
    /// nodePoolSoakDuration property.
    pub node_pool_soak_duration: Option<String>,
    /// standardRolloutPolicy property.
    pub standard_rollout_policy: Option<StandardRolloutPolicy>,
}

/// `ClientCertificateConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClientCertificateConfig {
    /// issueClientCertificate property.
    pub issue_client_certificate: Option<bool>,
}

/// `AdditionalPodNetworkConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdditionalPodNetworkConfig {
    /// maxPodsPerNode property.
    pub max_pods_per_node: Option<MaxPodsConstraint>,
    /// networkAttachment property.
    pub network_attachment: Option<String>,
    /// secondaryPodRange property.
    pub secondary_pod_range: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
}

/// `IdentityServiceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdentityServiceConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `GetJSONWebKeysResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetJSONWebKeysResponse {
    /// cacheHeader property.
    pub cache_header: Option<HttpCacheControlResponseHeader>,
    /// keys property.
    pub keys: Option<Vec<Jwk>>,
}

/// `DesiredEnterpriseConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DesiredEnterpriseConfig {
    /// desiredTier property.
    pub desired_tier: Option<String>,
}

/// `ShieldedInstanceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShieldedInstanceConfig {
    /// enableIntegrityMonitoring property.
    pub enable_integrity_monitoring: Option<bool>,
    /// enableSecureBoot property.
    pub enable_secure_boot: Option<bool>,
}

/// `RecurringTimeWindow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RecurringTimeWindow {
    /// recurrence property.
    pub recurrence: Option<String>,
    /// window property.
    pub window: Option<TimeWindow>,
}

/// `MaxPodsConstraint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MaxPodsConstraint {
    /// maxPodsPerNode property.
    pub max_pods_per_node: Option<String>,
}

/// `SyncRotationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SyncRotationConfig {
    /// enabled property.
    pub enabled: Option<bool>,
    /// rotationInterval property.
    pub rotation_interval: Option<String>,
}

/// `NetworkPolicyConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkPolicyConfig {
    /// disabled property.
    pub disabled: Option<bool>,
}

/// `NetworkConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkConfig {
    /// datapathProvider property.
    pub datapath_provider: Option<String>,
    /// defaultEnablePrivateNodes property.
    pub default_enable_private_nodes: Option<bool>,
    /// defaultSnatStatus property.
    pub default_snat_status: Option<DefaultSnatStatus>,
    /// disableL4LbFirewallReconciliation property.
    pub disable_l4_lb_firewall_reconciliation: Option<bool>,
    /// dnsConfig property.
    pub dns_config: Option<DNSConfig>,
    /// enableCiliumClusterwideNetworkPolicy property.
    pub enable_cilium_clusterwide_network_policy: Option<bool>,
    /// enableFqdnNetworkPolicy property.
    pub enable_fqdn_network_policy: Option<bool>,
    /// enableIntraNodeVisibility property.
    pub enable_intra_node_visibility: Option<bool>,
    /// enableL4ilbSubsetting property.
    pub enable_l4ilb_subsetting: Option<bool>,
    /// enableMultiNetworking property.
    pub enable_multi_networking: Option<bool>,
    /// gatewayApiConfig property.
    pub gateway_api_config: Option<GatewayAPIConfig>,
    /// inTransitEncryptionConfig property.
    pub in_transit_encryption_config: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// networkPerformanceConfig property.
    pub network_performance_config: Option<ClusterNetworkPerformanceConfig>,
    /// privateIpv6GoogleAccess property.
    pub private_ipv6_google_access: Option<String>,
    /// serviceExternalIpsConfig property.
    pub service_external_ips_config: Option<ServiceExternalIPsConfig>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
}

/// `FastSocket` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FastSocket {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `PodCIDROverprovisionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PodCIDROverprovisionConfig {
    /// disable property.
    pub disable: Option<bool>,
}

/// `NodeLabels` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeLabels {
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `DefaultSnatStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DefaultSnatStatus {
    /// disabled property.
    pub disabled: Option<bool>,
}

/// `SetMonitoringServiceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetMonitoringServiceRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// monitoringService property.
    pub monitoring_service: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `SetLocationsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetLocationsRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// locations property.
    pub locations: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `NodePoolAutoscaling` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodePoolAutoscaling {
    /// autoprovisioned property.
    pub autoprovisioned: Option<bool>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// locationPolicy property.
    pub location_policy: Option<String>,
    /// maxNodeCount property.
    pub max_node_count: Option<i64>,
    /// minNodeCount property.
    pub min_node_count: Option<i64>,
    /// totalMaxNodeCount property.
    pub total_max_node_count: Option<i64>,
    /// totalMinNodeCount property.
    pub total_min_node_count: Option<i64>,
}

/// `BinaryAuthorization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BinaryAuthorization {
    /// enabled property.
    pub enabled: Option<bool>,
    /// evaluationMode property.
    pub evaluation_mode: Option<String>,
}

/// `WritableCgroups` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WritableCgroups {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `StatefulHAConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StatefulHAConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `EnterpriseConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EnterpriseConfig {
    /// clusterTier property.
    pub cluster_tier: Option<String>,
    /// desiredTier property.
    pub desired_tier: Option<String>,
}

/// `GkeBackupAgentConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GkeBackupAgentConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `Autopilot` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Autopilot {
    /// clusterPolicyConfig property.
    pub cluster_policy_config: Option<ClusterPolicyConfig>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// privilegedAdmissionConfig property.
    pub privileged_admission_config: Option<PrivilegedAdmissionConfig>,
    /// workloadPolicyConfig property.
    pub workload_policy_config: Option<WorkloadPolicyConfig>,
}

/// `NodePoolUpgradeInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodePoolUpgradeInfo {
    /// autoUpgradeStatus property.
    pub auto_upgrade_status: Option<Vec<String>>,
    /// endOfExtendedSupportTimestamp property.
    pub end_of_extended_support_timestamp: Option<String>,
    /// endOfStandardSupportTimestamp property.
    pub end_of_standard_support_timestamp: Option<String>,
    /// minorTargetVersion property.
    pub minor_target_version: Option<String>,
    /// patchTargetVersion property.
    pub patch_target_version: Option<String>,
    /// pausedReason property.
    pub paused_reason: Option<Vec<String>>,
    /// upgradeDetails property.
    pub upgrade_details: Option<Vec<UpgradeDetails>>,
}

/// `CompleteNodePoolUpgradeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompleteNodePoolUpgradeRequest {}

/// `EphemeralLocalSsdProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EphemeralLocalSsdProfile {
    /// swapSizeGib property.
    pub swap_size_gib: Option<String>,
    /// swapSizePercent property.
    pub swap_size_percent: Option<i64>,
}

/// `AutoUpgradeOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoUpgradeOptions {
    /// autoUpgradeStartTime property.
    pub auto_upgrade_start_time: Option<String>,
    /// description property.
    pub description: Option<String>,
}

/// `AddonsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AddonsConfig {
    /// cloudRunConfig property.
    pub cloud_run_config: Option<CloudRunConfig>,
    /// configConnectorConfig property.
    pub config_connector_config: Option<ConfigConnectorConfig>,
    /// dnsCacheConfig property.
    pub dns_cache_config: Option<DnsCacheConfig>,
    /// gcePersistentDiskCsiDriverConfig property.
    pub gce_persistent_disk_csi_driver_config: Option<GcePersistentDiskCsiDriverConfig>,
    /// gcpFilestoreCsiDriverConfig property.
    pub gcp_filestore_csi_driver_config: Option<GcpFilestoreCsiDriverConfig>,
    /// gcsFuseCsiDriverConfig property.
    pub gcs_fuse_csi_driver_config: Option<GcsFuseCsiDriverConfig>,
    /// gkeBackupAgentConfig property.
    pub gke_backup_agent_config: Option<GkeBackupAgentConfig>,
    /// highScaleCheckpointingConfig property.
    pub high_scale_checkpointing_config: Option<HighScaleCheckpointingConfig>,
    /// horizontalPodAutoscaling property.
    pub horizontal_pod_autoscaling: Option<HorizontalPodAutoscaling>,
    /// httpLoadBalancing property.
    pub http_load_balancing: Option<HttpLoadBalancing>,
    /// kubernetesDashboard property.
    pub kubernetes_dashboard: Option<KubernetesDashboard>,
    /// lustreCsiDriverConfig property.
    pub lustre_csi_driver_config: Option<LustreCsiDriverConfig>,
    /// networkPolicyConfig property.
    pub network_policy_config: Option<NetworkPolicyConfig>,
    /// parallelstoreCsiDriverConfig property.
    pub parallelstore_csi_driver_config: Option<ParallelstoreCsiDriverConfig>,
    /// podSnapshotConfig property.
    pub pod_snapshot_config: Option<PodSnapshotConfig>,
    /// rayOperatorConfig property.
    pub ray_operator_config: Option<RayOperatorConfig>,
    /// sliceControllerConfig property.
    pub slice_controller_config: Option<SliceControllerConfig>,
    /// statefulHaConfig property.
    pub stateful_ha_config: Option<StatefulHAConfig>,
}

/// `BootDisk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BootDisk {
    /// diskType property.
    pub disk_type: Option<String>,
    /// provisionedIops property.
    pub provisioned_iops: Option<String>,
    /// provisionedThroughput property.
    pub provisioned_throughput: Option<String>,
    /// sizeGb property.
    pub size_gb: Option<String>,
}

/// `ReleaseChannel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReleaseChannel {
    /// channel property.
    pub channel: Option<String>,
}

/// `ResourceUsageExportConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceUsageExportConfig {
    /// bigqueryDestination property.
    pub bigquery_destination: Option<BigQueryDestination>,
    /// consumptionMeteringConfig property.
    pub consumption_metering_config: Option<ConsumptionMeteringConfig>,
    /// enableNetworkEgressMetering property.
    pub enable_network_egress_metering: Option<bool>,
}

/// `ListOperationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListOperationsResponse {
    /// missingZones property.
    pub missing_zones: Option<Vec<String>>,
    /// operations property.
    pub operations: Option<Vec<Operation>>,
}

/// `ConsumptionMeteringConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConsumptionMeteringConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `ConfidentialNodes` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConfidentialNodes {
    /// confidentialInstanceType property.
    pub confidential_instance_type: Option<String>,
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `HighScaleCheckpointingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HighScaleCheckpointingConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `EvictionMinimumReclaim` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EvictionMinimumReclaim {
    /// imagefsAvailable property.
    pub imagefs_available: Option<String>,
    /// imagefsInodesFree property.
    pub imagefs_inodes_free: Option<String>,
    /// memoryAvailable property.
    pub memory_available: Option<String>,
    /// nodefsAvailable property.
    pub nodefs_available: Option<String>,
    /// nodefsInodesFree property.
    pub nodefs_inodes_free: Option<String>,
    /// pidAvailable property.
    pub pid_available: Option<String>,
}

/// `RayOperatorConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RayOperatorConfig {
    /// enabled property.
    pub enabled: Option<bool>,
    /// rayClusterLoggingConfig property.
    pub ray_cluster_logging_config: Option<RayClusterLoggingConfig>,
    /// rayClusterMonitoringConfig property.
    pub ray_cluster_monitoring_config: Option<RayClusterMonitoringConfig>,
}

/// `EphemeralStorageLocalSsdConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EphemeralStorageLocalSsdConfig {
    /// dataCacheCount property.
    pub data_cache_count: Option<i64>,
    /// localSsdCount property.
    pub local_ssd_count: Option<i64>,
}

/// `K8SBetaAPIConfig` response type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct K8SBetaAPIConfig {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `IPEndpointsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IPEndpointsConfig {
    /// authorizedNetworksConfig property.
    pub authorized_networks_config: Option<MasterAuthorizedNetworksConfig>,
    /// enablePublicEndpoint property.
    pub enable_public_endpoint: Option<bool>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// globalAccess property.
    pub global_access: Option<bool>,
    /// privateEndpoint property.
    pub private_endpoint: Option<String>,
    /// privateEndpointSubnetwork property.
    pub private_endpoint_subnetwork: Option<String>,
    /// publicEndpoint property.
    pub public_endpoint: Option<String>,
}

/// `BigQueryDestination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BigQueryDestination {
    /// datasetId property.
    pub dataset_id: Option<String>,
}

/// `UpgradeDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpgradeDetails {
    /// endTime property.
    pub end_time: Option<String>,
    /// initialVersion property.
    pub initial_version: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// startType property.
    pub start_type: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// targetVersion property.
    pub target_version: Option<String>,
}

/// `NodeKernelModuleLoading` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeKernelModuleLoading {
    /// policy property.
    pub policy: Option<String>,
}

/// `TopologyManager` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TopologyManager {
    /// policy property.
    pub policy: Option<String>,
    /// scope property.
    pub scope: Option<String>,
}

/// `NodeTaints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeTaints {
    /// taints property.
    pub taints: Option<Vec<NodeTaint>>,
}

/// `ConfigConnectorConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConfigConnectorConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `ScheduleUpgradeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ScheduleUpgradeConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `SetNodePoolSizeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetNodePoolSizeRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nodeCount property.
    pub node_count: Option<i64>,
    /// nodePoolId property.
    pub node_pool_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `ControlPlaneEndpointsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ControlPlaneEndpointsConfig {
    /// dnsEndpointConfig property.
    pub dns_endpoint_config: Option<DNSEndpointConfig>,
    /// ipEndpointsConfig property.
    pub ip_endpoints_config: Option<IPEndpointsConfig>,
}

/// `MaintenanceExclusionOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MaintenanceExclusionOptions {
    /// endTimeBehavior property.
    pub end_time_behavior: Option<String>,
    /// scope property.
    pub scope: Option<String>,
}

/// `GPUDriverInstallationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GPUDriverInstallationConfig {
    /// gpuDriverVersion property.
    pub gpu_driver_version: Option<String>,
}

/// `RayClusterMonitoringConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RayClusterMonitoringConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `AdvancedDatapathObservabilityConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvancedDatapathObservabilityConfig {
    /// enableMetrics property.
    pub enable_metrics: Option<bool>,
    /// enableRelay property.
    pub enable_relay: Option<bool>,
    /// relayMode property.
    pub relay_mode: Option<String>,
}

/// `HostConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HostConfig {
    /// ca property.
    pub ca: Option<Vec<CertificateConfig>>,
    /// capabilities property.
    pub capabilities: Option<Vec<String>>,
    /// client property.
    pub client: Option<Vec<CertificateConfigPair>>,
    /// dialTimeout property.
    pub dial_timeout: Option<String>,
    /// header property.
    pub header: Option<Vec<RegistryHeader>>,
    /// host property.
    pub host: Option<String>,
    /// overridePath property.
    pub override_path: Option<bool>,
}

/// `UpgradeSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpgradeSettings {
    /// blueGreenSettings property.
    pub blue_green_settings: Option<BlueGreenSettings>,
    /// maxSurge property.
    pub max_surge: Option<i64>,
    /// maxUnavailable property.
    pub max_unavailable: Option<i64>,
    /// strategy property.
    pub strategy: Option<String>,
}

/// `SwapConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SwapConfig {
    /// bootDiskProfile property.
    pub boot_disk_profile: Option<BootDiskProfile>,
    /// dedicatedLocalSsdProfile property.
    pub dedicated_local_ssd_profile: Option<DedicatedLocalSsdProfile>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// encryptionConfig property.
    pub encryption_config: Option<EncryptionConfig>,
    /// ephemeralLocalSsdProfile property.
    pub ephemeral_local_ssd_profile: Option<EphemeralLocalSsdProfile>,
}

/// `AuthenticatorGroupsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuthenticatorGroupsConfig {
    /// enabled property.
    pub enabled: Option<bool>,
    /// securityGroup property.
    pub security_group: Option<String>,
}

/// `AccurateTimeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccurateTimeConfig {
    /// enablePtpKvmTimeSync property.
    pub enable_ptp_kvm_time_sync: Option<bool>,
}

/// `IPAllocationPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IPAllocationPolicy {
    /// additionalIpRangesConfigs property.
    pub additional_ip_ranges_configs: Option<Vec<AdditionalIPRangesConfig>>,
    /// additionalPodRangesConfig property.
    pub additional_pod_ranges_config: Option<AdditionalPodRangesConfig>,
    /// autoIpamConfig property.
    pub auto_ipam_config: Option<AutoIpamConfig>,
    /// clusterIpv4Cidr property.
    pub cluster_ipv4_cidr: Option<String>,
    /// clusterIpv4CidrBlock property.
    pub cluster_ipv4_cidr_block: Option<String>,
    /// clusterSecondaryRangeName property.
    pub cluster_secondary_range_name: Option<String>,
    /// createSubnetwork property.
    pub create_subnetwork: Option<bool>,
    /// defaultPodIpv4RangeUtilization property.
    pub default_pod_ipv4_range_utilization: Option<f64>,
    /// ipv6AccessType property.
    pub ipv6_access_type: Option<String>,
    /// networkTierConfig property.
    pub network_tier_config: Option<NetworkTierConfig>,
    /// nodeIpv4Cidr property.
    pub node_ipv4_cidr: Option<String>,
    /// nodeIpv4CidrBlock property.
    pub node_ipv4_cidr_block: Option<String>,
    /// podCidrOverprovisionConfig property.
    pub pod_cidr_overprovision_config: Option<PodCIDROverprovisionConfig>,
    /// servicesIpv4Cidr property.
    pub services_ipv4_cidr: Option<String>,
    /// servicesIpv4CidrBlock property.
    pub services_ipv4_cidr_block: Option<String>,
    /// servicesIpv6CidrBlock property.
    pub services_ipv6_cidr_block: Option<String>,
    /// servicesSecondaryRangeName property.
    pub services_secondary_range_name: Option<String>,
    /// stackType property.
    pub stack_type: Option<String>,
    /// subnetIpv6CidrBlock property.
    pub subnet_ipv6_cidr_block: Option<String>,
    /// subnetworkName property.
    pub subnetwork_name: Option<String>,
    /// tpuIpv4CidrBlock property.
    pub tpu_ipv4_cidr_block: Option<String>,
    /// useIpAliases property.
    pub use_ip_aliases: Option<bool>,
    /// useRoutes property.
    pub use_routes: Option<bool>,
}

/// `CidrBlock` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CidrBlock {
    /// cidrBlock property.
    pub cidr_block: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
}

/// `VerticalPodAutoscaling` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VerticalPodAutoscaling {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `NodeAffinity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeAffinity {
    /// key property.
    pub key: Option<String>,
    /// operator property.
    pub operator: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `WorkloadPolicyConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkloadPolicyConfig {
    /// allowNetAdmin property.
    pub allow_net_admin: Option<bool>,
    /// autopilotCompatibilityAuditingEnabled property.
    pub autopilot_compatibility_auditing_enabled: Option<bool>,
}

/// `WindowsNodeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WindowsNodeConfig {
    /// osVersion property.
    pub os_version: Option<String>,
}

/// `DnsCacheConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DnsCacheConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `CrashLoopBackOffConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CrashLoopBackOffConfig {
    /// maxContainerRestartPeriod property.
    pub max_container_restart_period: Option<String>,
}

/// `NodePoolLoggingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodePoolLoggingConfig {
    /// variantConfig property.
    pub variant_config: Option<LoggingVariantConfig>,
}

/// `GcePersistentDiskCsiDriverConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GcePersistentDiskCsiDriverConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `GcfsConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GcfsConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `CompliancePostureConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompliancePostureConfig {
    /// complianceStandards property.
    pub compliance_standards: Option<Vec<ComplianceStandard>>,
    /// mode property.
    pub mode: Option<String>,
}

/// `ResourceLimit` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceLimit {
    /// maximum property.
    pub maximum: Option<String>,
    /// minimum property.
    pub minimum: Option<String>,
    /// resourceType property.
    pub resource_type: Option<String>,
}

/// `AutopilotCompatibilityIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutopilotCompatibilityIssue {
    /// constraintType property.
    pub constraint_type: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// documentationUrl property.
    pub documentation_url: Option<String>,
    /// incompatibilityType property.
    pub incompatibility_type: Option<String>,
    /// lastObservation property.
    pub last_observation: Option<String>,
    /// subjects property.
    pub subjects: Option<Vec<String>>,
}

/// `SecondaryBootDisk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecondaryBootDisk {
    /// diskImage property.
    pub disk_image: Option<String>,
    /// mode property.
    pub mode: Option<String>,
}

/// `SecurityPostureConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPostureConfig {
    /// mode property.
    pub mode: Option<String>,
    /// vulnerabilityMode property.
    pub vulnerability_mode: Option<String>,
}

/// `SoleTenantConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SoleTenantConfig {
    /// minNodeCpus property.
    pub min_node_cpus: Option<i64>,
    /// nodeAffinities property.
    pub node_affinities: Option<Vec<NodeAffinity>>,
}

/// `SetLegacyAbacRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetLegacyAbacRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `DesiredAdditionalIPRangesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DesiredAdditionalIPRangesConfig {
    /// additionalIpRangesConfigs property.
    pub additional_ip_ranges_configs: Option<Vec<AdditionalIPRangesConfig>>,
}

/// `UpdateMasterRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpdateMasterRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// masterVersion property.
    pub master_version: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `GatewayAPIConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GatewayAPIConfig {
    /// channel property.
    pub channel: Option<String>,
}

/// `SetNodePoolAutoscalingRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetNodePoolAutoscalingRequest {
    /// autoscaling property.
    pub autoscaling: Option<NodePoolAutoscaling>,
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nodePoolId property.
    pub node_pool_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `PodAutoscaling` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PodAutoscaling {
    /// hpaProfile property.
    pub hpa_profile: Option<String>,
}

/// `NodeManagement` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeManagement {
    /// autoRepair property.
    pub auto_repair: Option<bool>,
    /// autoUpgrade property.
    pub auto_upgrade: Option<bool>,
    /// upgradeOptions property.
    pub upgrade_options: Option<AutoUpgradeOptions>,
}

/// `ClusterUpgradeInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClusterUpgradeInfo {
    /// autoUpgradeStatus property.
    pub auto_upgrade_status: Option<Vec<String>>,
    /// endOfExtendedSupportTimestamp property.
    pub end_of_extended_support_timestamp: Option<String>,
    /// endOfStandardSupportTimestamp property.
    pub end_of_standard_support_timestamp: Option<String>,
    /// minorTargetVersion property.
    pub minor_target_version: Option<String>,
    /// patchTargetVersion property.
    pub patch_target_version: Option<String>,
    /// pausedReason property.
    pub paused_reason: Option<Vec<String>>,
    /// upgradeDetails property.
    pub upgrade_details: Option<Vec<UpgradeDetails>>,
}

/// `PrivilegedAdmissionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PrivilegedAdmissionConfig {
    /// allowlistPaths property.
    pub allowlist_paths: Option<Vec<String>>,
}

/// `RegistryHeader` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegistryHeader {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<Vec<String>>,
}

/// `ClusterUpdate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClusterUpdate {
    /// additionalPodRangesConfig property.
    pub additional_pod_ranges_config: Option<AdditionalPodRangesConfig>,
    /// desiredAdditionalIpRangesConfig property.
    pub desired_additional_ip_ranges_config: Option<DesiredAdditionalIPRangesConfig>,
    /// desiredAddonsConfig property.
    pub desired_addons_config: Option<AddonsConfig>,
    /// desiredAnonymousAuthenticationConfig property.
    pub desired_anonymous_authentication_config: Option<AnonymousAuthenticationConfig>,
    /// desiredAuthenticatorGroupsConfig property.
    pub desired_authenticator_groups_config: Option<AuthenticatorGroupsConfig>,
    /// desiredAutoIpamConfig property.
    pub desired_auto_ipam_config: Option<AutoIpamConfig>,
    /// desiredAutopilotClusterPolicyConfig property.
    pub desired_autopilot_cluster_policy_config: Option<ClusterPolicyConfig>,
    /// desiredAutopilotWorkloadPolicyConfig property.
    pub desired_autopilot_workload_policy_config: Option<WorkloadPolicyConfig>,
    /// desiredBinaryAuthorization property.
    pub desired_binary_authorization: Option<BinaryAuthorization>,
    /// desiredClusterAutoscaling property.
    pub desired_cluster_autoscaling: Option<ClusterAutoscaling>,
    /// desiredCompliancePostureConfig property.
    pub desired_compliance_posture_config: Option<CompliancePostureConfig>,
    /// desiredContainerdConfig property.
    pub desired_containerd_config: Option<ContainerdConfig>,
    /// desiredControlPlaneEgress property.
    pub desired_control_plane_egress: Option<ControlPlaneEgress>,
    /// desiredControlPlaneEndpointsConfig property.
    pub desired_control_plane_endpoints_config: Option<ControlPlaneEndpointsConfig>,
    /// desiredCostManagementConfig property.
    pub desired_cost_management_config: Option<CostManagementConfig>,
    /// desiredDatabaseEncryption property.
    pub desired_database_encryption: Option<DatabaseEncryption>,
    /// desiredDatapathProvider property.
    pub desired_datapath_provider: Option<String>,
    /// desiredDefaultEnablePrivateNodes property.
    pub desired_default_enable_private_nodes: Option<bool>,
    /// desiredDefaultSnatStatus property.
    pub desired_default_snat_status: Option<DefaultSnatStatus>,
    /// desiredDisableL4LbFirewallReconciliation property.
    pub desired_disable_l4_lb_firewall_reconciliation: Option<bool>,
    /// desiredDnsConfig property.
    pub desired_dns_config: Option<DNSConfig>,
    /// desiredEnableCiliumClusterwideNetworkPolicy property.
    pub desired_enable_cilium_clusterwide_network_policy: Option<bool>,
    /// desiredEnableFqdnNetworkPolicy property.
    pub desired_enable_fqdn_network_policy: Option<bool>,
    /// desiredEnableMultiNetworking property.
    pub desired_enable_multi_networking: Option<bool>,
    /// desiredEnablePrivateEndpoint property.
    pub desired_enable_private_endpoint: Option<bool>,
    /// desiredEnterpriseConfig property.
    pub desired_enterprise_config: Option<DesiredEnterpriseConfig>,
    /// desiredFleet property.
    pub desired_fleet: Option<Fleet>,
    /// desiredGatewayApiConfig property.
    pub desired_gateway_api_config: Option<GatewayAPIConfig>,
    /// desiredGcfsConfig property.
    pub desired_gcfs_config: Option<GcfsConfig>,
    /// desiredIdentityServiceConfig property.
    pub desired_identity_service_config: Option<IdentityServiceConfig>,
    /// desiredImageType property.
    pub desired_image_type: Option<String>,
    /// desiredInTransitEncryptionConfig property.
    pub desired_in_transit_encryption_config: Option<String>,
    /// desiredIntraNodeVisibilityConfig property.
    pub desired_intra_node_visibility_config: Option<IntraNodeVisibilityConfig>,
    /// desiredK8sBetaApis property.
    pub desired_k8s_beta_apis: Option<K8SBetaAPIConfig>,
    /// desiredL4ilbSubsettingConfig property.
    pub desired_l4ilb_subsetting_config: Option<ILBSubsettingConfig>,
    /// desiredLocations property.
    pub desired_locations: Option<Vec<String>>,
    /// desiredLoggingConfig property.
    pub desired_logging_config: Option<LoggingConfig>,
    /// desiredLoggingService property.
    pub desired_logging_service: Option<String>,
    /// desiredManagedMachineLearningDiagnosticsConfig property.
    pub desired_managed_machine_learning_diagnostics_config:
        Option<ManagedMachineLearningDiagnosticsConfig>,
    /// desiredManagedOpentelemetryConfig property.
    pub desired_managed_opentelemetry_config: Option<ManagedOpenTelemetryConfig>,
    /// desiredMasterAuthorizedNetworksConfig property.
    pub desired_master_authorized_networks_config: Option<MasterAuthorizedNetworksConfig>,
    /// desiredMasterVersion property.
    pub desired_master_version: Option<String>,
    /// desiredMeshCertificates property.
    pub desired_mesh_certificates: Option<MeshCertificates>,
    /// desiredMonitoringConfig property.
    pub desired_monitoring_config: Option<MonitoringConfig>,
    /// desiredMonitoringService property.
    pub desired_monitoring_service: Option<String>,
    /// desiredNetworkPerformanceConfig property.
    pub desired_network_performance_config: Option<ClusterNetworkPerformanceConfig>,
    /// desiredNetworkTierConfig property.
    pub desired_network_tier_config: Option<NetworkTierConfig>,
    /// desiredNodeKubeletConfig property.
    pub desired_node_kubelet_config: Option<NodeKubeletConfig>,
    /// desiredNodePoolAutoConfigKubeletConfig property.
    pub desired_node_pool_auto_config_kubelet_config: Option<NodeKubeletConfig>,
    /// desiredNodePoolAutoConfigLinuxNodeConfig property.
    pub desired_node_pool_auto_config_linux_node_config: Option<LinuxNodeConfig>,
    /// desiredNodePoolAutoConfigNetworkTags property.
    pub desired_node_pool_auto_config_network_tags: Option<NetworkTags>,
    /// desiredNodePoolAutoConfigResourceManagerTags property.
    pub desired_node_pool_auto_config_resource_manager_tags: Option<ResourceManagerTags>,
    /// desiredNodePoolAutoscaling property.
    pub desired_node_pool_autoscaling: Option<NodePoolAutoscaling>,
    /// desiredNodePoolId property.
    pub desired_node_pool_id: Option<String>,
    /// desiredNodePoolLoggingConfig property.
    pub desired_node_pool_logging_config: Option<NodePoolLoggingConfig>,
    /// desiredNodeVersion property.
    pub desired_node_version: Option<String>,
    /// desiredNotificationConfig property.
    pub desired_notification_config: Option<NotificationConfig>,
    /// desiredParentProductConfig property.
    pub desired_parent_product_config: Option<ParentProductConfig>,
    /// desiredPodAutoscaling property.
    pub desired_pod_autoscaling: Option<PodAutoscaling>,
    /// desiredPrivateClusterConfig property.
    pub desired_private_cluster_config: Option<PrivateClusterConfig>,
    /// desiredPrivateIpv6GoogleAccess property.
    pub desired_private_ipv6_google_access: Option<String>,
    /// desiredPrivilegedAdmissionConfig property.
    pub desired_privileged_admission_config: Option<PrivilegedAdmissionConfig>,
    /// desiredRbacBindingConfig property.
    pub desired_rbac_binding_config: Option<RBACBindingConfig>,
    /// desiredReleaseChannel property.
    pub desired_release_channel: Option<ReleaseChannel>,
    /// desiredResourceUsageExportConfig property.
    pub desired_resource_usage_export_config: Option<ResourceUsageExportConfig>,
    /// desiredSecretManagerConfig property.
    pub desired_secret_manager_config: Option<SecretManagerConfig>,
    /// desiredSecretSyncConfig property.
    pub desired_secret_sync_config: Option<SecretSyncConfig>,
    /// desiredSecurityPostureConfig property.
    pub desired_security_posture_config: Option<SecurityPostureConfig>,
    /// desiredServiceExternalIpsConfig property.
    pub desired_service_external_ips_config: Option<ServiceExternalIPsConfig>,
    /// desiredShieldedNodes property.
    pub desired_shielded_nodes: Option<ShieldedNodes>,
    /// desiredStackType property.
    pub desired_stack_type: Option<String>,
    /// desiredUserManagedKeysConfig property.
    pub desired_user_managed_keys_config: Option<UserManagedKeysConfig>,
    /// desiredVerticalPodAutoscaling property.
    pub desired_vertical_pod_autoscaling: Option<VerticalPodAutoscaling>,
    /// desiredWorkloadIdentityConfig property.
    pub desired_workload_identity_config: Option<WorkloadIdentityConfig>,
    /// enableK8sBetaApis property.
    pub enable_k8s_beta_apis: Option<K8SBetaAPIConfig>,
    /// etag property.
    pub etag: Option<String>,
    /// gkeAutoUpgradeConfig property.
    pub gke_auto_upgrade_config: Option<GkeAutoUpgradeConfig>,
    /// removedAdditionalPodRangesConfig property.
    pub removed_additional_pod_ranges_config: Option<AdditionalPodRangesConfig>,
    /// userManagedKeysConfig property.
    pub user_managed_keys_config: Option<UserManagedKeysConfig>,
}

/// `IntraNodeVisibilityConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntraNodeVisibilityConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `PrivateClusterConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PrivateClusterConfig {
    /// enablePrivateEndpoint property.
    pub enable_private_endpoint: Option<bool>,
    /// enablePrivateNodes property.
    pub enable_private_nodes: Option<bool>,
    /// masterGlobalAccessConfig property.
    pub master_global_access_config: Option<PrivateClusterMasterGlobalAccessConfig>,
    /// masterIpv4CidrBlock property.
    pub master_ipv4_cidr_block: Option<String>,
    /// peeringName property.
    pub peering_name: Option<String>,
    /// privateEndpoint property.
    pub private_endpoint: Option<String>,
    /// privateEndpointSubnetwork property.
    pub private_endpoint_subnetwork: Option<String>,
    /// publicEndpoint property.
    pub public_endpoint: Option<String>,
}

/// `EncryptionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EncryptionConfig {
    /// disabled property.
    pub disabled: Option<bool>,
}

/// `GkeAutoUpgradeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GkeAutoUpgradeConfig {
    /// patchMode property.
    pub patch_mode: Option<String>,
}

/// `UpdateClusterRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpdateClusterRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// update property.
    pub update: Option<ClusterUpdate>,
    /// zone property.
    pub zone: Option<String>,
}

/// `SecretManagerConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecretManagerConfig {
    /// enabled property.
    pub enabled: Option<bool>,
    /// rotationConfig property.
    pub rotation_config: Option<RotationConfig>,
}

/// `AnonymousAuthenticationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnonymousAuthenticationConfig {
    /// mode property.
    pub mode: Option<String>,
}

/// `Filter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Filter {
    /// eventType property.
    pub event_type: Option<Vec<String>>,
}

/// `UsableSubnetwork` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsableSubnetwork {
    /// ipCidrRange property.
    pub ip_cidr_range: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// secondaryIpRanges property.
    pub secondary_ip_ranges: Option<Vec<UsableSubnetworkSecondaryRange>>,
    /// statusMessage property.
    pub status_message: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
}

/// `RBACBindingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RBACBindingConfig {
    /// enableInsecureBindingSystemAuthenticated property.
    pub enable_insecure_binding_system_authenticated: Option<bool>,
    /// enableInsecureBindingSystemUnauthenticated property.
    pub enable_insecure_binding_system_unauthenticated: Option<bool>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `DatabaseEncryption` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatabaseEncryption {
    /// currentState property.
    pub current_state: Option<String>,
    /// decryptionKeys property.
    pub decryption_keys: Option<Vec<String>>,
    /// keyName property.
    pub key_name: Option<String>,
    /// lastOperationErrors property.
    pub last_operation_errors: Option<Vec<OperationError>>,
    /// state property.
    pub state: Option<String>,
}

/// `RotationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RotationConfig {
    /// enabled property.
    pub enabled: Option<bool>,
    /// rotationInterval property.
    pub rotation_interval: Option<String>,
}

/// `ResourceManagerTags` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceManagerTags {
    /// tags property.
    pub tags: Option<serde_json::Value>,
}

/// `HttpLoadBalancing` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpLoadBalancing {
    /// disabled property.
    pub disabled: Option<bool>,
}

/// `UserManagedKeysConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserManagedKeysConfig {
    /// aggregationCa property.
    pub aggregation_ca: Option<String>,
    /// clusterCa property.
    pub cluster_ca: Option<String>,
    /// controlPlaneDiskEncryptionKey property.
    pub control_plane_disk_encryption_key: Option<String>,
    /// controlPlaneDiskEncryptionKeyVersions property.
    pub control_plane_disk_encryption_key_versions: Option<Vec<String>>,
    /// etcdApiCa property.
    pub etcd_api_ca: Option<String>,
    /// etcdPeerCa property.
    pub etcd_peer_ca: Option<String>,
    /// gkeopsEtcdBackupEncryptionKey property.
    pub gkeops_etcd_backup_encryption_key: Option<String>,
    /// serviceAccountSigningKeys property.
    pub service_account_signing_keys: Option<Vec<String>>,
    /// serviceAccountVerificationKeys property.
    pub service_account_verification_keys: Option<Vec<String>>,
}

/// `MemoryManager` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MemoryManager {
    /// policy property.
    pub policy: Option<String>,
}

/// `WorkloadIdentityConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkloadIdentityConfig {
    /// workloadPool property.
    pub workload_pool: Option<String>,
}

/// `ClusterAutoscaling` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClusterAutoscaling {
    /// autopilotGeneralProfile property.
    pub autopilot_general_profile: Option<String>,
    /// autoprovisioningLocations property.
    pub autoprovisioning_locations: Option<Vec<String>>,
    /// autoprovisioningNodePoolDefaults property.
    pub autoprovisioning_node_pool_defaults: Option<AutoprovisioningNodePoolDefaults>,
    /// autoscalingProfile property.
    pub autoscaling_profile: Option<String>,
    /// defaultComputeClassConfig property.
    pub default_compute_class_config: Option<DefaultComputeClassConfig>,
    /// enableNodeAutoprovisioning property.
    pub enable_node_autoprovisioning: Option<bool>,
    /// resourceLimits property.
    pub resource_limits: Option<Vec<ResourceLimit>>,
}

/// `UsableSubnetworkSecondaryRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsableSubnetworkSecondaryRange {
    /// ipCidrRange property.
    pub ip_cidr_range: Option<String>,
    /// rangeName property.
    pub range_name: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `BootDiskProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BootDiskProfile {
    /// swapSizeGib property.
    pub swap_size_gib: Option<String>,
    /// swapSizePercent property.
    pub swap_size_percent: Option<i64>,
}

/// `MasterAuthorizedNetworksConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MasterAuthorizedNetworksConfig {
    /// cidrBlocks property.
    pub cidr_blocks: Option<Vec<CidrBlock>>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// gcpPublicCidrsAccessEnabled property.
    pub gcp_public_cidrs_access_enabled: Option<bool>,
    /// privateEndpointEnforcementEnabled property.
    pub private_endpoint_enforcement_enabled: Option<bool>,
}

/// `SetMaintenancePolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetMaintenancePolicyRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// maintenancePolicy property.
    pub maintenance_policy: Option<MaintenancePolicy>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `LocalNvmeSsdBlockConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalNvmeSsdBlockConfig {
    /// localSsdCount property.
    pub local_ssd_count: Option<i64>,
}

/// `GPUSharingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GPUSharingConfig {
    /// gpuSharingStrategy property.
    pub gpu_sharing_strategy: Option<String>,
    /// maxSharedClientsPerGpu property.
    pub max_shared_clients_per_gpu: Option<String>,
}

/// `DailyMaintenanceWindow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DailyMaintenanceWindow {
    /// duration property.
    pub duration: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `NetworkTierConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkTierConfig {
    /// networkTier property.
    pub network_tier: Option<String>,
}

/// `SecondaryBootDiskUpdateStrategy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecondaryBootDiskUpdateStrategy {}

/// `SetNetworkPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetNetworkPolicyRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// networkPolicy property.
    pub network_policy: Option<NetworkPolicy>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `NodeTaint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeTaint {
    /// effect property.
    pub effect: Option<String>,
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `PodSnapshotConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PodSnapshotConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `UpdateInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpdateInfo {
    /// blueGreenInfo property.
    pub blue_green_info: Option<BlueGreenInfo>,
}

/// `OperationProgress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OperationProgress {
    /// metrics property.
    pub metrics: Option<Vec<Metric>>,
    /// name property.
    pub name: Option<String>,
    /// stages property.
    pub stages: Option<Vec<Box<OperationProgress>>>,
    /// status property.
    pub status: Option<String>,
}

/// `AutoprovisioningNodePoolDefaults` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoprovisioningNodePoolDefaults {
    /// bootDiskKmsKey property.
    pub boot_disk_kms_key: Option<String>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<i64>,
    /// diskType property.
    pub disk_type: Option<String>,
    /// imageType property.
    pub image_type: Option<String>,
    /// insecureKubeletReadonlyPortEnabled property.
    pub insecure_kubelet_readonly_port_enabled: Option<bool>,
    /// management property.
    pub management: Option<NodeManagement>,
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
    /// oauthScopes property.
    pub oauth_scopes: Option<Vec<String>>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// shieldedInstanceConfig property.
    pub shielded_instance_config: Option<ShieldedInstanceConfig>,
    /// upgradeSettings property.
    pub upgrade_settings: Option<UpgradeSettings>,
}

/// `DNSConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DNSConfig {
    /// additiveVpcScopeDnsDomain property.
    pub additive_vpc_scope_dns_domain: Option<String>,
    /// clusterDns property.
    pub cluster_dns: Option<String>,
    /// clusterDnsDomain property.
    pub cluster_dns_domain: Option<String>,
    /// clusterDnsScope property.
    pub cluster_dns_scope: Option<String>,
}

/// `NodeDrainConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeDrainConfig {
    /// respectPdbDuringNodePoolDeletion property.
    pub respect_pdb_during_node_pool_deletion: Option<bool>,
}

/// `AdditionalNodeNetworkConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdditionalNodeNetworkConfig {
    /// network property.
    pub network: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
}

/// `RayClusterLoggingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RayClusterLoggingConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `ListNodePoolsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListNodePoolsResponse {
    /// nodePools property.
    pub node_pools: Option<Vec<NodePool>>,
}

/// `LegacyAbac` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LegacyAbac {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `ShieldedNodes` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShieldedNodes {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `NodeKubeletConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeKubeletConfig {
    /// allowedUnsafeSysctls property.
    pub allowed_unsafe_sysctls: Option<Vec<String>>,
    /// containerLogMaxFiles property.
    pub container_log_max_files: Option<i64>,
    /// containerLogMaxSize property.
    pub container_log_max_size: Option<String>,
    /// cpuCfsQuota property.
    pub cpu_cfs_quota: Option<bool>,
    /// cpuCfsQuotaPeriod property.
    pub cpu_cfs_quota_period: Option<String>,
    /// cpuManagerPolicy property.
    pub cpu_manager_policy: Option<String>,
    /// crashLoopBackOff property.
    pub crash_loop_back_off: Option<CrashLoopBackOffConfig>,
    /// evictionMaxPodGracePeriodSeconds property.
    pub eviction_max_pod_grace_period_seconds: Option<i64>,
    /// evictionMinimumReclaim property.
    pub eviction_minimum_reclaim: Option<EvictionMinimumReclaim>,
    /// evictionSoft property.
    pub eviction_soft: Option<EvictionSignals>,
    /// evictionSoftGracePeriod property.
    pub eviction_soft_grace_period: Option<EvictionGracePeriod>,
    /// imageGcHighThresholdPercent property.
    pub image_gc_high_threshold_percent: Option<i64>,
    /// imageGcLowThresholdPercent property.
    pub image_gc_low_threshold_percent: Option<i64>,
    /// imageMaximumGcAge property.
    pub image_maximum_gc_age: Option<String>,
    /// imageMinimumGcAge property.
    pub image_minimum_gc_age: Option<String>,
    /// insecureKubeletReadonlyPortEnabled property.
    pub insecure_kubelet_readonly_port_enabled: Option<bool>,
    /// maxParallelImagePulls property.
    pub max_parallel_image_pulls: Option<i64>,
    /// memoryManager property.
    pub memory_manager: Option<MemoryManager>,
    /// podPidsLimit property.
    pub pod_pids_limit: Option<String>,
    /// shutdownGracePeriodCriticalPodsSeconds property.
    pub shutdown_grace_period_critical_pods_seconds: Option<i64>,
    /// shutdownGracePeriodSeconds property.
    pub shutdown_grace_period_seconds: Option<i64>,
    /// singleProcessOomKill property.
    pub single_process_oom_kill: Option<bool>,
    /// topologyManager property.
    pub topology_manager: Option<TopologyManager>,
}

/// `PlacementPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlacementPolicy {
    /// policyName property.
    pub policy_name: Option<String>,
    /// tpuTopology property.
    pub tpu_topology: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `CreateNodePoolRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreateNodePoolRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// nodePool property.
    pub node_pool: Option<NodePool>,
    /// parent property.
    pub parent: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `ListClustersResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListClustersResponse {
    /// clusters property.
    pub clusters: Option<Vec<Cluster>>,
    /// missingZones property.
    pub missing_zones: Option<Vec<String>>,
}

/// `HugepagesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HugepagesConfig {
    /// hugepageSize1g property.
    pub hugepage_size1g: Option<i64>,
    /// hugepageSize2m property.
    pub hugepage_size2m: Option<i64>,
}

/// `Metric` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metric {
    /// doubleValue property.
    pub double_value: Option<f64>,
    /// intValue property.
    pub int_value: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// stringValue property.
    pub string_value: Option<String>,
}

/// `SetLoggingServiceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetLoggingServiceRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// loggingService property.
    pub logging_service: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `NodeNetworkConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeNetworkConfig {
    /// acceleratorNetworkProfile property.
    pub accelerator_network_profile: Option<String>,
    /// additionalNodeNetworkConfigs property.
    pub additional_node_network_configs: Option<Vec<AdditionalNodeNetworkConfig>>,
    /// additionalPodNetworkConfigs property.
    pub additional_pod_network_configs: Option<Vec<AdditionalPodNetworkConfig>>,
    /// createPodRange property.
    pub create_pod_range: Option<bool>,
    /// enablePrivateNodes property.
    pub enable_private_nodes: Option<bool>,
    /// networkPerformanceConfig property.
    pub network_performance_config: Option<NetworkPerformanceConfig>,
    /// networkTierConfig property.
    pub network_tier_config: Option<NetworkTierConfig>,
    /// podCidrOverprovisionConfig property.
    pub pod_cidr_overprovision_config: Option<PodCIDROverprovisionConfig>,
    /// podIpv4CidrBlock property.
    pub pod_ipv4_cidr_block: Option<String>,
    /// podIpv4RangeUtilization property.
    pub pod_ipv4_range_utilization: Option<f64>,
    /// podRange property.
    pub pod_range: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
}

/// `WorkloadMetadataConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkloadMetadataConfig {
    /// mode property.
    pub mode: Option<String>,
}

/// `SetNodePoolManagementRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetNodePoolManagementRequest {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// management property.
    pub management: Option<NodeManagement>,
    /// name property.
    pub name: Option<String>,
    /// nodePoolId property.
    pub node_pool_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `GetOpenIDConfigResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetOpenIDConfigResponse {
    /// cacheHeader property.
    pub cache_header: Option<HttpCacheControlResponseHeader>,
    /// claims_supported property.
    pub claims_supported: Option<Vec<String>>,
    /// grant_types property.
    pub grant_types: Option<Vec<String>>,
    /// id_token_signing_alg_values_supported property.
    pub id_token_signing_alg_values_supported: Option<Vec<String>>,
    /// issuer property.
    pub issuer: Option<String>,
    /// jwks_uri property.
    pub jwks_uri: Option<String>,
    /// response_types_supported property.
    pub response_types_supported: Option<Vec<String>>,
    /// subject_types_supported property.
    pub subject_types_supported: Option<Vec<String>>,
}

/// `AdvancedMachineFeatures` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvancedMachineFeatures {
    /// enableNestedVirtualization property.
    pub enable_nested_virtualization: Option<bool>,
    /// performanceMonitoringUnit property.
    pub performance_monitoring_unit: Option<String>,
    /// threadsPerCore property.
    pub threads_per_core: Option<String>,
}

/// `TimeWindow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeWindow {
    /// endTime property.
    pub end_time: Option<String>,
    /// maintenanceExclusionOptions property.
    pub maintenance_exclusion_options: Option<MaintenanceExclusionOptions>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `BestEffortProvisioning` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BestEffortProvisioning {
    /// enabled property.
    pub enabled: Option<bool>,
    /// minProvisionNodes property.
    pub min_provision_nodes: Option<i64>,
}

/// `ReleaseChannelConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReleaseChannelConfig {
    /// channel property.
    pub channel: Option<String>,
    /// defaultVersion property.
    pub default_version: Option<String>,
    /// upgradeTargetVersion property.
    pub upgrade_target_version: Option<String>,
    /// validVersions property.
    pub valid_versions: Option<Vec<String>>,
}

/// `CertificateConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CertificateConfig {
    /// gcpSecretManagerSecretUri property.
    pub gcp_secret_manager_secret_uri: Option<String>,
}

/// `EvictionGracePeriod` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EvictionGracePeriod {
    /// imagefsAvailable property.
    pub imagefs_available: Option<String>,
    /// imagefsInodesFree property.
    pub imagefs_inodes_free: Option<String>,
    /// memoryAvailable property.
    pub memory_available: Option<String>,
    /// nodefsAvailable property.
    pub nodefs_available: Option<String>,
    /// nodefsInodesFree property.
    pub nodefs_inodes_free: Option<String>,
    /// pidAvailable property.
    pub pid_available: Option<String>,
}

/// `NetworkPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkPolicy {
    /// enabled property.
    pub enabled: Option<bool>,
    /// provider property.
    pub provider: Option<String>,
}

/// `LinuxNodeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LinuxNodeConfig {
    /// accurateTimeConfig property.
    pub accurate_time_config: Option<AccurateTimeConfig>,
    /// cgroupMode property.
    pub cgroup_mode: Option<String>,
    /// hugepages property.
    pub hugepages: Option<HugepagesConfig>,
    /// nodeKernelModuleLoading property.
    pub node_kernel_module_loading: Option<NodeKernelModuleLoading>,
    /// swapConfig property.
    pub swap_config: Option<SwapConfig>,
    /// sysctls property.
    pub sysctls: Option<serde_json::Value>,
    /// transparentHugepageDefrag property.
    pub transparent_hugepage_defrag: Option<String>,
    /// transparentHugepageEnabled property.
    pub transparent_hugepage_enabled: Option<String>,
}

/// `StandardRolloutPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StandardRolloutPolicy {
    /// batchNodeCount property.
    pub batch_node_count: Option<i64>,
    /// batchPercentage property.
    pub batch_percentage: Option<f64>,
    /// batchSoakDuration property.
    pub batch_soak_duration: Option<String>,
}

/// `CertificateAuthorityDomainConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CertificateAuthorityDomainConfig {
    /// fqdns property.
    pub fqdns: Option<Vec<String>>,
    /// gcpSecretManagerCertificateConfig property.
    pub gcp_secret_manager_certificate_config: Option<GCPSecretManagerCertificateConfig>,
}

/// `GcpFilestoreCsiDriverConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GcpFilestoreCsiDriverConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `RangeInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RangeInfo {
    /// rangeName property.
    pub range_name: Option<String>,
    /// utilization property.
    pub utilization: Option<f64>,
}

/// `CheckAutopilotCompatibilityResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CheckAutopilotCompatibilityResponse {
    /// issues property.
    pub issues: Option<Vec<AutopilotCompatibilityIssue>>,
    /// summary property.
    pub summary: Option<String>,
}

/// `MasterAuth` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MasterAuth {
    /// clientCertificate property.
    pub client_certificate: Option<String>,
    /// clientCertificateConfig property.
    pub client_certificate_config: Option<ClientCertificateConfig>,
    /// clientKey property.
    pub client_key: Option<String>,
    /// clusterCaCertificate property.
    pub cluster_ca_certificate: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `NodeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodeConfig {
    /// accelerators property.
    pub accelerators: Option<Vec<AcceleratorConfig>>,
    /// advancedMachineFeatures property.
    pub advanced_machine_features: Option<AdvancedMachineFeatures>,
    /// bootDisk property.
    pub boot_disk: Option<BootDisk>,
    /// bootDiskKmsKey property.
    pub boot_disk_kms_key: Option<String>,
    /// confidentialNodes property.
    pub confidential_nodes: Option<ConfidentialNodes>,
    /// consolidationDelay property.
    pub consolidation_delay: Option<String>,
    /// containerdConfig property.
    pub containerd_config: Option<ContainerdConfig>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<i64>,
    /// diskType property.
    pub disk_type: Option<String>,
    /// effectiveCgroupMode property.
    pub effective_cgroup_mode: Option<String>,
    /// enableConfidentialStorage property.
    pub enable_confidential_storage: Option<bool>,
    /// ephemeralStorageLocalSsdConfig property.
    pub ephemeral_storage_local_ssd_config: Option<EphemeralStorageLocalSsdConfig>,
    /// fastSocket property.
    pub fast_socket: Option<FastSocket>,
    /// flexStart property.
    pub flex_start: Option<bool>,
    /// gcfsConfig property.
    pub gcfs_config: Option<GcfsConfig>,
    /// gpuDirectConfig property.
    pub gpu_direct_config: Option<GPUDirectConfig>,
    /// gvnic property.
    pub gvnic: Option<VirtualNIC>,
    /// imageType property.
    pub image_type: Option<String>,
    /// kubeletConfig property.
    pub kubelet_config: Option<NodeKubeletConfig>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// linuxNodeConfig property.
    pub linux_node_config: Option<LinuxNodeConfig>,
    /// localNvmeSsdBlockConfig property.
    pub local_nvme_ssd_block_config: Option<LocalNvmeSsdBlockConfig>,
    /// localSsdCount property.
    pub local_ssd_count: Option<i64>,
    /// localSsdEncryptionMode property.
    pub local_ssd_encryption_mode: Option<String>,
    /// loggingConfig property.
    pub logging_config: Option<NodePoolLoggingConfig>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// maxRunDuration property.
    pub max_run_duration: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
    /// nodeGroup property.
    pub node_group: Option<String>,
    /// oauthScopes property.
    pub oauth_scopes: Option<Vec<String>>,
    /// preemptible property.
    pub preemptible: Option<bool>,
    /// reservationAffinity property.
    pub reservation_affinity: Option<ReservationAffinity>,
    /// resourceLabels property.
    pub resource_labels: Option<serde_json::Value>,
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<ResourceManagerTags>,
    /// sandboxConfig property.
    pub sandbox_config: Option<SandboxConfig>,
    /// secondaryBootDiskUpdateStrategy property.
    pub secondary_boot_disk_update_strategy: Option<SecondaryBootDiskUpdateStrategy>,
    /// secondaryBootDisks property.
    pub secondary_boot_disks: Option<Vec<SecondaryBootDisk>>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// shieldedInstanceConfig property.
    pub shielded_instance_config: Option<ShieldedInstanceConfig>,
    /// soleTenantConfig property.
    pub sole_tenant_config: Option<SoleTenantConfig>,
    /// spot property.
    pub spot: Option<bool>,
    /// storagePools property.
    pub storage_pools: Option<Vec<String>>,
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// taintConfig property.
    pub taint_config: Option<TaintConfig>,
    /// taints property.
    pub taints: Option<Vec<NodeTaint>>,
    /// windowsNodeConfig property.
    pub windows_node_config: Option<WindowsNodeConfig>,
    /// workloadMetadataConfig property.
    pub workload_metadata_config: Option<WorkloadMetadataConfig>,
}

/// `MonitoringComponentConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MonitoringComponentConfig {
    /// enableComponents property.
    pub enable_components: Option<Vec<String>>,
}

/// `Fleet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Fleet {
    /// membership property.
    pub membership: Option<String>,
    /// membershipType property.
    pub membership_type: Option<String>,
    /// preRegistered property.
    pub pre_registered: Option<bool>,
    /// project property.
    pub project: Option<String>,
}

/// `UpdateNodePoolRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpdateNodePoolRequest {
    /// accelerators property.
    pub accelerators: Option<Vec<AcceleratorConfig>>,
    /// bootDisk property.
    pub boot_disk: Option<BootDisk>,
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// confidentialNodes property.
    pub confidential_nodes: Option<ConfidentialNodes>,
    /// consolidationDelay property.
    pub consolidation_delay: Option<String>,
    /// containerdConfig property.
    pub containerd_config: Option<ContainerdConfig>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<String>,
    /// diskType property.
    pub disk_type: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// fastSocket property.
    pub fast_socket: Option<FastSocket>,
    /// flexStart property.
    pub flex_start: Option<bool>,
    /// gcfsConfig property.
    pub gcfs_config: Option<GcfsConfig>,
    /// gvnic property.
    pub gvnic: Option<VirtualNIC>,
    /// imageType property.
    pub image_type: Option<String>,
    /// kubeletConfig property.
    pub kubelet_config: Option<NodeKubeletConfig>,
    /// labels property.
    pub labels: Option<NodeLabels>,
    /// linuxNodeConfig property.
    pub linux_node_config: Option<LinuxNodeConfig>,
    /// locations property.
    pub locations: Option<Vec<String>>,
    /// loggingConfig property.
    pub logging_config: Option<NodePoolLoggingConfig>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// maxRunDuration property.
    pub max_run_duration: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nodeDrainConfig property.
    pub node_drain_config: Option<NodeDrainConfig>,
    /// nodeNetworkConfig property.
    pub node_network_config: Option<NodeNetworkConfig>,
    /// nodePoolId property.
    pub node_pool_id: Option<String>,
    /// nodeVersion property.
    pub node_version: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// queuedProvisioning property.
    pub queued_provisioning: Option<QueuedProvisioning>,
    /// resourceLabels property.
    pub resource_labels: Option<ResourceLabels>,
    /// resourceManagerTags property.
    pub resource_manager_tags: Option<ResourceManagerTags>,
    /// storagePools property.
    pub storage_pools: Option<Vec<String>>,
    /// tags property.
    pub tags: Option<NetworkTags>,
    /// taints property.
    pub taints: Option<NodeTaints>,
    /// upgradeSettings property.
    pub upgrade_settings: Option<UpgradeSettings>,
    /// windowsNodeConfig property.
    pub windows_node_config: Option<WindowsNodeConfig>,
    /// workloadMetadataConfig property.
    pub workload_metadata_config: Option<WorkloadMetadataConfig>,
    /// zone property.
    pub zone: Option<String>,
}

/// `PrivateRegistryAccessConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PrivateRegistryAccessConfig {
    /// certificateAuthorityDomainConfig property.
    pub certificate_authority_domain_config: Option<Vec<CertificateAuthorityDomainConfig>>,
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `HorizontalPodAutoscaling` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HorizontalPodAutoscaling {
    /// disabled property.
    pub disabled: Option<bool>,
}

/// `QueuedProvisioning` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueuedProvisioning {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `LustreCsiDriverConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LustreCsiDriverConfig {
    /// disableMultiNic property.
    pub disable_multi_nic: Option<bool>,
    /// enableLegacyLustrePort property.
    pub enable_legacy_lustre_port: Option<bool>,
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `BlueGreenInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BlueGreenInfo {
    /// blueInstanceGroupUrls property.
    pub blue_instance_group_urls: Option<Vec<String>>,
    /// bluePoolDeletionStartTime property.
    pub blue_pool_deletion_start_time: Option<String>,
    /// greenInstanceGroupUrls property.
    pub green_instance_group_urls: Option<Vec<String>>,
    /// greenPoolVersion property.
    pub green_pool_version: Option<String>,
    /// phase property.
    pub phase: Option<String>,
}

/// `ClusterPolicyConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClusterPolicyConfig {
    /// noStandardNodePools property.
    pub no_standard_node_pools: Option<bool>,
    /// noSystemImpersonation property.
    pub no_system_impersonation: Option<bool>,
    /// noSystemMutation property.
    pub no_system_mutation: Option<bool>,
    /// noUnsafeWebhooks property.
    pub no_unsafe_webhooks: Option<bool>,
}

/// `GPUDirectConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GPUDirectConfig {
    /// gpuDirectStrategy property.
    pub gpu_direct_strategy: Option<String>,
}

/// `SetMasterAuthRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetMasterAuthRequest {
    /// action property.
    pub action: Option<String>,
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// update property.
    pub update: Option<MasterAuth>,
    /// zone property.
    pub zone: Option<String>,
}

/// `AcceleratorConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AcceleratorConfig {
    /// acceleratorCount property.
    pub accelerator_count: Option<String>,
    /// acceleratorType property.
    pub accelerator_type: Option<String>,
    /// gpuDriverInstallationConfig property.
    pub gpu_driver_installation_config: Option<GPUDriverInstallationConfig>,
    /// gpuPartitionSize property.
    pub gpu_partition_size: Option<String>,
    /// gpuSharingConfig property.
    pub gpu_sharing_config: Option<GPUSharingConfig>,
}

/// `MonitoringConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MonitoringConfig {
    /// advancedDatapathObservabilityConfig property.
    pub advanced_datapath_observability_config: Option<AdvancedDatapathObservabilityConfig>,
    /// componentConfig property.
    pub component_config: Option<MonitoringComponentConfig>,
    /// managedPrometheusConfig property.
    pub managed_prometheus_config: Option<ManagedPrometheusConfig>,
}

/// `GcsFuseCsiDriverConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GcsFuseCsiDriverConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `SandboxConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SandboxConfig {
    /// type property.
    pub r#type: Option<String>,
}

/// `AutopilotConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutopilotConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

/// `OperationError` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OperationError {
    /// errorMessage property.
    pub error_message: Option<String>,
    /// keyName property.
    pub key_name: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `LoggingComponentConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoggingComponentConfig {
    /// enableComponents property.
    pub enable_components: Option<Vec<String>>,
}

/// `KubernetesDashboard` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct KubernetesDashboard {
    /// disabled property.
    pub disabled: Option<bool>,
}

/// `NetworkPerformanceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkPerformanceConfig {
    /// totalEgressBandwidthTier property.
    pub total_egress_bandwidth_tier: Option<String>,
}

/// `NodePoolDefaults` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NodePoolDefaults {
    /// nodeConfigDefaults property.
    pub node_config_defaults: Option<NodeConfigDefaults>,
}

/// `AutoscaledRolloutPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscaledRolloutPolicy {
    /// waitForDrainDuration property.
    pub wait_for_drain_duration: Option<String>,
}

/// `MaintenancePolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MaintenancePolicy {
    /// disruptionBudget property.
    pub disruption_budget: Option<DisruptionBudget>,
    /// resourceVersion property.
    pub resource_version: Option<String>,
    /// window property.
    pub window: Option<MaintenanceWindow>,
}

/// `ListUsableSubnetworksResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListUsableSubnetworksResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// subnetworks property.
    pub subnetworks: Option<Vec<UsableSubnetwork>>,
}

/// `DefaultComputeClassConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DefaultComputeClassConfig {
    /// enabled property.
    pub enabled: Option<bool>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`container.projects.aggregated.usableSubnetworks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsAggregatedUsableSubnetworksListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`container.projects.locations.getServerConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsGetServerConfigArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
    /// Query parameter: `zone`.
    pub zone: Option<String>,
}

/// Arguments for [`container.projects.locations.clusters.checkAutopilotCompatibility_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersCheckAutopilotCompatibilityArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`container.projects.locations.clusters.completeIpRotation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersCompleteIpRotationArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: CompleteIPRotationRequest,
}

/// Arguments for [`container.projects.locations.clusters.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: CreateClusterRequest,
}

/// Arguments for [`container.projects.locations.clusters.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `clusterId`.
    pub cluster_id: Option<String>,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
    /// Query parameter: `zone`.
    pub zone: Option<String>,
}

/// Arguments for [`container.projects.locations.clusters.fetchClusterUpgradeInfo_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersFetchClusterUpgradeInfoArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
}

/// Arguments for [`container.projects.locations.clusters.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `clusterId`.
    pub cluster_id: Option<String>,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
    /// Query parameter: `zone`.
    pub zone: Option<String>,
}

/// Arguments for [`container.projects.locations.clusters.getJwks_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersGetJwksArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`container.projects.locations.clusters.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
    /// Query parameter: `zone`.
    pub zone: Option<String>,
}

/// Arguments for [`container.projects.locations.clusters.setAddons_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersSetAddonsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetAddonsConfigRequest,
}

/// Arguments for [`container.projects.locations.clusters.setLegacyAbac_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersSetLegacyAbacArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetLegacyAbacRequest,
}

/// Arguments for [`container.projects.locations.clusters.setLocations_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersSetLocationsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetLocationsRequest,
}

/// Arguments for [`container.projects.locations.clusters.setLogging_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersSetLoggingArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetLoggingServiceRequest,
}

/// Arguments for [`container.projects.locations.clusters.setMaintenancePolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersSetMaintenancePolicyArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetMaintenancePolicyRequest,
}

/// Arguments for [`container.projects.locations.clusters.setMasterAuth_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersSetMasterAuthArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetMasterAuthRequest,
}

/// Arguments for [`container.projects.locations.clusters.setMonitoring_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersSetMonitoringArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetMonitoringServiceRequest,
}

/// Arguments for [`container.projects.locations.clusters.setNetworkPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersSetNetworkPolicyArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetNetworkPolicyRequest,
}

/// Arguments for [`container.projects.locations.clusters.setResourceLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersSetResourceLabelsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetLabelsRequest,
}

/// Arguments for [`container.projects.locations.clusters.startIpRotation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersStartIpRotationArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: StartIPRotationRequest,
}

/// Arguments for [`container.projects.locations.clusters.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersUpdateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: UpdateClusterRequest,
}

/// Arguments for [`container.projects.locations.clusters.updateMaster_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersUpdateMasterArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: UpdateMasterRequest,
}

/// Arguments for [`container.projects.locations.clusters.nodePools.completeUpgrade_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersNodePoolsCompleteUpgradeArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: CompleteNodePoolUpgradeRequest,
}

/// Arguments for [`container.projects.locations.clusters.nodePools.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersNodePoolsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: CreateNodePoolRequest,
}

/// Arguments for [`container.projects.locations.clusters.nodePools.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersNodePoolsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `clusterId`.
    pub cluster_id: Option<String>,
    /// Query parameter: `nodePoolId`.
    pub node_pool_id: Option<String>,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
    /// Query parameter: `zone`.
    pub zone: Option<String>,
}

/// Arguments for [`container.projects.locations.clusters.nodePools.fetchNodePoolUpgradeInfo_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersNodePoolsFetchNodePoolUpgradeInfoArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
}

/// Arguments for [`container.projects.locations.clusters.nodePools.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersNodePoolsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `clusterId`.
    pub cluster_id: Option<String>,
    /// Query parameter: `nodePoolId`.
    pub node_pool_id: Option<String>,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
    /// Query parameter: `zone`.
    pub zone: Option<String>,
}

/// Arguments for [`container.projects.locations.clusters.nodePools.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersNodePoolsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `clusterId`.
    pub cluster_id: Option<String>,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
    /// Query parameter: `zone`.
    pub zone: Option<String>,
}

/// Arguments for [`container.projects.locations.clusters.nodePools.rollback_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersNodePoolsRollbackArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: RollbackNodePoolUpgradeRequest,
}

/// Arguments for [`container.projects.locations.clusters.nodePools.setAutoscaling_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersNodePoolsSetAutoscalingArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetNodePoolAutoscalingRequest,
}

/// Arguments for [`container.projects.locations.clusters.nodePools.setManagement_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersNodePoolsSetManagementArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetNodePoolManagementRequest,
}

/// Arguments for [`container.projects.locations.clusters.nodePools.setSize_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersNodePoolsSetSizeArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SetNodePoolSizeRequest,
}

/// Arguments for [`container.projects.locations.clusters.nodePools.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersNodePoolsUpdateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: UpdateNodePoolRequest,
}

/// Arguments for [`container.projects.locations.clusters.well-known.getOpenid-configuration_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsClustersWellKnownGetOpenidConfigurationArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`container.projects.locations.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsOperationsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// Arguments for [`container.projects.locations.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `operationId`.
    pub operation_id: Option<String>,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
    /// Query parameter: `zone`.
    pub zone: Option<String>,
}

/// Arguments for [`container.projects.locations.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsLocationsOperationsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
    /// Query parameter: `zone`.
    pub zone: Option<String>,
}

/// Arguments for [`container.projects.zones.getServerconfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesGetServerconfigArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`container.projects.zones.clusters.addons_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersAddonsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: SetAddonsConfigRequest,
}

/// Arguments for [`container.projects.zones.clusters.completeIpRotation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersCompleteIpRotationArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: CompleteIPRotationRequest,
}

/// Arguments for [`container.projects.zones.clusters.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Request body.
    pub body: CreateClusterRequest,
}

/// Arguments for [`container.projects.zones.clusters.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersDeleteArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`container.projects.zones.clusters.fetchClusterUpgradeInfo_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersFetchClusterUpgradeInfoArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
}

/// Arguments for [`container.projects.zones.clusters.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`container.projects.zones.clusters.legacyAbac_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersLegacyAbacArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: SetLegacyAbacRequest,
}

/// Arguments for [`container.projects.zones.clusters.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`container.projects.zones.clusters.locations_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersLocationsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: SetLocationsRequest,
}

/// Arguments for [`container.projects.zones.clusters.logging_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersLoggingArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: SetLoggingServiceRequest,
}

/// Arguments for [`container.projects.zones.clusters.master_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersMasterArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: UpdateMasterRequest,
}

/// Arguments for [`container.projects.zones.clusters.monitoring_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersMonitoringArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: SetMonitoringServiceRequest,
}

/// Arguments for [`container.projects.zones.clusters.resourceLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersResourceLabelsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: SetLabelsRequest,
}

/// Arguments for [`container.projects.zones.clusters.setMaintenancePolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersSetMaintenancePolicyArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: SetMaintenancePolicyRequest,
}

/// Arguments for [`container.projects.zones.clusters.setMasterAuth_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersSetMasterAuthArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: SetMasterAuthRequest,
}

/// Arguments for [`container.projects.zones.clusters.setNetworkPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersSetNetworkPolicyArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: SetNetworkPolicyRequest,
}

/// Arguments for [`container.projects.zones.clusters.startIpRotation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersStartIpRotationArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: StartIPRotationRequest,
}

/// Arguments for [`container.projects.zones.clusters.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersUpdateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: UpdateClusterRequest,
}

/// Arguments for [`container.projects.zones.clusters.nodePools.autoscaling_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsAutoscalingArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Path parameter: `nodePoolId`.
    pub node_pool_id: String,
    /// Request body.
    pub body: SetNodePoolAutoscalingRequest,
}

/// Arguments for [`container.projects.zones.clusters.nodePools.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Request body.
    pub body: CreateNodePoolRequest,
}

/// Arguments for [`container.projects.zones.clusters.nodePools.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsDeleteArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Path parameter: `nodePoolId`.
    pub node_pool_id: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`container.projects.zones.clusters.nodePools.fetchNodePoolUpgradeInfo_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsFetchNodePoolUpgradeInfoArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `version`.
    pub version: Option<String>,
}

/// Arguments for [`container.projects.zones.clusters.nodePools.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Path parameter: `nodePoolId`.
    pub node_pool_id: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`container.projects.zones.clusters.nodePools.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`container.projects.zones.clusters.nodePools.rollback_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsRollbackArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Path parameter: `nodePoolId`.
    pub node_pool_id: String,
    /// Request body.
    pub body: RollbackNodePoolUpgradeRequest,
}

/// Arguments for [`container.projects.zones.clusters.nodePools.setManagement_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsSetManagementArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Path parameter: `nodePoolId`.
    pub node_pool_id: String,
    /// Request body.
    pub body: SetNodePoolManagementRequest,
}

/// Arguments for [`container.projects.zones.clusters.nodePools.setSize_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsSetSizeArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Path parameter: `nodePoolId`.
    pub node_pool_id: String,
    /// Request body.
    pub body: SetNodePoolSizeRequest,
}

/// Arguments for [`container.projects.zones.clusters.nodePools.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesClustersNodePoolsUpdateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
    /// Path parameter: `nodePoolId`.
    pub node_pool_id: String,
    /// Request body.
    pub body: UpdateNodePoolRequest,
}

/// Arguments for [`container.projects.zones.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesOperationsCancelArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `operationId`.
    pub operation_id: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// Arguments for [`container.projects.zones.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesOperationsGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `operationId`.
    pub operation_id: String,
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`container.projects.zones.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ContainerProjectsZonesOperationsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/{+parent}/aggregated/usableSubnetworks
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/aggregated/usableSubnetworks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_aggregated_usable_subnetworks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_aggregated_usable_subnetworks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsAggregatedUsableSubnetworksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUsableSubnetworksResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}/aggregated/usableSubnetworks",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListUsableSubnetworksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/serverConfig
// -----------------------------------------------------------------------------

/// GET v1/{+name}/serverConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_get_server_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_get_server_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsGetServerConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServerConfig>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}/serverConfig",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("zone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ServerConfig =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:checkAutopilotCompatibility
// -----------------------------------------------------------------------------

/// GET v1/{+name}:checkAutopilotCompatibility.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_check_autopilot_compatibility_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_check_autopilot_compatibility_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersCheckAutopilotCompatibilityArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<CheckAutopilotCompatibilityResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:checkAutopilotCompatibility",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CheckAutopilotCompatibilityResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:completeIpRotation
// -----------------------------------------------------------------------------

/// POST v1/{+name}:completeIpRotation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_complete_ip_rotation_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_complete_ip_rotation_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersCompleteIpRotationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:completeIpRotation",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/clusters
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/clusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}/clusters",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://container.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cluster_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("clusterId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("zone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:fetchClusterUpgradeInfo
// -----------------------------------------------------------------------------

/// GET v1/{+name}:fetchClusterUpgradeInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_fetch_cluster_upgrade_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_fetch_cluster_upgrade_info_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersFetchClusterUpgradeInfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClusterUpgradeInfo>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:fetchClusterUpgradeInfo",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ClusterUpgradeInfo =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Cluster>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://container.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cluster_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("clusterId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("zone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Cluster =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/jwks
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/jwks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_get_jwks_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_get_jwks_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersGetJwksArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetJSONWebKeysResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://container.googleapis.com/v1/{}/jwks", args.parent,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetJSONWebKeysResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/clusters
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/clusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClustersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}/clusters",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("zone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListClustersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setAddons
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setAddons.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_set_addons_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_set_addons_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersSetAddonsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:setAddons",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setLegacyAbac
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setLegacyAbac.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_set_legacy_abac_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_set_legacy_abac_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersSetLegacyAbacArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:setLegacyAbac",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setLocations
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setLocations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_set_locations_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_set_locations_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersSetLocationsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:setLocations",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setLogging
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setLogging.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_set_logging_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_set_logging_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersSetLoggingArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:setLogging",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setMaintenancePolicy
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setMaintenancePolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_set_maintenance_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_set_maintenance_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersSetMaintenancePolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:setMaintenancePolicy",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setMasterAuth
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setMasterAuth.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_set_master_auth_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_set_master_auth_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersSetMasterAuthArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:setMasterAuth",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setMonitoring
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setMonitoring.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_set_monitoring_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_set_monitoring_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersSetMonitoringArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:setMonitoring",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setNetworkPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setNetworkPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_set_network_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_set_network_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersSetNetworkPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:setNetworkPolicy",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setResourceLabels
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setResourceLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_set_resource_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_set_resource_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersSetResourceLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:setResourceLabels",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:startIpRotation
// -----------------------------------------------------------------------------

/// POST v1/{+name}:startIpRotation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_start_ip_rotation_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_start_ip_rotation_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersStartIpRotationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:startIpRotation",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/{+name}
// -----------------------------------------------------------------------------

/// PUT v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://container.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:updateMaster
// -----------------------------------------------------------------------------

/// POST v1/{+name}:updateMaster.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_update_master_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_update_master_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersUpdateMasterArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:updateMaster",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:completeUpgrade
// -----------------------------------------------------------------------------

/// POST v1/{+name}:completeUpgrade.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_node_pools_complete_upgrade_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_node_pools_complete_upgrade_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersNodePoolsCompleteUpgradeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:completeUpgrade",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/nodePools
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/nodePools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_node_pools_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_node_pools_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersNodePoolsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}/nodePools",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_node_pools_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_node_pools_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersNodePoolsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://container.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cluster_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("clusterId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.node_pool_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("nodePoolId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("zone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:fetchNodePoolUpgradeInfo
// -----------------------------------------------------------------------------

/// GET v1/{+name}:fetchNodePoolUpgradeInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_node_pools_fetch_node_pool_upgrade_info_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersNodePoolsFetchNodePoolUpgradeInfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodePoolUpgradeInfo>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:fetchNodePoolUpgradeInfo",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodePoolUpgradeInfo =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_node_pools_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_node_pools_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersNodePoolsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodePool>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://container.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cluster_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("clusterId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.node_pool_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("nodePoolId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("zone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodePool =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/nodePools
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/nodePools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_node_pools_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_node_pools_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersNodePoolsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNodePoolsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}/nodePools",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cluster_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("clusterId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("zone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListNodePoolsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:rollback
// -----------------------------------------------------------------------------

/// POST v1/{+name}:rollback.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_node_pools_rollback_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_node_pools_rollback_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersNodePoolsRollbackArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://container.googleapis.com/v1/{}:rollback", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setAutoscaling
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setAutoscaling.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_node_pools_set_autoscaling_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_node_pools_set_autoscaling_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersNodePoolsSetAutoscalingArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:setAutoscaling",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setManagement
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setManagement.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_node_pools_set_management_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_node_pools_set_management_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersNodePoolsSetManagementArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:setManagement",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setSize
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setSize.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_node_pools_set_size_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_node_pools_set_size_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersNodePoolsSetSizeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://container.googleapis.com/v1/{}:setSize", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/{+name}
// -----------------------------------------------------------------------------

/// PUT v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_node_pools_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_node_pools_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersNodePoolsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://container.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/.well-known/openid-configuration
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/.well-known/openid-configuration.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_clusters_well_known_get_openid_configuration_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_clusters_well_known_get_openid_configuration_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsClustersWellKnownGetOpenidConfigurationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetOpenIDConfigResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}/.well-known/openid-configuration",
        args.parent,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetOpenIDConfigResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://container.googleapis.com/v1/{}:cancel", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://container.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.operation_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("operationId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("zone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/operations
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_locations_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_locations_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsLocationsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}/operations",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("zone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOperationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/zones/{zone}/serverconfig
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/zones/{zone}/serverconfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_get_serverconfig_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_get_serverconfig_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesGetServerconfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServerConfig>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/serverconfig",
        args.project_id, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ServerConfig =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/addons
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/addons.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_addons_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_addons_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersAddonsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/addons",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:completeIpRotation
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:completeIpRotation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_complete_ip_rotation_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_complete_ip_rotation_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersCompleteIpRotationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}:completeIpRotation",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters",
        args.project_id, args.zone,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}",
        args.project_id, args.zone, args.cluster_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:fetchClusterUpgradeInfo
// -----------------------------------------------------------------------------

/// GET v1/{+name}:fetchClusterUpgradeInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_fetch_cluster_upgrade_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_fetch_cluster_upgrade_info_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersFetchClusterUpgradeInfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClusterUpgradeInfo>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:fetchClusterUpgradeInfo",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ClusterUpgradeInfo =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Cluster>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}",
        args.project_id, args.zone, args.cluster_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Cluster =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/legacyAbac
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/legacyAbac.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_legacy_abac_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_legacy_abac_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersLegacyAbacArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/legacyAbac",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/zones/{zone}/clusters
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/zones/{zone}/clusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClustersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters",
        args.project_id, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListClustersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/locations
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/locations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_locations_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_locations_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersLocationsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/locations",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/logging
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/logging.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_logging_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_logging_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersLoggingArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/logging",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/master
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/master.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_master_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_master_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersMasterArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/master",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/monitoring
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/monitoring.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_monitoring_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_monitoring_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersMonitoringArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/monitoring",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/resourceLabels
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/resourceLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_resource_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_resource_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersResourceLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/resourceLabels",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMaintenancePolicy
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMaintenancePolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_set_maintenance_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_set_maintenance_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersSetMaintenancePolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}:setMaintenancePolicy",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMasterAuth
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setMasterAuth.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_set_master_auth_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_set_master_auth_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersSetMasterAuthArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}:setMasterAuth",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setNetworkPolicy
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:setNetworkPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_set_network_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_set_network_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersSetNetworkPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}:setNetworkPolicy",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:startIpRotation
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}:startIpRotation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_start_ip_rotation_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_start_ip_rotation_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersStartIpRotationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}:startIpRotation",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}
// -----------------------------------------------------------------------------

/// PUT v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/autoscaling
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/autoscaling.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_node_pools_autoscaling_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_node_pools_autoscaling_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersNodePoolsAutoscalingArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}/autoscaling",
        args.project_id,
        args.zone,
        args.cluster_id,
        args.node_pool_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_node_pools_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_node_pools_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersNodePoolsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools",
        args.project_id, args.zone, args.cluster_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_node_pools_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_node_pools_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersNodePoolsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}",
        args.project_id, args.zone, args.cluster_id, args.node_pool_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}:fetchNodePoolUpgradeInfo
// -----------------------------------------------------------------------------

/// GET v1/{+name}:fetchNodePoolUpgradeInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_node_pools_fetch_node_pool_upgrade_info_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersNodePoolsFetchNodePoolUpgradeInfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodePoolUpgradeInfo>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/{}:fetchNodePoolUpgradeInfo",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodePoolUpgradeInfo =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_node_pools_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_node_pools_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersNodePoolsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NodePool>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}",
        args.project_id, args.zone, args.cluster_id, args.node_pool_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: NodePool =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_node_pools_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_node_pools_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersNodePoolsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNodePoolsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools",
        args.project_id, args.zone, args.cluster_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListNodePoolsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}:rollback
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}:rollback.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_node_pools_rollback_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_node_pools_rollback_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersNodePoolsRollbackArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}:rollback",
        args.project_id,
        args.zone,
        args.cluster_id,
        args.node_pool_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setManagement
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setManagement.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_node_pools_set_management_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_node_pools_set_management_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersNodePoolsSetManagementArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}/setManagement",
        args.project_id,
        args.zone,
        args.cluster_id,
        args.node_pool_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setSize
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/setSize.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_node_pools_set_size_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_node_pools_set_size_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersNodePoolsSetSizeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}/setSize",
        args.project_id, args.zone, args.cluster_id, args.node_pool_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/update
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/clusters/{clusterId}/nodePools/{nodePoolId}/update.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_clusters_node_pools_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_clusters_node_pools_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesClustersNodePoolsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/clusters/{}/nodePools/{}/update",
        args.project_id, args.zone, args.cluster_id, args.node_pool_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}/zones/{zone}/operations/{operationId}:cancel
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}/zones/{zone}/operations/{operationId}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/operations/{}:cancel",
        args.project_id, args.zone, args.operation_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/zones/{zone}/operations/{operationId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/operations/{}",
        args.project_id, args.zone, args.operation_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectId}/zones/{zone}/operations
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectId}/zones/{zone}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = container_projects_zones_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn container_projects_zones_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ContainerProjectsZonesOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://container.googleapis.com/v1/projects/{}/zones/{}/operations",
        args.project_id, args.zone,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOperationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
