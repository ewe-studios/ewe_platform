//! Auto-generated API module for gcp/cloudsearch cloudsearch.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_cloudsearch_cloudsearch `

#![cfg(feature = "gcp_cloudsearch_cloudsearch")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ObjectValues` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObjectValues {
    /// values property.
    pub values: Option<Vec<Box<StructuredDataObject>>>,
}

/// `ErrorMessage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ErrorMessage {
    /// errorMessage property.
    pub error_message: Option<String>,
    /// source property.
    pub source: Option<Source>,
}

/// `ObjectDefinition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObjectDefinition {
    /// name property.
    pub name: Option<String>,
    /// options property.
    pub options: Option<ObjectOptions>,
    /// propertyDefinitions property.
    pub property_definitions: Option<Vec<Box<PropertyDefinition>>>,
}

/// `EnumOperatorOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EnumOperatorOptions {
    /// operatorName property.
    pub operator_name: Option<String>,
}

/// `SearchApplicationQueryStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchApplicationQueryStats {
    /// date property.
    pub date: Option<Date>,
    /// queryCountByStatus property.
    pub query_count_by_status: Option<Vec<QueryCountByStatus>>,
}

/// `AuditLoggingSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuditLoggingSettings {
    /// logAdminReadActions property.
    pub log_admin_read_actions: Option<bool>,
    /// logDataReadActions property.
    pub log_data_read_actions: Option<bool>,
    /// logDataWriteActions property.
    pub log_data_write_actions: Option<bool>,
    /// project property.
    pub project: Option<String>,
}

/// `CustomerUserStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomerUserStats {
    /// date property.
    pub date: Option<Date>,
    /// oneDayActiveUsersCount property.
    pub one_day_active_users_count: Option<String>,
    /// sevenDaysActiveUsersCount property.
    pub seven_days_active_users_count: Option<String>,
    /// thirtyDaysActiveUsersCount property.
    pub thirty_days_active_users_count: Option<String>,
}

/// `CustomerIndexStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomerIndexStats {
    /// date property.
    pub date: Option<Date>,
    /// itemCountByStatus property.
    pub item_count_by_status: Option<Vec<ItemCountByStatus>>,
}

/// `IntegerValues` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntegerValues {
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `DoubleValues` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DoubleValues {
    /// values property.
    pub values: Option<Vec<f64>>,
}

/// `CustomerSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomerSettings {
    /// auditLoggingSettings property.
    pub audit_logging_settings: Option<AuditLoggingSettings>,
    /// vpcSettings property.
    pub vpc_settings: Option<VPCSettings>,
}

/// `UpdateSchemaRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpdateSchemaRequest {
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
    /// schema property.
    pub schema: Option<Schema>,
    /// validateOnly property.
    pub validate_only: Option<bool>,
}

/// `SourceResultCount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceResultCount {
    /// hasMoreResults property.
    pub has_more_results: Option<bool>,
    /// resultCountEstimate property.
    pub result_count_estimate: Option<String>,
    /// resultCountExact property.
    pub result_count_exact: Option<String>,
    /// source property.
    pub source: Option<Source>,
}

/// `HtmlOperatorOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HtmlOperatorOptions {
    /// operatorName property.
    pub operator_name: Option<String>,
}

/// `SearchResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchResult {
    /// clusteredResults property.
    pub clustered_results: Option<Vec<Box<SearchResult>>>,
    /// debugInfo property.
    pub debug_info: Option<ResultDebugInfo>,
    /// metadata property.
    pub metadata: Option<Metadata>,
    /// snippet property.
    pub snippet: Option<Snippet>,
    /// title property.
    pub title: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `TimestampValues` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimestampValues {
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `ProcessingError` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProcessingError {
    /// code property.
    pub code: Option<String>,
    /// errorMessage property.
    pub error_message: Option<String>,
    /// fieldViolations property.
    pub field_violations: Option<Vec<FieldViolation>>,
}

/// `EnumPropertyOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EnumPropertyOptions {
    /// operatorOptions property.
    pub operator_options: Option<EnumOperatorOptions>,
    /// orderedRanking property.
    pub ordered_ranking: Option<String>,
    /// possibleValues property.
    pub possible_values: Option<Vec<EnumValuePair>>,
}

/// `Principal` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Principal {
    /// groupResourceName property.
    pub group_resource_name: Option<String>,
    /// gsuitePrincipal property.
    pub gsuite_principal: Option<GSuitePrincipal>,
    /// userResourceName property.
    pub user_resource_name: Option<String>,
}

/// `ItemAcl` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ItemAcl {
    /// aclInheritanceType property.
    pub acl_inheritance_type: Option<String>,
    /// deniedReaders property.
    pub denied_readers: Option<Vec<Principal>>,
    /// inheritAclFrom property.
    pub inherit_acl_from: Option<String>,
    /// owners property.
    pub owners: Option<Vec<Principal>>,
    /// readers property.
    pub readers: Option<Vec<Principal>>,
}

/// `SourceCrowdingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceCrowdingConfig {
    /// numResults property.
    pub num_results: Option<i64>,
    /// numSuggestions property.
    pub num_suggestions: Option<i64>,
}

/// `ContextAttribute` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContextAttribute {
    /// name property.
    pub name: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `RemoveActivityRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemoveActivityRequest {
    /// requestOptions property.
    pub request_options: Option<RequestOptions>,
    /// userActivity property.
    pub user_activity: Option<UserActivity>,
}

/// `ItemStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ItemStatus {
    /// code property.
    pub code: Option<String>,
    /// processingErrors property.
    pub processing_errors: Option<Vec<ProcessingError>>,
    /// repositoryErrors property.
    pub repository_errors: Option<Vec<RepositoryError>>,
}

/// `PeopleSuggestion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PeopleSuggestion {
    /// person property.
    pub person: Option<Person>,
}

/// `EnumValuePair` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EnumValuePair {
    /// integerValue property.
    pub integer_value: Option<i64>,
    /// stringValue property.
    pub string_value: Option<String>,
}

/// `SuggestResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuggestResponse {
    /// suggestResults property.
    pub suggest_results: Option<Vec<SuggestResult>>,
}

/// `PollItemsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PollItemsRequest {
    /// connectorName property.
    pub connector_name: Option<String>,
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
    /// limit property.
    pub limit: Option<i64>,
    /// queue property.
    pub queue: Option<String>,
    /// statusCodes property.
    pub status_codes: Option<Vec<String>>,
}

/// `Item` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Item {
    /// acl property.
    pub acl: Option<ItemAcl>,
    /// content property.
    pub content: Option<ItemContent>,
    /// itemType property.
    pub item_type: Option<String>,
    /// metadata property.
    pub metadata: Option<ItemMetadata>,
    /// name property.
    pub name: Option<String>,
    /// payload property.
    pub payload: Option<String>,
    /// queue property.
    pub queue: Option<String>,
    /// status property.
    pub status: Option<ItemStatus>,
    /// structuredData property.
    pub structured_data: Option<ItemStructuredData>,
    /// version property.
    pub version: Option<String>,
}

/// `StartUploadItemRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StartUploadItemRequest {
    /// connectorName property.
    pub connector_name: Option<String>,
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
}

/// `UserActivity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserActivity {
    /// queryActivity property.
    pub query_activity: Option<QueryActivity>,
}

/// `ListUnmappedIdentitiesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListUnmappedIdentitiesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unmappedIdentities property.
    pub unmapped_identities: Option<Vec<UnmappedIdentity>>,
}

/// `Date` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `ResponseDebugInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResponseDebugInfo {
    /// formattedDebugInfo property.
    pub formatted_debug_info: Option<String>,
}

/// `MatchRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MatchRange {
    /// end property.
    pub end: Option<i64>,
    /// start property.
    pub start: Option<i64>,
}

/// `DateValues` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DateValues {
    /// values property.
    pub values: Option<Vec<Date>>,
}

/// `GetCustomerSessionStatsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetCustomerSessionStatsResponse {
    /// stats property.
    pub stats: Option<Vec<CustomerSessionStats>>,
}

/// `FieldViolation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FieldViolation {
    /// description property.
    pub description: Option<String>,
    /// field property.
    pub field: Option<String>,
}

/// `ListQuerySourcesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListQuerySourcesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sources property.
    pub sources: Option<Vec<QuerySource>>,
}

/// `SearchQualityMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchQualityMetadata {
    /// quality property.
    pub quality: Option<f64>,
}

/// `IndexItemRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IndexItemRequest {
    /// connectorName property.
    pub connector_name: Option<String>,
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
    /// indexItemOptions property.
    pub index_item_options: Option<IndexItemOptions>,
    /// item property.
    pub item: Option<Item>,
    /// mode property.
    pub mode: Option<String>,
}

/// `GetCustomerQueryStatsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetCustomerQueryStatsResponse {
    /// stats property.
    pub stats: Option<Vec<CustomerQueryStats>>,
    /// totalQueryCount property.
    pub total_query_count: Option<String>,
}

/// `SourceScoringConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceScoringConfig {
    /// sourceImportance property.
    pub source_importance: Option<String>,
}

/// `VPCSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VPCSettings {
    /// project property.
    pub project: Option<String>,
}

/// `FacetResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FacetResult {
    /// buckets property.
    pub buckets: Option<Vec<FacetBucket>>,
    /// objectType property.
    pub object_type: Option<String>,
    /// operatorName property.
    pub operator_name: Option<String>,
    /// sourceName property.
    pub source_name: Option<String>,
}

/// `DateOperatorOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DateOperatorOptions {
    /// greaterThanOperatorName property.
    pub greater_than_operator_name: Option<String>,
    /// lessThanOperatorName property.
    pub less_than_operator_name: Option<String>,
    /// operatorName property.
    pub operator_name: Option<String>,
}

/// `FilterOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FilterOptions {
    /// filter property.
    pub filter: Option<Box<Filter>>,
    /// objectType property.
    pub object_type: Option<String>,
}

/// `ScoringConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ScoringConfig {
    /// disableFreshness property.
    pub disable_freshness: Option<bool>,
    /// disablePersonalization property.
    pub disable_personalization: Option<bool>,
}

/// `SearchItemsByViewUrlResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchItemsByViewUrlResponse {
    /// items property.
    pub items: Option<Vec<Item>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `UnreserveItemsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UnreserveItemsRequest {
    /// connectorName property.
    pub connector_name: Option<String>,
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
    /// queue property.
    pub queue: Option<String>,
}

/// `GetSearchApplicationSessionStatsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetSearchApplicationSessionStatsResponse {
    /// stats property.
    pub stats: Option<Vec<SearchApplicationSessionStats>>,
}

/// `RequestOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RequestOptions {
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// searchApplicationId property.
    pub search_application_id: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
}

/// `PropertyDisplayOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PropertyDisplayOptions {
    /// displayLabel property.
    pub display_label: Option<String>,
}

/// `Metaline` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metaline {
    /// properties property.
    pub properties: Option<Vec<DisplayedProperty>>,
}

/// `Media` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Media {
    /// resourceName property.
    pub resource_name: Option<String>,
}

/// `SuggestResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuggestResult {
    /// peopleSuggestion property.
    pub people_suggestion: Option<PeopleSuggestion>,
    /// querySuggestion property.
    pub query_suggestion: Option<QuerySuggestion>,
    /// source property.
    pub source: Option<Source>,
    /// suggestedQuery property.
    pub suggested_query: Option<String>,
}

/// `FacetOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FacetOptions {
    /// integerFacetingOptions property.
    pub integer_faceting_options: Option<IntegerFacetingOptions>,
    /// numFacetBuckets property.
    pub num_facet_buckets: Option<i64>,
    /// objectType property.
    pub object_type: Option<String>,
    /// operatorName property.
    pub operator_name: Option<String>,
    /// sourceName property.
    pub source_name: Option<String>,
}

/// `CheckAccessResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CheckAccessResponse {
    /// hasAccess property.
    pub has_access: Option<bool>,
}

/// `UpdateDataSourceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpdateDataSourceRequest {
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
    /// source property.
    pub source: Option<DataSource>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `Metadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metadata {
    /// createTime property.
    pub create_time: Option<String>,
    /// displayOptions property.
    pub display_options: Option<ResultDisplayMetadata>,
    /// fields property.
    pub fields: Option<Vec<Box<NamedProperty>>>,
    /// mimeType property.
    pub mime_type: Option<String>,
    /// objectType property.
    pub object_type: Option<String>,
    /// owner property.
    pub owner: Option<Person>,
    /// source property.
    pub source: Option<Source>,
    /// thumbnailUrl property.
    pub thumbnail_url: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `ResetSearchApplicationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResetSearchApplicationRequest {
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
}

/// `ListOperationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListOperationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<Operation>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `ObjectPropertyOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObjectPropertyOptions {
    /// subobjectProperties property.
    pub subobject_properties: Option<Vec<Box<PropertyDefinition>>>,
}

/// `Photo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Photo {
    /// url property.
    pub url: Option<String>,
}

/// `ItemStructuredData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ItemStructuredData {
    /// hash property.
    pub hash: Option<String>,
    /// object property.
    pub object: Option<Box<StructuredDataObject>>,
}

/// `IntegerFacetingOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntegerFacetingOptions {
    /// integerBuckets property.
    pub integer_buckets: Option<Vec<String>>,
}

/// `PushItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PushItem {
    /// contentHash property.
    pub content_hash: Option<String>,
    /// metadataHash property.
    pub metadata_hash: Option<String>,
    /// payload property.
    pub payload: Option<String>,
    /// queue property.
    pub queue: Option<String>,
    /// repositoryError property.
    pub repository_error: Option<RepositoryError>,
    /// structuredDataHash property.
    pub structured_data_hash: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `TimestampOperatorOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimestampOperatorOptions {
    /// greaterThanOperatorName property.
    pub greater_than_operator_name: Option<String>,
    /// lessThanOperatorName property.
    pub less_than_operator_name: Option<String>,
    /// operatorName property.
    pub operator_name: Option<String>,
}

/// `ListSearchApplicationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListSearchApplicationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// searchApplications property.
    pub search_applications: Option<Vec<SearchApplication>>,
}

/// `GetCustomerSearchApplicationStatsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetCustomerSearchApplicationStatsResponse {
    /// averageSearchApplicationCount property.
    pub average_search_application_count: Option<String>,
    /// stats property.
    pub stats: Option<Vec<CustomerSearchApplicationStats>>,
}

/// `ResultCounts` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResultCounts {
    /// sourceResultCounts property.
    pub source_result_counts: Option<Vec<SourceResultCount>>,
}

/// `DataSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataSource {
    /// disableModifications property.
    pub disable_modifications: Option<bool>,
    /// disableServing property.
    pub disable_serving: Option<bool>,
    /// displayName property.
    pub display_name: Option<String>,
    /// indexingServiceAccounts property.
    pub indexing_service_accounts: Option<Vec<String>>,
    /// itemsVisibility property.
    pub items_visibility: Option<Vec<GSuitePrincipal>>,
    /// name property.
    pub name: Option<String>,
    /// operationIds property.
    pub operation_ids: Option<Vec<String>>,
    /// returnThumbnailUrls property.
    pub return_thumbnail_urls: Option<bool>,
    /// shortName property.
    pub short_name: Option<String>,
}

/// `SearchApplicationUserStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchApplicationUserStats {
    /// date property.
    pub date: Option<Date>,
    /// oneDayActiveUsersCount property.
    pub one_day_active_users_count: Option<String>,
    /// sevenDaysActiveUsersCount property.
    pub seven_days_active_users_count: Option<String>,
    /// thirtyDaysActiveUsersCount property.
    pub thirty_days_active_users_count: Option<String>,
}

/// `CompositeFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompositeFilter {
    /// logicOperator property.
    pub logic_operator: Option<String>,
    /// subFilters property.
    pub sub_filters: Option<Vec<Box<Filter>>>,
}

/// `ListDataSourceResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListDataSourceResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sources property.
    pub sources: Option<Vec<DataSource>>,
}

/// `SortOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SortOptions {
    /// operatorName property.
    pub operator_name: Option<String>,
    /// sortOrder property.
    pub sort_order: Option<String>,
}

/// `Name` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Name {
    /// displayName property.
    pub display_name: Option<String>,
}

/// `ResultDisplayLine` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResultDisplayLine {
    /// fields property.
    pub fields: Option<Vec<ResultDisplayField>>,
}

/// `CustomerSessionStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomerSessionStats {
    /// date property.
    pub date: Option<Date>,
    /// searchSessionsCount property.
    pub search_sessions_count: Option<String>,
}

/// `PhoneNumber` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PhoneNumber {
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `RetrievalImportance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RetrievalImportance {
    /// importance property.
    pub importance: Option<String>,
}

/// `GetSearchApplicationUserStatsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetSearchApplicationUserStatsResponse {
    /// stats property.
    pub stats: Option<Vec<SearchApplicationUserStats>>,
}

/// `TextValues` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TextValues {
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `Schema` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Schema {
    /// objectDefinitions property.
    pub object_definitions: Option<Vec<ObjectDefinition>>,
    /// operationIds property.
    pub operation_ids: Option<Vec<String>>,
}

/// `SearchRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchRequest {
    /// contextAttributes property.
    pub context_attributes: Option<Vec<ContextAttribute>>,
    /// dataSourceRestrictions property.
    pub data_source_restrictions: Option<Vec<DataSourceRestriction>>,
    /// facetOptions property.
    pub facet_options: Option<Vec<FacetOptions>>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// query property.
    pub query: Option<String>,
    /// queryInterpretationOptions property.
    pub query_interpretation_options: Option<QueryInterpretationOptions>,
    /// requestOptions property.
    pub request_options: Option<RequestOptions>,
    /// sortOptions property.
    pub sort_options: Option<SortOptions>,
    /// start property.
    pub start: Option<i64>,
}

/// `BooleanPropertyOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BooleanPropertyOptions {
    /// operatorOptions property.
    pub operator_options: Option<BooleanOperatorOptions>,
}

/// `SafeHtmlProto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SafeHtmlProto {
    /// privateDoNotAccessOrElseSafeHtmlWrappedValue property.
    pub private_do_not_access_or_else_safe_html_wrapped_value: Option<String>,
}

/// `QueryOperator` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryOperator {
    /// displayName property.
    pub display_name: Option<String>,
    /// enumValues property.
    pub enum_values: Option<Vec<String>>,
    /// greaterThanOperatorName property.
    pub greater_than_operator_name: Option<String>,
    /// isFacetable property.
    pub is_facetable: Option<bool>,
    /// isRepeatable property.
    pub is_repeatable: Option<bool>,
    /// isReturnable property.
    pub is_returnable: Option<bool>,
    /// isSortable property.
    pub is_sortable: Option<bool>,
    /// isSuggestable property.
    pub is_suggestable: Option<bool>,
    /// lessThanOperatorName property.
    pub less_than_operator_name: Option<String>,
    /// objectType property.
    pub object_type: Option<String>,
    /// operatorName property.
    pub operator_name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `PushItemRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PushItemRequest {
    /// connectorName property.
    pub connector_name: Option<String>,
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
    /// item property.
    pub item: Option<PushItem>,
}

/// `DoublePropertyOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DoublePropertyOptions {
    /// operatorOptions property.
    pub operator_options: Option<DoubleOperatorOptions>,
}

/// `IndexItemOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IndexItemOptions {
    /// allowUnknownGsuitePrincipals property.
    pub allow_unknown_gsuite_principals: Option<bool>,
}

/// `SearchApplication` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchApplication {
    /// dataSourceRestrictions property.
    pub data_source_restrictions: Option<Vec<DataSourceRestriction>>,
    /// defaultFacetOptions property.
    pub default_facet_options: Option<Vec<FacetOptions>>,
    /// defaultSortOptions property.
    pub default_sort_options: Option<SortOptions>,
    /// displayName property.
    pub display_name: Option<String>,
    /// enableAuditLog property.
    pub enable_audit_log: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// operationIds property.
    pub operation_ids: Option<Vec<String>>,
    /// queryInterpretationConfig property.
    pub query_interpretation_config: Option<QueryInterpretationConfig>,
    /// returnResultThumbnailUrls property.
    pub return_result_thumbnail_urls: Option<bool>,
    /// scoringConfig property.
    pub scoring_config: Option<ScoringConfig>,
    /// sourceConfig property.
    pub source_config: Option<Vec<SourceConfig>>,
}

/// `QueryInterpretationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryInterpretationConfig {
    /// forceDisableSupplementalResults property.
    pub force_disable_supplemental_results: Option<bool>,
    /// forceVerbatimMode property.
    pub force_verbatim_mode: Option<bool>,
}

/// `BooleanOperatorOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BooleanOperatorOptions {
    /// operatorName property.
    pub operator_name: Option<String>,
}

/// `TimestampPropertyOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimestampPropertyOptions {
    /// operatorOptions property.
    pub operator_options: Option<TimestampOperatorOptions>,
}

/// `SourceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceConfig {
    /// crowdingConfig property.
    pub crowding_config: Option<SourceCrowdingConfig>,
    /// scoringConfig property.
    pub scoring_config: Option<SourceScoringConfig>,
    /// source property.
    pub source: Option<Source>,
}

/// `UnmappedIdentity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UnmappedIdentity {
    /// externalIdentity property.
    pub external_identity: Option<Principal>,
    /// resolutionStatusCode property.
    pub resolution_status_code: Option<String>,
}

/// `DoubleOperatorOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DoubleOperatorOptions {
    /// operatorName property.
    pub operator_name: Option<String>,
}

/// `QueryInterpretationOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryInterpretationOptions {
    /// disableNlInterpretation property.
    pub disable_nl_interpretation: Option<bool>,
    /// disableSupplementalResults property.
    pub disable_supplemental_results: Option<bool>,
    /// enableVerbatimMode property.
    pub enable_verbatim_mode: Option<bool>,
}

/// `EnumValues` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EnumValues {
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `EmailAddress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EmailAddress {
    /// customType property.
    pub custom_type: Option<String>,
    /// emailAddress property.
    pub email_address: Option<String>,
    /// emailUrl property.
    pub email_url: Option<String>,
    /// primary property.
    pub primary: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
}

/// `PollItemsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PollItemsResponse {
    /// items property.
    pub items: Option<Vec<Item>>,
}

/// `Person` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Person {
    /// emailAddresses property.
    pub email_addresses: Option<Vec<EmailAddress>>,
    /// name property.
    pub name: Option<String>,
    /// obfuscatedId property.
    pub obfuscated_id: Option<String>,
    /// personNames property.
    pub person_names: Option<Vec<Name>>,
    /// phoneNumbers property.
    pub phone_numbers: Option<Vec<PhoneNumber>>,
    /// photos property.
    pub photos: Option<Vec<Photo>>,
}

/// `ResultDisplayField` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResultDisplayField {
    /// label property.
    pub label: Option<String>,
    /// operatorName property.
    pub operator_name: Option<String>,
    /// property property.
    pub property: Option<Box<NamedProperty>>,
}

/// `RepositoryError` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RepositoryError {
    /// errorMessage property.
    pub error_message: Option<String>,
    /// httpStatusCode property.
    pub http_status_code: Option<i64>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GetSearchApplicationQueryStatsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetSearchApplicationQueryStatsResponse {
    /// stats property.
    pub stats: Option<Vec<SearchApplicationQueryStats>>,
    /// totalQueryCount property.
    pub total_query_count: Option<String>,
}

/// `FreshnessOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FreshnessOptions {
    /// freshnessDuration property.
    pub freshness_duration: Option<String>,
    /// freshnessProperty property.
    pub freshness_property: Option<String>,
}

/// `ItemMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ItemMetadata {
    /// containerName property.
    pub container_name: Option<String>,
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// contextAttributes property.
    pub context_attributes: Option<Vec<ContextAttribute>>,
    /// createTime property.
    pub create_time: Option<String>,
    /// hash property.
    pub hash: Option<String>,
    /// interactions property.
    pub interactions: Option<Vec<Interaction>>,
    /// keywords property.
    pub keywords: Option<Vec<String>>,
    /// mimeType property.
    pub mime_type: Option<String>,
    /// objectType property.
    pub object_type: Option<String>,
    /// searchQualityMetadata property.
    pub search_quality_metadata: Option<SearchQualityMetadata>,
    /// sourceRepositoryUrl property.
    pub source_repository_url: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `SpellResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpellResult {
    /// suggestedQuery property.
    pub suggested_query: Option<String>,
    /// suggestedQueryHtml property.
    pub suggested_query_html: Option<SafeHtmlProto>,
    /// suggestionType property.
    pub suggestion_type: Option<String>,
}

/// `StructuredDataObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StructuredDataObject {
    /// properties property.
    pub properties: Option<Vec<Box<NamedProperty>>>,
}

/// `TextPropertyOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TextPropertyOptions {
    /// operatorOptions property.
    pub operator_options: Option<TextOperatorOptions>,
    /// retrievalImportance property.
    pub retrieval_importance: Option<RetrievalImportance>,
}

/// `SearchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchResponse {
    /// debugInfo property.
    pub debug_info: Option<ResponseDebugInfo>,
    /// errorInfo property.
    pub error_info: Option<ErrorInfo>,
    /// facetResults property.
    pub facet_results: Option<Vec<FacetResult>>,
    /// hasMoreResults property.
    pub has_more_results: Option<bool>,
    /// queryInterpretation property.
    pub query_interpretation: Option<QueryInterpretation>,
    /// resultCountEstimate property.
    pub result_count_estimate: Option<String>,
    /// resultCountExact property.
    pub result_count_exact: Option<String>,
    /// resultCounts property.
    pub result_counts: Option<ResultCounts>,
    /// results property.
    pub results: Option<Vec<Box<SearchResult>>>,
    /// spellResults property.
    pub spell_results: Option<Vec<SpellResult>>,
    /// structuredResults property.
    pub structured_results: Option<Vec<StructuredResult>>,
}

/// `ResultDisplayMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResultDisplayMetadata {
    /// metalines property.
    pub metalines: Option<Vec<ResultDisplayLine>>,
    /// objectTypeLabel property.
    pub object_type_label: Option<String>,
}

/// `QuerySuggestion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QuerySuggestion {
    /// lastQueryTime property.
    pub last_query_time: Option<String>,
    /// sourceCorpus property.
    pub source_corpus: Option<String>,
}

/// `GetCustomerUserStatsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetCustomerUserStatsResponse {
    /// stats property.
    pub stats: Option<Vec<CustomerUserStats>>,
}

/// `ValueFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ValueFilter {
    /// operatorName property.
    pub operator_name: Option<String>,
    /// value property.
    pub value: Option<Value>,
}

/// `DatePropertyOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatePropertyOptions {
    /// operatorOptions property.
    pub operator_options: Option<DateOperatorOptions>,
}

/// `StructuredResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StructuredResult {
    /// person property.
    pub person: Option<Person>,
}

/// `Source` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Source {
    /// name property.
    pub name: Option<String>,
    /// predefinedSource property.
    pub predefined_source: Option<String>,
}

/// `TextOperatorOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TextOperatorOptions {
    /// exactMatchWithOperator property.
    pub exact_match_with_operator: Option<bool>,
    /// operatorName property.
    pub operator_name: Option<String>,
}

/// `DataSourceIndexStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataSourceIndexStats {
    /// date property.
    pub date: Option<Date>,
    /// itemCountByStatus property.
    pub item_count_by_status: Option<Vec<ItemCountByStatus>>,
}

/// `DebugOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DebugOptions {
    /// enableDebugging property.
    pub enable_debugging: Option<bool>,
}

/// `CustomerSearchApplicationStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomerSearchApplicationStats {
    /// count property.
    pub count: Option<String>,
    /// date property.
    pub date: Option<Date>,
}

/// `NamedProperty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NamedProperty {
    /// booleanValue property.
    pub boolean_value: Option<bool>,
    /// dateValues property.
    pub date_values: Option<DateValues>,
    /// doubleValues property.
    pub double_values: Option<DoubleValues>,
    /// enumValues property.
    pub enum_values: Option<EnumValues>,
    /// htmlValues property.
    pub html_values: Option<HtmlValues>,
    /// integerValues property.
    pub integer_values: Option<IntegerValues>,
    /// name property.
    pub name: Option<String>,
    /// objectValues property.
    pub object_values: Option<Box<ObjectValues>>,
    /// textValues property.
    pub text_values: Option<TextValues>,
    /// timestampValues property.
    pub timestamp_values: Option<TimestampValues>,
}

/// `Filter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Filter {
    /// compositeFilter property.
    pub composite_filter: Option<Box<CompositeFilter>>,
    /// valueFilter property.
    pub value_filter: Option<ValueFilter>,
}

/// `GSuitePrincipal` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GSuitePrincipal {
    /// gsuiteDomain property.
    pub gsuite_domain: Option<bool>,
    /// gsuiteGroupEmail property.
    pub gsuite_group_email: Option<String>,
    /// gsuiteUserEmail property.
    pub gsuite_user_email: Option<String>,
}

/// `Value` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Value {
    /// booleanValue property.
    pub boolean_value: Option<bool>,
    /// dateValue property.
    pub date_value: Option<Date>,
    /// doubleValue property.
    pub double_value: Option<f64>,
    /// integerValue property.
    pub integer_value: Option<String>,
    /// stringValue property.
    pub string_value: Option<String>,
    /// timestampValue property.
    pub timestamp_value: Option<String>,
}

/// `ListItemNamesForUnmappedIdentityResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListItemNamesForUnmappedIdentityResponse {
    /// itemNames property.
    pub item_names: Option<Vec<String>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Interaction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Interaction {
    /// interactionTime property.
    pub interaction_time: Option<String>,
    /// principal property.
    pub principal: Option<Principal>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `DisplayedProperty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DisplayedProperty {
    /// propertyName property.
    pub property_name: Option<String>,
}

/// `UploadItemRef` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UploadItemRef {
    /// name property.
    pub name: Option<String>,
}

/// `SuggestRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuggestRequest {
    /// dataSourceRestrictions property.
    pub data_source_restrictions: Option<Vec<DataSourceRestriction>>,
    /// query property.
    pub query: Option<String>,
    /// requestOptions property.
    pub request_options: Option<RequestOptions>,
}

/// `QuerySource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QuerySource {
    /// displayName property.
    pub display_name: Option<String>,
    /// operators property.
    pub operators: Option<Vec<QueryOperator>>,
    /// shortName property.
    pub short_name: Option<String>,
    /// source property.
    pub source: Option<Source>,
}

/// `ListItemsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListItemsResponse {
    /// items property.
    pub items: Option<Vec<Item>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `DataSourceRestriction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataSourceRestriction {
    /// filterOptions property.
    pub filter_options: Option<Vec<FilterOptions>>,
    /// source property.
    pub source: Option<Source>,
}

/// `PropertyDefinition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PropertyDefinition {
    /// booleanPropertyOptions property.
    pub boolean_property_options: Option<BooleanPropertyOptions>,
    /// datePropertyOptions property.
    pub date_property_options: Option<DatePropertyOptions>,
    /// displayOptions property.
    pub display_options: Option<PropertyDisplayOptions>,
    /// doublePropertyOptions property.
    pub double_property_options: Option<DoublePropertyOptions>,
    /// enumPropertyOptions property.
    pub enum_property_options: Option<EnumPropertyOptions>,
    /// htmlPropertyOptions property.
    pub html_property_options: Option<HtmlPropertyOptions>,
    /// integerPropertyOptions property.
    pub integer_property_options: Option<IntegerPropertyOptions>,
    /// isFacetable property.
    pub is_facetable: Option<bool>,
    /// isRepeatable property.
    pub is_repeatable: Option<bool>,
    /// isReturnable property.
    pub is_returnable: Option<bool>,
    /// isSortable property.
    pub is_sortable: Option<bool>,
    /// isSuggestable property.
    pub is_suggestable: Option<bool>,
    /// isWildcardSearchable property.
    pub is_wildcard_searchable: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// objectPropertyOptions property.
    pub object_property_options: Option<Box<ObjectPropertyOptions>>,
    /// textPropertyOptions property.
    pub text_property_options: Option<TextPropertyOptions>,
    /// timestampPropertyOptions property.
    pub timestamp_property_options: Option<TimestampPropertyOptions>,
}

/// `CustomerQueryStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomerQueryStats {
    /// date property.
    pub date: Option<Date>,
    /// queryCountByStatus property.
    pub query_count_by_status: Option<Vec<QueryCountByStatus>>,
}

/// `QueryInterpretation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryInterpretation {
    /// interpretationType property.
    pub interpretation_type: Option<String>,
    /// interpretedQuery property.
    pub interpreted_query: Option<String>,
    /// interpretedQueryActualResultCount property.
    pub interpreted_query_actual_result_count: Option<i64>,
    /// interpretedQueryEstimatedResultCount property.
    pub interpreted_query_estimated_result_count: Option<String>,
    /// reason property.
    pub reason: Option<String>,
}

/// `ErrorInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ErrorInfo {
    /// errorMessages property.
    pub error_messages: Option<Vec<ErrorMessage>>,
}

/// `FacetBucket` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FacetBucket {
    /// count property.
    pub count: Option<i64>,
    /// filter property.
    pub filter: Option<Box<Filter>>,
    /// percentage property.
    pub percentage: Option<i64>,
    /// value property.
    pub value: Option<Value>,
}

/// `SearchItemsByViewUrlRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchItemsByViewUrlRequest {
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// viewUrl property.
    pub view_url: Option<String>,
}

/// `HtmlPropertyOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HtmlPropertyOptions {
    /// operatorOptions property.
    pub operator_options: Option<HtmlOperatorOptions>,
    /// retrievalImportance property.
    pub retrieval_importance: Option<RetrievalImportance>,
}

/// `IntegerOperatorOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntegerOperatorOptions {
    /// greaterThanOperatorName property.
    pub greater_than_operator_name: Option<String>,
    /// lessThanOperatorName property.
    pub less_than_operator_name: Option<String>,
    /// operatorName property.
    pub operator_name: Option<String>,
}

/// `GetCustomerIndexStatsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetCustomerIndexStatsResponse {
    /// averageIndexedItemCount property.
    pub average_indexed_item_count: Option<String>,
    /// stats property.
    pub stats: Option<Vec<CustomerIndexStats>>,
}

/// `ResultDebugInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResultDebugInfo {
    /// formattedDebugInfo property.
    pub formatted_debug_info: Option<String>,
}

/// `ObjectOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObjectOptions {
    /// displayOptions property.
    pub display_options: Option<ObjectDisplayOptions>,
    /// freshnessOptions property.
    pub freshness_options: Option<FreshnessOptions>,
    /// suggestionFilteringOperators property.
    pub suggestion_filtering_operators: Option<Vec<String>>,
}

/// `QueryActivity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryActivity {
    /// query property.
    pub query: Option<String>,
}

/// `SearchApplicationSessionStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchApplicationSessionStats {
    /// date property.
    pub date: Option<Date>,
    /// searchSessionsCount property.
    pub search_sessions_count: Option<String>,
}

/// `ItemCountByStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ItemCountByStatus {
    /// count property.
    pub count: Option<String>,
    /// indexedItemsCount property.
    pub indexed_items_count: Option<String>,
    /// statusCode property.
    pub status_code: Option<String>,
}

/// `ObjectDisplayOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObjectDisplayOptions {
    /// metalines property.
    pub metalines: Option<Vec<Metaline>>,
    /// objectDisplayLabel property.
    pub object_display_label: Option<String>,
}

/// `Snippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Snippet {
    /// matchRanges property.
    pub match_ranges: Option<Vec<MatchRange>>,
    /// snippet property.
    pub snippet: Option<String>,
}

/// `InitializeCustomerRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InitializeCustomerRequest {}

/// `IntegerPropertyOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntegerPropertyOptions {
    /// integerFacetingOptions property.
    pub integer_faceting_options: Option<IntegerFacetingOptions>,
    /// maximumValue property.
    pub maximum_value: Option<String>,
    /// minimumValue property.
    pub minimum_value: Option<String>,
    /// operatorOptions property.
    pub operator_options: Option<IntegerOperatorOptions>,
    /// orderedRanking property.
    pub ordered_ranking: Option<String>,
}

/// `ItemContent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ItemContent {
    /// contentDataRef property.
    pub content_data_ref: Option<UploadItemRef>,
    /// contentFormat property.
    pub content_format: Option<String>,
    /// hash property.
    pub hash: Option<String>,
    /// inlineContent property.
    pub inline_content: Option<String>,
}

/// `DeleteQueueItemsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeleteQueueItemsRequest {
    /// connectorName property.
    pub connector_name: Option<String>,
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
    /// queue property.
    pub queue: Option<String>,
}

/// `RemoveActivityResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemoveActivityResponse {}

/// `GetDataSourceIndexStatsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetDataSourceIndexStatsResponse {
    /// averageIndexedItemCount property.
    pub average_indexed_item_count: Option<String>,
    /// stats property.
    pub stats: Option<Vec<DataSourceIndexStats>>,
}

/// `QueryCountByStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryCountByStatus {
    /// count property.
    pub count: Option<String>,
    /// statusCode property.
    pub status_code: Option<i64>,
}

/// `HtmlValues` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HtmlValues {
    /// values property.
    pub values: Option<Vec<String>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`cloudsearch.debug.datasources.items.checkAccess_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchDebugDatasourcesItemsCheckAccessArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
    /// Request body.
    pub body: Principal,
}

/// Arguments for [`cloudsearch.debug.datasources.items.searchByViewUrl_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchDebugDatasourcesItemsSearchByViewUrlArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: SearchItemsByViewUrlRequest,
}

/// Arguments for [`cloudsearch.debug.datasources.items.unmappedids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchDebugDatasourcesItemsUnmappedidsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`cloudsearch.debug.identitysources.items.listForunmappedidentity_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchDebugIdentitysourcesItemsListForunmappedidentityArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
    /// Query parameter: `groupResourceName`.
    pub group_resource_name: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `userResourceName`.
    pub user_resource_name: Option<String>,
}

/// Arguments for [`cloudsearch.debug.identitysources.unmappedids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchDebugIdentitysourcesUnmappedidsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `resolutionStatusCode`.
    pub resolution_status_code: Option<String>,
}

/// Arguments for [`cloudsearch.indexing.datasources.deleteSchema_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesDeleteSchemaArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
}

/// Arguments for [`cloudsearch.indexing.datasources.getSchema_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesGetSchemaArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
}

/// Arguments for [`cloudsearch.indexing.datasources.updateSchema_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesUpdateSchemaArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: UpdateSchemaRequest,
}

/// Arguments for [`cloudsearch.indexing.datasources.items.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `connectorName`.
    pub connector_name: Option<String>,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
    /// Query parameter: `mode`.
    pub mode: Option<String>,
    /// Query parameter: `version`.
    pub version: Option<String>,
}

/// Arguments for [`cloudsearch.indexing.datasources.items.deleteQueueItems_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsDeleteQueueItemsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: DeleteQueueItemsRequest,
}

/// Arguments for [`cloudsearch.indexing.datasources.items.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `connectorName`.
    pub connector_name: Option<String>,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
}

/// Arguments for [`cloudsearch.indexing.datasources.items.index_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsIndexArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: IndexItemRequest,
}

/// Arguments for [`cloudsearch.indexing.datasources.items.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `brief`.
    pub brief: Option<String>,
    /// Query parameter: `connectorName`.
    pub connector_name: Option<String>,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`cloudsearch.indexing.datasources.items.poll_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsPollArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: PollItemsRequest,
}

/// Arguments for [`cloudsearch.indexing.datasources.items.push_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsPushArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: PushItemRequest,
}

/// Arguments for [`cloudsearch.indexing.datasources.items.unreserve_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsUnreserveArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: UnreserveItemsRequest,
}

/// Arguments for [`cloudsearch.indexing.datasources.items.upload_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchIndexingDatasourcesItemsUploadArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: StartUploadItemRequest,
}

/// Arguments for [`cloudsearch.media.upload_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchMediaUploadArgs {
    /// Path parameter: `resourceName`.
    pub resource_name: String,
    /// Request body.
    pub body: Media,
}

/// Arguments for [`cloudsearch.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`cloudsearch.operations.lro.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchOperationsLroListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`cloudsearch.query.removeActivity_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchQueryRemoveActivityArgs {
    /// Request body.
    pub body: RemoveActivityRequest,
}

/// Arguments for [`cloudsearch.query.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchQuerySearchArgs {
    /// Request body.
    pub body: SearchRequest,
}

/// Arguments for [`cloudsearch.query.suggest_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchQuerySuggestArgs {
    /// Request body.
    pub body: SuggestRequest,
}

/// Arguments for [`cloudsearch.query.sources.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchQuerySourcesListArgs {
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `requestOptions.debugOptions.enableDebugging`.
    pub request_options_debug_options_enable_debugging: Option<String>,
    /// Query parameter: `requestOptions.languageCode`.
    pub request_options_language_code: Option<String>,
    /// Query parameter: `requestOptions.searchApplicationId`.
    pub request_options_search_application_id: Option<String>,
    /// Query parameter: `requestOptions.timeZone`.
    pub request_options_time_zone: Option<String>,
}

/// Arguments for [`cloudsearch.settings.updateCustomer_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsUpdateCustomerArgs {
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: CustomerSettings,
}

/// Arguments for [`cloudsearch.settings.datasources.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsDatasourcesCreateArgs {
    /// Request body.
    pub body: DataSource,
}

/// Arguments for [`cloudsearch.settings.datasources.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsDatasourcesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
}

/// Arguments for [`cloudsearch.settings.datasources.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsDatasourcesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
}

/// Arguments for [`cloudsearch.settings.datasources.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsDatasourcesListArgs {
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`cloudsearch.settings.datasources.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsDatasourcesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: DataSource,
}

/// Arguments for [`cloudsearch.settings.datasources.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsDatasourcesUpdateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: UpdateDataSourceRequest,
}

/// Arguments for [`cloudsearch.settings.searchapplications.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsCreateArgs {
    /// Request body.
    pub body: SearchApplication,
}

/// Arguments for [`cloudsearch.settings.searchapplications.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
}

/// Arguments for [`cloudsearch.settings.searchapplications.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
}

/// Arguments for [`cloudsearch.settings.searchapplications.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsListArgs {
    /// Query parameter: `debugOptions.enableDebugging`.
    pub debug_options_enable_debugging: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`cloudsearch.settings.searchapplications.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: SearchApplication,
}

/// Arguments for [`cloudsearch.settings.searchapplications.reset_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsResetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: ResetSearchApplicationRequest,
}

/// Arguments for [`cloudsearch.settings.searchapplications.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchSettingsSearchapplicationsUpdateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: SearchApplication,
}

/// Arguments for [`cloudsearch.stats.getIndex_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchStatsGetIndexArgs {
    /// Query parameter: `fromDate.day`.
    pub from_date_day: Option<String>,
    /// Query parameter: `fromDate.month`.
    pub from_date_month: Option<String>,
    /// Query parameter: `fromDate.year`.
    pub from_date_year: Option<String>,
    /// Query parameter: `toDate.day`.
    pub to_date_day: Option<String>,
    /// Query parameter: `toDate.month`.
    pub to_date_month: Option<String>,
    /// Query parameter: `toDate.year`.
    pub to_date_year: Option<String>,
}

/// Arguments for [`cloudsearch.stats.getQuery_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchStatsGetQueryArgs {
    /// Query parameter: `fromDate.day`.
    pub from_date_day: Option<String>,
    /// Query parameter: `fromDate.month`.
    pub from_date_month: Option<String>,
    /// Query parameter: `fromDate.year`.
    pub from_date_year: Option<String>,
    /// Query parameter: `toDate.day`.
    pub to_date_day: Option<String>,
    /// Query parameter: `toDate.month`.
    pub to_date_month: Option<String>,
    /// Query parameter: `toDate.year`.
    pub to_date_year: Option<String>,
}

/// Arguments for [`cloudsearch.stats.getSearchapplication_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchStatsGetSearchapplicationArgs {
    /// Query parameter: `endDate.day`.
    pub end_date_day: Option<String>,
    /// Query parameter: `endDate.month`.
    pub end_date_month: Option<String>,
    /// Query parameter: `endDate.year`.
    pub end_date_year: Option<String>,
    /// Query parameter: `startDate.day`.
    pub start_date_day: Option<String>,
    /// Query parameter: `startDate.month`.
    pub start_date_month: Option<String>,
    /// Query parameter: `startDate.year`.
    pub start_date_year: Option<String>,
}

/// Arguments for [`cloudsearch.stats.getSession_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchStatsGetSessionArgs {
    /// Query parameter: `fromDate.day`.
    pub from_date_day: Option<String>,
    /// Query parameter: `fromDate.month`.
    pub from_date_month: Option<String>,
    /// Query parameter: `fromDate.year`.
    pub from_date_year: Option<String>,
    /// Query parameter: `toDate.day`.
    pub to_date_day: Option<String>,
    /// Query parameter: `toDate.month`.
    pub to_date_month: Option<String>,
    /// Query parameter: `toDate.year`.
    pub to_date_year: Option<String>,
}

/// Arguments for [`cloudsearch.stats.getUser_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchStatsGetUserArgs {
    /// Query parameter: `fromDate.day`.
    pub from_date_day: Option<String>,
    /// Query parameter: `fromDate.month`.
    pub from_date_month: Option<String>,
    /// Query parameter: `fromDate.year`.
    pub from_date_year: Option<String>,
    /// Query parameter: `toDate.day`.
    pub to_date_day: Option<String>,
    /// Query parameter: `toDate.month`.
    pub to_date_month: Option<String>,
    /// Query parameter: `toDate.year`.
    pub to_date_year: Option<String>,
}

/// Arguments for [`cloudsearch.stats.index.datasources.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchStatsIndexDatasourcesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `fromDate.day`.
    pub from_date_day: Option<String>,
    /// Query parameter: `fromDate.month`.
    pub from_date_month: Option<String>,
    /// Query parameter: `fromDate.year`.
    pub from_date_year: Option<String>,
    /// Query parameter: `toDate.day`.
    pub to_date_day: Option<String>,
    /// Query parameter: `toDate.month`.
    pub to_date_month: Option<String>,
    /// Query parameter: `toDate.year`.
    pub to_date_year: Option<String>,
}

/// Arguments for [`cloudsearch.stats.query.searchapplications.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchStatsQuerySearchapplicationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `fromDate.day`.
    pub from_date_day: Option<String>,
    /// Query parameter: `fromDate.month`.
    pub from_date_month: Option<String>,
    /// Query parameter: `fromDate.year`.
    pub from_date_year: Option<String>,
    /// Query parameter: `toDate.day`.
    pub to_date_day: Option<String>,
    /// Query parameter: `toDate.month`.
    pub to_date_month: Option<String>,
    /// Query parameter: `toDate.year`.
    pub to_date_year: Option<String>,
}

/// Arguments for [`cloudsearch.stats.session.searchapplications.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchStatsSessionSearchapplicationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `fromDate.day`.
    pub from_date_day: Option<String>,
    /// Query parameter: `fromDate.month`.
    pub from_date_month: Option<String>,
    /// Query parameter: `fromDate.year`.
    pub from_date_year: Option<String>,
    /// Query parameter: `toDate.day`.
    pub to_date_day: Option<String>,
    /// Query parameter: `toDate.month`.
    pub to_date_month: Option<String>,
    /// Query parameter: `toDate.year`.
    pub to_date_year: Option<String>,
}

/// Arguments for [`cloudsearch.stats.user.searchapplications.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchStatsUserSearchapplicationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `fromDate.day`.
    pub from_date_day: Option<String>,
    /// Query parameter: `fromDate.month`.
    pub from_date_month: Option<String>,
    /// Query parameter: `fromDate.year`.
    pub from_date_year: Option<String>,
    /// Query parameter: `toDate.day`.
    pub to_date_day: Option<String>,
    /// Query parameter: `toDate.month`.
    pub to_date_month: Option<String>,
    /// Query parameter: `toDate.year`.
    pub to_date_year: Option<String>,
}

/// Arguments for [`cloudsearch.initializeCustomer_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudsearchInitializeCustomerArgs {
    /// Request body.
    pub body: InitializeCustomerRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/debug/{+name}:checkAccess
// -----------------------------------------------------------------------------

/// POST v1/debug/{+name}:checkAccess.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_debug_datasources_items_check_access_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_debug_datasources_items_check_access_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchDebugDatasourcesItemsCheckAccessArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckAccessResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/debug/{}:checkAccess",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CheckAccessResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/debug/{+name}/items:searchByViewUrl
// -----------------------------------------------------------------------------

/// POST v1/debug/{+name}/items:searchByViewUrl.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_debug_datasources_items_search_by_view_url_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_debug_datasources_items_search_by_view_url_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchDebugDatasourcesItemsSearchByViewUrlArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchItemsByViewUrlResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/debug/{}/items:searchByViewUrl",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchItemsByViewUrlResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/debug/{+parent}/unmappedids
// -----------------------------------------------------------------------------

/// GET v1/debug/{+parent}/unmappedids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_debug_datasources_items_unmappedids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_debug_datasources_items_unmappedids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchDebugDatasourcesItemsUnmappedidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUnmappedIdentitiesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/debug/{}/unmappedids",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListUnmappedIdentitiesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/debug/{+parent}/items:forunmappedidentity
// -----------------------------------------------------------------------------

/// GET v1/debug/{+parent}/items:forunmappedidentity.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_debug_identitysources_items_list_forunmappedidentity_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_debug_identitysources_items_list_forunmappedidentity_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchDebugIdentitysourcesItemsListForunmappedidentityArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListItemNamesForUnmappedIdentityResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/debug/{}/items:forunmappedidentity",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.group_resource_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("groupResourceName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_resource_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("userResourceName=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListItemNamesForUnmappedIdentityResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/debug/{+parent}/unmappedids
// -----------------------------------------------------------------------------

/// GET v1/debug/{+parent}/unmappedids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_debug_identitysources_unmappedids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_debug_identitysources_unmappedids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchDebugIdentitysourcesUnmappedidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUnmappedIdentitiesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/debug/{}/unmappedids",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.resolution_status_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("resolutionStatusCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListUnmappedIdentitiesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/indexing/{+name}/schema
// -----------------------------------------------------------------------------

/// DELETE v1/indexing/{+name}/schema.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_delete_schema_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_delete_schema_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesDeleteSchemaArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}/schema",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/indexing/{+name}/schema
// -----------------------------------------------------------------------------

/// GET v1/indexing/{+name}/schema.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_get_schema_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_get_schema_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesGetSchemaArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Schema>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}/schema",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Schema =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/indexing/{+name}/schema
// -----------------------------------------------------------------------------

/// PUT v1/indexing/{+name}/schema.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_update_schema_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_update_schema_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesUpdateSchemaArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}/schema",
        args.name,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/indexing/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/indexing/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_items_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_items_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesItemsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.connector_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("connectorName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.mode {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("version=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/indexing/{+name}/items:deleteQueueItems
// -----------------------------------------------------------------------------

/// POST v1/indexing/{+name}/items:deleteQueueItems.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_items_delete_queue_items_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_items_delete_queue_items_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesItemsDeleteQueueItemsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}/items:deleteQueueItems",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/indexing/{+name}
// -----------------------------------------------------------------------------

/// GET v1/indexing/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_items_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_items_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesItemsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Item>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.connector_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("connectorName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Item =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/indexing/{+name}:index
// -----------------------------------------------------------------------------

/// POST v1/indexing/{+name}:index.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_items_index_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_items_index_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesItemsIndexArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}:index",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/indexing/{+name}/items
// -----------------------------------------------------------------------------

/// GET v1/indexing/{+name}/items.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_items_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_items_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesItemsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListItemsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}/items",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.brief {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("brief=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.connector_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("connectorName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListItemsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/indexing/{+name}/items:poll
// -----------------------------------------------------------------------------

/// POST v1/indexing/{+name}/items:poll.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_items_poll_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_items_poll_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesItemsPollArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PollItemsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}/items:poll",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PollItemsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/indexing/{+name}:push
// -----------------------------------------------------------------------------

/// POST v1/indexing/{+name}:push.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_items_push_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_items_push_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesItemsPushArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Item>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}:push",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Item =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/indexing/{+name}/items:unreserve
// -----------------------------------------------------------------------------

/// POST v1/indexing/{+name}/items:unreserve.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_items_unreserve_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_items_unreserve_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesItemsUnreserveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}/items:unreserve",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/indexing/{+name}:upload
// -----------------------------------------------------------------------------

/// POST v1/indexing/{+name}:upload.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_indexing_datasources_items_upload_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_indexing_datasources_items_upload_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchIndexingDatasourcesItemsUploadArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UploadItemRef>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/indexing/{}:upload",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UploadItemRef =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/media/{+resourceName}
// -----------------------------------------------------------------------------

/// POST v1/media/{+resourceName}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_media_upload_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_media_upload_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchMediaUploadArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Media>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/media/{}",
        args.resource_name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Media =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/lro
// -----------------------------------------------------------------------------

/// GET v1/{+name}/lro.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_operations_lro_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_operations_lro_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchOperationsLroListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/{}/lro", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOperationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/query:removeActivity
// -----------------------------------------------------------------------------

/// POST v1/query:removeActivity.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_query_remove_activity_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_query_remove_activity_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchQueryRemoveActivityArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemoveActivityResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/query:removeActivity",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RemoveActivityResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/query/search
// -----------------------------------------------------------------------------

/// POST v1/query/search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_query_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_query_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchQuerySearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/query/search",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/query/suggest
// -----------------------------------------------------------------------------

/// POST v1/query/suggest.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_query_suggest_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_query_suggest_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchQuerySuggestArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SuggestResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/query/suggest",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SuggestResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/query/sources
// -----------------------------------------------------------------------------

/// GET v1/query/sources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_query_sources_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_query_sources_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchQuerySourcesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListQuerySourcesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/query/sources",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_options_debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestOptions_debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_options_language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestOptions_languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_options_search_application_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestOptions_searchApplicationId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_options_time_zone {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestOptions_timeZone=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListQuerySourcesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/settings/customer
// -----------------------------------------------------------------------------

/// GET v1/settings/customer.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_get_customer_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_get_customer_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsGetCustomerArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomerSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/settings/customer",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomerSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/settings/customer
// -----------------------------------------------------------------------------

/// PATCH v1/settings/customer.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_update_customer_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_update_customer_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsUpdateCustomerArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/settings/customer",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/settings/datasources
// -----------------------------------------------------------------------------

/// POST v1/settings/datasources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_datasources_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_datasources_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsDatasourcesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/settings/datasources",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/settings/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/settings/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_datasources_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_datasources_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsDatasourcesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/settings/{+name}
// -----------------------------------------------------------------------------

/// GET v1/settings/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_datasources_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_datasources_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsDatasourcesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataSource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DataSource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/settings/datasources
// -----------------------------------------------------------------------------

/// GET v1/settings/datasources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_datasources_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_datasources_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsDatasourcesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataSourceResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/settings/datasources",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDataSourceResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/settings/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/settings/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_datasources_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_datasources_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsDatasourcesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/settings/{+name}
// -----------------------------------------------------------------------------

/// PUT v1/settings/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_datasources_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_datasources_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsDatasourcesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/{}",
        args.name,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/settings/searchapplications
// -----------------------------------------------------------------------------

/// POST v1/settings/searchapplications.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_searchapplications_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_searchapplications_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsSearchapplicationsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://cloudsearch.googleapis.com/v1/settings/searchapplications",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/settings/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/settings/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_searchapplications_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_searchapplications_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsSearchapplicationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/settings/{+name}
// -----------------------------------------------------------------------------

/// GET v1/settings/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_searchapplications_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_searchapplications_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsSearchapplicationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchApplication>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchApplication =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/settings/searchapplications
// -----------------------------------------------------------------------------

/// GET v1/settings/searchapplications.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_searchapplications_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_searchapplications_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsSearchapplicationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSearchApplicationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://cloudsearch.googleapis.com/v1/settings/searchapplications",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.debug_options_enable_debugging {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("debugOptions_enableDebugging=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListSearchApplicationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/settings/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/settings/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_searchapplications_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_searchapplications_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsSearchapplicationsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/settings/{+name}:reset
// -----------------------------------------------------------------------------

/// POST v1/settings/{+name}:reset.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_searchapplications_reset_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_searchapplications_reset_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsSearchapplicationsResetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/{}:reset",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/settings/{+name}
// -----------------------------------------------------------------------------

/// PUT v1/settings/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_settings_searchapplications_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_settings_searchapplications_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchSettingsSearchapplicationsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/settings/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/stats/index
// -----------------------------------------------------------------------------

/// GET v1/stats/index.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_stats_get_index_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_stats_get_index_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchStatsGetIndexArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetCustomerIndexStatsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/stats/index",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.from_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetCustomerIndexStatsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/stats/query
// -----------------------------------------------------------------------------

/// GET v1/stats/query.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_stats_get_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_stats_get_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchStatsGetQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetCustomerQueryStatsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/stats/query",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.from_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetCustomerQueryStatsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/stats/searchapplication
// -----------------------------------------------------------------------------

/// GET v1/stats/searchapplication.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_stats_get_searchapplication_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_stats_get_searchapplication_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchStatsGetSearchapplicationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GetCustomerSearchApplicationStatsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/stats/searchapplication",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.end_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetCustomerSearchApplicationStatsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/stats/session
// -----------------------------------------------------------------------------

/// GET v1/stats/session.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_stats_get_session_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_stats_get_session_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchStatsGetSessionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetCustomerSessionStatsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/stats/session",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.from_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetCustomerSessionStatsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/stats/user
// -----------------------------------------------------------------------------

/// GET v1/stats/user.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_stats_get_user_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_stats_get_user_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchStatsGetUserArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetCustomerUserStatsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1/stats/user",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.from_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetCustomerUserStatsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/stats/index/{+name}
// -----------------------------------------------------------------------------

/// GET v1/stats/index/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_stats_index_datasources_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_stats_index_datasources_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchStatsIndexDatasourcesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetDataSourceIndexStatsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/stats/index/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.from_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetDataSourceIndexStatsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/stats/query/{+name}
// -----------------------------------------------------------------------------

/// GET v1/stats/query/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_stats_query_searchapplications_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_stats_query_searchapplications_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchStatsQuerySearchapplicationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GetSearchApplicationQueryStatsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/stats/query/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.from_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetSearchApplicationQueryStatsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/stats/session/{+name}
// -----------------------------------------------------------------------------

/// GET v1/stats/session/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_stats_session_searchapplications_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_stats_session_searchapplications_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchStatsSessionSearchapplicationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GetSearchApplicationSessionStatsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/stats/session/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.from_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetSearchApplicationSessionStatsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/stats/user/{+name}
// -----------------------------------------------------------------------------

/// GET v1/stats/user/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_stats_user_searchapplications_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_stats_user_searchapplications_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchStatsUserSearchapplicationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GetSearchApplicationUserStatsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudsearch.googleapis.com/v1/stats/user/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.from_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.from_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fromDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.to_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("toDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetSearchApplicationUserStatsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1:initializeCustomer
// -----------------------------------------------------------------------------

/// POST v1:initializeCustomer.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudsearch_initialize_customer_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudsearch_initialize_customer_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudsearchInitializeCustomerArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudsearch.googleapis.com/v1:initializeCustomer",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
