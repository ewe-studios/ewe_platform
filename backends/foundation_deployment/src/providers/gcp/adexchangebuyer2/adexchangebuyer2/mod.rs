//! Auto-generated API module for gcp/adexchangebuyer2 adexchangebuyer2.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_adexchangebuyer2_adexchangebuyer2 `

#![cfg(feature = "gcp_adexchangebuyer2_adexchangebuyer2")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `PauseProposalDealsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PauseProposalDealsRequest {
    /// externalDealIds property.
    pub external_deal_ids: Option<Vec<String>>,
    /// reason property.
    pub reason: Option<String>,
}

/// `RemoveDealAssociationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemoveDealAssociationRequest {
    /// association property.
    pub association: Option<CreativeDealAssociation>,
}

/// `GuaranteedFixedPriceTerms` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GuaranteedFixedPriceTerms {
    /// fixedPrices property.
    pub fixed_prices: Option<Vec<PricePerBuyer>>,
    /// guaranteedImpressions property.
    pub guaranteed_impressions: Option<String>,
    /// guaranteedLooks property.
    pub guaranteed_looks: Option<String>,
    /// impressionCap property.
    pub impression_cap: Option<String>,
    /// minimumDailyLooks property.
    pub minimum_daily_looks: Option<String>,
    /// percentShareOfVoice property.
    pub percent_share_of_voice: Option<String>,
    /// reservationType property.
    pub reservation_type: Option<String>,
}

/// `NonGuaranteedAuctionTerms` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NonGuaranteedAuctionTerms {
    /// autoOptimizePrivateAuction property.
    pub auto_optimize_private_auction: Option<bool>,
    /// reservePricesPerBuyer property.
    pub reserve_prices_per_buyer: Option<Vec<PricePerBuyer>>,
}

/// `ListBidResponsesWithoutBidsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListBidResponsesWithoutBidsResponse {
    /// bidResponseWithoutBidsStatusRows property.
    pub bid_response_without_bids_status_rows: Option<Vec<BidResponseWithoutBidsStatusRow>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `NonGuaranteedFixedPriceTerms` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NonGuaranteedFixedPriceTerms {
    /// fixedPrices property.
    pub fixed_prices: Option<Vec<PricePerBuyer>>,
}

/// `DealServingMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DealServingMetadata {
    /// dealPauseStatus property.
    pub deal_pause_status: Option<DealPauseStatus>,
}

/// `DeliveryControl` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeliveryControl {
    /// creativeBlockingLevel property.
    pub creative_blocking_level: Option<String>,
    /// deliveryRateType property.
    pub delivery_rate_type: Option<String>,
    /// frequencyCaps property.
    pub frequency_caps: Option<Vec<FrequencyCap>>,
}

/// `MobileApplicationTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MobileApplicationTargeting {
    /// firstPartyTargeting property.
    pub first_party_targeting: Option<FirstPartyMobileApplicationTargeting>,
}

/// `CalloutStatusRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CalloutStatusRow {
    /// calloutStatusId property.
    pub callout_status_id: Option<i64>,
    /// impressionCount property.
    pub impression_count: Option<MetricValue>,
    /// rowDimensions property.
    pub row_dimensions: Option<RowDimensions>,
}

/// `ImpressionMetricsRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ImpressionMetricsRow {
    /// availableImpressions property.
    pub available_impressions: Option<MetricValue>,
    /// bidRequests property.
    pub bid_requests: Option<MetricValue>,
    /// inventoryMatches property.
    pub inventory_matches: Option<MetricValue>,
    /// responsesWithBids property.
    pub responses_with_bids: Option<MetricValue>,
    /// rowDimensions property.
    pub row_dimensions: Option<RowDimensions>,
    /// successfulResponses property.
    pub successful_responses: Option<MetricValue>,
}

/// `ListFilterSetsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListFilterSetsResponse {
    /// filterSets property.
    pub filter_sets: Option<Vec<FilterSet>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `PricePerBuyer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PricePerBuyer {
    /// advertiserIds property.
    pub advertiser_ids: Option<Vec<String>>,
    /// buyer property.
    pub buyer: Option<Buyer>,
    /// price property.
    pub price: Option<Price>,
}

/// `FilteredBidDetailRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FilteredBidDetailRow {
    /// bidCount property.
    pub bid_count: Option<MetricValue>,
    /// detail property.
    pub detail: Option<String>,
    /// detailId property.
    pub detail_id: Option<i64>,
    /// rowDimensions property.
    pub row_dimensions: Option<RowDimensions>,
}

/// `AddDealAssociationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AddDealAssociationRequest {
    /// association property.
    pub association: Option<CreativeDealAssociation>,
}

/// `TimeOfDay` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeOfDay {
    /// hours property.
    pub hours: Option<i64>,
    /// minutes property.
    pub minutes: Option<i64>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<i64>,
}

/// `AdTechnologyProviders` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdTechnologyProviders {
    /// detectedProviderIds property.
    pub detected_provider_ids: Option<Vec<String>>,
    /// hasUnidentifiedProvider property.
    pub has_unidentified_provider: Option<bool>,
}

/// `UrlTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlTargeting {
    /// excludedUrls property.
    pub excluded_urls: Option<Vec<String>>,
    /// targetedUrls property.
    pub targeted_urls: Option<Vec<String>>,
}

/// `HtmlContent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HtmlContent {
    /// height property.
    pub height: Option<i64>,
    /// snippet property.
    pub snippet: Option<String>,
    /// width property.
    pub width: Option<i64>,
}

/// `NativeContent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NativeContent {
    /// advertiserName property.
    pub advertiser_name: Option<String>,
    /// appIcon property.
    pub app_icon: Option<Image>,
    /// body property.
    pub body: Option<String>,
    /// callToAction property.
    pub call_to_action: Option<String>,
    /// clickLinkUrl property.
    pub click_link_url: Option<String>,
    /// clickTrackingUrl property.
    pub click_tracking_url: Option<String>,
    /// headline property.
    pub headline: Option<String>,
    /// image property.
    pub image: Option<Image>,
    /// logo property.
    pub logo: Option<Image>,
    /// priceDisplayText property.
    pub price_display_text: Option<String>,
    /// starRating property.
    pub star_rating: Option<f64>,
    /// storeUrl property.
    pub store_url: Option<String>,
    /// videoUrl property.
    pub video_url: Option<String>,
}

/// `Client` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Client {
    /// clientAccountId property.
    pub client_account_id: Option<String>,
    /// clientName property.
    pub client_name: Option<String>,
    /// entityId property.
    pub entity_id: Option<String>,
    /// entityName property.
    pub entity_name: Option<String>,
    /// entityType property.
    pub entity_type: Option<String>,
    /// partnerClientId property.
    pub partner_client_id: Option<String>,
    /// role property.
    pub role: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// visibleToSeller property.
    pub visible_to_seller: Option<bool>,
}

/// `ServingRestriction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServingRestriction {
    /// contexts property.
    pub contexts: Option<Vec<ServingContext>>,
    /// disapproval property.
    pub disapproval: Option<Disapproval>,
    /// disapprovalReasons property.
    pub disapproval_reasons: Option<Vec<Disapproval>>,
    /// status property.
    pub status: Option<String>,
}

/// `Price` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Price {
    /// amount property.
    pub amount: Option<Money>,
    /// pricingType property.
    pub pricing_type: Option<String>,
}

/// `Disapproval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Disapproval {
    /// details property.
    pub details: Option<Vec<String>>,
    /// reason property.
    pub reason: Option<String>,
}

/// `NonBillableWinningBidStatusRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NonBillableWinningBidStatusRow {
    /// bidCount property.
    pub bid_count: Option<MetricValue>,
    /// rowDimensions property.
    pub row_dimensions: Option<RowDimensions>,
    /// status property.
    pub status: Option<String>,
}

/// `Creative` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Creative {
    /// accountId property.
    pub account_id: Option<String>,
    /// adChoicesDestinationUrl property.
    pub ad_choices_destination_url: Option<String>,
    /// adTechnologyProviders property.
    pub ad_technology_providers: Option<AdTechnologyProviders>,
    /// advertiserName property.
    pub advertiser_name: Option<String>,
    /// agencyId property.
    pub agency_id: Option<String>,
    /// apiUpdateTime property.
    pub api_update_time: Option<String>,
    /// attributes property.
    pub attributes: Option<Vec<String>>,
    /// clickThroughUrls property.
    pub click_through_urls: Option<Vec<String>>,
    /// corrections property.
    pub corrections: Option<Vec<Correction>>,
    /// creativeId property.
    pub creative_id: Option<String>,
    /// dealsStatus property.
    pub deals_status: Option<String>,
    /// declaredClickThroughUrls property.
    pub declared_click_through_urls: Option<Vec<String>>,
    /// detectedAdvertiserIds property.
    pub detected_advertiser_ids: Option<Vec<String>>,
    /// detectedDomains property.
    pub detected_domains: Option<Vec<String>>,
    /// detectedLanguages property.
    pub detected_languages: Option<Vec<String>>,
    /// detectedProductCategories property.
    pub detected_product_categories: Option<Vec<i64>>,
    /// detectedSensitiveCategories property.
    pub detected_sensitive_categories: Option<Vec<i64>>,
    /// html property.
    pub html: Option<HtmlContent>,
    /// impressionTrackingUrls property.
    pub impression_tracking_urls: Option<Vec<String>>,
    /// native property.
    pub native: Option<NativeContent>,
    /// openAuctionStatus property.
    pub open_auction_status: Option<String>,
    /// restrictedCategories property.
    pub restricted_categories: Option<Vec<String>>,
    /// servingRestrictions property.
    pub serving_restrictions: Option<Vec<ServingRestriction>>,
    /// vendorIds property.
    pub vendor_ids: Option<Vec<i64>>,
    /// version property.
    pub version: Option<i64>,
    /// video property.
    pub video: Option<VideoContent>,
}

/// `ListImpressionMetricsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListImpressionMetricsResponse {
    /// impressionMetricsRows property.
    pub impression_metrics_rows: Option<Vec<ImpressionMetricsRow>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `CancelNegotiationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelNegotiationRequest {}

/// `ClientUserInvitation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClientUserInvitation {
    /// clientAccountId property.
    pub client_account_id: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// invitationId property.
    pub invitation_id: Option<String>,
}

/// `TimeInterval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeInterval {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `Buyer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Buyer {
    /// accountId property.
    pub account_id: Option<String>,
}

/// `TechnologyTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TechnologyTargeting {
    /// deviceCapabilityTargeting property.
    pub device_capability_targeting: Option<CriteriaTargeting>,
    /// deviceCategoryTargeting property.
    pub device_category_targeting: Option<CriteriaTargeting>,
    /// operatingSystemTargeting property.
    pub operating_system_targeting: Option<OperatingSystemTargeting>,
}

/// `VideoTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoTargeting {
    /// excludedPositionTypes property.
    pub excluded_position_types: Option<Vec<String>>,
    /// targetedPositionTypes property.
    pub targeted_position_types: Option<Vec<String>>,
}

/// `AuctionContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuctionContext {
    /// auctionTypes property.
    pub auction_types: Option<Vec<String>>,
}

/// `ListDealAssociationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListDealAssociationsResponse {
    /// associations property.
    pub associations: Option<Vec<CreativeDealAssociation>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `PublisherProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PublisherProfile {
    /// audienceDescription property.
    pub audience_description: Option<String>,
    /// buyerPitchStatement property.
    pub buyer_pitch_statement: Option<String>,
    /// directDealsContact property.
    pub direct_deals_contact: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// domains property.
    pub domains: Option<Vec<String>>,
    /// googlePlusUrl property.
    pub google_plus_url: Option<String>,
    /// isParent property.
    pub is_parent: Option<bool>,
    /// logoUrl property.
    pub logo_url: Option<String>,
    /// mediaKitUrl property.
    pub media_kit_url: Option<String>,
    /// mobileApps property.
    pub mobile_apps: Option<Vec<PublisherProfileMobileApplication>>,
    /// overview property.
    pub overview: Option<String>,
    /// programmaticDealsContact property.
    pub programmatic_deals_contact: Option<String>,
    /// publisherProfileId property.
    pub publisher_profile_id: Option<String>,
    /// rateCardInfoUrl property.
    pub rate_card_info_url: Option<String>,
    /// samplePageUrl property.
    pub sample_page_url: Option<String>,
    /// seller property.
    pub seller: Option<Seller>,
    /// topHeadlines property.
    pub top_headlines: Option<Vec<String>>,
}

/// `ListFilteredBidRequestsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListFilteredBidRequestsResponse {
    /// calloutStatusRows property.
    pub callout_status_rows: Option<Vec<CalloutStatusRow>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ListBidResponseErrorsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListBidResponseErrorsResponse {
    /// calloutStatusRows property.
    pub callout_status_rows: Option<Vec<CalloutStatusRow>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ListLosingBidsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListLosingBidsResponse {
    /// creativeStatusRows property.
    pub creative_status_rows: Option<Vec<CreativeStatusRow>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ListBidMetricsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListBidMetricsResponse {
    /// bidMetricsRows property.
    pub bid_metrics_rows: Option<Vec<BidMetricsRow>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `CreativeSize` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreativeSize {
    /// allowedFormats property.
    pub allowed_formats: Option<Vec<String>>,
    /// companionSizes property.
    pub companion_sizes: Option<Vec<Size>>,
    /// creativeSizeType property.
    pub creative_size_type: Option<String>,
    /// nativeTemplate property.
    pub native_template: Option<String>,
    /// size property.
    pub size: Option<Size>,
    /// skippableAdType property.
    pub skippable_ad_type: Option<String>,
}

/// `CreativeSpecification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreativeSpecification {
    /// creativeCompanionSizes property.
    pub creative_companion_sizes: Option<Vec<AdSize>>,
    /// creativeSize property.
    pub creative_size: Option<AdSize>,
}

/// `CriteriaTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CriteriaTargeting {
    /// excludedCriteriaIds property.
    pub excluded_criteria_ids: Option<Vec<String>>,
    /// targetedCriteriaIds property.
    pub targeted_criteria_ids: Option<Vec<String>>,
}

/// `FilteredBidCreativeRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FilteredBidCreativeRow {
    /// bidCount property.
    pub bid_count: Option<MetricValue>,
    /// creativeId property.
    pub creative_id: Option<String>,
    /// rowDimensions property.
    pub row_dimensions: Option<RowDimensions>,
}

/// `AppContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppContext {
    /// appTypes property.
    pub app_types: Option<Vec<String>>,
}

/// `BidResponseWithoutBidsStatusRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BidResponseWithoutBidsStatusRow {
    /// impressionCount property.
    pub impression_count: Option<MetricValue>,
    /// rowDimensions property.
    pub row_dimensions: Option<RowDimensions>,
    /// status property.
    pub status: Option<String>,
}

/// `FrequencyCap` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FrequencyCap {
    /// maxImpressions property.
    pub max_impressions: Option<i64>,
    /// numTimeUnits property.
    pub num_time_units: Option<i64>,
    /// timeUnitType property.
    pub time_unit_type: Option<String>,
}

/// `TargetingCriteria` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetingCriteria {
    /// exclusions property.
    pub exclusions: Option<Vec<TargetingValue>>,
    /// inclusions property.
    pub inclusions: Option<Vec<TargetingValue>>,
    /// key property.
    pub key: Option<String>,
}

/// `ResumeProposalRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResumeProposalRequest {}

/// `MetricValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MetricValue {
    /// value property.
    pub value: Option<String>,
    /// variance property.
    pub variance: Option<String>,
}

/// `CreativeDealAssociation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreativeDealAssociation {
    /// accountId property.
    pub account_id: Option<String>,
    /// creativeId property.
    pub creative_id: Option<String>,
    /// dealsId property.
    pub deals_id: Option<String>,
}

/// `CompleteSetupRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompleteSetupRequest {}

/// `DealPauseStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DealPauseStatus {
    /// buyerPauseReason property.
    pub buyer_pause_reason: Option<String>,
    /// firstPausedBy property.
    pub first_paused_by: Option<String>,
    /// hasBuyerPaused property.
    pub has_buyer_paused: Option<bool>,
    /// hasSellerPaused property.
    pub has_seller_paused: Option<bool>,
    /// sellerPauseReason property.
    pub seller_pause_reason: Option<String>,
}

/// `LocationContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocationContext {
    /// geoCriteriaIds property.
    pub geo_criteria_ids: Option<Vec<i64>>,
}

/// `Correction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Correction {
    /// contexts property.
    pub contexts: Option<Vec<ServingContext>>,
    /// details property.
    pub details: Option<Vec<String>>,
    /// type property.
    pub r#type: Option<String>,
}

/// `WatchCreativeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WatchCreativeRequest {
    /// topic property.
    pub topic: Option<String>,
}

/// `CreativeRestrictions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreativeRestrictions {
    /// creativeFormat property.
    pub creative_format: Option<String>,
    /// creativeSpecifications property.
    pub creative_specifications: Option<Vec<CreativeSpecification>>,
    /// skippableAdType property.
    pub skippable_ad_type: Option<String>,
}

/// `Image` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Image {
    /// height property.
    pub height: Option<i64>,
    /// url property.
    pub url: Option<String>,
    /// width property.
    pub width: Option<i64>,
}

/// `PlacementTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlacementTargeting {
    /// mobileApplicationTargeting property.
    pub mobile_application_targeting: Option<MobileApplicationTargeting>,
    /// urlTargeting property.
    pub url_targeting: Option<UrlTargeting>,
}

/// `DayPart` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DayPart {
    /// dayOfWeek property.
    pub day_of_week: Option<String>,
    /// endTime property.
    pub end_time: Option<TimeOfDay>,
    /// startTime property.
    pub start_time: Option<TimeOfDay>,
}

/// `FirstPartyMobileApplicationTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirstPartyMobileApplicationTargeting {
    /// excludedAppIds property.
    pub excluded_app_ids: Option<Vec<String>>,
    /// targetedAppIds property.
    pub targeted_app_ids: Option<Vec<String>>,
}

/// `ResumeProposalDealsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResumeProposalDealsRequest {
    /// externalDealIds property.
    pub external_deal_ids: Option<Vec<String>>,
}

/// `DayPartTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DayPartTargeting {
    /// dayParts property.
    pub day_parts: Option<Vec<DayPart>>,
    /// timeZoneType property.
    pub time_zone_type: Option<String>,
}

/// `MarketplaceTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MarketplaceTargeting {
    /// geoTargeting property.
    pub geo_targeting: Option<CriteriaTargeting>,
    /// inventorySizeTargeting property.
    pub inventory_size_targeting: Option<InventorySizeTargeting>,
    /// placementTargeting property.
    pub placement_targeting: Option<PlacementTargeting>,
    /// technologyTargeting property.
    pub technology_targeting: Option<TechnologyTargeting>,
    /// videoTargeting property.
    pub video_targeting: Option<VideoTargeting>,
}

/// `AdSize` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdSize {
    /// height property.
    pub height: Option<String>,
    /// sizeType property.
    pub size_type: Option<String>,
    /// width property.
    pub width: Option<String>,
}

/// `ListPublisherProfilesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListPublisherProfilesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// publisherProfiles property.
    pub publisher_profiles: Option<Vec<PublisherProfile>>,
}

/// `TargetingValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetingValue {
    /// creativeSizeValue property.
    pub creative_size_value: Option<CreativeSize>,
    /// dayPartTargetingValue property.
    pub day_part_targeting_value: Option<DayPartTargeting>,
    /// longValue property.
    pub long_value: Option<String>,
    /// stringValue property.
    pub string_value: Option<String>,
}

/// `ListCreativeStatusBreakdownByCreativeResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListCreativeStatusBreakdownByCreativeResponse {
    /// filteredBidCreativeRows property.
    pub filtered_bid_creative_rows: Option<Vec<FilteredBidCreativeRow>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `AcceptProposalRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AcceptProposalRequest {
    /// proposalRevision property.
    pub proposal_revision: Option<String>,
}

/// `PlatformContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PlatformContext {
    /// platforms property.
    pub platforms: Option<Vec<String>>,
}

/// `ListProposalsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListProposalsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// proposals property.
    pub proposals: Option<Vec<Proposal>>,
}

/// `ClientUser` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClientUser {
    /// clientAccountId property.
    pub client_account_id: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// userId property.
    pub user_id: Option<String>,
}

/// `Deal` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Deal {
    /// availableEndTime property.
    pub available_end_time: Option<String>,
    /// availableStartTime property.
    pub available_start_time: Option<String>,
    /// buyerPrivateData property.
    pub buyer_private_data: Option<PrivateData>,
    /// createProductId property.
    pub create_product_id: Option<String>,
    /// createProductRevision property.
    pub create_product_revision: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// creativePreApprovalPolicy property.
    pub creative_pre_approval_policy: Option<String>,
    /// creativeRestrictions property.
    pub creative_restrictions: Option<CreativeRestrictions>,
    /// creativeSafeFrameCompatibility property.
    pub creative_safe_frame_compatibility: Option<String>,
    /// dealId property.
    pub deal_id: Option<String>,
    /// dealServingMetadata property.
    pub deal_serving_metadata: Option<DealServingMetadata>,
    /// dealTerms property.
    pub deal_terms: Option<DealTerms>,
    /// deliveryControl property.
    pub delivery_control: Option<DeliveryControl>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// externalDealId property.
    pub external_deal_id: Option<String>,
    /// isSetupComplete property.
    pub is_setup_complete: Option<bool>,
    /// programmaticCreativeSource property.
    pub programmatic_creative_source: Option<String>,
    /// proposalId property.
    pub proposal_id: Option<String>,
    /// sellerContacts property.
    pub seller_contacts: Option<Vec<ContactInformation>>,
    /// syndicationProduct property.
    pub syndication_product: Option<String>,
    /// targeting property.
    pub targeting: Option<MarketplaceTargeting>,
    /// targetingCriterion property.
    pub targeting_criterion: Option<Vec<TargetingCriteria>>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// webPropertyCode property.
    pub web_property_code: Option<String>,
}

/// `RelativeDateRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RelativeDateRange {
    /// durationDays property.
    pub duration_days: Option<i64>,
    /// offsetDays property.
    pub offset_days: Option<i64>,
}

/// `PauseProposalRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PauseProposalRequest {
    /// reason property.
    pub reason: Option<String>,
}

/// `ListClientUserInvitationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListClientUserInvitationsResponse {
    /// invitations property.
    pub invitations: Option<Vec<ClientUserInvitation>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ContactInformation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContactInformation {
    /// email property.
    pub email: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `PrivateData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PrivateData {
    /// referenceId property.
    pub reference_id: Option<String>,
}

/// `ListClientsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListClientsResponse {
    /// clients property.
    pub clients: Option<Vec<Client>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `PublisherProfileMobileApplication` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PublisherProfileMobileApplication {
    /// appStore property.
    pub app_store: Option<String>,
    /// externalAppId property.
    pub external_app_id: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `AddNoteRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AddNoteRequest {
    /// note property.
    pub note: Option<Note>,
}

/// `DealTerms` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DealTerms {
    /// brandingType property.
    pub branding_type: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// estimatedGrossSpend property.
    pub estimated_gross_spend: Option<Price>,
    /// estimatedImpressionsPerDay property.
    pub estimated_impressions_per_day: Option<String>,
    /// guaranteedFixedPriceTerms property.
    pub guaranteed_fixed_price_terms: Option<GuaranteedFixedPriceTerms>,
    /// nonGuaranteedAuctionTerms property.
    pub non_guaranteed_auction_terms: Option<NonGuaranteedAuctionTerms>,
    /// nonGuaranteedFixedPriceTerms property.
    pub non_guaranteed_fixed_price_terms: Option<NonGuaranteedFixedPriceTerms>,
    /// sellerTimeZone property.
    pub seller_time_zone: Option<String>,
}

/// `StopWatchingCreativeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StopWatchingCreativeRequest {}

/// `Size` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Size {
    /// height property.
    pub height: Option<i64>,
    /// width property.
    pub width: Option<i64>,
}

/// `InventorySizeTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InventorySizeTargeting {
    /// excludedInventorySizes property.
    pub excluded_inventory_sizes: Option<Vec<AdSize>>,
    /// targetedInventorySizes property.
    pub targeted_inventory_sizes: Option<Vec<AdSize>>,
}

/// `ServingContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServingContext {
    /// all property.
    pub all: Option<String>,
    /// appType property.
    pub app_type: Option<AppContext>,
    /// auctionType property.
    pub auction_type: Option<AuctionContext>,
    /// location property.
    pub location: Option<LocationContext>,
    /// platform property.
    pub platform: Option<PlatformContext>,
    /// securityType property.
    pub security_type: Option<SecurityContext>,
}

/// `Seller` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Seller {
    /// accountId property.
    pub account_id: Option<String>,
    /// subAccountId property.
    pub sub_account_id: Option<String>,
}

/// `Money` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Money {
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// units property.
    pub units: Option<String>,
}

/// `Proposal` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Proposal {
    /// billedBuyer property.
    pub billed_buyer: Option<Buyer>,
    /// buyer property.
    pub buyer: Option<Buyer>,
    /// buyerContacts property.
    pub buyer_contacts: Option<Vec<ContactInformation>>,
    /// buyerPrivateData property.
    pub buyer_private_data: Option<PrivateData>,
    /// deals property.
    pub deals: Option<Vec<Deal>>,
    /// displayName property.
    pub display_name: Option<String>,
    /// isRenegotiating property.
    pub is_renegotiating: Option<bool>,
    /// isSetupComplete property.
    pub is_setup_complete: Option<bool>,
    /// lastUpdaterOrCommentorRole property.
    pub last_updater_or_commentor_role: Option<String>,
    /// notes property.
    pub notes: Option<Vec<Note>>,
    /// originatorRole property.
    pub originator_role: Option<String>,
    /// privateAuctionId property.
    pub private_auction_id: Option<String>,
    /// proposalId property.
    pub proposal_id: Option<String>,
    /// proposalRevision property.
    pub proposal_revision: Option<String>,
    /// proposalState property.
    pub proposal_state: Option<String>,
    /// seller property.
    pub seller: Option<Seller>,
    /// sellerContacts property.
    pub seller_contacts: Option<Vec<ContactInformation>>,
    /// termsAndConditions property.
    pub terms_and_conditions: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `SecurityContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityContext {
    /// securities property.
    pub securities: Option<Vec<String>>,
}

/// `BidMetricsRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BidMetricsRow {
    /// bids property.
    pub bids: Option<MetricValue>,
    /// bidsInAuction property.
    pub bids_in_auction: Option<MetricValue>,
    /// billedImpressions property.
    pub billed_impressions: Option<MetricValue>,
    /// impressionsWon property.
    pub impressions_won: Option<MetricValue>,
    /// measurableImpressions property.
    pub measurable_impressions: Option<MetricValue>,
    /// reachedQueries property.
    pub reached_queries: Option<MetricValue>,
    /// rowDimensions property.
    pub row_dimensions: Option<RowDimensions>,
    /// viewableImpressions property.
    pub viewable_impressions: Option<MetricValue>,
}

/// `OperatingSystemTargeting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OperatingSystemTargeting {
    /// operatingSystemCriteria property.
    pub operating_system_criteria: Option<CriteriaTargeting>,
    /// operatingSystemVersionCriteria property.
    pub operating_system_version_criteria: Option<CriteriaTargeting>,
}

/// `Product` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Product {
    /// availableEndTime property.
    pub available_end_time: Option<String>,
    /// availableStartTime property.
    pub available_start_time: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// creatorContacts property.
    pub creator_contacts: Option<Vec<ContactInformation>>,
    /// displayName property.
    pub display_name: Option<String>,
    /// hasCreatorSignedOff property.
    pub has_creator_signed_off: Option<bool>,
    /// productId property.
    pub product_id: Option<String>,
    /// productRevision property.
    pub product_revision: Option<String>,
    /// publisherProfileId property.
    pub publisher_profile_id: Option<String>,
    /// seller property.
    pub seller: Option<Seller>,
    /// syndicationProduct property.
    pub syndication_product: Option<String>,
    /// targetingCriterion property.
    pub targeting_criterion: Option<Vec<TargetingCriteria>>,
    /// terms property.
    pub terms: Option<DealTerms>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// webPropertyCode property.
    pub web_property_code: Option<String>,
}

/// `ListProductsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListProductsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// products property.
    pub products: Option<Vec<Product>>,
}

/// `VideoContent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VideoContent {
    /// videoUrl property.
    pub video_url: Option<String>,
    /// videoVastXml property.
    pub video_vast_xml: Option<String>,
}

/// `FilterSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FilterSet {
    /// absoluteDateRange property.
    pub absolute_date_range: Option<AbsoluteDateRange>,
    /// breakdownDimensions property.
    pub breakdown_dimensions: Option<Vec<String>>,
    /// creativeId property.
    pub creative_id: Option<String>,
    /// dealId property.
    pub deal_id: Option<String>,
    /// environment property.
    pub environment: Option<String>,
    /// format property.
    pub format: Option<String>,
    /// formats property.
    pub formats: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// platforms property.
    pub platforms: Option<Vec<String>>,
    /// publisherIdentifiers property.
    pub publisher_identifiers: Option<Vec<String>>,
    /// realtimeTimeRange property.
    pub realtime_time_range: Option<RealtimeTimeRange>,
    /// relativeDateRange property.
    pub relative_date_range: Option<RelativeDateRange>,
    /// sellerNetworkIds property.
    pub seller_network_ids: Option<Vec<i64>>,
    /// timeSeriesGranularity property.
    pub time_series_granularity: Option<String>,
}

/// `ListNonBillableWinningBidsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListNonBillableWinningBidsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// nonBillableWinningBidStatusRows property.
    pub non_billable_winning_bid_status_rows: Option<Vec<NonBillableWinningBidStatusRow>>,
}

/// `ListClientUsersResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListClientUsersResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// users property.
    pub users: Option<Vec<ClientUser>>,
}

/// `ListCreativeStatusBreakdownByDetailResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListCreativeStatusBreakdownByDetailResponse {
    /// detailType property.
    pub detail_type: Option<String>,
    /// filteredBidDetailRows property.
    pub filtered_bid_detail_rows: Option<Vec<FilteredBidDetailRow>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `AbsoluteDateRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AbsoluteDateRange {
    /// endDate property.
    pub end_date: Option<Date>,
    /// startDate property.
    pub start_date: Option<Date>,
}

/// `ListFilteredBidsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListFilteredBidsResponse {
    /// creativeStatusRows property.
    pub creative_status_rows: Option<Vec<CreativeStatusRow>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ListCreativesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListCreativesResponse {
    /// creatives property.
    pub creatives: Option<Vec<Creative>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Note` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Note {
    /// createTime property.
    pub create_time: Option<String>,
    /// creatorRole property.
    pub creator_role: Option<String>,
    /// note property.
    pub note: Option<String>,
    /// noteId property.
    pub note_id: Option<String>,
    /// proposalRevision property.
    pub proposal_revision: Option<String>,
}

/// `Date` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `RealtimeTimeRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RealtimeTimeRange {
    /// startTimestamp property.
    pub start_timestamp: Option<String>,
}

/// `CreativeStatusRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreativeStatusRow {
    /// bidCount property.
    pub bid_count: Option<MetricValue>,
    /// creativeStatusId property.
    pub creative_status_id: Option<i64>,
    /// rowDimensions property.
    pub row_dimensions: Option<RowDimensions>,
}

/// `RowDimensions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RowDimensions {
    /// publisherIdentifier property.
    pub publisher_identifier: Option<String>,
    /// timeInterval property.
    pub time_interval: Option<TimeInterval>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`adexchangebuyer2.accounts.clients.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsCreateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: Client,
}

/// Arguments for [`adexchangebuyer2.accounts.clients.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `clientAccountId`.
    pub client_account_id: String,
}

/// Arguments for [`adexchangebuyer2.accounts.clients.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `partnerClientId`.
    pub partner_client_id: Option<String>,
}

/// Arguments for [`adexchangebuyer2.accounts.clients.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `clientAccountId`.
    pub client_account_id: String,
    /// Request body.
    pub body: Client,
}

/// Arguments for [`adexchangebuyer2.accounts.clients.invitations.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsInvitationsCreateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `clientAccountId`.
    pub client_account_id: String,
    /// Request body.
    pub body: ClientUserInvitation,
}

/// Arguments for [`adexchangebuyer2.accounts.clients.invitations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsInvitationsGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `clientAccountId`.
    pub client_account_id: String,
    /// Path parameter: `invitationId`.
    pub invitation_id: String,
}

/// Arguments for [`adexchangebuyer2.accounts.clients.invitations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsInvitationsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `clientAccountId`.
    pub client_account_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.accounts.clients.users.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsUsersGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `clientAccountId`.
    pub client_account_id: String,
    /// Path parameter: `userId`.
    pub user_id: String,
}

/// Arguments for [`adexchangebuyer2.accounts.clients.users.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsUsersListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `clientAccountId`.
    pub client_account_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.accounts.clients.users.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsClientsUsersUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `clientAccountId`.
    pub client_account_id: String,
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Request body.
    pub body: ClientUser,
}

/// Arguments for [`adexchangebuyer2.accounts.creatives.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesCreateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `duplicateIdMode`.
    pub duplicate_id_mode: Option<String>,
    /// Request body.
    pub body: Creative,
}

/// Arguments for [`adexchangebuyer2.accounts.creatives.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `creativeId`.
    pub creative_id: String,
}

/// Arguments for [`adexchangebuyer2.accounts.creatives.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `query`.
    pub query: Option<String>,
}

/// Arguments for [`adexchangebuyer2.accounts.creatives.stopWatching_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesStopWatchingArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `creativeId`.
    pub creative_id: String,
    /// Request body.
    pub body: StopWatchingCreativeRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.creatives.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `creativeId`.
    pub creative_id: String,
    /// Request body.
    pub body: Creative,
}

/// Arguments for [`adexchangebuyer2.accounts.creatives.watch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesWatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `creativeId`.
    pub creative_id: String,
    /// Request body.
    pub body: WatchCreativeRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.creatives.dealAssociations.add_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesDealAssociationsAddArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `creativeId`.
    pub creative_id: String,
    /// Request body.
    pub body: AddDealAssociationRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.creatives.dealAssociations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesDealAssociationsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `creativeId`.
    pub creative_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `query`.
    pub query: Option<String>,
}

/// Arguments for [`adexchangebuyer2.accounts.creatives.dealAssociations.remove_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsCreativesDealAssociationsRemoveArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `creativeId`.
    pub creative_id: String,
    /// Request body.
    pub body: RemoveDealAssociationRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.finalizedProposals.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsFinalizedProposalsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `filterSyntax`.
    pub filter_syntax: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.accounts.finalizedProposals.pause_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsFinalizedProposalsPauseArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
    /// Request body.
    pub body: PauseProposalDealsRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.finalizedProposals.resume_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsFinalizedProposalsResumeArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
    /// Request body.
    pub body: ResumeProposalDealsRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.products.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProductsGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `productId`.
    pub product_id: String,
}

/// Arguments for [`adexchangebuyer2.accounts.products.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProductsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.accounts.proposals.accept_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsAcceptArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
    /// Request body.
    pub body: AcceptProposalRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.proposals.addNote_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsAddNoteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
    /// Request body.
    pub body: AddNoteRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.proposals.cancelNegotiation_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsCancelNegotiationArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
    /// Request body.
    pub body: CancelNegotiationRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.proposals.completeSetup_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsCompleteSetupArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
    /// Request body.
    pub body: CompleteSetupRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.proposals.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsCreateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: Proposal,
}

/// Arguments for [`adexchangebuyer2.accounts.proposals.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
}

/// Arguments for [`adexchangebuyer2.accounts.proposals.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `filterSyntax`.
    pub filter_syntax: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.accounts.proposals.pause_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsPauseArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
    /// Request body.
    pub body: PauseProposalRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.proposals.resume_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsResumeArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
    /// Request body.
    pub body: ResumeProposalRequest,
}

/// Arguments for [`adexchangebuyer2.accounts.proposals.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsProposalsUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
    /// Request body.
    pub body: Proposal,
}

/// Arguments for [`adexchangebuyer2.accounts.publisherProfiles.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsPublisherProfilesGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `publisherProfileId`.
    pub publisher_profile_id: String,
}

/// Arguments for [`adexchangebuyer2.accounts.publisherProfiles.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2AccountsPublisherProfilesListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsCreateArgs {
    /// Path parameter: `ownerName`.
    pub owner_name: String,
    /// Query parameter: `isTransient`.
    pub is_transient: Option<String>,
    /// Request body.
    pub body: FilterSet,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsListArgs {
    /// Path parameter: `ownerName`.
    pub owner_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.bidMetrics.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsBidMetricsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.bidResponseErrors.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsBidResponseErrorsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.bidResponsesWithoutBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsBidResponsesWithoutBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.filteredBidRequests.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidRequestsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.filteredBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.filteredBids.creatives.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsCreativesListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Path parameter: `creativeStatusId`.
    pub creative_status_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.filteredBids.details.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsDetailsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Path parameter: `creativeStatusId`.
    pub creative_status_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.impressionMetrics.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsImpressionMetricsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.losingBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsLosingBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.accounts.filterSets.nonBillableWinningBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersAccountsFilterSetsNonBillableWinningBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsCreateArgs {
    /// Path parameter: `ownerName`.
    pub owner_name: String,
    /// Query parameter: `isTransient`.
    pub is_transient: Option<String>,
    /// Request body.
    pub body: FilterSet,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsListArgs {
    /// Path parameter: `ownerName`.
    pub owner_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.bidMetrics.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsBidMetricsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.bidResponseErrors.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsBidResponseErrorsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.bidResponsesWithoutBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsBidResponsesWithoutBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.filteredBidRequests.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsFilteredBidRequestsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.filteredBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsFilteredBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.filteredBids.creatives.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsFilteredBidsCreativesListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Path parameter: `creativeStatusId`.
    pub creative_status_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.filteredBids.details.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsFilteredBidsDetailsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Path parameter: `creativeStatusId`.
    pub creative_status_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.impressionMetrics.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsImpressionMetricsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.losingBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsLosingBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.bidders.filterSets.nonBillableWinningBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BiddersFilterSetsNonBillableWinningBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsCreateArgs {
    /// Path parameter: `ownerName`.
    pub owner_name: String,
    /// Query parameter: `isTransient`.
    pub is_transient: Option<String>,
    /// Request body.
    pub body: FilterSet,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsListArgs {
    /// Path parameter: `ownerName`.
    pub owner_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.bidMetrics.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsBidMetricsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.bidResponseErrors.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsBidResponseErrorsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.bidResponsesWithoutBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsBidResponsesWithoutBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.filteredBidRequests.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsFilteredBidRequestsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.filteredBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsFilteredBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.filteredBids.creatives.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsFilteredBidsCreativesListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Path parameter: `creativeStatusId`.
    pub creative_status_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.filteredBids.details.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsFilteredBidsDetailsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Path parameter: `creativeStatusId`.
    pub creative_status_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.impressionMetrics.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsImpressionMetricsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.losingBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsLosingBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`adexchangebuyer2.buyers.filterSets.nonBillableWinningBids.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct Adexchangebuyer2BuyersFilterSetsNonBillableWinningBidsListArgs {
    /// Path parameter: `filterSetName`.
    pub filter_set_name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/clients
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/clients.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_clients_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_clients_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsClientsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Client>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Client =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_clients_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_clients_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsClientsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Client>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}",
        args.account_id, args.client_account_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Client =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/clients
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/clients.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_clients_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_clients_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsClientsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClientsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.partner_client_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("partnerClientId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListClientsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v2beta1/accounts/{accountId}/clients/{clientAccountId}
// -----------------------------------------------------------------------------

/// PUT v2beta1/accounts/{accountId}/clients/{clientAccountId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_clients_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_clients_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsClientsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Client>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}",
        args.account_id, args.client_account_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Client =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_clients_invitations_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_clients_invitations_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsClientsInvitationsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientUserInvitation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/invitations",
        args.account_id, args.client_account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ClientUserInvitation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations/{invitationId}
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations/{invitationId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_clients_invitations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_clients_invitations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsClientsInvitationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientUserInvitation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/invitations/{}",
        args.account_id, args.client_account_id, args.invitation_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ClientUserInvitation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/invitations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_clients_invitations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_clients_invitations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsClientsInvitationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClientUserInvitationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/invitations",
        args.account_id, args.client_account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListClientUserInvitationsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_clients_users_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_clients_users_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsClientsUsersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientUser>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/users/{}",
        args.account_id, args.client_account_id, args.user_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ClientUser =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/clients/{clientAccountId}/users.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_clients_users_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_clients_users_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsClientsUsersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListClientUsersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/users",
        args.account_id, args.client_account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListClientUsersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}
// -----------------------------------------------------------------------------

/// PUT v2beta1/accounts/{accountId}/clients/{clientAccountId}/users/{userId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_clients_users_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_clients_users_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsClientsUsersUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ClientUser>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/clients/{}/users/{}",
        args.account_id, args.client_account_id, args.user_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ClientUser =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/creatives
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/creatives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_creatives_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_creatives_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsCreativesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.duplicate_id_mode {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("duplicateIdMode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Creative =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/creatives/{creativeId}
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_creatives_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_creatives_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsCreativesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}",
        args.account_id, args.creative_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Creative =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/creatives
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/creatives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_creatives_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_creatives_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsCreativesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCreativesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.query {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("query=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCreativesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/creatives/{creativeId}:stopWatching
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/creatives/{creativeId}:stopWatching.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_creatives_stop_watching_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_creatives_stop_watching_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsCreativesStopWatchingArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}:stopWatching",
        args.account_id, args.creative_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v2beta1/accounts/{accountId}/creatives/{creativeId}
// -----------------------------------------------------------------------------

/// PUT v2beta1/accounts/{accountId}/creatives/{creativeId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_creatives_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_creatives_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsCreativesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}",
        args.account_id, args.creative_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Creative =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/creatives/{creativeId}:watch
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/creatives/{creativeId}:watch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_creatives_watch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_creatives_watch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsCreativesWatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}:watch",
        args.account_id, args.creative_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:add
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:add.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_creatives_deal_associations_add_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_creatives_deal_associations_add_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsCreativesDealAssociationsAddArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}/dealAssociations:add",
        args.account_id,
        args.creative_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_creatives_deal_associations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_creatives_deal_associations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsCreativesDealAssociationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDealAssociationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}/dealAssociations",
        args.account_id, args.creative_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.query {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("query=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDealAssociationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:remove
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/creatives/{creativeId}/dealAssociations:remove.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_creatives_deal_associations_remove_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_creatives_deal_associations_remove_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsCreativesDealAssociationsRemoveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/creatives/{}/dealAssociations:remove",
        args.account_id,
        args.creative_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/finalizedProposals
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/finalizedProposals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_finalized_proposals_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_finalized_proposals_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsFinalizedProposalsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProposalsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/finalizedProposals",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter_syntax {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filterSyntax=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListProposalsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:pause
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:pause.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_finalized_proposals_pause_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_finalized_proposals_pause_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsFinalizedProposalsPauseArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/finalizedProposals/{}:pause",
        args.account_id, args.proposal_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Proposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:resume
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/finalizedProposals/{proposalId}:resume.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_finalized_proposals_resume_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_finalized_proposals_resume_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsFinalizedProposalsResumeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/finalizedProposals/{}:resume",
        args.account_id, args.proposal_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Proposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/products/{productId}
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/products/{productId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_products_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_products_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProductsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Product>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/products/{}",
        args.account_id, args.product_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Product =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/products
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/products.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_products_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_products_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProductsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProductsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/products",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListProductsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:accept
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:accept.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_proposals_accept_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_proposals_accept_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProposalsAcceptArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:accept",
        args.account_id, args.proposal_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Proposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:addNote
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:addNote.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_proposals_add_note_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_proposals_add_note_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProposalsAddNoteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Note>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:addNote",
        args.account_id, args.proposal_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Note =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:cancelNegotiation
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:cancelNegotiation.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_proposals_cancel_negotiation_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_proposals_cancel_negotiation_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProposalsCancelNegotiationArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:cancelNegotiation",
        args.account_id, args.proposal_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Proposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:completeSetup
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:completeSetup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_proposals_complete_setup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_proposals_complete_setup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProposalsCompleteSetupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:completeSetup",
        args.account_id, args.proposal_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Proposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/proposals
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/proposals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_proposals_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_proposals_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProposalsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Proposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/proposals/{proposalId}
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/proposals/{proposalId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_proposals_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_proposals_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProposalsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}",
        args.account_id, args.proposal_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Proposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/proposals
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/proposals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_proposals_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_proposals_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProposalsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProposalsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter_syntax {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filterSyntax=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListProposalsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:pause
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:pause.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_proposals_pause_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_proposals_pause_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProposalsPauseArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:pause",
        args.account_id, args.proposal_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Proposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:resume
// -----------------------------------------------------------------------------

/// POST v2beta1/accounts/{accountId}/proposals/{proposalId}:resume.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_proposals_resume_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_proposals_resume_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProposalsResumeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}:resume",
        args.account_id, args.proposal_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Proposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v2beta1/accounts/{accountId}/proposals/{proposalId}
// -----------------------------------------------------------------------------

/// PUT v2beta1/accounts/{accountId}/proposals/{proposalId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_proposals_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_proposals_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsProposalsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Proposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/proposals/{}",
        args.account_id, args.proposal_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Proposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/publisherProfiles/{publisherProfileId}
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/publisherProfiles/{publisherProfileId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_publisher_profiles_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_publisher_profiles_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsPublisherProfilesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublisherProfile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/publisherProfiles/{}",
        args.account_id, args.publisher_profile_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PublisherProfile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/accounts/{accountId}/publisherProfiles
// -----------------------------------------------------------------------------

/// GET v2beta1/accounts/{accountId}/publisherProfiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_accounts_publisher_profiles_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_accounts_publisher_profiles_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2AccountsPublisherProfilesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPublisherProfilesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/accounts/{}/publisherProfiles",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPublisherProfilesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/{+ownerName}/filterSets
// -----------------------------------------------------------------------------

/// POST v2beta1/{+ownerName}/filterSets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FilterSet>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filterSets",
        args.owner_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.is_transient {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("isTransient=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FilterSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2beta1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v2beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}",
        args.name,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+name}
// -----------------------------------------------------------------------------

/// GET v2beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FilterSet>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FilterSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+ownerName}/filterSets
// -----------------------------------------------------------------------------

/// GET v2beta1/{+ownerName}/filterSets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFilterSetsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filterSets",
        args.owner_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFilterSetsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/bidMetrics
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/bidMetrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_metrics_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsBidMetricsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBidMetricsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/bidMetrics",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBidMetricsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/bidResponseErrors
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/bidResponseErrors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_response_errors_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsBidResponseErrorsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBidResponseErrorsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/bidResponseErrors",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBidResponseErrorsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/bidResponsesWithoutBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/bidResponsesWithoutBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_bid_responses_without_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsBidResponsesWithoutBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListBidResponsesWithoutBidsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/bidResponsesWithoutBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBidResponsesWithoutBidsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBidRequests
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBidRequests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bid_requests_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidRequestsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFilteredBidRequestsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBidRequests",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFilteredBidRequestsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFilteredBidsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFilteredBidsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/creatives
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/creatives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_creatives_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsCreativesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListCreativeStatusBreakdownByCreativeResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBids/{}/creatives",
        args.filter_set_name, args.creative_status_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByCreativeResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/details
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/details.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_filtered_bids_details_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsFilteredBidsDetailsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListCreativeStatusBreakdownByDetailResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBids/{}/details",
        args.filter_set_name, args.creative_status_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByDetailResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/impressionMetrics
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/impressionMetrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_impression_metrics_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsImpressionMetricsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListImpressionMetricsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/impressionMetrics",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListImpressionMetricsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/losingBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/losingBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_losing_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsLosingBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLosingBidsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/losingBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListLosingBidsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/nonBillableWinningBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/nonBillableWinningBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_accounts_filter_sets_non_billable_winning_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersAccountsFilterSetsNonBillableWinningBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListNonBillableWinningBidsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/nonBillableWinningBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListNonBillableWinningBidsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/{+ownerName}/filterSets
// -----------------------------------------------------------------------------

/// POST v2beta1/{+ownerName}/filterSets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FilterSet>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filterSets",
        args.owner_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.is_transient {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("isTransient=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FilterSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2beta1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v2beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}",
        args.name,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+name}
// -----------------------------------------------------------------------------

/// GET v2beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FilterSet>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FilterSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+ownerName}/filterSets
// -----------------------------------------------------------------------------

/// GET v2beta1/{+ownerName}/filterSets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFilterSetsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filterSets",
        args.owner_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFilterSetsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/bidMetrics
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/bidMetrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_bid_metrics_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_bid_metrics_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsBidMetricsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBidMetricsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/bidMetrics",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBidMetricsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/bidResponseErrors
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/bidResponseErrors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_bid_response_errors_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_bid_response_errors_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsBidResponseErrorsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBidResponseErrorsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/bidResponseErrors",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBidResponseErrorsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/bidResponsesWithoutBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/bidResponsesWithoutBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_bid_responses_without_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsBidResponsesWithoutBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListBidResponsesWithoutBidsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/bidResponsesWithoutBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBidResponsesWithoutBidsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBidRequests
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBidRequests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_filtered_bid_requests_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsFilteredBidRequestsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFilteredBidRequestsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBidRequests",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFilteredBidRequestsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_filtered_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsFilteredBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFilteredBidsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFilteredBidsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/creatives
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/creatives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_creatives_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsFilteredBidsCreativesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListCreativeStatusBreakdownByCreativeResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBids/{}/creatives",
        args.filter_set_name, args.creative_status_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByCreativeResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/details
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/details.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_filtered_bids_details_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsFilteredBidsDetailsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListCreativeStatusBreakdownByDetailResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBids/{}/details",
        args.filter_set_name, args.creative_status_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByDetailResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/impressionMetrics
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/impressionMetrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_impression_metrics_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_impression_metrics_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsImpressionMetricsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListImpressionMetricsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/impressionMetrics",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListImpressionMetricsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/losingBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/losingBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_losing_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_losing_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsLosingBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLosingBidsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/losingBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListLosingBidsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/nonBillableWinningBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/nonBillableWinningBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_bidders_filter_sets_non_billable_winning_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BiddersFilterSetsNonBillableWinningBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListNonBillableWinningBidsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/nonBillableWinningBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListNonBillableWinningBidsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2beta1/{+ownerName}/filterSets
// -----------------------------------------------------------------------------

/// POST v2beta1/{+ownerName}/filterSets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FilterSet>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filterSets",
        args.owner_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.is_transient {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("isTransient=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FilterSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2beta1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v2beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}",
        args.name,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+name}
// -----------------------------------------------------------------------------

/// GET v2beta1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FilterSet>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FilterSet =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+ownerName}/filterSets
// -----------------------------------------------------------------------------

/// GET v2beta1/{+ownerName}/filterSets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFilterSetsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filterSets",
        args.owner_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFilterSetsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/bidMetrics
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/bidMetrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_bid_metrics_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_bid_metrics_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsBidMetricsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBidMetricsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/bidMetrics",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBidMetricsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/bidResponseErrors
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/bidResponseErrors.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_bid_response_errors_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_bid_response_errors_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsBidResponseErrorsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBidResponseErrorsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/bidResponseErrors",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBidResponseErrorsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/bidResponsesWithoutBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/bidResponsesWithoutBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_bid_responses_without_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsBidResponsesWithoutBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListBidResponsesWithoutBidsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/bidResponsesWithoutBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBidResponsesWithoutBidsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBidRequests
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBidRequests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_filtered_bid_requests_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsFilteredBidRequestsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFilteredBidRequestsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBidRequests",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFilteredBidRequestsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_filtered_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsFilteredBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFilteredBidsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFilteredBidsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/creatives
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/creatives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_creatives_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsFilteredBidsCreativesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListCreativeStatusBreakdownByCreativeResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBids/{}/creatives",
        args.filter_set_name, args.creative_status_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByCreativeResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/details
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/filteredBids/{creativeStatusId}/details.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_filtered_bids_details_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsFilteredBidsDetailsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListCreativeStatusBreakdownByDetailResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/filteredBids/{}/details",
        args.filter_set_name, args.creative_status_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCreativeStatusBreakdownByDetailResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/impressionMetrics
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/impressionMetrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_impression_metrics_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_impression_metrics_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsImpressionMetricsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListImpressionMetricsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/impressionMetrics",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListImpressionMetricsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/losingBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/losingBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_losing_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_losing_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsLosingBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLosingBidsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/losingBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListLosingBidsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2beta1/{+filterSetName}/nonBillableWinningBids
// -----------------------------------------------------------------------------

/// GET v2beta1/{+filterSetName}/nonBillableWinningBids.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn adexchangebuyer2_buyers_filter_sets_non_billable_winning_bids_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &Adexchangebuyer2BuyersFilterSetsNonBillableWinningBidsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListNonBillableWinningBidsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://adexchangebuyer.googleapis.com/v2beta1/{}/nonBillableWinningBids",
        args.filter_set_name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListNonBillableWinningBidsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
