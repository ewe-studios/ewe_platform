//! Auto-generated API module for gcp/workloadmanager workloadmanager.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_workloadmanager_workloadmanager `

#![cfg(feature = "gcp_workloadmanager_workloadmanager")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GceInstanceFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GceInstanceFilter {
    /// serviceAccounts property.
    pub service_accounts: Option<Vec<String>>,
}

/// `SapDiscoveryResourceInstancePropertiesKernelVersionVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryResourceInstancePropertiesKernelVersionVersion {
    /// build property.
    pub build: Option<i64>,
    /// major property.
    pub major: Option<i64>,
    /// minor property.
    pub minor: Option<i64>,
    /// patch property.
    pub patch: Option<i64>,
    /// remainder property.
    pub remainder: Option<String>,
}

/// `ResourceFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceFilter {
    /// gceInstanceFilter property.
    pub gce_instance_filter: Option<GceInstanceFilter>,
    /// inclusionLabels property.
    pub inclusion_labels: Option<serde_json::Value>,
    /// resourceIdPatterns property.
    pub resource_id_patterns: Option<Vec<String>>,
    /// scopes property.
    pub scopes: Option<Vec<String>>,
}

/// `ListDeploymentsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListDeploymentsResponse {
    /// deployments property.
    pub deployments: Option<Vec<Deployment>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `ListEvaluationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListEvaluationsResponse {
    /// evaluations property.
    pub evaluations: Option<Vec<Evaluation>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `Pacemaker` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Pacemaker {
    /// bucketNameNodeCertificates property.
    pub bucket_name_node_certificates: Option<String>,
    /// pacemakerCluster property.
    pub pacemaker_cluster: Option<String>,
    /// pacemakerClusterSecret property.
    pub pacemaker_cluster_secret: Option<String>,
    /// pacemakerClusterUsername property.
    pub pacemaker_cluster_username: Option<String>,
    /// sqlPacemakerSecret property.
    pub sql_pacemaker_secret: Option<String>,
    /// sqlPacemakerUsername property.
    pub sql_pacemaker_username: Option<String>,
}

/// `Deployment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Deployment {
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// sapSystemS4Config property.
    pub sap_system_s4_config: Option<SapSystemS4Config>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// sqlServerWorkload property.
    pub sql_server_workload: Option<SqlServerWorkload>,
    /// state property.
    pub state: Option<String>,
    /// terraformVariables property.
    pub terraform_variables: Option<serde_json::Value>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// workerPool property.
    pub worker_pool: Option<String>,
    /// workloadType property.
    pub workload_type: Option<String>,
}

/// `ListActuationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListActuationsResponse {
    /// actuations property.
    pub actuations: Option<Vec<Actuation>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `RuleExecutionResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RuleExecutionResult {
    /// message property.
    pub message: Option<String>,
    /// resultCount property.
    pub result_count: Option<String>,
    /// rule property.
    pub rule: Option<String>,
    /// scannedResourceCount property.
    pub scanned_resource_count: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `ServiceStates` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceStates {
    /// iamPermissions property.
    pub iam_permissions: Option<Vec<IAMPermission>>,
    /// state property.
    pub state: Option<String>,
}

/// `ListLocationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListLocationsResponse {
    /// locations property.
    pub locations: Option<Vec<Location>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ResourceStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceStatus {
    /// state property.
    pub state: Option<String>,
}

/// `SapDiscoveryWorkloadPropertiesSoftwareComponentProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryWorkloadPropertiesSoftwareComponentProperties {
    /// extVersion property.
    pub ext_version: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `SapDiscoveryComponentApplicationProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryComponentApplicationProperties {
    /// abap property.
    pub abap: Option<bool>,
    /// appInstanceNumber property.
    pub app_instance_number: Option<String>,
    /// applicationType property.
    pub application_type: Option<String>,
    /// ascsInstanceNumber property.
    pub ascs_instance_number: Option<String>,
    /// ascsUri property.
    pub ascs_uri: Option<String>,
    /// ersInstanceNumber property.
    pub ers_instance_number: Option<String>,
    /// kernelVersion property.
    pub kernel_version: Option<String>,
    /// nfsUri property.
    pub nfs_uri: Option<String>,
}

/// `SapDiscoveryResourceInstancePropertiesDiskMount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryResourceInstancePropertiesDiskMount {
    /// diskNames property.
    pub disk_names: Option<Vec<String>>,
    /// mountPoint property.
    pub mount_point: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `BackupProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackupProperties {
    /// latestBackupStatus property.
    pub latest_backup_status: Option<String>,
    /// latestBackupTime property.
    pub latest_backup_time: Option<String>,
}

/// `SapComponent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapComponent {
    /// databaseProperties property.
    pub database_properties: Option<DatabaseProperties>,
    /// haHosts property.
    pub ha_hosts: Option<Vec<String>>,
    /// resources property.
    pub resources: Option<Vec<CloudResource>>,
    /// sid property.
    pub sid: Option<String>,
    /// topologyType property.
    pub topology_type: Option<String>,
}

/// `AgentCommand` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AgentCommand {
    /// command property.
    pub command: Option<String>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
}

/// `UpcomingMaintenanceEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpcomingMaintenanceEvent {
    /// endTime property.
    pub end_time: Option<String>,
    /// maintenanceStatus property.
    pub maintenance_status: Option<String>,
    /// onHostMaintenance property.
    pub on_host_maintenance: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SqlserverValidationValidationDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlserverValidationValidationDetail {
    /// details property.
    pub details: Option<Vec<SqlserverValidationDetails>>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SapDiscoveryMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryMetadata {
    /// customerRegion property.
    pub customer_region: Option<String>,
    /// definedSystem property.
    pub defined_system: Option<String>,
    /// environmentType property.
    pub environment_type: Option<String>,
    /// sapProduct property.
    pub sap_product: Option<String>,
}

/// `SapDiscoveryWorkloadPropertiesProductVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryWorkloadPropertiesProductVersion {
    /// name property.
    pub name: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `HealthCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthCheck {
    /// message property.
    pub message: Option<String>,
    /// metric property.
    pub metric: Option<String>,
    /// resource property.
    pub resource: Option<CloudResource>,
    /// source property.
    pub source: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `ListScannedResourcesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListScannedResourcesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// scannedResources property.
    pub scanned_resources: Option<Vec<ScannedResource>>,
}

/// `Resource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Resource {
    /// name property.
    pub name: Option<String>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SapValidation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapValidation {
    /// projectId property.
    pub project_id: Option<String>,
    /// validationDetails property.
    pub validation_details: Option<Vec<SapValidationValidationDetail>>,
    /// zone property.
    pub zone: Option<String>,
}

/// `ActuationOutput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActuationOutput {
    /// actuateLogs property.
    pub actuate_logs: Option<String>,
    /// ansibleError property.
    pub ansible_error: Option<String>,
    /// ansibleFailedTask property.
    pub ansible_failed_task: Option<Vec<String>>,
    /// blueprintId property.
    pub blueprint_id: Option<String>,
    /// cloudbuildId property.
    pub cloudbuild_id: Option<String>,
    /// errorCode property.
    pub error_code: Option<String>,
    /// errorLogs property.
    pub error_logs: Option<String>,
    /// hasUserFacingErrorMsg property.
    pub has_user_facing_error_msg: Option<bool>,
    /// terraformError property.
    pub terraform_error: Option<String>,
    /// terraformTemplate property.
    pub terraform_template: Option<String>,
}

/// `SapDiscoveryComponent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryComponent {
    /// applicationProperties property.
    pub application_properties: Option<SapDiscoveryComponentApplicationProperties>,
    /// databaseProperties property.
    pub database_properties: Option<SapDiscoveryComponentDatabaseProperties>,
    /// haHosts property.
    pub ha_hosts: Option<Vec<String>>,
    /// hostProject property.
    pub host_project: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// replicationSites property.
    pub replication_sites: Option<Vec<Box<SapDiscoveryComponentReplicationSite>>>,
    /// resources property.
    pub resources: Option<Vec<SapDiscoveryResource>>,
    /// sid property.
    pub sid: Option<String>,
    /// topologyType property.
    pub topology_type: Option<String>,
}

/// `SapSystemS4Config` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapSystemS4Config {
    /// allowStoppingForUpdate property.
    pub allow_stopping_for_update: Option<bool>,
    /// ansibleRunnerServiceAccount property.
    pub ansible_runner_service_account: Option<String>,
    /// app property.
    pub app: Option<AppDetails>,
    /// database property.
    pub database: Option<DatabaseDetails>,
    /// deploymentModel property.
    pub deployment_model: Option<String>,
    /// environmentType property.
    pub environment_type: Option<String>,
    /// gcpProjectId property.
    pub gcp_project_id: Option<String>,
    /// location property.
    pub location: Option<LocationDetails>,
    /// mediaBucketName property.
    pub media_bucket_name: Option<String>,
    /// sapBootDiskImage property.
    pub sap_boot_disk_image: Option<String>,
    /// scalingMethod property.
    pub scaling_method: Option<String>,
    /// version property.
    pub version: Option<String>,
    /// vmPrefix property.
    pub vm_prefix: Option<String>,
}

/// `Notice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Notice {
    /// message property.
    pub message: Option<String>,
}

/// `Rule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Rule {
    /// assetType property.
    pub asset_type: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// errorMessage property.
    pub error_message: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// primaryCategory property.
    pub primary_category: Option<String>,
    /// remediation property.
    pub remediation: Option<String>,
    /// revisionId property.
    pub revision_id: Option<String>,
    /// ruleType property.
    pub rule_type: Option<String>,
    /// secondaryCategory property.
    pub secondary_category: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// uri property.
    pub uri: Option<String>,
}

/// `SapValidationValidationDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapValidationValidationDetail {
    /// details property.
    pub details: Option<serde_json::Value>,
    /// isPresent property.
    pub is_present: Option<bool>,
    /// sapValidationType property.
    pub sap_validation_type: Option<String>,
}

/// `SapDiscoveryComponentDatabaseProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryComponentDatabaseProperties {
    /// databaseSid property.
    pub database_sid: Option<String>,
    /// databaseType property.
    pub database_type: Option<String>,
    /// databaseVersion property.
    pub database_version: Option<String>,
    /// instanceNumber property.
    pub instance_number: Option<String>,
    /// landscapeId property.
    pub landscape_id: Option<String>,
    /// primaryInstanceUri property.
    pub primary_instance_uri: Option<String>,
    /// sharedNfsUri property.
    pub shared_nfs_uri: Option<String>,
}

/// `DeploymentOutput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeploymentOutput {
    /// name property.
    pub name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SapInstanceProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapInstanceProperties {
    /// agentStates property.
    pub agent_states: Option<AgentStates>,
    /// numbers property.
    pub numbers: Option<Vec<String>>,
}

/// `ViolationDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ViolationDetails {
    /// asset property.
    pub asset: Option<String>,
    /// observed property.
    pub observed: Option<serde_json::Value>,
    /// ruleOutput property.
    pub rule_output: Option<Vec<RuleOutput>>,
    /// serviceAccount property.
    pub service_account: Option<String>,
}

/// `SqlServerWorkload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerWorkload {
    /// activeDirectory property.
    pub active_directory: Option<ActiveDirectory>,
    /// computeEngineServiceAccount property.
    pub compute_engine_service_account: Option<String>,
    /// database property.
    pub database: Option<Database>,
    /// deploymentModel property.
    pub deployment_model: Option<String>,
    /// environmentType property.
    pub environment_type: Option<String>,
    /// fciType property.
    pub fci_type: Option<String>,
    /// haType property.
    pub ha_type: Option<String>,
    /// isSqlPayg property.
    pub is_sql_payg: Option<bool>,
    /// location property.
    pub location: Option<SqlLocationDetails>,
    /// mediaBucket property.
    pub media_bucket: Option<String>,
    /// operatingSystemType property.
    pub operating_system_type: Option<String>,
    /// osImage property.
    pub os_image: Option<String>,
    /// osImageType property.
    pub os_image_type: Option<String>,
    /// pacemaker property.
    pub pacemaker: Option<Pacemaker>,
    /// sqlServerEdition property.
    pub sql_server_edition: Option<String>,
    /// sqlServerVersion property.
    pub sql_server_version: Option<String>,
    /// vmPrefix property.
    pub vm_prefix: Option<String>,
}

/// `Execution` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Execution {
    /// endTime property.
    pub end_time: Option<String>,
    /// engine property.
    pub engine: Option<String>,
    /// evaluationId property.
    pub evaluation_id: Option<String>,
    /// externalDataSources property.
    pub external_data_sources: Option<Vec<ExternalDataSources>>,
    /// inventoryTime property.
    pub inventory_time: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// notices property.
    pub notices: Option<Vec<Notice>>,
    /// resultSummary property.
    pub result_summary: Option<Summary>,
    /// ruleResults property.
    pub rule_results: Option<Vec<RuleExecutionResult>>,
    /// runType property.
    pub run_type: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `SapDiscoveryResource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryResource {
    /// instanceProperties property.
    pub instance_properties: Option<SapDiscoveryResourceInstanceProperties>,
    /// relatedResources property.
    pub related_resources: Option<Vec<String>>,
    /// resourceKind property.
    pub resource_kind: Option<String>,
    /// resourceType property.
    pub resource_type: Option<String>,
    /// resourceUri property.
    pub resource_uri: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `ListRulesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListRulesResponse {
    /// rules property.
    pub rules: Option<Vec<Rule>>,
}

/// `ExternalDataSources` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExternalDataSources {
    /// assetType property.
    pub asset_type: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// uri property.
    pub uri: Option<String>,
}

/// `AgentStates` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AgentStates {
    /// availableVersion property.
    pub available_version: Option<String>,
    /// hanaMonitoring property.
    pub hana_monitoring: Option<ServiceStates>,
    /// installedVersion property.
    pub installed_version: Option<String>,
    /// isFullyEnabled property.
    pub is_fully_enabled: Option<bool>,
    /// processMetrics property.
    pub process_metrics: Option<ServiceStates>,
    /// systemDiscovery property.
    pub system_discovery: Option<ServiceStates>,
}

/// `OpenShiftValidation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OpenShiftValidation {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// validationDetails property.
    pub validation_details: Option<serde_json::Value>,
}

/// `DatabaseDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatabaseDetails {
    /// databaseServiceAccount property.
    pub database_service_account: Option<String>,
    /// diskType property.
    pub disk_type: Option<String>,
    /// image property.
    pub image: Option<String>,
    /// instanceId property.
    pub instance_id: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// primaryDbVm property.
    pub primary_db_vm: Option<String>,
    /// secondaryDbVm property.
    pub secondary_db_vm: Option<String>,
    /// secretManagerSecret property.
    pub secret_manager_secret: Option<String>,
    /// sid property.
    pub sid: Option<String>,
}

/// `RunEvaluationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RunEvaluationRequest {
    /// execution property.
    pub execution: Option<Execution>,
    /// executionId property.
    pub execution_id: Option<String>,
    /// requestId property.
    pub request_id: Option<String>,
}

/// `ShellCommand` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShellCommand {
    /// args property.
    pub args: Option<String>,
    /// command property.
    pub command: Option<String>,
    /// timeoutSeconds property.
    pub timeout_seconds: Option<i64>,
}

/// `Insight` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Insight {
    /// agentStatus property.
    pub agent_status: Option<AgentStatus>,
    /// instanceId property.
    pub instance_id: Option<String>,
    /// openShiftValidation property.
    pub open_shift_validation: Option<OpenShiftValidation>,
    /// sapDiscovery property.
    pub sap_discovery: Option<SapDiscovery>,
    /// sapValidation property.
    pub sap_validation: Option<SapValidation>,
    /// sentTime property.
    pub sent_time: Option<String>,
    /// sqlserverValidation property.
    pub sqlserver_validation: Option<SqlserverValidation>,
    /// torsoValidation property.
    pub torso_validation: Option<TorsoValidation>,
}

/// `SapDiscoveryComponentReplicationSite` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryComponentReplicationSite {
    /// component property.
    pub component: Option<Box<SapDiscoveryComponent>>,
    /// sourceSite property.
    pub source_site: Option<String>,
}

/// `SapDiscoveryResourceInstancePropertiesAppInstance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryResourceInstancePropertiesAppInstance {
    /// name property.
    pub name: Option<String>,
    /// number property.
    pub number: Option<String>,
}

/// `CloudResource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CloudResource {
    /// instanceProperties property.
    pub instance_properties: Option<InstanceProperties>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `DatabaseProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatabaseProperties {
    /// backupProperties property.
    pub backup_properties: Option<BackupProperties>,
    /// databaseType property.
    pub database_type: Option<String>,
}

/// `CancelOperationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelOperationRequest {}

/// `ScannedResource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ScannedResource {
    /// resource property.
    pub resource: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `BigQueryDestination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BigQueryDestination {
    /// createNewResultsTable property.
    pub create_new_results_table: Option<bool>,
    /// destinationDataset property.
    pub destination_dataset: Option<String>,
}

/// `AgentStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AgentStatus {
    /// agentName property.
    pub agent_name: Option<String>,
    /// availableVersion property.
    pub available_version: Option<String>,
    /// cloudApiAccessFullScopesGranted property.
    pub cloud_api_access_full_scopes_granted: Option<String>,
    /// configurationErrorMessage property.
    pub configuration_error_message: Option<String>,
    /// configurationFilePath property.
    pub configuration_file_path: Option<String>,
    /// configurationValid property.
    pub configuration_valid: Option<String>,
    /// installedVersion property.
    pub installed_version: Option<String>,
    /// instanceUri property.
    pub instance_uri: Option<String>,
    /// kernelVersion property.
    pub kernel_version: Option<SapDiscoveryResourceInstancePropertiesKernelVersion>,
    /// references property.
    pub references: Option<Vec<AgentStatusReference>>,
    /// services property.
    pub services: Option<Vec<AgentStatusServiceStatus>>,
    /// systemdServiceEnabled property.
    pub systemd_service_enabled: Option<String>,
    /// systemdServiceRunning property.
    pub systemd_service_running: Option<String>,
}

/// `AppDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppDetails {
    /// appInstanceId property.
    pub app_instance_id: Option<String>,
    /// appServiceAccount property.
    pub app_service_account: Option<String>,
    /// appVmNames property.
    pub app_vm_names: Option<Vec<String>>,
    /// ascsImage property.
    pub ascs_image: Option<String>,
    /// ascsInstanceId property.
    pub ascs_instance_id: Option<String>,
    /// ascsMachineType property.
    pub ascs_machine_type: Option<String>,
    /// ascsServiceAccount property.
    pub ascs_service_account: Option<String>,
    /// ascsVm property.
    pub ascs_vm: Option<String>,
    /// ersInstanceId property.
    pub ers_instance_id: Option<String>,
    /// ersVm property.
    pub ers_vm: Option<String>,
    /// image property.
    pub image: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// secretManagerSecret property.
    pub secret_manager_secret: Option<String>,
    /// sharedStorage property.
    pub shared_storage: Option<String>,
    /// sid property.
    pub sid: Option<String>,
    /// vmsMultiplier property.
    pub vms_multiplier: Option<i64>,
}

/// `Command` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Command {
    /// agentCommand property.
    pub agent_command: Option<AgentCommand>,
    /// shellCommand property.
    pub shell_command: Option<ShellCommand>,
}

/// `ActiveDirectory` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActiveDirectory {
    /// dnsAddress property.
    pub dns_address: Option<String>,
    /// domain property.
    pub domain: Option<String>,
    /// domainUsername property.
    pub domain_username: Option<String>,
    /// secretManagerSecret property.
    pub secret_manager_secret: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SqlLocationDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlLocationDetails {
    /// dnsZone property.
    pub dns_zone: Option<String>,
    /// gcpProjectId property.
    pub gcp_project_id: Option<String>,
    /// internetAccess property.
    pub internet_access: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// primaryZone property.
    pub primary_zone: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// secondaryZone property.
    pub secondary_zone: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// tertiaryZone property.
    pub tertiary_zone: Option<String>,
}

/// `WriteInsightResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WriteInsightResponse {}

/// `SapDiscoveryWorkloadProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryWorkloadProperties {
    /// productVersions property.
    pub product_versions: Option<Vec<SapDiscoveryWorkloadPropertiesProductVersion>>,
    /// softwareComponentVersions property.
    pub software_component_versions:
        Option<Vec<SapDiscoveryWorkloadPropertiesSoftwareComponentProperties>>,
}

/// `AgentStatusConfigValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AgentStatusConfigValue {
    /// isDefault property.
    pub is_default: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `Evaluation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Evaluation {
    /// bigQueryDestination property.
    pub big_query_destination: Option<BigQueryDestination>,
    /// createTime property.
    pub create_time: Option<String>,
    /// customRulesBucket property.
    pub custom_rules_bucket: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// evaluationType property.
    pub evaluation_type: Option<String>,
    /// kmsKey property.
    pub kms_key: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// resourceFilter property.
    pub resource_filter: Option<ResourceFilter>,
    /// resourceStatus property.
    pub resource_status: Option<ResourceStatus>,
    /// ruleNames property.
    pub rule_names: Option<Vec<String>>,
    /// schedule property.
    pub schedule: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `SapDiscoveryResourceInstancePropertiesKernelVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryResourceInstancePropertiesKernelVersion {
    /// distroKernel property.
    pub distro_kernel: Option<SapDiscoveryResourceInstancePropertiesKernelVersionVersion>,
    /// osKernel property.
    pub os_kernel: Option<SapDiscoveryResourceInstancePropertiesKernelVersionVersion>,
    /// rawString property.
    pub raw_string: Option<String>,
}

/// `Actuation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Actuation {
    /// actuationOutput property.
    pub actuation_output: Option<ActuationOutput>,
    /// deploymentOutput property.
    pub deployment_output: Option<Vec<DeploymentOutput>>,
    /// endTime property.
    pub end_time: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `WriteInsightRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WriteInsightRequest {
    /// agentVersion property.
    pub agent_version: Option<String>,
    /// insight property.
    pub insight: Option<Insight>,
    /// requestId property.
    pub request_id: Option<String>,
}

/// `IAMPermission` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IAMPermission {
    /// granted property.
    pub granted: Option<bool>,
    /// name property.
    pub name: Option<String>,
}

/// `LocationDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocationDetails {
    /// createCommsFirewall property.
    pub create_comms_firewall: Option<bool>,
    /// customTags property.
    pub custom_tags: Option<Vec<String>>,
    /// deploymentDnsEnabled property.
    pub deployment_dns_enabled: Option<bool>,
    /// dnsZone property.
    pub dns_zone: Option<String>,
    /// dnsZoneNameSuffix property.
    pub dns_zone_name_suffix: Option<String>,
    /// internetAccess property.
    pub internet_access: Option<String>,
    /// networkProject property.
    pub network_project: Option<String>,
    /// regionName property.
    pub region_name: Option<String>,
    /// subnetName property.
    pub subnet_name: Option<String>,
    /// vpcName property.
    pub vpc_name: Option<String>,
    /// zone1Name property.
    pub zone1_name: Option<String>,
    /// zone2Name property.
    pub zone2_name: Option<String>,
}

/// `RuleOutput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RuleOutput {
    /// details property.
    pub details: Option<serde_json::Value>,
    /// message property.
    pub message: Option<String>,
}

/// `SapDiscovery` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscovery {
    /// applicationLayer property.
    pub application_layer: Option<Box<SapDiscoveryComponent>>,
    /// databaseLayer property.
    pub database_layer: Option<Box<SapDiscoveryComponent>>,
    /// metadata property.
    pub metadata: Option<SapDiscoveryMetadata>,
    /// projectNumber property.
    pub project_number: Option<String>,
    /// systemId property.
    pub system_id: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// useDrReconciliation property.
    pub use_dr_reconciliation: Option<bool>,
    /// workloadProperties property.
    pub workload_properties: Option<SapDiscoveryWorkloadProperties>,
}

/// `WorkloadProfileHealth` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkloadProfileHealth {
    /// checkTime property.
    pub check_time: Option<String>,
    /// componentsHealth property.
    pub components_health: Option<Vec<Box<ComponentHealth>>>,
    /// state property.
    pub state: Option<String>,
}

/// `SqlserverValidation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlserverValidation {
    /// agentVersion property.
    pub agent_version: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// validationDetails property.
    pub validation_details: Option<Vec<SqlserverValidationValidationDetail>>,
}

/// `AgentStatusServiceStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AgentStatusServiceStatus {
    /// configValues property.
    pub config_values: Option<Vec<AgentStatusConfigValue>>,
    /// errorMessage property.
    pub error_message: Option<String>,
    /// fullyFunctional property.
    pub fully_functional: Option<String>,
    /// iamPermissions property.
    pub iam_permissions: Option<Vec<AgentStatusIAMPermission>>,
    /// name property.
    pub name: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// unspecifiedStateMessage property.
    pub unspecified_state_message: Option<String>,
}

/// `ListExecutionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListExecutionsResponse {
    /// executions property.
    pub executions: Option<Vec<Execution>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `SapWorkload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapWorkload {
    /// application property.
    pub application: Option<SapComponent>,
    /// architecture property.
    pub architecture: Option<String>,
    /// database property.
    pub database: Option<SapComponent>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// products property.
    pub products: Option<Vec<Product>>,
}

/// `SqlserverValidationDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlserverValidationDetails {
    /// fields property.
    pub fields: Option<serde_json::Value>,
}

/// `AgentStatusIAMPermission` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AgentStatusIAMPermission {
    /// granted property.
    pub granted: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `Database` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Database {
    /// diskType property.
    pub disk_type: Option<String>,
    /// floatingIpAddress property.
    pub floating_ip_address: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// secondarySoleTenantNode property.
    pub secondary_sole_tenant_node: Option<String>,
    /// secondarySoleTenantNodeType property.
    pub secondary_sole_tenant_node_type: Option<String>,
    /// secretManagerSecret property.
    pub secret_manager_secret: Option<String>,
    /// smt property.
    pub smt: Option<bool>,
    /// soleTenantNode property.
    pub sole_tenant_node: Option<String>,
    /// soleTenantNodeType property.
    pub sole_tenant_node_type: Option<String>,
    /// tempdbOnSsd property.
    pub tempdb_on_ssd: Option<bool>,
    /// tenancyModel property.
    pub tenancy_model: Option<String>,
}

/// `Location` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Location {
    /// displayName property.
    pub display_name: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// locationId property.
    pub location_id: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
}

/// `ListExecutionResultsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListExecutionResultsResponse {
    /// executionResults property.
    pub execution_results: Option<Vec<ExecutionResult>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ListDiscoveredProfilesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListDiscoveredProfilesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
    /// workloadProfiles property.
    pub workload_profiles: Option<Vec<WorkloadProfile>>,
}

/// `Summary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Summary {
    /// failures property.
    pub failures: Option<String>,
    /// newFailures property.
    pub new_failures: Option<String>,
    /// newFixes property.
    pub new_fixes: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `InstanceProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstanceProperties {
    /// instanceNumber property.
    pub instance_number: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// roles property.
    pub roles: Option<Vec<String>>,
    /// sapInstanceProperties property.
    pub sap_instance_properties: Option<SapInstanceProperties>,
    /// status property.
    pub status: Option<String>,
    /// upcomingMaintenanceEvent property.
    pub upcoming_maintenance_event: Option<UpcomingMaintenanceEvent>,
}

/// `Product` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Product {
    /// name property.
    pub name: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `AgentStatusReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AgentStatusReference {
    /// name property.
    pub name: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `ComponentHealth` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComponentHealth {
    /// component property.
    pub component: Option<String>,
    /// componentHealthChecks property.
    pub component_health_checks: Option<Vec<HealthCheck>>,
    /// componentHealthType property.
    pub component_health_type: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// subComponentsHealth property.
    pub sub_components_health: Option<Vec<Box<ComponentHealth>>>,
}

/// `WorkloadProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkloadProfile {
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// refreshedTime property.
    pub refreshed_time: Option<String>,
    /// sapWorkload property.
    pub sap_workload: Option<SapWorkload>,
    /// workloadType property.
    pub workload_type: Option<String>,
}

/// `SapDiscoveryResourceInstanceProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SapDiscoveryResourceInstanceProperties {
    /// appInstances property.
    pub app_instances: Option<Vec<SapDiscoveryResourceInstancePropertiesAppInstance>>,
    /// clusterInstances property.
    pub cluster_instances: Option<Vec<String>>,
    /// diskMounts property.
    pub disk_mounts: Option<Vec<SapDiscoveryResourceInstancePropertiesDiskMount>>,
    /// instanceNumber property.
    pub instance_number: Option<String>,
    /// instanceRole property.
    pub instance_role: Option<String>,
    /// isDrSite property.
    pub is_dr_site: Option<bool>,
    /// osKernelVersion property.
    pub os_kernel_version: Option<SapDiscoveryResourceInstancePropertiesKernelVersion>,
    /// virtualHostname property.
    pub virtual_hostname: Option<String>,
}

/// `ExecutionResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecutionResult {
    /// commands property.
    pub commands: Option<Vec<Command>>,
    /// documentationUrl property.
    pub documentation_url: Option<String>,
    /// resource property.
    pub resource: Option<Resource>,
    /// rule property.
    pub rule: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// violationDetails property.
    pub violation_details: Option<ViolationDetails>,
    /// violationMessage property.
    pub violation_message: Option<String>,
}

/// `ListOperationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListOperationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<Operation>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `TorsoValidation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TorsoValidation {
    /// agentVersion property.
    pub agent_version: Option<String>,
    /// instanceName property.
    pub instance_name: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// validationDetails property.
    pub validation_details: Option<serde_json::Value>,
    /// workloadType property.
    pub workload_type: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`workloadmanager.projects.locations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `extraLocationTypes`.
    pub extra_location_types: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.deployments.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `deploymentId`.
    pub deployment_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Deployment,
}

/// Arguments for [`workloadmanager.projects.locations.deployments.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.deployments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.deployments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.deployments.actuations.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsActuationsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Actuation,
}

/// Arguments for [`workloadmanager.projects.locations.deployments.actuations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsActuationsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.deployments.actuations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsActuationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.deployments.actuations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsActuationsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.discoveredprofiles.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDiscoveredprofilesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.discoveredprofiles.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDiscoveredprofilesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.discoveredprofiles.health.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDiscoveredprofilesHealthGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.evaluations.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `evaluationId`.
    pub evaluation_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Evaluation,
}

/// Arguments for [`workloadmanager.projects.locations.evaluations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.evaluations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.evaluations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.evaluations.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Evaluation,
}

/// Arguments for [`workloadmanager.projects.locations.evaluations.executions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsExecutionsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.evaluations.executions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsExecutionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.evaluations.executions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsExecutionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.evaluations.executions.run_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsExecutionsRunArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: RunEvaluationRequest,
}

/// Arguments for [`workloadmanager.projects.locations.evaluations.executions.results.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsExecutionsResultsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.evaluations.executions.scannedResources.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsExecutionsScannedResourcesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `rule`.
    pub rule: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.insights.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsInsightsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.insights.writeInsight_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsInsightsWriteInsightArgs {
    /// Path parameter: `location`.
    pub location: String,
    /// Request body.
    pub body: WriteInsightRequest,
}

/// Arguments for [`workloadmanager.projects.locations.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsOperationsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// Arguments for [`workloadmanager.projects.locations.operations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.rules.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsRulesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `customRulesBucket`.
    pub custom_rules_bucket: Option<String>,
    /// Query parameter: `evaluationType`.
    pub evaluation_type: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Location =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/locations
// -----------------------------------------------------------------------------

/// GET v1/{+name}/locations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/locations",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.extra_location_types {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("extraLocationTypes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListLocationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/deployments
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/deployments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsDeploymentsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/deployments",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.deployment_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deploymentId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsDeploymentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsDeploymentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Deployment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Deployment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/deployments
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/deployments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsDeploymentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDeploymentsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/deployments",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDeploymentsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/actuations
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/actuations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_actuations_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_actuations_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsDeploymentsActuationsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/actuations",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_actuations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_actuations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsDeploymentsActuationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_actuations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_actuations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsDeploymentsActuationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Actuation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Actuation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/actuations
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/actuations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_actuations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_actuations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsDeploymentsActuationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListActuationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/actuations",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListActuationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_discoveredprofiles_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_discoveredprofiles_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsDiscoveredprofilesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkloadProfile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WorkloadProfile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/discoveredprofiles
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/discoveredprofiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_discoveredprofiles_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_discoveredprofiles_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsDiscoveredprofilesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDiscoveredProfilesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/discoveredprofiles",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDiscoveredProfilesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_discoveredprofiles_health_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_discoveredprofiles_health_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsDiscoveredprofilesHealthGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkloadProfileHealth>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WorkloadProfileHealth =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/evaluations
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/evaluations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsEvaluationsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/evaluations",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.evaluation_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("evaluationId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsEvaluationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsEvaluationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Evaluation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Evaluation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/evaluations
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/evaluations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsEvaluationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListEvaluationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/evaluations",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListEvaluationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsEvaluationsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_executions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_executions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsEvaluationsExecutionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_executions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_executions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsEvaluationsExecutionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Execution>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Execution =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/executions
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/executions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_executions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_executions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsEvaluationsExecutionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListExecutionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/executions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListExecutionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}/executions:run
// -----------------------------------------------------------------------------

/// POST v1/{+name}/executions:run.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_executions_run_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_executions_run_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsEvaluationsExecutionsRunArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/executions:run",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/results
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/results.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_executions_results_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_executions_results_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsEvaluationsExecutionsResultsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListExecutionResultsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/results",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListExecutionResultsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/scannedResources
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/scannedResources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_executions_scanned_resources_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_executions_scanned_resources_list_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsEvaluationsExecutionsScannedResourcesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListScannedResourcesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/scannedResources",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.rule {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("rule=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListScannedResourcesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_insights_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_insights_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsInsightsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+location}/insights:writeInsight
// -----------------------------------------------------------------------------

/// POST v1/{+location}/insights:writeInsight.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_insights_write_insight_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_insights_write_insight_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsInsightsWriteInsightArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WriteInsightResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/insights:writeInsight",
        args.location,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WriteInsightResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}:cancel",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_operations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_operations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsOperationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://workloadmanager.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/operations
// -----------------------------------------------------------------------------

/// GET v1/{+name}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/operations",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOperationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/rules
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/rules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_rules_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_rules_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WorkloadmanagerProjectsLocationsRulesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRulesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/{}/rules",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.custom_rules_bucket {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("customRulesBucket=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.evaluation_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("evaluationType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListRulesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
