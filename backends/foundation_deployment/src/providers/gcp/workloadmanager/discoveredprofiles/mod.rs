//! Auto-generated API module for gcp/workloadmanager discoveredprofiles.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_workloadmanager_discoveredprofiles `

#![cfg(feature = "gcp_workloadmanager_discoveredprofiles")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `CloudResource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CloudResource {
    /// instanceProperties property.
    pub instance_properties: Option<InstanceProperties>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `DatabaseProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabaseProperties {
    /// backupProperties property.
    pub backup_properties: Option<BackupProperties>,
    /// databaseType property.
    pub database_type: Option<String>,
}

/// `SapWorkload` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SapWorkload {
    /// application property.
    pub application: Option<SapComponent>,
    /// architecture property.
    pub architecture: Option<String>,
    /// database property.
    pub database: Option<SapComponent>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// products property.
    pub products: Option<Vec<Product>>,
}

/// `Product` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Product {
    /// name property.
    pub name: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `AgentStates` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AgentStates {
    /// availableVersion property.
    pub available_version: Option<String>,
    /// hanaMonitoring property.
    pub hana_monitoring: Option<ServiceStates>,
    /// installedVersion property.
    pub installed_version: Option<String>,
    /// isFullyEnabled property.
    pub is_fully_enabled: Option<bool>,
    /// processMetrics property.
    pub process_metrics: Option<ServiceStates>,
    /// systemDiscovery property.
    pub system_discovery: Option<ServiceStates>,
}

/// `ListDiscoveredProfilesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListDiscoveredProfilesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
    /// workloadProfiles property.
    pub workload_profiles: Option<Vec<WorkloadProfile>>,
}

/// `WorkloadProfile` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WorkloadProfile {
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// refreshedTime property.
    pub refreshed_time: Option<String>,
    /// sapWorkload property.
    pub sap_workload: Option<SapWorkload>,
    /// workloadType property.
    pub workload_type: Option<String>,
}

/// `InstanceProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstanceProperties {
    /// instanceNumber property.
    pub instance_number: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// roles property.
    pub roles: Option<Vec<String>>,
    /// sapInstanceProperties property.
    pub sap_instance_properties: Option<SapInstanceProperties>,
    /// status property.
    pub status: Option<String>,
    /// upcomingMaintenanceEvent property.
    pub upcoming_maintenance_event: Option<UpcomingMaintenanceEvent>,
}

/// `SapInstanceProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SapInstanceProperties {
    /// agentStates property.
    pub agent_states: Option<AgentStates>,
    /// numbers property.
    pub numbers: Option<Vec<String>>,
}

/// `BackupProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupProperties {
    /// latestBackupStatus property.
    pub latest_backup_status: Option<String>,
    /// latestBackupTime property.
    pub latest_backup_time: Option<String>,
}

/// `IAMPermission` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IAMPermission {
    /// granted property.
    pub granted: Option<bool>,
    /// name property.
    pub name: Option<String>,
}

/// `ServiceStates` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ServiceStates {
    /// iamPermissions property.
    pub iam_permissions: Option<Vec<IAMPermission>>,
    /// state property.
    pub state: Option<String>,
}

/// `UpcomingMaintenanceEvent` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpcomingMaintenanceEvent {
    /// endTime property.
    pub end_time: Option<String>,
    /// maintenanceStatus property.
    pub maintenance_status: Option<String>,
    /// onHostMaintenance property.
    pub on_host_maintenance: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SapComponent` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SapComponent {
    /// databaseProperties property.
    pub database_properties: Option<DatabaseProperties>,
    /// haHosts property.
    pub ha_hosts: Option<Vec<String>>,
    /// resources property.
    pub resources: Option<Vec<CloudResource>>,
    /// sid property.
    pub sid: Option<String>,
    /// topologyType property.
    pub topology_type: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`workloadmanager.projects.locations.discoveredprofiles.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDiscoveredprofilesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.discoveredprofiles.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDiscoveredprofilesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/discoveredprofiles/{discoveredprofilesId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveredprofiles/{discoveredprofilesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_discoveredprofiles_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_discoveredprofiles_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkloadProfile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/discoveredprofiles/{{discoveredprofilesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WorkloadProfile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/discoveredprofiles
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveredprofiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_discoveredprofiles_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_discoveredprofiles_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDiscoveredProfilesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/discoveredprofiles",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDiscoveredProfilesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
