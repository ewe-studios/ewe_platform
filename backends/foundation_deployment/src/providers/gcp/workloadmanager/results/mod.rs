//! Auto-generated API module for gcp/workloadmanager results.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_workloadmanager_results `

#![cfg(feature = "gcp_workloadmanager_results")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ListExecutionResultsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListExecutionResultsResponse {
    /// executionResults property.
    pub execution_results: Option<Vec<ExecutionResult>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `RuleOutput` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RuleOutput {
    /// details property.
    pub details: Option<serde_json::Value>,
    /// message property.
    pub message: Option<String>,
}

/// `Resource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Resource {
    /// name property.
    pub name: Option<String>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ViolationDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ViolationDetails {
    /// asset property.
    pub asset: Option<String>,
    /// observed property.
    pub observed: Option<serde_json::Value>,
    /// ruleOutput property.
    pub rule_output: Option<Vec<RuleOutput>>,
    /// serviceAccount property.
    pub service_account: Option<String>,
}

/// `AgentCommand` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AgentCommand {
    /// command property.
    pub command: Option<String>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
}

/// `ExecutionResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExecutionResult {
    /// commands property.
    pub commands: Option<Vec<Command>>,
    /// documentationUrl property.
    pub documentation_url: Option<String>,
    /// resource property.
    pub resource: Option<Resource>,
    /// rule property.
    pub rule: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// violationDetails property.
    pub violation_details: Option<ViolationDetails>,
    /// violationMessage property.
    pub violation_message: Option<String>,
}

/// `Command` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Command {
    /// agentCommand property.
    pub agent_command: Option<AgentCommand>,
    /// shellCommand property.
    pub shell_command: Option<ShellCommand>,
}

/// `ShellCommand` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ShellCommand {
    /// args property.
    pub args: Option<String>,
    /// command property.
    pub command: Option<String>,
    /// timeoutSeconds property.
    pub timeout_seconds: Option<i64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`workloadmanager.projects.locations.evaluations.executions.results.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsEvaluationsExecutionsResultsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/evaluations/{evaluationsId}/executions/{executionsId}/results
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/evaluations/{evaluationsId}/executions/{executionsId}/results.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_evaluations_executions_results_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_evaluations_executions_results_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListExecutionResultsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/evaluations/{{evaluationsId}}/executions/{{executionsId}}/results",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListExecutionResultsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
