//! Auto-generated API module for gcp/workloadmanager health.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_workloadmanager_health `

#![cfg(feature = "gcp_workloadmanager_health")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `WorkloadProfileHealth` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WorkloadProfileHealth {
    /// checkTime property.
    pub check_time: Option<String>,
    /// componentsHealth property.
    pub components_health: Option<Vec<Box<ComponentHealth>>>,
    /// state property.
    pub state: Option<String>,
}

/// `IAMPermission` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IAMPermission {
    /// granted property.
    pub granted: Option<bool>,
    /// name property.
    pub name: Option<String>,
}

/// `SapInstanceProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SapInstanceProperties {
    /// agentStates property.
    pub agent_states: Option<AgentStates>,
    /// numbers property.
    pub numbers: Option<Vec<String>>,
}

/// `InstanceProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstanceProperties {
    /// instanceNumber property.
    pub instance_number: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// roles property.
    pub roles: Option<Vec<String>>,
    /// sapInstanceProperties property.
    pub sap_instance_properties: Option<SapInstanceProperties>,
    /// status property.
    pub status: Option<String>,
    /// upcomingMaintenanceEvent property.
    pub upcoming_maintenance_event: Option<UpcomingMaintenanceEvent>,
}

/// `UpcomingMaintenanceEvent` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpcomingMaintenanceEvent {
    /// endTime property.
    pub end_time: Option<String>,
    /// maintenanceStatus property.
    pub maintenance_status: Option<String>,
    /// onHostMaintenance property.
    pub on_host_maintenance: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `CloudResource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CloudResource {
    /// instanceProperties property.
    pub instance_properties: Option<InstanceProperties>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `ServiceStates` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ServiceStates {
    /// iamPermissions property.
    pub iam_permissions: Option<Vec<IAMPermission>>,
    /// state property.
    pub state: Option<String>,
}

/// `HealthCheck` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HealthCheck {
    /// message property.
    pub message: Option<String>,
    /// metric property.
    pub metric: Option<String>,
    /// resource property.
    pub resource: Option<CloudResource>,
    /// source property.
    pub source: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `ComponentHealth` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComponentHealth {
    /// component property.
    pub component: Option<String>,
    /// componentHealthChecks property.
    pub component_health_checks: Option<Vec<HealthCheck>>,
    /// componentHealthType property.
    pub component_health_type: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// subComponentsHealth property.
    pub sub_components_health: Option<Vec<Box<ComponentHealth>>>,
}

/// `AgentStates` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AgentStates {
    /// availableVersion property.
    pub available_version: Option<String>,
    /// hanaMonitoring property.
    pub hana_monitoring: Option<ServiceStates>,
    /// installedVersion property.
    pub installed_version: Option<String>,
    /// isFullyEnabled property.
    pub is_fully_enabled: Option<bool>,
    /// processMetrics property.
    pub process_metrics: Option<ServiceStates>,
    /// systemDiscovery property.
    pub system_discovery: Option<ServiceStates>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`workloadmanager.projects.locations.discoveredprofiles.health.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDiscoveredprofilesHealthGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/discoveredprofiles/{discoveredprofilesId}/health/{healthId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveredprofiles/{discoveredprofilesId}/health/{healthId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_discoveredprofiles_health_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_discoveredprofiles_health_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WorkloadProfileHealth>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/discoveredprofiles/{{discoveredprofilesId}}/health/{{healthId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WorkloadProfileHealth =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
