//! Auto-generated API module for gcp/workloadmanager deployments.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_workloadmanager_deployments `

#![cfg(feature = "gcp_workloadmanager_deployments")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SapSystemS4Config` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SapSystemS4Config {
    /// allowStoppingForUpdate property.
    pub allow_stopping_for_update: Option<bool>,
    /// ansibleRunnerServiceAccount property.
    pub ansible_runner_service_account: Option<String>,
    /// app property.
    pub app: Option<AppDetails>,
    /// database property.
    pub database: Option<DatabaseDetails>,
    /// deploymentModel property.
    pub deployment_model: Option<String>,
    /// environmentType property.
    pub environment_type: Option<String>,
    /// gcpProjectId property.
    pub gcp_project_id: Option<String>,
    /// location property.
    pub location: Option<LocationDetails>,
    /// mediaBucketName property.
    pub media_bucket_name: Option<String>,
    /// sapBootDiskImage property.
    pub sap_boot_disk_image: Option<String>,
    /// scalingMethod property.
    pub scaling_method: Option<String>,
    /// version property.
    pub version: Option<String>,
    /// vmPrefix property.
    pub vm_prefix: Option<String>,
}

/// `Pacemaker` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Pacemaker {
    /// bucketNameNodeCertificates property.
    pub bucket_name_node_certificates: Option<String>,
    /// pacemakerCluster property.
    pub pacemaker_cluster: Option<String>,
    /// pacemakerClusterSecret property.
    pub pacemaker_cluster_secret: Option<String>,
    /// pacemakerClusterUsername property.
    pub pacemaker_cluster_username: Option<String>,
    /// sqlPacemakerSecret property.
    pub sql_pacemaker_secret: Option<String>,
    /// sqlPacemakerUsername property.
    pub sql_pacemaker_username: Option<String>,
}

/// `SqlServerWorkload` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SqlServerWorkload {
    /// activeDirectory property.
    pub active_directory: Option<ActiveDirectory>,
    /// computeEngineServiceAccount property.
    pub compute_engine_service_account: Option<String>,
    /// database property.
    pub database: Option<Database>,
    /// deploymentModel property.
    pub deployment_model: Option<String>,
    /// environmentType property.
    pub environment_type: Option<String>,
    /// fciType property.
    pub fci_type: Option<String>,
    /// haType property.
    pub ha_type: Option<String>,
    /// isSqlPayg property.
    pub is_sql_payg: Option<bool>,
    /// location property.
    pub location: Option<SqlLocationDetails>,
    /// mediaBucket property.
    pub media_bucket: Option<String>,
    /// operatingSystemType property.
    pub operating_system_type: Option<String>,
    /// osImage property.
    pub os_image: Option<String>,
    /// osImageType property.
    pub os_image_type: Option<String>,
    /// pacemaker property.
    pub pacemaker: Option<Pacemaker>,
    /// sqlServerEdition property.
    pub sql_server_edition: Option<String>,
    /// sqlServerVersion property.
    pub sql_server_version: Option<String>,
    /// vmPrefix property.
    pub vm_prefix: Option<String>,
}

/// `LocationDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LocationDetails {
    /// createCommsFirewall property.
    pub create_comms_firewall: Option<bool>,
    /// customTags property.
    pub custom_tags: Option<Vec<String>>,
    /// deploymentDnsEnabled property.
    pub deployment_dns_enabled: Option<bool>,
    /// dnsZone property.
    pub dns_zone: Option<String>,
    /// dnsZoneNameSuffix property.
    pub dns_zone_name_suffix: Option<String>,
    /// internetAccess property.
    pub internet_access: Option<String>,
    /// networkProject property.
    pub network_project: Option<String>,
    /// regionName property.
    pub region_name: Option<String>,
    /// subnetName property.
    pub subnet_name: Option<String>,
    /// vpcName property.
    pub vpc_name: Option<String>,
    /// zone1Name property.
    pub zone1_name: Option<String>,
    /// zone2Name property.
    pub zone2_name: Option<String>,
}

/// `DatabaseDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabaseDetails {
    /// databaseServiceAccount property.
    pub database_service_account: Option<String>,
    /// diskType property.
    pub disk_type: Option<String>,
    /// image property.
    pub image: Option<String>,
    /// instanceId property.
    pub instance_id: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// primaryDbVm property.
    pub primary_db_vm: Option<String>,
    /// secondaryDbVm property.
    pub secondary_db_vm: Option<String>,
    /// secretManagerSecret property.
    pub secret_manager_secret: Option<String>,
    /// sid property.
    pub sid: Option<String>,
}

/// `ActiveDirectory` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ActiveDirectory {
    /// dnsAddress property.
    pub dns_address: Option<String>,
    /// domain property.
    pub domain: Option<String>,
    /// domainUsername property.
    pub domain_username: Option<String>,
    /// secretManagerSecret property.
    pub secret_manager_secret: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `Database` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Database {
    /// diskType property.
    pub disk_type: Option<String>,
    /// floatingIpAddress property.
    pub floating_ip_address: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// secondarySoleTenantNode property.
    pub secondary_sole_tenant_node: Option<String>,
    /// secondarySoleTenantNodeType property.
    pub secondary_sole_tenant_node_type: Option<String>,
    /// secretManagerSecret property.
    pub secret_manager_secret: Option<String>,
    /// smt property.
    pub smt: Option<bool>,
    /// soleTenantNode property.
    pub sole_tenant_node: Option<String>,
    /// soleTenantNodeType property.
    pub sole_tenant_node_type: Option<String>,
    /// tempdbOnSsd property.
    pub tempdb_on_ssd: Option<bool>,
    /// tenancyModel property.
    pub tenancy_model: Option<String>,
}

/// `Deployment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Deployment {
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// sapSystemS4Config property.
    pub sap_system_s4_config: Option<SapSystemS4Config>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// sqlServerWorkload property.
    pub sql_server_workload: Option<SqlServerWorkload>,
    /// state property.
    pub state: Option<String>,
    /// terraformVariables property.
    pub terraform_variables: Option<serde_json::Value>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// workerPool property.
    pub worker_pool: Option<String>,
    /// workloadType property.
    pub workload_type: Option<String>,
}

/// `ListDeploymentsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListDeploymentsResponse {
    /// deployments property.
    pub deployments: Option<Vec<Deployment>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `SqlLocationDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SqlLocationDetails {
    /// dnsZone property.
    pub dns_zone: Option<String>,
    /// gcpProjectId property.
    pub gcp_project_id: Option<String>,
    /// internetAccess property.
    pub internet_access: Option<String>,
    /// network property.
    pub network: Option<String>,
    /// primaryZone property.
    pub primary_zone: Option<String>,
    /// region property.
    pub region: Option<String>,
    /// secondaryZone property.
    pub secondary_zone: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// tertiaryZone property.
    pub tertiary_zone: Option<String>,
}

/// `AppDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AppDetails {
    /// appInstanceId property.
    pub app_instance_id: Option<String>,
    /// appServiceAccount property.
    pub app_service_account: Option<String>,
    /// appVmNames property.
    pub app_vm_names: Option<Vec<String>>,
    /// ascsImage property.
    pub ascs_image: Option<String>,
    /// ascsInstanceId property.
    pub ascs_instance_id: Option<String>,
    /// ascsMachineType property.
    pub ascs_machine_type: Option<String>,
    /// ascsServiceAccount property.
    pub ascs_service_account: Option<String>,
    /// ascsVm property.
    pub ascs_vm: Option<String>,
    /// ersInstanceId property.
    pub ers_instance_id: Option<String>,
    /// ersVm property.
    pub ers_vm: Option<String>,
    /// image property.
    pub image: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// secretManagerSecret property.
    pub secret_manager_secret: Option<String>,
    /// sharedStorage property.
    pub shared_storage: Option<String>,
    /// sid property.
    pub sid: Option<String>,
    /// vmsMultiplier property.
    pub vms_multiplier: Option<i64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`workloadmanager.projects.locations.deployments.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `deploymentId`.
    pub deployment_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.deployments.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`workloadmanager.projects.locations.deployments.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`workloadmanager.projects.locations.deployments.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkloadmanagerProjectsLocationsDeploymentsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/deployments
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/deployments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/deployments",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/deployments/{deploymentsId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/deployments/{deploymentsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/deployments/{{deploymentsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/deployments/{deploymentsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployments/{deploymentsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Deployment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/deployments/{{deploymentsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Deployment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/deployments
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/deployments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workloadmanager_projects_locations_deployments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workloadmanager_projects_locations_deployments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDeploymentsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workloadmanager.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/deployments",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDeploymentsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
