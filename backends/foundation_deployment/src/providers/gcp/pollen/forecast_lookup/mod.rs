//! Auto-generated API module for gcp/pollen forecast:lookup.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_pollen_forecast_lookup `

#![cfg(feature = "gcp_pollen_forecast_lookup")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `LookupForecastResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LookupForecastResponse {
    /// dailyInfo property.
    pub daily_info: Option<Vec<DayInfo>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `Color` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Color {
    /// alpha property.
    pub alpha: Option<f64>,
    /// blue property.
    pub blue: Option<f64>,
    /// green property.
    pub green: Option<f64>,
    /// red property.
    pub red: Option<f64>,
}

/// `PlantInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlantInfo {
    /// code property.
    pub code: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// inSeason property.
    pub in_season: Option<bool>,
    /// indexInfo property.
    pub index_info: Option<IndexInfo>,
    /// plantDescription property.
    pub plant_description: Option<PlantDescription>,
}

/// `PlantDescription` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlantDescription {
    /// crossReaction property.
    pub cross_reaction: Option<String>,
    /// family property.
    pub family: Option<String>,
    /// picture property.
    pub picture: Option<String>,
    /// pictureCloseup property.
    pub picture_closeup: Option<String>,
    /// season property.
    pub season: Option<String>,
    /// specialColors property.
    pub special_colors: Option<String>,
    /// specialShapes property.
    pub special_shapes: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Date` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `PollenTypeInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PollenTypeInfo {
    /// code property.
    pub code: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// healthRecommendations property.
    pub health_recommendations: Option<Vec<String>>,
    /// inSeason property.
    pub in_season: Option<bool>,
    /// indexInfo property.
    pub index_info: Option<IndexInfo>,
}

/// `DayInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DayInfo {
    /// date property.
    pub date: Option<Date>,
    /// plantInfo property.
    pub plant_info: Option<Vec<PlantInfo>>,
    /// pollenTypeInfo property.
    pub pollen_type_info: Option<Vec<PollenTypeInfo>>,
}

/// `IndexInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IndexInfo {
    /// category property.
    pub category: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// color property.
    pub color: Option<Color>,
    /// displayName property.
    pub display_name: Option<String>,
    /// indexDescription property.
    pub index_description: Option<String>,
    /// value property.
    pub value: Option<i64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`pollen.forecast.lookup_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PollenForecastLookupArgs {
    /// Query parameter: `days`.
    pub days: Option<String>,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `location.latitude`.
    pub location_latitude: Option<String>,
    /// Query parameter: `location.longitude`.
    pub location_longitude: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `plantsDescription`.
    pub plants_description: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/forecast:lookup
// -----------------------------------------------------------------------------

/// GET v1/forecast:lookup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = pollen_forecast_lookup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn pollen_forecast_lookup_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupForecastResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://pollen.googleapis.com/v1/forecast:lookup",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LookupForecastResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
