//! Auto-generated API module for gcp/connectors connectors.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_connectors_connectors `

#![cfg(feature = "gcp_connectors_connectors")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ExchangeAuthCodeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExchangeAuthCodeRequest {
    /// authCodeData property.
    pub auth_code_data: Option<AuthCodeData>,
    /// executionConfig property.
    pub execution_config: Option<ExecutionConfig>,
    /// oauth2Config property.
    pub oauth2_config: Option<OAuth2Config>,
}

/// `ExecuteSqlQueryRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecuteSqlQueryRequest {
    /// query property.
    pub query: Option<Query>,
}

/// `ToolSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolSpec {
    /// toolDefinitions property.
    pub tool_definitions: Option<Vec<serde_json::Value>>,
    /// toolSpecVersion property.
    pub tool_spec_version: Option<String>,
}

/// `Resource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Resource {
    /// _meta property.
    pub meta: Option<serde_json::Value>,
    /// description property.
    pub description: Option<String>,
    /// mimeType property.
    pub mime_type: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// size property.
    pub size: Option<String>,
    /// uri property.
    pub uri: Option<String>,
}

/// `ToolAnnotations` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolAnnotations {
    /// destructiveHint property.
    pub destructive_hint: Option<bool>,
    /// idempotentHint property.
    pub idempotent_hint: Option<bool>,
    /// openWorldHint property.
    pub open_world_hint: Option<bool>,
    /// readOnlyHint property.
    pub read_only_hint: Option<bool>,
    /// title property.
    pub title: Option<String>,
}

/// `EntityType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EntityType {
    /// defaultSortBy property.
    pub default_sort_by: Option<String>,
    /// fields property.
    pub fields: Option<Vec<Field>>,
    /// jsonSchema property.
    pub json_schema: Option<Box<JsonSchema>>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// operations property.
    pub operations: Option<Vec<String>>,
}

/// `ListToolsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListToolsResponse {
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// tools property.
    pub tools: Option<Vec<Tool>>,
}

/// `OAuth2Config` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OAuth2Config {
    /// authUri property.
    pub auth_uri: Option<String>,
    /// clientId property.
    pub client_id: Option<String>,
    /// clientSecret property.
    pub client_secret: Option<String>,
}

/// `ListEntitiesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListEntitiesResponse {
    /// entities property.
    pub entities: Option<Vec<Entity>>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GenerateCustomToolspecResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateCustomToolspecResponse {
    /// toolSpec property.
    pub tool_spec: Option<ToolSpec>,
}

/// `ExecuteActionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecuteActionResponse {
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// results property.
    pub results: Option<Vec<serde_json::Value>>,
}

/// `Field` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Field {
    /// additionalDetails property.
    pub additional_details: Option<serde_json::Value>,
    /// dataType property.
    pub data_type: Option<String>,
    /// defaultValue property.
    pub default_value: Option<serde_json::Value>,
    /// description property.
    pub description: Option<String>,
    /// jsonSchema property.
    pub json_schema: Option<Box<JsonSchema>>,
    /// key property.
    pub key: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// nullable property.
    pub nullable: Option<bool>,
    /// reference property.
    pub reference: Option<Reference>,
}

/// `ExchangeAuthCodeResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExchangeAuthCodeResponse {
    /// accessCredentials property.
    pub access_credentials: Option<AccessCredentials>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
}

/// `Reference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Reference {
    /// name property.
    pub name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `AuthCodeData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuthCodeData {
    /// authCode property.
    pub auth_code: Option<String>,
    /// pkceVerifier property.
    pub pkce_verifier: Option<String>,
    /// redirectUri property.
    pub redirect_uri: Option<String>,
    /// scopes property.
    pub scopes: Option<Vec<String>>,
}

/// `ExecutionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecutionConfig {
    /// headers property.
    pub headers: Option<String>,
}

/// `GenerateCustomToolspecRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateCustomToolspecRequest {
    /// toolNames property.
    pub tool_names: Option<Vec<ToolName>>,
}

/// `RefreshAccessTokenRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RefreshAccessTokenRequest {
    /// executionConfig property.
    pub execution_config: Option<ExecutionConfig>,
    /// oauth2Config property.
    pub oauth2_config: Option<OAuth2Config>,
    /// refreshToken property.
    pub refresh_token: Option<String>,
}

/// `ListActionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListActionsResponse {
    /// actions property.
    pub actions: Option<Vec<Action>>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unsupportedActionNames property.
    pub unsupported_action_names: Option<Vec<String>>,
}

/// `InputParameter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InputParameter {
    /// additionalDetails property.
    pub additional_details: Option<serde_json::Value>,
    /// dataType property.
    pub data_type: Option<String>,
    /// defaultValue property.
    pub default_value: Option<serde_json::Value>,
    /// description property.
    pub description: Option<String>,
    /// jsonSchema property.
    pub json_schema: Option<Box<JsonSchema>>,
    /// name property.
    pub name: Option<String>,
    /// nullable property.
    pub nullable: Option<bool>,
}

/// `ExecuteToolRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecuteToolRequest {
    /// executionConfig property.
    pub execution_config: Option<ExecutionConfig>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
    /// toolDefinition property.
    pub tool_definition: Option<serde_json::Value>,
}

/// `RefreshAccessTokenResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RefreshAccessTokenResponse {
    /// accessCredentials property.
    pub access_credentials: Option<AccessCredentials>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
}

/// `Action` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Action {
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// inputJsonSchema property.
    pub input_json_schema: Option<Box<JsonSchema>>,
    /// inputParameters property.
    pub input_parameters: Option<Vec<InputParameter>>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// resultJsonSchema property.
    pub result_json_schema: Option<Box<JsonSchema>>,
    /// resultMetadata property.
    pub result_metadata: Option<Vec<ResultMetadata>>,
}

/// `ToolName` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolName {
    /// entityName property.
    pub entity_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// operation property.
    pub operation: Option<String>,
}

/// `Query` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Query {
    /// maxRows property.
    pub max_rows: Option<String>,
    /// query property.
    pub query: Option<String>,
    /// queryParameters property.
    pub query_parameters: Option<Vec<QueryParameter>>,
    /// timeout property.
    pub timeout: Option<String>,
}

/// `ListCustomToolNamesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListCustomToolNamesResponse {
    /// toolNames property.
    pub tool_names: Option<Vec<ToolName>>,
}

/// `Tool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Tool {
    /// _meta property.
    pub meta: Option<serde_json::Value>,
    /// annotations property.
    pub annotations: Option<ToolAnnotations>,
    /// dependsOn property.
    pub depends_on: Option<Vec<String>>,
    /// description property.
    pub description: Option<String>,
    /// inputSchema property.
    pub input_schema: Option<Box<JsonSchema>>,
    /// name property.
    pub name: Option<String>,
    /// outputSchema property.
    pub output_schema: Option<Box<JsonSchema>>,
}

/// `Entity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Entity {
    /// fields property.
    pub fields: Option<serde_json::Value>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
}

/// `ExecuteSqlQueryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecuteSqlQueryResponse {
    /// results property.
    pub results: Option<Vec<serde_json::Value>>,
}

/// `ListToolsPostRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListToolsPostRequest {
    /// executionConfig property.
    pub execution_config: Option<ExecutionConfig>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// toolSpec property.
    pub tool_spec: Option<ToolSpec>,
}

/// `CheckStatusResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CheckStatusResponse {
    /// description property.
    pub description: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// state property.
    pub state: Option<String>,
}

/// `ListResourcesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListResourcesResponse {
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<Resource>>,
}

/// `ResultMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResultMetadata {
    /// dataType property.
    pub data_type: Option<String>,
    /// defaultValue property.
    pub default_value: Option<serde_json::Value>,
    /// description property.
    pub description: Option<String>,
    /// jsonSchema property.
    pub json_schema: Option<Box<JsonSchema>>,
    /// name property.
    pub name: Option<String>,
    /// nullable property.
    pub nullable: Option<bool>,
}

/// `ExecuteToolResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecuteToolResponse {
    /// _meta property.
    pub meta: Option<serde_json::Value>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// result property.
    pub result: Option<serde_json::Value>,
}

/// `QueryParameter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QueryParameter {
    /// dataType property.
    pub data_type: Option<String>,
    /// value property.
    pub value: Option<serde_json::Value>,
}

/// `CheckReadinessResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CheckReadinessResponse {
    /// status property.
    pub status: Option<String>,
}

/// `ListEntityTypesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListEntityTypesResponse {
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// types property.
    pub types: Option<Vec<EntityType>>,
    /// unsupportedTypeNames property.
    pub unsupported_type_names: Option<Vec<String>>,
}

/// `ExecuteActionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecuteActionRequest {
    /// executionConfig property.
    pub execution_config: Option<ExecutionConfig>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
}

/// `UpdateEntitiesWithConditionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpdateEntitiesWithConditionsResponse {
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// response property.
    pub response: Option<serde_json::Value>,
}

/// `GetResourcePostRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetResourcePostRequest {
    /// executionConfig property.
    pub execution_config: Option<ExecutionConfig>,
    /// toolSpec property.
    pub tool_spec: Option<ToolSpec>,
}

/// `AccessCredentials` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessCredentials {
    /// accessToken property.
    pub access_token: Option<String>,
    /// expiresIn property.
    pub expires_in: Option<String>,
    /// refreshToken property.
    pub refresh_token: Option<String>,
}

/// `JsonSchema` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JsonSchema {
    /// additionalDetails property.
    pub additional_details: Option<serde_json::Value>,
    /// default property.
    pub default: Option<serde_json::Value>,
    /// description property.
    pub description: Option<String>,
    /// enum property.
    pub r#enum: Option<Vec<serde_json::Value>>,
    /// exclusiveMaximum property.
    pub exclusive_maximum: Option<bool>,
    /// exclusiveMinimum property.
    pub exclusive_minimum: Option<bool>,
    /// format property.
    pub format: Option<String>,
    /// items property.
    pub items: Option<Box<JsonSchema>>,
    /// jdbcType property.
    pub jdbc_type: Option<String>,
    /// maxItems property.
    pub max_items: Option<i64>,
    /// maxLength property.
    pub max_length: Option<i64>,
    /// maximum property.
    pub maximum: Option<serde_json::Value>,
    /// minItems property.
    pub min_items: Option<i64>,
    /// minLength property.
    pub min_length: Option<i64>,
    /// minimum property.
    pub minimum: Option<serde_json::Value>,
    /// pattern property.
    pub pattern: Option<String>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
    /// required property.
    pub required: Option<Vec<String>>,
    /// type property.
    pub r#type: Option<Vec<String>>,
    /// uniqueItems property.
    pub unique_items: Option<bool>,
}

/// `GetResourceResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetResourceResponse {
    /// _meta property.
    pub meta: Option<serde_json::Value>,
    /// data property.
    pub data: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// mimeType property.
    pub mime_type: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`connectors.projects.locations.connections.checkReadiness_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsCheckReadinessArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`connectors.projects.locations.connections.checkStatus_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsCheckStatusArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
}

/// Arguments for [`connectors.projects.locations.connections.exchangeAuthCode_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsExchangeAuthCodeArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: ExchangeAuthCodeRequest,
}

/// Arguments for [`connectors.projects.locations.connections.executeSqlQuery_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsExecuteSqlQueryArgs {
    /// Path parameter: `connection`.
    pub connection: String,
    /// Request body.
    pub body: ExecuteSqlQueryRequest,
}

/// Arguments for [`connectors.projects.locations.connections.generateConnectionToolspecOverride_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsGenerateConnectionToolspecOverrideArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GenerateCustomToolspecRequest,
}

/// Arguments for [`connectors.projects.locations.connections.listCustomToolNames_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsListCustomToolNamesArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`connectors.projects.locations.connections.refreshAccessToken_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsRefreshAccessTokenArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: RefreshAccessTokenRequest,
}

/// Arguments for [`connectors.projects.locations.connections.tools_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsToolsArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: ListToolsPostRequest,
}

/// Arguments for [`connectors.projects.locations.connections.actions.execute_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsActionsExecuteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: ExecuteActionRequest,
}

/// Arguments for [`connectors.projects.locations.connections.actions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsActionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`connectors.projects.locations.connections.actions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsActionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`connectors.projects.locations.connections.entityTypes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `contextMetadata`.
    pub context_metadata: Option<String>,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`connectors.projects.locations.connections.entityTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`connectors.projects.locations.connections.entityTypes.entities.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
    /// Request body.
    pub body: Entity,
}

/// Arguments for [`connectors.projects.locations.connections.entityTypes.entities.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
}

/// Arguments for [`connectors.projects.locations.connections.entityTypes.entities.deleteEntitiesWithConditions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesDeleteEntitiesWithConditionsArgs
{
    /// Path parameter: `entityType`.
    pub entity_type: String,
    /// Query parameter: `conditions`.
    pub conditions: Option<String>,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
}

/// Arguments for [`connectors.projects.locations.connections.entityTypes.entities.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
}

/// Arguments for [`connectors.projects.locations.connections.entityTypes.entities.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `conditions`.
    pub conditions: Option<String>,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `sortBy`.
    pub sort_by: Option<String>,
    /// Query parameter: `sortOrder`.
    pub sort_order: Option<String>,
}

/// Arguments for [`connectors.projects.locations.connections.entityTypes.entities.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
    /// Request body.
    pub body: Entity,
}

/// Arguments for [`connectors.projects.locations.connections.entityTypes.entities.updateEntitiesWithConditions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesUpdateEntitiesWithConditionsArgs
{
    /// Path parameter: `entityType`.
    pub entity_type: String,
    /// Query parameter: `conditions`.
    pub conditions: Option<String>,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
    /// Request body.
    pub body: Entity,
}

/// Arguments for [`connectors.projects.locations.connections.resources.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsResourcesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
}

/// Arguments for [`connectors.projects.locations.connections.resources.getResourcePost_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsResourcesGetResourcePostArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GetResourcePostRequest,
}

/// Arguments for [`connectors.projects.locations.connections.resources.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsResourcesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`connectors.projects.locations.connections.tools.execute_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsToolsExecuteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: ExecuteToolRequest,
}

/// Arguments for [`connectors.projects.locations.connections.tools.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ConnectorsProjectsLocationsConnectionsToolsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `executionConfig.headers`.
    pub execution_config_headers: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v2/{+name}:checkReadiness
// -----------------------------------------------------------------------------

/// GET v2/{+name}:checkReadiness.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_check_readiness_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_check_readiness_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsCheckReadinessArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckReadinessResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}:checkReadiness",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CheckReadinessResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}:checkStatus
// -----------------------------------------------------------------------------

/// GET v2/{+name}:checkStatus.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_check_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_check_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsCheckStatusArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckStatusResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}:checkStatus",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CheckStatusResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:exchangeAuthCode
// -----------------------------------------------------------------------------

/// POST v2/{+name}:exchangeAuthCode.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_exchange_auth_code_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_exchange_auth_code_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsExchangeAuthCodeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExchangeAuthCodeResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}:exchangeAuthCode",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExchangeAuthCodeResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+connection}:executeSqlQuery
// -----------------------------------------------------------------------------

/// POST v2/{+connection}:executeSqlQuery.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_execute_sql_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_execute_sql_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsExecuteSqlQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExecuteSqlQueryResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}:executeSqlQuery",
        args.connection,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExecuteSqlQueryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:generateConnectionToolspecOverride
// -----------------------------------------------------------------------------

/// POST v2/{+name}:generateConnectionToolspecOverride.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_generate_connection_toolspec_override_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_generate_connection_toolspec_override_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsGenerateConnectionToolspecOverrideArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateCustomToolspecResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}:generateConnectionToolspecOverride",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenerateCustomToolspecResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}:listCustomToolNames
// -----------------------------------------------------------------------------

/// GET v2/{+name}:listCustomToolNames.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_list_custom_tool_names_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_list_custom_tool_names_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsListCustomToolNamesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCustomToolNamesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}:listCustomToolNames",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCustomToolNamesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:refreshAccessToken
// -----------------------------------------------------------------------------

/// POST v2/{+name}:refreshAccessToken.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_refresh_access_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_refresh_access_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsRefreshAccessTokenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RefreshAccessTokenResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}:refreshAccessToken",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RefreshAccessTokenResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/tools
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/tools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_tools_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_tools_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsToolsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListToolsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://connectors.googleapis.com/v2/{}/tools", args.parent,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListToolsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:execute
// -----------------------------------------------------------------------------

/// POST v2/{+name}:execute.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_actions_execute_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_actions_execute_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsActionsExecuteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExecuteActionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://connectors.googleapis.com/v2/{}:execute", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExecuteActionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_actions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_actions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsActionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Action>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://connectors.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Action =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/actions
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/actions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_actions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_actions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsActionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListActionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}/actions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListActionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_entity_types_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_entity_types_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityType>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://connectors.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.context_metadata {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contextMetadata=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/entityTypes
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/entityTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_entity_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_entity_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListEntityTypesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}/entityTypes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListEntityTypesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/entities
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/entities.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_entity_types_entities_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_entity_types_entities_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Entity>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}/entities",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Entity =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2/{+name}
// -----------------------------------------------------------------------------

/// DELETE v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_entity_types_entities_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_entity_types_entities_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://connectors.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+entityType}/entities:deleteEntitiesWithConditions
// -----------------------------------------------------------------------------

/// POST v2/{+entityType}/entities:deleteEntitiesWithConditions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_entity_types_entities_delete_entities_with_conditions_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesDeleteEntitiesWithConditionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}/entities:deleteEntitiesWithConditions",
        args.entity_type,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.conditions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("conditions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_entity_types_entities_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_entity_types_entities_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Entity>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://connectors.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Entity =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/entities
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/entities.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_entity_types_entities_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_entity_types_entities_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListEntitiesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}/entities",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.conditions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("conditions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sortBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort_order {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sortOrder=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListEntitiesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+name}
// -----------------------------------------------------------------------------

/// PATCH v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_entity_types_entities_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_entity_types_entities_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Entity>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://connectors.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Entity =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+entityType}/entities:updateEntitiesWithConditions
// -----------------------------------------------------------------------------

/// POST v2/{+entityType}/entities:updateEntitiesWithConditions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_entity_types_entities_update_entities_with_conditions_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsEntityTypesEntitiesUpdateEntitiesWithConditionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<UpdateEntitiesWithConditionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}/entities:updateEntitiesWithConditions",
        args.entity_type,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.conditions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("conditions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UpdateEntitiesWithConditionsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_resources_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_resources_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsResourcesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetResourceResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://connectors.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetResourceResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}
// -----------------------------------------------------------------------------

/// POST v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_resources_get_resource_post_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_resources_get_resource_post_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsResourcesGetResourcePostArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetResourceResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://connectors.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetResourceResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/resources
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/resources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_resources_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_resources_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsResourcesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListResourcesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://connectors.googleapis.com/v2/{}/resources",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListResourcesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:execute
// -----------------------------------------------------------------------------

/// POST v2/{+name}:execute.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_tools_execute_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_tools_execute_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsToolsExecuteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExecuteToolResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://connectors.googleapis.com/v2/{}:execute", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExecuteToolResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/tools
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/tools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = connectors_projects_locations_connections_tools_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn connectors_projects_locations_connections_tools_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ConnectorsProjectsLocationsConnectionsToolsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListToolsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://connectors.googleapis.com/v2/{}/tools", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.execution_config_headers {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("executionConfig_headers=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListToolsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
