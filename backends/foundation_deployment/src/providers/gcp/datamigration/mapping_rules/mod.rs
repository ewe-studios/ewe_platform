//! Auto-generated API module for gcp/datamigration mappingRules.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_datamigration_mapping_rules `

#![cfg(feature = "gcp_datamigration_mapping_rules")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SinglePackageChange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SinglePackageChange {
    /// packageBody property.
    pub package_body: Option<String>,
    /// packageDescription property.
    pub package_description: Option<String>,
}

/// `ListMappingRulesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListMappingRulesResponse {
    /// mappingRules property.
    pub mapping_rules: Option<Vec<MappingRule>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `FilterTableColumns` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FilterTableColumns {
    /// excludeColumns property.
    pub exclude_columns: Option<Vec<String>>,
    /// includeColumns property.
    pub include_columns: Option<Vec<String>>,
}

/// `MultiColumnDatatypeChange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MultiColumnDatatypeChange {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// newDataType property.
    pub new_data_type: Option<String>,
    /// overrideFractionalSecondsPrecision property.
    pub override_fractional_seconds_precision: Option<i64>,
    /// overrideLength property.
    pub override_length: Option<String>,
    /// overridePrecision property.
    pub override_precision: Option<i64>,
    /// overrideScale property.
    pub override_scale: Option<i64>,
    /// sourceDataTypeFilter property.
    pub source_data_type_filter: Option<String>,
    /// sourceNumericFilter property.
    pub source_numeric_filter: Option<SourceNumericFilter>,
    /// sourceTextFilter property.
    pub source_text_filter: Option<SourceTextFilter>,
}

/// `SingleColumnChange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SingleColumnChange {
    /// array property.
    pub array: Option<bool>,
    /// arrayLength property.
    pub array_length: Option<i64>,
    /// autoGenerated property.
    pub auto_generated: Option<bool>,
    /// charset property.
    pub charset: Option<String>,
    /// collation property.
    pub collation: Option<String>,
    /// comment property.
    pub comment: Option<String>,
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// dataType property.
    pub data_type: Option<String>,
    /// fractionalSecondsPrecision property.
    pub fractional_seconds_precision: Option<i64>,
    /// length property.
    pub length: Option<String>,
    /// nullable property.
    pub nullable: Option<bool>,
    /// precision property.
    pub precision: Option<i64>,
    /// scale property.
    pub scale: Option<i64>,
    /// setValues property.
    pub set_values: Option<Vec<String>>,
    /// udt property.
    pub udt: Option<bool>,
}

/// `ConditionalColumnSetValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConditionalColumnSetValue {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// sourceNumericFilter property.
    pub source_numeric_filter: Option<SourceNumericFilter>,
    /// sourceTextFilter property.
    pub source_text_filter: Option<SourceTextFilter>,
    /// valueTransformation property.
    pub value_transformation: Option<ValueTransformation>,
}

/// `MappingRule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MappingRule {
    /// conditionalColumnSetValue property.
    pub conditional_column_set_value: Option<ConditionalColumnSetValue>,
    /// convertRowidColumn property.
    pub convert_rowid_column: Option<ConvertRowIdToColumn>,
    /// displayName property.
    pub display_name: Option<String>,
    /// entityMove property.
    pub entity_move: Option<EntityMove>,
    /// filter property.
    pub filter: Option<MappingRuleFilter>,
    /// filterTableColumns property.
    pub filter_table_columns: Option<FilterTableColumns>,
    /// multiColumnDataTypeChange property.
    pub multi_column_data_type_change: Option<MultiColumnDatatypeChange>,
    /// multiEntityRename property.
    pub multi_entity_rename: Option<MultiEntityRename>,
    /// name property.
    pub name: Option<String>,
    /// revisionCreateTime property.
    pub revision_create_time: Option<String>,
    /// revisionId property.
    pub revision_id: Option<String>,
    /// ruleOrder property.
    pub rule_order: Option<String>,
    /// ruleScope property.
    pub rule_scope: Option<String>,
    /// setTablePrimaryKey property.
    pub set_table_primary_key: Option<SetTablePrimaryKey>,
    /// singleColumnChange property.
    pub single_column_change: Option<SingleColumnChange>,
    /// singleEntityRename property.
    pub single_entity_rename: Option<SingleEntityRename>,
    /// singlePackageChange property.
    pub single_package_change: Option<SinglePackageChange>,
    /// sourceSqlChange property.
    pub source_sql_change: Option<SourceSqlChange>,
    /// state property.
    pub state: Option<String>,
}

/// `MappingRuleFilter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MappingRuleFilter {
    /// entities property.
    pub entities: Option<Vec<String>>,
    /// entityNameContains property.
    pub entity_name_contains: Option<String>,
    /// entityNamePrefix property.
    pub entity_name_prefix: Option<String>,
    /// entityNameSuffix property.
    pub entity_name_suffix: Option<String>,
    /// parentEntity property.
    pub parent_entity: Option<String>,
}

/// `SingleEntityRename` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SingleEntityRename {
    /// newName property.
    pub new_name: Option<String>,
}

/// `SourceSqlChange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SourceSqlChange {
    /// sqlCode property.
    pub sql_code: Option<String>,
}

/// `ConvertRowIdToColumn` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConvertRowIdToColumn {
    /// onlyIfNoPrimaryKey property.
    pub only_if_no_primary_key: Option<bool>,
}

/// `ValueTransformation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ValueTransformation {
    /// applyHash property.
    pub apply_hash: Option<ApplyHash>,
    /// assignMaxValue property.
    pub assign_max_value: Option<Empty>,
    /// assignMinValue property.
    pub assign_min_value: Option<Empty>,
    /// assignNull property.
    pub assign_null: Option<Empty>,
    /// assignSpecificValue property.
    pub assign_specific_value: Option<AssignSpecificValue>,
    /// doubleComparison property.
    pub double_comparison: Option<DoubleComparisonFilter>,
    /// intComparison property.
    pub int_comparison: Option<IntComparisonFilter>,
    /// isNull property.
    pub is_null: Option<Empty>,
    /// roundScale property.
    pub round_scale: Option<RoundToScale>,
    /// valueList property.
    pub value_list: Option<ValueListFilter>,
}

/// `EntityMove` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntityMove {
    /// newSchema property.
    pub new_schema: Option<String>,
}

/// `ApplyHash` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApplyHash {
    /// uuidFromBytes property.
    pub uuid_from_bytes: Option<Empty>,
}

/// `SourceNumericFilter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SourceNumericFilter {
    /// numericFilterOption property.
    pub numeric_filter_option: Option<String>,
    /// sourceMaxPrecisionFilter property.
    pub source_max_precision_filter: Option<i64>,
    /// sourceMaxScaleFilter property.
    pub source_max_scale_filter: Option<i64>,
    /// sourceMinPrecisionFilter property.
    pub source_min_precision_filter: Option<i64>,
    /// sourceMinScaleFilter property.
    pub source_min_scale_filter: Option<i64>,
}

/// `SetTablePrimaryKey` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SetTablePrimaryKey {
    /// primaryKey property.
    pub primary_key: Option<String>,
    /// primaryKeyColumns property.
    pub primary_key_columns: Option<Vec<String>>,
}

/// `AssignSpecificValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AssignSpecificValue {
    /// value property.
    pub value: Option<String>,
}

/// `MultiEntityRename` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MultiEntityRename {
    /// newNamePattern property.
    pub new_name_pattern: Option<String>,
    /// sourceNameTransformation property.
    pub source_name_transformation: Option<String>,
}

/// `IntComparisonFilter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IntComparisonFilter {
    /// value property.
    pub value: Option<String>,
    /// valueComparison property.
    pub value_comparison: Option<String>,
}

/// `RoundToScale` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RoundToScale {
    /// scale property.
    pub scale: Option<i64>,
}

/// `ValueListFilter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ValueListFilter {
    /// ignoreCase property.
    pub ignore_case: Option<bool>,
    /// valuePresentList property.
    pub value_present_list: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `SourceTextFilter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SourceTextFilter {
    /// sourceMaxLengthFilter property.
    pub source_max_length_filter: Option<String>,
    /// sourceMinLengthFilter property.
    pub source_min_length_filter: Option<String>,
}

/// `DoubleComparisonFilter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DoubleComparisonFilter {
    /// value property.
    pub value: Option<f64>,
    /// valueComparison property.
    pub value_comparison: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.mappingRules.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesMappingRulesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `mappingRuleId`.
    pub mapping_rule_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.mappingRules.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesMappingRulesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.mappingRules.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesMappingRulesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.mappingRules.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesMappingRulesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}/mappingRules
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}/mappingRules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_mapping_rules_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_mapping_rules_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MappingRule>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}/mappingRules",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: MappingRule =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}/mappingRules/{mappingRulesId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}/mappingRules/{mappingRulesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_mapping_rules_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_mapping_rules_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}/mappingRules/{{mappingRulesId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}/mappingRules/{mappingRulesId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}/mappingRules/{mappingRulesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_mapping_rules_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_mapping_rules_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MappingRule>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}/mappingRules/{{mappingRulesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: MappingRule =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}/mappingRules
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}/mappingRules.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_mapping_rules_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_mapping_rules_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMappingRulesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}/mappingRules",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListMappingRulesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
