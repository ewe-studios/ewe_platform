//! Auto-generated API module for gcp/datamigration conversionWorkspaces.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_datamigration_conversion_workspaces `

#![cfg(feature = "gcp_datamigration_conversion_workspaces")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;
use super::shared::Policy;
use super::shared::TestIamPermissionsResponse;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `DatabaseEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabaseEntity {
    /// database property.
    pub database: Option<DatabaseInstanceEntity>,
    /// databaseFunction property.
    pub database_function: Option<FunctionEntity>,
    /// databasePackage property.
    pub database_package: Option<PackageEntity>,
    /// entityDdl property.
    pub entity_ddl: Option<Vec<EntityDdl>>,
    /// entityType property.
    pub entity_type: Option<String>,
    /// issues property.
    pub issues: Option<Vec<EntityIssue>>,
    /// mappings property.
    pub mappings: Option<Vec<EntityMapping>>,
    /// materializedView property.
    pub materialized_view: Option<MaterializedViewEntity>,
    /// parentEntity property.
    pub parent_entity: Option<String>,
    /// schema property.
    pub schema: Option<SchemaEntity>,
    /// sequence property.
    pub sequence: Option<SequenceEntity>,
    /// shortName property.
    pub short_name: Option<String>,
    /// storedProcedure property.
    pub stored_procedure: Option<StoredProcedureEntity>,
    /// synonym property.
    pub synonym: Option<SynonymEntity>,
    /// table property.
    pub table: Option<TableEntity>,
    /// tree property.
    pub tree: Option<String>,
    /// udt property.
    pub udt: Option<UDTEntity>,
    /// view property.
    pub view: Option<ViewEntity>,
}

/// `UDTEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UDTEntity {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// udtBody property.
    pub udt_body: Option<String>,
    /// udtSqlCode property.
    pub udt_sql_code: Option<String>,
}

/// `AuditLogConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditLogConfig {
    /// exemptedMembers property.
    pub exempted_members: Option<Vec<String>>,
    /// logType property.
    pub log_type: Option<String>,
}

/// `EntityMappingLogEntry` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntityMappingLogEntry {
    /// mappingComment property.
    pub mapping_comment: Option<String>,
    /// ruleId property.
    pub rule_id: Option<String>,
    /// ruleRevisionId property.
    pub rule_revision_id: Option<String>,
}

/// `EntityDdl` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntityDdl {
    /// ddl property.
    pub ddl: Option<String>,
    /// ddlKind property.
    pub ddl_kind: Option<String>,
    /// ddlType property.
    pub ddl_type: Option<String>,
    /// editedDdlKind property.
    pub edited_ddl_kind: Option<String>,
    /// entity property.
    pub entity: Option<String>,
    /// entityType property.
    pub entity_type: Option<String>,
    /// issueId property.
    pub issue_id: Option<Vec<String>>,
}

/// `SequenceEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SequenceEntity {
    /// cache property.
    pub cache: Option<String>,
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// cycle property.
    pub cycle: Option<bool>,
    /// increment property.
    pub increment: Option<String>,
    /// maxValue property.
    pub max_value: Option<String>,
    /// minValue property.
    pub min_value: Option<String>,
    /// startValue property.
    pub start_value: Option<String>,
}

/// `SynonymEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SynonymEntity {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// sourceEntity property.
    pub source_entity: Option<String>,
    /// sourceType property.
    pub source_type: Option<String>,
}

/// `ListConversionWorkspacesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListConversionWorkspacesResponse {
    /// conversionWorkspaces property.
    pub conversion_workspaces: Option<Vec<ConversionWorkspace>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `DatabaseInstanceEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabaseInstanceEntity {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
}

/// `FunctionEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FunctionEntity {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// sqlCode property.
    pub sql_code: Option<String>,
}

/// `EntityMapping` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntityMapping {
    /// draftEntity property.
    pub draft_entity: Option<String>,
    /// draftType property.
    pub draft_type: Option<String>,
    /// mappingLog property.
    pub mapping_log: Option<Vec<EntityMappingLogEntry>>,
    /// sourceEntity property.
    pub source_entity: Option<String>,
    /// sourceType property.
    pub source_type: Option<String>,
}

/// `TriggerEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TriggerEntity {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// sqlCode property.
    pub sql_code: Option<String>,
    /// triggerType property.
    pub trigger_type: Option<String>,
    /// triggeringEvents property.
    pub triggering_events: Option<Vec<String>>,
}

/// `DescribeDatabaseEntitiesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DescribeDatabaseEntitiesResponse {
    /// databaseEntities property.
    pub database_entities: Option<Vec<DatabaseEntity>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ConversionWorkspace` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConversionWorkspace {
    /// createTime property.
    pub create_time: Option<String>,
    /// destination property.
    pub destination: Option<DatabaseEngineInfo>,
    /// destinationProvider property.
    pub destination_provider: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// globalSettings property.
    pub global_settings: Option<serde_json::Value>,
    /// hasUncommittedChanges property.
    pub has_uncommitted_changes: Option<bool>,
    /// latestCommitId property.
    pub latest_commit_id: Option<String>,
    /// latestCommitTime property.
    pub latest_commit_time: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// source property.
    pub source: Option<DatabaseEngineInfo>,
    /// sourceProvider property.
    pub source_provider: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `SchemaEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SchemaEntity {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
}

/// `PackageEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PackageEntity {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// packageBody property.
    pub package_body: Option<String>,
    /// packageSqlCode property.
    pub package_sql_code: Option<String>,
}

/// `ApplyJobDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ApplyJobDetails {
    /// connectionProfile property.
    pub connection_profile: Option<String>,
    /// filter property.
    pub filter: Option<String>,
}

/// `StoredProcedureEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StoredProcedureEntity {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// sqlCode property.
    pub sql_code: Option<String>,
}

/// `EntityIssue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntityIssue {
    /// code property.
    pub code: Option<String>,
    /// ddl property.
    pub ddl: Option<String>,
    /// entityType property.
    pub entity_type: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// message property.
    pub message: Option<String>,
    /// position property.
    pub position: Option<Position>,
    /// severity property.
    pub severity: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Position` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Position {
    /// column property.
    pub column: Option<i64>,
    /// length property.
    pub length: Option<i64>,
    /// line property.
    pub line: Option<i64>,
    /// offset property.
    pub offset: Option<i64>,
}

/// `DescribeConversionWorkspaceRevisionsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DescribeConversionWorkspaceRevisionsResponse {
    /// revisions property.
    pub revisions: Option<Vec<ConversionWorkspace>>,
}

/// `ImportRulesJobDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ImportRulesJobDetails {
    /// fileFormat property.
    pub file_format: Option<String>,
    /// files property.
    pub files: Option<Vec<String>>,
}

/// `DatabaseEngineInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabaseEngineInfo {
    /// engine property.
    pub engine: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `TableEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TableEntity {
    /// columns property.
    pub columns: Option<Vec<ColumnEntity>>,
    /// comment property.
    pub comment: Option<String>,
    /// constraints property.
    pub constraints: Option<Vec<ConstraintEntity>>,
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// indices property.
    pub indices: Option<Vec<IndexEntity>>,
    /// triggers property.
    pub triggers: Option<Vec<TriggerEntity>>,
}

/// `ConstraintEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConstraintEntity {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// referenceColumns property.
    pub reference_columns: Option<Vec<String>>,
    /// referenceTable property.
    pub reference_table: Option<String>,
    /// tableColumns property.
    pub table_columns: Option<Vec<String>>,
    /// tableName property.
    pub table_name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `SearchBackgroundJobsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchBackgroundJobsResponse {
    /// jobs property.
    pub jobs: Option<Vec<BackgroundJobLogEntry>>,
}

/// `BackgroundJobLogEntry` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackgroundJobLogEntry {
    /// applyJobDetails property.
    pub apply_job_details: Option<ApplyJobDetails>,
    /// completionComment property.
    pub completion_comment: Option<String>,
    /// completionState property.
    pub completion_state: Option<String>,
    /// convertJobDetails property.
    pub convert_job_details: Option<ConvertJobDetails>,
    /// finishTime property.
    pub finish_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// importRulesJobDetails property.
    pub import_rules_job_details: Option<ImportRulesJobDetails>,
    /// jobType property.
    pub job_type: Option<String>,
    /// requestAutocommit property.
    pub request_autocommit: Option<bool>,
    /// seedJobDetails property.
    pub seed_job_details: Option<SeedJobDetails>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `IndexEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IndexEntity {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// tableColumns property.
    pub table_columns: Option<Vec<String>>,
    /// tableColumnsDescending property.
    pub table_columns_descending: Option<Vec<bool>>,
    /// type property.
    pub r#type: Option<String>,
    /// unique property.
    pub unique: Option<bool>,
}

/// `AuditConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditConfig {
    /// auditLogConfigs property.
    pub audit_log_configs: Option<Vec<AuditLogConfig>>,
    /// service property.
    pub service: Option<String>,
}

/// `ColumnEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ColumnEntity {
    /// array property.
    pub array: Option<bool>,
    /// arrayLength property.
    pub array_length: Option<i64>,
    /// autoGenerated property.
    pub auto_generated: Option<bool>,
    /// charset property.
    pub charset: Option<String>,
    /// collation property.
    pub collation: Option<String>,
    /// comment property.
    pub comment: Option<String>,
    /// computed property.
    pub computed: Option<bool>,
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// dataType property.
    pub data_type: Option<String>,
    /// defaultValue property.
    pub default_value: Option<String>,
    /// fractionalSecondsPrecision property.
    pub fractional_seconds_precision: Option<i64>,
    /// length property.
    pub length: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nullable property.
    pub nullable: Option<bool>,
    /// ordinalPosition property.
    pub ordinal_position: Option<i64>,
    /// precision property.
    pub precision: Option<i64>,
    /// scale property.
    pub scale: Option<i64>,
    /// setValues property.
    pub set_values: Option<Vec<String>>,
    /// udt property.
    pub udt: Option<bool>,
}

/// `ViewEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ViewEntity {
    /// constraints property.
    pub constraints: Option<Vec<ConstraintEntity>>,
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// sqlCode property.
    pub sql_code: Option<String>,
}

/// `Binding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `Expr` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `SeedJobDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SeedJobDetails {
    /// connectionProfile property.
    pub connection_profile: Option<String>,
}

/// `ConvertJobDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConvertJobDetails {
    /// filter property.
    pub filter: Option<String>,
}

/// `MaterializedViewEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MaterializedViewEntity {
    /// customFeatures property.
    pub custom_features: Option<serde_json::Value>,
    /// indices property.
    pub indices: Option<Vec<IndexEntity>>,
    /// sqlCode property.
    pub sql_code: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.apply_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesApplyArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.commit_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesCommitArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.convert_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesConvertArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `conversionWorkspaceId`.
    pub conversion_workspace_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.describeConversionWorkspaceRevisions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesDescribeConversionWorkspaceRevisionsArgs
{
    /// Path parameter: `conversionWorkspace`.
    pub conversion_workspace: String,
    /// Query parameter: `commitId`.
    pub commit_id: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.describeDatabaseEntities_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesDescribeDatabaseEntitiesArgs {
    /// Path parameter: `conversionWorkspace`.
    pub conversion_workspace: String,
    /// Query parameter: `commitId`.
    pub commit_id: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `tree`.
    pub tree: Option<String>,
    /// Query parameter: `uncommitted`.
    pub uncommitted: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.getIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `options.requestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.rollback_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesRollbackArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.searchBackgroundJobs_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesSearchBackgroundJobsArgs {
    /// Path parameter: `conversionWorkspace`.
    pub conversion_workspace: String,
    /// Query parameter: `completedUntilTime`.
    pub completed_until_time: Option<String>,
    /// Query parameter: `maxSize`.
    pub max_size: Option<String>,
    /// Query parameter: `returnMostRecentPerJobType`.
    pub return_most_recent_per_job_type: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.seed_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesSeedArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.setIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`datamigration.projects.locations.conversionWorkspaces.testIamPermissions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConversionWorkspacesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:apply
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:apply.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_apply_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_apply_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}:apply",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:commit
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:commit.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_commit_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_commit_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}:commit",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:convert
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:convert.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_convert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_convert_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}:convert",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:describeConversionWorkspaceRevisions
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:describeConversionWorkspaceRevisions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_describe_conversion_workspace_revisions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_describe_conversion_workspace_revisions_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<DescribeConversionWorkspaceRevisionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}:describeConversionWorkspaceRevisions",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DescribeConversionWorkspaceRevisionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:describeDatabaseEntities
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:describeDatabaseEntities.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_describe_database_entities_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_describe_database_entities_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DescribeDatabaseEntitiesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}:describeDatabaseEntities",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DescribeDatabaseEntitiesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionWorkspace>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConversionWorkspace =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:getIamPolicy
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}:getIamPolicy",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConversionWorkspacesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListConversionWorkspacesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:rollback
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:rollback.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_rollback_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_rollback_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}:rollback",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:searchBackgroundJobs
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:searchBackgroundJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_search_background_jobs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_search_background_jobs_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchBackgroundJobsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}:searchBackgroundJobs",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchBackgroundJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:seed
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:seed.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_seed_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_seed_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}:seed",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}:setIamPolicy",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/conversionWorkspaces/{conversionWorkspacesId}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_conversion_workspaces_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_conversion_workspaces_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/conversionWorkspaces/{{conversionWorkspacesId}}:testIamPermissions",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
