//! Auto-generated API module for gcp/datamigration connectionProfiles.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_datamigration_connection_profiles `

#![cfg(feature = "gcp_datamigration_connection_profiles")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;
use super::shared::Policy;
use super::shared::TestIamPermissionsResponse;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `AuditConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditConfig {
    /// auditLogConfigs property.
    pub audit_log_configs: Option<Vec<AuditLogConfig>>,
    /// service property.
    pub service: Option<String>,
}

/// `AuthorizedNetwork` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuthorizedNetwork {
    /// cidrRange property.
    pub cidr_range: Option<String>,
}

/// `CloudSqlConnectionProfile` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CloudSqlConnectionProfile {
    /// additionalPublicIp property.
    pub additional_public_ip: Option<String>,
    /// cloudSqlId property.
    pub cloud_sql_id: Option<String>,
    /// privateIp property.
    pub private_ip: Option<String>,
    /// publicIp property.
    pub public_ip: Option<String>,
    /// settings property.
    pub settings: Option<CloudSqlSettings>,
}

/// `AlloyDbConnectionProfile` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AlloyDbConnectionProfile {
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// settings property.
    pub settings: Option<AlloyDbSettings>,
}

/// `StaticIpConnectivity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StaticIpConnectivity {}

/// `PostgreSqlConnectionProfile` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PostgreSqlConnectionProfile {
    /// alloydbClusterId property.
    pub alloydb_cluster_id: Option<String>,
    /// cloudSqlId property.
    pub cloud_sql_id: Option<String>,
    /// database property.
    pub database: Option<String>,
    /// enableIamAuthentication property.
    pub enable_iam_authentication: Option<bool>,
    /// forwardSshConnectivity property.
    pub forward_ssh_connectivity: Option<ForwardSshTunnelConnectivity>,
    /// host property.
    pub host: Option<String>,
    /// networkArchitecture property.
    pub network_architecture: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// passwordSet property.
    pub password_set: Option<bool>,
    /// port property.
    pub port: Option<i64>,
    /// privateConnectivity property.
    pub private_connectivity: Option<PrivateConnectivity>,
    /// privateServiceConnectConnectivity property.
    pub private_service_connect_connectivity: Option<PrivateServiceConnectConnectivity>,
    /// ssl property.
    pub ssl: Option<SslConfig>,
    /// staticIpConnectivity property.
    pub static_ip_connectivity: Option<StaticIpConnectivity>,
    /// username property.
    pub username: Option<String>,
}

/// `SqlServerConnectionProfile` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SqlServerConnectionProfile {
    /// backups property.
    pub backups: Option<SqlServerBackups>,
    /// cloudSqlId property.
    pub cloud_sql_id: Option<String>,
    /// cloudSqlProjectId property.
    pub cloud_sql_project_id: Option<String>,
    /// database property.
    pub database: Option<String>,
    /// dbmPort property.
    pub dbm_port: Option<i64>,
    /// forwardSshConnectivity property.
    pub forward_ssh_connectivity: Option<ForwardSshTunnelConnectivity>,
    /// host property.
    pub host: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// passwordSet property.
    pub password_set: Option<bool>,
    /// port property.
    pub port: Option<i64>,
    /// privateConnectivity property.
    pub private_connectivity: Option<PrivateConnectivity>,
    /// privateServiceConnectConnectivity property.
    pub private_service_connect_connectivity: Option<PrivateServiceConnectConnectivity>,
    /// ssl property.
    pub ssl: Option<SslConfig>,
    /// staticIpConnectivity property.
    pub static_ip_connectivity: Option<StaticIpConnectivity>,
    /// username property.
    pub username: Option<String>,
}

/// `ForwardSshTunnelConnectivity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ForwardSshTunnelConnectivity {
    /// hostname property.
    pub hostname: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// privateKey property.
    pub private_key: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `SqlIpConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SqlIpConfig {
    /// allocatedIpRange property.
    pub allocated_ip_range: Option<String>,
    /// authorizedNetworks property.
    pub authorized_networks: Option<Vec<SqlAclEntry>>,
    /// enableIpv4 property.
    pub enable_ipv4: Option<bool>,
    /// privateNetwork property.
    pub private_network: Option<String>,
    /// requireSsl property.
    pub require_ssl: Option<bool>,
}

/// `OracleConnectionProfile` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OracleConnectionProfile {
    /// databaseService property.
    pub database_service: Option<String>,
    /// forwardSshConnectivity property.
    pub forward_ssh_connectivity: Option<ForwardSshTunnelConnectivity>,
    /// host property.
    pub host: Option<String>,
    /// oracleAsmConfig property.
    pub oracle_asm_config: Option<OracleAsmConfig>,
    /// password property.
    pub password: Option<String>,
    /// passwordSet property.
    pub password_set: Option<bool>,
    /// port property.
    pub port: Option<i64>,
    /// privateConnectivity property.
    pub private_connectivity: Option<PrivateConnectivity>,
    /// ssl property.
    pub ssl: Option<SslConfig>,
    /// staticServiceIpConnectivity property.
    pub static_service_ip_connectivity: Option<StaticServiceIpConnectivity>,
    /// username property.
    pub username: Option<String>,
}

/// `DataCacheConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DataCacheConfig {
    /// dataCacheEnabled property.
    pub data_cache_enabled: Option<bool>,
}

/// `UserPassword` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UserPassword {
    /// password property.
    pub password: Option<String>,
    /// passwordSet property.
    pub password_set: Option<bool>,
    /// user property.
    pub user: Option<String>,
}

/// `AlloyDbSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AlloyDbSettings {
    /// databaseVersion property.
    pub database_version: Option<String>,
    /// encryptionConfig property.
    pub encryption_config: Option<EncryptionConfig>,
    /// initialUser property.
    pub initial_user: Option<UserPassword>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// primaryInstanceSettings property.
    pub primary_instance_settings: Option<PrimaryInstanceSettings>,
    /// vpcNetwork property.
    pub vpc_network: Option<String>,
}

/// `ListConnectionProfilesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListConnectionProfilesResponse {
    /// connectionProfiles property.
    pub connection_profiles: Option<Vec<ConnectionProfile>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `SqlServerBackups` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SqlServerBackups {
    /// gcsBucket property.
    pub gcs_bucket: Option<String>,
    /// gcsPrefix property.
    pub gcs_prefix: Option<String>,
}

/// `OracleAsmConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OracleAsmConfig {
    /// asmService property.
    pub asm_service: Option<String>,
    /// hostname property.
    pub hostname: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// passwordSet property.
    pub password_set: Option<bool>,
    /// port property.
    pub port: Option<i64>,
    /// ssl property.
    pub ssl: Option<SslConfig>,
    /// username property.
    pub username: Option<String>,
}

/// `MachineConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MachineConfig {
    /// cpuCount property.
    pub cpu_count: Option<i64>,
    /// machineType property.
    pub machine_type: Option<String>,
}

/// `SslConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SslConfig {
    /// caCertificate property.
    pub ca_certificate: Option<String>,
    /// clientCertificate property.
    pub client_certificate: Option<String>,
    /// clientKey property.
    pub client_key: Option<String>,
    /// sslFlags property.
    pub ssl_flags: Option<serde_json::Value>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Binding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `SqlAclEntry` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SqlAclEntry {
    /// expireTime property.
    pub expire_time: Option<String>,
    /// label property.
    pub label: Option<String>,
    /// ttl property.
    pub ttl: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `CloudSqlSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CloudSqlSettings {
    /// activationPolicy property.
    pub activation_policy: Option<String>,
    /// autoStorageIncrease property.
    pub auto_storage_increase: Option<bool>,
    /// availabilityType property.
    pub availability_type: Option<String>,
    /// cmekKeyName property.
    pub cmek_key_name: Option<String>,
    /// collation property.
    pub collation: Option<String>,
    /// dataCacheConfig property.
    pub data_cache_config: Option<DataCacheConfig>,
    /// dataDiskProvisionedIops property.
    pub data_disk_provisioned_iops: Option<String>,
    /// dataDiskProvisionedThroughput property.
    pub data_disk_provisioned_throughput: Option<String>,
    /// dataDiskSizeGb property.
    pub data_disk_size_gb: Option<String>,
    /// dataDiskType property.
    pub data_disk_type: Option<String>,
    /// databaseFlags property.
    pub database_flags: Option<serde_json::Value>,
    /// databaseVersion property.
    pub database_version: Option<String>,
    /// databaseVersionName property.
    pub database_version_name: Option<String>,
    /// edition property.
    pub edition: Option<String>,
    /// ipConfig property.
    pub ip_config: Option<SqlIpConfig>,
    /// rootPassword property.
    pub root_password: Option<String>,
    /// rootPasswordSet property.
    pub root_password_set: Option<bool>,
    /// secondaryZone property.
    pub secondary_zone: Option<String>,
    /// sourceId property.
    pub source_id: Option<String>,
    /// storageAutoResizeLimit property.
    pub storage_auto_resize_limit: Option<String>,
    /// tier property.
    pub tier: Option<String>,
    /// userLabels property.
    pub user_labels: Option<serde_json::Value>,
    /// zone property.
    pub zone: Option<String>,
}

/// `EncryptionConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EncryptionConfig {
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
}

/// `Expr` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `PrimaryInstanceSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PrimaryInstanceSettings {
    /// databaseFlags property.
    pub database_flags: Option<serde_json::Value>,
    /// id property.
    pub id: Option<String>,
    /// instanceNetworkConfig property.
    pub instance_network_config: Option<InstanceNetworkConfig>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// machineConfig property.
    pub machine_config: Option<MachineConfig>,
    /// outboundPublicIpAddresses property.
    pub outbound_public_ip_addresses: Option<Vec<String>>,
    /// privateIp property.
    pub private_ip: Option<String>,
}

/// `InstanceNetworkConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstanceNetworkConfig {
    /// authorizedExternalNetworks property.
    pub authorized_external_networks: Option<Vec<AuthorizedNetwork>>,
    /// enableOutboundPublicIp property.
    pub enable_outbound_public_ip: Option<bool>,
    /// enablePublicIp property.
    pub enable_public_ip: Option<bool>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `MySqlConnectionProfile` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MySqlConnectionProfile {
    /// cloudSqlId property.
    pub cloud_sql_id: Option<String>,
    /// host property.
    pub host: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// passwordSet property.
    pub password_set: Option<bool>,
    /// port property.
    pub port: Option<i64>,
    /// ssl property.
    pub ssl: Option<SslConfig>,
    /// username property.
    pub username: Option<String>,
}

/// `PrivateConnectivity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PrivateConnectivity {
    /// privateConnection property.
    pub private_connection: Option<String>,
}

/// `PrivateServiceConnectConnectivity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PrivateServiceConnectConnectivity {
    /// serviceAttachment property.
    pub service_attachment: Option<String>,
}

/// `StaticServiceIpConnectivity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StaticServiceIpConnectivity {}

/// `AuditLogConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditLogConfig {
    /// exemptedMembers property.
    pub exempted_members: Option<Vec<String>>,
    /// logType property.
    pub log_type: Option<String>,
}

/// `ConnectionProfile` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConnectionProfile {
    /// alloydb property.
    pub alloydb: Option<AlloyDbConnectionProfile>,
    /// cloudsql property.
    pub cloudsql: Option<CloudSqlConnectionProfile>,
    /// createTime property.
    pub create_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// error property.
    pub error: Option<Status>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// mysql property.
    pub mysql: Option<MySqlConnectionProfile>,
    /// name property.
    pub name: Option<String>,
    /// oracle property.
    pub oracle: Option<OracleConnectionProfile>,
    /// postgresql property.
    pub postgresql: Option<PostgreSqlConnectionProfile>,
    /// provider property.
    pub provider: Option<String>,
    /// role property.
    pub role: Option<String>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// sqlserver property.
    pub sqlserver: Option<SqlServerConnectionProfile>,
    /// state property.
    pub state: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`datamigration.projects.locations.connectionProfiles.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConnectionProfilesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `connectionProfileId`.
    pub connection_profile_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `skipValidation`.
    pub skip_validation: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.connectionProfiles.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConnectionProfilesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.connectionProfiles.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConnectionProfilesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datamigration.projects.locations.connectionProfiles.getIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConnectionProfilesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `options.requestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.connectionProfiles.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConnectionProfilesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.connectionProfiles.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConnectionProfilesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `skipValidation`.
    pub skip_validation: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`datamigration.projects.locations.connectionProfiles.setIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConnectionProfilesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`datamigration.projects.locations.connectionProfiles.testIamPermissions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatamigrationProjectsLocationsConnectionProfilesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_connection_profiles_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_connection_profiles_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connectionProfiles",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_connection_profiles_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_connection_profiles_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connectionProfiles/{{connectionProfilesId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_connection_profiles_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_connection_profiles_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConnectionProfile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connectionProfiles/{{connectionProfilesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConnectionProfile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}:getIamPolicy
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_connection_profiles_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_connection_profiles_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connectionProfiles/{{connectionProfilesId}}:getIamPolicy",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_connection_profiles_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_connection_profiles_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConnectionProfilesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connectionProfiles",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListConnectionProfilesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_connection_profiles_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_connection_profiles_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connectionProfiles/{{connectionProfilesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_connection_profiles_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_connection_profiles_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connectionProfiles/{{connectionProfilesId}}:setIamPolicy",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/connectionProfiles/{connectionProfilesId}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datamigration_projects_locations_connection_profiles_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datamigration_projects_locations_connection_profiles_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datamigration.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connectionProfiles/{{connectionProfilesId}}:testIamPermissions",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
