//! Auto-generated resource types for Google Cloud Platform - domains.
//!
//! This file is generated by `cargo run --bin ewe_platform gen_resource_types`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use super::*;
use serde::{Deserialize, Serialize};

/// Specifies the audit configuration for a service. The configuration determines which permission types are logged, and what identities, if any, are exempted from logging. An AuditConfig must have one or more AuditLogConfigs. If there are AuditConfigs for both allServices and a specific service, the union of the two AuditConfigs is used for that service: the log_types specified in each AuditConfig are enabled, and the exempted_members in each AuditLogConfig are exempted. Example Policy with multiple AuditConfigs: { "audit_configs": [ { "service": "allServices", "audit_log_configs": [ { "log_type": "DATA_READ", "exempted_members": [ "user:jose@example.com" ] }, { "log_type": "DATA_WRITE" }, { "log_type": "ADMIN_READ" } ] }, { "service": "sampleservice.googleapis.com", "audit_log_configs": [ { "log_type": "DATA_READ" }, { "log_type": "DATA_WRITE", "exempted_members": [ "user:aliya@example.com" ] } ] } ] } For sampleservice, this policy enables DATA_READ, DATA_WRITE and ADMIN_READ logging. It also exempts jose@example.com from DATA_READ logging, and aliya@example.com from DATA_WRITE logging.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AuditConfig {
    /// The configuration for logging of each type of permission.
    #[serde(default, rename = "auditLogConfigs")]
    pub audit_log_configs: ::core::option::Option<::std::vec::Vec<AuditLogConfig>>,
    /// Specifies a service that will be enabled for audit logging. For example, storage.googleapis.com, cloudsql.googleapis.com. allServices is a special value that covers all services.
    #[serde(default)]
    pub service: ::core::option::Option<String>,
}

/// Provides the configuration for logging a type of permissions. Example: { "audit_log_configs": [ { "log_type": "DATA_READ", "exempted_members": [ "user:jose@example.com" ] }, { "log_type": "DATA_WRITE" } ] } This enables ''DATA_READ'' and ''DATA_WRITE'' logging, while exempting jose@example.com from DATA_READ logging.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AuditLogConfig {
    /// Specifies the identities that do not cause logging for this type of permission. Follows the same format of Binding.members.
    #[serde(default, rename = "exemptedMembers")]
    pub exempted_members: ::core::option::Option<::std::vec::Vec<String>>,
    /// The log type that this config enables. // TODO: enum values: ["LOG_TYPE_UNSPECIFIED", "ADMIN_READ", "DATA_WRITE", "DATA_READ"]
    #[serde(default, rename = "logType")]
    pub log_type: ::core::option::Option<String>,
}

/// Defines an authorization code.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AuthorizationCode {
    /// The Authorization Code in ASCII. It can be used to transfer the domain to or from another registrar.
    #[serde(default)]
    pub code: ::core::option::Option<String>,
}

/// Associates members, or principals, with a role.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Binding {
    /// The condition that is associated with this binding. If the condition evaluates to true, then this binding applies to the current request. If the condition evaluates to false, then this binding does not apply to the current request. However, a different role binding might grant the same role to one or more of the principals in this binding. To learn which resources support conditions in their IAM policies, see the [IAM documentation](https://cloud.google.com/iam/help/conditions/resource-policies).
    #[serde(default)]
    pub condition: ::core::option::Option<Expr>,
    /// Specifies the principals requesting access for a Google Cloud resource. members can have the following values: * allUsers: A special identifier that represents anyone who is on the internet; with or without a Google account. * allAuthenticatedUsers: A special identifier that represents anyone who is authenticated with a Google account or a service account. Does not include identities that come from external identity providers (IdPs) through identity federation. * user:{emailid}: An email address that represents a specific Google account. For example, alice@example.com . * serviceAccount:{emailid}: An email address that represents a Google service account. For example, my-other-app@appspot.gserviceaccount.com. * serviceAccount:{projectid}.svc.id.goog[{namespace}/{kubernetes-sa}]: An identifier for a [Kubernetes service account](https://cloud.google.com/kubernetes-engine/docs/how-to/kubernetes-service-accounts). For example, my-project.svc.id.goog[my-namespace/my-kubernetes-sa]. * group:{emailid}: An email address that represents a Google group. For example, admins@example.com. * domain:{domain}: The G Suite domain (primary) that represents all the users of that domain. For example, google.com or example.com. * principal://iam.googleapis.com/locations/global/workforcePools/{pool_id}/subject/{subject_attribute_value}: A single identity in a workforce identity pool. * principalSet://iam.googleapis.com/locations/global/workforcePools/{pool_id}/group/{group_id}: All workforce identities in a group. * principalSet://iam.googleapis.com/locations/global/workforcePools/{pool_id}/attribute.{attribute_name}/{attribute_value}: All workforce identities with a specific attribute value. * principalSet://iam.googleapis.com/locations/global/workforcePools/{pool_id}/*: All identities in a workforce identity pool. * principal://iam.googleapis.com/projects/{project_number}/locations/global/workloadIdentityPools/{pool_id}/subject/{subject_attribute_value}: A single identity in a workload identity pool. * principalSet://iam.googleapis.com/projects/{project_number}/locations/global/workloadIdentityPools/{pool_id}/group/{group_id}: A workload identity pool group. * principalSet://iam.googleapis.com/projects/{project_number}/locations/global/workloadIdentityPools/{pool_id}/attribute.{attribute_name}/{attribute_value}: All identities in a workload identity pool with a certain attribute. * principalSet://iam.googleapis.com/projects/{project_number}/locations/global/workloadIdentityPools/{pool_id}/*: All identities in a workload identity pool. * deleted:user:{emailid}?uid={uniqueid}: An email address (plus unique identifier) representing a user that has been recently deleted. For example, alice@example.com?uid=123456789012345678901. If the user is recovered, this value reverts to user:{emailid} and the recovered user retains the role in the binding. * deleted:serviceAccount:{emailid}?uid={uniqueid}: An email address (plus unique identifier) representing a service account that has been recently deleted. For example, my-other-app@appspot.gserviceaccount.com?uid=123456789012345678901. If the service account is undeleted, this value reverts to serviceAccount:{emailid} and the undeleted service account retains the role in the binding. * deleted:group:{emailid}?uid={uniqueid}: An email address (plus unique identifier) representing a Google group that has been recently deleted. For example, admins@example.com?uid=123456789012345678901. If the group is recovered, this value reverts to group:{emailid} and the recovered group retains the role in the binding. * deleted:principal://iam.googleapis.com/locations/global/workforcePools/{pool_id}/subject/{subject_attribute_value}: Deleted single identity in a workforce identity pool. For example, deleted:principal://iam.googleapis.com/locations/global/workforcePools/my-pool-id/subject/my-subject-attribute-value.
    #[serde(default)]
    pub members: ::core::option::Option<::std::vec::Vec<String>>,
    /// Role that is assigned to the list of members, or principals. For example, roles/viewer, roles/editor, or roles/owner. For an overview of the IAM roles and permissions, see the [IAM documentation](https://cloud.google.com/iam/docs/roles-overview). For a list of the available pre-defined roles, see [here](https://cloud.google.com/iam/docs/understanding-roles).
    #[serde(default)]
    pub role: ::core::option::Option<String>,
}

/// Request for the ConfigureContactSettings method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ConfigureContactSettingsRequest {
    /// The list of contact notices that the caller acknowledges. The notices needed here depend on the values specified in contact_settings.
    #[serde(default, rename = "contactNotices")]
    pub contact_notices: ::core::option::Option<::std::vec::Vec<String>>,
    /// Fields of the ContactSettings to update.
    #[serde(default, rename = "contactSettings")]
    pub contact_settings: ::core::option::Option<ContactSettings>,
    /// Required. The field mask describing which fields to update as a comma-separated list. For example, if only the registrant contact is being updated, the update_mask is "registrant_contact".
    #[serde(default, rename = "updateMask")]
    pub update_mask: ::core::option::Option<String>,
    /// Validate the request without actually updating the contact settings.
    #[serde(default, rename = "validateOnly")]
    pub validate_only: ::core::option::Option<bool>,
}

/// Request for the ConfigureDnsSettings method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ConfigureDnsSettingsRequest {
    /// Fields of the DnsSettings to update.
    #[serde(default, rename = "dnsSettings")]
    pub dns_settings: ::core::option::Option<DnsSettings>,
    /// Required. The field mask describing which fields to update as a comma-separated list. For example, if only the name servers are being updated for an existing Custom DNS configuration, the update_mask is "custom_dns.name_servers". When changing the DNS provider from one type to another, pass the new provider''s field name as part of the field mask. For example, when changing from a Google Domains DNS configuration to a Custom DNS configuration, the update_mask is "custom_dns". //
    #[serde(default, rename = "updateMask")]
    pub update_mask: ::core::option::Option<String>,
    /// Validate the request without actually updating the DNS settings.
    #[serde(default, rename = "validateOnly")]
    pub validate_only: ::core::option::Option<bool>,
}

/// Request for the ConfigureManagementSettings method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ConfigureManagementSettingsRequest {
    /// Fields of the ManagementSettings to update.
    #[serde(default, rename = "managementSettings")]
    pub management_settings: ::core::option::Option<ManagementSettings>,
    /// Required. The field mask describing which fields to update as a comma-separated list. For example, if only the transfer lock is being updated, the update_mask is "transfer_lock_state".
    #[serde(default, rename = "updateMask")]
    pub update_mask: ::core::option::Option<String>,
}

/// Details required for a contact associated with a Registration.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Contact {
    /// Required. Email address of the contact.
    #[serde(default)]
    pub email: ::core::option::Option<String>,
    /// Fax number of the contact in international format. For example, "+1-800-555-0123".
    #[serde(default, rename = "faxNumber")]
    pub fax_number: ::core::option::Option<String>,
    /// Required. Phone number of the contact in international format. For example, "+1-800-555-0123".
    #[serde(default, rename = "phoneNumber")]
    pub phone_number: ::core::option::Option<String>,
    /// Required. Postal address of the contact.
    #[serde(default, rename = "postalAddress")]
    pub postal_address: ::core::option::Option<PostalAddress>,
}

/// Defines the contact information associated with a Registration. [ICANN](https://icann.org/) requires all domain names to have associated contact information. The registrant_contact is considered the domain''s legal owner, and often the other contacts are identical.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ContactSettings {
    /// Required. The administrative contact for the Registration.
    #[serde(default, rename = "adminContact")]
    pub admin_contact: ::core::option::Option<Contact>,
    /// Required. Privacy setting for the contacts associated with the Registration. // TODO: enum values: ["CONTACT_PRIVACY_UNSPECIFIED", "PUBLIC_CONTACT_DATA", "PRIVATE_CONTACT_DATA", "REDACTED_CONTACT_DATA"]
    #[serde(default)]
    pub privacy: ::core::option::Option<String>,
    /// Required. The registrant contact for the Registration. *Caution: Anyone with access to this email address, phone number, and/or postal address can take control of the domain.* *Warning: For new Registrations, the registrant receives an email confirmation that they must complete within 15 days to avoid domain suspension.*
    #[serde(default, rename = "registrantContact")]
    pub registrant_contact: ::core::option::Option<Contact>,
    /// Required. The technical contact for the Registration.
    #[serde(default, rename = "technicalContact")]
    pub technical_contact: ::core::option::Option<Contact>,
}

/// Configuration for an arbitrary DNS provider.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CustomDns {
    /// The list of DS records for this domain, which are used to enable DNSSEC. The domain''s DNS provider can provide the values to set here. If this field is empty, DNSSEC is disabled.
    #[serde(default, rename = "dsRecords")]
    pub ds_records: ::core::option::Option<::std::vec::Vec<DsRecord>>,
    /// Required. A list of name servers that store the DNS zone for this domain. Each name server is a domain name, with Unicode domain names expressed in Punycode format.
    #[serde(default, rename = "nameServers")]
    pub name_servers: ::core::option::Option<::std::vec::Vec<String>>,
}

/// Defines the DNS configuration of a Registration, including name servers, DNSSEC, and glue records.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DnsSettings {
    /// An arbitrary DNS provider identified by its name servers.
    #[serde(default, rename = "customDns")]
    pub custom_dns: ::core::option::Option<CustomDns>,
    /// The list of glue records for this Registration. Commonly empty.
    #[serde(default, rename = "glueRecords")]
    pub glue_records: ::core::option::Option<::std::vec::Vec<GlueRecord>>,
    /// Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/domains/docs/deprecations/feature-deprecations). The free DNS zone provided by [Google Domains](https://domains.google/).
    #[serde(default, rename = "googleDomainsDns")]
    pub google_domains_dns: ::core::option::Option<GoogleDomainsDns>,
    /// Output only. Indicates if this Registration has configured one of the following deprecated Google Domains DNS features: * Domain forwarding (HTTP 301 and 302 response status codes), * Email forwarding. See https://cloud.google.com/domains/docs/deprecations/feature-deprecations for more details. If any of these features is enabled call the RetrieveGoogleDomainsForwardingConfig method to get details about the feature''s configuration. A forwarding configuration might not work correctly if required DNS records are not present in the domain''s authoritative DNS Zone.
    #[serde(default, rename = "googleDomainsRedirectsDataAvailable")]
    pub google_domains_redirects_data_available: ::core::option::Option<bool>,
}

/// A domain that the calling user manages in Google Domains.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Domain {
    /// The domain name. Unicode domain names are expressed in Punycode format.
    #[serde(default, rename = "domainName")]
    pub domain_name: ::core::option::Option<String>,
    /// The state of this domain as a Registration resource. // TODO: enum values: ["RESOURCE_STATE_UNSPECIFIED", "IMPORTABLE", "UNSUPPORTED", "SUSPENDED", "EXPIRED", "DELETED"]
    #[serde(default, rename = "resourceState")]
    pub resource_state: ::core::option::Option<String>,
    /// Price to renew the domain for one year. Only set when resource_state is IMPORTABLE.
    #[serde(default, rename = "yearlyPrice")]
    pub yearly_price: ::core::option::Option<Money>,
}

/// Domain forwarding configuration.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DomainForwarding {
    /// If true, forwards the path after the domain name to the same path at the new address.
    #[serde(default, rename = "pathForwarding")]
    pub path_forwarding: ::core::option::Option<bool>,
    /// The PEM-encoded certificate chain.
    #[serde(default, rename = "pemCertificate")]
    pub pem_certificate: ::core::option::Option<String>,
    /// The redirect type. // TODO: enum values: ["REDIRECT_TYPE_UNSPECIFIED", "TEMPORARY", "PERMANENT"]
    #[serde(default, rename = "redirectType")]
    pub redirect_type: ::core::option::Option<String>,
    /// If true, the forwarding works also over HTTPS.
    #[serde(default, rename = "sslEnabled")]
    pub ssl_enabled: ::core::option::Option<bool>,
    /// The subdomain of the registered domain that is being forwarded. E.g. www.example.com, example.com (i.e. the registered domain itself) or *.example.com (i.e. all subdomains).
    #[serde(default)]
    pub subdomain: ::core::option::Option<String>,
    /// The target of the domain forwarding, i.e. the path to redirect the subdomain to.
    #[serde(default, rename = "targetUri")]
    pub target_uri: ::core::option::Option<String>,
}

/// Defines a Delegation Signer (DS) record, which is needed to enable DNSSEC for a domain. It contains a digest (hash) of a DNSKEY record that must be present in the domain''s DNS zone.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DsRecord {
    /// The algorithm used to generate the referenced DNSKEY. // TODO: enum values: ["ALGORITHM_UNSPECIFIED", "RSAMD5", "DH", "DSA", "ECC", "RSASHA1", "DSANSEC3SHA1", "RSASHA1NSEC3SHA1", "RSASHA256", "RSASHA512", "ECCGOST", "ECDSAP256SHA256", "ECDSAP384SHA384", "ED25519", "ED448", "INDIRECT", "PRIVATEDNS", "PRIVATEOID"]
    #[serde(default)]
    pub algorithm: ::core::option::Option<String>,
    /// The digest generated from the referenced DNSKEY.
    #[serde(default)]
    pub digest: ::core::option::Option<String>,
    /// The hash function used to generate the digest of the referenced DNSKEY. // TODO: enum values: ["DIGEST_TYPE_UNSPECIFIED", "SHA1", "SHA256", "GOST3411", "SHA384"]
    #[serde(default, rename = "digestType")]
    pub digest_type: ::core::option::Option<String>,
    /// The key tag of the record. Must be set in range 0 -- 65535.
    #[serde(default, rename = "keyTag")]
    pub key_tag: ::core::option::Option<i32>,
}

/// Email forwarding configuration.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EmailForwarding {
    /// An alias recipient email that forwards emails to the target_email_address. For example, admin@example.com or *@example.com (wildcard alias forwards all the emails under the registered domain).
    #[serde(default)]
    pub alias: ::core::option::Option<String>,
    /// Target email that receives emails sent to the alias.
    #[serde(default, rename = "targetEmailAddress")]
    pub target_email_address: ::core::option::Option<String>,
}

/// Represents a textual expression in the Common Expression Language (CEL) syntax. CEL is a C-like expression language. The syntax and semantics of CEL are documented at https://github.com/google/cel-spec. Example (Comparison): title: "Summary size limit" description: "Determines if a summary is less than 100 chars" expression: "document.summary.size() &lt; 100" Example (Equality): title: "Requestor is owner" description: "Determines if requestor is the document owner" expression: "document.owner == request.auth.claims.email" Example (Logic): title: "Public documents" description: "Determine whether the document should be publicly visible" expression: "document.type != ''private'' && document.type != ''internal''" Example (Data Manipulation): title: "Notification string" description: "Create a notification string with a timestamp." expression: "''New message received at '' + string(document.create_time)" The exact variables and functions that may be referenced within an expression are determined by the service that evaluates it. See the service documentation for additional information.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Expr {
    /// Optional. Description of the expression. This is a longer text which describes the expression, e.g. when hovered over it in a UI.
    #[serde(default)]
    pub description: ::core::option::Option<String>,
    /// Textual representation of an expression in Common Expression Language syntax.
    #[serde(default)]
    pub expression: ::core::option::Option<String>,
    /// Optional. String indicating the location of the expression for error reporting, e.g. a file name and a position in the file.
    #[serde(default)]
    pub location: ::core::option::Option<String>,
    /// Optional. Title for the expression, i.e. a short string describing its purpose. This can be used e.g. in UIs which allow to enter the expression.
    #[serde(default)]
    pub title: ::core::option::Option<String>,
}

/// Configures a RRSetRoutingPolicy that routes based on the geo location of the querying user.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GeoPolicy {
    /// Without fencing, if health check fails for all configured items in the current geo bucket, we failover to the next nearest geo bucket. With fencing, if health checking is enabled, as long as some targets in the current geo bucket are healthy, we return only the healthy targets. However, if all targets are unhealthy, we don''t failover to the next nearest bucket; instead, we return all the items in the current bucket even when all targets are unhealthy.
    #[serde(default, rename = "enableFencing")]
    pub enable_fencing: ::core::option::Option<bool>,
    /// The primary geo routing configuration. If there are multiple items with the same location, an error is returned instead.
    #[serde(default)]
    pub item: ::core::option::Option<::std::vec::Vec<GeoPolicyItem>>,
}

/// ResourceRecordSet data for one geo location.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GeoPolicyItem {
    /// For A and AAAA types only. Endpoints to return in the query result only if they are healthy. These can be specified along with rrdata within this item.
    #[serde(default, rename = "healthCheckedTargets")]
    pub health_checked_targets: ::core::option::Option<HealthCheckTargets>,
    /// The geo-location granularity is a GCP region. This location string should correspond to a GCP region. e.g. "us-east1", "southamerica-east1", "asia-east1", etc.
    #[serde(default)]
    pub location: ::core::option::Option<String>,
    #[serde(default)]
    pub rrdata: ::core::option::Option<::std::vec::Vec<String>>,
    /// DNSSEC generated signatures for all the rrdata within this item. When using health-checked targets for DNSSEC-enabled zones, you can only use at most one health-checked IP address per item.
    #[serde(default, rename = "signatureRrdata")]
    pub signature_rrdata: ::core::option::Option<::std::vec::Vec<String>>,
}

/// Defines a host on your domain that is a DNS name server for your domain and/or other domains. Glue records are a way of making the IP address of a name server known, even when it serves DNS queries for its parent domain. For example, when ns.example.com is a name server for example.com, the host ns.example.com must have a glue record to break the circular DNS reference.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GlueRecord {
    /// Required. Domain name of the host in Punycode format.
    #[serde(default, rename = "hostName")]
    pub host_name: ::core::option::Option<String>,
    /// List of IPv4 addresses corresponding to this host in the standard decimal format (e.g. 198.51.100.1). At least one of ipv4_address and ipv6_address must be set.
    #[serde(default, rename = "ipv4Addresses")]
    pub ipv4_addresses: ::core::option::Option<::std::vec::Vec<String>>,
    /// List of IPv6 addresses corresponding to this host in the standard hexadecimal format (e.g. 2001:db8::). At least one of ipv4_address and ipv6_address must be set.
    #[serde(default, rename = "ipv6Addresses")]
    pub ipv6_addresses: ::core::option::Option<::std::vec::Vec<String>>,
}

/// Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/domains/docs/deprecations/feature-deprecations). Configuration for using the free DNS zone provided by Google Domains as a Registration''s dns_provider. You cannot configure the DNS zone itself using the API. To configure the DNS zone, go to [Google Domains](https://domains.google/).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GoogleDomainsDns {
    /// Output only. The list of DS records published for this domain. The list is automatically populated when ds_state is DS_RECORDS_PUBLISHED, otherwise it remains empty.
    #[serde(default, rename = "dsRecords")]
    pub ds_records: ::core::option::Option<::std::vec::Vec<DsRecord>>,
    /// Required. The state of DS records for this domain. Used to enable or disable automatic DNSSEC. // TODO: enum values: ["DS_STATE_UNSPECIFIED", "DS_RECORDS_UNPUBLISHED", "DS_RECORDS_PUBLISHED"]
    #[serde(default, rename = "dsState")]
    pub ds_state: ::core::option::Option<String>,
    /// Output only. A list of name servers that store the DNS zone for this domain. Each name server is a domain name, with Unicode domain names expressed in Punycode format. This field is automatically populated with the name servers assigned to the Google Domains DNS zone.
    #[serde(default, rename = "nameServers")]
    pub name_servers: ::core::option::Option<::std::vec::Vec<String>>,
}

/// HealthCheckTargets describes endpoints to health-check when responding to Routing Policy queries. Only the healthy endpoints will be included in the response. Set either internal_load_balancer or external_endpoints. Do not set both.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct HealthCheckTargets {
    /// The Internet IP addresses to be health checked. The format matches the format of ResourceRecordSet.rrdata as defined in RFC 1035 (section 5) and RFC 1034 (section 3.6.1)
    #[serde(default, rename = "externalEndpoints")]
    pub external_endpoints: ::core::option::Option<::std::vec::Vec<String>>,
    /// Configuration for internal load balancers to be health checked.
    #[serde(default, rename = "internalLoadBalancer")]
    pub internal_load_balancer: ::core::option::Option<::std::vec::Vec<LoadBalancerTarget>>,
}

/// Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/domains/docs/deprecations/feature-deprecations). Request for the ImportDomain method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ImportDomainRequest {
    /// Required. The domain name. Unicode domain names must be expressed in Punycode format.
    #[serde(default, rename = "domainName")]
    pub domain_name: ::core::option::Option<String>,
    /// Set of labels associated with the Registration.
    #[serde(default)]
    pub labels: ::core::option::Option<serde_json::Value>,
}

/// Request for the InitiatePushTransfer method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct InitiatePushTransferRequest {
    /// Required. The Tag of the new registrar. Can be found at [List of registrars](https://nominet.uk/registrar-list/).
    #[serde(default)]
    pub tag: ::core::option::Option<String>,
}

/// The response message for Locations.ListLocations.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListLocationsResponse {
    /// A list of locations that matches the specified filter in the request.
    #[serde(default)]
    pub locations: ::core::option::Option<::std::vec::Vec<Location>>,
    /// The standard List next-page token.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
}

/// The response message for Operations.ListOperations.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListOperationsResponse {
    /// The standard List next-page token.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
    /// A list of operations that matches the specified filter in the request.
    #[serde(default)]
    pub operations: ::core::option::Option<::std::vec::Vec<Operation>>,
    /// Unordered list. Unreachable resources. Populated when the request sets ListOperationsRequest.return_partial_success and reads across collections. For example, when attempting to list all resources across all supported locations.
    #[serde(default)]
    pub unreachable: ::core::option::Option<::std::vec::Vec<String>>,
}

/// Response for the ListRegistrations method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListRegistrationsResponse {
    /// When present, there are more results to retrieve. Set page_token to this value on a subsequent call to get the next page of results.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
    /// A list of Registrations.
    #[serde(default)]
    pub registrations: ::core::option::Option<::std::vec::Vec<Registration>>,
}

/// The configuration for an individual load balancer to health check.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct LoadBalancerTarget {
    /// The frontend IP address of the load balancer to health check.
    #[serde(default, rename = "ipAddress")]
    pub ip_address: ::core::option::Option<String>,
    /// The protocol of the load balancer to health check. // TODO: enum values: ["UNDEFINED", "TCP", "UDP"]
    #[serde(default, rename = "ipProtocol")]
    pub ip_protocol: ::core::option::Option<String>,
    /// The type of load balancer specified by this target. This value must match the configuration of the load balancer located at the LoadBalancerTarget''s IP address, port, and region. Use the following: - *regionalL4ilb*: for a regional internal passthrough Network Load Balancer. - *regionalL7ilb*: for a regional internal Application Load Balancer. - *globalL7ilb*: for a global internal Application Load Balancer.  // TODO: enum values: ["NONE", "GLOBAL_L7ILB", "REGIONAL_L4ILB", "REGIONAL_L7ILB"]
    #[serde(default, rename = "loadBalancerType")]
    pub load_balancer_type: ::core::option::Option<String>,
    /// The fully qualified URL of the network that the load balancer is attached to. This should be formatted like https://www.googleapis.com/compute/v1/projects/{project}/global/networks/{network}.
    #[serde(default, rename = "networkUrl")]
    pub network_url: ::core::option::Option<String>,
    /// The configured port of the load balancer.
    #[serde(default)]
    pub port: ::core::option::Option<String>,
    /// The project ID in which the load balancer is located.
    #[serde(default)]
    pub project: ::core::option::Option<String>,
    /// The region in which the load balancer is located.
    #[serde(default)]
    pub region: ::core::option::Option<String>,
}

/// A resource that represents a Google Cloud location.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Location {
    /// The friendly name for this location, typically a nearby city name. For example, "Tokyo".
    #[serde(default, rename = "displayName")]
    pub display_name: ::core::option::Option<String>,
    /// Cross-service attributes for the location. For example {"cloud.googleapis.com/region": "us-east1"}
    #[serde(default)]
    pub labels: ::core::option::Option<serde_json::Value>,
    /// The canonical id for this location. For example: "us-east1".
    #[serde(default, rename = "locationId")]
    pub location_id: ::core::option::Option<String>,
    /// Service-specific metadata. For example the available capacity at the given location.
    #[serde(default)]
    pub metadata: ::core::option::Option<serde_json::Value>,
    /// Resource name for the location, which may vary between implementations. For example: "projects/example-project/locations/us-east1"
    #[serde(default)]
    pub name: ::core::option::Option<String>,
}

/// Defines renewal, billing, and transfer settings for a Registration.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ManagementSettings {
    /// Output only. The actual transfer lock state for this Registration. // TODO: enum values: ["TRANSFER_LOCK_STATE_UNSPECIFIED", "UNLOCKED", "LOCKED"]
    #[serde(default, rename = "effectiveTransferLockState")]
    pub effective_transfer_lock_state: ::core::option::Option<String>,
    /// Optional. The desired renewal method for this Registration. The actual renewal_method is automatically updated to reflect this choice. If unset or equal to RENEWAL_METHOD_UNSPECIFIED, the actual renewalMethod is treated as if it were set to AUTOMATIC_RENEWAL. You cannot use RENEWAL_DISABLED during resource creation, and you can update the renewal status only when the Registration resource has state ACTIVE or SUSPENDED. When preferred_renewal_method is set to AUTOMATIC_RENEWAL, the actual renewal_method can be set to RENEWAL_DISABLED in case of problems with the billing account or reported domain abuse. In such cases, check the issues field on the Registration. After the problem is resolved, the renewal_method is automatically updated to preferred_renewal_method in a few hours. // TODO: enum values: ["RENEWAL_METHOD_UNSPECIFIED", "AUTOMATIC_RENEWAL", "MANUAL_RENEWAL", "RENEWAL_DISABLED"]
    #[serde(default, rename = "preferredRenewalMethod")]
    pub preferred_renewal_method: ::core::option::Option<String>,
    /// Output only. The actual renewal method for this Registration. When preferred_renewal_method is set to AUTOMATIC_RENEWAL, the actual renewal_method can be equal to RENEWAL_DISABLED—for example, when there are problems with the billing account or reported domain abuse. In such cases, check the issues field on the Registration. After the problem is resolved, the renewal_method is automatically updated to preferred_renewal_method in a few hours. // TODO: enum values: ["RENEWAL_METHOD_UNSPECIFIED", "AUTOMATIC_RENEWAL", "MANUAL_RENEWAL", "RENEWAL_DISABLED"]
    #[serde(default, rename = "renewalMethod")]
    pub renewal_method: ::core::option::Option<String>,
    /// This is the desired transfer lock state for this Registration. A transfer lock controls whether the domain can be transferred to another registrar. The transfer lock state of the domain is returned in the effective_transfer_lock_state property. The transfer lock state values might be different for the following reasons: * transfer_lock_state was updated only a short time ago. * Domains with the TRANSFER_LOCK_UNSUPPORTED_BY_REGISTRY state are in the list of domain_properties. These domains are always in the UNLOCKED state. // TODO: enum values: ["TRANSFER_LOCK_STATE_UNSPECIFIED", "UNLOCKED", "LOCKED"]
    #[serde(default, rename = "transferLockState")]
    pub transfer_lock_state: ::core::option::Option<String>,
}

/// Represents an amount of money with its currency type.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Money {
    /// The three-letter currency code defined in ISO 4217.
    #[serde(default, rename = "currencyCode")]
    pub currency_code: ::core::option::Option<String>,
    /// Number of nano (10^-9) units of the amount. The value must be between -999,999,999 and +999,999,999 inclusive. If units is positive, nanos must be positive or zero. If units is zero, nanos can be positive, zero, or negative. If units is negative, nanos must be negative or zero. For example $-1.75 is represented as units=-1 and nanos=-750,000,000.
    #[serde(default)]
    pub nanos: ::core::option::Option<i32>,
    /// The whole units of the amount. For example if currencyCode is "USD", then 1 unit is one US dollar.
    #[serde(default)]
    pub units: ::core::option::Option<String>,
}

/// This resource represents a long-running operation that is the result of a network API call.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Operation {
    /// If the value is false, it means the operation is still in progress. If true, the operation is completed, and either error or response is available.
    #[serde(default)]
    pub done: ::core::option::Option<bool>,
    /// The error result of the operation in case of failure or cancellation.
    #[serde(default)]
    pub error: ::core::option::Option<Status>,
    /// Service-specific metadata associated with the operation. It typically contains progress information and common metadata such as create time. Some services might not provide such metadata. Any method that returns a long-running operation should document the metadata type, if any.
    #[serde(default)]
    pub metadata: ::core::option::Option<serde_json::Value>,
    /// The server-assigned name, which is only unique within the same service that originally returns it. If you use the default HTTP mapping, the name should be a resource name ending with operations/{unique_id}.
    #[serde(default)]
    pub name: ::core::option::Option<String>,
    /// The normal, successful response of the operation. If the original method returns no data on success, such as Delete, the response is google.protobuf.Empty. If the original method is standard Get/Create/Update, the response should be the resource. For other methods, the response should have the type XxxResponse, where Xxx is the original method name. For example, if the original method name is TakeSnapshot(), the inferred response type is TakeSnapshotResponse.
    #[serde(default)]
    pub response: ::core::option::Option<serde_json::Value>,
}

/// Represents the metadata of the long-running operation. Output only.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct OperationMetadata {
    /// API version used to start the operation.
    #[serde(default, rename = "apiVersion")]
    pub api_version: ::core::option::Option<String>,
    /// The time the operation was created.
    #[serde(default, rename = "createTime")]
    pub create_time: ::core::option::Option<String>,
    /// The time the operation finished running.
    #[serde(default, rename = "endTime")]
    pub end_time: ::core::option::Option<String>,
    /// Human-readable status of the operation, if any.
    #[serde(default, rename = "statusDetail")]
    pub status_detail: ::core::option::Option<String>,
    /// Server-defined resource path for the target of the operation.
    #[serde(default)]
    pub target: ::core::option::Option<String>,
    /// Name of the verb executed by the operation.
    #[serde(default)]
    pub verb: ::core::option::Option<String>,
}

/// An Identity and Access Management (IAM) policy, which specifies access controls for Google Cloud resources. A Policy is a collection of bindings. A binding binds one or more members, or principals, to a single role. Principals can be user accounts, service accounts, Google groups, and domains (such as G Suite). A role is a named list of permissions; each role can be an IAM predefined role or a user-created custom role. For some types of Google Cloud resources, a binding can also specify a condition, which is a logical expression that allows access to a resource only if the expression evaluates to true. A condition can add constraints based on attributes of the request, the resource, or both. To learn which resources support conditions in their IAM policies, see the [IAM documentation](https://cloud.google.com/iam/help/conditions/resource-policies). **JSON example:**  { "bindings": [ { "role": "roles/resourcemanager.organizationAdmin", "members": [ "user:mike@example.com", "group:admins@example.com", "domain:google.com", "serviceAccount:my-project-id@appspot.gserviceaccount.com" ] }, { "role": "roles/resourcemanager.organizationViewer", "members": [ "user:eve@example.com" ], "condition": { "title": "expirable access", "description": "Does not grant access after Sep 2020", "expression": "request.time &lt; timestamp(''2020-10-01T00:00:00.000Z'')", } } ], "etag": "BwWWja0YfJA=", "version": 3 }  **YAML example:**  bindings: - members: - user:mike@example.com - group:admins@example.com - domain:google.com - serviceAccount:my-project-id@appspot.gserviceaccount.com role: roles/resourcemanager.organizationAdmin - members: - user:eve@example.com role: roles/resourcemanager.organizationViewer condition: title: expirable access description: Does not grant access after Sep 2020 expression: request.time &lt; timestamp(''2020-10-01T00:00:00.000Z'') etag: BwWWja0YfJA= version: 3  For a description of IAM and its features, see the [IAM documentation](https://cloud.google.com/iam/docs/).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Policy {
    /// Specifies cloud audit logging configuration for this policy.
    #[serde(default, rename = "auditConfigs")]
    pub audit_configs: ::core::option::Option<::std::vec::Vec<AuditConfig>>,
    /// Associates a list of members, or principals, with a role. Optionally, may specify a condition that determines how and when the bindings are applied. Each of the bindings must contain at least one principal. The bindings in a Policy can refer to up to 1,500 principals; up to 250 of these principals can be Google groups. Each occurrence of a principal counts towards these limits. For example, if the bindings grant 50 different roles to user:alice@example.com, and not to any other principal, then you can add another 1,450 principals to the bindings in the Policy.
    #[serde(default)]
    pub bindings: ::core::option::Option<::std::vec::Vec<Binding>>,
    /// etag is used for optimistic concurrency control as a way to help prevent simultaneous updates of a policy from overwriting each other. It is strongly suggested that systems make use of the etag in the read-modify-write cycle to perform policy updates in order to avoid race conditions: An etag is returned in the response to getIamPolicy, and systems are expected to put that etag in the request to setIamPolicy to ensure that their change will be applied to the same version of the policy. **Important:** If you use IAM Conditions, you must include the etag field whenever you call setIamPolicy. If you omit this field, then IAM allows you to overwrite a version 3 policy with a version 1 policy, and all of the conditions in the version 3 policy are lost.
    #[serde(default)]
    pub etag: ::core::option::Option<String>,
    /// Specifies the format of the policy. Valid values are 0, 1, and 3. Requests that specify an invalid value are rejected. Any operation that affects conditional role bindings must specify version 3. This requirement applies to the following operations: * Getting a policy that includes a conditional role binding * Adding a conditional role binding to a policy * Changing a conditional role binding in a policy * Removing any role binding, with or without a condition, from a policy that includes conditions **Important:** If you use IAM Conditions, you must include the etag field whenever you call setIamPolicy. If you omit this field, then IAM allows you to overwrite a version 3 policy with a version 1 policy, and all of the conditions in the version 3 policy are lost. If a policy does not include any conditions, operations on that policy may specify any valid version or leave the field unset. To learn which resources support conditions in their IAM policies, see the [IAM documentation](https://cloud.google.com/iam/help/conditions/resource-policies).
    #[serde(default)]
    pub version: ::core::option::Option<i32>,
}

/// Represents a postal address, such as for postal delivery or payments addresses. With a postal address, a postal service can deliver items to a premise, P.O. box, or similar. A postal address is not intended to model geographical locations like roads, towns, or mountains. In typical usage, an address would be created by user input or from importing existing data, depending on the type of process. Advice on address input or editing: - Use an internationalization-ready address widget such as https://github.com/google/libaddressinput. - Users should not be presented with UI elements for input or editing of fields outside countries where that field is used. For more guidance on how to use this schema, see: https://support.google.com/business/answer/6397478.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PostalAddress {
    /// Unstructured address lines describing the lower levels of an address. Because values in address_lines do not have type information and may sometimes contain multiple values in a single field (for example, "Austin, TX"), it is important that the line order is clear. The order of address lines should be "envelope order" for the country or region of the address. In places where this can vary (for example, Japan), address_language is used to make it explicit (for example, "ja" for large-to-small ordering and "ja-Latn" or "en" for small-to-large). In this way, the most specific line of an address can be selected based on the language. The minimum permitted structural representation of an address consists of a region_code with all remaining information placed in the address_lines. It would be possible to format such an address very approximately without geocoding, but no semantic reasoning could be made about any of the address components until it was at least partially resolved. Creating an address only containing a region_code and address_lines and then geocoding is the recommended way to handle completely unstructured addresses (as opposed to guessing which parts of the address should be localities or administrative areas).
    #[serde(default, rename = "addressLines")]
    pub address_lines: ::core::option::Option<::std::vec::Vec<String>>,
    /// Optional. Highest administrative subdivision which is used for postal addresses of a country or region. For example, this can be a state, a province, an oblast, or a prefecture. For Spain, this is the province and not the autonomous community (for example, "Barcelona" and not "Catalonia"). Many countries don''t use an administrative area in postal addresses. For example, in Switzerland, this should be left unpopulated.
    #[serde(default, rename = "administrativeArea")]
    pub administrative_area: ::core::option::Option<String>,
    /// Optional. BCP-47 language code of the contents of this address (if known). This is often the UI language of the input form or is expected to match one of the languages used in the address'' country/region, or their transliterated equivalents. This can affect formatting in certain countries, but is not critical to the correctness of the data and will never affect any validation or other non-formatting related operations. If this value is not known, it should be omitted (rather than specifying a possibly incorrect default). Examples: "zh-Hant", "ja", "ja-Latn", "en".
    #[serde(default, rename = "languageCode")]
    pub language_code: ::core::option::Option<String>,
    /// Optional. Generally refers to the city or town portion of the address. Examples: US city, IT comune, UK post town. In regions of the world where localities are not well defined or do not fit into this structure well, leave locality empty and use address_lines.
    #[serde(default)]
    pub locality: ::core::option::Option<String>,
    /// Optional. The name of the organization at the address.
    #[serde(default)]
    pub organization: ::core::option::Option<String>,
    /// Optional. Postal code of the address. Not all countries use or require postal codes to be present, but where they are used, they may trigger additional validation with other parts of the address (for example, state or zip code validation in the United States).
    #[serde(default, rename = "postalCode")]
    pub postal_code: ::core::option::Option<String>,
    /// Optional. The recipient at the address. This field may, under certain circumstances, contain multiline information. For example, it might contain "care of" information.
    #[serde(default)]
    pub recipients: ::core::option::Option<::std::vec::Vec<String>>,
    /// Required. CLDR region code of the country/region of the address. This is never inferred and it is up to the user to ensure the value is correct. See https://cldr.unicode.org/ and https://www.unicode.org/cldr/charts/30/supplemental/territory_information.html for details. Example: "CH" for Switzerland.
    #[serde(default, rename = "regionCode")]
    pub region_code: ::core::option::Option<String>,
    /// The schema revision of the PostalAddress. This must be set to 0, which is the latest revision. All new revisions **must** be backward compatible with old revisions.
    #[serde(default)]
    pub revision: ::core::option::Option<i32>,
    /// Optional. Additional, country-specific, sorting code. This is not used in most regions. Where it is used, the value is either a string like "CEDEX", optionally followed by a number (for example, "CEDEX 7"), or just a number alone, representing the "sector code" (Jamaica), "delivery area indicator" (Malawi) or "post office indicator" (Côte d''Ivoire).
    #[serde(default, rename = "sortingCode")]
    pub sorting_code: ::core::option::Option<String>,
    /// Optional. Sublocality of the address. For example, this can be a neighborhood, borough, or district.
    #[serde(default)]
    pub sublocality: ::core::option::Option<String>,
}

/// Configures a RRSetRoutingPolicy such that all queries are responded with the primary_targets if they are healthy. And if all of them are unhealthy, then we fallback to a geo localized policy.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PrimaryBackupPolicy {
    /// Backup targets provide a regional failover policy for the otherwise global primary targets. If serving state is set to BACKUP, this policy essentially becomes a geo routing policy.
    #[serde(default, rename = "backupGeoTargets")]
    pub backup_geo_targets: ::core::option::Option<GeoPolicy>,
    /// Endpoints that are health checked before making the routing decision. Unhealthy endpoints are omitted from the results. If all endpoints are unhealthy, we serve a response based on the backup_geo_targets.
    #[serde(default, rename = "primaryTargets")]
    pub primary_targets: ::core::option::Option<HealthCheckTargets>,
    /// When serving state is PRIMARY, this field provides the option of sending a small percentage of the traffic to the backup targets.
    #[serde(default, rename = "trickleTraffic")]
    pub trickle_traffic: ::core::option::Option<f64>,
}

/// A RRSetRoutingPolicy represents ResourceRecordSet data that is returned dynamically with the response varying based on configured properties such as geolocation or by weighted random selection.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RRSetRoutingPolicy {
    #[serde(default)]
    pub geo: ::core::option::Option<GeoPolicy>,
    #[serde(default, rename = "geoPolicy")]
    pub geo_policy: ::core::option::Option<GeoPolicy>,
    /// The fully qualified URL of the HealthCheck to use for this RRSetRoutingPolicy. Format this URL like https://www.googleapis.com/compute/v1/projects/{project}/global/healthChecks/{healthCheck}. https://cloud.google.com/compute/docs/reference/rest/v1/healthChecks
    #[serde(default, rename = "healthCheck")]
    pub health_check: ::core::option::Option<String>,
    #[serde(default, rename = "primaryBackup")]
    pub primary_backup: ::core::option::Option<PrimaryBackupPolicy>,
    #[serde(default)]
    pub wrr: ::core::option::Option<WrrPolicy>,
    #[serde(default, rename = "wrrPolicy")]
    pub wrr_policy: ::core::option::Option<WrrPolicy>,
}

/// Request for the RegisterDomain method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RegisterDomainRequest {
    /// The list of contact notices that the caller acknowledges. The notices needed here depend on the values specified in registration.contact_settings.
    #[serde(default, rename = "contactNotices")]
    pub contact_notices: ::core::option::Option<::std::vec::Vec<String>>,
    /// The list of domain notices that you acknowledge. Call RetrieveRegisterParameters to see the notices that need acknowledgement.
    #[serde(default, rename = "domainNotices")]
    pub domain_notices: ::core::option::Option<::std::vec::Vec<String>>,
    /// Required. The complete Registration resource to be created.
    #[serde(default)]
    pub registration: ::core::option::Option<Registration>,
    /// When true, only validation is performed, without actually registering the domain. Follows: https://cloud.google.com/apis/design/design_patterns#request_validation
    #[serde(default, rename = "validateOnly")]
    pub validate_only: ::core::option::Option<bool>,
    /// Required. Yearly price to register or renew the domain. The value that should be put here can be obtained from RetrieveRegisterParameters or SearchDomains calls.
    #[serde(default, rename = "yearlyPrice")]
    pub yearly_price: ::core::option::Option<Money>,
}

/// Parameters required to register a new domain.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RegisterParameters {
    /// Indicates whether the domain is available for registration. This value is accurate when obtained by calling RetrieveRegisterParameters, but is approximate when obtained by calling SearchDomains. // TODO: enum values: ["AVAILABILITY_UNSPECIFIED", "AVAILABLE", "UNAVAILABLE", "UNSUPPORTED", "UNKNOWN"]
    #[serde(default)]
    pub availability: ::core::option::Option<String>,
    /// The domain name. Unicode domain names are expressed in Punycode format.
    #[serde(default, rename = "domainName")]
    pub domain_name: ::core::option::Option<String>,
    /// Notices about special properties of the domain.
    #[serde(default, rename = "domainNotices")]
    pub domain_notices: ::core::option::Option<::std::vec::Vec<String>>,
    /// Contact privacy options that the domain supports.
    #[serde(default, rename = "supportedPrivacy")]
    pub supported_privacy: ::core::option::Option<::std::vec::Vec<String>>,
    /// Price to register or renew the domain for one year.
    #[serde(default, rename = "yearlyPrice")]
    pub yearly_price: ::core::option::Option<Money>,
}

/// The Registration resource facilitates managing and configuring domain name registrations. There are several ways to create a new Registration resource: To create a new Registration resource, find a suitable domain name by calling the SearchDomains method with a query to see available domain name options. After choosing a name, call RetrieveRegisterParameters to ensure availability and obtain information like pricing, which is needed to build a call to RegisterDomain. Another way to create a new Registration is to transfer an existing domain from another registrar (Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/domains/docs/deprecations/feature-deprecations)). First, go to the current registrar to unlock the domain for transfer and retrieve the domain''s transfer authorization code. Then call RetrieveTransferParameters to confirm that the domain is unlocked and to get values needed to build a call to TransferDomain. Finally, you can create a new Registration by importing an existing domain managed with [Google Domains](https://domains.google/) (Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/domains/docs/deprecations/feature-deprecations)). First, call RetrieveImportableDomains to list domains to which the calling user has sufficient access. Then call ImportDomain on any domain names you want to use with Cloud Domains.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Registration {
    /// Required. Settings for contact information linked to the Registration. You cannot update these with the UpdateRegistration method. To update these settings, use the ConfigureContactSettings method.
    #[serde(default, rename = "contactSettings")]
    pub contact_settings: ::core::option::Option<ContactSettings>,
    /// Output only. The creation timestamp of the Registration resource.
    #[serde(default, rename = "createTime")]
    pub create_time: ::core::option::Option<String>,
    /// Settings controlling the DNS configuration of the Registration. You cannot update these with the UpdateRegistration method. To update these settings, use the ConfigureDnsSettings method.
    #[serde(default, rename = "dnsSettings")]
    pub dns_settings: ::core::option::Option<DnsSettings>,
    /// Required. Immutable. The domain name. Unicode domain names must be expressed in Punycode format.
    #[serde(default, rename = "domainName")]
    pub domain_name: ::core::option::Option<String>,
    /// Output only. Special properties of the domain.
    #[serde(default, rename = "domainProperties")]
    pub domain_properties: ::core::option::Option<::std::vec::Vec<String>>,
    /// Output only. The expiration timestamp of the Registration.
    #[serde(default, rename = "expireTime")]
    pub expire_time: ::core::option::Option<String>,
    /// Output only. The set of issues with the Registration that require attention.
    #[serde(default)]
    pub issues: ::core::option::Option<::std::vec::Vec<String>>,
    /// Set of labels associated with the Registration.
    #[serde(default)]
    pub labels: ::core::option::Option<serde_json::Value>,
    /// Settings for management of the Registration, including renewal, billing, and transfer. You cannot update these with the UpdateRegistration method. To update these settings, use the ConfigureManagementSettings method.
    #[serde(default, rename = "managementSettings")]
    pub management_settings: ::core::option::Option<ManagementSettings>,
    /// Output only. Name of the Registration resource, in the format projects/*/locations/*/registrations/.
    #[serde(default)]
    pub name: ::core::option::Option<String>,
    /// Output only. Pending contact settings for the Registration. Updates to the contact_settings field that change its registrant_contact or privacy fields require email confirmation by the registrant_contact before taking effect. This field is set only if there are pending updates to the contact_settings that have not been confirmed. To confirm the changes, the registrant_contact must follow the instructions in the email they receive.
    #[serde(default, rename = "pendingContactSettings")]
    pub pending_contact_settings: ::core::option::Option<ContactSettings>,
    /// Output only. The reason the domain registration failed. Only set for domains in REGISTRATION_FAILED state. // TODO: enum values: ["REGISTER_FAILURE_REASON_UNSPECIFIED", "REGISTER_FAILURE_REASON_UNKNOWN", "DOMAIN_NOT_AVAILABLE", "INVALID_CONTACTS"]
    #[serde(default, rename = "registerFailureReason")]
    pub register_failure_reason: ::core::option::Option<String>,
    /// Output only. The state of the Registration // TODO: enum values: ["STATE_UNSPECIFIED", "REGISTRATION_PENDING", "REGISTRATION_FAILED", "TRANSFER_PENDING", "TRANSFER_FAILED", "IMPORT_PENDING", "ACTIVE", "SUSPENDED", "EXPORTED", "EXPIRED"]
    #[serde(default)]
    pub state: ::core::option::Option<String>,
    /// Output only. Set of options for the contact_settings.privacy field that this Registration supports.
    #[serde(default, rename = "supportedPrivacy")]
    pub supported_privacy: ::core::option::Option<::std::vec::Vec<String>>,
    /// Output only. Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/domains/docs/deprecations/feature-deprecations). The reason the domain transfer failed. Only set for domains in TRANSFER_FAILED state. // TODO: enum values: ["TRANSFER_FAILURE_REASON_UNSPECIFIED", "TRANSFER_FAILURE_REASON_UNKNOWN", "EMAIL_CONFIRMATION_FAILURE", "DOMAIN_NOT_REGISTERED", "DOMAIN_HAS_TRANSFER_LOCK", "INVALID_AUTHORIZATION_CODE", "TRANSFER_CANCELLED", "TRANSFER_REJECTED", "INVALID_REGISTRANT_EMAIL_ADDRESS", "DOMAIN_NOT_ELIGIBLE_FOR_TRANSFER", "TRANSFER_ALREADY_PENDING"]
    #[serde(default, rename = "transferFailureReason")]
    pub transfer_failure_reason: ::core::option::Option<String>,
}

/// Request for the RenewDomain method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RenewDomainRequest {
    /// Optional. When true, only validation is performed, without actually renewing the domain. For more information, see [Request validation](https://cloud.google.com/apis/design/design_patterns#request_validation)
    #[serde(default, rename = "validateOnly")]
    pub validate_only: ::core::option::Option<bool>,
    /// Required. Acknowledgement of the price to renew the domain for one year. To get the price, see [Cloud Domains pricing](https://cloud.google.com/domains/pricing). If not provided, the expected price is returned in the error message.
    #[serde(default, rename = "yearlyPrice")]
    pub yearly_price: ::core::option::Option<Money>,
}

/// A unit of data that is returned by the DNS servers.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ResourceRecordSet {
    /// For example, www.example.com.
    #[serde(default)]
    pub name: ::core::option::Option<String>,
    /// Configures dynamic query responses based on either the geo location of the querying user or a weighted round robin based routing policy. A valid ResourceRecordSet contains only rrdata (for static resolution) or a routing_policy (for dynamic resolution).
    #[serde(default, rename = "routingPolicy")]
    pub routing_policy: ::core::option::Option<RRSetRoutingPolicy>,
    /// As defined in RFC 1035 (section 5) and RFC 1034 (section 3.6.1) -- see examples.
    #[serde(default)]
    pub rrdata: ::core::option::Option<::std::vec::Vec<String>>,
    /// As defined in RFC 4034 (section 3.2).
    #[serde(default, rename = "signatureRrdata")]
    pub signature_rrdata: ::core::option::Option<::std::vec::Vec<String>>,
    /// Number of seconds that this ResourceRecordSet can be cached by resolvers.
    #[serde(default)]
    pub ttl: ::core::option::Option<i32>,
    /// The identifier of a supported record type. See the list of Supported DNS record types.
    #[serde(default, rename = "type")]
    pub type_: ::core::option::Option<String>,
}

/// Response for the RetrieveGoogleDomainsDnsRecords method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RetrieveGoogleDomainsDnsRecordsResponse {
    /// When present, there are more results to retrieve. Set page_token to this value on a subsequent call to get the next page of results.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
    /// The resource record set resources (DNS Zone records).
    #[serde(default)]
    pub rrset: ::core::option::Option<::std::vec::Vec<ResourceRecordSet>>,
}

/// Response for the RetrieveGoogleDomainsForwardingConfig method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RetrieveGoogleDomainsForwardingConfigResponse {
    /// The list of domain forwarding configurations. A forwarding configuration might not work correctly if the required DNS records are not present in the domain''s authoritative DNS zone.
    #[serde(default, rename = "domainForwardings")]
    pub domain_forwardings: ::core::option::Option<::std::vec::Vec<DomainForwarding>>,
    /// The list of email forwarding configurations. A forwarding configuration might not work correctly if the required DNS records are not present in the domain''s authoritative DNS zone.
    #[serde(default, rename = "emailForwardings")]
    pub email_forwardings: ::core::option::Option<::std::vec::Vec<EmailForwarding>>,
}

/// Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/domains/docs/deprecations/feature-deprecations). Response for the RetrieveImportableDomains method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RetrieveImportableDomainsResponse {
    /// A list of domains that the calling user manages in Google Domains.
    #[serde(default)]
    pub domains: ::core::option::Option<::std::vec::Vec<Domain>>,
    /// When present, there are more results to retrieve. Set page_token to this value on a subsequent call to get the next page of results.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
}

/// Response for the RetrieveRegisterParameters method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RetrieveRegisterParametersResponse {
    /// Parameters to use when calling the RegisterDomain method.
    #[serde(default, rename = "registerParameters")]
    pub register_parameters: ::core::option::Option<RegisterParameters>,
}

/// Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/domains/docs/deprecations/feature-deprecations). Response for the RetrieveTransferParameters method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RetrieveTransferParametersResponse {
    /// Parameters to use when calling the TransferDomain method.
    #[serde(default, rename = "transferParameters")]
    pub transfer_parameters: ::core::option::Option<TransferParameters>,
}

/// Response for the SearchDomains method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SearchDomainsResponse {
    /// Results of the domain name search.
    #[serde(default, rename = "registerParameters")]
    pub register_parameters: ::core::option::Option<::std::vec::Vec<RegisterParameters>>,
}

/// Request message for SetIamPolicy method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SetIamPolicyRequest {
    /// REQUIRED: The complete policy to be applied to the resource. The size of the policy is limited to a few 10s of KB. An empty policy is a valid policy but certain Google Cloud services (such as Projects) might reject them.
    #[serde(default)]
    pub policy: ::core::option::Option<Policy>,
    /// OPTIONAL: A FieldMask specifying which fields of the policy to modify. Only the fields in the mask will be modified. If no mask is provided, the following default mask is used: paths: "bindings, etag"
    #[serde(default, rename = "updateMask")]
    pub update_mask: ::core::option::Option<String>,
}

/// The Status type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each Status message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Status {
    /// The status code, which should be an enum value of google.rpc.Code.
    #[serde(default)]
    pub code: ::core::option::Option<i32>,
    /// A list of messages that carry the error details. There is a common set of message types for APIs to use.
    #[serde(default)]
    pub details: ::core::option::Option<::std::vec::Vec<serde_json::Value>>,
    /// A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client.
    #[serde(default)]
    pub message: ::core::option::Option<String>,
}

/// Request message for TestIamPermissions method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TestIamPermissionsRequest {
    /// The set of permissions to check for the resource. Permissions with wildcards (such as * or storage.*) are not allowed. For more information see [IAM Overview](https://cloud.google.com/iam/docs/overview#permissions).
    #[serde(default)]
    pub permissions: ::core::option::Option<::std::vec::Vec<String>>,
}

/// Response message for TestIamPermissions method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TestIamPermissionsResponse {
    /// A subset of TestPermissionsRequest.permissions that the caller is allowed.
    #[serde(default)]
    pub permissions: ::core::option::Option<::std::vec::Vec<String>>,
}

/// Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/domains/docs/deprecations/feature-deprecations). Request for the TransferDomain method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TransferDomainRequest {
    /// The domain''s transfer authorization code. You can obtain this from the domain''s current registrar.
    #[serde(default, rename = "authorizationCode")]
    pub authorization_code: ::core::option::Option<AuthorizationCode>,
    /// The list of contact notices that you acknowledge. The notices needed here depend on the values specified in registration.contact_settings.
    #[serde(default, rename = "contactNotices")]
    pub contact_notices: ::core::option::Option<::std::vec::Vec<String>>,
    /// Required. The complete Registration resource to be created. You can leave registration.dns_settings unset to import the domain''s current DNS configuration from its current registrar. Use this option only if you are sure that the domain''s current DNS service does not cease upon transfer, as is often the case for DNS services provided for free by the registrar.
    #[serde(default)]
    pub registration: ::core::option::Option<Registration>,
    /// Validate the request without actually transferring the domain.
    #[serde(default, rename = "validateOnly")]
    pub validate_only: ::core::option::Option<bool>,
    /// Required. Acknowledgement of the price to transfer or renew the domain for one year. Call RetrieveTransferParameters to obtain the price, which you must acknowledge.
    #[serde(default, rename = "yearlyPrice")]
    pub yearly_price: ::core::option::Option<Money>,
}

/// Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/domains/docs/deprecations/feature-deprecations). Parameters required to transfer a domain from another registrar.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TransferParameters {
    /// The registrar that currently manages the domain.
    #[serde(default, rename = "currentRegistrar")]
    pub current_registrar: ::core::option::Option<String>,
    /// The URL of the registrar that currently manages the domain.
    #[serde(default, rename = "currentRegistrarUri")]
    pub current_registrar_uri: ::core::option::Option<String>,
    /// The domain name. Unicode domain names are expressed in Punycode format.
    #[serde(default, rename = "domainName")]
    pub domain_name: ::core::option::Option<String>,
    /// The name servers that currently store the configuration of the domain.
    #[serde(default, rename = "nameServers")]
    pub name_servers: ::core::option::Option<::std::vec::Vec<String>>,
    /// Contact privacy options that the domain supports.
    #[serde(default, rename = "supportedPrivacy")]
    pub supported_privacy: ::core::option::Option<::std::vec::Vec<String>>,
    /// Indicates whether the domain is protected by a transfer lock. For a transfer to succeed, this must show UNLOCKED. To unlock a domain, go to its current registrar. // TODO: enum values: ["TRANSFER_LOCK_STATE_UNSPECIFIED", "UNLOCKED", "LOCKED"]
    #[serde(default, rename = "transferLockState")]
    pub transfer_lock_state: ::core::option::Option<String>,
    /// Price to transfer or renew the domain for one year.
    #[serde(default, rename = "yearlyPrice")]
    pub yearly_price: ::core::option::Option<Money>,
}

/// Configures a RRSetRoutingPolicy that routes in a weighted round robin fashion.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WrrPolicy {
    #[serde(default)]
    pub item: ::core::option::Option<::std::vec::Vec<WrrPolicyItem>>,
}

/// A routing block which contains the routing information for one WRR item.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WrrPolicyItem {
    /// Endpoints that are health checked before making the routing decision. The unhealthy endpoints are omitted from the result. If all endpoints within a bucket are unhealthy, we choose a different bucket (sampled with respect to its weight) for responding. If DNSSEC is enabled for this zone, only one of rrdata or health_checked_targets can be set.
    #[serde(default, rename = "healthCheckedTargets")]
    pub health_checked_targets: ::core::option::Option<HealthCheckTargets>,
    #[serde(default)]
    pub rrdata: ::core::option::Option<::std::vec::Vec<String>>,
    /// DNSSEC generated signatures for all the rrdata within this item. When using health-checked targets for DNSSEC-enabled zones, you can only use at most one health-checked IP address per item.
    #[serde(default, rename = "signatureRrdata")]
    pub signature_rrdata: ::core::option::Option<::std::vec::Vec<String>>,
    /// The weight corresponding to this WrrPolicyItem object. When multiple WrrPolicyItem objects are configured, the probability of returning an WrrPolicyItem object''s data is proportional to its weight relative to the sum of weights configured for all items. This weight must be non-negative.
    #[serde(default)]
    pub weight: ::core::option::Option<f64>,
}
