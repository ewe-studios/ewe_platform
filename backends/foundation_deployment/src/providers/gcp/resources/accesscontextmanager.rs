//! Auto-generated resource types for Google Cloud Platform - accesscontextmanager.
//!
//! This file is generated by `cargo run --bin ewe_platform gen_resource_types`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use super::*;
use serde::{Deserialize, Serialize};

/// Metadata of Access Context Manager''s Long Running Operations.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AccessContextManagerOperationMetadata {}

/// The request message for Operations.CancelOperation.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CancelOperationRequest {}

/// A request to commit dry-run specs in all Service Perimeters belonging to an Access Policy.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CommitServicePerimetersRequest {
    /// Optional. The etag for the version of the Access Policy that this commit operation is to be performed on. If, at the time of commit, the etag for the Access Policy stored in Access Context Manager is different from the specified etag, then the commit operation will not be performed and the call will fail. This field is not required. If etag is not provided, the operation will be performed as if a valid etag is provided.
    #[serde(default)]
    pub etag: ::core::option::Option<String>,
}

/// A response to CommitServicePerimetersRequest. This will be put inside of Operation.response field.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CommitServicePerimetersResponse {
    /// List of all the Service Perimeter instances in the Access Policy.
    #[serde(default, rename = "servicePerimeters")]
    pub service_perimeters: ::core::option::Option<::std::vec::Vec<ServicePerimeter>>,
}

/// A generic empty message that you can re-use to avoid defining duplicated empty messages in your APIs. A typical example is to use it as the request or the response type of an API method. For instance: service Foo { rpc Bar(google.protobuf.Empty) returns (google.protobuf.Empty); }
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Empty {}

/// Metadata of Google Cloud Access Binding Long Running Operations.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GcpUserAccessBindingOperationMetadata {}

/// Request message for GetIamPolicy method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetIamPolicyRequest {
    /// OPTIONAL: A GetPolicyOptions object for specifying options to GetIamPolicy.
    #[serde(default)]
    pub options: ::core::option::Option<GetPolicyOptions>,
}

/// A response to ListAccessLevelsRequest.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListAccessLevelsResponse {
    /// List of the Access Level instances.
    #[serde(default, rename = "accessLevels")]
    pub access_levels: ::core::option::Option<::std::vec::Vec<AccessLevel>>,
    /// The pagination token to retrieve the next page of results. If the value is empty, no further results remain.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
}

/// A response to ListAccessPoliciesRequest.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListAccessPoliciesResponse {
    /// List of the AccessPolicy instances.
    #[serde(default, rename = "accessPolicies")]
    pub access_policies: ::core::option::Option<::std::vec::Vec<AccessPolicy>>,
    /// The pagination token to retrieve the next page of results. If the value is empty, no further results remain.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
}

/// A response to ListAuthorizedOrgsDescsRequest.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListAuthorizedOrgsDescsResponse {
    /// List of all the Authorized Orgs Desc instances.
    #[serde(default, rename = "authorizedOrgsDescs")]
    pub authorized_orgs_descs: ::core::option::Option<::std::vec::Vec<AuthorizedOrgsDesc>>,
    /// The pagination token to retrieve the next page of results. If the value is empty, no further results remain.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
}

/// Response of ListGcpUserAccessBindings.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListGcpUserAccessBindingsResponse {
    /// GcpUserAccessBinding
    #[serde(default, rename = "gcpUserAccessBindings")]
    pub gcp_user_access_bindings: ::core::option::Option<::std::vec::Vec<GcpUserAccessBinding>>,
    /// Token to get the next page of items. If blank, there are no more items.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
}

/// The response message for Operations.ListOperations.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListOperationsResponse {
    /// The standard List next-page token.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
    /// A list of operations that matches the specified filter in the request.
    #[serde(default)]
    pub operations: ::core::option::Option<::std::vec::Vec<Operation>>,
    /// Unordered list. Unreachable resources. Populated when the request sets ListOperationsRequest.return_partial_success and reads across collections. For example, when attempting to list all resources across all supported locations.
    #[serde(default)]
    pub unreachable: ::core::option::Option<::std::vec::Vec<String>>,
}

/// A response to ListServicePerimetersRequest.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListServicePerimetersResponse {
    /// The pagination token to retrieve the next page of results. If the value is empty, no further results remain.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
    /// List of the Service Perimeter instances.
    #[serde(default, rename = "servicePerimeters")]
    pub service_perimeters: ::core::option::Option<::std::vec::Vec<ServicePerimeter>>,
}

/// A response to ListSupportedPermissionsRequest.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListSupportedPermissionsResponse {
    /// The pagination token to retrieve the next page of results. If the value is empty, no further results remain.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
    /// List of VPC-SC supported permissions.
    #[serde(default, rename = "supportedPermissions")]
    pub supported_permissions: ::core::option::Option<::std::vec::Vec<String>>,
}

/// A response to ListSupportedServicesRequest.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListSupportedServicesResponse {
    /// The pagination token to retrieve the next page of results. If the value is empty, no further results remain.
    #[serde(default, rename = "nextPageToken")]
    pub next_page_token: ::core::option::Option<String>,
    /// List of services supported by VPC Service Controls instances.
    #[serde(default, rename = "supportedServices")]
    pub supported_services: ::core::option::Option<::std::vec::Vec<SupportedService>>,
}

/// A request to replace all existing Access Levels in an Access Policy with the Access Levels provided. This is done atomically.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ReplaceAccessLevelsRequest {
    /// Required. The desired Access Levels that should replace all existing Access Levels in the Access Policy.
    #[serde(default, rename = "accessLevels")]
    pub access_levels: ::core::option::Option<::std::vec::Vec<AccessLevel>>,
    /// Optional. The etag for the version of the Access Policy that this replace operation is to be performed on. If, at the time of replace, the etag for the Access Policy stored in Access Context Manager is different from the specified etag, then the replace operation will not be performed and the call will fail. This field is not required. If etag is not provided, the operation will be performed as if a valid etag is provided.
    #[serde(default)]
    pub etag: ::core::option::Option<String>,
}

/// A response to ReplaceAccessLevelsRequest. This will be put inside of Operation.response field.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ReplaceAccessLevelsResponse {
    /// List of the Access Level instances.
    #[serde(default, rename = "accessLevels")]
    pub access_levels: ::core::option::Option<::std::vec::Vec<AccessLevel>>,
}

/// A request to replace all existing Service Perimeters in an Access Policy with the Service Perimeters provided. This is done atomically.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ReplaceServicePerimetersRequest {
    /// Optional. The etag for the version of the Access Policy that this replace operation is to be performed on. If, at the time of replace, the etag for the Access Policy stored in Access Context Manager is different from the specified etag, then the replace operation will not be performed and the call will fail. This field is not required. If etag is not provided, the operation will be performed as if a valid etag is provided.
    #[serde(default)]
    pub etag: ::core::option::Option<String>,
    /// Required. The desired Service Perimeters that should replace all existing Service Perimeters in the Access Policy.
    #[serde(default, rename = "servicePerimeters")]
    pub service_perimeters: ::core::option::Option<::std::vec::Vec<ServicePerimeter>>,
}

/// A response to ReplaceServicePerimetersRequest. This will be put inside of Operation.response field.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ReplaceServicePerimetersResponse {
    /// List of the Service Perimeter instances.
    #[serde(default, rename = "servicePerimeters")]
    pub service_perimeters: ::core::option::Option<::std::vec::Vec<ServicePerimeter>>,
}

/// Request message for SetIamPolicy method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SetIamPolicyRequest {
    /// REQUIRED: The complete policy to be applied to the resource. The size of the policy is limited to a few 10s of KB. An empty policy is a valid policy but certain Google Cloud services (such as Projects) might reject them.
    #[serde(default)]
    pub policy: ::core::option::Option<Policy>,
    /// OPTIONAL: A FieldMask specifying which fields of the policy to modify. Only the fields in the mask will be modified. If no mask is provided, the following default mask is used: paths: "bindings, etag"
    #[serde(default, rename = "updateMask")]
    pub update_mask: ::core::option::Option<String>,
}

/// Request message for TestIamPermissions method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TestIamPermissionsRequest {
    /// The set of permissions to check for the resource. Permissions with wildcards (such as * or storage.*) are not allowed. For more information see [IAM Overview](https://cloud.google.com/iam/docs/overview#permissions).
    #[serde(default)]
    pub permissions: ::core::option::Option<::std::vec::Vec<String>>,
}

/// Response message for TestIamPermissions method.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TestIamPermissionsResponse {
    /// A subset of TestPermissionsRequest.permissions that the caller is allowed.
    #[serde(default)]
    pub permissions: ::core::option::Option<::std::vec::Vec<String>>,
}

/// Encapsulates settings provided to GetIamPolicy.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetPolicyOptions {
    /// Optional. The maximum policy version that will be used to format the policy. Valid values are 0, 1, and 3. Requests specifying an invalid value will be rejected. Requests for policies with any conditional role bindings must specify version 3. Policies with no conditional role bindings may specify any valid value or leave the field unset. The policy in the response might use the policy version that you specified, or it might use a lower policy version. For example, if you specify version 3, but the policy has no conditional role bindings, the response uses version 1. To learn which resources support conditions in their IAM policies, see the [IAM documentation](https://cloud.google.com/iam/help/conditions/resource-policies).
    #[serde(default, rename = "requestedPolicyVersion")]
    pub requested_policy_version: ::core::option::Option<i32>,
}

/// AccessPolicy is a container for AccessLevels (which define the necessary attributes to use Google Cloud services) and ServicePerimeters (which define regions of services able to freely pass data within a perimeter). An access policy is globally visible within an organization, and the restrictions it specifies apply to all projects within an organization.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AccessPolicy {
    /// Output only. An opaque identifier for the current version of the AccessPolicy. This will always be a strongly validated etag, meaning that two Access Policies will be identical if and only if their etags are identical. Clients should not expect this to be in any specific format.
    #[serde(default)]
    pub etag: ::core::option::Option<String>,
    /// Output only. Identifier. Resource name of the AccessPolicy. Format: accessPolicies/{access_policy}
    #[serde(default)]
    pub name: ::core::option::Option<String>,
    /// Required. The parent of this AccessPolicy in the Cloud Resource Hierarchy. Currently immutable once created. Format: organizations/{organization_id}
    #[serde(default)]
    pub parent: ::core::option::Option<String>,
    /// The scopes of the AccessPolicy. Scopes define which resources a policy can restrict and where its resources can be referenced. For example, policy A with scopes=["folders/123"] has the following behavior: - ServicePerimeter can only restrict projects within folders/123. - ServicePerimeter within policy A can only reference access levels defined within policy A. - Only one policy can include a given scope; thus, attempting to create a second policy which includes folders/123 will result in an error. If no scopes are provided, then any resource within the organization can be restricted. Scopes cannot be modified after a policy is created. Policies can only have a single scope. Format: list of folders/{folder_number} or projects/{project_number}
    #[serde(default)]
    pub scopes: ::core::option::Option<::std::vec::Vec<String>>,
    /// Required. Human readable title. Does not affect behavior.
    #[serde(default)]
    pub title: ::core::option::Option<String>,
}

/// AuthorizedOrgsDesc contains data for an organization''s authorization policy.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AuthorizedOrgsDesc {
    /// The asset type of this authorized orgs desc. Valid values are ASSET_TYPE_DEVICE, and ASSET_TYPE_CREDENTIAL_STRENGTH. // TODO: enum values: ["ASSET_TYPE_UNSPECIFIED", "ASSET_TYPE_DEVICE", "ASSET_TYPE_CREDENTIAL_STRENGTH"]
    #[serde(default, rename = "assetType")]
    pub asset_type: ::core::option::Option<String>,
    /// The direction of the authorization relationship between this organization and the organizations listed in the orgs field. The valid values for this field include the following: AUTHORIZATION_DIRECTION_FROM: Allows this organization to evaluate traffic in the organizations listed in the orgs field. AUTHORIZATION_DIRECTION_TO: Allows the organizations listed in the orgs field to evaluate the traffic in this organization. For the authorization relationship to take effect, all of the organizations must authorize and specify the appropriate relationship direction. For example, if organization A authorized organization B and C to evaluate its traffic, by specifying AUTHORIZATION_DIRECTION_TO as the authorization direction, organizations B and C must specify AUTHORIZATION_DIRECTION_FROM as the authorization direction in their AuthorizedOrgsDesc resource. // TODO: enum values: ["AUTHORIZATION_DIRECTION_UNSPECIFIED", "AUTHORIZATION_DIRECTION_TO", "AUTHORIZATION_DIRECTION_FROM"]
    #[serde(default, rename = "authorizationDirection")]
    pub authorization_direction: ::core::option::Option<String>,
    /// A granular control type for authorization levels. Valid value is AUTHORIZATION_TYPE_TRUST. // TODO: enum values: ["AUTHORIZATION_TYPE_UNSPECIFIED", "AUTHORIZATION_TYPE_TRUST"]
    #[serde(default, rename = "authorizationType")]
    pub authorization_type: ::core::option::Option<String>,
    /// Identifier. Resource name for the AuthorizedOrgsDesc. Format: accessPolicies/{access_policy}/authorizedOrgsDescs/{authorized_orgs_desc}. The authorized_orgs_desc component must begin with a letter, followed by alphanumeric characters or _. After you create an AuthorizedOrgsDesc, you cannot change its name.
    #[serde(default)]
    pub name: ::core::option::Option<String>,
    /// The list of organization ids in this AuthorizedOrgsDesc. Format: organizations/ Example: organizations/123456
    #[serde(default)]
    pub orgs: ::core::option::Option<::std::vec::Vec<String>>,
}

/// Restricts access to Cloud Console and Google Cloud APIs for a set of users using Context-Aware Access.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GcpUserAccessBinding {
    /// Optional. Access level that a user must have to be granted access. Only one access level is supported, not multiple. This repeated field must have exactly one element. Example: "accessPolicies/9522/accessLevels/device_trusted"
    #[serde(default, rename = "accessLevels")]
    pub access_levels: ::core::option::Option<::std::vec::Vec<String>>,
    /// Optional. Dry run access level that will be evaluated but will not be enforced. The access denial based on dry run policy will be logged. Only one access level is supported, not multiple. This list must have exactly one element. Example: "accessPolicies/9522/accessLevels/device_trusted"
    #[serde(default, rename = "dryRunAccessLevels")]
    pub dry_run_access_levels: ::core::option::Option<::std::vec::Vec<String>>,
    /// Optional. Immutable. Google Group id whose users are subject to this binding''s restrictions. See "id" in the [Google Workspace Directory API''s Group Resource] (https://developers.google.com/admin-sdk/directory/v1/reference/groups#resource). If a group''s email address/alias is changed, this resource will continue to point at the changed group. This field does not accept group email addresses or aliases. Example: "01d520gv4vjcrht"
    #[serde(default, rename = "groupKey")]
    pub group_key: ::core::option::Option<String>,
    /// Immutable. Assigned by the server during creation. The last segment has an arbitrary length and has only URI unreserved characters (as defined by [RFC 3986 Section 2.3](https://tools.ietf.org/html/rfc3986#section-2.3)). Should not be specified by the client during creation. Example: "organizations/256/gcpUserAccessBindings/b3-BhcX_Ud5N"
    #[serde(default)]
    pub name: ::core::option::Option<String>,
    /// Optional. A list of applications that are subject to this binding''s restrictions. If the list is empty, the binding restrictions will universally apply to all applications.
    #[serde(default, rename = "restrictedClientApplications")]
    pub restricted_client_applications: ::core::option::Option<::std::vec::Vec<Application>>,
    /// Optional. A list of scoped access settings that set this binding''s restrictions on a subset of applications. This field cannot be set if restricted_client_applications is set.
    #[serde(default, rename = "scopedAccessSettings")]
    pub scoped_access_settings: ::core::option::Option<::std::vec::Vec<ScopedAccessSettings>>,
    /// Optional. The Google Cloud session length (GCSL) policy for the group key.
    #[serde(default, rename = "sessionSettings")]
    pub session_settings: ::core::option::Option<SessionSettings>,
}

/// This resource represents a long-running operation that is the result of a network API call.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Operation {
    /// If the value is false, it means the operation is still in progress. If true, the operation is completed, and either error or response is available.
    #[serde(default)]
    pub done: ::core::option::Option<bool>,
    /// The error result of the operation in case of failure or cancellation.
    #[serde(default)]
    pub error: ::core::option::Option<Status>,
    /// Service-specific metadata associated with the operation. It typically contains progress information and common metadata such as create time. Some services might not provide such metadata. Any method that returns a long-running operation should document the metadata type, if any.
    #[serde(default)]
    pub metadata: ::core::option::Option<serde_json::Value>,
    /// The server-assigned name, which is only unique within the same service that originally returns it. If you use the default HTTP mapping, the name should be a resource name ending with operations/{unique_id}.
    #[serde(default)]
    pub name: ::core::option::Option<String>,
    /// The normal, successful response of the operation. If the original method returns no data on success, such as Delete, the response is google.protobuf.Empty. If the original method is standard Get/Create/Update, the response should be the resource. For other methods, the response should have the type XxxResponse, where Xxx is the original method name. For example, if the original method name is TakeSnapshot(), the inferred response type is TakeSnapshotResponse.
    #[serde(default)]
    pub response: ::core::option::Option<serde_json::Value>,
}

/// SupportedService specifies the VPC Service Controls and its properties.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SupportedService {
    /// True if the service is available on the restricted VIP. Services on the restricted VIP typically either support VPC Service Controls or are core infrastructure services required for the functioning of Google Cloud.
    #[serde(default, rename = "availableOnRestrictedVip")]
    pub available_on_restricted_vip: ::core::option::Option<bool>,
    /// True if the service is supported with some limitations. Check [documentation](https://cloud.google.com/vpc-service-controls/docs/supported-products) for details.
    #[serde(default, rename = "knownLimitations")]
    pub known_limitations: ::core::option::Option<bool>,
    /// The service name or address of the supported service, such as service.googleapis.com.
    #[serde(default)]
    pub name: ::core::option::Option<String>,
    /// The support stage of the service. // TODO: enum values: ["SERVICE_SUPPORT_STAGE_UNSPECIFIED", "GA", "PREVIEW", "DEPRECATED"]
    #[serde(default, rename = "serviceSupportStage")]
    pub service_support_stage: ::core::option::Option<String>,
    /// The support stage of the service. // TODO: enum values: ["LAUNCH_STAGE_UNSPECIFIED", "UNIMPLEMENTED", "PRELAUNCH", "EARLY_ACCESS", "ALPHA", "BETA", "GA", "DEPRECATED"]
    #[serde(default, rename = "supportStage")]
    pub support_stage: ::core::option::Option<String>,
    /// The list of the supported methods. This field exists only in response to GetSupportedService
    #[serde(default, rename = "supportedMethods")]
    pub supported_methods: ::core::option::Option<::std::vec::Vec<MethodSelector>>,
    /// The name of the supported product, such as ''Cloud Product API''.
    #[serde(default)]
    pub title: ::core::option::Option<String>,
}

/// An AccessLevel is a label that can be applied to requests to Google Cloud services, along with a list of requirements necessary for the label to be applied.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AccessLevel {
    /// A BasicLevel composed of Conditions.
    #[serde(default)]
    pub basic: ::core::option::Option<BasicLevel>,
    /// A CustomLevel written in the Common Expression Language.
    #[serde(default)]
    pub custom: ::core::option::Option<CustomLevel>,
    /// Description of the AccessLevel and its use. Does not affect behavior.
    #[serde(default)]
    pub description: ::core::option::Option<String>,
    /// Identifier. Resource name for the AccessLevel. Format: accessPolicies/{access_policy}/accessLevels/{access_level}. The access_level component must begin with a letter, followed by alphanumeric characters or _. Its maximum length is 50 characters. After you create an AccessLevel, you cannot change its name.
    #[serde(default)]
    pub name: ::core::option::Option<String>,
    /// Human readable title. Must be unique within the Policy.
    #[serde(default)]
    pub title: ::core::option::Option<String>,
}

/// ServicePerimeter describes a set of Google Cloud resources which can freely import and export data amongst themselves, but not export outside of the ServicePerimeter. If a request with a source within this ServicePerimeter has a target outside of the ServicePerimeter, the request will be blocked. Otherwise the request is allowed. There are two types of Service Perimeter - Regular and Bridge. Regular Service Perimeters cannot overlap, a single Google Cloud project or VPC network can only belong to a single regular Service Perimeter. Service Perimeter Bridges can contain only Google Cloud projects as members, a single Google Cloud project may belong to multiple Service Perimeter Bridges.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ServicePerimeter {
    /// Description of the ServicePerimeter and its use. Does not affect behavior.
    #[serde(default)]
    pub description: ::core::option::Option<String>,
    /// Optional. An opaque identifier for the current version of the ServicePerimeter. This identifier does not follow any specific format. If an etag is not provided, the operation will be performed as if a valid etag is provided.
    #[serde(default)]
    pub etag: ::core::option::Option<String>,
    /// Identifier. Resource name for the ServicePerimeter. Format: accessPolicies/{access_policy}/servicePerimeters/{service_perimeter}. The service_perimeter component must begin with a letter, followed by alphanumeric characters or _. After you create a ServicePerimeter, you cannot change its name.
    #[serde(default)]
    pub name: ::core::option::Option<String>,
    /// Perimeter type indicator. A single project or VPC network is allowed to be a member of single regular perimeter, but multiple service perimeter bridges. A project cannot be a included in a perimeter bridge without being included in regular perimeter. For perimeter bridges, the restricted service list as well as access level lists must be empty. // TODO: enum values: ["PERIMETER_TYPE_REGULAR", "PERIMETER_TYPE_BRIDGE"]
    #[serde(default, rename = "perimeterType")]
    pub perimeter_type: ::core::option::Option<String>,
    /// Proposed (or dry run) ServicePerimeter configuration. This configuration allows to specify and test ServicePerimeter configuration without enforcing actual access restrictions. Only allowed to be set when the "use_explicit_dry_run_spec" flag is set.
    #[serde(default)]
    pub spec: ::core::option::Option<ServicePerimeterConfig>,
    /// Current ServicePerimeter configuration. Specifies sets of resources, restricted services and access levels that determine perimeter content and boundaries.
    #[serde(default)]
    pub status: ::core::option::Option<ServicePerimeterConfig>,
    /// Human readable title. Must be unique within the Policy.
    #[serde(default)]
    pub title: ::core::option::Option<String>,
    /// Use explicit dry run spec flag. Ordinarily, a dry-run spec implicitly exists for all Service Perimeters, and that spec is identical to the status for those Service Perimeters. When this flag is set, it inhibits the generation of the implicit spec, thereby allowing the user to explicitly provide a configuration ("spec") to use in a dry-run version of the Service Perimeter. This allows the user to test changes to the enforced config ("status") without actually enforcing them. This testing is done through analyzing the differences between currently enforced and suggested restrictions. use_explicit_dry_run_spec must bet set to True if any of the fields in the spec are set to non-default values.
    #[serde(default, rename = "useExplicitDryRunSpec")]
    pub use_explicit_dry_run_spec: ::core::option::Option<bool>,
}

/// An Identity and Access Management (IAM) policy, which specifies access controls for Google Cloud resources. A Policy is a collection of bindings. A binding binds one or more members, or principals, to a single role. Principals can be user accounts, service accounts, Google groups, and domains (such as G Suite). A role is a named list of permissions; each role can be an IAM predefined role or a user-created custom role. For some types of Google Cloud resources, a binding can also specify a condition, which is a logical expression that allows access to a resource only if the expression evaluates to true. A condition can add constraints based on attributes of the request, the resource, or both. To learn which resources support conditions in their IAM policies, see the [IAM documentation](https://cloud.google.com/iam/help/conditions/resource-policies). **JSON example:**  { "bindings": [ { "role": "roles/resourcemanager.organizationAdmin", "members": [ "user:mike@example.com", "group:admins@example.com", "domain:google.com", "serviceAccount:my-project-id@appspot.gserviceaccount.com" ] }, { "role": "roles/resourcemanager.organizationViewer", "members": [ "user:eve@example.com" ], "condition": { "title": "expirable access", "description": "Does not grant access after Sep 2020", "expression": "request.time &lt; timestamp(''2020-10-01T00:00:00.000Z'')", } } ], "etag": "BwWWja0YfJA=", "version": 3 }  **YAML example:**  bindings: - members: - user:mike@example.com - group:admins@example.com - domain:google.com - serviceAccount:my-project-id@appspot.gserviceaccount.com role: roles/resourcemanager.organizationAdmin - members: - user:eve@example.com role: roles/resourcemanager.organizationViewer condition: title: expirable access description: Does not grant access after Sep 2020 expression: request.time &lt; timestamp(''2020-10-01T00:00:00.000Z'') etag: BwWWja0YfJA= version: 3  For a description of IAM and its features, see the [IAM documentation](https://cloud.google.com/iam/docs/).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Policy {
    /// Specifies cloud audit logging configuration for this policy.
    #[serde(default, rename = "auditConfigs")]
    pub audit_configs: ::core::option::Option<::std::vec::Vec<AuditConfig>>,
    /// Associates a list of members, or principals, with a role. Optionally, may specify a condition that determines how and when the bindings are applied. Each of the bindings must contain at least one principal. The bindings in a Policy can refer to up to 1,500 principals; up to 250 of these principals can be Google groups. Each occurrence of a principal counts towards these limits. For example, if the bindings grant 50 different roles to user:alice@example.com, and not to any other principal, then you can add another 1,450 principals to the bindings in the Policy.
    #[serde(default)]
    pub bindings: ::core::option::Option<::std::vec::Vec<Binding>>,
    /// etag is used for optimistic concurrency control as a way to help prevent simultaneous updates of a policy from overwriting each other. It is strongly suggested that systems make use of the etag in the read-modify-write cycle to perform policy updates in order to avoid race conditions: An etag is returned in the response to getIamPolicy, and systems are expected to put that etag in the request to setIamPolicy to ensure that their change will be applied to the same version of the policy. **Important:** If you use IAM Conditions, you must include the etag field whenever you call setIamPolicy. If you omit this field, then IAM allows you to overwrite a version 3 policy with a version 1 policy, and all of the conditions in the version 3 policy are lost.
    #[serde(default)]
    pub etag: ::core::option::Option<String>,
    /// Specifies the format of the policy. Valid values are 0, 1, and 3. Requests that specify an invalid value are rejected. Any operation that affects conditional role bindings must specify version 3. This requirement applies to the following operations: * Getting a policy that includes a conditional role binding * Adding a conditional role binding to a policy * Changing a conditional role binding in a policy * Removing any role binding, with or without a condition, from a policy that includes conditions **Important:** If you use IAM Conditions, you must include the etag field whenever you call setIamPolicy. If you omit this field, then IAM allows you to overwrite a version 3 policy with a version 1 policy, and all of the conditions in the version 3 policy are lost. If a policy does not include any conditions, operations on that policy may specify any valid version or leave the field unset. To learn which resources support conditions in their IAM policies, see the [IAM documentation](https://cloud.google.com/iam/help/conditions/resource-policies).
    #[serde(default)]
    pub version: ::core::option::Option<i32>,
}

/// A relationship between access settings and its scope.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ScopedAccessSettings {
    /// Optional. Access settings for this scoped access settings. This field may be empty if dry_run_settings is set.
    #[serde(default, rename = "activeSettings")]
    pub active_settings: ::core::option::Option<AccessSettings>,
    /// Optional. Dry-run access settings for this scoped access settings. This field may be empty if active_settings is set.
    #[serde(default, rename = "dryRunSettings")]
    pub dry_run_settings: ::core::option::Option<AccessSettings>,
    /// Optional. Application, etc. to which the access settings will be applied to. Implicitly, this is the scoped access settings key; as such, it must be unique and non-empty.
    #[serde(default)]
    pub scope: ::core::option::Option<AccessScope>,
}

/// The Status type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each Status message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Status {
    /// The status code, which should be an enum value of google.rpc.Code.
    #[serde(default)]
    pub code: ::core::option::Option<i32>,
    /// A list of messages that carry the error details. There is a common set of message types for APIs to use.
    #[serde(default)]
    pub details: ::core::option::Option<::std::vec::Vec<serde_json::Value>>,
    /// A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the google.rpc.Status.details field, or localized by the client.
    #[serde(default)]
    pub message: ::core::option::Option<String>,
}

/// BasicLevel is an AccessLevel using a set of recommended features.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BasicLevel {
    /// How the conditions list should be combined to determine if a request is granted this AccessLevel. If AND is used, each Condition in conditions must be satisfied for the AccessLevel to be applied. If OR is used, at least one Condition in conditions must be satisfied for the AccessLevel to be applied. Default behavior is AND. // TODO: enum values: ["AND", "OR"]
    #[serde(default, rename = "combiningFunction")]
    pub combining_function: ::core::option::Option<String>,
    /// Required. A list of requirements for the AccessLevel to be granted.
    #[serde(default)]
    pub conditions: ::core::option::Option<::std::vec::Vec<Condition>>,
}

/// CustomLevel is an AccessLevel using the Cloud Common Expression Language to represent the necessary conditions for the level to apply to a request. See CEL spec at: https://github.com/google/cel-spec
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CustomLevel {
    /// Required. A Cloud CEL expression evaluating to a boolean.
    #[serde(default)]
    pub expr: ::core::option::Option<Expr>,
}

/// ServicePerimeterConfig specifies a set of Google Cloud resources that describe specific Service Perimeter configuration.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ServicePerimeterConfig {
    /// A list of AccessLevel resource names that allow resources within the ServicePerimeter to be accessed from the internet. AccessLevels listed must be in the same policy as this ServicePerimeter. Referencing a nonexistent AccessLevel is a syntax error. If no AccessLevel names are listed, resources within the perimeter can only be accessed via Google Cloud calls with request origins within the perimeter. Example: "accessPolicies/MY_POLICY/accessLevels/MY_LEVEL". For Service Perimeter Bridge, must be empty.
    #[serde(default, rename = "accessLevels")]
    pub access_levels: ::core::option::Option<::std::vec::Vec<String>>,
    /// List of EgressPolicies to apply to the perimeter. A perimeter may have multiple EgressPolicies, each of which is evaluated separately. Access is granted if any EgressPolicy grants it. Must be empty for a perimeter bridge.
    #[serde(default, rename = "egressPolicies")]
    pub egress_policies: ::core::option::Option<::std::vec::Vec<EgressPolicy>>,
    /// List of IngressPolicies to apply to the perimeter. A perimeter may have multiple IngressPolicies, each of which is evaluated separately. Access is granted if any Ingress Policy grants it. Must be empty for a perimeter bridge.
    #[serde(default, rename = "ingressPolicies")]
    pub ingress_policies: ::core::option::Option<::std::vec::Vec<IngressPolicy>>,
    /// A list of Google Cloud resources that are inside of the service perimeter. Currently only projects and VPCs are allowed. Project format: projects/{project_number} VPC network format: //compute.googleapis.com/projects/{PROJECT_ID}/global/networks/{NAME}.
    #[serde(default)]
    pub resources: ::core::option::Option<::std::vec::Vec<String>>,
    /// Google Cloud services that are subject to the Service Perimeter restrictions. For example, if storage.googleapis.com is specified, access to the storage buckets inside the perimeter must meet the perimeter''s access restrictions.
    #[serde(default, rename = "restrictedServices")]
    pub restricted_services: ::core::option::Option<::std::vec::Vec<String>>,
    /// Configuration for APIs allowed within Perimeter.
    #[serde(default, rename = "vpcAccessibleServices")]
    pub vpc_accessible_services: ::core::option::Option<VpcAccessibleServices>,
}

/// Specifies the audit configuration for a service. The configuration determines which permission types are logged, and what identities, if any, are exempted from logging. An AuditConfig must have one or more AuditLogConfigs. If there are AuditConfigs for both allServices and a specific service, the union of the two AuditConfigs is used for that service: the log_types specified in each AuditConfig are enabled, and the exempted_members in each AuditLogConfig are exempted. Example Policy with multiple AuditConfigs: { "audit_configs": [ { "service": "allServices", "audit_log_configs": [ { "log_type": "DATA_READ", "exempted_members": [ "user:jose@example.com" ] }, { "log_type": "DATA_WRITE" }, { "log_type": "ADMIN_READ" } ] }, { "service": "sampleservice.googleapis.com", "audit_log_configs": [ { "log_type": "DATA_READ" }, { "log_type": "DATA_WRITE", "exempted_members": [ "user:aliya@example.com" ] } ] } ] } For sampleservice, this policy enables DATA_READ, DATA_WRITE and ADMIN_READ logging. It also exempts jose@example.com from DATA_READ logging, and aliya@example.com from DATA_WRITE logging.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AuditConfig {
    /// The configuration for logging of each type of permission.
    #[serde(default, rename = "auditLogConfigs")]
    pub audit_log_configs: ::core::option::Option<::std::vec::Vec<AuditLogConfig>>,
    /// Specifies a service that will be enabled for audit logging. For example, storage.googleapis.com, cloudsql.googleapis.com. allServices is a special value that covers all services.
    #[serde(default)]
    pub service: ::core::option::Option<String>,
}

/// Associates members, or principals, with a role.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Binding {
    /// The condition that is associated with this binding. If the condition evaluates to true, then this binding applies to the current request. If the condition evaluates to false, then this binding does not apply to the current request. However, a different role binding might grant the same role to one or more of the principals in this binding. To learn which resources support conditions in their IAM policies, see the [IAM documentation](https://cloud.google.com/iam/help/conditions/resource-policies).
    #[serde(default)]
    pub condition: ::core::option::Option<Expr>,
    /// Specifies the principals requesting access for a Google Cloud resource. members can have the following values: * allUsers: A special identifier that represents anyone who is on the internet; with or without a Google account. * allAuthenticatedUsers: A special identifier that represents anyone who is authenticated with a Google account or a service account. Does not include identities that come from external identity providers (IdPs) through identity federation. * user:{emailid}: An email address that represents a specific Google account. For example, alice@example.com . * serviceAccount:{emailid}: An email address that represents a Google service account. For example, my-other-app@appspot.gserviceaccount.com. * serviceAccount:{projectid}.svc.id.goog[{namespace}/{kubernetes-sa}]: An identifier for a [Kubernetes service account](https://cloud.google.com/kubernetes-engine/docs/how-to/kubernetes-service-accounts). For example, my-project.svc.id.goog[my-namespace/my-kubernetes-sa]. * group:{emailid}: An email address that represents a Google group. For example, admins@example.com. * domain:{domain}: The G Suite domain (primary) that represents all the users of that domain. For example, google.com or example.com. * principal://iam.googleapis.com/locations/global/workforcePools/{pool_id}/subject/{subject_attribute_value}: A single identity in a workforce identity pool. * principalSet://iam.googleapis.com/locations/global/workforcePools/{pool_id}/group/{group_id}: All workforce identities in a group. * principalSet://iam.googleapis.com/locations/global/workforcePools/{pool_id}/attribute.{attribute_name}/{attribute_value}: All workforce identities with a specific attribute value. * principalSet://iam.googleapis.com/locations/global/workforcePools/{pool_id}/*: All identities in a workforce identity pool. * principal://iam.googleapis.com/projects/{project_number}/locations/global/workloadIdentityPools/{pool_id}/subject/{subject_attribute_value}: A single identity in a workload identity pool. * principalSet://iam.googleapis.com/projects/{project_number}/locations/global/workloadIdentityPools/{pool_id}/group/{group_id}: A workload identity pool group. * principalSet://iam.googleapis.com/projects/{project_number}/locations/global/workloadIdentityPools/{pool_id}/attribute.{attribute_name}/{attribute_value}: All identities in a workload identity pool with a certain attribute. * principalSet://iam.googleapis.com/projects/{project_number}/locations/global/workloadIdentityPools/{pool_id}/*: All identities in a workload identity pool. * deleted:user:{emailid}?uid={uniqueid}: An email address (plus unique identifier) representing a user that has been recently deleted. For example, alice@example.com?uid=123456789012345678901. If the user is recovered, this value reverts to user:{emailid} and the recovered user retains the role in the binding. * deleted:serviceAccount:{emailid}?uid={uniqueid}: An email address (plus unique identifier) representing a service account that has been recently deleted. For example, my-other-app@appspot.gserviceaccount.com?uid=123456789012345678901. If the service account is undeleted, this value reverts to serviceAccount:{emailid} and the undeleted service account retains the role in the binding. * deleted:group:{emailid}?uid={uniqueid}: An email address (plus unique identifier) representing a Google group that has been recently deleted. For example, admins@example.com?uid=123456789012345678901. If the group is recovered, this value reverts to group:{emailid} and the recovered group retains the role in the binding. * deleted:principal://iam.googleapis.com/locations/global/workforcePools/{pool_id}/subject/{subject_attribute_value}: Deleted single identity in a workforce identity pool. For example, deleted:principal://iam.googleapis.com/locations/global/workforcePools/my-pool-id/subject/my-subject-attribute-value.
    #[serde(default)]
    pub members: ::core::option::Option<::std::vec::Vec<String>>,
    /// Role that is assigned to the list of members, or principals. For example, roles/viewer, roles/editor, or roles/owner. For an overview of the IAM roles and permissions, see the [IAM documentation](https://cloud.google.com/iam/docs/roles-overview). For a list of the available pre-defined roles, see [here](https://cloud.google.com/iam/docs/understanding-roles).
    #[serde(default)]
    pub role: ::core::option::Option<String>,
}

/// Access settings represent the set of conditions that must be met for access to be granted. At least one of the fields must be set.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AccessSettings {
    /// Optional. Access level that a user must have to be granted access. Only one access level is supported, not multiple. This repeated field must have exactly one element. Example: "accessPolicies/9522/accessLevels/device_trusted"
    #[serde(default, rename = "accessLevels")]
    pub access_levels: ::core::option::Option<::std::vec::Vec<String>>,
    /// Optional. Session settings applied to user access on a given AccessScope.
    #[serde(default, rename = "sessionSettings")]
    pub session_settings: ::core::option::Option<SessionSettings>,
}

/// Access scope represents the client scope, etc. to which the settings will be applied to.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AccessScope {
    /// Optional. Client scope for this access scope.
    #[serde(default, rename = "clientScope")]
    pub client_scope: ::core::option::Option<ClientScope>,
}

/// A condition necessary for an AccessLevel to be granted. The Condition is an AND over its fields. So a Condition is true if: 1) the request IP is from one of the listed subnetworks AND 2) the originating device complies with the listed device policy AND 3) all listed access levels are granted AND 4) the request was sent at a time allowed by the DateTimeRestriction.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Condition {
    /// Device specific restrictions, all restrictions must hold for the Condition to be true. If not specified, all devices are allowed.
    #[serde(default, rename = "devicePolicy")]
    pub device_policy: ::core::option::Option<DevicePolicy>,
    /// CIDR block IP subnetwork specification. May be IPv4 or IPv6. Note that for a CIDR IP address block, the specified IP address portion must be properly truncated (i.e. all the host bits must be zero) or the input is considered malformed. For example, "192.0.2.0/24" is accepted but "192.0.2.1/24" is not. Similarly, for IPv6, "2001:db8::/32" is accepted whereas "2001:db8::1/32" is not. The originating IP of a request must be in one of the listed subnets in order for this Condition to be true. If empty, all IP addresses are allowed.
    #[serde(default, rename = "ipSubnetworks")]
    pub ip_subnetworks: ::core::option::Option<::std::vec::Vec<String>>,
    /// The request must be made by one of the provided user or service accounts. Groups are not supported. Syntax: user:{emailid} serviceAccount:{emailid} If not specified, a request may come from any user.
    #[serde(default)]
    pub members: ::core::option::Option<::std::vec::Vec<String>>,
    /// Whether to negate the Condition. If true, the Condition becomes a NAND over its non-empty fields. Any non-empty field criteria evaluating to false will result in the Condition to be satisfied. Defaults to false.
    #[serde(default)]
    pub negate: ::core::option::Option<bool>,
    /// The request must originate from one of the provided countries/regions. Must be valid ISO 3166-1 alpha-2 codes.
    #[serde(default)]
    pub regions: ::core::option::Option<::std::vec::Vec<String>>,
    /// A list of other access levels defined in the same Policy, referenced by resource name. Referencing an AccessLevel which does not exist is an error. All access levels listed must be granted for the Condition to be true. Example: "accessPolicies/MY_POLICY/accessLevels/LEVEL_NAME"
    #[serde(default, rename = "requiredAccessLevels")]
    pub required_access_levels: ::core::option::Option<::std::vec::Vec<String>>,
    /// The request must originate from one of the provided VPC networks in Google Cloud. Cannot specify this field together with ip_subnetworks.
    #[serde(default, rename = "vpcNetworkSources")]
    pub vpc_network_sources: ::core::option::Option<::std::vec::Vec<VpcNetworkSource>>,
}

/// Policy for egress from perimeter. EgressPolicies match requests based on egress_from and egress_to stanzas. For an EgressPolicy to match, both egress_from and egress_to stanzas must be matched. If an EgressPolicy matches a request, the request is allowed to span the ServicePerimeter boundary. For example, an EgressPolicy can be used to allow VMs on networks within the ServicePerimeter to access a defined set of projects outside the perimeter in certain contexts (e.g. to read data from a Cloud Storage bucket or query against a BigQuery dataset). EgressPolicies are concerned with the *resources* that a request relates as well as the API services and API actions being used. They do not related to the direction of data movement. More detailed documentation for this concept can be found in the descriptions of EgressFrom and EgressTo.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EgressPolicy {
    /// Defines conditions on the source of a request causing this EgressPolicy to apply.
    #[serde(default, rename = "egressFrom")]
    pub egress_from: ::core::option::Option<EgressFrom>,
    /// Defines the conditions on the ApiOperation and destination resources that cause this EgressPolicy to apply.
    #[serde(default, rename = "egressTo")]
    pub egress_to: ::core::option::Option<EgressTo>,
    /// Optional. Human-readable title for the egress rule. The title must be unique within the perimeter and can not exceed 100 characters. Within the access policy, the combined length of all rule titles must not exceed 240,000 characters.
    #[serde(default)]
    pub title: ::core::option::Option<String>,
}

/// Policy for ingress into ServicePerimeter. IngressPolicies match requests based on ingress_from and ingress_to stanzas. For an ingress policy to match, both the ingress_from and ingress_to stanzas must be matched. If an IngressPolicy matches a request, the request is allowed through the perimeter boundary from outside the perimeter. For example, access from the internet can be allowed either based on an AccessLevel or, for traffic hosted on Google Cloud, the project of the source network. For access from private networks, using the project of the hosting network is required. Individual ingress policies can be limited by restricting which services and/or actions they match using the ingress_to field.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct IngressPolicy {
    /// Defines the conditions on the source of a request causing this IngressPolicy to apply.
    #[serde(default, rename = "ingressFrom")]
    pub ingress_from: ::core::option::Option<IngressFrom>,
    /// Defines the conditions on the ApiOperation and request destination that cause this IngressPolicy to apply.
    #[serde(default, rename = "ingressTo")]
    pub ingress_to: ::core::option::Option<IngressTo>,
    /// Optional. Human-readable title for the ingress rule. The title must be unique within the perimeter and can not exceed 100 characters. Within the access policy, the combined length of all rule titles must not exceed 240,000 characters.
    #[serde(default)]
    pub title: ::core::option::Option<String>,
}

/// Specifies how APIs are allowed to communicate within the Service Perimeter.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct VpcAccessibleServices {
    /// The list of APIs usable within the Service Perimeter. Must be empty unless ''enable_restriction'' is True. You can specify a list of individual services, as well as include the ''RESTRICTED-SERVICES'' value, which automatically includes all of the services protected by the perimeter.
    #[serde(default, rename = "allowedServices")]
    pub allowed_services: ::core::option::Option<::std::vec::Vec<String>>,
    /// Whether to restrict API calls within the Service Perimeter to the list of APIs specified in ''allowed_services''.
    #[serde(default, rename = "enableRestriction")]
    pub enable_restriction: ::core::option::Option<bool>,
}

/// Provides the configuration for logging a type of permissions. Example: { "audit_log_configs": [ { "log_type": "DATA_READ", "exempted_members": [ "user:jose@example.com" ] }, { "log_type": "DATA_WRITE" } ] } This enables ''DATA_READ'' and ''DATA_WRITE'' logging, while exempting jose@example.com from DATA_READ logging.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AuditLogConfig {
    /// Specifies the identities that do not cause logging for this type of permission. Follows the same format of Binding.members.
    #[serde(default, rename = "exemptedMembers")]
    pub exempted_members: ::core::option::Option<::std::vec::Vec<String>>,
    /// The log type that this config enables. // TODO: enum values: ["LOG_TYPE_UNSPECIFIED", "ADMIN_READ", "DATA_WRITE", "DATA_READ"]
    #[serde(default, rename = "logType")]
    pub log_type: ::core::option::Option<String>,
}

/// Represents a textual expression in the Common Expression Language (CEL) syntax. CEL is a C-like expression language. The syntax and semantics of CEL are documented at https://github.com/google/cel-spec. Example (Comparison): title: "Summary size limit" description: "Determines if a summary is less than 100 chars" expression: "document.summary.size() &lt; 100" Example (Equality): title: "Requestor is owner" description: "Determines if requestor is the document owner" expression: "document.owner == request.auth.claims.email" Example (Logic): title: "Public documents" description: "Determine whether the document should be publicly visible" expression: "document.type != ''private'' && document.type != ''internal''" Example (Data Manipulation): title: "Notification string" description: "Create a notification string with a timestamp." expression: "''New message received at '' + string(document.create_time)" The exact variables and functions that may be referenced within an expression are determined by the service that evaluates it. See the service documentation for additional information.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Expr {
    /// Optional. Description of the expression. This is a longer text which describes the expression, e.g. when hovered over it in a UI.
    #[serde(default)]
    pub description: ::core::option::Option<String>,
    /// Textual representation of an expression in Common Expression Language syntax.
    #[serde(default)]
    pub expression: ::core::option::Option<String>,
    /// Optional. String indicating the location of the expression for error reporting, e.g. a file name and a position in the file.
    #[serde(default)]
    pub location: ::core::option::Option<String>,
    /// Optional. Title for the expression, i.e. a short string describing its purpose. This can be used e.g. in UIs which allow to enter the expression.
    #[serde(default)]
    pub title: ::core::option::Option<String>,
}

/// Stores settings related to Google Cloud Session Length including session duration, the type of challenge (i.e. method) they should face when their session expires, and other related settings.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SessionSettings {
    /// Optional. How long a user is allowed to take between actions before a new access token must be issued. Only set for Google Cloud apps.
    #[serde(default, rename = "maxInactivity")]
    pub max_inactivity: ::core::option::Option<String>,
    /// Optional. The session length. Setting this field to zero is equal to disabling session. Also can set infinite session by flipping the enabled bit to false below. If use_oidc_max_age is true, for OIDC apps, the session length will be the minimum of this field and OIDC max_age param.
    #[serde(default, rename = "sessionLength")]
    pub session_length: ::core::option::Option<String>,
    /// Optional. This field enables or disables Google Cloud session length. When false, all fields set above will be disregarded and the session length is basically infinite.
    #[serde(default, rename = "sessionLengthEnabled")]
    pub session_length_enabled: ::core::option::Option<bool>,
    /// Optional. Session method when user''s Google Cloud session is up. // TODO: enum values: ["SESSION_REAUTH_METHOD_UNSPECIFIED", "LOGIN", "SECURITY_KEY", "PASSWORD"]
    #[serde(default, rename = "sessionReauthMethod")]
    pub session_reauth_method: ::core::option::Option<String>,
    /// Optional. Only useful for OIDC apps. When false, the OIDC max_age param, if passed in the authentication request will be ignored. When true, the re-auth period will be the minimum of the session_length field and the max_age OIDC param.
    #[serde(default, rename = "useOidcMaxAge")]
    pub use_oidc_max_age: ::core::option::Option<bool>,
}

/// Client scope represents the application, etc. subject to this binding''s restrictions.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ClientScope {
    /// Optional. The application that is subject to this binding''s scope.
    #[serde(default, rename = "restrictedClientApplication")]
    pub restricted_client_application: ::core::option::Option<Application>,
}

/// DevicePolicy specifies device specific restrictions necessary to acquire a given access level. A DevicePolicy specifies requirements for requests from devices to be granted access levels, it does not do any enforcement on the device. DevicePolicy acts as an AND over all specified fields, and each repeated field is an OR over its elements. Any unset fields are ignored. For example, if the proto is { os_type : DESKTOP_WINDOWS, os_type : DESKTOP_LINUX, encryption_status: ENCRYPTED}, then the DevicePolicy will be true for requests originating from encrypted Linux desktops and encrypted Windows desktops.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DevicePolicy {
    /// Allowed device management levels, an empty list allows all management levels.
    #[serde(default, rename = "allowedDeviceManagementLevels")]
    pub allowed_device_management_levels: ::core::option::Option<::std::vec::Vec<String>>,
    /// Allowed encryptions statuses, an empty list allows all statuses.
    #[serde(default, rename = "allowedEncryptionStatuses")]
    pub allowed_encryption_statuses: ::core::option::Option<::std::vec::Vec<String>>,
    /// Allowed OS versions, an empty list allows all types and all versions.
    #[serde(default, rename = "osConstraints")]
    pub os_constraints: ::core::option::Option<::std::vec::Vec<OsConstraint>>,
    /// Whether the device needs to be approved by the customer admin.
    #[serde(default, rename = "requireAdminApproval")]
    pub require_admin_approval: ::core::option::Option<bool>,
    /// Whether the device needs to be corp owned.
    #[serde(default, rename = "requireCorpOwned")]
    pub require_corp_owned: ::core::option::Option<bool>,
    /// Whether or not screenlock is required for the DevicePolicy to be true. Defaults to false.
    #[serde(default, rename = "requireScreenlock")]
    pub require_screenlock: ::core::option::Option<bool>,
}

/// The originating network source in Google Cloud.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct VpcNetworkSource {
    /// Sub-segment ranges of a VPC network.
    #[serde(default, rename = "vpcSubnetwork")]
    pub vpc_subnetwork: ::core::option::Option<VpcSubNetwork>,
}

/// Defines the conditions under which an EgressPolicy matches a request. Conditions based on information about the source of the request. Note that if the destination of the request is also protected by a ServicePerimeter, then that ServicePerimeter must have an IngressPolicy which allows access in order for this request to succeed.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EgressFrom {
    /// A list of identities that are allowed access through [EgressPolicy]. Identities can be an individual user, service account, Google group, or third-party identity. For the list of supported identity types, see https://docs.cloud.google.com/vpc-service-controls/docs/supported-identities.
    #[serde(default)]
    pub identities: ::core::option::Option<::std::vec::Vec<String>>,
    /// Specifies the type of identities that are allowed access to outside the perimeter. If left unspecified, then members of identities field will be allowed access. // TODO: enum values: ["IDENTITY_TYPE_UNSPECIFIED", "ANY_IDENTITY", "ANY_USER_ACCOUNT", "ANY_SERVICE_ACCOUNT"]
    #[serde(default, rename = "identityType")]
    pub identity_type: ::core::option::Option<String>,
    /// Whether to enforce traffic restrictions based on sources field. If the sources fields is non-empty, then this field must be set to SOURCE_RESTRICTION_ENABLED. // TODO: enum values: ["SOURCE_RESTRICTION_UNSPECIFIED", "SOURCE_RESTRICTION_ENABLED", "SOURCE_RESTRICTION_DISABLED"]
    #[serde(default, rename = "sourceRestriction")]
    pub source_restriction: ::core::option::Option<String>,
    /// Sources that this EgressPolicy authorizes access from. If this field is not empty, then source_restriction must be set to SOURCE_RESTRICTION_ENABLED.
    #[serde(default)]
    pub sources: ::core::option::Option<::std::vec::Vec<EgressSource>>,
}

/// Defines the conditions under which an EgressPolicy matches a request. Conditions are based on information about the ApiOperation intended to be performed on the resources specified. Note that if the destination of the request is also protected by a ServicePerimeter, then that ServicePerimeter must have an IngressPolicy which allows access in order for this request to succeed. The request must match operations AND resources fields in order to be allowed egress out of the perimeter.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EgressTo {
    /// A list of external resources that are allowed to be accessed. Only AWS and Azure resources are supported. For Amazon S3, the supported formats are s3://BUCKET_NAME, s3a://BUCKET_NAME, and s3n://BUCKET_NAME. For Azure Storage, the supported format is azure://myaccount.blob.core.windows.net/CONTAINER_NAME. A request matches if it contains an external resource in this list (Example: s3://bucket/path). Currently ''*'' is not allowed.
    #[serde(default, rename = "externalResources")]
    pub external_resources: ::core::option::Option<::std::vec::Vec<String>>,
    /// A list of ApiOperations allowed to be performed by the sources specified in the corresponding EgressFrom. A request matches if it uses an operation/service in this list.
    #[serde(default)]
    pub operations: ::core::option::Option<::std::vec::Vec<ApiOperation>>,
    /// A list of resources, currently only projects in the form projects/, that are allowed to be accessed by sources defined in the corresponding EgressFrom. A request matches if it contains a resource in this list. If * is specified for resources, then this EgressTo rule will authorize access to all resources outside the perimeter.
    #[serde(default)]
    pub resources: ::core::option::Option<::std::vec::Vec<String>>,
    /// IAM roles that represent the set of operations that the sources specified in the corresponding EgressFrom. are allowed to perform in this ServicePerimeter.
    #[serde(default)]
    pub roles: ::core::option::Option<::std::vec::Vec<String>>,
}

/// Defines the conditions under which an IngressPolicy matches a request. Conditions are based on information about the source of the request. The request must satisfy what is defined in sources AND identity related fields in order to match.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct IngressFrom {
    /// A list of identities that are allowed access through [IngressPolicy]. Identities can be an individual user, service account, Google group, or third-party identity. For the list of supported identity types, see https://docs.cloud.google.com/vpc-service-controls/docs/supported-identities.
    #[serde(default)]
    pub identities: ::core::option::Option<::std::vec::Vec<String>>,
    /// Specifies the type of identities that are allowed access from outside the perimeter. If left unspecified, then members of identities field will be allowed access. // TODO: enum values: ["IDENTITY_TYPE_UNSPECIFIED", "ANY_IDENTITY", "ANY_USER_ACCOUNT", "ANY_SERVICE_ACCOUNT"]
    #[serde(default, rename = "identityType")]
    pub identity_type: ::core::option::Option<String>,
    /// Sources that this IngressPolicy authorizes access from.
    #[serde(default)]
    pub sources: ::core::option::Option<::std::vec::Vec<IngressSource>>,
}

/// Defines the conditions under which an IngressPolicy matches a request. Conditions are based on information about the ApiOperation intended to be performed on the target resource of the request. The request must satisfy what is defined in operations AND resources in order to match.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct IngressTo {
    /// A list of ApiOperations allowed to be performed by the sources specified in corresponding IngressFrom in this ServicePerimeter.
    #[serde(default)]
    pub operations: ::core::option::Option<::std::vec::Vec<ApiOperation>>,
    /// A list of resources, currently only projects in the form projects/, protected by this ServicePerimeter that are allowed to be accessed by sources defined in the corresponding IngressFrom. If a single * is specified, then access to all resources inside the perimeter are allowed.
    #[serde(default)]
    pub resources: ::core::option::Option<::std::vec::Vec<String>>,
    /// IAM roles that represent the set of operations that the sources specified in the corresponding IngressFrom are allowed to perform in this ServicePerimeter.
    #[serde(default)]
    pub roles: ::core::option::Option<::std::vec::Vec<String>>,
}

/// An application that accesses Google Cloud APIs.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Application {
    /// The OAuth client ID of the application.
    #[serde(default, rename = "clientId")]
    pub client_id: ::core::option::Option<String>,
    /// The name of the application. Example: "Cloud Console"
    #[serde(default)]
    pub name: ::core::option::Option<String>,
}

/// A restriction on the OS type and version of devices making requests.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct OsConstraint {
    /// The minimum allowed OS version. If not set, any version of this OS satisfies the constraint. Format: "major.minor.patch". Examples: "10.5.301", "9.2.1".
    #[serde(default, rename = "minimumVersion")]
    pub minimum_version: ::core::option::Option<String>,
    /// Required. The allowed OS type. // TODO: enum values: ["OS_UNSPECIFIED", "DESKTOP_MAC", "DESKTOP_WINDOWS", "DESKTOP_LINUX", "DESKTOP_CHROME_OS", "ANDROID", "IOS"]
    #[serde(default, rename = "osType")]
    pub os_type: ::core::option::Option<String>,
    /// Only allows requests from devices with a verified Chrome OS. Verifications includes requirements that the device is enterprise-managed, conformant to domain policies, and the caller has permission to call the API targeted by the request.
    #[serde(default, rename = "requireVerifiedChromeOs")]
    pub require_verified_chrome_os: ::core::option::Option<bool>,
}

/// Sub-segment ranges inside of a VPC Network.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct VpcSubNetwork {
    /// Required. Network name. If the network is not part of the organization, the compute.network.get permission must be granted to the caller. Format: //compute.googleapis.com/projects/{PROJECT_ID}/global/networks/{NETWORK_NAME} Example: //compute.googleapis.com/projects/my-project/global/networks/network-1
    #[serde(default)]
    pub network: ::core::option::Option<String>,
    /// CIDR block IP subnetwork specification. The IP address must be an IPv4 address and can be a public or private IP address. Note that for a CIDR IP address block, the specified IP address portion must be properly truncated (i.e. all the host bits must be zero) or the input is considered malformed. For example, "192.0.2.0/24" is accepted but "192.0.2.1/24" is not. If empty, all IP addresses are allowed.
    #[serde(default, rename = "vpcIpSubnetworks")]
    pub vpc_ip_subnetworks: ::core::option::Option<::std::vec::Vec<String>>,
}

/// The source that EgressPolicy authorizes access from inside the ServicePerimeter to somewhere outside the ServicePerimeter boundaries.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EgressSource {
    /// An AccessLevel resource name that allows protected resources inside the ServicePerimeters to access outside the ServicePerimeter boundaries. AccessLevels listed must be in the same policy as this ServicePerimeter. Referencing a nonexistent AccessLevel will cause an error. If an AccessLevel name is not specified, only resources within the perimeter can be accessed through Google Cloud calls with request origins within the perimeter. Example: accessPolicies/MY_POLICY/accessLevels/MY_LEVEL. If a single * is specified for access_level, then all EgressSources will be allowed.
    #[serde(default, rename = "accessLevel")]
    pub access_level: ::core::option::Option<String>,
    /// A Google Cloud resource from the service perimeter that you want to allow to access data outside the perimeter. This field supports only projects. The project format is projects/{project_number}. You can''t use * in this field to allow all Google Cloud resources.
    #[serde(default)]
    pub resource: ::core::option::Option<String>,
}

/// The source that IngressPolicy authorizes access from.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct IngressSource {
    /// An AccessLevel resource name that allow resources within the ServicePerimeters to be accessed from the internet. AccessLevels listed must be in the same policy as this ServicePerimeter. Referencing a nonexistent AccessLevel will cause an error. If no AccessLevel names are listed, resources within the perimeter can only be accessed via Google Cloud calls with request origins within the perimeter. Example: accessPolicies/MY_POLICY/accessLevels/MY_LEVEL. If a single * is specified for access_level, then all IngressSources will be allowed.
    #[serde(default, rename = "accessLevel")]
    pub access_level: ::core::option::Option<String>,
    /// A Google Cloud resource that is allowed to ingress the perimeter. Requests from these resources will be allowed to access perimeter data. Currently only projects and VPCs are allowed. Project format: projects/{project_number} VPC network format: //compute.googleapis.com/projects/{PROJECT_ID}/global/networks/{NAME}. The project may be in any Google Cloud organization, not just the organization that the perimeter is defined in. * is not allowed, the case of allowing all Google Cloud resources only is not supported.
    #[serde(default)]
    pub resource: ::core::option::Option<String>,
}

/// Identification for an API Operation.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ApiOperation {
    /// API methods or permissions to allow. Method or permission must belong to the service specified by service_name field. A single MethodSelector entry with * specified for the method field will allow all methods AND permissions for the service specified in service_name.
    #[serde(default, rename = "methodSelectors")]
    pub method_selectors: ::core::option::Option<::std::vec::Vec<MethodSelector>>,
    /// The name of the API whose methods or permissions the IngressPolicy or EgressPolicy want to allow. A single ApiOperation with service_name field set to * will allow all methods AND permissions for all services.
    #[serde(default, rename = "serviceName")]
    pub service_name: ::core::option::Option<String>,
}

/// An allowed method or permission of a service specified in ApiOperation.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MethodSelector {
    /// A valid method name for the corresponding service_name in ApiOperation. If * is used as the value for the method, then ALL methods and permissions are allowed.
    #[serde(default)]
    pub method: ::core::option::Option<String>,
    /// A valid Cloud IAM permission for the corresponding service_name in ApiOperation.
    #[serde(default)]
    pub permission: ::core::option::Option<String>,
}
