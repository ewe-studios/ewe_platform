//! Auto-generated API module for gcp/clouddeploy clouddeploy.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_clouddeploy_clouddeploy `

#![cfg(feature = "gcp_clouddeploy_clouddeploy")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `TargetsPresentCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetsPresentCondition {
    /// missingTargets property.
    pub missing_targets: Option<Vec<String>>,
    /// status property.
    pub status: Option<bool>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `RepairRolloutRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RepairRolloutRule {
    /// condition property.
    pub condition: Option<AutomationRuleCondition>,
    /// id property.
    pub id: Option<String>,
    /// jobs property.
    pub jobs: Option<Vec<String>>,
    /// phases property.
    pub phases: Option<Vec<String>>,
    /// repairPhases property.
    pub repair_phases: Option<Vec<RepairPhaseConfig>>,
}

/// `CloudRunConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CloudRunConfig {
    /// automaticTrafficControl property.
    pub automatic_traffic_control: Option<bool>,
    /// canaryRevisionTags property.
    pub canary_revision_tags: Option<Vec<String>>,
    /// priorRevisionTags property.
    pub prior_revision_tags: Option<Vec<String>>,
    /// stableRevisionTags property.
    pub stable_revision_tags: Option<Vec<String>>,
}

/// `AdvanceRolloutRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvanceRolloutRule {
    /// condition property.
    pub condition: Option<AutomationRuleCondition>,
    /// id property.
    pub id: Option<String>,
    /// sourcePhases property.
    pub source_phases: Option<Vec<String>>,
    /// wait property.
    pub wait: Option<String>,
}

/// `AbandonReleaseRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AbandonReleaseRequest {}

/// `TimedPromoteReleaseCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimedPromoteReleaseCondition {
    /// nextPromotionTime property.
    pub next_promotion_time: Option<String>,
    /// targetsList property.
    pub targets_list: Option<Vec<Targets>>,
}

/// `PredeployJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PredeployJob {
    /// actions property.
    pub actions: Option<Vec<String>>,
    /// tasks property.
    pub tasks: Option<Vec<Task>>,
}

/// `RolloutRestriction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RolloutRestriction {
    /// actions property.
    pub actions: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// invokers property.
    pub invokers: Option<Vec<String>>,
    /// timeWindows property.
    pub time_windows: Option<TimeWindows>,
}

/// `AutomationRun` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutomationRun {
    /// advanceRolloutOperation property.
    pub advance_rollout_operation: Option<AdvanceRolloutOperation>,
    /// automationId property.
    pub automation_id: Option<String>,
    /// automationSnapshot property.
    pub automation_snapshot: Option<Automation>,
    /// createTime property.
    pub create_time: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// expireTime property.
    pub expire_time: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// policyViolation property.
    pub policy_violation: Option<PolicyViolation>,
    /// promoteReleaseOperation property.
    pub promote_release_operation: Option<PromoteReleaseOperation>,
    /// repairRolloutOperation property.
    pub repair_rollout_operation: Option<RepairRolloutOperation>,
    /// ruleId property.
    pub rule_id: Option<String>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// stateDescription property.
    pub state_description: Option<String>,
    /// targetId property.
    pub target_id: Option<String>,
    /// timedPromoteReleaseOperation property.
    pub timed_promote_release_operation: Option<TimedPromoteReleaseOperation>,
    /// uid property.
    pub uid: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// waitUntilTime property.
    pub wait_until_time: Option<String>,
}

/// `CustomCanaryDeployment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomCanaryDeployment {
    /// phaseConfigs property.
    pub phase_configs: Option<Vec<PhaseConfig>>,
}

/// `PhaseConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PhaseConfig {
    /// analysis property.
    pub analysis: Option<Analysis>,
    /// percentage property.
    pub percentage: Option<i64>,
    /// phaseId property.
    pub phase_id: Option<String>,
    /// postdeploy property.
    pub postdeploy: Option<Postdeploy>,
    /// predeploy property.
    pub predeploy: Option<Predeploy>,
    /// profiles property.
    pub profiles: Option<Vec<String>>,
    /// verify property.
    pub verify: Option<bool>,
    /// verifyConfig property.
    pub verify_config: Option<Verify>,
}

/// `Policy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Policy {
    /// auditConfigs property.
    pub audit_configs: Option<Vec<AuditConfig>>,
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// version property.
    pub version: Option<i64>,
}

/// `ListLocationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListLocationsResponse {
    /// locations property.
    pub locations: Option<Vec<Location>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ListOperationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListOperationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<Operation>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `SkaffoldGitSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SkaffoldGitSource {
    /// path property.
    pub path: Option<String>,
    /// ref property.
    pub r#ref: Option<String>,
    /// repo property.
    pub repo: Option<String>,
}

/// `PromoteReleaseOperation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PromoteReleaseOperation {
    /// phase property.
    pub phase: Option<String>,
    /// rollout property.
    pub rollout: Option<String>,
    /// targetId property.
    pub target_id: Option<String>,
    /// wait property.
    pub wait: Option<String>,
}

/// `DeliveryPipelineAttribute` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeliveryPipelineAttribute {
    /// id property.
    pub id: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `PolicyViolation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PolicyViolation {
    /// policyViolationDetails property.
    pub policy_violation_details: Option<Vec<PolicyViolationDetails>>,
}

/// `CustomTarget` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomTarget {
    /// customTargetType property.
    pub custom_target_type: Option<String>,
}

/// `AbandonReleaseResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AbandonReleaseResponse {}

/// `CancelRolloutResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelRolloutResponse {}

/// `Config` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Config {
    /// defaultSkaffoldVersion property.
    pub default_skaffold_version: Option<String>,
    /// defaultToolVersions property.
    pub default_tool_versions: Option<ToolVersions>,
    /// name property.
    pub name: Option<String>,
    /// supportedVersions property.
    pub supported_versions: Option<Vec<SkaffoldVersion>>,
}

/// `SerialPipeline` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SerialPipeline {
    /// stages property.
    pub stages: Option<Vec<Stage>>,
}

/// `TestIamPermissionsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestIamPermissionsRequest {
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `ListRolloutsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListRolloutsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// rollouts property.
    pub rollouts: Option<Vec<Rollout>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `TerminateJobRunRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TerminateJobRunRequest {
    /// overrideDeployPolicy property.
    pub override_deploy_policy: Option<Vec<String>>,
}

/// `ListTargetsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListTargetsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// targets property.
    pub targets: Option<Vec<Target>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `PromoteReleaseRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PromoteReleaseRule {
    /// condition property.
    pub condition: Option<AutomationRuleCondition>,
    /// destinationPhase property.
    pub destination_phase: Option<String>,
    /// destinationTargetId property.
    pub destination_target_id: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// wait property.
    pub wait: Option<String>,
}

/// `DeploymentJobs` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeploymentJobs {
    /// analysisJob property.
    pub analysis_job: Option<Job>,
    /// deployJob property.
    pub deploy_job: Option<Job>,
    /// postdeployJob property.
    pub postdeploy_job: Option<Job>,
    /// predeployJob property.
    pub predeploy_job: Option<Job>,
    /// verifyJob property.
    pub verify_job: Option<Job>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `RollbackTargetRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RollbackTargetRequest {
    /// overrideDeployPolicy property.
    pub override_deploy_policy: Option<Vec<String>>,
    /// releaseId property.
    pub release_id: Option<String>,
    /// rollbackConfig property.
    pub rollback_config: Option<RollbackTargetConfig>,
    /// rolloutId property.
    pub rollout_id: Option<String>,
    /// rolloutToRollBack property.
    pub rollout_to_roll_back: Option<String>,
    /// targetId property.
    pub target_id: Option<String>,
    /// validateOnly property.
    pub validate_only: Option<bool>,
}

/// `BuildArtifact` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuildArtifact {
    /// image property.
    pub image: Option<String>,
    /// tag property.
    pub tag: Option<String>,
}

/// `PolicyRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PolicyRule {
    /// rolloutRestriction property.
    pub rollout_restriction: Option<RolloutRestriction>,
}

/// `RetryPhase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RetryPhase {
    /// attempts property.
    pub attempts: Option<Vec<RetryAttempt>>,
    /// backoffMode property.
    pub backoff_mode: Option<String>,
    /// totalAttempts property.
    pub total_attempts: Option<String>,
}

/// `RuntimeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RuntimeConfig {
    /// cloudRun property.
    pub cloud_run: Option<CloudRunConfig>,
    /// kubernetes property.
    pub kubernetes: Option<KubernetesConfig>,
}

/// `TargetAttribute` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetAttribute {
    /// id property.
    pub id: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `CreateChildRolloutJobRun` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreateChildRolloutJobRun {
    /// rollout property.
    pub rollout: Option<String>,
    /// rolloutPhaseId property.
    pub rollout_phase_id: Option<String>,
}

/// `RepairPhase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RepairPhase {
    /// retry property.
    pub retry: Option<RetryPhase>,
    /// rollback property.
    pub rollback: Option<RollbackAttempt>,
}

/// `AutomationRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutomationRule {
    /// advanceRolloutRule property.
    pub advance_rollout_rule: Option<AdvanceRolloutRule>,
    /// promoteReleaseRule property.
    pub promote_release_rule: Option<PromoteReleaseRule>,
    /// repairRolloutRule property.
    pub repair_rollout_rule: Option<RepairRolloutRule>,
    /// timedPromoteReleaseRule property.
    pub timed_promote_release_rule: Option<TimedPromoteReleaseRule>,
}

/// `AnthosCluster` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnthosCluster {
    /// membership property.
    pub membership: Option<String>,
}

/// `ReleaseReadyCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReleaseReadyCondition {
    /// status property.
    pub status: Option<bool>,
}

/// `AdvanceChildRolloutJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvanceChildRolloutJob {}

/// `AutomationRolloutMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutomationRolloutMetadata {
    /// advanceAutomationRuns property.
    pub advance_automation_runs: Option<Vec<String>>,
    /// promoteAutomationRun property.
    pub promote_automation_run: Option<String>,
    /// repairAutomationRuns property.
    pub repair_automation_runs: Option<Vec<String>>,
}

/// `RollbackTargetConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RollbackTargetConfig {
    /// rollout property.
    pub rollout: Option<Rollout>,
    /// startingPhaseId property.
    pub starting_phase_id: Option<String>,
}

/// `GkeCluster` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GkeCluster {
    /// cluster property.
    pub cluster: Option<String>,
    /// dnsEndpoint property.
    pub dns_endpoint: Option<bool>,
    /// internalIp property.
    pub internal_ip: Option<bool>,
    /// proxyUrl property.
    pub proxy_url: Option<String>,
}

/// `IgnoreJobResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IgnoreJobResponse {}

/// `ApproveRolloutRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApproveRolloutRequest {
    /// approved property.
    pub approved: Option<bool>,
    /// overrideDeployPolicy property.
    pub override_deploy_policy: Option<Vec<String>>,
}

/// `AdvanceRolloutRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvanceRolloutRequest {
    /// overrideDeployPolicy property.
    pub override_deploy_policy: Option<Vec<String>>,
    /// phaseId property.
    pub phase_id: Option<String>,
}

/// `CloudRunMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CloudRunMetadata {
    /// job property.
    pub job: Option<String>,
    /// previousRevision property.
    pub previous_revision: Option<String>,
    /// revision property.
    pub revision: Option<String>,
    /// service property.
    pub service: Option<String>,
    /// serviceUrls property.
    pub service_urls: Option<Vec<String>>,
    /// workerPool property.
    pub worker_pool: Option<String>,
}

/// `ServiceNetworking` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceNetworking {
    /// deployment property.
    pub deployment: Option<String>,
    /// disablePodOverprovisioning property.
    pub disable_pod_overprovisioning: Option<bool>,
    /// podSelectorLabel property.
    pub pod_selector_label: Option<String>,
    /// service property.
    pub service: Option<String>,
}

/// `ChildRolloutJobs` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChildRolloutJobs {
    /// advanceRolloutJobs property.
    pub advance_rollout_jobs: Option<Vec<Job>>,
    /// createRolloutJobs property.
    pub create_rollout_jobs: Option<Vec<Job>>,
}

/// `DeliveryPipeline` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeliveryPipeline {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// condition property.
    pub condition: Option<PipelineCondition>,
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// serialPipeline property.
    pub serial_pipeline: Option<SerialPipeline>,
    /// suspended property.
    pub suspended: Option<bool>,
    /// uid property.
    pub uid: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `JobRun` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobRun {
    /// advanceChildRolloutJobRun property.
    pub advance_child_rollout_job_run: Option<AdvanceChildRolloutJobRun>,
    /// analysisJobRun property.
    pub analysis_job_run: Option<AnalysisJobRun>,
    /// createChildRolloutJobRun property.
    pub create_child_rollout_job_run: Option<CreateChildRolloutJobRun>,
    /// createTime property.
    pub create_time: Option<String>,
    /// deployJobRun property.
    pub deploy_job_run: Option<DeployJobRun>,
    /// endTime property.
    pub end_time: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// jobId property.
    pub job_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// phaseId property.
    pub phase_id: Option<String>,
    /// postdeployJobRun property.
    pub postdeploy_job_run: Option<PostdeployJobRun>,
    /// predeployJobRun property.
    pub predeploy_job_run: Option<PredeployJobRun>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// uid property.
    pub uid: Option<String>,
    /// verifyJobRun property.
    pub verify_job_run: Option<VerifyJobRun>,
}

/// `AlertPolicyCheckStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AlertPolicyCheckStatus {
    /// alertPolicies property.
    pub alert_policies: Option<Vec<String>>,
    /// failedAlertPolicies property.
    pub failed_alert_policies: Option<Vec<FailedAlertPolicy>>,
    /// failureMessage property.
    pub failure_message: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `ExecutionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecutionConfig {
    /// artifactStorage property.
    pub artifact_storage: Option<String>,
    /// defaultPool property.
    pub default_pool: Option<DefaultPool>,
    /// executionTimeout property.
    pub execution_timeout: Option<String>,
    /// privatePool property.
    pub private_pool: Option<PrivatePool>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// usages property.
    pub usages: Option<Vec<String>>,
    /// verbose property.
    pub verbose: Option<bool>,
    /// workerPool property.
    pub worker_pool: Option<String>,
}

/// `AutomationRuleCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutomationRuleCondition {
    /// targetsPresentCondition property.
    pub targets_present_condition: Option<TargetsPresentCondition>,
    /// timedPromoteReleaseCondition property.
    pub timed_promote_release_condition: Option<TimedPromoteReleaseCondition>,
}

/// `CanaryDeployment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CanaryDeployment {
    /// analysis property.
    pub analysis: Option<Analysis>,
    /// percentages property.
    pub percentages: Option<Vec<i64>>,
    /// postdeploy property.
    pub postdeploy: Option<Postdeploy>,
    /// predeploy property.
    pub predeploy: Option<Predeploy>,
    /// verify property.
    pub verify: Option<bool>,
    /// verifyConfig property.
    pub verify_config: Option<Verify>,
}

/// `CreateChildRolloutJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreateChildRolloutJob {}

/// `PolicyViolationDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PolicyViolationDetails {
    /// failureMessage property.
    pub failure_message: Option<String>,
    /// policy property.
    pub policy: Option<String>,
    /// ruleId property.
    pub rule_id: Option<String>,
}

/// `DeployPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeployPolicy {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// rules property.
    pub rules: Option<Vec<PolicyRule>>,
    /// selectors property.
    pub selectors: Option<Vec<DeployPolicyResourceSelector>>,
    /// suspended property.
    pub suspended: Option<bool>,
    /// uid property.
    pub uid: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `DeployArtifact` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeployArtifact {
    /// artifactUri property.
    pub artifact_uri: Option<String>,
    /// manifestPaths property.
    pub manifest_paths: Option<Vec<String>>,
}

/// `PipelineReadyCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PipelineReadyCondition {
    /// status property.
    pub status: Option<bool>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `Predeploy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Predeploy {
    /// actions property.
    pub actions: Option<Vec<String>>,
    /// tasks property.
    pub tasks: Option<Vec<Task>>,
}

/// `ListReleasesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListReleasesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// releases property.
    pub releases: Option<Vec<Release>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `RetryJobResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RetryJobResponse {}

/// `CustomCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomCheck {
    /// frequency property.
    pub frequency: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// task property.
    pub task: Option<Task>,
}

/// `CancelOperationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelOperationRequest {}

/// `Target` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Target {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// anthosCluster property.
    pub anthos_cluster: Option<AnthosCluster>,
    /// associatedEntities property.
    pub associated_entities: Option<serde_json::Value>,
    /// createTime property.
    pub create_time: Option<String>,
    /// customTarget property.
    pub custom_target: Option<CustomTarget>,
    /// deployParameters property.
    pub deploy_parameters: Option<serde_json::Value>,
    /// description property.
    pub description: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// executionConfigs property.
    pub execution_configs: Option<Vec<ExecutionConfig>>,
    /// gke property.
    pub gke: Option<GkeCluster>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// multiTarget property.
    pub multi_target: Option<MultiTarget>,
    /// name property.
    pub name: Option<String>,
    /// requireApproval property.
    pub require_approval: Option<bool>,
    /// run property.
    pub run: Option<CloudRunLocation>,
    /// targetId property.
    pub target_id: Option<String>,
    /// uid property.
    pub uid: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `VerifyJobRunMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VerifyJobRunMetadata {
    /// custom property.
    pub custom: Option<CustomMetadata>,
}

/// `Release` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Release {
    /// abandoned property.
    pub abandoned: Option<bool>,
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// buildArtifacts property.
    pub build_artifacts: Option<Vec<BuildArtifact>>,
    /// condition property.
    pub condition: Option<ReleaseCondition>,
    /// createTime property.
    pub create_time: Option<String>,
    /// customTargetTypeSnapshots property.
    pub custom_target_type_snapshots: Option<Vec<CustomTargetType>>,
    /// deliveryPipelineSnapshot property.
    pub delivery_pipeline_snapshot: Option<DeliveryPipeline>,
    /// deployParameters property.
    pub deploy_parameters: Option<serde_json::Value>,
    /// description property.
    pub description: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// renderEndTime property.
    pub render_end_time: Option<String>,
    /// renderStartTime property.
    pub render_start_time: Option<String>,
    /// renderState property.
    pub render_state: Option<String>,
    /// skaffoldConfigPath property.
    pub skaffold_config_path: Option<String>,
    /// skaffoldConfigUri property.
    pub skaffold_config_uri: Option<String>,
    /// skaffoldVersion property.
    pub skaffold_version: Option<String>,
    /// targetArtifacts property.
    pub target_artifacts: Option<serde_json::Value>,
    /// targetRenders property.
    pub target_renders: Option<serde_json::Value>,
    /// targetSnapshots property.
    pub target_snapshots: Option<Vec<Target>>,
    /// toolVersions property.
    pub tool_versions: Option<ToolVersions>,
    /// uid property.
    pub uid: Option<String>,
}

/// `ReleaseCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReleaseCondition {
    /// dockerVersionSupportedCondition property.
    pub docker_version_supported_condition: Option<ToolVersionSupportedCondition>,
    /// helmVersionSupportedCondition property.
    pub helm_version_supported_condition: Option<ToolVersionSupportedCondition>,
    /// kptVersionSupportedCondition property.
    pub kpt_version_supported_condition: Option<ToolVersionSupportedCondition>,
    /// kubectlVersionSupportedCondition property.
    pub kubectl_version_supported_condition: Option<ToolVersionSupportedCondition>,
    /// kustomizeVersionSupportedCondition property.
    pub kustomize_version_supported_condition: Option<ToolVersionSupportedCondition>,
    /// releaseReadyCondition property.
    pub release_ready_condition: Option<ReleaseReadyCondition>,
    /// skaffoldSupportedCondition property.
    pub skaffold_supported_condition: Option<SkaffoldSupportedCondition>,
    /// skaffoldVersionSupportedCondition property.
    pub skaffold_version_supported_condition: Option<ToolVersionSupportedCondition>,
}

/// `PostdeployJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostdeployJob {
    /// actions property.
    pub actions: Option<Vec<String>>,
    /// tasks property.
    pub tasks: Option<Vec<Task>>,
}

/// `Verify` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Verify {
    /// tasks property.
    pub tasks: Option<Vec<Task>>,
}

/// `TimedPromoteReleaseRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimedPromoteReleaseRule {
    /// condition property.
    pub condition: Option<AutomationRuleCondition>,
    /// destinationPhase property.
    pub destination_phase: Option<String>,
    /// destinationTargetId property.
    pub destination_target_id: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// schedule property.
    pub schedule: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
}

/// `RepairRolloutOperation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RepairRolloutOperation {
    /// currentRepairPhaseIndex property.
    pub current_repair_phase_index: Option<String>,
    /// jobId property.
    pub job_id: Option<String>,
    /// phaseId property.
    pub phase_id: Option<String>,
    /// repairPhases property.
    pub repair_phases: Option<Vec<RepairPhase>>,
    /// rollout property.
    pub rollout: Option<String>,
}

/// `Phase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Phase {
    /// childRolloutJobs property.
    pub child_rollout_jobs: Option<ChildRolloutJobs>,
    /// deploymentJobs property.
    pub deployment_jobs: Option<DeploymentJobs>,
    /// id property.
    pub id: Option<String>,
    /// skipMessage property.
    pub skip_message: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `CustomCheckStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomCheckStatus {
    /// failureCause property.
    pub failure_cause: Option<String>,
    /// failureMessage property.
    pub failure_message: Option<String>,
    /// frequency property.
    pub frequency: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// latestBuild property.
    pub latest_build: Option<String>,
    /// metadata property.
    pub metadata: Option<CustomMetadata>,
    /// task property.
    pub task: Option<Task>,
}

/// `RouteDestinations` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RouteDestinations {
    /// destinationIds property.
    pub destination_ids: Option<Vec<String>>,
    /// propagateService property.
    pub propagate_service: Option<bool>,
}

/// `Rollout` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Rollout {
    /// activeRepairAutomationRun property.
    pub active_repair_automation_run: Option<String>,
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// approvalState property.
    pub approval_state: Option<String>,
    /// approveTime property.
    pub approve_time: Option<String>,
    /// controllerRollout property.
    pub controller_rollout: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// deployEndTime property.
    pub deploy_end_time: Option<String>,
    /// deployFailureCause property.
    pub deploy_failure_cause: Option<String>,
    /// deployStartTime property.
    pub deploy_start_time: Option<String>,
    /// deployingBuild property.
    pub deploying_build: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// enqueueTime property.
    pub enqueue_time: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// failureReason property.
    pub failure_reason: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// metadata property.
    pub metadata: Option<Metadata>,
    /// name property.
    pub name: Option<String>,
    /// phases property.
    pub phases: Option<Vec<Phase>>,
    /// rollbackOfRollout property.
    pub rollback_of_rollout: Option<String>,
    /// rolledBackByRollouts property.
    pub rolled_back_by_rollouts: Option<Vec<String>>,
    /// state property.
    pub state: Option<String>,
    /// targetId property.
    pub target_id: Option<String>,
    /// uid property.
    pub uid: Option<String>,
}

/// `AnalysisJobRun` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnalysisJobRun {
    /// alertPolicyAnalyses property.
    pub alert_policy_analyses: Option<Vec<AlertPolicyCheckStatus>>,
    /// customCheckAnalyses property.
    pub custom_check_analyses: Option<Vec<CustomCheckStatus>>,
    /// failedCheckId property.
    pub failed_check_id: Option<String>,
}

/// `DeployJobRun` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeployJobRun {
    /// artifact property.
    pub artifact: Option<DeployArtifact>,
    /// build property.
    pub build: Option<String>,
    /// failureCause property.
    pub failure_cause: Option<String>,
    /// failureMessage property.
    pub failure_message: Option<String>,
    /// metadata property.
    pub metadata: Option<DeployJobRunMetadata>,
}

/// `TimedPromoteReleaseOperation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimedPromoteReleaseOperation {
    /// phase property.
    pub phase: Option<String>,
    /// release property.
    pub release: Option<String>,
    /// targetId property.
    pub target_id: Option<String>,
}

/// `Analysis` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Analysis {
    /// customChecks property.
    pub custom_checks: Option<Vec<CustomCheck>>,
    /// duration property.
    pub duration: Option<String>,
    /// googleCloud property.
    pub google_cloud: Option<GoogleCloudAnalysis>,
}

/// `Location` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Location {
    /// displayName property.
    pub display_name: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// locationId property.
    pub location_id: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
}

/// `AdvanceRolloutResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvanceRolloutResponse {}

/// `TargetsTypeCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetsTypeCondition {
    /// errorDetails property.
    pub error_details: Option<String>,
    /// status property.
    pub status: Option<bool>,
}

/// `CancelRolloutRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelRolloutRequest {
    /// overrideDeployPolicy property.
    pub override_deploy_policy: Option<Vec<String>>,
}

/// `CancelAutomationRunResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelAutomationRunResponse {}

/// `ListAutomationRunsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListAutomationRunsResponse {
    /// automationRuns property.
    pub automation_runs: Option<Vec<AutomationRun>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `Automation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Automation {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// rules property.
    pub rules: Option<Vec<AutomationRule>>,
    /// selector property.
    pub selector: Option<AutomationResourceSelector>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// suspended property.
    pub suspended: Option<bool>,
    /// uid property.
    pub uid: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `AdvanceRolloutOperation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvanceRolloutOperation {
    /// destinationPhase property.
    pub destination_phase: Option<String>,
    /// rollout property.
    pub rollout: Option<String>,
    /// sourcePhase property.
    pub source_phase: Option<String>,
    /// wait property.
    pub wait: Option<String>,
}

/// `RepairPhaseConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RepairPhaseConfig {
    /// retry property.
    pub retry: Option<Retry>,
    /// rollback property.
    pub rollback: Option<Rollback>,
}

/// `Rollback` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Rollback {
    /// destinationPhase property.
    pub destination_phase: Option<String>,
    /// disableRollbackIfRolloutPending property.
    pub disable_rollback_if_rollout_pending: Option<bool>,
}

/// `AuditConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuditConfig {
    /// auditLogConfigs property.
    pub audit_log_configs: Option<Vec<AuditLogConfig>>,
    /// service property.
    pub service: Option<String>,
}

/// `TestIamPermissionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestIamPermissionsResponse {
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `TimeOfDay` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeOfDay {
    /// hours property.
    pub hours: Option<i64>,
    /// minutes property.
    pub minutes: Option<i64>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<i64>,
}

/// `DeployJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeployJob {}

/// `CustomTargetTasks` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomTargetTasks {
    /// deploy property.
    pub deploy: Option<Task>,
    /// render property.
    pub render: Option<Task>,
}

/// `PrivatePool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PrivatePool {
    /// artifactStorage property.
    pub artifact_storage: Option<String>,
    /// serviceAccount property.
    pub service_account: Option<String>,
    /// workerPool property.
    pub worker_pool: Option<String>,
}

/// `ListDeployPoliciesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListDeployPoliciesResponse {
    /// deployPolicies property.
    pub deploy_policies: Option<Vec<DeployPolicy>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `Canary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Canary {
    /// canaryDeployment property.
    pub canary_deployment: Option<CanaryDeployment>,
    /// customCanaryDeployment property.
    pub custom_canary_deployment: Option<CustomCanaryDeployment>,
    /// runtimeConfig property.
    pub runtime_config: Option<RuntimeConfig>,
}

/// `Postdeploy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Postdeploy {
    /// actions property.
    pub actions: Option<Vec<String>>,
    /// tasks property.
    pub tasks: Option<Vec<Task>>,
}

/// `GatewayServiceMesh` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GatewayServiceMesh {
    /// deployment property.
    pub deployment: Option<String>,
    /// httpRoute property.
    pub http_route: Option<String>,
    /// podSelectorLabel property.
    pub pod_selector_label: Option<String>,
    /// routeDestinations property.
    pub route_destinations: Option<RouteDestinations>,
    /// routeUpdateWaitTime property.
    pub route_update_wait_time: Option<String>,
    /// service property.
    pub service: Option<String>,
    /// stableCutbackDuration property.
    pub stable_cutback_duration: Option<String>,
}

/// `MultiTarget` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MultiTarget {
    /// targetIds property.
    pub target_ids: Option<Vec<String>>,
}

/// `ToolVersionSupportedCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolVersionSupportedCondition {
    /// maintenanceModeTime property.
    pub maintenance_mode_time: Option<String>,
    /// status property.
    pub status: Option<bool>,
    /// supportExpirationTime property.
    pub support_expiration_time: Option<String>,
    /// toolVersionSupportState property.
    pub tool_version_support_state: Option<String>,
}

/// `Binding` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `OneTimeWindow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OneTimeWindow {
    /// endDate property.
    pub end_date: Option<Date>,
    /// endTime property.
    pub end_time: Option<TimeOfDay>,
    /// startDate property.
    pub start_date: Option<Date>,
    /// startTime property.
    pub start_time: Option<TimeOfDay>,
}

/// `ContainerTask` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContainerTask {
    /// args property.
    pub args: Option<Vec<String>>,
    /// command property.
    pub command: Option<Vec<String>>,
    /// env property.
    pub env: Option<serde_json::Value>,
    /// image property.
    pub image: Option<String>,
}

/// `Standard` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Standard {
    /// analysis property.
    pub analysis: Option<Analysis>,
    /// postdeploy property.
    pub postdeploy: Option<Postdeploy>,
    /// predeploy property.
    pub predeploy: Option<Predeploy>,
    /// verify property.
    pub verify: Option<bool>,
    /// verifyConfig property.
    pub verify_config: Option<Verify>,
}

/// `Date` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `ListAutomationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListAutomationsResponse {
    /// automations property.
    pub automations: Option<Vec<Automation>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `PostdeployJobRunMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostdeployJobRunMetadata {
    /// custom property.
    pub custom: Option<CustomMetadata>,
}

/// `AnalysisJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnalysisJob {
    /// customChecks property.
    pub custom_checks: Option<Vec<CustomCheck>>,
    /// duration property.
    pub duration: Option<String>,
    /// googleCloud property.
    pub google_cloud: Option<GoogleCloudAnalysis>,
}

/// `Task` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Task {
    /// container property.
    pub container: Option<ContainerTask>,
}

/// `WeeklyWindow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WeeklyWindow {
    /// daysOfWeek property.
    pub days_of_week: Option<Vec<String>>,
    /// endTime property.
    pub end_time: Option<TimeOfDay>,
    /// startTime property.
    pub start_time: Option<TimeOfDay>,
}

/// `FailedAlertPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FailedAlertPolicy {
    /// alertPolicy property.
    pub alert_policy: Option<String>,
    /// alerts property.
    pub alerts: Option<Vec<String>>,
}

/// `Targets` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Targets {
    /// destinationTargetId property.
    pub destination_target_id: Option<String>,
    /// sourceTargetId property.
    pub source_target_id: Option<String>,
}

/// `CancelAutomationRunRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelAutomationRunRequest {}

/// `VerifyJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VerifyJob {
    /// tasks property.
    pub tasks: Option<Vec<Task>>,
}

/// `Job` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Job {
    /// advanceChildRolloutJob property.
    pub advance_child_rollout_job: Option<AdvanceChildRolloutJob>,
    /// analysisJob property.
    pub analysis_job: Option<AnalysisJob>,
    /// createChildRolloutJob property.
    pub create_child_rollout_job: Option<CreateChildRolloutJob>,
    /// deployJob property.
    pub deploy_job: Option<DeployJob>,
    /// id property.
    pub id: Option<String>,
    /// jobRun property.
    pub job_run: Option<String>,
    /// postdeployJob property.
    pub postdeploy_job: Option<PostdeployJob>,
    /// predeployJob property.
    pub predeploy_job: Option<PredeployJob>,
    /// skipMessage property.
    pub skip_message: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// verifyJob property.
    pub verify_job: Option<VerifyJob>,
}

/// `Retry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Retry {
    /// attempts property.
    pub attempts: Option<String>,
    /// backoffMode property.
    pub backoff_mode: Option<String>,
    /// wait property.
    pub wait: Option<String>,
}

/// `AlertPolicyCheck` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AlertPolicyCheck {
    /// alertPolicies property.
    pub alert_policies: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
}

/// `CustomTargetType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomTargetType {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// createTime property.
    pub create_time: Option<String>,
    /// customActions property.
    pub custom_actions: Option<CustomTargetSkaffoldActions>,
    /// customTargetTypeId property.
    pub custom_target_type_id: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// tasks property.
    pub tasks: Option<CustomTargetTasks>,
    /// uid property.
    pub uid: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `ToolVersions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolVersions {
    /// docker property.
    pub docker: Option<String>,
    /// helm property.
    pub helm: Option<String>,
    /// kpt property.
    pub kpt: Option<String>,
    /// kubectl property.
    pub kubectl: Option<String>,
    /// kustomize property.
    pub kustomize: Option<String>,
    /// skaffold property.
    pub skaffold: Option<String>,
}

/// `ListDeliveryPipelinesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListDeliveryPipelinesResponse {
    /// deliveryPipelines property.
    pub delivery_pipelines: Option<Vec<DeliveryPipeline>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `RollbackTargetResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RollbackTargetResponse {
    /// rollbackConfig property.
    pub rollback_config: Option<RollbackTargetConfig>,
}

/// `AutomationResourceSelector` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutomationResourceSelector {
    /// targets property.
    pub targets: Option<Vec<TargetAttribute>>,
}

/// `ApproveRolloutResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApproveRolloutResponse {}

/// `RetryJobRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RetryJobRequest {
    /// jobId property.
    pub job_id: Option<String>,
    /// overrideDeployPolicy property.
    pub override_deploy_policy: Option<Vec<String>>,
    /// phaseId property.
    pub phase_id: Option<String>,
}

/// `DeployParameters` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeployParameters {
    /// matchTargetLabels property.
    pub match_target_labels: Option<serde_json::Value>,
    /// values property.
    pub values: Option<serde_json::Value>,
}

/// `PostdeployJobRun` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostdeployJobRun {
    /// build property.
    pub build: Option<String>,
    /// failureCause property.
    pub failure_cause: Option<String>,
    /// failureMessage property.
    pub failure_message: Option<String>,
    /// metadata property.
    pub metadata: Option<PostdeployJobRunMetadata>,
}

/// `IgnoreJobRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IgnoreJobRequest {
    /// jobId property.
    pub job_id: Option<String>,
    /// overrideDeployPolicy property.
    pub override_deploy_policy: Option<Vec<String>>,
    /// phaseId property.
    pub phase_id: Option<String>,
}

/// `RollbackAttempt` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RollbackAttempt {
    /// destinationPhase property.
    pub destination_phase: Option<String>,
    /// disableRollbackIfRolloutPending property.
    pub disable_rollback_if_rollout_pending: Option<bool>,
    /// rolloutId property.
    pub rollout_id: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// stateDesc property.
    pub state_desc: Option<String>,
}

/// `Stage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Stage {
    /// deployParameters property.
    pub deploy_parameters: Option<Vec<DeployParameters>>,
    /// profiles property.
    pub profiles: Option<Vec<String>>,
    /// strategy property.
    pub strategy: Option<Strategy>,
    /// targetId property.
    pub target_id: Option<String>,
}

/// `Strategy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Strategy {
    /// canary property.
    pub canary: Option<Canary>,
    /// standard property.
    pub standard: Option<Standard>,
}

/// `SkaffoldVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SkaffoldVersion {
    /// maintenanceModeTime property.
    pub maintenance_mode_time: Option<String>,
    /// supportEndDate property.
    pub support_end_date: Option<Date>,
    /// supportExpirationTime property.
    pub support_expiration_time: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `ListCustomTargetTypesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListCustomTargetTypesResponse {
    /// customTargetTypes property.
    pub custom_target_types: Option<Vec<CustomTargetType>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `DeployJobRunMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeployJobRunMetadata {
    /// cloudRun property.
    pub cloud_run: Option<CloudRunMetadata>,
    /// custom property.
    pub custom: Option<CustomMetadata>,
    /// customTarget property.
    pub custom_target: Option<CustomTargetDeployMetadata>,
}

/// `CustomTargetSkaffoldActions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomTargetSkaffoldActions {
    /// deployAction property.
    pub deploy_action: Option<String>,
    /// includeSkaffoldModules property.
    pub include_skaffold_modules: Option<Vec<SkaffoldModules>>,
    /// renderAction property.
    pub render_action: Option<String>,
}

/// `TimeWindows` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeWindows {
    /// oneTimeWindows property.
    pub one_time_windows: Option<Vec<OneTimeWindow>>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// weeklyWindows property.
    pub weekly_windows: Option<Vec<WeeklyWindow>>,
}

/// `GoogleCloudAnalysis` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudAnalysis {
    /// alertPolicyChecks property.
    pub alert_policy_checks: Option<Vec<AlertPolicyCheck>>,
}

/// `CloudRunLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CloudRunLocation {
    /// location property.
    pub location: Option<String>,
}

/// `Metadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metadata {
    /// automation property.
    pub automation: Option<AutomationRolloutMetadata>,
    /// cloudRun property.
    pub cloud_run: Option<CloudRunMetadata>,
    /// custom property.
    pub custom: Option<CustomMetadata>,
}

/// `PredeployJobRunMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PredeployJobRunMetadata {
    /// custom property.
    pub custom: Option<CustomMetadata>,
}

/// `Expr` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `DefaultPool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DefaultPool {
    /// artifactStorage property.
    pub artifact_storage: Option<String>,
    /// serviceAccount property.
    pub service_account: Option<String>,
}

/// `SkaffoldGCBRepoSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SkaffoldGCBRepoSource {
    /// path property.
    pub path: Option<String>,
    /// ref property.
    pub r#ref: Option<String>,
    /// repository property.
    pub repository: Option<String>,
}

/// `SkaffoldSupportedCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SkaffoldSupportedCondition {
    /// maintenanceModeTime property.
    pub maintenance_mode_time: Option<String>,
    /// skaffoldSupportState property.
    pub skaffold_support_state: Option<String>,
    /// status property.
    pub status: Option<bool>,
    /// supportExpirationTime property.
    pub support_expiration_time: Option<String>,
}

/// `PredeployJobRun` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PredeployJobRun {
    /// build property.
    pub build: Option<String>,
    /// failureCause property.
    pub failure_cause: Option<String>,
    /// failureMessage property.
    pub failure_message: Option<String>,
    /// metadata property.
    pub metadata: Option<PredeployJobRunMetadata>,
}

/// `CustomTargetDeployMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomTargetDeployMetadata {
    /// skipMessage property.
    pub skip_message: Option<String>,
}

/// `TerminateJobRunResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TerminateJobRunResponse {}

/// `DeployPolicyResourceSelector` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeployPolicyResourceSelector {
    /// deliveryPipeline property.
    pub delivery_pipeline: Option<DeliveryPipelineAttribute>,
    /// target property.
    pub target: Option<TargetAttribute>,
}

/// `PipelineCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PipelineCondition {
    /// pipelineReadyCondition property.
    pub pipeline_ready_condition: Option<PipelineReadyCondition>,
    /// targetsPresentCondition property.
    pub targets_present_condition: Option<TargetsPresentCondition>,
    /// targetsTypeCondition property.
    pub targets_type_condition: Option<TargetsTypeCondition>,
}

/// `AuditLogConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuditLogConfig {
    /// exemptedMembers property.
    pub exempted_members: Option<Vec<String>>,
    /// logType property.
    pub log_type: Option<String>,
}

/// `SkaffoldModules` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SkaffoldModules {
    /// configs property.
    pub configs: Option<Vec<String>>,
    /// git property.
    pub git: Option<SkaffoldGitSource>,
    /// googleCloudBuildRepo property.
    pub google_cloud_build_repo: Option<SkaffoldGCBRepoSource>,
    /// googleCloudStorage property.
    pub google_cloud_storage: Option<SkaffoldGCSSource>,
}

/// `AdvanceChildRolloutJobRun` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvanceChildRolloutJobRun {
    /// rollout property.
    pub rollout: Option<String>,
    /// rolloutPhaseId property.
    pub rollout_phase_id: Option<String>,
}

/// `SetIamPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetIamPolicyRequest {
    /// policy property.
    pub policy: Option<Policy>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `VerifyJobRun` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VerifyJobRun {
    /// artifactUri property.
    pub artifact_uri: Option<String>,
    /// build property.
    pub build: Option<String>,
    /// eventLogPath property.
    pub event_log_path: Option<String>,
    /// failureCause property.
    pub failure_cause: Option<String>,
    /// failureMessage property.
    pub failure_message: Option<String>,
    /// metadata property.
    pub metadata: Option<VerifyJobRunMetadata>,
}

/// `KubernetesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct KubernetesConfig {
    /// gatewayServiceMesh property.
    pub gateway_service_mesh: Option<GatewayServiceMesh>,
    /// serviceNetworking property.
    pub service_networking: Option<ServiceNetworking>,
}

/// `RetryAttempt` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RetryAttempt {
    /// attempt property.
    pub attempt: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// stateDesc property.
    pub state_desc: Option<String>,
    /// wait property.
    pub wait: Option<String>,
}

/// `CustomMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomMetadata {
    /// values property.
    pub values: Option<serde_json::Value>,
}

/// `ListJobRunsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListJobRunsResponse {
    /// jobRuns property.
    pub job_runs: Option<Vec<JobRun>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `SkaffoldGCSSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SkaffoldGCSSource {
    /// path property.
    pub path: Option<String>,
    /// source property.
    pub source: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`clouddeploy.projects.locations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.getConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsGetConfigArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `extraLocationTypes`.
    pub extra_location_types: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.customTargetTypes.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `customTargetTypeId`.
    pub custom_target_type_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: CustomTargetType,
}

/// Arguments for [`clouddeploy.projects.locations.customTargetTypes.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `etag`.
    pub etag: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.customTargetTypes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.customTargetTypes.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `options.requestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.customTargetTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.customTargetTypes.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: CustomTargetType,
}

/// Arguments for [`clouddeploy.projects.locations.customTargetTypes.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsCustomTargetTypesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `deliveryPipelineId`.
    pub delivery_pipeline_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: DeliveryPipeline,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `etag`.
    pub etag: Option<String>,
    /// Query parameter: `force`.
    pub force: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `options.requestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: DeliveryPipeline,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.rollbackTarget_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesRollbackTargetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: RollbackTargetRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.automationRuns.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: CancelAutomationRunRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.automationRuns.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.automationRuns.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.automations.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `automationId`.
    pub automation_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: Automation,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.automations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `etag`.
    pub etag: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.automations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.automations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.automations.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesAutomationsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: Automation,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.abandon_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesAbandonArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: AbandonReleaseRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `overrideDeployPolicy`.
    pub override_deploy_policy: Option<String>,
    /// Query parameter: `releaseId`.
    pub release_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: Release,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.rollouts.advance_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsAdvanceArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: AdvanceRolloutRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.rollouts.approve_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsApproveArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: ApproveRolloutRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.rollouts.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: CancelRolloutRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.rollouts.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `overrideDeployPolicy`.
    pub override_deploy_policy: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `rolloutId`.
    pub rollout_id: Option<String>,
    /// Query parameter: `startingPhaseId`.
    pub starting_phase_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: Rollout,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.rollouts.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.rollouts.ignoreJob_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsIgnoreJobArgs {
    /// Path parameter: `rollout`.
    pub rollout: String,
    /// Request body.
    pub body: IgnoreJobRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.rollouts.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.rollouts.retryJob_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsRetryJobArgs {
    /// Path parameter: `rollout`.
    pub rollout: String,
    /// Request body.
    pub body: RetryJobRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.rollouts.jobRuns.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.rollouts.jobRuns.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deliveryPipelines.releases.rollouts.jobRuns.terminate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsTerminateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: TerminateJobRunRequest,
}

/// Arguments for [`clouddeploy.projects.locations.deployPolicies.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `deployPolicyId`.
    pub deploy_policy_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: DeployPolicy,
}

/// Arguments for [`clouddeploy.projects.locations.deployPolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `etag`.
    pub etag: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deployPolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.deployPolicies.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `options.requestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deployPolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.deployPolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: DeployPolicy,
}

/// Arguments for [`clouddeploy.projects.locations.deployPolicies.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsDeployPoliciesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`clouddeploy.projects.locations.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsOperationsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// Arguments for [`clouddeploy.projects.locations.operations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.targets.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `targetId`.
    pub target_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: Target,
}

/// Arguments for [`clouddeploy.projects.locations.targets.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `etag`.
    pub etag: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.targets.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`clouddeploy.projects.locations.targets.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `options.requestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.targets.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`clouddeploy.projects.locations.targets.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: Target,
}

/// Arguments for [`clouddeploy.projects.locations.targets.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// Arguments for [`clouddeploy.projects.locations.targets.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ClouddeployProjectsLocationsTargetsTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Location =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_get_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_get_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsGetConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Config>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Config =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/locations
// -----------------------------------------------------------------------------

/// GET v1/{+name}/locations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/locations",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.extra_location_types {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("extraLocationTypes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListLocationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/customTargetTypes
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/customTargetTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_custom_target_types_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_custom_target_types_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsCustomTargetTypesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/customTargetTypes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.custom_target_type_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("customTargetTypeId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_custom_target_types_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_custom_target_types_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsCustomTargetTypesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_missing {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowMissing=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.etag {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("etag=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_custom_target_types_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_custom_target_types_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsCustomTargetTypesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomTargetType>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomTargetType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// GET v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_custom_target_types_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_custom_target_types_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsCustomTargetTypesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("options_requestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/customTargetTypes
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/customTargetTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_custom_target_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_custom_target_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsCustomTargetTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCustomTargetTypesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/customTargetTypes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCustomTargetTypesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_custom_target_types_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_custom_target_types_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsCustomTargetTypesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_missing {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowMissing=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_custom_target_types_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_custom_target_types_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsCustomTargetTypesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/deliveryPipelines
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/deliveryPipelines.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/deliveryPipelines",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.delivery_pipeline_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deliveryPipelineId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_missing {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowMissing=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.etag {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("etag=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeliveryPipeline>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DeliveryPipeline =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// GET v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("options_requestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/deliveryPipelines
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/deliveryPipelines.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDeliveryPipelinesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/deliveryPipelines",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDeliveryPipelinesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_missing {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowMissing=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:rollbackTarget
// -----------------------------------------------------------------------------

/// POST v1/{+name}:rollbackTarget.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_rollback_target_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_rollback_target_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesRollbackTargetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RollbackTargetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:rollbackTarget",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RollbackTargetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelAutomationRunResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}:cancel", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CancelAutomationRunResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutomationRun>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AutomationRun =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/automationRuns
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/automationRuns.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_automation_runs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationRunsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAutomationRunsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/automationRuns",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListAutomationRunsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/automations
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/automations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_automations_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_automations_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/automations",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.automation_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("automationId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_automations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_automations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_missing {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowMissing=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.etag {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("etag=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_automations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_automations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Automation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Automation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/automations
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/automations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_automations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_automations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAutomationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/automations",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListAutomationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_automations_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_automations_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesAutomationsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_missing {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowMissing=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:abandon
// -----------------------------------------------------------------------------

/// POST v1/{+name}:abandon.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_abandon_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_abandon_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesAbandonArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AbandonReleaseResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:abandon",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AbandonReleaseResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/releases
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/releases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/releases",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.override_deploy_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("overrideDeployPolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.release_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("releaseId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Release>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Release =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/releases
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/releases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReleasesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/releases",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListReleasesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:advance
// -----------------------------------------------------------------------------

/// POST v1/{+name}:advance.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_advance_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsAdvanceArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdvanceRolloutResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:advance",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AdvanceRolloutResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:approve
// -----------------------------------------------------------------------------

/// POST v1/{+name}:approve.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_approve_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsApproveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApproveRolloutResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:approve",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ApproveRolloutResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CancelRolloutResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}:cancel", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CancelRolloutResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/rollouts
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/rollouts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/rollouts",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.override_deploy_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("overrideDeployPolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.rollout_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("rolloutId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.starting_phase_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startingPhaseId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Rollout>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Rollout =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+rollout}:ignoreJob
// -----------------------------------------------------------------------------

/// POST v1/{+rollout}:ignoreJob.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_ignore_job_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsIgnoreJobArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IgnoreJobResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:ignoreJob",
        args.rollout,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IgnoreJobResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/rollouts
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/rollouts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRolloutsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/rollouts",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListRolloutsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+rollout}:retryJob
// -----------------------------------------------------------------------------

/// POST v1/{+rollout}:retryJob.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_retry_job_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsRetryJobArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RetryJobResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:retryJob",
        args.rollout,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RetryJobResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_get_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<JobRun>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: JobRun =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/jobRuns
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/jobRuns.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_list_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobRunsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/jobRuns",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListJobRunsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:terminate
// -----------------------------------------------------------------------------

/// POST v1/{+name}:terminate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_delivery_pipelines_releases_rollouts_job_runs_terminate_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeliveryPipelinesReleasesRolloutsJobRunsTerminateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TerminateJobRunResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:terminate",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TerminateJobRunResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/deployPolicies
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/deployPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_deploy_policies_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_deploy_policies_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeployPoliciesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/deployPolicies",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.deploy_policy_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deployPolicyId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_deploy_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_deploy_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeployPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_missing {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowMissing=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.etag {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("etag=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_deploy_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_deploy_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeployPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeployPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DeployPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// GET v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_deploy_policies_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_deploy_policies_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeployPoliciesGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("options_requestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/deployPolicies
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/deployPolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_deploy_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_deploy_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeployPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDeployPoliciesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/deployPolicies",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDeployPoliciesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_deploy_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_deploy_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeployPoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_missing {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowMissing=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_deploy_policies_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_deploy_policies_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsDeployPoliciesSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}:cancel", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_operations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_operations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsOperationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/operations
// -----------------------------------------------------------------------------

/// GET v1/{+name}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/operations",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOperationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/targets
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/targets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_targets_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_targets_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsTargetsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/targets",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.target_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("targetId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_targets_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_targets_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsTargetsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_missing {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowMissing=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.etag {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("etag=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_targets_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_targets_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsTargetsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Target>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Target =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+resource}:getIamPolicy
// -----------------------------------------------------------------------------

/// GET v1/{+resource}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_targets_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_targets_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsTargetsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:getIamPolicy",
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("options_requestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/targets
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/targets.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_targets_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_targets_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsTargetsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTargetsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}/targets",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListTargetsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_targets_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_targets_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsTargetsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://clouddeploy.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_missing {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowMissing=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_targets_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_targets_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsTargetsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:setIamPolicy",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+resource}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/{+resource}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = clouddeploy_projects_locations_targets_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn clouddeploy_projects_locations_targets_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ClouddeployProjectsLocationsTargetsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://clouddeploy.googleapis.com/v1/{}:testIamPermissions",
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
