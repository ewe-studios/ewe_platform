//! Auto-generated API module for gcp/contentwarehouse linkedSources.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_contentwarehouse_linked_sources `

#![cfg(feature = "gcp_contentwarehouse_linked_sources")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleCloudContentwarehouseV1DocumentLink` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudContentwarehouseV1DocumentLink {
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// sourceDocumentReference property.
    pub source_document_reference: Option<GoogleCloudContentwarehouseV1DocumentReference>,
    /// state property.
    pub state: Option<String>,
    /// targetDocumentReference property.
    pub target_document_reference: Option<GoogleCloudContentwarehouseV1DocumentReference>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `GoogleCloudContentwarehouseV1ListLinkedSourcesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudContentwarehouseV1ListLinkedSourcesResponse {
    /// documentLinks property.
    pub document_links: Option<Vec<GoogleCloudContentwarehouseV1DocumentLink>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudContentwarehouseV1DocumentReference` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudContentwarehouseV1DocumentReference {
    /// createTime property.
    pub create_time: Option<String>,
    /// deleteTime property.
    pub delete_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// documentIsFolder property.
    pub document_is_folder: Option<bool>,
    /// documentIsLegalHoldFolder property.
    pub document_is_legal_hold_folder: Option<bool>,
    /// documentIsRetentionFolder property.
    pub document_is_retention_folder: Option<bool>,
    /// documentName property.
    pub document_name: Option<String>,
    /// snippet property.
    pub snippet: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`contentwarehouse.projects.locations.documents.linkedSources_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContentwarehouseProjectsLocationsDocumentsLinkedSourcesArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/linkedSources
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/documents/{documentsId}/linkedSources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = contentwarehouse_projects_locations_documents_linked_sources_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn contentwarehouse_projects_locations_documents_linked_sources_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudContentwarehouseV1ListLinkedSourcesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://contentwarehouse.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/documents/{{documentsId}}/linkedSources",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudContentwarehouseV1ListLinkedSourcesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
