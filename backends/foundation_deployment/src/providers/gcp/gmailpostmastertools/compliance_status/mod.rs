//! Auto-generated API module for gcp/gmailpostmastertools complianceStatus.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_gmailpostmastertools_compliance_status `

#![cfg(feature = "gcp_gmailpostmastertools_compliance_status")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ComplianceStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComplianceStatus {
    /// status property.
    pub status: Option<String>,
}

/// `OneClickUnsubscribeVerdict` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OneClickUnsubscribeVerdict {
    /// reason property.
    pub reason: Option<String>,
    /// status property.
    pub status: Option<ComplianceStatus>,
}

/// `ComplianceRowData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComplianceRowData {
    /// requirement property.
    pub requirement: Option<String>,
    /// status property.
    pub status: Option<ComplianceStatus>,
}

/// `HonorUnsubscribeVerdict` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HonorUnsubscribeVerdict {
    /// reason property.
    pub reason: Option<String>,
    /// status property.
    pub status: Option<ComplianceStatus>,
}

/// `DomainComplianceData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DomainComplianceData {
    /// domainId property.
    pub domain_id: Option<String>,
    /// honorUnsubscribeVerdict property.
    pub honor_unsubscribe_verdict: Option<HonorUnsubscribeVerdict>,
    /// oneClickUnsubscribeVerdict property.
    pub one_click_unsubscribe_verdict: Option<OneClickUnsubscribeVerdict>,
    /// rowData property.
    pub row_data: Option<Vec<ComplianceRowData>>,
}

/// `DomainComplianceStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DomainComplianceStatus {
    /// complianceData property.
    pub compliance_data: Option<DomainComplianceData>,
    /// name property.
    pub name: Option<String>,
    /// subdomainComplianceData property.
    pub subdomain_compliance_data: Option<DomainComplianceData>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`gmailpostmastertools.domains.getComplianceStatus_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GmailpostmastertoolsDomainsGetComplianceStatusArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v2/domains/{domainsId}/complianceStatus
// -----------------------------------------------------------------------------

/// GET v2/domains/{domainsId}/complianceStatus.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = gmailpostmastertools_domains_get_compliance_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn gmailpostmastertools_domains_get_compliance_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DomainComplianceStatus>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://gmailpostmastertools.googleapis.com/v2/domains/{{domainsId}}/complianceStatus",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DomainComplianceStatus =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
