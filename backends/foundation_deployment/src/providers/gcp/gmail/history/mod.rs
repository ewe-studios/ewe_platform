//! Auto-generated API module for gcp/gmail history.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_gmail_history `

#![cfg(feature = "gcp_gmail_history")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ClassificationLabelValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClassificationLabelValue {
    /// fields property.
    pub fields: Option<Vec<ClassificationLabelFieldValue>>,
    /// labelId property.
    pub label_id: Option<String>,
}

/// `HistoryMessageAdded` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HistoryMessageAdded {
    /// message property.
    pub message: Option<Message>,
}

/// `MessagePart` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MessagePart {
    /// body property.
    pub body: Option<MessagePartBody>,
    /// filename property.
    pub filename: Option<String>,
    /// headers property.
    pub headers: Option<Vec<MessagePartHeader>>,
    /// mimeType property.
    pub mime_type: Option<String>,
    /// partId property.
    pub part_id: Option<String>,
    /// parts property.
    pub parts: Option<Vec<MessagePart>>,
}

/// `HistoryLabelRemoved` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HistoryLabelRemoved {
    /// labelIds property.
    pub label_ids: Option<Vec<String>>,
    /// message property.
    pub message: Option<Message>,
}

/// `ClassificationLabelFieldValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClassificationLabelFieldValue {
    /// fieldId property.
    pub field_id: Option<String>,
    /// selection property.
    pub selection: Option<String>,
}

/// `MessagePartHeader` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MessagePartHeader {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `MessagePartBody` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MessagePartBody {
    /// attachmentId property.
    pub attachment_id: Option<String>,
    /// data property.
    pub data: Option<String>,
    /// size property.
    pub size: Option<i64>,
}

/// `HistoryLabelAdded` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HistoryLabelAdded {
    /// labelIds property.
    pub label_ids: Option<Vec<String>>,
    /// message property.
    pub message: Option<Message>,
}

/// `History` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct History {
    /// id property.
    pub id: Option<String>,
    /// labelsAdded property.
    pub labels_added: Option<Vec<HistoryLabelAdded>>,
    /// labelsRemoved property.
    pub labels_removed: Option<Vec<HistoryLabelRemoved>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// messagesAdded property.
    pub messages_added: Option<Vec<HistoryMessageAdded>>,
    /// messagesDeleted property.
    pub messages_deleted: Option<Vec<HistoryMessageDeleted>>,
}

/// `HistoryMessageDeleted` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HistoryMessageDeleted {
    /// message property.
    pub message: Option<Message>,
}

/// `ListHistoryResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListHistoryResponse {
    /// history property.
    pub history: Option<Vec<History>>,
    /// historyId property.
    pub history_id: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`gmail.users.history.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GmailUsersHistoryListArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Query parameter: `historyTypes`.
    pub history_types: Option<String>,
    /// Query parameter: `labelId`.
    pub label_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `startHistoryId`.
    pub start_history_id: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET gmail/v1/users/{userId}/history
// -----------------------------------------------------------------------------

/// GET gmail/v1/users/{userId}/history.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = gmail_users_history_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn gmail_users_history_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GmailUsersHistoryListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListHistoryResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://gmail.googleapis.com/gmail/v1/users/{}/history",
        args.user_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListHistoryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
