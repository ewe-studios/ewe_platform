//! Auto-generated API module for gcp/manufacturers manufacturers.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_manufacturers_manufacturers `

#![cfg(feature = "gcp_manufacturers_manufacturers")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Grocery` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Grocery {
    /// activeIngredients property.
    pub active_ingredients: Option<String>,
    /// alcoholByVolume property.
    pub alcohol_by_volume: Option<f64>,
    /// allergens property.
    pub allergens: Option<String>,
    /// derivedNutritionClaim property.
    pub derived_nutrition_claim: Option<Vec<String>>,
    /// directions property.
    pub directions: Option<String>,
    /// indications property.
    pub indications: Option<String>,
    /// ingredients property.
    pub ingredients: Option<String>,
    /// nutritionClaim property.
    pub nutrition_claim: Option<Vec<String>>,
    /// storageInstructions property.
    pub storage_instructions: Option<String>,
}

/// `ProductDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductDetail {
    /// attributeName property.
    pub attribute_name: Option<String>,
    /// attributeValue property.
    pub attribute_value: Option<String>,
    /// sectionName property.
    pub section_name: Option<String>,
}

/// `Nutrition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Nutrition {
    /// addedSugars property.
    pub added_sugars: Option<FloatUnit>,
    /// addedSugarsDailyPercentage property.
    pub added_sugars_daily_percentage: Option<f64>,
    /// calcium property.
    pub calcium: Option<FloatUnit>,
    /// calciumDailyPercentage property.
    pub calcium_daily_percentage: Option<f64>,
    /// cholesterol property.
    pub cholesterol: Option<FloatUnit>,
    /// cholesterolDailyPercentage property.
    pub cholesterol_daily_percentage: Option<f64>,
    /// dietaryFiber property.
    pub dietary_fiber: Option<FloatUnit>,
    /// dietaryFiberDailyPercentage property.
    pub dietary_fiber_daily_percentage: Option<f64>,
    /// energy property.
    pub energy: Option<FloatUnit>,
    /// energyFromFat property.
    pub energy_from_fat: Option<FloatUnit>,
    /// folateDailyPercentage property.
    pub folate_daily_percentage: Option<f64>,
    /// folateFolicAcid property.
    pub folate_folic_acid: Option<FloatUnit>,
    /// folateMcgDfe property.
    pub folate_mcg_dfe: Option<f64>,
    /// iron property.
    pub iron: Option<FloatUnit>,
    /// ironDailyPercentage property.
    pub iron_daily_percentage: Option<f64>,
    /// monounsaturatedFat property.
    pub monounsaturated_fat: Option<FloatUnit>,
    /// nutritionFactMeasure property.
    pub nutrition_fact_measure: Option<String>,
    /// polyols property.
    pub polyols: Option<FloatUnit>,
    /// polyunsaturatedFat property.
    pub polyunsaturated_fat: Option<FloatUnit>,
    /// potassium property.
    pub potassium: Option<FloatUnit>,
    /// potassiumDailyPercentage property.
    pub potassium_daily_percentage: Option<f64>,
    /// preparedSizeDescription property.
    pub prepared_size_description: Option<String>,
    /// protein property.
    pub protein: Option<FloatUnit>,
    /// proteinDailyPercentage property.
    pub protein_daily_percentage: Option<f64>,
    /// saturatedFat property.
    pub saturated_fat: Option<FloatUnit>,
    /// saturatedFatDailyPercentage property.
    pub saturated_fat_daily_percentage: Option<f64>,
    /// servingSizeDescription property.
    pub serving_size_description: Option<String>,
    /// servingSizeMeasure property.
    pub serving_size_measure: Option<FloatUnit>,
    /// servingsPerContainer property.
    pub servings_per_container: Option<String>,
    /// sodium property.
    pub sodium: Option<FloatUnit>,
    /// sodiumDailyPercentage property.
    pub sodium_daily_percentage: Option<f64>,
    /// starch property.
    pub starch: Option<FloatUnit>,
    /// totalCarbohydrate property.
    pub total_carbohydrate: Option<FloatUnit>,
    /// totalCarbohydrateDailyPercentage property.
    pub total_carbohydrate_daily_percentage: Option<f64>,
    /// totalFat property.
    pub total_fat: Option<FloatUnit>,
    /// totalFatDailyPercentage property.
    pub total_fat_daily_percentage: Option<f64>,
    /// totalSugars property.
    pub total_sugars: Option<FloatUnit>,
    /// totalSugarsDailyPercentage property.
    pub total_sugars_daily_percentage: Option<f64>,
    /// transFat property.
    pub trans_fat: Option<FloatUnit>,
    /// transFatDailyPercentage property.
    pub trans_fat_daily_percentage: Option<f64>,
    /// vitaminD property.
    pub vitamin_d: Option<FloatUnit>,
    /// vitaminDDailyPercentage property.
    pub vitamin_d_daily_percentage: Option<f64>,
    /// voluntaryNutritionFact property.
    pub voluntary_nutrition_fact: Option<Vec<VoluntaryNutritionFact>>,
}

/// `VoluntaryNutritionFact` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VoluntaryNutritionFact {
    /// dailyPercentage property.
    pub daily_percentage: Option<f64>,
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<FloatUnit>,
}

/// `FeatureDescription` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FeatureDescription {
    /// headline property.
    pub headline: Option<String>,
    /// image property.
    pub image: Option<Image>,
    /// text property.
    pub text: Option<String>,
}

/// `Issue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Issue {
    /// applicableCountries property.
    pub applicable_countries: Option<Vec<String>>,
    /// attribute property.
    pub attribute: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// destination property.
    pub destination: Option<String>,
    /// resolution property.
    pub resolution: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ListProductCertificationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListProductCertificationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// productCertifications property.
    pub product_certifications: Option<Vec<ProductCertification>>,
}

/// `Price` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Price {
    /// amount property.
    pub amount: Option<String>,
    /// currency property.
    pub currency: Option<String>,
}

/// `Attributes` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Attributes {
    /// additionalImageLink property.
    pub additional_image_link: Option<Vec<Image>>,
    /// ageGroup property.
    pub age_group: Option<String>,
    /// brand property.
    pub brand: Option<String>,
    /// capacity property.
    pub capacity: Option<Capacity>,
    /// certification property.
    pub certification: Option<Vec<GoogleShoppingManufacturersV1ProductCertification>>,
    /// color property.
    pub color: Option<String>,
    /// count property.
    pub count: Option<Count>,
    /// description property.
    pub description: Option<String>,
    /// disclosureDate property.
    pub disclosure_date: Option<String>,
    /// excludedDestination property.
    pub excluded_destination: Option<Vec<String>>,
    /// featureDescription property.
    pub feature_description: Option<Vec<FeatureDescription>>,
    /// flavor property.
    pub flavor: Option<String>,
    /// format property.
    pub format: Option<String>,
    /// gender property.
    pub gender: Option<String>,
    /// grocery property.
    pub grocery: Option<Grocery>,
    /// gtin property.
    pub gtin: Option<Vec<String>>,
    /// imageLink property.
    pub image_link: Option<Image>,
    /// includedDestination property.
    pub included_destination: Option<Vec<String>>,
    /// intendedCountry property.
    pub intended_country: Option<Vec<String>>,
    /// itemGroupId property.
    pub item_group_id: Option<String>,
    /// material property.
    pub material: Option<String>,
    /// mpn property.
    pub mpn: Option<String>,
    /// nutrition property.
    pub nutrition: Option<Nutrition>,
    /// pattern property.
    pub pattern: Option<String>,
    /// productDetail property.
    pub product_detail: Option<Vec<ProductDetail>>,
    /// productHighlight property.
    pub product_highlight: Option<Vec<String>>,
    /// productLine property.
    pub product_line: Option<String>,
    /// productName property.
    pub product_name: Option<String>,
    /// productPageUrl property.
    pub product_page_url: Option<String>,
    /// productType property.
    pub product_type: Option<Vec<String>>,
    /// releaseDate property.
    pub release_date: Option<String>,
    /// richProductContent property.
    pub rich_product_content: Option<Vec<String>>,
    /// scent property.
    pub scent: Option<String>,
    /// size property.
    pub size: Option<String>,
    /// sizeSystem property.
    pub size_system: Option<String>,
    /// sizeType property.
    pub size_type: Option<Vec<String>>,
    /// suggestedRetailPrice property.
    pub suggested_retail_price: Option<Price>,
    /// targetClientId property.
    pub target_client_id: Option<String>,
    /// theme property.
    pub theme: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// videoLink property.
    pub video_link: Option<Vec<String>>,
    /// virtualModelLink property.
    pub virtual_model_link: Option<String>,
}

/// `Certification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Certification {
    /// authority property.
    pub authority: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// link property.
    pub link: Option<String>,
    /// logo property.
    pub logo: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// validUntil property.
    pub valid_until: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `Product` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Product {
    /// attributes property.
    pub attributes: Option<Attributes>,
    /// contentLanguage property.
    pub content_language: Option<String>,
    /// destinationStatuses property.
    pub destination_statuses: Option<Vec<DestinationStatus>>,
    /// feedLabel property.
    pub feed_label: Option<String>,
    /// issues property.
    pub issues: Option<Vec<Issue>>,
    /// name property.
    pub name: Option<String>,
    /// parent property.
    pub parent: Option<String>,
    /// productId property.
    pub product_id: Option<String>,
    /// targetCountry property.
    pub target_country: Option<String>,
}

/// `Image` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Image {
    /// imageUrl property.
    pub image_url: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Capacity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Capacity {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `GoogleShoppingManufacturersV1ProductCertification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleShoppingManufacturersV1ProductCertification {
    /// authority property.
    pub authority: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// link property.
    pub link: Option<String>,
    /// logo property.
    pub logo: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// validUntil property.
    pub valid_until: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `ProductCertification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProductCertification {
    /// brand property.
    pub brand: Option<String>,
    /// certification property.
    pub certification: Option<Vec<Certification>>,
    /// countryCode property.
    pub country_code: Option<Vec<String>>,
    /// destinationStatuses property.
    pub destination_statuses: Option<Vec<DestinationStatus>>,
    /// issues property.
    pub issues: Option<Vec<Issue>>,
    /// mpn property.
    pub mpn: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// productCode property.
    pub product_code: Option<Vec<String>>,
    /// productType property.
    pub product_type: Option<Vec<String>>,
    /// title property.
    pub title: Option<String>,
}

/// `DestinationStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DestinationStatus {
    /// approvedCountries property.
    pub approved_countries: Option<Vec<String>>,
    /// destination property.
    pub destination: Option<String>,
    /// disapprovedCountries property.
    pub disapproved_countries: Option<Vec<String>>,
    /// pendingCountries property.
    pub pending_countries: Option<Vec<String>>,
    /// status property.
    pub status: Option<String>,
}

/// `FloatUnit` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FloatUnit {
    /// amount property.
    pub amount: Option<f64>,
    /// unit property.
    pub unit: Option<String>,
}

/// `Count` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Count {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `ListProductsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListProductsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// products property.
    pub products: Option<Vec<Product>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`manufacturers.accounts.languages.productCertifications.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ManufacturersAccountsLanguagesProductCertificationsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`manufacturers.accounts.languages.productCertifications.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ManufacturersAccountsLanguagesProductCertificationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`manufacturers.accounts.languages.productCertifications.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ManufacturersAccountsLanguagesProductCertificationsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`manufacturers.accounts.languages.productCertifications.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ManufacturersAccountsLanguagesProductCertificationsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: ProductCertification,
}

/// Arguments for [`manufacturers.accounts.products.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ManufacturersAccountsProductsDeleteArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`manufacturers.accounts.products.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ManufacturersAccountsProductsGetArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `include`.
    pub include: Option<String>,
}

/// Arguments for [`manufacturers.accounts.products.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ManufacturersAccountsProductsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `include`.
    pub include: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`manufacturers.accounts.products.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ManufacturersAccountsProductsUpdateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: Attributes,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = manufacturers_accounts_languages_product_certifications_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn manufacturers_accounts_languages_product_certifications_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ManufacturersAccountsLanguagesProductCertificationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://manufacturers.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = manufacturers_accounts_languages_product_certifications_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn manufacturers_accounts_languages_product_certifications_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ManufacturersAccountsLanguagesProductCertificationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductCertification>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://manufacturers.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProductCertification =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/productCertifications
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/productCertifications.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = manufacturers_accounts_languages_product_certifications_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn manufacturers_accounts_languages_product_certifications_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ManufacturersAccountsLanguagesProductCertificationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProductCertificationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://manufacturers.googleapis.com/v1/{}/productCertifications",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListProductCertificationsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = manufacturers_accounts_languages_product_certifications_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn manufacturers_accounts_languages_product_certifications_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ManufacturersAccountsLanguagesProductCertificationsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductCertification>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://manufacturers.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProductCertification =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+parent}/products/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+parent}/products/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = manufacturers_accounts_products_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn manufacturers_accounts_products_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ManufacturersAccountsProductsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://manufacturers.googleapis.com/v1/{}/products/{}",
        args.parent, args.name,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/products/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/products/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = manufacturers_accounts_products_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn manufacturers_accounts_products_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ManufacturersAccountsProductsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Product>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://manufacturers.googleapis.com/v1/{}/products/{}",
        args.parent, args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.include {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("include=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Product =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/products
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/products.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = manufacturers_accounts_products_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn manufacturers_accounts_products_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ManufacturersAccountsProductsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProductsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://manufacturers.googleapis.com/v1/{}/products",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.include {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("include=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListProductsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/{+parent}/products/{+name}
// -----------------------------------------------------------------------------

/// PUT v1/{+parent}/products/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = manufacturers_accounts_products_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn manufacturers_accounts_products_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ManufacturersAccountsProductsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://manufacturers.googleapis.com/v1/{}/products/{}",
        args.parent, args.name,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
