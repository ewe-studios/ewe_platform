//! Auto-generated API module for gcp/threatintelligence configurations.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_threatintelligence_configurations `

#![cfg(feature = "gcp_threatintelligence_configurations")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `CustomerProfileCompany` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfileCompany {
    /// citationIds property.
    pub citation_ids: Option<Vec<String>>,
    /// company property.
    pub company: Option<String>,
}

/// `CustomerProfileContactInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfileContactInfo {
    /// address property.
    pub address: Option<String>,
    /// citationIds property.
    pub citation_ids: Option<Vec<String>>,
    /// email property.
    pub email: Option<String>,
    /// label property.
    pub label: Option<String>,
    /// other property.
    pub other: Option<String>,
    /// phone property.
    pub phone: Option<String>,
}

/// `CustomerProfileLocation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfileLocation {
    /// address property.
    pub address: Option<String>,
    /// brand property.
    pub brand: Option<String>,
    /// citationIds property.
    pub citation_ids: Option<Vec<String>>,
    /// facilityType property.
    pub facility_type: Option<String>,
}

/// `CustomerProfileProduct` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfileProduct {
    /// brand property.
    pub brand: Option<String>,
    /// citationIds property.
    pub citation_ids: Option<Vec<String>>,
    /// product property.
    pub product: Option<String>,
}

/// `CustomerProfileCitedString` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfileCitedString {
    /// citationIds property.
    pub citation_ids: Option<Vec<String>>,
    /// value property.
    pub value: Option<String>,
}

/// `CustomerProfileCitation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfileCitation {
    /// citationId property.
    pub citation_id: Option<String>,
    /// document property.
    pub document: Option<String>,
    /// retrievalTime property.
    pub retrieval_time: Option<String>,
    /// source property.
    pub source: Option<String>,
    /// uri property.
    pub uri: Option<String>,
}

/// `CustomerProfilePerson` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfilePerson {
    /// citationIds property.
    pub citation_ids: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `CustomerProfileIndustry` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfileIndustry {
    /// citationIds property.
    pub citation_ids: Option<Vec<String>>,
    /// industry property.
    pub industry: Option<String>,
}

/// `CustomerProfileSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfileSummary {
    /// areaServed property.
    pub area_served: Option<CustomerProfileCitedString>,
    /// brands property.
    pub brands: Option<CustomerProfileCitedString>,
    /// entityType property.
    pub entity_type: Option<CustomerProfileCitedString>,
    /// founded property.
    pub founded: Option<CustomerProfileCitedString>,
    /// headquarters property.
    pub headquarters: Option<CustomerProfileCitedString>,
    /// industry property.
    pub industry: Option<CustomerProfileCitedString>,
    /// keyPeopleSummary property.
    pub key_people_summary: Option<CustomerProfileCitedString>,
    /// parentCompany property.
    pub parent_company: Option<CustomerProfileCitedString>,
    /// primaryWebsite property.
    pub primary_website: Option<CustomerProfileCitedString>,
    /// productsSummary property.
    pub products_summary: Option<CustomerProfileCitedString>,
    /// servicesSummary property.
    pub services_summary: Option<CustomerProfileCitedString>,
    /// title property.
    pub title: Option<CustomerProfileCitedString>,
}

/// `Configuration` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Configuration {
    /// audit property.
    pub audit: Option<Audit>,
    /// description property.
    pub description: Option<String>,
    /// detail property.
    pub detail: Option<ConfigurationDetail>,
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// provider property.
    pub provider: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `CustomerProfileConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfileConfig {
    /// citations property.
    pub citations: Option<Vec<CustomerProfileCitation>>,
    /// contactInfo property.
    pub contact_info: Option<Vec<CustomerProfileContactInfo>>,
    /// executives property.
    pub executives: Option<Vec<CustomerProfilePerson>>,
    /// industries property.
    pub industries: Option<Vec<CustomerProfileIndustry>>,
    /// locations property.
    pub locations: Option<Vec<CustomerProfileLocation>>,
    /// org property.
    pub org: Option<String>,
    /// orgSummary property.
    pub org_summary: Option<String>,
    /// parentCompanies property.
    pub parent_companies: Option<Vec<CustomerProfileCompany>>,
    /// products property.
    pub products: Option<Vec<CustomerProfileProduct>>,
    /// securityConsiderations property.
    pub security_considerations: Option<CustomerProfileSecurityConsiderations>,
    /// summary property.
    pub summary: Option<CustomerProfileSummary>,
    /// technologyPresence property.
    pub technology_presence: Option<String>,
    /// webPresences property.
    pub web_presences: Option<Vec<CustomerProfileWebPresence>>,
}

/// `Audit` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Audit {
    /// createTime property.
    pub create_time: Option<String>,
    /// creator property.
    pub creator: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// updater property.
    pub updater: Option<String>,
}

/// `ConfigurationDetail` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConfigurationDetail {
    /// customerProfile property.
    pub customer_profile: Option<CustomerProfileConfig>,
    /// detailType property.
    pub detail_type: Option<String>,
}

/// `ListConfigurationsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListConfigurationsResponse {
    /// configurations property.
    pub configurations: Option<Vec<Configuration>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `CustomerProfileWebPresence` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfileWebPresence {
    /// citationIds property.
    pub citation_ids: Option<Vec<String>>,
    /// domain property.
    pub domain: Option<String>,
}

/// `CustomerProfileSecurityConsiderations` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomerProfileSecurityConsiderations {
    /// considerations property.
    pub considerations: Option<Vec<String>>,
    /// note property.
    pub note: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`threatintelligence.projects.configurations.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ThreatintelligenceProjectsConfigurationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`threatintelligence.projects.configurations.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ThreatintelligenceProjectsConfigurationsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1beta/projects/{projectsId}/configurations/{configurationsId}
// -----------------------------------------------------------------------------

/// GET v1beta/projects/{projectsId}/configurations/{configurationsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = threatintelligence_projects_configurations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn threatintelligence_projects_configurations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Configuration>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{{projectsId}}/configurations/{{configurationsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Configuration =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1beta/projects/{projectsId}/configurations
// -----------------------------------------------------------------------------

/// GET v1beta/projects/{projectsId}/configurations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = threatintelligence_projects_configurations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn threatintelligence_projects_configurations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConfigurationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{{projectsId}}/configurations",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListConfigurationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
