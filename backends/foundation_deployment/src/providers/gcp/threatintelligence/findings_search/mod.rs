//! Auto-generated API module for gcp/threatintelligence findings:search.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_threatintelligence_findings_search `

#![cfg(feature = "gcp_threatintelligence_findings_search")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `InsiderThreatFindingDetail` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InsiderThreatFindingDetail {
    /// documentId property.
    pub document_id: Option<String>,
    /// matchScore property.
    pub match_score: Option<f64>,
    /// severity property.
    pub severity: Option<String>,
}

/// `SeverityAnalysis` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SeverityAnalysis {
    /// confidence property.
    pub confidence: Option<String>,
    /// reasoning property.
    pub reasoning: Option<String>,
    /// severityLevel property.
    pub severity_level: Option<String>,
}

/// `RelevanceAnalysis` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RelevanceAnalysis {
    /// confidence property.
    pub confidence: Option<String>,
    /// evidence property.
    pub evidence: Option<Evidence>,
    /// reasoning property.
    pub reasoning: Option<String>,
    /// relevanceLevel property.
    pub relevance_level: Option<String>,
    /// relevant property.
    pub relevant: Option<bool>,
}

/// `DataLeakFindingDetail` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DataLeakFindingDetail {
    /// documentId property.
    pub document_id: Option<String>,
    /// matchScore property.
    pub match_score: Option<f64>,
    /// severity property.
    pub severity: Option<String>,
}

/// `Finding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Finding {
    /// aiSummary property.
    pub ai_summary: Option<String>,
    /// alert property.
    pub alert: Option<String>,
    /// audit property.
    pub audit: Option<Audit>,
    /// configurations property.
    pub configurations: Option<Vec<String>>,
    /// detail property.
    pub detail: Option<FindingDetail>,
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// provider property.
    pub provider: Option<String>,
    /// relevanceAnalysis property.
    pub relevance_analysis: Option<RelevanceAnalysis>,
    /// reoccurrenceTimes property.
    pub reoccurrence_times: Option<Vec<String>>,
    /// severity property.
    pub severity: Option<f64>,
    /// severityAnalysis property.
    pub severity_analysis: Option<SeverityAnalysis>,
}

/// `Evidence` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Evidence {
    /// commonThemes property.
    pub common_themes: Option<Vec<String>>,
    /// distinctThemes property.
    pub distinct_themes: Option<Vec<String>>,
}

/// `SearchFindingsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchFindingsResponse {
    /// findings property.
    pub findings: Option<Vec<Finding>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Audit` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Audit {
    /// createTime property.
    pub create_time: Option<String>,
    /// creator property.
    pub creator: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// updater property.
    pub updater: Option<String>,
}

/// `FindingDetail` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FindingDetail {
    /// dataLeak property.
    pub data_leak: Option<DataLeakFindingDetail>,
    /// detailType property.
    pub detail_type: Option<String>,
    /// initialAccessBroker property.
    pub initial_access_broker: Option<InitialAccessBrokerFindingDetail>,
    /// insiderThreat property.
    pub insider_threat: Option<InsiderThreatFindingDetail>,
}

/// `InitialAccessBrokerFindingDetail` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InitialAccessBrokerFindingDetail {
    /// documentId property.
    pub document_id: Option<String>,
    /// matchScore property.
    pub match_score: Option<f64>,
    /// severity property.
    pub severity: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`threatintelligence.projects.findings.search_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ThreatintelligenceProjectsFindingsSearchArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `query`.
    pub query: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1beta/projects/{projectsId}/findings:search
// -----------------------------------------------------------------------------

/// GET v1beta/projects/{projectsId}/findings:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = threatintelligence_projects_findings_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn threatintelligence_projects_findings_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchFindingsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://threatintelligence.googleapis.com/v1beta/projects/{{projectsId}}/findings:search",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchFindingsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
