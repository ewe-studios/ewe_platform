//! Auto-generated API module for gcp/css cssProductInputs.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_css_css_product_inputs `

#![cfg(feature = "gcp_css_css_product_inputs")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::CssProductInput;
use super::shared::Empty;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Price` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Price {
    /// amountMicros property.
    pub amount_micros: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
}

/// `ProductDimension` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductDimension {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `ProductWeight` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductWeight {
    /// unit property.
    pub unit: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `Certification` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Certification {
    /// authority property.
    pub authority: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `HeadlineOfferInstallment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HeadlineOfferInstallment {
    /// amount property.
    pub amount: Option<Price>,
    /// downpayment property.
    pub downpayment: Option<Price>,
    /// months property.
    pub months: Option<String>,
}

/// `ProductDetail` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductDetail {
    /// attributeName property.
    pub attribute_name: Option<String>,
    /// attributeValue property.
    pub attribute_value: Option<String>,
    /// sectionName property.
    pub section_name: Option<String>,
}

/// `Attributes` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Attributes {
    /// additionalImageLinks property.
    pub additional_image_links: Option<Vec<String>>,
    /// adult property.
    pub adult: Option<bool>,
    /// ageGroup property.
    pub age_group: Option<String>,
    /// brand property.
    pub brand: Option<String>,
    /// certifications property.
    pub certifications: Option<Vec<Certification>>,
    /// color property.
    pub color: Option<String>,
    /// cppAdsRedirect property.
    pub cpp_ads_redirect: Option<String>,
    /// cppLink property.
    pub cpp_link: Option<String>,
    /// cppMobileLink property.
    pub cpp_mobile_link: Option<String>,
    /// customLabel0 property.
    pub custom_label0: Option<String>,
    /// customLabel1 property.
    pub custom_label1: Option<String>,
    /// customLabel2 property.
    pub custom_label2: Option<String>,
    /// customLabel3 property.
    pub custom_label3: Option<String>,
    /// customLabel4 property.
    pub custom_label4: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// excludedDestinations property.
    pub excluded_destinations: Option<Vec<String>>,
    /// expirationDate property.
    pub expiration_date: Option<String>,
    /// gender property.
    pub gender: Option<String>,
    /// googleProductCategory property.
    pub google_product_category: Option<String>,
    /// gtin property.
    pub gtin: Option<String>,
    /// headlineOfferCondition property.
    pub headline_offer_condition: Option<String>,
    /// headlineOfferInstallment property.
    pub headline_offer_installment: Option<HeadlineOfferInstallment>,
    /// headlineOfferLink property.
    pub headline_offer_link: Option<String>,
    /// headlineOfferMobileLink property.
    pub headline_offer_mobile_link: Option<String>,
    /// headlineOfferPrice property.
    pub headline_offer_price: Option<Price>,
    /// headlineOfferShippingPrice property.
    pub headline_offer_shipping_price: Option<Price>,
    /// headlineOfferSubscriptionCost property.
    pub headline_offer_subscription_cost: Option<HeadlineOfferSubscriptionCost>,
    /// highPrice property.
    pub high_price: Option<Price>,
    /// imageLink property.
    pub image_link: Option<String>,
    /// includedDestinations property.
    pub included_destinations: Option<Vec<String>>,
    /// isBundle property.
    pub is_bundle: Option<bool>,
    /// itemGroupId property.
    pub item_group_id: Option<String>,
    /// lowPrice property.
    pub low_price: Option<Price>,
    /// material property.
    pub material: Option<String>,
    /// maxRating property.
    pub max_rating: Option<String>,
    /// minRating property.
    pub min_rating: Option<String>,
    /// mpn property.
    pub mpn: Option<String>,
    /// multipack property.
    pub multipack: Option<String>,
    /// numberOfOffers property.
    pub number_of_offers: Option<String>,
    /// pattern property.
    pub pattern: Option<String>,
    /// pause property.
    pub pause: Option<String>,
    /// productDetails property.
    pub product_details: Option<Vec<ProductDetail>>,
    /// productHeight property.
    pub product_height: Option<ProductDimension>,
    /// productHighlights property.
    pub product_highlights: Option<Vec<String>>,
    /// productLength property.
    pub product_length: Option<ProductDimension>,
    /// productTypes property.
    pub product_types: Option<Vec<String>>,
    /// productWeight property.
    pub product_weight: Option<ProductWeight>,
    /// productWidth property.
    pub product_width: Option<ProductDimension>,
    /// rating property.
    pub rating: Option<f64>,
    /// reviewCount property.
    pub review_count: Option<String>,
    /// size property.
    pub size: Option<String>,
    /// sizeSystem property.
    pub size_system: Option<String>,
    /// sizeTypes property.
    pub size_types: Option<Vec<String>>,
    /// title property.
    pub title: Option<String>,
}

/// `HeadlineOfferSubscriptionCost` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HeadlineOfferSubscriptionCost {
    /// amount property.
    pub amount: Option<Price>,
    /// period property.
    pub period: Option<String>,
    /// periodLength property.
    pub period_length: Option<String>,
}

/// `CustomAttribute` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomAttribute {
    /// groupValues property.
    pub group_values: Option<Vec<CustomAttribute>>,
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`css.accounts.cssProductInputs.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CssAccountsCssProductInputsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `supplementalFeedId`.
    pub supplemental_feed_id: Option<String>,
}

/// Arguments for [`css.accounts.cssProductInputs.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CssAccountsCssProductInputsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// DELETE v1/accounts/{accountsId}/cssProductInputs/{cssProductInputsId}
// -----------------------------------------------------------------------------

/// DELETE v1/accounts/{accountsId}/cssProductInputs/{cssProductInputsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = css_accounts_css_product_inputs_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn css_accounts_css_product_inputs_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://css.googleapis.com/v1/accounts/{{accountsId}}/cssProductInputs/{{cssProductInputsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/accounts/{accountsId}/cssProductInputs/{cssProductInputsId}
// -----------------------------------------------------------------------------

/// PATCH v1/accounts/{accountsId}/cssProductInputs/{cssProductInputsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = css_accounts_css_product_inputs_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn css_accounts_css_product_inputs_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CssProductInput>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://css.googleapis.com/v1/accounts/{{accountsId}}/cssProductInputs/{{cssProductInputsId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CssProductInput =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
