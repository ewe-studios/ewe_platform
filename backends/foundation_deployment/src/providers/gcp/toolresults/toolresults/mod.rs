//! Auto-generated API module for gcp/toolresults toolresults.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_toolresults_toolresults `

#![cfg(feature = "gcp_toolresults_toolresults")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ShardSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShardSummary {
    /// runs property.
    pub runs: Option<Vec<StepSummary>>,
    /// shardResult property.
    pub shard_result: Option<MergedResult>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `PerfEnvironment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PerfEnvironment {
    /// cpuInfo property.
    pub cpu_info: Option<CPUInfo>,
    /// memoryInfo property.
    pub memory_info: Option<MemoryInfo>,
}

/// `MemoryInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MemoryInfo {
    /// memoryCapInKibibyte property.
    pub memory_cap_in_kibibyte: Option<String>,
    /// memoryTotalInKibibyte property.
    pub memory_total_in_kibibyte: Option<String>,
}

/// `FailureDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FailureDetail {
    /// crashed property.
    pub crashed: Option<bool>,
    /// deviceOutOfMemory property.
    pub device_out_of_memory: Option<bool>,
    /// failedRoboscript property.
    pub failed_roboscript: Option<bool>,
    /// notInstalled property.
    pub not_installed: Option<bool>,
    /// otherNativeCrash property.
    pub other_native_crash: Option<bool>,
    /// timedOut property.
    pub timed_out: Option<bool>,
    /// unableToCrawl property.
    pub unable_to_crawl: Option<bool>,
}

/// `SuccessDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuccessDetail {
    /// otherNativeCrash property.
    pub other_native_crash: Option<bool>,
}

/// `CPUInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CPUInfo {
    /// cpuProcessor property.
    pub cpu_processor: Option<String>,
    /// cpuSpeedInGhz property.
    pub cpu_speed_in_ghz: Option<f64>,
    /// numberOfCores property.
    pub number_of_cores: Option<i64>,
}

/// `GraphicsStatsBucket` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GraphicsStatsBucket {
    /// frameCount property.
    pub frame_count: Option<String>,
    /// renderMillis property.
    pub render_millis: Option<String>,
}

/// `ListHistoriesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListHistoriesResponse {
    /// histories property.
    pub histories: Option<Vec<History>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `History` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct History {
    /// displayName property.
    pub display_name: Option<String>,
    /// historyId property.
    pub history_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// testPlatform property.
    pub test_platform: Option<String>,
}

/// `StepLabelsEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StepLabelsEntry {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `PerfSample` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PerfSample {
    /// sampleTime property.
    pub sample_time: Option<Timestamp>,
    /// value property.
    pub value: Option<f64>,
}

/// `ListStepThumbnailsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListStepThumbnailsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// thumbnails property.
    pub thumbnails: Option<Vec<Image>>,
}

/// `ListExecutionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListExecutionsResponse {
    /// executions property.
    pub executions: Option<Vec<Execution>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ResultsStorage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResultsStorage {
    /// resultsStoragePath property.
    pub results_storage_path: Option<FileReference>,
    /// xunitXmlFile property.
    pub xunit_xml_file: Option<FileReference>,
}

/// `Outcome` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Outcome {
    /// failureDetail property.
    pub failure_detail: Option<FailureDetail>,
    /// inconclusiveDetail property.
    pub inconclusive_detail: Option<InconclusiveDetail>,
    /// skippedDetail property.
    pub skipped_detail: Option<SkippedDetail>,
    /// successDetail property.
    pub success_detail: Option<SuccessDetail>,
    /// summary property.
    pub summary: Option<String>,
}

/// `AppStartTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppStartTime {
    /// fullyDrawnTime property.
    pub fully_drawn_time: Option<Duration>,
    /// initialDisplayTime property.
    pub initial_display_time: Option<Duration>,
}

/// `TestExecutionStep` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestExecutionStep {
    /// testIssues property.
    pub test_issues: Option<Vec<TestIssue>>,
    /// testSuiteOverviews property.
    pub test_suite_overviews: Option<Vec<TestSuiteOverview>>,
    /// testTiming property.
    pub test_timing: Option<TestTiming>,
    /// toolExecution property.
    pub tool_execution: Option<ToolExecution>,
}

/// `BatchCreatePerfSamplesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BatchCreatePerfSamplesRequest {
    /// perfSamples property.
    pub perf_samples: Option<Vec<PerfSample>>,
}

/// `ToolOutputReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolOutputReference {
    /// creationTime property.
    pub creation_time: Option<Timestamp>,
    /// output property.
    pub output: Option<FileReference>,
    /// testCase property.
    pub test_case: Option<TestCaseReference>,
}

/// `MatrixDimensionDefinition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MatrixDimensionDefinition {}

/// `Step` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Step {
    /// completionTime property.
    pub completion_time: Option<Timestamp>,
    /// creationTime property.
    pub creation_time: Option<Timestamp>,
    /// description property.
    pub description: Option<String>,
    /// deviceUsageDuration property.
    pub device_usage_duration: Option<Duration>,
    /// dimensionValue property.
    pub dimension_value: Option<Vec<StepDimensionValueEntry>>,
    /// hasImages property.
    pub has_images: Option<bool>,
    /// labels property.
    pub labels: Option<Vec<StepLabelsEntry>>,
    /// multiStep property.
    pub multi_step: Option<MultiStep>,
    /// name property.
    pub name: Option<String>,
    /// outcome property.
    pub outcome: Option<Outcome>,
    /// runDuration property.
    pub run_duration: Option<Duration>,
    /// state property.
    pub state: Option<String>,
    /// stepId property.
    pub step_id: Option<String>,
    /// testExecutionStep property.
    pub test_execution_step: Option<TestExecutionStep>,
    /// toolExecutionStep property.
    pub tool_execution_step: Option<ToolExecutionStep>,
}

/// `ListPerfSamplesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListPerfSamplesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// perfSamples property.
    pub perf_samples: Option<Vec<PerfSample>>,
}

/// `MergedResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MergedResult {
    /// outcome property.
    pub outcome: Option<Outcome>,
    /// state property.
    pub state: Option<String>,
    /// testSuiteOverviews property.
    pub test_suite_overviews: Option<Vec<TestSuiteOverview>>,
}

/// `ToolExecutionStep` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolExecutionStep {
    /// toolExecution property.
    pub tool_execution: Option<ToolExecution>,
}

/// `SafeHtmlProto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SafeHtmlProto {
    /// privateDoNotAccessOrElseSafeHtmlWrappedValue property.
    pub private_do_not_access_or_else_safe_html_wrapped_value: Option<String>,
}

/// `TestSuiteOverview` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestSuiteOverview {
    /// elapsedTime property.
    pub elapsed_time: Option<Duration>,
    /// errorCount property.
    pub error_count: Option<i64>,
    /// failureCount property.
    pub failure_count: Option<i64>,
    /// flakyCount property.
    pub flaky_count: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// skippedCount property.
    pub skipped_count: Option<i64>,
    /// totalCount property.
    pub total_count: Option<i64>,
    /// xmlSource property.
    pub xml_source: Option<FileReference>,
}

/// `AndroidTestLoop` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidTestLoop {}

/// `SuggestionClusterProto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuggestionClusterProto {
    /// category property.
    pub category: Option<String>,
    /// suggestions property.
    pub suggestions: Option<Vec<SuggestionProto>>,
}

/// `Duration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Duration {
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<String>,
}

/// `StackTrace` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StackTrace {
    /// exception property.
    pub exception: Option<String>,
}

/// `IndividualOutcome` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IndividualOutcome {
    /// multistepNumber property.
    pub multistep_number: Option<i64>,
    /// outcomeSummary property.
    pub outcome_summary: Option<String>,
    /// runDuration property.
    pub run_duration: Option<Duration>,
    /// stepId property.
    pub step_id: Option<String>,
}

/// `Any` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Any {
    /// typeUrl property.
    pub type_url: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `PrimaryStep` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PrimaryStep {
    /// individualOutcome property.
    pub individual_outcome: Option<Vec<IndividualOutcome>>,
    /// rollUp property.
    pub roll_up: Option<String>,
}

/// `AndroidTest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidTest {
    /// androidAppInfo property.
    pub android_app_info: Option<AndroidAppInfo>,
    /// androidInstrumentationTest property.
    pub android_instrumentation_test: Option<AndroidInstrumentationTest>,
    /// androidRoboTest property.
    pub android_robo_test: Option<AndroidRoboTest>,
    /// androidTestLoop property.
    pub android_test_loop: Option<AndroidTestLoop>,
    /// testTimeout property.
    pub test_timeout: Option<Duration>,
}

/// `IosXcTest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosXcTest {
    /// bundleId property.
    pub bundle_id: Option<String>,
    /// xcodeVersion property.
    pub xcode_version: Option<String>,
}

/// `ProjectSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProjectSettings {
    /// defaultBucket property.
    pub default_bucket: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `IosAppInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosAppInfo {
    /// name property.
    pub name: Option<String>,
}

/// `GraphicsStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GraphicsStats {
    /// buckets property.
    pub buckets: Option<Vec<GraphicsStatsBucket>>,
    /// highInputLatencyCount property.
    pub high_input_latency_count: Option<String>,
    /// jankyFrames property.
    pub janky_frames: Option<String>,
    /// missedVsyncCount property.
    pub missed_vsync_count: Option<String>,
    /// p50Millis property.
    pub p50_millis: Option<String>,
    /// p90Millis property.
    pub p90_millis: Option<String>,
    /// p95Millis property.
    pub p95_millis: Option<String>,
    /// p99Millis property.
    pub p99_millis: Option<String>,
    /// slowBitmapUploadCount property.
    pub slow_bitmap_upload_count: Option<String>,
    /// slowDrawCount property.
    pub slow_draw_count: Option<String>,
    /// slowUiThreadCount property.
    pub slow_ui_thread_count: Option<String>,
    /// totalFrames property.
    pub total_frames: Option<String>,
}

/// `StepSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StepSummary {}

/// `Specification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Specification {
    /// androidTest property.
    pub android_test: Option<AndroidTest>,
    /// iosTest property.
    pub ios_test: Option<IosTest>,
}

/// `ToolExitCode` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolExitCode {
    /// number property.
    pub number: Option<i64>,
}

/// `InconclusiveDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InconclusiveDetail {
    /// abortedByUser property.
    pub aborted_by_user: Option<bool>,
    /// hasErrorLogs property.
    pub has_error_logs: Option<bool>,
    /// infrastructureFailure property.
    pub infrastructure_failure: Option<bool>,
}

/// `TestIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestIssue {
    /// category property.
    pub category: Option<String>,
    /// errorMessage property.
    pub error_message: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// stackTrace property.
    pub stack_trace: Option<StackTrace>,
    /// type property.
    pub r#type: Option<String>,
    /// warning_migration property.
    pub warning_migration: Option<Any>,
}

/// `ListStepsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListStepsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// steps property.
    pub steps: Option<Vec<Step>>,
}

/// `Timestamp` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Timestamp {
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<String>,
}

/// `IosRoboTest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosRoboTest {}

/// `ListScreenshotClustersResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListScreenshotClustersResponse {
    /// clusters property.
    pub clusters: Option<Vec<ScreenshotCluster>>,
}

/// `Execution` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Execution {
    /// completionTime property.
    pub completion_time: Option<Timestamp>,
    /// creationTime property.
    pub creation_time: Option<Timestamp>,
    /// dimensionDefinitions property.
    pub dimension_definitions: Option<Vec<MatrixDimensionDefinition>>,
    /// executionId property.
    pub execution_id: Option<String>,
    /// outcome property.
    pub outcome: Option<Outcome>,
    /// specification property.
    pub specification: Option<Specification>,
    /// state property.
    pub state: Option<String>,
    /// testExecutionMatrixId property.
    pub test_execution_matrix_id: Option<String>,
}

/// `EnvironmentDimensionValueEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EnvironmentDimensionValueEntry {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `Image` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Image {
    /// error property.
    pub error: Option<Status>,
    /// sourceImage property.
    pub source_image: Option<ToolOutputReference>,
    /// stepId property.
    pub step_id: Option<String>,
    /// thumbnail property.
    pub thumbnail: Option<Thumbnail>,
}

/// `BatchCreatePerfSamplesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BatchCreatePerfSamplesResponse {
    /// perfSamples property.
    pub perf_samples: Option<Vec<PerfSample>>,
}

/// `StepDimensionValueEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StepDimensionValueEntry {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `TestCaseReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestCaseReference {
    /// className property.
    pub class_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// testSuiteName property.
    pub test_suite_name: Option<String>,
}

/// `AndroidInstrumentationTest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidInstrumentationTest {
    /// testPackageId property.
    pub test_package_id: Option<String>,
    /// testRunnerClass property.
    pub test_runner_class: Option<String>,
    /// testTargets property.
    pub test_targets: Option<Vec<String>>,
    /// useOrchestrator property.
    pub use_orchestrator: Option<bool>,
}

/// `IosTest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosTest {
    /// iosAppInfo property.
    pub ios_app_info: Option<IosAppInfo>,
    /// iosRoboTest property.
    pub ios_robo_test: Option<IosRoboTest>,
    /// iosTestLoop property.
    pub ios_test_loop: Option<IosTestLoop>,
    /// iosXcTest property.
    pub ios_xc_test: Option<IosXcTest>,
    /// testTimeout property.
    pub test_timeout: Option<Duration>,
}

/// `PerfMetricsSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PerfMetricsSummary {
    /// appStartTime property.
    pub app_start_time: Option<AppStartTime>,
    /// executionId property.
    pub execution_id: Option<String>,
    /// graphicsStats property.
    pub graphics_stats: Option<GraphicsStats>,
    /// historyId property.
    pub history_id: Option<String>,
    /// perfEnvironment property.
    pub perf_environment: Option<PerfEnvironment>,
    /// perfMetrics property.
    pub perf_metrics: Option<Vec<String>>,
    /// projectId property.
    pub project_id: Option<String>,
    /// stepId property.
    pub step_id: Option<String>,
}

/// `Environment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Environment {
    /// completionTime property.
    pub completion_time: Option<Timestamp>,
    /// creationTime property.
    pub creation_time: Option<Timestamp>,
    /// dimensionValue property.
    pub dimension_value: Option<Vec<EnvironmentDimensionValueEntry>>,
    /// displayName property.
    pub display_name: Option<String>,
    /// environmentId property.
    pub environment_id: Option<String>,
    /// environmentResult property.
    pub environment_result: Option<MergedResult>,
    /// executionId property.
    pub execution_id: Option<String>,
    /// historyId property.
    pub history_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// resultsStorage property.
    pub results_storage: Option<ResultsStorage>,
    /// shardSummaries property.
    pub shard_summaries: Option<Vec<ShardSummary>>,
}

/// `Thumbnail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Thumbnail {
    /// contentType property.
    pub content_type: Option<String>,
    /// data property.
    pub data: Option<String>,
    /// heightPx property.
    pub height_px: Option<i64>,
    /// widthPx property.
    pub width_px: Option<i64>,
}

/// `ListPerfSampleSeriesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListPerfSampleSeriesResponse {
    /// perfSampleSeries property.
    pub perf_sample_series: Option<Vec<PerfSampleSeries>>,
}

/// `RegionProto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RegionProto {
    /// heightPx property.
    pub height_px: Option<i64>,
    /// leftPx property.
    pub left_px: Option<i64>,
    /// topPx property.
    pub top_px: Option<i64>,
    /// widthPx property.
    pub width_px: Option<i64>,
}

/// `SuggestionProto` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SuggestionProto {
    /// helpUrl property.
    pub help_url: Option<String>,
    /// longMessage property.
    pub long_message: Option<SafeHtmlProto>,
    /// priority property.
    pub priority: Option<String>,
    /// pseudoResourceId property.
    pub pseudo_resource_id: Option<String>,
    /// region property.
    pub region: Option<RegionProto>,
    /// resourceName property.
    pub resource_name: Option<String>,
    /// screenId property.
    pub screen_id: Option<String>,
    /// secondaryPriority property.
    pub secondary_priority: Option<f64>,
    /// shortMessage property.
    pub short_message: Option<SafeHtmlProto>,
    /// title property.
    pub title: Option<String>,
}

/// `BasicPerfSampleSeries` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BasicPerfSampleSeries {
    /// perfMetricType property.
    pub perf_metric_type: Option<String>,
    /// perfUnit property.
    pub perf_unit: Option<String>,
    /// sampleSeriesLabel property.
    pub sample_series_label: Option<String>,
}

/// `ListTestCasesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListTestCasesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// testCases property.
    pub test_cases: Option<Vec<TestCase>>,
}

/// `IosTestLoop` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IosTestLoop {
    /// bundleId property.
    pub bundle_id: Option<String>,
}

/// `ListEnvironmentsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListEnvironmentsResponse {
    /// environments property.
    pub environments: Option<Vec<Environment>>,
    /// executionId property.
    pub execution_id: Option<String>,
    /// historyId property.
    pub history_id: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
}

/// `TestCase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestCase {
    /// elapsedTime property.
    pub elapsed_time: Option<Duration>,
    /// endTime property.
    pub end_time: Option<Timestamp>,
    /// skippedMessage property.
    pub skipped_message: Option<String>,
    /// stackTraces property.
    pub stack_traces: Option<Vec<StackTrace>>,
    /// startTime property.
    pub start_time: Option<Timestamp>,
    /// status property.
    pub status: Option<String>,
    /// testCaseId property.
    pub test_case_id: Option<String>,
    /// testCaseReference property.
    pub test_case_reference: Option<TestCaseReference>,
    /// toolOutputs property.
    pub tool_outputs: Option<Vec<ToolOutputReference>>,
}

/// `Screen` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Screen {
    /// fileReference property.
    pub file_reference: Option<String>,
    /// locale property.
    pub locale: Option<String>,
    /// model property.
    pub model: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `TestTiming` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestTiming {
    /// testProcessDuration property.
    pub test_process_duration: Option<Duration>,
}

/// `AndroidRoboTest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidRoboTest {
    /// appInitialActivity property.
    pub app_initial_activity: Option<String>,
    /// bootstrapPackageId property.
    pub bootstrap_package_id: Option<String>,
    /// bootstrapRunnerClass property.
    pub bootstrap_runner_class: Option<String>,
    /// maxDepth property.
    pub max_depth: Option<i64>,
    /// maxSteps property.
    pub max_steps: Option<i64>,
}

/// `PerfSampleSeries` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PerfSampleSeries {
    /// basicPerfSampleSeries property.
    pub basic_perf_sample_series: Option<BasicPerfSampleSeries>,
    /// executionId property.
    pub execution_id: Option<String>,
    /// historyId property.
    pub history_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// sampleSeriesId property.
    pub sample_series_id: Option<String>,
    /// stepId property.
    pub step_id: Option<String>,
}

/// `PublishXunitXmlFilesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PublishXunitXmlFilesRequest {
    /// xunitXmlFiles property.
    pub xunit_xml_files: Option<Vec<FileReference>>,
}

/// `SkippedDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SkippedDetail {
    /// incompatibleAppVersion property.
    pub incompatible_app_version: Option<bool>,
    /// incompatibleArchitecture property.
    pub incompatible_architecture: Option<bool>,
    /// incompatibleDevice property.
    pub incompatible_device: Option<bool>,
    /// pendingTimeout property.
    pub pending_timeout: Option<bool>,
}

/// `ToolExecution` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ToolExecution {
    /// commandLineArguments property.
    pub command_line_arguments: Option<Vec<String>>,
    /// exitCode property.
    pub exit_code: Option<ToolExitCode>,
    /// toolLogs property.
    pub tool_logs: Option<Vec<FileReference>>,
    /// toolOutputs property.
    pub tool_outputs: Option<Vec<ToolOutputReference>>,
}

/// `FileReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FileReference {
    /// fileUri property.
    pub file_uri: Option<String>,
}

/// `ScreenshotCluster` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ScreenshotCluster {
    /// activity property.
    pub activity: Option<String>,
    /// clusterId property.
    pub cluster_id: Option<String>,
    /// keyScreen property.
    pub key_screen: Option<Screen>,
    /// screens property.
    pub screens: Option<Vec<Screen>>,
}

/// `MultiStep` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MultiStep {
    /// multistepNumber property.
    pub multistep_number: Option<i64>,
    /// primaryStep property.
    pub primary_step: Option<PrimaryStep>,
    /// primaryStepId property.
    pub primary_step_id: Option<String>,
}

/// `ListStepAccessibilityClustersResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListStepAccessibilityClustersResponse {
    /// clusters property.
    pub clusters: Option<Vec<SuggestionClusterProto>>,
    /// name property.
    pub name: Option<String>,
}

/// `AndroidAppInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AndroidAppInfo {
    /// name property.
    pub name: Option<String>,
    /// packageName property.
    pub package_name: Option<String>,
    /// versionCode property.
    pub version_code: Option<String>,
    /// versionName property.
    pub version_name: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`toolresults.projects.getSettings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsGetSettingsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

/// Arguments for [`toolresults.projects.initializeSettings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsInitializeSettingsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

/// Arguments for [`toolresults.projects.histories.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: History,
}

/// Arguments for [`toolresults.projects.histories.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
}

/// Arguments for [`toolresults.projects.histories.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `filterByName`.
    pub filter_by_name: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`toolresults.projects.histories.executions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Execution,
}

/// Arguments for [`toolresults.projects.histories.executions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
}

/// Arguments for [`toolresults.projects.histories.executions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`toolresults.projects.histories.executions.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsPatchArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Execution,
}

/// Arguments for [`toolresults.projects.histories.executions.clusters.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsClustersGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `clusterId`.
    pub cluster_id: String,
}

/// Arguments for [`toolresults.projects.histories.executions.clusters.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsClustersListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
}

/// Arguments for [`toolresults.projects.histories.executions.environments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsEnvironmentsGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `environmentId`.
    pub environment_id: String,
}

/// Arguments for [`toolresults.projects.histories.executions.environments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsEnvironmentsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.accessibilityClusters_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsAccessibilityClustersArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Step,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.getPerfMetricsSummary_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsGetPerfMetricsSummaryArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPatchArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Step,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.publishXunitXmlFiles_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPublishXunitXmlFilesArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
    /// Request body.
    pub body: PublishXunitXmlFilesRequest,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.perfMetricsSummary.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfMetricsSummaryCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
    /// Request body.
    pub body: PerfMetricsSummary,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.perfSampleSeries.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
    /// Request body.
    pub body: PerfSampleSeries,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.perfSampleSeries.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
    /// Path parameter: `sampleSeriesId`.
    pub sample_series_id: String,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.perfSampleSeries.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.perfSampleSeries.samples.batchCreate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesSamplesBatchCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
    /// Path parameter: `sampleSeriesId`.
    pub sample_series_id: String,
    /// Request body.
    pub body: BatchCreatePerfSamplesRequest,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.perfSampleSeries.samples.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesSamplesListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
    /// Path parameter: `sampleSeriesId`.
    pub sample_series_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.testCases.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsTestCasesGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
    /// Path parameter: `testCaseId`.
    pub test_case_id: String,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.testCases.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsTestCasesListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`toolresults.projects.histories.executions.steps.thumbnails.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ToolresultsProjectsHistoriesExecutionsStepsThumbnailsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `historyId`.
    pub history_id: String,
    /// Path parameter: `executionId`.
    pub execution_id: String,
    /// Path parameter: `stepId`.
    pub step_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/settings
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/settings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_get_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_get_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsGetSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProjectSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/settings",
        args.project_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProjectSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST toolresults/v1beta3/projects/{projectId}:initializeSettings
// -----------------------------------------------------------------------------

/// POST toolresults/v1beta3/projects/{projectId}:initializeSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_initialize_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_initialize_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsInitializeSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProjectSettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}:initializeSettings",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProjectSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST toolresults/v1beta3/projects/{projectId}/histories
// -----------------------------------------------------------------------------

/// POST toolresults/v1beta3/projects/{projectId}/histories.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<History>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: History =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<History>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}",
        args.project_id, args.history_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: History =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListHistoriesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter_by_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filterByName=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListHistoriesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions
// -----------------------------------------------------------------------------

/// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Execution>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions",
        args.project_id,
        args.history_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Execution =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Execution>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}",
        args.project_id,
        args.history_id,
        args.execution_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Execution =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListExecutionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions",
        args.project_id,
        args.history_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListExecutionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}
// -----------------------------------------------------------------------------

/// PATCH toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Execution>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}",
        args.project_id,
        args.history_id,
        args.execution_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Execution =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters/{clusterId}
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters/{clusterId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_clusters_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_clusters_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsClustersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ScreenshotCluster>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/clusters/{}",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.cluster_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ScreenshotCluster =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/clusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_clusters_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_clusters_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsClustersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListScreenshotClustersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/clusters",
        args.project_id,
        args.history_id,
        args.execution_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListScreenshotClustersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments/{environmentId}
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments/{environmentId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_environments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_environments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsEnvironmentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Environment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/environments/{}",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.environment_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Environment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/environments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_environments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_environments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsEnvironmentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListEnvironmentsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/environments",
        args.project_id,
        args.history_id,
        args.execution_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListEnvironmentsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/{+name}:accessibilityClusters
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/{+name}:accessibilityClusters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_accessibility_clusters_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_accessibility_clusters_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsAccessibilityClustersArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListStepAccessibilityClustersResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/{}:accessibilityClusters",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListStepAccessibilityClustersResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps
// -----------------------------------------------------------------------------

/// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Step>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps",
        args.project_id,
        args.history_id,
        args.execution_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Step =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Step>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Step =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_get_perf_metrics_summary_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_get_perf_metrics_summary_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsGetPerfMetricsSummaryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PerfMetricsSummary>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfMetricsSummary",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PerfMetricsSummary =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListStepsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps",
        args.project_id,
        args.history_id,
        args.execution_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListStepsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}
// -----------------------------------------------------------------------------

/// PATCH toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Step>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Step =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}:publishXunitXmlFiles
// -----------------------------------------------------------------------------

/// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}:publishXunitXmlFiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_publish_xunit_xml_files_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_publish_xunit_xml_files_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPublishXunitXmlFilesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Step>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}:publishXunitXmlFiles",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Step =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary
// -----------------------------------------------------------------------------

/// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfMetricsSummary.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_perf_metrics_summary_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_perf_metrics_summary_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfMetricsSummaryCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PerfMetricsSummary>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfMetricsSummary",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PerfMetricsSummary =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries
// -----------------------------------------------------------------------------

/// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_perf_sample_series_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_perf_sample_series_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PerfSampleSeries>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfSampleSeries",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PerfSampleSeries =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_perf_sample_series_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_perf_sample_series_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PerfSampleSeries>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfSampleSeries/{}",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
        args.sample_series_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PerfSampleSeries =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_perf_sample_series_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_perf_sample_series_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPerfSampleSeriesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfSampleSeries",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPerfSampleSeriesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples:batchCreate
// -----------------------------------------------------------------------------

/// POST toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples:batchCreate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_perf_sample_series_samples_batch_create_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesSamplesBatchCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchCreatePerfSamplesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfSampleSeries/{}/samples:batchCreate",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
        args.sample_series_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BatchCreatePerfSamplesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/perfSampleSeries/{sampleSeriesId}/samples.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_perf_sample_series_samples_list_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsPerfSampleSeriesSamplesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPerfSamplesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/perfSampleSeries/{}/samples",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
        args.sample_series_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPerfSamplesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases/{testCaseId}
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases/{testCaseId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_test_cases_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_test_cases_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsTestCasesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestCase>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/testCases/{}",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
        args.test_case_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestCase =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/testCases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_test_cases_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_test_cases_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsTestCasesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTestCasesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/testCases",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListTestCasesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/thumbnails
// -----------------------------------------------------------------------------

/// GET toolresults/v1beta3/projects/{projectId}/histories/{historyId}/executions/{executionId}/steps/{stepId}/thumbnails.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = toolresults_projects_histories_executions_steps_thumbnails_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn toolresults_projects_histories_executions_steps_thumbnails_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ToolresultsProjectsHistoriesExecutionsStepsThumbnailsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListStepThumbnailsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://toolresults.googleapis.com/toolresults/v1beta3/projects/{}/histories/{}/executions/{}/steps/{}/thumbnails",
        args.project_id,
        args.history_id,
        args.execution_id,
        args.step_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListStepThumbnailsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
