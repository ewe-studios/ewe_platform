//! Auto-generated API module for gcp/games players.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_games_players `

#![cfg(feature = "gcp_games_players")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `PlayerListResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlayerListResponse {
    /// items property.
    pub items: Option<Vec<Player>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Player` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Player {
    /// avatarImageUrl property.
    pub avatar_image_url: Option<String>,
    /// bannerUrlLandscape property.
    pub banner_url_landscape: Option<String>,
    /// bannerUrlPortrait property.
    pub banner_url_portrait: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// experienceInfo property.
    pub experience_info: Option<PlayerExperienceInfo>,
    /// friendStatus property.
    pub friend_status: Option<String>,
    /// gamePlayerId property.
    pub game_player_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// originalPlayerId property.
    pub original_player_id: Option<String>,
    /// playerId property.
    pub player_id: Option<String>,
    /// profileSettings property.
    pub profile_settings: Option<ProfileSettings>,
    /// title property.
    pub title: Option<String>,
}

/// `PlayerExperienceInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlayerExperienceInfo {
    /// currentExperiencePoints property.
    pub current_experience_points: Option<String>,
    /// currentLevel property.
    pub current_level: Option<PlayerLevel>,
    /// kind property.
    pub kind: Option<String>,
    /// lastLevelUpTimestampMillis property.
    pub last_level_up_timestamp_millis: Option<String>,
    /// nextLevel property.
    pub next_level: Option<PlayerLevel>,
}

/// `ProfileSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProfileSettings {
    /// friendsListVisibility property.
    pub friends_list_visibility: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// profileVisible property.
    pub profile_visible: Option<bool>,
}

/// `PlayerLevel` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlayerLevel {
    /// kind property.
    pub kind: Option<String>,
    /// level property.
    pub level: Option<i64>,
    /// maxExperiencePoints property.
    pub max_experience_points: Option<String>,
    /// minExperiencePoints property.
    pub min_experience_points: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`games.players.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesPlayersGetArgs {
    /// Path parameter: `playerId`.
    pub player_id: String,
    /// Query parameter: `language`.
    pub language: Option<String>,
    /// Query parameter: `playerIdConsistencyToken`.
    pub player_id_consistency_token: Option<String>,
}

/// Arguments for [`games.players.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesPlayersListArgs {
    /// Path parameter: `collection`.
    pub collection: String,
    /// Query parameter: `language`.
    pub language: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET games/v1/players/{playerId}
// -----------------------------------------------------------------------------

/// GET games/v1/players/{playerId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = games_players_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn games_players_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GamesPlayersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Player>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/players/{}",
        args.player_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Player =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET games/v1/players/me/players/{collection}
// -----------------------------------------------------------------------------

/// GET games/v1/players/me/players/{collection}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = games_players_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn games_players_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GamesPlayersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlayerListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://games.googleapis.com/games/v1/players/me/players/{}",
        args.collection,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlayerListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
