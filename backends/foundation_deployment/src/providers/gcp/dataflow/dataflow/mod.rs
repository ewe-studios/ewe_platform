//! Auto-generated API module for gcp/dataflow dataflow.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dataflow_dataflow `

#![cfg(feature = "gcp_dataflow_dataflow")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `MemInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MemInfo {
    /// currentLimitBytes property.
    pub current_limit_bytes: Option<String>,
    /// currentOoms property.
    pub current_ooms: Option<String>,
    /// currentRssBytes property.
    pub current_rss_bytes: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
    /// totalGbMs property.
    pub total_gb_ms: Option<String>,
}

/// `BoundedTrieNode` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BoundedTrieNode {
    /// children property.
    pub children: Option<serde_json::Value>,
    /// truncated property.
    pub truncated: Option<bool>,
}

/// `DebugOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DebugOptions {
    /// dataSampling property.
    pub data_sampling: Option<DataSamplingConfig>,
    /// enableHotKeyLogging property.
    pub enable_hot_key_logging: Option<bool>,
}

/// `StageExecutionDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageExecutionDetails {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// workers property.
    pub workers: Option<Vec<WorkerDetails>>,
}

/// `PubsubLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PubsubLocation {
    /// dropLateData property.
    pub drop_late_data: Option<bool>,
    /// dynamicDestinations property.
    pub dynamic_destinations: Option<bool>,
    /// idLabel property.
    pub id_label: Option<String>,
    /// subscription property.
    pub subscription: Option<String>,
    /// timestampLabel property.
    pub timestamp_label: Option<String>,
    /// topic property.
    pub topic: Option<String>,
    /// trackingSubscription property.
    pub tracking_subscription: Option<String>,
    /// withAttributes property.
    pub with_attributes: Option<bool>,
}

/// `StreamingApplianceSnapshotConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingApplianceSnapshotConfig {
    /// importStateEndpoint property.
    pub import_state_endpoint: Option<String>,
    /// snapshotId property.
    pub snapshot_id: Option<String>,
}

/// `StreamingScalingReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingScalingReport {
    /// activeBundleCount property.
    pub active_bundle_count: Option<i64>,
    /// activeThreadCount property.
    pub active_thread_count: Option<i64>,
    /// maximumBundleCount property.
    pub maximum_bundle_count: Option<i64>,
    /// maximumBytes property.
    pub maximum_bytes: Option<String>,
    /// maximumBytesCount property.
    pub maximum_bytes_count: Option<i64>,
    /// maximumThreadCount property.
    pub maximum_thread_count: Option<i64>,
    /// outstandingBundleCount property.
    pub outstanding_bundle_count: Option<i64>,
    /// outstandingBytes property.
    pub outstanding_bytes: Option<String>,
    /// outstandingBytesCount property.
    pub outstanding_bytes_count: Option<i64>,
}

/// `DeleteSnapshotResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeleteSnapshotResponse {}

/// `Position` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Position {
    /// byteOffset property.
    pub byte_offset: Option<String>,
    /// concatPosition property.
    pub concat_position: Option<Box<ConcatPosition>>,
    /// end property.
    pub end: Option<bool>,
    /// key property.
    pub key: Option<String>,
    /// recordIndex property.
    pub record_index: Option<String>,
    /// shufflePosition property.
    pub shuffle_position: Option<String>,
}

/// `ListSnapshotsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListSnapshotsResponse {
    /// snapshots property.
    pub snapshots: Option<Vec<Snapshot>>,
}

/// `KeyRangeLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct KeyRangeLocation {
    /// dataDisk property.
    pub data_disk: Option<String>,
    /// deliveryEndpoint property.
    pub delivery_endpoint: Option<String>,
    /// deprecatedPersistentDirectory property.
    pub deprecated_persistent_directory: Option<String>,
    /// end property.
    pub end: Option<String>,
    /// start property.
    pub start: Option<String>,
}

/// `DataflowHistogramValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataflowHistogramValue {
    /// bucketCounts property.
    pub bucket_counts: Option<Vec<String>>,
    /// bucketOptions property.
    pub bucket_options: Option<BucketOptions>,
    /// count property.
    pub count: Option<String>,
    /// outlierStats property.
    pub outlier_stats: Option<OutlierStats>,
}

/// `AutoscalingSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalingSettings {
    /// algorithm property.
    pub algorithm: Option<String>,
    /// maxNumWorkers property.
    pub max_num_workers: Option<i64>,
}

/// `MetricShortId` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MetricShortId {
    /// metricIndex property.
    pub metric_index: Option<i64>,
    /// shortId property.
    pub short_id: Option<String>,
}

/// `ResourceUtilizationReportResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceUtilizationReportResponse {}

/// `Base2Exponent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Base2Exponent {
    /// numberOfBuckets property.
    pub number_of_buckets: Option<i64>,
    /// scale property.
    pub scale: Option<i64>,
}

/// `Package` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Package {
    /// location property.
    pub location: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `DistributionUpdate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DistributionUpdate {
    /// count property.
    pub count: Option<SplitInt64>,
    /// histogram property.
    pub histogram: Option<Histogram>,
    /// max property.
    pub max: Option<SplitInt64>,
    /// min property.
    pub min: Option<SplitInt64>,
    /// sum property.
    pub sum: Option<SplitInt64>,
    /// sumOfSquares property.
    pub sum_of_squares: Option<f64>,
}

/// `SourceMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceMetadata {
    /// estimatedSizeBytes property.
    pub estimated_size_bytes: Option<String>,
    /// infinite property.
    pub infinite: Option<bool>,
    /// producesSortedKeys property.
    pub produces_sorted_keys: Option<bool>,
}

/// `GPUUtilization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GPUUtilization {
    /// rate property.
    pub rate: Option<f64>,
}

/// `IntegerGauge` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntegerGauge {
    /// timestamp property.
    pub timestamp: Option<String>,
    /// value property.
    pub value: Option<SplitInt64>,
}

/// `WorkerHealthReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerHealthReport {
    /// msg property.
    pub msg: Option<String>,
    /// pods property.
    pub pods: Option<Vec<serde_json::Value>>,
    /// reportInterval property.
    pub report_interval: Option<String>,
    /// vmBrokenCode property.
    pub vm_broken_code: Option<String>,
    /// vmIsBroken property.
    pub vm_is_broken: Option<bool>,
    /// vmIsHealthy property.
    pub vm_is_healthy: Option<bool>,
    /// vmStartupTime property.
    pub vm_startup_time: Option<String>,
}

/// `TransformSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransformSummary {
    /// displayData property.
    pub display_data: Option<Vec<DisplayData>>,
    /// id property.
    pub id: Option<String>,
    /// inputCollectionName property.
    pub input_collection_name: Option<Vec<String>>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// outputCollectionName property.
    pub output_collection_name: Option<Vec<String>>,
}

/// `BucketOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BucketOptions {
    /// exponential property.
    pub exponential: Option<Base2Exponent>,
    /// linear property.
    pub linear: Option<Linear>,
}

/// `TaskRunnerSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TaskRunnerSettings {
    /// alsologtostderr property.
    pub alsologtostderr: Option<bool>,
    /// baseTaskDir property.
    pub base_task_dir: Option<String>,
    /// baseUrl property.
    pub base_url: Option<String>,
    /// commandlinesFileName property.
    pub commandlines_file_name: Option<String>,
    /// continueOnException property.
    pub continue_on_exception: Option<bool>,
    /// dataflowApiVersion property.
    pub dataflow_api_version: Option<String>,
    /// harnessCommand property.
    pub harness_command: Option<String>,
    /// languageHint property.
    pub language_hint: Option<String>,
    /// logDir property.
    pub log_dir: Option<String>,
    /// logToSerialconsole property.
    pub log_to_serialconsole: Option<bool>,
    /// logUploadLocation property.
    pub log_upload_location: Option<String>,
    /// oauthScopes property.
    pub oauth_scopes: Option<Vec<String>>,
    /// parallelWorkerSettings property.
    pub parallel_worker_settings: Option<WorkerSettings>,
    /// streamingWorkerMainClass property.
    pub streaming_worker_main_class: Option<String>,
    /// taskGroup property.
    pub task_group: Option<String>,
    /// taskUser property.
    pub task_user: Option<String>,
    /// tempStoragePrefix property.
    pub temp_storage_prefix: Option<String>,
    /// vmId property.
    pub vm_id: Option<String>,
    /// workflowFileName property.
    pub workflow_file_name: Option<String>,
}

/// `FloatingPointMean` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FloatingPointMean {
    /// count property.
    pub count: Option<SplitInt64>,
    /// sum property.
    pub sum: Option<f64>,
}

/// `WorkItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkItem {
    /// configuration property.
    pub configuration: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// initialReportIndex property.
    pub initial_report_index: Option<String>,
    /// jobId property.
    pub job_id: Option<String>,
    /// leaseExpireTime property.
    pub lease_expire_time: Option<String>,
    /// mapTask property.
    pub map_task: Option<MapTask>,
    /// packages property.
    pub packages: Option<Vec<Package>>,
    /// projectId property.
    pub project_id: Option<String>,
    /// reportStatusInterval property.
    pub report_status_interval: Option<String>,
    /// seqMapTask property.
    pub seq_map_task: Option<SeqMapTask>,
    /// shellTask property.
    pub shell_task: Option<ShellTask>,
    /// sourceOperationTask property.
    pub source_operation_task: Option<SourceOperationRequest>,
    /// streamingComputationTask property.
    pub streaming_computation_task: Option<StreamingComputationTask>,
    /// streamingConfigTask property.
    pub streaming_config_task: Option<StreamingConfigTask>,
    /// streamingSetupTask property.
    pub streaming_setup_task: Option<StreamingSetupTask>,
}

/// `FileIODetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FileIODetails {
    /// filePattern property.
    pub file_pattern: Option<String>,
}

/// `ExecutionStageSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecutionStageSummary {
    /// componentSource property.
    pub component_source: Option<Vec<ComponentSource>>,
    /// componentTransform property.
    pub component_transform: Option<Vec<ComponentTransform>>,
    /// id property.
    pub id: Option<String>,
    /// inputSource property.
    pub input_source: Option<Vec<StageSource>>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// outputSource property.
    pub output_source: Option<Vec<StageSource>>,
    /// prerequisiteStage property.
    pub prerequisite_stage: Option<Vec<String>>,
}

/// `BigQueryIODetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BigQueryIODetails {
    /// dataset property.
    pub dataset: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// query property.
    pub query: Option<String>,
    /// table property.
    pub table: Option<String>,
}

/// `MetricStructuredName` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MetricStructuredName {
    /// context property.
    pub context: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// origin property.
    pub origin: Option<String>,
}

/// `MountedDataDisk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MountedDataDisk {
    /// dataDisk property.
    pub data_disk: Option<String>,
}

/// `ListJobsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListJobsResponse {
    /// failedLocation property.
    pub failed_location: Option<Vec<FailedLocation>>,
    /// jobs property.
    pub jobs: Option<Vec<Job>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `WriteInstruction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WriteInstruction {
    /// input property.
    pub input: Option<InstructionInput>,
    /// sink property.
    pub sink: Option<Sink>,
}

/// `LaunchTemplateParameters` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LaunchTemplateParameters {
    /// environment property.
    pub environment: Option<RuntimeEnvironment>,
    /// jobName property.
    pub job_name: Option<String>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
    /// transformNameMapping property.
    pub transform_name_mapping: Option<serde_json::Value>,
    /// update property.
    pub update: Option<bool>,
}

/// `InstructionInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstructionInput {
    /// outputNum property.
    pub output_num: Option<i64>,
    /// producerInstructionIndex property.
    pub producer_instruction_index: Option<i64>,
}

/// `DerivedSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DerivedSource {
    /// derivationMode property.
    pub derivation_mode: Option<String>,
    /// source property.
    pub source: Option<Source>,
}

/// `ComponentSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComponentSource {
    /// name property.
    pub name: Option<String>,
    /// originalTransformOrCollection property.
    pub original_transform_or_collection: Option<String>,
    /// userName property.
    pub user_name: Option<String>,
}

/// `PubSubIODetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PubSubIODetails {
    /// subscription property.
    pub subscription: Option<String>,
    /// topic property.
    pub topic: Option<String>,
}

/// `GetWorkerStacktracesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetWorkerStacktracesRequest {
    /// endTime property.
    pub end_time: Option<String>,
    /// workerId property.
    pub worker_id: Option<String>,
}

/// `FloatingPointList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FloatingPointList {
    /// elements property.
    pub elements: Option<Vec<f64>>,
}

/// `NameAndKind` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NameAndKind {
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `DatastoreIODetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatastoreIODetails {
    /// namespace property.
    pub namespace: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
}

/// `SdkBug` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SdkBug {
    /// severity property.
    pub severity: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// uri property.
    pub uri: Option<String>,
}

/// `StringList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StringList {
    /// elements property.
    pub elements: Option<Vec<String>>,
}

/// `MetricUpdate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MetricUpdate {
    /// boundedTrie property.
    pub bounded_trie: Option<serde_json::Value>,
    /// cumulative property.
    pub cumulative: Option<bool>,
    /// distribution property.
    pub distribution: Option<serde_json::Value>,
    /// gauge property.
    pub gauge: Option<serde_json::Value>,
    /// internal property.
    pub internal: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// meanCount property.
    pub mean_count: Option<serde_json::Value>,
    /// meanSum property.
    pub mean_sum: Option<serde_json::Value>,
    /// name property.
    pub name: Option<MetricStructuredName>,
    /// scalar property.
    pub scalar: Option<serde_json::Value>,
    /// set property.
    pub set: Option<serde_json::Value>,
    /// trie property.
    pub trie: Option<serde_json::Value>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `ProgressTimeseries` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProgressTimeseries {
    /// currentProgress property.
    pub current_progress: Option<f64>,
    /// dataPoints property.
    pub data_points: Option<Vec<Point>>,
}

/// `SendWorkerMessagesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SendWorkerMessagesRequest {
    /// location property.
    pub location: Option<String>,
    /// workerMessages property.
    pub worker_messages: Option<Vec<WorkerMessage>>,
}

/// `StageSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageSource {
    /// name property.
    pub name: Option<String>,
    /// originalTransformOrCollection property.
    pub original_transform_or_collection: Option<String>,
    /// sizeBytes property.
    pub size_bytes: Option<String>,
    /// userName property.
    pub user_name: Option<String>,
}

/// `ParameterMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ParameterMetadata {
    /// customMetadata property.
    pub custom_metadata: Option<serde_json::Value>,
    /// defaultValue property.
    pub default_value: Option<String>,
    /// enumOptions property.
    pub enum_options: Option<Vec<ParameterMetadataEnumOption>>,
    /// groupName property.
    pub group_name: Option<String>,
    /// helpText property.
    pub help_text: Option<String>,
    /// hiddenUi property.
    pub hidden_ui: Option<bool>,
    /// isOptional property.
    pub is_optional: Option<bool>,
    /// label property.
    pub label: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// paramType property.
    pub param_type: Option<String>,
    /// parentName property.
    pub parent_name: Option<String>,
    /// parentTriggerValues property.
    pub parent_trigger_values: Option<Vec<String>>,
    /// regexes property.
    pub regexes: Option<Vec<String>>,
}

/// `WorkItemDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkItemDetails {
    /// attemptId property.
    pub attempt_id: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<MetricUpdate>>,
    /// progress property.
    pub progress: Option<ProgressTimeseries>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// stragglerInfo property.
    pub straggler_info: Option<StragglerInfo>,
    /// taskId property.
    pub task_id: Option<String>,
}

/// `PubsubSnapshotMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PubsubSnapshotMetadata {
    /// expireTime property.
    pub expire_time: Option<String>,
    /// snapshotName property.
    pub snapshot_name: Option<String>,
    /// topicName property.
    pub topic_name: Option<String>,
}

/// `InstructionOutput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstructionOutput {
    /// codec property.
    pub codec: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// onlyCountKeyBytes property.
    pub only_count_key_bytes: Option<bool>,
    /// onlyCountValueBytes property.
    pub only_count_value_bytes: Option<bool>,
    /// originalName property.
    pub original_name: Option<String>,
    /// systemName property.
    pub system_name: Option<String>,
}

/// `Snapshot` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Snapshot {
    /// creationTime property.
    pub creation_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// diskSizeBytes property.
    pub disk_size_bytes: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// pubsubMetadata property.
    pub pubsub_metadata: Option<Vec<PubsubSnapshotMetadata>>,
    /// region property.
    pub region: Option<String>,
    /// sourceJobId property.
    pub source_job_id: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// ttl property.
    pub ttl: Option<String>,
}

/// `TopologyConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TopologyConfig {
    /// computations property.
    pub computations: Option<Vec<ComputationTopology>>,
    /// dataDiskAssignments property.
    pub data_disk_assignments: Option<Vec<DataDiskAssignment>>,
    /// forwardingKeyBits property.
    pub forwarding_key_bits: Option<i64>,
    /// persistentStateVersion property.
    pub persistent_state_version: Option<i64>,
    /// userStageToComputationNameMap property.
    pub user_stage_to_computation_name_map: Option<serde_json::Value>,
}

/// `SideInputInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SideInputInfo {
    /// kind property.
    pub kind: Option<serde_json::Value>,
    /// sources property.
    pub sources: Option<Vec<Source>>,
    /// tag property.
    pub tag: Option<String>,
}

/// `SourceSplitRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceSplitRequest {
    /// options property.
    pub options: Option<SourceSplitOptions>,
    /// source property.
    pub source: Option<Source>,
}

/// `Straggler` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Straggler {
    /// batchStraggler property.
    pub batch_straggler: Option<StragglerInfo>,
    /// streamingStraggler property.
    pub streaming_straggler: Option<StreamingStragglerInfo>,
}

/// `MetricValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MetricValue {
    /// metric property.
    pub metric: Option<String>,
    /// metricLabels property.
    pub metric_labels: Option<serde_json::Value>,
    /// valueGauge64 property.
    pub value_gauge64: Option<DataflowGaugeValue>,
    /// valueHistogram property.
    pub value_histogram: Option<DataflowHistogramValue>,
    /// valueInt64 property.
    pub value_int64: Option<String>,
}

/// `AutoscalingEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AutoscalingEvent {
    /// currentNumWorkers property.
    pub current_num_workers: Option<String>,
    /// description property.
    pub description: Option<StructuredMessage>,
    /// eventType property.
    pub event_type: Option<String>,
    /// targetNumWorkers property.
    pub target_num_workers: Option<String>,
    /// time property.
    pub time: Option<String>,
    /// workerPool property.
    pub worker_pool: Option<String>,
}

/// `LaunchFlexTemplateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LaunchFlexTemplateResponse {
    /// job property.
    pub job: Option<Job>,
}

/// `WorkItemServiceState` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkItemServiceState {
    /// completeWorkStatus property.
    pub complete_work_status: Option<Status>,
    /// harnessData property.
    pub harness_data: Option<serde_json::Value>,
    /// hotKeyDetection property.
    pub hot_key_detection: Option<HotKeyDetection>,
    /// leaseExpireTime property.
    pub lease_expire_time: Option<String>,
    /// metricShortId property.
    pub metric_short_id: Option<Vec<MetricShortId>>,
    /// nextReportIndex property.
    pub next_report_index: Option<String>,
    /// reportStatusInterval property.
    pub report_status_interval: Option<String>,
    /// splitRequest property.
    pub split_request: Option<ApproximateSplitRequest>,
    /// suggestedStopPoint property.
    pub suggested_stop_point: Option<ApproximateProgress>,
    /// suggestedStopPosition property.
    pub suggested_stop_position: Option<Box<Position>>,
}

/// `DynamicSourceSplit` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DynamicSourceSplit {
    /// primary property.
    pub primary: Option<DerivedSource>,
    /// residual property.
    pub residual: Option<DerivedSource>,
}

/// `JobMessage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobMessage {
    /// id property.
    pub id: Option<String>,
    /// messageImportance property.
    pub message_importance: Option<String>,
    /// messageText property.
    pub message_text: Option<String>,
    /// time property.
    pub time: Option<String>,
}

/// `JobMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobMetrics {
    /// metricTime property.
    pub metric_time: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<MetricUpdate>>,
}

/// `CounterStructuredNameAndMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CounterStructuredNameAndMetadata {
    /// metadata property.
    pub metadata: Option<CounterMetadata>,
    /// name property.
    pub name: Option<CounterStructuredName>,
}

/// `StreamingConfigTask` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingConfigTask {
    /// commitStreamChunkSizeBytes property.
    pub commit_stream_chunk_size_bytes: Option<String>,
    /// getDataStreamChunkSizeBytes property.
    pub get_data_stream_chunk_size_bytes: Option<String>,
    /// maxWorkItemCommitBytes property.
    pub max_work_item_commit_bytes: Option<String>,
    /// operationalLimits property.
    pub operational_limits: Option<StreamingOperationalLimits>,
    /// streamingComputationConfigs property.
    pub streaming_computation_configs: Option<Vec<StreamingComputationConfig>>,
    /// streamingEngineStateTagEncodingVersion property.
    pub streaming_engine_state_tag_encoding_version: Option<i64>,
    /// userStepToStateFamilyNameMap property.
    pub user_step_to_state_family_name_map: Option<serde_json::Value>,
    /// userWorkerRunnerV1Settings property.
    pub user_worker_runner_v1_settings: Option<String>,
    /// userWorkerRunnerV2Settings property.
    pub user_worker_runner_v2_settings: Option<String>,
    /// windmillServiceEndpoint property.
    pub windmill_service_endpoint: Option<String>,
    /// windmillServicePort property.
    pub windmill_service_port: Option<String>,
}

/// `StreamingComputationTask` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingComputationTask {
    /// computationRanges property.
    pub computation_ranges: Option<Vec<StreamingComputationRanges>>,
    /// dataDisks property.
    pub data_disks: Option<Vec<MountedDataDisk>>,
    /// taskType property.
    pub task_type: Option<String>,
}

/// `TemplateMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TemplateMetadata {
    /// defaultStreamingMode property.
    pub default_streaming_mode: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// parameters property.
    pub parameters: Option<Vec<ParameterMetadata>>,
    /// streaming property.
    pub streaming: Option<bool>,
    /// supportsAtLeastOnce property.
    pub supports_at_least_once: Option<bool>,
    /// supportsExactlyOnce property.
    pub supports_exactly_once: Option<bool>,
    /// yamlDefinition property.
    pub yaml_definition: Option<String>,
}

/// `CounterStructuredName` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CounterStructuredName {
    /// componentStepName property.
    pub component_step_name: Option<String>,
    /// executionStepName property.
    pub execution_step_name: Option<String>,
    /// inputIndex property.
    pub input_index: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// origin property.
    pub origin: Option<String>,
    /// originNamespace property.
    pub origin_namespace: Option<String>,
    /// originalRequestingStepName property.
    pub original_requesting_step_name: Option<String>,
    /// originalStepName property.
    pub original_step_name: Option<String>,
    /// portion property.
    pub portion: Option<String>,
    /// workerId property.
    pub worker_id: Option<String>,
}

/// `MapTask` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MapTask {
    /// counterPrefix property.
    pub counter_prefix: Option<String>,
    /// instructions property.
    pub instructions: Option<Vec<ParallelInstruction>>,
    /// stageName property.
    pub stage_name: Option<String>,
    /// systemName property.
    pub system_name: Option<String>,
}

/// `KeyRangeDataDiskAssignment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct KeyRangeDataDiskAssignment {
    /// dataDisk property.
    pub data_disk: Option<String>,
    /// end property.
    pub end: Option<String>,
    /// start property.
    pub start: Option<String>,
}

/// `OutlierStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OutlierStats {
    /// overflowCount property.
    pub overflow_count: Option<String>,
    /// overflowMean property.
    pub overflow_mean: Option<f64>,
    /// underflowCount property.
    pub underflow_count: Option<String>,
    /// underflowMean property.
    pub underflow_mean: Option<f64>,
}

/// `Source` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Source {
    /// baseSpecs property.
    pub base_specs: Option<Vec<serde_json::Value>>,
    /// codec property.
    pub codec: Option<serde_json::Value>,
    /// doesNotNeedSplitting property.
    pub does_not_need_splitting: Option<bool>,
    /// metadata property.
    pub metadata: Option<SourceMetadata>,
    /// spec property.
    pub spec: Option<serde_json::Value>,
}

/// `ContainerSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContainerSpec {
    /// defaultEnvironment property.
    pub default_environment: Option<FlexTemplateRuntimeEnvironment>,
    /// image property.
    pub image: Option<String>,
    /// imageRepositoryCertPath property.
    pub image_repository_cert_path: Option<String>,
    /// imageRepositoryPasswordSecretId property.
    pub image_repository_password_secret_id: Option<String>,
    /// imageRepositoryUsernameSecretId property.
    pub image_repository_username_secret_id: Option<String>,
    /// metadata property.
    pub metadata: Option<TemplateMetadata>,
    /// sdkInfo property.
    pub sdk_info: Option<SDKInfo>,
}

/// `GetDebugConfigResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetDebugConfigResponse {
    /// config property.
    pub config: Option<String>,
}

/// `WorkerHealthReportResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerHealthReportResponse {
    /// reportInterval property.
    pub report_interval: Option<String>,
}

/// `CreateJobFromTemplateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CreateJobFromTemplateRequest {
    /// environment property.
    pub environment: Option<RuntimeEnvironment>,
    /// gcsPath property.
    pub gcs_path: Option<String>,
    /// jobName property.
    pub job_name: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
}

/// `JobExecutionDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobExecutionDetails {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// stages property.
    pub stages: Option<Vec<StageSummary>>,
}

/// `SourceSplitShard` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceSplitShard {
    /// derivationMode property.
    pub derivation_mode: Option<String>,
    /// source property.
    pub source: Option<Source>,
}

/// `WorkerMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerMessageResponse {
    /// streamingScalingReportResponse property.
    pub streaming_scaling_report_response: Option<StreamingScalingReportResponse>,
    /// workerHealthReportResponse property.
    pub worker_health_report_response: Option<WorkerHealthReportResponse>,
    /// workerMetricsResponse property.
    pub worker_metrics_response: Option<ResourceUtilizationReportResponse>,
    /// workerShutdownNoticeResponse property.
    pub worker_shutdown_notice_response: Option<WorkerShutdownNoticeResponse>,
    /// workerThreadScalingReportResponse property.
    pub worker_thread_scaling_report_response: Option<WorkerThreadScalingReportResponse>,
}

/// `DataSamplingConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataSamplingConfig {
    /// behaviors property.
    pub behaviors: Option<Vec<String>>,
}

/// `SdkHarnessContainerImage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SdkHarnessContainerImage {
    /// capabilities property.
    pub capabilities: Option<Vec<String>>,
    /// containerImage property.
    pub container_image: Option<String>,
    /// environmentId property.
    pub environment_id: Option<String>,
    /// useSingleCorePerContainer property.
    pub use_single_core_per_container: Option<bool>,
}

/// `SourceSplitResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceSplitResponse {
    /// bundles property.
    pub bundles: Option<Vec<DerivedSource>>,
    /// outcome property.
    pub outcome: Option<String>,
    /// shards property.
    pub shards: Option<Vec<SourceSplitShard>>,
}

/// `Job` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Job {
    /// clientRequestId property.
    pub client_request_id: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// createdFromSnapshotId property.
    pub created_from_snapshot_id: Option<String>,
    /// currentState property.
    pub current_state: Option<String>,
    /// currentStateTime property.
    pub current_state_time: Option<String>,
    /// environment property.
    pub environment: Option<Environment>,
    /// executionInfo property.
    pub execution_info: Option<JobExecutionInfo>,
    /// id property.
    pub id: Option<String>,
    /// jobMetadata property.
    pub job_metadata: Option<JobMetadata>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// location property.
    pub location: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// pausable property.
    pub pausable: Option<bool>,
    /// pipelineDescription property.
    pub pipeline_description: Option<PipelineDescription>,
    /// projectId property.
    pub project_id: Option<String>,
    /// replaceJobId property.
    pub replace_job_id: Option<String>,
    /// replacedByJobId property.
    pub replaced_by_job_id: Option<String>,
    /// requestedState property.
    pub requested_state: Option<String>,
    /// runtimeUpdatableParams property.
    pub runtime_updatable_params: Option<RuntimeUpdatableParams>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// serviceResources property.
    pub service_resources: Option<ServiceResources>,
    /// stageStates property.
    pub stage_states: Option<Vec<ExecutionStageState>>,
    /// startTime property.
    pub start_time: Option<String>,
    /// steps property.
    pub steps: Option<Vec<Step>>,
    /// stepsLocation property.
    pub steps_location: Option<String>,
    /// tempFiles property.
    pub temp_files: Option<Vec<String>>,
    /// transformNameMapping property.
    pub transform_name_mapping: Option<serde_json::Value>,
    /// type property.
    pub r#type: Option<String>,
}

/// `StateFamilyConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StateFamilyConfig {
    /// isRead property.
    pub is_read: Option<bool>,
    /// stateFamily property.
    pub state_family: Option<String>,
}

/// `RuntimeEnvironment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RuntimeEnvironment {
    /// additionalExperiments property.
    pub additional_experiments: Option<Vec<String>>,
    /// additionalPipelineOptions property.
    pub additional_pipeline_options: Option<Vec<String>>,
    /// additionalUserLabels property.
    pub additional_user_labels: Option<serde_json::Value>,
    /// bypassTempDirValidation property.
    pub bypass_temp_dir_validation: Option<bool>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<i64>,
    /// enableStreamingEngine property.
    pub enable_streaming_engine: Option<bool>,
    /// ipConfiguration property.
    pub ip_configuration: Option<String>,
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// maxWorkers property.
    pub max_workers: Option<i64>,
    /// network property.
    pub network: Option<String>,
    /// numWorkers property.
    pub num_workers: Option<i64>,
    /// serviceAccountEmail property.
    pub service_account_email: Option<String>,
    /// streamingMode property.
    pub streaming_mode: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// tempLocation property.
    pub temp_location: Option<String>,
    /// workerRegion property.
    pub worker_region: Option<String>,
    /// workerZone property.
    pub worker_zone: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `WorkerLifecycleEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerLifecycleEvent {
    /// containerStartTime property.
    pub container_start_time: Option<String>,
    /// event property.
    pub event: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
}

/// `FlattenInstruction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlattenInstruction {
    /// inputs property.
    pub inputs: Option<Vec<InstructionInput>>,
}

/// `IntegerMean` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntegerMean {
    /// count property.
    pub count: Option<SplitInt64>,
    /// sum property.
    pub sum: Option<SplitInt64>,
}

/// `WorkerMessageCode` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerMessageCode {
    /// code property.
    pub code: Option<String>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
}

/// `BoundedTrie` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BoundedTrie {
    /// bound property.
    pub bound: Option<i64>,
    /// root property.
    pub root: Option<BoundedTrieNode>,
    /// singleton property.
    pub singleton: Option<Vec<String>>,
}

/// `SourceFork` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceFork {
    /// primary property.
    pub primary: Option<SourceSplitShard>,
    /// primarySource property.
    pub primary_source: Option<DerivedSource>,
    /// residual property.
    pub residual: Option<SourceSplitShard>,
    /// residualSource property.
    pub residual_source: Option<DerivedSource>,
}

/// `PipelineDescription` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PipelineDescription {
    /// displayData property.
    pub display_data: Option<Vec<DisplayData>>,
    /// executionPipelineStage property.
    pub execution_pipeline_stage: Option<Vec<ExecutionStageSummary>>,
    /// originalPipelineTransform property.
    pub original_pipeline_transform: Option<Vec<TransformSummary>>,
    /// stepNamesHash property.
    pub step_names_hash: Option<String>,
}

/// `WorkerSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerSettings {
    /// baseUrl property.
    pub base_url: Option<String>,
    /// reportingEnabled property.
    pub reporting_enabled: Option<bool>,
    /// servicePath property.
    pub service_path: Option<String>,
    /// shuffleServicePath property.
    pub shuffle_service_path: Option<String>,
    /// tempStoragePrefix property.
    pub temp_storage_prefix: Option<String>,
    /// workerId property.
    pub worker_id: Option<String>,
}

/// `ReportWorkItemStatusRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportWorkItemStatusRequest {
    /// currentWorkerTime property.
    pub current_worker_time: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// projectNumber property.
    pub project_number: Option<String>,
    /// unifiedWorkerRequest property.
    pub unified_worker_request: Option<serde_json::Value>,
    /// workItemStatuses property.
    pub work_item_statuses: Option<Vec<WorkItemStatus>>,
    /// workerId property.
    pub worker_id: Option<String>,
}

/// `StreamingComputationRanges` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingComputationRanges {
    /// computationId property.
    pub computation_id: Option<String>,
    /// rangeAssignments property.
    pub range_assignments: Option<Vec<KeyRangeDataDiskAssignment>>,
}

/// `SendDebugCaptureResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SendDebugCaptureResponse {}

/// `Linear` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Linear {
    /// numberOfBuckets property.
    pub number_of_buckets: Option<i64>,
    /// start property.
    pub start: Option<f64>,
    /// width property.
    pub width: Option<f64>,
}

/// `HotKeyDetection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HotKeyDetection {
    /// hotKeyAge property.
    pub hot_key_age: Option<String>,
    /// systemName property.
    pub system_name: Option<String>,
    /// userStepName property.
    pub user_step_name: Option<String>,
}

/// `WorkerShutdownNoticeResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerShutdownNoticeResponse {}

/// `CustomSourceLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomSourceLocation {
    /// stateful property.
    pub stateful: Option<bool>,
}

/// `StreamingScalingReportResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingScalingReportResponse {
    /// maximumThreadCount property.
    pub maximum_thread_count: Option<i64>,
}

/// `Stack` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Stack {
    /// stackContent property.
    pub stack_content: Option<String>,
    /// threadCount property.
    pub thread_count: Option<i64>,
    /// threadName property.
    pub thread_name: Option<String>,
    /// threadState property.
    pub thread_state: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
}

/// `LaunchFlexTemplateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LaunchFlexTemplateRequest {
    /// launchParameter property.
    pub launch_parameter: Option<LaunchFlexTemplateParameter>,
    /// validateOnly property.
    pub validate_only: Option<bool>,
}

/// `WorkItemStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkItemStatus {
    /// completed property.
    pub completed: Option<bool>,
    /// counterUpdates property.
    pub counter_updates: Option<Vec<CounterUpdate>>,
    /// dynamicSourceSplit property.
    pub dynamic_source_split: Option<DynamicSourceSplit>,
    /// errors property.
    pub errors: Option<Vec<Status>>,
    /// metricUpdates property.
    pub metric_updates: Option<Vec<MetricUpdate>>,
    /// progress property.
    pub progress: Option<ApproximateProgress>,
    /// reportIndex property.
    pub report_index: Option<String>,
    /// reportedProgress property.
    pub reported_progress: Option<ApproximateReportedProgress>,
    /// requestedLeaseDuration property.
    pub requested_lease_duration: Option<String>,
    /// sourceFork property.
    pub source_fork: Option<SourceFork>,
    /// sourceOperationResponse property.
    pub source_operation_response: Option<SourceOperationResponse>,
    /// stopPosition property.
    pub stop_position: Option<Box<Position>>,
    /// totalThrottlerWaitTimeSeconds property.
    pub total_throttler_wait_time_seconds: Option<f64>,
    /// workItemId property.
    pub work_item_id: Option<String>,
}

/// `Parameter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Parameter {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<serde_json::Value>,
}

/// `SourceSplitOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceSplitOptions {
    /// desiredBundleSizeBytes property.
    pub desired_bundle_size_bytes: Option<String>,
    /// desiredShardSizeBytes property.
    pub desired_shard_size_bytes: Option<String>,
}

/// `SplitInt64` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SplitInt64 {
    /// highBits property.
    pub high_bits: Option<i64>,
    /// lowBits property.
    pub low_bits: Option<i64>,
}

/// `WorkerThreadScalingReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerThreadScalingReport {
    /// currentThreadCount property.
    pub current_thread_count: Option<i64>,
}

/// `GPUUsage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GPUUsage {
    /// timestamp property.
    pub timestamp: Option<String>,
    /// utilization property.
    pub utilization: Option<GPUUtilization>,
}

/// `Step` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Step {
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
}

/// `ResourceUtilizationReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceUtilizationReport {
    /// containers property.
    pub containers: Option<serde_json::Value>,
    /// cpuTime property.
    pub cpu_time: Option<Vec<CPUTime>>,
    /// gpuUsage property.
    pub gpu_usage: Option<Vec<GPUUsage>>,
    /// memoryInfo property.
    pub memory_info: Option<Vec<MemInfo>>,
}

/// `DataflowGaugeValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataflowGaugeValue {
    /// measuredTime property.
    pub measured_time: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `LaunchFlexTemplateParameter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LaunchFlexTemplateParameter {
    /// containerSpec property.
    pub container_spec: Option<ContainerSpec>,
    /// containerSpecGcsPath property.
    pub container_spec_gcs_path: Option<String>,
    /// environment property.
    pub environment: Option<FlexTemplateRuntimeEnvironment>,
    /// jobName property.
    pub job_name: Option<String>,
    /// launchOptions property.
    pub launch_options: Option<serde_json::Value>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
    /// transformNameMappings property.
    pub transform_name_mappings: Option<serde_json::Value>,
    /// update property.
    pub update: Option<bool>,
}

/// `SourceGetMetadataResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceGetMetadataResponse {
    /// metadata property.
    pub metadata: Option<SourceMetadata>,
}

/// `IntegerList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntegerList {
    /// elements property.
    pub elements: Option<Vec<SplitInt64>>,
}

/// `FailedLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FailedLocation {
    /// name property.
    pub name: Option<String>,
}

/// `StreamingStragglerInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingStragglerInfo {
    /// dataWatermarkLag property.
    pub data_watermark_lag: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// systemWatermarkLag property.
    pub system_watermark_lag: Option<String>,
    /// workerName property.
    pub worker_name: Option<String>,
}

/// `ApproximateProgress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApproximateProgress {
    /// percentComplete property.
    pub percent_complete: Option<f64>,
    /// position property.
    pub position: Option<Box<Position>>,
    /// remainingTime property.
    pub remaining_time: Option<String>,
}

/// `StreamingStageLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingStageLocation {
    /// streamId property.
    pub stream_id: Option<String>,
}

/// `DataSamplingReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataSamplingReport {
    /// bytesWrittenDelta property.
    pub bytes_written_delta: Option<String>,
    /// elementsSampledBytes property.
    pub elements_sampled_bytes: Option<String>,
    /// elementsSampledCount property.
    pub elements_sampled_count: Option<String>,
    /// exceptionsSampledCount property.
    pub exceptions_sampled_count: Option<String>,
    /// pcollectionsSampledCount property.
    pub pcollections_sampled_count: Option<String>,
    /// persistenceErrorsCount property.
    pub persistence_errors_count: Option<String>,
    /// translationErrorsCount property.
    pub translation_errors_count: Option<String>,
}

/// `LeaseWorkItemRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LeaseWorkItemRequest {
    /// currentWorkerTime property.
    pub current_worker_time: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// projectNumber property.
    pub project_number: Option<String>,
    /// requestedLeaseDuration property.
    pub requested_lease_duration: Option<String>,
    /// unifiedWorkerRequest property.
    pub unified_worker_request: Option<serde_json::Value>,
    /// workItemTypes property.
    pub work_item_types: Option<Vec<String>>,
    /// workerCapabilities property.
    pub worker_capabilities: Option<Vec<String>>,
    /// workerId property.
    pub worker_id: Option<String>,
}

/// `SeqMapTask` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SeqMapTask {
    /// inputs property.
    pub inputs: Option<Vec<SideInputInfo>>,
    /// name property.
    pub name: Option<String>,
    /// outputInfos property.
    pub output_infos: Option<Vec<SeqMapTaskOutputInfo>>,
    /// stageName property.
    pub stage_name: Option<String>,
    /// systemName property.
    pub system_name: Option<String>,
    /// userFn property.
    pub user_fn: Option<serde_json::Value>,
}

/// `LaunchTemplateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LaunchTemplateResponse {
    /// job property.
    pub job: Option<Job>,
}

/// `PartialGroupByKeyInstruction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PartialGroupByKeyInstruction {
    /// input property.
    pub input: Option<InstructionInput>,
    /// inputElementCodec property.
    pub input_element_codec: Option<serde_json::Value>,
    /// originalCombineValuesInputStoreName property.
    pub original_combine_values_input_store_name: Option<String>,
    /// originalCombineValuesStepName property.
    pub original_combine_values_step_name: Option<String>,
    /// sideInputs property.
    pub side_inputs: Option<Vec<SideInputInfo>>,
    /// valueCombiningFn property.
    pub value_combining_fn: Option<serde_json::Value>,
}

/// `Histogram` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Histogram {
    /// bucketCounts property.
    pub bucket_counts: Option<Vec<String>>,
    /// firstBucketOffset property.
    pub first_bucket_offset: Option<i64>,
}

/// `DisplayData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DisplayData {
    /// boolValue property.
    pub bool_value: Option<bool>,
    /// durationValue property.
    pub duration_value: Option<String>,
    /// floatValue property.
    pub float_value: Option<f64>,
    /// int64Value property.
    pub int64_value: Option<String>,
    /// javaClassValue property.
    pub java_class_value: Option<String>,
    /// key property.
    pub key: Option<String>,
    /// label property.
    pub label: Option<String>,
    /// namespace property.
    pub namespace: Option<String>,
    /// shortStrValue property.
    pub short_str_value: Option<String>,
    /// strValue property.
    pub str_value: Option<String>,
    /// timestampValue property.
    pub timestamp_value: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `RuntimeUpdatableParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RuntimeUpdatableParams {
    /// acceptableBacklogDuration property.
    pub acceptable_backlog_duration: Option<String>,
    /// autoscalingTier property.
    pub autoscaling_tier: Option<String>,
    /// latencyTier property.
    pub latency_tier: Option<String>,
    /// maxNumWorkers property.
    pub max_num_workers: Option<i64>,
    /// minNumWorkers property.
    pub min_num_workers: Option<i64>,
    /// workerUtilizationHint property.
    pub worker_utilization_hint: Option<f64>,
}

/// `ComponentTransform` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComponentTransform {
    /// name property.
    pub name: Option<String>,
    /// originalTransform property.
    pub original_transform: Option<String>,
    /// userName property.
    pub user_name: Option<String>,
}

/// `StructuredMessage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StructuredMessage {
    /// messageKey property.
    pub message_key: Option<String>,
    /// messageText property.
    pub message_text: Option<String>,
    /// parameters property.
    pub parameters: Option<Vec<Parameter>>,
}

/// `StragglerSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StragglerSummary {
    /// recentStragglers property.
    pub recent_stragglers: Option<Vec<Straggler>>,
    /// stragglerCauseCount property.
    pub straggler_cause_count: Option<serde_json::Value>,
    /// totalStragglerCount property.
    pub total_straggler_count: Option<String>,
}

/// `CPUTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CPUTime {
    /// rate property.
    pub rate: Option<f64>,
    /// timestamp property.
    pub timestamp: Option<String>,
    /// totalMs property.
    pub total_ms: Option<String>,
}

/// `ReportWorkItemStatusResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportWorkItemStatusResponse {
    /// unifiedWorkerResponse property.
    pub unified_worker_response: Option<serde_json::Value>,
    /// workItemServiceStates property.
    pub work_item_service_states: Option<Vec<WorkItemServiceState>>,
}

/// `ParDoInstruction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ParDoInstruction {
    /// input property.
    pub input: Option<InstructionInput>,
    /// multiOutputInfos property.
    pub multi_output_infos: Option<Vec<MultiOutputInfo>>,
    /// numOutputs property.
    pub num_outputs: Option<i64>,
    /// sideInputs property.
    pub side_inputs: Option<Vec<SideInputInfo>>,
    /// userFn property.
    pub user_fn: Option<serde_json::Value>,
}

/// `CounterMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CounterMetadata {
    /// description property.
    pub description: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// otherUnits property.
    pub other_units: Option<String>,
    /// standardUnits property.
    pub standard_units: Option<String>,
}

/// `SDKInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SDKInfo {
    /// language property.
    pub language: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `GetTemplateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetTemplateResponse {
    /// metadata property.
    pub metadata: Option<TemplateMetadata>,
    /// runtimeMetadata property.
    pub runtime_metadata: Option<RuntimeMetadata>,
    /// status property.
    pub status: Option<Status>,
    /// templateType property.
    pub template_type: Option<String>,
}

/// `WorkerDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerDetails {
    /// workItems property.
    pub work_items: Option<Vec<WorkItemDetails>>,
    /// workerName property.
    pub worker_name: Option<String>,
}

/// `Sink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Sink {
    /// codec property.
    pub codec: Option<serde_json::Value>,
    /// spec property.
    pub spec: Option<serde_json::Value>,
}

/// `SourceOperationResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceOperationResponse {
    /// getMetadata property.
    pub get_metadata: Option<SourceGetMetadataResponse>,
    /// split property.
    pub split: Option<SourceSplitResponse>,
}

/// `JobMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobMetadata {
    /// bigTableDetails property.
    pub big_table_details: Option<Vec<BigTableIODetails>>,
    /// bigqueryDetails property.
    pub bigquery_details: Option<Vec<BigQueryIODetails>>,
    /// datastoreDetails property.
    pub datastore_details: Option<Vec<DatastoreIODetails>>,
    /// fileDetails property.
    pub file_details: Option<Vec<FileIODetails>>,
    /// pubsubDetails property.
    pub pubsub_details: Option<Vec<PubSubIODetails>>,
    /// sdkVersion property.
    pub sdk_version: Option<SdkVersion>,
    /// spannerDetails property.
    pub spanner_details: Option<Vec<SpannerIODetails>>,
    /// userDisplayProperties property.
    pub user_display_properties: Option<serde_json::Value>,
}

/// `StragglerInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StragglerInfo {
    /// causes property.
    pub causes: Option<serde_json::Value>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `CounterUpdate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CounterUpdate {
    /// boolean property.
    pub boolean: Option<bool>,
    /// boundedTrie property.
    pub bounded_trie: Option<BoundedTrie>,
    /// cumulative property.
    pub cumulative: Option<bool>,
    /// distribution property.
    pub distribution: Option<DistributionUpdate>,
    /// floatingPoint property.
    pub floating_point: Option<f64>,
    /// floatingPointList property.
    pub floating_point_list: Option<FloatingPointList>,
    /// floatingPointMean property.
    pub floating_point_mean: Option<FloatingPointMean>,
    /// integer property.
    pub integer: Option<SplitInt64>,
    /// integerGauge property.
    pub integer_gauge: Option<IntegerGauge>,
    /// integerList property.
    pub integer_list: Option<IntegerList>,
    /// integerMean property.
    pub integer_mean: Option<IntegerMean>,
    /// internal property.
    pub internal: Option<serde_json::Value>,
    /// nameAndKind property.
    pub name_and_kind: Option<NameAndKind>,
    /// shortId property.
    pub short_id: Option<String>,
    /// stringList property.
    pub string_list: Option<StringList>,
    /// structuredNameAndMetadata property.
    pub structured_name_and_metadata: Option<CounterStructuredNameAndMetadata>,
}

/// `StreamingOperationalLimits` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingOperationalLimits {
    /// maxBagElementBytes property.
    pub max_bag_element_bytes: Option<String>,
    /// maxGlobalDataBytes property.
    pub max_global_data_bytes: Option<String>,
    /// maxKeyBytes property.
    pub max_key_bytes: Option<String>,
    /// maxProductionOutputBytes property.
    pub max_production_output_bytes: Option<String>,
    /// maxSortedListElementBytes property.
    pub max_sorted_list_element_bytes: Option<String>,
    /// maxSourceStateBytes property.
    pub max_source_state_bytes: Option<String>,
    /// maxTagBytes property.
    pub max_tag_bytes: Option<String>,
    /// maxValueBytes property.
    pub max_value_bytes: Option<String>,
}

/// `WorkerThreadScalingReportResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerThreadScalingReportResponse {
    /// recommendedThreadCount property.
    pub recommended_thread_count: Option<i64>,
}

/// `SeqMapTaskOutputInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SeqMapTaskOutputInfo {
    /// sink property.
    pub sink: Option<Sink>,
    /// tag property.
    pub tag: Option<String>,
}

/// `SourceGetMetadataRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceGetMetadataRequest {
    /// source property.
    pub source: Option<Source>,
}

/// `ReportedParallelism` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportedParallelism {
    /// isInfinite property.
    pub is_infinite: Option<bool>,
    /// value property.
    pub value: Option<f64>,
}

/// `ApproximateSplitRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApproximateSplitRequest {
    /// fractionConsumed property.
    pub fraction_consumed: Option<f64>,
    /// fractionOfRemainder property.
    pub fraction_of_remainder: Option<f64>,
    /// position property.
    pub position: Option<Box<Position>>,
}

/// `RuntimeMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RuntimeMetadata {
    /// parameters property.
    pub parameters: Option<Vec<ParameterMetadata>>,
    /// sdkInfo property.
    pub sdk_info: Option<SDKInfo>,
}

/// `SourceOperationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceOperationRequest {
    /// getMetadata property.
    pub get_metadata: Option<SourceGetMetadataRequest>,
    /// name property.
    pub name: Option<String>,
    /// originalName property.
    pub original_name: Option<String>,
    /// split property.
    pub split: Option<SourceSplitRequest>,
    /// stageName property.
    pub stage_name: Option<String>,
    /// systemName property.
    pub system_name: Option<String>,
}

/// `StageSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StageSummary {
    /// endTime property.
    pub end_time: Option<String>,
    /// metrics property.
    pub metrics: Option<Vec<MetricUpdate>>,
    /// progress property.
    pub progress: Option<ProgressTimeseries>,
    /// stageId property.
    pub stage_id: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// stragglerSummary property.
    pub straggler_summary: Option<StragglerSummary>,
}

/// `SdkVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SdkVersion {
    /// bugs property.
    pub bugs: Option<Vec<SdkBug>>,
    /// sdkSupportStatus property.
    pub sdk_support_status: Option<String>,
    /// version property.
    pub version: Option<String>,
    /// versionDisplayName property.
    pub version_display_name: Option<String>,
}

/// `WorkerPool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerPool {
    /// autoscalingSettings property.
    pub autoscaling_settings: Option<AutoscalingSettings>,
    /// dataDisks property.
    pub data_disks: Option<Vec<Disk>>,
    /// defaultPackageSet property.
    pub default_package_set: Option<String>,
    /// diskProvisionedIops property.
    pub disk_provisioned_iops: Option<String>,
    /// diskProvisionedThroughputMibps property.
    pub disk_provisioned_throughput_mibps: Option<String>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<i64>,
    /// diskSourceImage property.
    pub disk_source_image: Option<String>,
    /// diskType property.
    pub disk_type: Option<String>,
    /// ipConfiguration property.
    pub ip_configuration: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// network property.
    pub network: Option<String>,
    /// numThreadsPerWorker property.
    pub num_threads_per_worker: Option<i64>,
    /// numWorkers property.
    pub num_workers: Option<i64>,
    /// onHostMaintenance property.
    pub on_host_maintenance: Option<String>,
    /// packages property.
    pub packages: Option<Vec<Package>>,
    /// poolArgs property.
    pub pool_args: Option<serde_json::Value>,
    /// sdkHarnessContainerImages property.
    pub sdk_harness_container_images: Option<Vec<SdkHarnessContainerImage>>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// taskrunnerSettings property.
    pub taskrunner_settings: Option<TaskRunnerSettings>,
    /// teardownPolicy property.
    pub teardown_policy: Option<String>,
    /// workerHarnessContainerImage property.
    pub worker_harness_container_image: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `Sdk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Sdk {
    /// sdkId property.
    pub sdk_id: Option<String>,
    /// stacks property.
    pub stacks: Option<Vec<Stack>>,
}

/// `GetDebugConfigRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetDebugConfigRequest {
    /// componentId property.
    pub component_id: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// workerId property.
    pub worker_id: Option<String>,
}

/// `LeaseWorkItemResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LeaseWorkItemResponse {
    /// unifiedWorkerResponse property.
    pub unified_worker_response: Option<serde_json::Value>,
    /// workItems property.
    pub work_items: Option<Vec<WorkItem>>,
}

/// `ConcatPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConcatPosition {
    /// index property.
    pub index: Option<i64>,
    /// position property.
    pub position: Option<Box<Position>>,
}

/// `ComputationTopology` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComputationTopology {
    /// computationId property.
    pub computation_id: Option<String>,
    /// inputs property.
    pub inputs: Option<Vec<StreamLocation>>,
    /// keyRanges property.
    pub key_ranges: Option<Vec<KeyRangeLocation>>,
    /// outputs property.
    pub outputs: Option<Vec<StreamLocation>>,
    /// stateFamilies property.
    pub state_families: Option<Vec<StateFamilyConfig>>,
    /// systemStageName property.
    pub system_stage_name: Option<String>,
}

/// `WorkerMessage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerMessage {
    /// dataSamplingReport property.
    pub data_sampling_report: Option<DataSamplingReport>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// perWorkerMetrics property.
    pub per_worker_metrics: Option<PerWorkerMetrics>,
    /// streamingScalingReport property.
    pub streaming_scaling_report: Option<StreamingScalingReport>,
    /// time property.
    pub time: Option<String>,
    /// workerHealthReport property.
    pub worker_health_report: Option<WorkerHealthReport>,
    /// workerLifecycleEvent property.
    pub worker_lifecycle_event: Option<WorkerLifecycleEvent>,
    /// workerMessageCode property.
    pub worker_message_code: Option<WorkerMessageCode>,
    /// workerMetrics property.
    pub worker_metrics: Option<ResourceUtilizationReport>,
    /// workerShutdownNotice property.
    pub worker_shutdown_notice: Option<WorkerShutdownNotice>,
    /// workerThreadScalingReport property.
    pub worker_thread_scaling_report: Option<WorkerThreadScalingReport>,
}

/// `WorkerShutdownNotice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkerShutdownNotice {
    /// reason property.
    pub reason: Option<String>,
}

/// `Environment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Environment {
    /// clusterManagerApiService property.
    pub cluster_manager_api_service: Option<String>,
    /// dataset property.
    pub dataset: Option<String>,
    /// debugOptions property.
    pub debug_options: Option<DebugOptions>,
    /// experiments property.
    pub experiments: Option<Vec<String>>,
    /// flexResourceSchedulingGoal property.
    pub flex_resource_scheduling_goal: Option<String>,
    /// internalExperiments property.
    pub internal_experiments: Option<serde_json::Value>,
    /// sdkPipelineOptions property.
    pub sdk_pipeline_options: Option<serde_json::Value>,
    /// serviceAccountEmail property.
    pub service_account_email: Option<String>,
    /// serviceKmsKeyName property.
    pub service_kms_key_name: Option<String>,
    /// serviceOptions property.
    pub service_options: Option<Vec<String>>,
    /// shuffleMode property.
    pub shuffle_mode: Option<String>,
    /// streamingMode property.
    pub streaming_mode: Option<String>,
    /// tempStoragePrefix property.
    pub temp_storage_prefix: Option<String>,
    /// usePublicIps property.
    pub use_public_ips: Option<bool>,
    /// useStreamingEngineResourceBasedBilling property.
    pub use_streaming_engine_resource_based_billing: Option<bool>,
    /// userAgent property.
    pub user_agent: Option<serde_json::Value>,
    /// version property.
    pub version: Option<serde_json::Value>,
    /// workerPools property.
    pub worker_pools: Option<Vec<WorkerPool>>,
    /// workerRegion property.
    pub worker_region: Option<String>,
    /// workerZone property.
    pub worker_zone: Option<String>,
}

/// `SendWorkerMessagesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SendWorkerMessagesResponse {
    /// workerMessageResponses property.
    pub worker_message_responses: Option<Vec<WorkerMessageResponse>>,
}

/// `StreamingSideInputLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingSideInputLocation {
    /// stateFamily property.
    pub state_family: Option<String>,
    /// tag property.
    pub tag: Option<String>,
}

/// `ExecutionStageState` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExecutionStageState {
    /// currentStateTime property.
    pub current_state_time: Option<String>,
    /// executionStageName property.
    pub execution_stage_name: Option<String>,
    /// executionStageState property.
    pub execution_stage_state: Option<String>,
}

/// `StreamingComputationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingComputationConfig {
    /// computationId property.
    pub computation_id: Option<String>,
    /// instructions property.
    pub instructions: Option<Vec<ParallelInstruction>>,
    /// stageName property.
    pub stage_name: Option<String>,
    /// systemName property.
    pub system_name: Option<String>,
    /// transformUserNameToStateFamily property.
    pub transform_user_name_to_state_family: Option<serde_json::Value>,
}

/// `BigTableIODetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BigTableIODetails {
    /// instanceId property.
    pub instance_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// tableId property.
    pub table_id: Option<String>,
}

/// `Disk` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Disk {
    /// diskType property.
    pub disk_type: Option<String>,
    /// mountPoint property.
    pub mount_point: Option<String>,
    /// sizeGb property.
    pub size_gb: Option<i64>,
}

/// `DataDiskAssignment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataDiskAssignment {
    /// dataDisks property.
    pub data_disks: Option<Vec<String>>,
    /// vmInstance property.
    pub vm_instance: Option<String>,
}

/// `ShellTask` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ShellTask {
    /// command property.
    pub command: Option<String>,
    /// exitCode property.
    pub exit_code: Option<i64>,
}

/// `JobExecutionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JobExecutionInfo {
    /// stages property.
    pub stages: Option<serde_json::Value>,
}

/// `ApproximateReportedProgress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApproximateReportedProgress {
    /// consumedParallelism property.
    pub consumed_parallelism: Option<ReportedParallelism>,
    /// fractionConsumed property.
    pub fraction_consumed: Option<f64>,
    /// position property.
    pub position: Option<Box<Position>>,
    /// remainingParallelism property.
    pub remaining_parallelism: Option<ReportedParallelism>,
}

/// `ServiceResources` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServiceResources {
    /// zones property.
    pub zones: Option<Vec<String>>,
}

/// `SnapshotJobRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SnapshotJobRequest {
    /// description property.
    pub description: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// snapshotSources property.
    pub snapshot_sources: Option<bool>,
    /// ttl property.
    pub ttl: Option<String>,
}

/// `SendDebugCaptureRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SendDebugCaptureRequest {
    /// componentId property.
    pub component_id: Option<String>,
    /// data property.
    pub data: Option<String>,
    /// dataFormat property.
    pub data_format: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// workerId property.
    pub worker_id: Option<String>,
}

/// `StreamingSetupTask` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamingSetupTask {
    /// drain property.
    pub drain: Option<bool>,
    /// receiveWorkPort property.
    pub receive_work_port: Option<i64>,
    /// snapshotConfig property.
    pub snapshot_config: Option<StreamingApplianceSnapshotConfig>,
    /// streamingComputationTopology property.
    pub streaming_computation_topology: Option<TopologyConfig>,
    /// workerHarnessPort property.
    pub worker_harness_port: Option<i64>,
}

/// `StreamLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamLocation {
    /// customSourceLocation property.
    pub custom_source_location: Option<CustomSourceLocation>,
    /// pubsubLocation property.
    pub pubsub_location: Option<PubsubLocation>,
    /// sideInputLocation property.
    pub side_input_location: Option<StreamingSideInputLocation>,
    /// streamingStageLocation property.
    pub streaming_stage_location: Option<StreamingStageLocation>,
}

/// `Point` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Point {
    /// time property.
    pub time: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `FlexTemplateRuntimeEnvironment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlexTemplateRuntimeEnvironment {
    /// additionalExperiments property.
    pub additional_experiments: Option<Vec<String>>,
    /// additionalPipelineOptions property.
    pub additional_pipeline_options: Option<Vec<String>>,
    /// additionalUserLabels property.
    pub additional_user_labels: Option<serde_json::Value>,
    /// autoscalingAlgorithm property.
    pub autoscaling_algorithm: Option<String>,
    /// diskSizeGb property.
    pub disk_size_gb: Option<i64>,
    /// dumpHeapOnOom property.
    pub dump_heap_on_oom: Option<bool>,
    /// enableLauncherVmSerialPortLogging property.
    pub enable_launcher_vm_serial_port_logging: Option<bool>,
    /// enableStreamingEngine property.
    pub enable_streaming_engine: Option<bool>,
    /// flexrsGoal property.
    pub flexrs_goal: Option<String>,
    /// ipConfiguration property.
    pub ip_configuration: Option<String>,
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
    /// launcherMachineType property.
    pub launcher_machine_type: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// maxWorkers property.
    pub max_workers: Option<i64>,
    /// network property.
    pub network: Option<String>,
    /// numWorkers property.
    pub num_workers: Option<i64>,
    /// saveHeapDumpsToGcsPath property.
    pub save_heap_dumps_to_gcs_path: Option<String>,
    /// sdkContainerImage property.
    pub sdk_container_image: Option<String>,
    /// serviceAccountEmail property.
    pub service_account_email: Option<String>,
    /// stagingLocation property.
    pub staging_location: Option<String>,
    /// streamingMode property.
    pub streaming_mode: Option<String>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
    /// tempLocation property.
    pub temp_location: Option<String>,
    /// workerRegion property.
    pub worker_region: Option<String>,
    /// workerZone property.
    pub worker_zone: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `GetWorkerStacktracesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetWorkerStacktracesResponse {
    /// sdks property.
    pub sdks: Option<Vec<Sdk>>,
}

/// `ListJobMessagesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListJobMessagesResponse {
    /// autoscalingEvents property.
    pub autoscaling_events: Option<Vec<AutoscalingEvent>>,
    /// jobMessages property.
    pub job_messages: Option<Vec<JobMessage>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `MultiOutputInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MultiOutputInfo {
    /// tag property.
    pub tag: Option<String>,
}

/// `ParallelInstruction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ParallelInstruction {
    /// flatten property.
    pub flatten: Option<FlattenInstruction>,
    /// name property.
    pub name: Option<String>,
    /// originalName property.
    pub original_name: Option<String>,
    /// outputs property.
    pub outputs: Option<Vec<InstructionOutput>>,
    /// parDo property.
    pub par_do: Option<ParDoInstruction>,
    /// partialGroupByKey property.
    pub partial_group_by_key: Option<PartialGroupByKeyInstruction>,
    /// read property.
    pub read: Option<ReadInstruction>,
    /// systemName property.
    pub system_name: Option<String>,
    /// write property.
    pub write: Option<WriteInstruction>,
}

/// `ReadInstruction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReadInstruction {
    /// source property.
    pub source: Option<Source>,
}

/// `PerWorkerMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PerWorkerMetrics {
    /// perStepNamespaceMetrics property.
    pub per_step_namespace_metrics: Option<Vec<PerStepNamespaceMetrics>>,
}

/// `PerStepNamespaceMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PerStepNamespaceMetrics {
    /// metricValues property.
    pub metric_values: Option<Vec<MetricValue>>,
    /// metricsNamespace property.
    pub metrics_namespace: Option<String>,
    /// originalStep property.
    pub original_step: Option<String>,
}

/// `ParameterMetadataEnumOption` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ParameterMetadataEnumOption {
    /// description property.
    pub description: Option<String>,
    /// label property.
    pub label: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `SpannerIODetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpannerIODetails {
    /// databaseId property.
    pub database_id: Option<String>,
    /// instanceId property.
    pub instance_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dataflow.projects.deleteSnapshots_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsDeleteSnapshotsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `snapshotId`.
    pub snapshot_id: Option<String>,
}

/// Arguments for [`dataflow.projects.workerMessages_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsWorkerMessagesArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Request body.
    pub body: SendWorkerMessagesRequest,
}

/// Arguments for [`dataflow.projects.jobs.aggregated_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsAggregatedArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`dataflow.projects.jobs.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `replaceJobId`.
    pub replace_job_id: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
    /// Request body.
    pub body: Job,
}

/// Arguments for [`dataflow.projects.jobs.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`dataflow.projects.jobs.getMetrics_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsGetMetricsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `startTime`.
    pub start_time: Option<String>,
}

/// Arguments for [`dataflow.projects.jobs.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`dataflow.projects.jobs.snapshot_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsSnapshotArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: SnapshotJobRequest,
}

/// Arguments for [`dataflow.projects.jobs.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsUpdateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Job,
}

/// Arguments for [`dataflow.projects.jobs.debug.getConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsDebugGetConfigArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: GetDebugConfigRequest,
}

/// Arguments for [`dataflow.projects.jobs.debug.sendCapture_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsDebugSendCaptureArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: SendDebugCaptureRequest,
}

/// Arguments for [`dataflow.projects.jobs.messages.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsMessagesListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `endTime`.
    pub end_time: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `minimumImportance`.
    pub minimum_importance: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `startTime`.
    pub start_time: Option<String>,
}

/// Arguments for [`dataflow.projects.jobs.workItems.lease_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsWorkItemsLeaseArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: LeaseWorkItemRequest,
}

/// Arguments for [`dataflow.projects.jobs.workItems.reportStatus_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsJobsWorkItemsReportStatusArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: ReportWorkItemStatusRequest,
}

/// Arguments for [`dataflow.projects.locations.workerMessages_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsWorkerMessagesArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Request body.
    pub body: SendWorkerMessagesRequest,
}

/// Arguments for [`dataflow.projects.locations.flexTemplates.launch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsFlexTemplatesLaunchArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Request body.
    pub body: LaunchFlexTemplateRequest,
}

/// Arguments for [`dataflow.projects.locations.jobs.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Query parameter: `replaceJobId`.
    pub replace_job_id: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
    /// Request body.
    pub body: Job,
}

/// Arguments for [`dataflow.projects.locations.jobs.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`dataflow.projects.locations.jobs.getExecutionDetails_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsGetExecutionDetailsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataflow.projects.locations.jobs.getMetrics_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsGetMetricsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `startTime`.
    pub start_time: Option<String>,
}

/// Arguments for [`dataflow.projects.locations.jobs.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`dataflow.projects.locations.jobs.snapshot_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsSnapshotArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: SnapshotJobRequest,
}

/// Arguments for [`dataflow.projects.locations.jobs.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsUpdateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Job,
}

/// Arguments for [`dataflow.projects.locations.jobs.debug.getConfig_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsDebugGetConfigArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: GetDebugConfigRequest,
}

/// Arguments for [`dataflow.projects.locations.jobs.debug.getWorkerStacktraces_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsDebugGetWorkerStacktracesArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: GetWorkerStacktracesRequest,
}

/// Arguments for [`dataflow.projects.locations.jobs.debug.sendCapture_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsDebugSendCaptureArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: SendDebugCaptureRequest,
}

/// Arguments for [`dataflow.projects.locations.jobs.messages.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsMessagesListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `endTime`.
    pub end_time: Option<String>,
    /// Query parameter: `minimumImportance`.
    pub minimum_importance: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `startTime`.
    pub start_time: Option<String>,
}

/// Arguments for [`dataflow.projects.locations.jobs.snapshots.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsSnapshotsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
}

/// Arguments for [`dataflow.projects.locations.jobs.stages.getExecutionDetails_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsStagesGetExecutionDetailsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Path parameter: `stageId`.
    pub stage_id: String,
    /// Query parameter: `endTime`.
    pub end_time: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `startTime`.
    pub start_time: Option<String>,
}

/// Arguments for [`dataflow.projects.locations.jobs.workItems.lease_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsWorkItemsLeaseArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: LeaseWorkItemRequest,
}

/// Arguments for [`dataflow.projects.locations.jobs.workItems.reportStatus_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsWorkItemsReportStatusArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Request body.
    pub body: ReportWorkItemStatusRequest,
}

/// Arguments for [`dataflow.projects.locations.snapshots.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsSnapshotsDeleteArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `snapshotId`.
    pub snapshot_id: String,
}

/// Arguments for [`dataflow.projects.locations.snapshots.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsSnapshotsGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Path parameter: `snapshotId`.
    pub snapshot_id: String,
}

/// Arguments for [`dataflow.projects.locations.snapshots.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsSnapshotsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Query parameter: `jobId`.
    pub job_id: Option<String>,
}

/// Arguments for [`dataflow.projects.locations.templates.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsTemplatesCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Request body.
    pub body: CreateJobFromTemplateRequest,
}

/// Arguments for [`dataflow.projects.locations.templates.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsTemplatesGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Query parameter: `gcsPath`.
    pub gcs_path: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`dataflow.projects.locations.templates.launch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsTemplatesLaunchArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `location`.
    pub location: String,
    /// Query parameter: `dynamicTemplate.gcsPath`.
    pub dynamic_template_gcs_path: Option<String>,
    /// Query parameter: `dynamicTemplate.stagingLocation`.
    pub dynamic_template_staging_location: Option<String>,
    /// Query parameter: `gcsPath`.
    pub gcs_path: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: LaunchTemplateParameters,
}

/// Arguments for [`dataflow.projects.snapshots.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsSnapshotsGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `snapshotId`.
    pub snapshot_id: String,
    /// Query parameter: `location`.
    pub location: Option<String>,
}

/// Arguments for [`dataflow.projects.snapshots.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsSnapshotsListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `jobId`.
    pub job_id: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
}

/// Arguments for [`dataflow.projects.templates.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsTemplatesCreateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Request body.
    pub body: CreateJobFromTemplateRequest,
}

/// Arguments for [`dataflow.projects.templates.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsTemplatesGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `gcsPath`.
    pub gcs_path: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`dataflow.projects.templates.launch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DataflowProjectsTemplatesLaunchArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Query parameter: `dynamicTemplate.gcsPath`.
    pub dynamic_template_gcs_path: Option<String>,
    /// Query parameter: `dynamicTemplate.stagingLocation`.
    pub dynamic_template_staging_location: Option<String>,
    /// Query parameter: `gcsPath`.
    pub gcs_path: Option<String>,
    /// Query parameter: `location`.
    pub location: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: LaunchTemplateParameters,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// DELETE v1b3/projects/{projectId}/snapshots
// -----------------------------------------------------------------------------

/// DELETE v1b3/projects/{projectId}/snapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_delete_snapshots_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_delete_snapshots_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsDeleteSnapshotsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteSnapshotResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/snapshots",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.snapshot_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("snapshotId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DeleteSnapshotResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/WorkerMessages
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/WorkerMessages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_worker_messages_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_worker_messages_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsWorkerMessagesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SendWorkerMessagesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/WorkerMessages",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SendWorkerMessagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/jobs:aggregated
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/jobs:aggregated.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_aggregated_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_aggregated_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsAggregatedArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs:aggregated",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/jobs
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.replace_job_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("replaceJobId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/jobs/{jobId}
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/jobs/{jobId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}",
        args.project_id, args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/jobs/{jobId}/metrics
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/jobs/{jobId}/metrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_get_metrics_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_get_metrics_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsGetMetricsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<JobMetrics>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/metrics",
        args.project_id, args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startTime=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: JobMetrics =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/jobs
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/jobs/{jobId}:snapshot
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/jobs/{jobId}:snapshot.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_snapshot_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_snapshot_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsSnapshotArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}:snapshot",
        args.project_id, args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Snapshot =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1b3/projects/{projectId}/jobs/{jobId}
// -----------------------------------------------------------------------------

/// PUT v1b3/projects/{projectId}/jobs/{jobId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}",
        args.project_id, args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/jobs/{jobId}/debug/getConfig
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/jobs/{jobId}/debug/getConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_debug_get_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_debug_get_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsDebugGetConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetDebugConfigResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/debug/getConfig",
        args.project_id, args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetDebugConfigResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/jobs/{jobId}/debug/sendCapture
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/jobs/{jobId}/debug/sendCapture.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_debug_send_capture_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_debug_send_capture_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsDebugSendCaptureArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SendDebugCaptureResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/debug/sendCapture",
        args.project_id, args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SendDebugCaptureResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/jobs/{jobId}/messages
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/jobs/{jobId}/messages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_messages_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_messages_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsMessagesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobMessagesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/messages",
        args.project_id, args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.end_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.minimum_importance {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minimumImportance=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startTime=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListJobMessagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/jobs/{jobId}/workItems:lease
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/jobs/{jobId}/workItems:lease.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_work_items_lease_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_work_items_lease_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsWorkItemsLeaseArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LeaseWorkItemResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/workItems:lease",
        args.project_id, args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LeaseWorkItemResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/jobs/{jobId}/workItems:reportStatus
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/jobs/{jobId}/workItems:reportStatus.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_jobs_work_items_report_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_jobs_work_items_report_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsJobsWorkItemsReportStatusArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReportWorkItemStatusResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/workItems:reportStatus",
        args.project_id, args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReportWorkItemStatusResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/locations/{location}/WorkerMessages
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/locations/{location}/WorkerMessages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_worker_messages_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_worker_messages_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsWorkerMessagesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SendWorkerMessagesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/WorkerMessages",
        args.project_id, args.location,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SendWorkerMessagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/locations/{location}/flexTemplates:launch
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/locations/{location}/flexTemplates:launch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_flex_templates_launch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_flex_templates_launch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsFlexTemplatesLaunchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LaunchFlexTemplateResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/flexTemplates:launch",
        args.project_id, args.location,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LaunchFlexTemplateResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/locations/{location}/jobs
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/locations/{location}/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs",
        args.project_id, args.location,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.replace_job_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("replaceJobId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}",
        args.project_id, args.location, args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/executionDetails
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/executionDetails.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_get_execution_details_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_get_execution_details_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsGetExecutionDetailsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<JobExecutionDetails>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/executionDetails",
        args.project_id, args.location, args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: JobExecutionDetails =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/metrics
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/metrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_get_metrics_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_get_metrics_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsGetMetricsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<JobMetrics>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/metrics",
        args.project_id, args.location, args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.start_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startTime=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: JobMetrics =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/locations/{location}/jobs
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/locations/{location}/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs",
        args.project_id, args.location,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}:snapshot
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}:snapshot.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_snapshot_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_snapshot_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsSnapshotArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}:snapshot",
        args.project_id, args.location, args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Snapshot =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
// -----------------------------------------------------------------------------

/// PUT v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}",
        args.project_id, args.location, args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getConfig
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getConfig.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_debug_get_config_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_debug_get_config_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsDebugGetConfigArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetDebugConfigResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/debug/getConfig",
        args.project_id, args.location, args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetDebugConfigResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getWorkerStacktraces
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getWorkerStacktraces.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_debug_get_worker_stacktraces_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_debug_get_worker_stacktraces_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsDebugGetWorkerStacktracesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetWorkerStacktracesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/debug/getWorkerStacktraces",
        args.project_id,
        args.location,
        args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetWorkerStacktracesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/sendCapture
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/sendCapture.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_debug_send_capture_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_debug_send_capture_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsDebugSendCaptureArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SendDebugCaptureResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/debug/sendCapture",
        args.project_id, args.location, args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SendDebugCaptureResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/messages
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/messages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_messages_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_messages_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsMessagesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobMessagesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/messages",
        args.project_id, args.location, args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.end_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.minimum_importance {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minimumImportance=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startTime=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListJobMessagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/snapshots
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/snapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_snapshots_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_snapshots_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsSnapshotsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSnapshotsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/snapshots",
        args.project_id, args.location, args.job_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListSnapshotsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/stages/{stageId}/executionDetails
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/stages/{stageId}/executionDetails.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_stages_get_execution_details_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_stages_get_execution_details_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsStagesGetExecutionDetailsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StageExecutionDetails>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/stages/{}/executionDetails",
        args.project_id,
        args.location,
        args.job_id,
        args.stage_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.end_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endTime=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startTime=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: StageExecutionDetails =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:lease
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:lease.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_work_items_lease_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_work_items_lease_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsWorkItemsLeaseArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LeaseWorkItemResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/workItems:lease",
        args.project_id, args.location, args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LeaseWorkItemResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:reportStatus
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:reportStatus.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_jobs_work_items_report_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_jobs_work_items_report_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsJobsWorkItemsReportStatusArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReportWorkItemStatusResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/workItems:reportStatus",
        args.project_id,
        args.location,
        args.job_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReportWorkItemStatusResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
// -----------------------------------------------------------------------------

/// DELETE v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_snapshots_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_snapshots_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsSnapshotsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteSnapshotResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/snapshots/{}",
        args.project_id, args.location, args.snapshot_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DeleteSnapshotResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_snapshots_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_snapshots_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsSnapshotsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/snapshots/{}",
        args.project_id, args.location, args.snapshot_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Snapshot =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/locations/{location}/snapshots
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_snapshots_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_snapshots_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsSnapshotsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSnapshotsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/snapshots",
        args.project_id, args.location,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.job_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("jobId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListSnapshotsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/locations/{location}/templates
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/locations/{location}/templates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_templates_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_templates_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsTemplatesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/templates",
        args.project_id, args.location,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/locations/{location}/templates:get
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/locations/{location}/templates:get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_templates_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_templates_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsTemplatesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetTemplateResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/templates:get",
        args.project_id, args.location,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.gcs_path {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("gcsPath=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetTemplateResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/locations/{location}/templates:launch
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/locations/{location}/templates:launch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_locations_templates_launch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_locations_templates_launch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsLocationsTemplatesLaunchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LaunchTemplateResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/templates:launch",
        args.project_id, args.location,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.dynamic_template_gcs_path {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dynamicTemplate_gcsPath=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.dynamic_template_staging_location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dynamicTemplate_stagingLocation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.gcs_path {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("gcsPath=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LaunchTemplateResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/snapshots/{snapshotId}
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/snapshots/{snapshotId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_snapshots_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_snapshots_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsSnapshotsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/snapshots/{}",
        args.project_id, args.snapshot_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Snapshot =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/snapshots
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/snapshots.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_snapshots_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_snapshots_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsSnapshotsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSnapshotsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/snapshots",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.job_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("jobId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListSnapshotsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/templates
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/templates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_templates_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_templates_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsTemplatesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/templates",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1b3/projects/{projectId}/templates:get
// -----------------------------------------------------------------------------

/// GET v1b3/projects/{projectId}/templates:get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_templates_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_templates_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsTemplatesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetTemplateResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/templates:get",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.gcs_path {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("gcsPath=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetTemplateResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1b3/projects/{projectId}/templates:launch
// -----------------------------------------------------------------------------

/// POST v1b3/projects/{projectId}/templates:launch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataflow_projects_templates_launch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataflow_projects_templates_launch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DataflowProjectsTemplatesLaunchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LaunchTemplateResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/templates:launch",
        args.project_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.dynamic_template_gcs_path {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dynamicTemplate_gcsPath=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.dynamic_template_staging_location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dynamicTemplate_stagingLocation=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.gcs_path {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("gcsPath=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LaunchTemplateResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
