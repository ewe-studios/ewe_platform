//! Auto-generated API module for gcp/binaryauthorization policies.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_binaryauthorization_policies `

#![cfg(feature = "gcp_binaryauthorization_policies")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Check` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Check {
    /// alwaysDeny property.
    pub always_deny: Option<bool>,
    /// displayName property.
    pub display_name: Option<String>,
    /// imageAllowlist property.
    pub image_allowlist: Option<ImageAllowlist>,
    /// imageFreshnessCheck property.
    pub image_freshness_check: Option<ImageFreshnessCheck>,
    /// sigstoreSignatureCheck property.
    pub sigstore_signature_check: Option<SigstoreSignatureCheck>,
    /// simpleSigningAttestationCheck property.
    pub simple_signing_attestation_check: Option<SimpleSigningAttestationCheck>,
    /// slsaCheck property.
    pub slsa_check: Option<SlsaCheck>,
    /// trustedDirectoryCheck property.
    pub trusted_directory_check: Option<TrustedDirectoryCheck>,
    /// vulnerabilityCheck property.
    pub vulnerability_check: Option<VulnerabilityCheck>,
}

/// `CheckResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CheckResult {
    /// allowlistResult property.
    pub allowlist_result: Option<AllowlistResult>,
    /// displayName property.
    pub display_name: Option<String>,
    /// evaluationResult property.
    pub evaluation_result: Option<EvaluationResult>,
    /// explanation property.
    pub explanation: Option<String>,
    /// index property.
    pub index: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `AllowlistResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AllowlistResult {
    /// matchedPattern property.
    pub matched_pattern: Option<String>,
}

/// `GkePolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GkePolicy {
    /// checkSets property.
    pub check_sets: Option<Vec<CheckSet>>,
    /// imageAllowlist property.
    pub image_allowlist: Option<ImageAllowlist>,
}

/// `AttestationAuthenticator` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AttestationAuthenticator {
    /// displayName property.
    pub display_name: Option<String>,
    /// pkixPublicKeySet property.
    pub pkix_public_key_set: Option<PkixPublicKeySet>,
}

/// `CheckResults` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CheckResults {
    /// results property.
    pub results: Option<Vec<CheckResult>>,
}

/// `PkixPublicKeySet` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PkixPublicKeySet {
    /// pkixPublicKeys property.
    pub pkix_public_keys: Option<Vec<PkixPublicKey>>,
}

/// `SigstoreAuthority` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SigstoreAuthority {
    /// displayName property.
    pub display_name: Option<String>,
    /// publicKeySet property.
    pub public_key_set: Option<SigstorePublicKeySet>,
}

/// `SlsaCheck` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SlsaCheck {
    /// rules property.
    pub rules: Option<Vec<VerificationRule>>,
}

/// `SigstorePublicKey` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SigstorePublicKey {
    /// publicKeyPem property.
    pub public_key_pem: Option<String>,
}

/// `EvaluationResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EvaluationResult {
    /// verdict property.
    pub verdict: Option<String>,
}

/// `TrustedDirectoryCheck` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TrustedDirectoryCheck {
    /// trustedDirPatterns property.
    pub trusted_dir_patterns: Option<Vec<String>>,
}

/// `ImageFreshnessCheck` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ImageFreshnessCheck {
    /// maxUploadAgeDays property.
    pub max_upload_age_days: Option<i64>,
}

/// `SimpleSigningAttestationCheck` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SimpleSigningAttestationCheck {
    /// attestationAuthenticators property.
    pub attestation_authenticators: Option<Vec<AttestationAuthenticator>>,
    /// containerAnalysisAttestationProjects property.
    pub container_analysis_attestation_projects: Option<Vec<String>>,
}

/// `ListPlatformPoliciesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListPlatformPoliciesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// platformPolicies property.
    pub platform_policies: Option<Vec<PlatformPolicy>>,
}

/// `EvaluateGkePolicyResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EvaluateGkePolicyResponse {
    /// results property.
    pub results: Option<Vec<PodResult>>,
    /// verdict property.
    pub verdict: Option<String>,
}

/// `VerificationRule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VerificationRule {
    /// attestationSource property.
    pub attestation_source: Option<AttestationSource>,
    /// configBasedBuildRequired property.
    pub config_based_build_required: Option<bool>,
    /// customConstraints property.
    pub custom_constraints: Option<String>,
    /// trustedBuilder property.
    pub trusted_builder: Option<String>,
    /// trustedSourceRepoPatterns property.
    pub trusted_source_repo_patterns: Option<Vec<String>>,
}

/// `AttestationSource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AttestationSource {
    /// containerAnalysisAttestationProjects property.
    pub container_analysis_attestation_projects: Option<Vec<String>>,
}

/// `VulnerabilityCheck` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VulnerabilityCheck {
    /// allowedCves property.
    pub allowed_cves: Option<Vec<String>>,
    /// blockedCves property.
    pub blocked_cves: Option<Vec<String>>,
    /// containerAnalysisVulnerabilityProjects property.
    pub container_analysis_vulnerability_projects: Option<Vec<String>>,
    /// maximumFixableSeverity property.
    pub maximum_fixable_severity: Option<String>,
    /// maximumUnfixableSeverity property.
    pub maximum_unfixable_severity: Option<String>,
}

/// `CheckSetResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CheckSetResult {
    /// allowlistResult property.
    pub allowlist_result: Option<AllowlistResult>,
    /// checkResults property.
    pub check_results: Option<CheckResults>,
    /// displayName property.
    pub display_name: Option<String>,
    /// explanation property.
    pub explanation: Option<String>,
    /// index property.
    pub index: Option<String>,
    /// scope property.
    pub scope: Option<Scope>,
}

/// `CheckSet` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CheckSet {
    /// checks property.
    pub checks: Option<Vec<Check>>,
    /// displayName property.
    pub display_name: Option<String>,
    /// imageAllowlist property.
    pub image_allowlist: Option<ImageAllowlist>,
    /// scope property.
    pub scope: Option<Scope>,
}

/// `ImageResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ImageResult {
    /// allowlistResult property.
    pub allowlist_result: Option<AllowlistResult>,
    /// checkSetResult property.
    pub check_set_result: Option<CheckSetResult>,
    /// explanation property.
    pub explanation: Option<String>,
    /// imageUri property.
    pub image_uri: Option<String>,
    /// verdict property.
    pub verdict: Option<String>,
}

/// `PlatformPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlatformPolicy {
    /// description property.
    pub description: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// gkePolicy property.
    pub gke_policy: Option<GkePolicy>,
    /// name property.
    pub name: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `ImageAllowlist` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ImageAllowlist {
    /// allowPattern property.
    pub allow_pattern: Option<Vec<String>>,
}

/// `SigstoreSignatureCheck` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SigstoreSignatureCheck {
    /// sigstoreAuthorities property.
    pub sigstore_authorities: Option<Vec<SigstoreAuthority>>,
}

/// `PodResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PodResult {
    /// imageResults property.
    pub image_results: Option<Vec<ImageResult>>,
    /// kubernetesNamespace property.
    pub kubernetes_namespace: Option<String>,
    /// kubernetesServiceAccount property.
    pub kubernetes_service_account: Option<String>,
    /// podName property.
    pub pod_name: Option<String>,
    /// verdict property.
    pub verdict: Option<String>,
}

/// `PkixPublicKey` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PkixPublicKey {
    /// keyId property.
    pub key_id: Option<String>,
    /// publicKeyPem property.
    pub public_key_pem: Option<String>,
    /// signatureAlgorithm property.
    pub signature_algorithm: Option<String>,
}

/// `Scope` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Scope {
    /// kubernetesNamespace property.
    pub kubernetes_namespace: Option<String>,
    /// kubernetesServiceAccount property.
    pub kubernetes_service_account: Option<String>,
}

/// `SigstorePublicKeySet` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SigstorePublicKeySet {
    /// publicKeys property.
    pub public_keys: Option<Vec<SigstorePublicKey>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`binaryauthorization.projects.platforms.gke.policies.evaluate_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationProjectsPlatformsGkePoliciesEvaluateArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`binaryauthorization.projects.platforms.policies.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationProjectsPlatformsPoliciesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `policyId`.
    pub policy_id: Option<String>,
}

/// Arguments for [`binaryauthorization.projects.platforms.policies.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationProjectsPlatformsPoliciesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `etag`.
    pub etag: Option<String>,
}

/// Arguments for [`binaryauthorization.projects.platforms.policies.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationProjectsPlatformsPoliciesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`binaryauthorization.projects.platforms.policies.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationProjectsPlatformsPoliciesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`binaryauthorization.projects.platforms.policies.replacePlatformPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BinaryauthorizationProjectsPlatformsPoliciesReplacePlatformPolicyArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/platforms/gke/policies/{policiesId}:evaluate
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/platforms/gke/policies/{policiesId}:evaluate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = binaryauthorization_projects_platforms_gke_policies_evaluate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn binaryauthorization_projects_platforms_gke_policies_evaluate_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EvaluateGkePolicyResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://binaryauthorization.googleapis.com/v1/projects/{{projectsId}}/platforms/gke/policies/{{policiesId}}:evaluate",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EvaluateGkePolicyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/platforms/{platformsId}/policies
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/platforms/{platformsId}/policies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = binaryauthorization_projects_platforms_policies_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn binaryauthorization_projects_platforms_policies_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlatformPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://binaryauthorization.googleapis.com/v1/projects/{{projectsId}}/platforms/{{platformsId}}/policies",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlatformPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/platforms/{platformsId}/policies/{policiesId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/platforms/{platformsId}/policies/{policiesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = binaryauthorization_projects_platforms_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn binaryauthorization_projects_platforms_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://binaryauthorization.googleapis.com/v1/projects/{{projectsId}}/platforms/{{platformsId}}/policies/{{policiesId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/platforms/{platformsId}/policies/{policiesId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/platforms/{platformsId}/policies/{policiesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = binaryauthorization_projects_platforms_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn binaryauthorization_projects_platforms_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlatformPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://binaryauthorization.googleapis.com/v1/projects/{{projectsId}}/platforms/{{platformsId}}/policies/{{policiesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlatformPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/platforms/{platformsId}/policies
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/platforms/{platformsId}/policies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = binaryauthorization_projects_platforms_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn binaryauthorization_projects_platforms_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPlatformPoliciesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://binaryauthorization.googleapis.com/v1/projects/{{projectsId}}/platforms/{{platformsId}}/policies",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPlatformPoliciesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/projects/{projectsId}/platforms/{platformsId}/policies/{policiesId}
// -----------------------------------------------------------------------------

/// PUT v1/projects/{projectsId}/platforms/{platformsId}/policies/{policiesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = binaryauthorization_projects_platforms_policies_replace_platform_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn binaryauthorization_projects_platforms_policies_replace_platform_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlatformPolicy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://binaryauthorization.googleapis.com/v1/projects/{{projectsId}}/platforms/{{platformsId}}/policies/{{policiesId}}",
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlatformPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
