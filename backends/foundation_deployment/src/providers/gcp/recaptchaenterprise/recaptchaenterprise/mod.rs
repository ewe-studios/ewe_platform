//! Auto-generated API module for gcp/recaptchaenterprise recaptchaenterprise.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_recaptchaenterprise_recaptchaenterprise `

#![cfg(feature = "gcp_recaptchaenterprise_recaptchaenterprise")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleCloudRecaptchaenterpriseV1WebKeySettingsActionSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1WebKeySettingsActionSettings {
    /// scoreThreshold property.
    pub score_threshold: Option<f64>,
}

/// `GoogleCloudRecaptchaenterpriseV1Assessment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1Assessment {
    /// accountDefenderAssessment property.
    pub account_defender_assessment:
        Option<GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessment>,
    /// accountVerification property.
    pub account_verification: Option<GoogleCloudRecaptchaenterpriseV1AccountVerificationInfo>,
    /// assessmentEnvironment property.
    pub assessment_environment: Option<GoogleCloudRecaptchaenterpriseV1AssessmentEnvironment>,
    /// event property.
    pub event: Option<GoogleCloudRecaptchaenterpriseV1Event>,
    /// firewallPolicyAssessment property.
    pub firewall_policy_assessment:
        Option<GoogleCloudRecaptchaenterpriseV1FirewallPolicyAssessment>,
    /// fraudPreventionAssessment property.
    pub fraud_prevention_assessment:
        Option<GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessment>,
    /// fraudSignals property.
    pub fraud_signals: Option<GoogleCloudRecaptchaenterpriseV1FraudSignals>,
    /// name property.
    pub name: Option<String>,
    /// phoneFraudAssessment property.
    pub phone_fraud_assessment: Option<GoogleCloudRecaptchaenterpriseV1PhoneFraudAssessment>,
    /// privatePasswordLeakVerification property.
    pub private_password_leak_verification:
        Option<GoogleCloudRecaptchaenterpriseV1PrivatePasswordLeakVerification>,
    /// riskAnalysis property.
    pub risk_analysis: Option<GoogleCloudRecaptchaenterpriseV1RiskAnalysis>,
    /// tokenProperties property.
    pub token_properties: Option<GoogleCloudRecaptchaenterpriseV1TokenProperties>,
}

/// `GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesRequest {
    /// names property.
    pub names: Option<Vec<String>>,
}

/// `GoogleCloudRecaptchaenterpriseV1ListFirewallPoliciesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ListFirewallPoliciesResponse {
    /// firewallPolicies property.
    pub firewall_policies: Option<Vec<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideRequest {
    /// ipOverrideData property.
    pub ip_override_data: Option<GoogleCloudRecaptchaenterpriseV1IpOverrideData>,
}

/// `GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// relatedAccountGroups property.
    pub related_account_groups: Option<Vec<GoogleCloudRecaptchaenterpriseV1RelatedAccountGroup>>,
}

/// `GoogleCloudRecaptchaenterpriseV1FirewallPolicyAssessment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FirewallPolicyAssessment {
    /// error property.
    pub error: Option<GoogleRpcStatus>,
    /// firewallPolicy property.
    pub firewall_policy: Option<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>,
}

/// `GoogleCloudRecaptchaenterpriseV1ExpressKeySettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ExpressKeySettings {}

/// `GoogleCloudRecaptchaenterpriseV1TransactionDataAddress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1TransactionDataAddress {
    /// address property.
    pub address: Option<Vec<String>>,
    /// administrativeArea property.
    pub administrative_area: Option<String>,
    /// locality property.
    pub locality: Option<String>,
    /// postalCode property.
    pub postal_code: Option<String>,
    /// recipient property.
    pub recipient: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1TransactionDataGatewayInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1TransactionDataGatewayInfo {
    /// avsResponseCode property.
    pub avs_response_code: Option<String>,
    /// cvvResponseCode property.
    pub cvv_response_code: Option<String>,
    /// gatewayResponseCode property.
    pub gateway_response_code: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentRiskReason` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentRiskReason {
    /// reason property.
    pub reason: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1RelatedAccountGroupMembership` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1RelatedAccountGroupMembership {
    /// accountId property.
    pub account_id: Option<String>,
    /// hashedAccountId property.
    pub hashed_account_id: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1WebKeySettingsChallengeSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1WebKeySettingsChallengeSettings {
    /// actionSettings property.
    pub action_settings: Option<serde_json::Value>,
    /// defaultSettings property.
    pub default_settings: Option<GoogleCloudRecaptchaenterpriseV1WebKeySettingsActionSettings>,
}

/// `GoogleRpcStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleRpcStatus {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1UserId` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1UserId {
    /// email property.
    pub email: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1FirewallActionIncludeRecaptchaScriptAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FirewallActionIncludeRecaptchaScriptAction {}

/// `GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessmentAccountRiskReason` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessmentAccountRiskReason {
    /// reason property.
    pub reason: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1MigrateKeyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1MigrateKeyRequest {
    /// skipBillingCheck property.
    pub skip_billing_check: Option<bool>,
}

/// `GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentRequest {
    /// accountId property.
    pub account_id: Option<String>,
    /// annotation property.
    pub annotation: Option<String>,
    /// hashedAccountId property.
    pub hashed_account_id: Option<String>,
    /// phoneAuthenticationEvent property.
    pub phone_authentication_event:
        Option<GoogleCloudRecaptchaenterpriseV1PhoneAuthenticationEvent>,
    /// reasons property.
    pub reasons: Option<Vec<String>>,
    /// transactionEvent property.
    pub transaction_event: Option<GoogleCloudRecaptchaenterpriseV1TransactionEvent>,
}

/// `GoogleCloudRecaptchaenterpriseV1AccountVerificationInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AccountVerificationInfo {
    /// endpoints property.
    pub endpoints: Option<Vec<GoogleCloudRecaptchaenterpriseV1EndpointVerificationInfo>>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// latestVerificationResult property.
    pub latest_verification_result: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentBehavioralTrustVerdict` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentBehavioralTrustVerdict {
    /// trust property.
    pub trust: Option<f64>,
}

/// `GoogleCloudRecaptchaenterpriseV1TestingOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1TestingOptions {
    /// testingChallenge property.
    pub testing_challenge: Option<String>,
    /// testingScore property.
    pub testing_score: Option<f64>,
}

/// `GoogleCloudRecaptchaenterpriseV1ListKeysResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ListKeysResponse {
    /// keys property.
    pub keys: Option<Vec<GoogleCloudRecaptchaenterpriseV1Key>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1AddIpOverrideResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AddIpOverrideResponse {}

/// `GoogleCloudRecaptchaenterpriseV1FirewallActionAllowAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FirewallActionAllowAction {}

/// `GoogleCloudRecaptchaenterpriseV1PhoneAuthenticationEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1PhoneAuthenticationEvent {
    /// eventTime property.
    pub event_time: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1RelatedAccountGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1RelatedAccountGroup {
    /// name property.
    pub name: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1TransactionDataUser` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1TransactionDataUser {
    /// accountId property.
    pub account_id: Option<String>,
    /// creationMs property.
    pub creation_ms: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// emailVerified property.
    pub email_verified: Option<bool>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// phoneVerified property.
    pub phone_verified: Option<bool>,
}

/// `GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentResponse {}

/// `GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesResponse {}

/// `GoogleCloudRecaptchaenterpriseV1FirewallActionSubstituteAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FirewallActionSubstituteAction {
    /// path property.
    pub path: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1ListIpOverridesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ListIpOverridesResponse {
    /// ipOverrides property.
    pub ip_overrides: Option<Vec<GoogleCloudRecaptchaenterpriseV1IpOverrideData>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1FirewallPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FirewallPolicy {
    /// actions property.
    pub actions: Option<Vec<GoogleCloudRecaptchaenterpriseV1FirewallAction>>,
    /// condition property.
    pub condition: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// path property.
    pub path: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsRequest {
    /// accountId property.
    pub account_id: Option<String>,
    /// hashedAccountId property.
    pub hashed_account_id: Option<String>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1IpOverrideData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1IpOverrideData {
    /// ip property.
    pub ip: Option<String>,
    /// overrideType property.
    pub override_type: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1AddIpOverrideRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AddIpOverrideRequest {
    /// ipOverrideData property.
    pub ip_override_data: Option<GoogleCloudRecaptchaenterpriseV1IpOverrideData>,
}

/// `GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessment {
    /// accountTakeoverVerdict property.
    pub account_takeover_verdict:
        Option<GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessmentAccountTakeoverVerdict>,
    /// labels property.
    pub labels: Option<Vec<String>>,
}

/// `GoogleCloudRecaptchaenterpriseV1FirewallActionSetHeaderAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FirewallActionSetHeaderAction {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1RiskAnalysis` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1RiskAnalysis {
    /// challenge property.
    pub challenge: Option<String>,
    /// extendedVerdictReasons property.
    pub extended_verdict_reasons: Option<Vec<String>>,
    /// reasons property.
    pub reasons: Option<Vec<String>>,
    /// score property.
    pub score: Option<f64>,
    /// verifiedBots property.
    pub verified_bots: Option<Vec<GoogleCloudRecaptchaenterpriseV1Bot>>,
}

/// `GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessment {
    /// behavioralTrustVerdict property.
    pub behavioral_trust_verdict:
        Option<GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentBehavioralTrustVerdict>,
    /// cardTestingVerdict property.
    pub card_testing_verdict:
        Option<GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentCardTestingVerdict>,
    /// riskReasons property.
    pub risk_reasons:
        Option<Vec<GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentRiskReason>>,
    /// stolenInstrumentVerdict property.
    pub stolen_instrument_verdict:
        Option<GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentStolenInstrumentVerdict>,
    /// transactionRisk property.
    pub transaction_risk: Option<f64>,
}

/// `GoogleCloudRecaptchaenterpriseV1AndroidKeySettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AndroidKeySettings {
    /// allowAllPackageNames property.
    pub allow_all_package_names: Option<bool>,
    /// allowedPackageNames property.
    pub allowed_package_names: Option<Vec<String>>,
    /// supportNonGoogleAppStoreDistribution property.
    pub support_non_google_app_store_distribution: Option<bool>,
}

/// `GoogleCloudRecaptchaenterpriseV1SmsTollFraudVerdict` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1SmsTollFraudVerdict {
    /// reasons property.
    pub reasons: Option<Vec<String>>,
    /// risk property.
    pub risk: Option<f64>,
}

/// `GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentCardTestingVerdict` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentCardTestingVerdict {
    /// risk property.
    pub risk: Option<f64>,
}

/// `GoogleCloudRecaptchaenterpriseV1PhoneFraudAssessment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1PhoneFraudAssessment {
    /// smsTollFraudVerdict property.
    pub sms_toll_fraud_verdict: Option<GoogleCloudRecaptchaenterpriseV1SmsTollFraudVerdict>,
}

/// `GoogleCloudRecaptchaenterpriseV1FirewallActionBlockAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FirewallActionBlockAction {}

/// `GoogleCloudRecaptchaenterpriseV1PrivatePasswordLeakVerification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1PrivatePasswordLeakVerification {
    /// encryptedLeakMatchPrefixes property.
    pub encrypted_leak_match_prefixes: Option<Vec<String>>,
    /// encryptedUserCredentialsHash property.
    pub encrypted_user_credentials_hash: Option<String>,
    /// lookupHashPrefix property.
    pub lookup_hash_prefix: Option<String>,
    /// reencryptedUserCredentialsHash property.
    pub reencrypted_user_credentials_hash: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentStolenInstrumentVerdict` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FraudPreventionAssessmentStolenInstrumentVerdict {
    /// risk property.
    pub risk: Option<f64>,
}

/// `GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessmentAccountTakeoverVerdict` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessmentAccountTakeoverVerdict {
    /// risk property.
    pub risk: Option<f64>,
    /// riskReasons property.
    pub risk_reasons:
        Option<Vec<GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessmentAccountRiskReason>>,
    /// trustReasons property.
    pub trust_reasons:
        Option<Vec<GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessmentAccountTrustReason>>,
}

/// `GoogleCloudRecaptchaenterpriseV1Bot` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1Bot {
    /// botType property.
    pub bot_type: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1RetrieveLegacySecretKeyResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1RetrieveLegacySecretKeyResponse {
    /// legacySecretKey property.
    pub legacy_secret_key: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1IOSKeySettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1IOSKeySettings {
    /// allowAllBundleIds property.
    pub allow_all_bundle_ids: Option<bool>,
    /// allowedBundleIds property.
    pub allowed_bundle_ids: Option<Vec<String>>,
    /// appleDeveloperId property.
    pub apple_developer_id: Option<GoogleCloudRecaptchaenterpriseV1AppleDeveloperId>,
}

/// `GoogleCloudRecaptchaenterpriseV1Key` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1Key {
    /// androidSettings property.
    pub android_settings: Option<GoogleCloudRecaptchaenterpriseV1AndroidKeySettings>,
    /// createTime property.
    pub create_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// expressSettings property.
    pub express_settings: Option<GoogleCloudRecaptchaenterpriseV1ExpressKeySettings>,
    /// iosSettings property.
    pub ios_settings: Option<GoogleCloudRecaptchaenterpriseV1IOSKeySettings>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// testingOptions property.
    pub testing_options: Option<GoogleCloudRecaptchaenterpriseV1TestingOptions>,
    /// wafSettings property.
    pub waf_settings: Option<GoogleCloudRecaptchaenterpriseV1WafSettings>,
    /// webSettings property.
    pub web_settings: Option<GoogleCloudRecaptchaenterpriseV1WebKeySettings>,
}

/// `GoogleCloudRecaptchaenterpriseV1UserInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1UserInfo {
    /// accountId property.
    pub account_id: Option<String>,
    /// createAccountTime property.
    pub create_account_time: Option<String>,
    /// userIds property.
    pub user_ids: Option<Vec<GoogleCloudRecaptchaenterpriseV1UserId>>,
}

/// `GoogleCloudRecaptchaenterpriseV1TransactionData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1TransactionData {
    /// billingAddress property.
    pub billing_address: Option<GoogleCloudRecaptchaenterpriseV1TransactionDataAddress>,
    /// cardBin property.
    pub card_bin: Option<String>,
    /// cardLastFour property.
    pub card_last_four: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// gatewayInfo property.
    pub gateway_info: Option<GoogleCloudRecaptchaenterpriseV1TransactionDataGatewayInfo>,
    /// items property.
    pub items: Option<Vec<GoogleCloudRecaptchaenterpriseV1TransactionDataItem>>,
    /// merchants property.
    pub merchants: Option<Vec<GoogleCloudRecaptchaenterpriseV1TransactionDataUser>>,
    /// paymentMethod property.
    pub payment_method: Option<String>,
    /// shippingAddress property.
    pub shipping_address: Option<GoogleCloudRecaptchaenterpriseV1TransactionDataAddress>,
    /// shippingValue property.
    pub shipping_value: Option<f64>,
    /// transactionId property.
    pub transaction_id: Option<String>,
    /// user property.
    pub user: Option<GoogleCloudRecaptchaenterpriseV1TransactionDataUser>,
    /// value property.
    pub value: Option<f64>,
}

/// `GoogleCloudRecaptchaenterpriseV1EndpointVerificationInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1EndpointVerificationInfo {
    /// emailAddress property.
    pub email_address: Option<String>,
    /// lastVerificationTime property.
    pub last_verification_time: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
    /// requestToken property.
    pub request_token: Option<String>,
}

/// `GoogleProtobufEmpty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleProtobufEmpty {}

/// `GoogleCloudRecaptchaenterpriseV1AssessmentEnvironment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AssessmentEnvironment {
    /// client property.
    pub client: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1FraudSignalsUserSignals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FraudSignalsUserSignals {
    /// activeDaysLowerBound property.
    pub active_days_lower_bound: Option<i64>,
    /// syntheticRisk property.
    pub synthetic_risk: Option<f64>,
}

/// `GoogleCloudRecaptchaenterpriseV1TransactionEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1TransactionEvent {
    /// eventTime property.
    pub event_time: Option<String>,
    /// eventType property.
    pub event_type: Option<String>,
    /// reason property.
    pub reason: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// relatedAccountGroupMemberships property.
    pub related_account_group_memberships:
        Option<Vec<GoogleCloudRecaptchaenterpriseV1RelatedAccountGroupMembership>>,
}

/// `GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupMembershipsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupMembershipsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// relatedAccountGroupMemberships property.
    pub related_account_group_memberships:
        Option<Vec<GoogleCloudRecaptchaenterpriseV1RelatedAccountGroupMembership>>,
}

/// `GoogleCloudRecaptchaenterpriseV1ScoreMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ScoreMetrics {
    /// actionMetrics property.
    pub action_metrics: Option<serde_json::Value>,
    /// overallMetrics property.
    pub overall_metrics: Option<GoogleCloudRecaptchaenterpriseV1ScoreDistribution>,
}

/// `GoogleCloudRecaptchaenterpriseV1ScoreDistribution` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ScoreDistribution {
    /// scoreBuckets property.
    pub score_buckets: Option<serde_json::Value>,
}

/// `GoogleCloudRecaptchaenterpriseV1TokenProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1TokenProperties {
    /// action property.
    pub action: Option<String>,
    /// androidPackageName property.
    pub android_package_name: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// hostname property.
    pub hostname: Option<String>,
    /// invalidReason property.
    pub invalid_reason: Option<String>,
    /// iosBundleId property.
    pub ios_bundle_id: Option<String>,
    /// valid property.
    pub valid: Option<bool>,
}

/// `GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessmentAccountTrustReason` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AccountDefenderAssessmentAccountTrustReason {
    /// reason property.
    pub reason: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1FraudSignals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FraudSignals {
    /// cardSignals property.
    pub card_signals: Option<GoogleCloudRecaptchaenterpriseV1FraudSignalsCardSignals>,
    /// userSignals property.
    pub user_signals: Option<GoogleCloudRecaptchaenterpriseV1FraudSignalsUserSignals>,
}

/// `GoogleCloudRecaptchaenterpriseV1Metrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1Metrics {
    /// challengeMetrics property.
    pub challenge_metrics: Option<Vec<GoogleCloudRecaptchaenterpriseV1ChallengeMetrics>>,
    /// name property.
    pub name: Option<String>,
    /// scoreMetrics property.
    pub score_metrics: Option<Vec<GoogleCloudRecaptchaenterpriseV1ScoreMetrics>>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1ChallengeMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ChallengeMetrics {
    /// failedCount property.
    pub failed_count: Option<String>,
    /// nocaptchaCount property.
    pub nocaptcha_count: Option<String>,
    /// pageloadCount property.
    pub pageload_count: Option<String>,
    /// passedCount property.
    pub passed_count: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1FirewallAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FirewallAction {
    /// allow property.
    pub allow: Option<GoogleCloudRecaptchaenterpriseV1FirewallActionAllowAction>,
    /// block property.
    pub block: Option<GoogleCloudRecaptchaenterpriseV1FirewallActionBlockAction>,
    /// includeRecaptchaScript property.
    pub include_recaptcha_script:
        Option<GoogleCloudRecaptchaenterpriseV1FirewallActionIncludeRecaptchaScriptAction>,
    /// redirect property.
    pub redirect: Option<GoogleCloudRecaptchaenterpriseV1FirewallActionRedirectAction>,
    /// setHeader property.
    pub set_header: Option<GoogleCloudRecaptchaenterpriseV1FirewallActionSetHeaderAction>,
    /// substitute property.
    pub substitute: Option<GoogleCloudRecaptchaenterpriseV1FirewallActionSubstituteAction>,
}

/// `GoogleCloudRecaptchaenterpriseV1TransactionDataItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1TransactionDataItem {
    /// merchantAccountId property.
    pub merchant_account_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// quantity property.
    pub quantity: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `GoogleCloudRecaptchaenterpriseV1WebKeySettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1WebKeySettings {
    /// allowAllDomains property.
    pub allow_all_domains: Option<bool>,
    /// allowAmpTraffic property.
    pub allow_amp_traffic: Option<bool>,
    /// allowedDomains property.
    pub allowed_domains: Option<Vec<String>>,
    /// challengeSecurityPreference property.
    pub challenge_security_preference: Option<String>,
    /// challengeSettings property.
    pub challenge_settings: Option<GoogleCloudRecaptchaenterpriseV1WebKeySettingsChallengeSettings>,
    /// integrationType property.
    pub integration_type: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1AppleDeveloperId` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1AppleDeveloperId {
    /// keyId property.
    pub key_id: Option<String>,
    /// privateKey property.
    pub private_key: Option<String>,
    /// teamId property.
    pub team_id: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1FraudSignalsCardSignals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FraudSignalsCardSignals {
    /// cardLabels property.
    pub card_labels: Option<Vec<String>>,
}

/// `GoogleCloudRecaptchaenterpriseV1Event` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1Event {
    /// expectedAction property.
    pub expected_action: Option<String>,
    /// express property.
    pub express: Option<bool>,
    /// firewallPolicyEvaluation property.
    pub firewall_policy_evaluation: Option<bool>,
    /// fraudPrevention property.
    pub fraud_prevention: Option<String>,
    /// hashedAccountId property.
    pub hashed_account_id: Option<String>,
    /// headers property.
    pub headers: Option<Vec<String>>,
    /// ja3 property.
    pub ja3: Option<String>,
    /// ja4 property.
    pub ja4: Option<String>,
    /// requestedUri property.
    pub requested_uri: Option<String>,
    /// siteKey property.
    pub site_key: Option<String>,
    /// token property.
    pub token: Option<String>,
    /// transactionData property.
    pub transaction_data: Option<GoogleCloudRecaptchaenterpriseV1TransactionData>,
    /// userAgent property.
    pub user_agent: Option<String>,
    /// userInfo property.
    pub user_info: Option<GoogleCloudRecaptchaenterpriseV1UserInfo>,
    /// userIpAddress property.
    pub user_ip_address: Option<String>,
    /// wafTokenAssessment property.
    pub waf_token_assessment: Option<bool>,
}

/// `GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideResponse {}

/// `GoogleCloudRecaptchaenterpriseV1FirewallActionRedirectAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1FirewallActionRedirectAction {}

/// `GoogleCloudRecaptchaenterpriseV1WafSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1WafSettings {
    /// wafFeature property.
    pub waf_feature: Option<String>,
    /// wafService property.
    pub waf_service: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`recaptchaenterprise.projects.assessments.annotate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsAssessmentsAnnotateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentRequest,
}

/// Arguments for [`recaptchaenterprise.projects.assessments.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsAssessmentsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1Assessment,
}

/// Arguments for [`recaptchaenterprise.projects.firewallpolicies.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsFirewallpoliciesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1FirewallPolicy,
}

/// Arguments for [`recaptchaenterprise.projects.firewallpolicies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsFirewallpoliciesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`recaptchaenterprise.projects.firewallpolicies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsFirewallpoliciesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`recaptchaenterprise.projects.firewallpolicies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsFirewallpoliciesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`recaptchaenterprise.projects.firewallpolicies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsFirewallpoliciesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1FirewallPolicy,
}

/// Arguments for [`recaptchaenterprise.projects.firewallpolicies.reorder_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsFirewallpoliciesReorderArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesRequest,
}

/// Arguments for [`recaptchaenterprise.projects.keys.addIpOverride_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysAddIpOverrideArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1AddIpOverrideRequest,
}

/// Arguments for [`recaptchaenterprise.projects.keys.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1Key,
}

/// Arguments for [`recaptchaenterprise.projects.keys.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`recaptchaenterprise.projects.keys.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`recaptchaenterprise.projects.keys.getMetrics_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysGetMetricsArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`recaptchaenterprise.projects.keys.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`recaptchaenterprise.projects.keys.listIpOverrides_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysListIpOverridesArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`recaptchaenterprise.projects.keys.migrate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysMigrateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1MigrateKeyRequest,
}

/// Arguments for [`recaptchaenterprise.projects.keys.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1Key,
}

/// Arguments for [`recaptchaenterprise.projects.keys.removeIpOverride_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysRemoveIpOverrideArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideRequest,
}

/// Arguments for [`recaptchaenterprise.projects.keys.retrieveLegacySecretKey_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysRetrieveLegacySecretKeyArgs {
    /// Path parameter: `key`.
    pub key: String,
}

/// Arguments for [`recaptchaenterprise.projects.relatedaccountgroupmemberships.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsRelatedaccountgroupmembershipsSearchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Request body.
    pub body: GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsRequest,
}

/// Arguments for [`recaptchaenterprise.projects.relatedaccountgroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsRelatedaccountgroupsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`recaptchaenterprise.projects.relatedaccountgroups.memberships.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsRelatedaccountgroupsMembershipsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/{+name}:annotate
// -----------------------------------------------------------------------------

/// POST v1/{+name}:annotate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_assessments_annotate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_assessments_annotate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsAssessmentsAnnotateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}:annotate",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1AnnotateAssessmentResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/assessments
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/assessments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_assessments_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_assessments_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsAssessmentsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1Assessment>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}/assessments",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Assessment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/firewallpolicies
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/firewallpolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_firewallpolicies_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_firewallpolicies_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsFirewallpoliciesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}/firewallpolicies",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1FirewallPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_firewallpolicies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_firewallpolicies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsFirewallpoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}",
        args.name,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_firewallpolicies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_firewallpolicies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsFirewallpoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1FirewallPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/firewallpolicies
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/firewallpolicies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_firewallpolicies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_firewallpolicies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsFirewallpoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListFirewallPoliciesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}/firewallpolicies",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ListFirewallPoliciesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_firewallpolicies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_firewallpolicies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsFirewallpoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1FirewallPolicy>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1FirewallPolicy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/firewallpolicies:reorder
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/firewallpolicies:reorder.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_firewallpolicies_reorder_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_firewallpolicies_reorder_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsFirewallpoliciesReorderArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}/firewallpolicies:reorder",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ReorderFirewallPoliciesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:addIpOverride
// -----------------------------------------------------------------------------

/// POST v1/{+name}:addIpOverride.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_add_ip_override_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_add_ip_override_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsKeysAddIpOverrideArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1AddIpOverrideResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}:addIpOverride",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1AddIpOverrideResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/keys
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/keys.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsKeysCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}/keys",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Key = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsKeysDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}",
        args.name,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsKeysGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Key = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_get_metrics_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_get_metrics_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsKeysGetMetricsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1Metrics>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Metrics = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/keys
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/keys.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsKeysListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListKeysResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}/keys",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ListKeysResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}:listIpOverrides
// -----------------------------------------------------------------------------

/// GET v1/{+parent}:listIpOverrides.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_list_ip_overrides_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_list_ip_overrides_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsKeysListIpOverridesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListIpOverridesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}:listIpOverrides",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ListIpOverridesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:migrate
// -----------------------------------------------------------------------------

/// POST v1/{+name}:migrate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_migrate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_migrate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsKeysMigrateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}:migrate",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Key = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsKeysPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1Key>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Key = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:removeIpOverride
// -----------------------------------------------------------------------------

/// POST v1/{+name}:removeIpOverride.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_remove_ip_override_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_remove_ip_override_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsKeysRemoveIpOverrideArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}:removeIpOverride",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1RemoveIpOverrideResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+key}:retrieveLegacySecretKey
// -----------------------------------------------------------------------------

/// GET v1/{+key}:retrieveLegacySecretKey.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_retrieve_legacy_secret_key_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_retrieve_legacy_secret_key_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsKeysRetrieveLegacySecretKeyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1RetrieveLegacySecretKeyResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}:retrieveLegacySecretKey",
        args.key,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1RetrieveLegacySecretKeyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+project}/relatedaccountgroupmemberships:search
// -----------------------------------------------------------------------------

/// POST v1/{+project}/relatedaccountgroupmemberships:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_relatedaccountgroupmemberships_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_relatedaccountgroupmemberships_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsRelatedaccountgroupmembershipsSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsResponse,
                >,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}/relatedaccountgroupmemberships:search",
        args.project,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(
        builder
            .build_send_request()
            .map_err(|e: foundation_core::wire::simple_http::HttpClientError| super::shared::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                super::shared::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                    let parsed: GoogleCloudRecaptchaenterpriseV1SearchRelatedAccountGroupMembershipsResponse = serde_json::from_str(&body).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                super::shared::RequestIntro::Failed(e) => Err(super::shared::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| super::shared::ApiPending::Sending)
    )
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/relatedaccountgroups
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/relatedaccountgroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_relatedaccountgroups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_relatedaccountgroups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsRelatedaccountgroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}/relatedaccountgroups",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/memberships
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/memberships.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_relatedaccountgroups_memberships_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_relatedaccountgroups_memberships_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &RecaptchaenterpriseProjectsRelatedaccountgroupsMembershipsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupMembershipsResponse,
                >,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/{}/memberships",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(
        builder
            .build_send_request()
            .map_err(|e: foundation_core::wire::simple_http::HttpClientError| super::shared::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                super::shared::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                    let parsed: GoogleCloudRecaptchaenterpriseV1ListRelatedAccountGroupMembershipsResponse = serde_json::from_str(&body).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                super::shared::RequestIntro::Failed(e) => Err(super::shared::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| super::shared::ApiPending::Sending)
    )
}
