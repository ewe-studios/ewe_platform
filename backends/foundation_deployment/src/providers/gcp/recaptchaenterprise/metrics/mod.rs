//! Auto-generated API module for gcp/recaptchaenterprise metrics.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_recaptchaenterprise_metrics `

#![cfg(feature = "gcp_recaptchaenterprise_metrics")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleCloudRecaptchaenterpriseV1ScoreMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ScoreMetrics {
    /// actionMetrics property.
    pub action_metrics: Option<serde_json::Value>,
    /// overallMetrics property.
    pub overall_metrics: Option<GoogleCloudRecaptchaenterpriseV1ScoreDistribution>,
}

/// `GoogleCloudRecaptchaenterpriseV1Metrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1Metrics {
    /// challengeMetrics property.
    pub challenge_metrics: Option<Vec<GoogleCloudRecaptchaenterpriseV1ChallengeMetrics>>,
    /// name property.
    pub name: Option<String>,
    /// scoreMetrics property.
    pub score_metrics: Option<Vec<GoogleCloudRecaptchaenterpriseV1ScoreMetrics>>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `GoogleCloudRecaptchaenterpriseV1ScoreDistribution` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ScoreDistribution {
    /// scoreBuckets property.
    pub score_buckets: Option<serde_json::Value>,
}

/// `GoogleCloudRecaptchaenterpriseV1ChallengeMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudRecaptchaenterpriseV1ChallengeMetrics {
    /// failedCount property.
    pub failed_count: Option<String>,
    /// nocaptchaCount property.
    pub nocaptcha_count: Option<String>,
    /// pageloadCount property.
    pub pageload_count: Option<String>,
    /// passedCount property.
    pub passed_count: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`recaptchaenterprise.projects.keys.getMetrics_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecaptchaenterpriseProjectsKeysGetMetricsArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/keys/{keysId}/metrics
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/keys/{keysId}/metrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = recaptchaenterprise_projects_keys_get_metrics_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn recaptchaenterprise_projects_keys_get_metrics_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecaptchaenterpriseV1Metrics>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://recaptchaenterprise.googleapis.com/v1/projects/{{projectsId}}/keys/{{keysId}}/metrics",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudRecaptchaenterpriseV1Metrics = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
