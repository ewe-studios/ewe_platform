//! Auto-generated API module for gcp/drive drive.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_drive_drive `

#![cfg(feature = "gcp_drive_drive")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ModifyLabelsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ModifyLabelsRequest {
    /// kind property.
    pub kind: Option<String>,
    /// labelModifications property.
    pub label_modifications: Option<Vec<LabelModification>>,
}

/// `LabelModification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LabelModification {
    /// fieldModifications property.
    pub field_modifications: Option<Vec<LabelFieldModification>>,
    /// kind property.
    pub kind: Option<String>,
    /// labelId property.
    pub label_id: Option<String>,
    /// removeLabel property.
    pub remove_label: Option<bool>,
}

/// `ListAccessProposalsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListAccessProposalsResponse {
    /// accessProposals property.
    pub access_proposals: Option<Vec<AccessProposal>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `LabelFieldModification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LabelFieldModification {
    /// fieldId property.
    pub field_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// setDateValues property.
    pub set_date_values: Option<Vec<String>>,
    /// setIntegerValues property.
    pub set_integer_values: Option<Vec<String>>,
    /// setSelectionValues property.
    pub set_selection_values: Option<Vec<String>>,
    /// setTextValues property.
    pub set_text_values: Option<Vec<String>>,
    /// setUserValues property.
    pub set_user_values: Option<Vec<String>>,
    /// unsetValues property.
    pub unset_values: Option<bool>,
}

/// `AppIcons` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppIcons {
    /// category property.
    pub category: Option<String>,
    /// iconUrl property.
    pub icon_url: Option<String>,
    /// size property.
    pub size: Option<i64>,
}

/// `App` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct App {
    /// authorized property.
    pub authorized: Option<bool>,
    /// createInFolderTemplate property.
    pub create_in_folder_template: Option<String>,
    /// createUrl property.
    pub create_url: Option<String>,
    /// hasDriveWideScope property.
    pub has_drive_wide_scope: Option<bool>,
    /// icons property.
    pub icons: Option<Vec<AppIcons>>,
    /// id property.
    pub id: Option<String>,
    /// installed property.
    pub installed: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// longDescription property.
    pub long_description: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// objectType property.
    pub object_type: Option<String>,
    /// openUrlTemplate property.
    pub open_url_template: Option<String>,
    /// primaryFileExtensions property.
    pub primary_file_extensions: Option<Vec<String>>,
    /// primaryMimeTypes property.
    pub primary_mime_types: Option<Vec<String>>,
    /// productId property.
    pub product_id: Option<String>,
    /// productUrl property.
    pub product_url: Option<String>,
    /// secondaryFileExtensions property.
    pub secondary_file_extensions: Option<Vec<String>>,
    /// secondaryMimeTypes property.
    pub secondary_mime_types: Option<Vec<String>>,
    /// shortDescription property.
    pub short_description: Option<String>,
    /// supportsCreate property.
    pub supports_create: Option<bool>,
    /// supportsImport property.
    pub supports_import: Option<bool>,
    /// supportsMultiOpen property.
    pub supports_multi_open: Option<bool>,
    /// supportsOfflineCreate property.
    pub supports_offline_create: Option<bool>,
    /// useByDefault property.
    pub use_by_default: Option<bool>,
}

/// `GeneratedIds` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GeneratedIds {
    /// ids property.
    pub ids: Option<Vec<String>>,
    /// kind property.
    pub kind: Option<String>,
    /// space property.
    pub space: Option<String>,
}

/// `LabelList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LabelList {
    /// kind property.
    pub kind: Option<String>,
    /// labels property.
    pub labels: Option<Vec<Label>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `File` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct File {
    /// appProperties property.
    pub app_properties: Option<serde_json::Value>,
    /// capabilities property.
    pub capabilities: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// clientEncryptionDetails property.
    pub client_encryption_details: Option<ClientEncryptionDetails>,
    /// contentHints property.
    pub content_hints: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// contentRestrictions property.
    pub content_restrictions: Option<Vec<ContentRestriction>>,
    /// copyRequiresWriterPermission property.
    pub copy_requires_writer_permission: Option<bool>,
    /// createdTime property.
    pub created_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// downloadRestrictions property.
    pub download_restrictions: Option<DownloadRestrictionsMetadata>,
    /// driveId property.
    pub drive_id: Option<String>,
    /// explicitlyTrashed property.
    pub explicitly_trashed: Option<bool>,
    /// exportLinks property.
    pub export_links: Option<serde_json::Value>,
    /// fileExtension property.
    pub file_extension: Option<String>,
    /// folderColorRgb property.
    pub folder_color_rgb: Option<String>,
    /// fullFileExtension property.
    pub full_file_extension: Option<String>,
    /// hasAugmentedPermissions property.
    pub has_augmented_permissions: Option<bool>,
    /// hasThumbnail property.
    pub has_thumbnail: Option<bool>,
    /// headRevisionId property.
    pub head_revision_id: Option<String>,
    /// iconLink property.
    pub icon_link: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// imageMediaMetadata property.
    pub image_media_metadata: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// inheritedPermissionsDisabled property.
    pub inherited_permissions_disabled: Option<bool>,
    /// isAppAuthorized property.
    pub is_app_authorized: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// labelInfo property.
    pub label_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// lastModifyingUser property.
    pub last_modifying_user: Option<User>,
    /// linkShareMetadata property.
    pub link_share_metadata: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// md5Checksum property.
    pub md5_checksum: Option<String>,
    /// mimeType property.
    pub mime_type: Option<String>,
    /// modifiedByMe property.
    pub modified_by_me: Option<bool>,
    /// modifiedByMeTime property.
    pub modified_by_me_time: Option<String>,
    /// modifiedTime property.
    pub modified_time: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// originalFilename property.
    pub original_filename: Option<String>,
    /// ownedByMe property.
    pub owned_by_me: Option<bool>,
    /// owners property.
    pub owners: Option<Vec<User>>,
    /// parents property.
    pub parents: Option<Vec<String>>,
    /// permissionIds property.
    pub permission_ids: Option<Vec<String>>,
    /// permissions property.
    pub permissions: Option<Vec<Permission>>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
    /// quotaBytesUsed property.
    pub quota_bytes_used: Option<String>,
    /// resourceKey property.
    pub resource_key: Option<String>,
    /// sha1Checksum property.
    pub sha1_checksum: Option<String>,
    /// sha256Checksum property.
    pub sha256_checksum: Option<String>,
    /// shared property.
    pub shared: Option<bool>,
    /// sharedWithMeTime property.
    pub shared_with_me_time: Option<String>,
    /// sharingUser property.
    pub sharing_user: Option<User>,
    /// shortcutDetails property.
    pub shortcut_details: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// size property.
    pub size: Option<String>,
    /// spaces property.
    pub spaces: Option<Vec<String>>,
    /// starred property.
    pub starred: Option<bool>,
    /// teamDriveId property.
    pub team_drive_id: Option<String>,
    /// thumbnailLink property.
    pub thumbnail_link: Option<String>,
    /// thumbnailVersion property.
    pub thumbnail_version: Option<String>,
    /// trashed property.
    pub trashed: Option<bool>,
    /// trashedTime property.
    pub trashed_time: Option<String>,
    /// trashingUser property.
    pub trashing_user: Option<User>,
    /// version property.
    pub version: Option<String>,
    /// videoMediaMetadata property.
    pub video_media_metadata: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// viewedByMe property.
    pub viewed_by_me: Option<bool>,
    /// viewedByMeTime property.
    pub viewed_by_me_time: Option<String>,
    /// viewersCanCopyContent property.
    pub viewers_can_copy_content: Option<bool>,
    /// webContentLink property.
    pub web_content_link: Option<String>,
    /// webViewLink property.
    pub web_view_link: Option<String>,
    /// writersCanShare property.
    pub writers_can_share: Option<bool>,
}

/// `Change` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Change {
    /// changeType property.
    pub change_type: Option<String>,
    /// drive property.
    pub drive: Option<Drive>,
    /// driveId property.
    pub drive_id: Option<String>,
    /// file property.
    pub file: Option<File>,
    /// fileId property.
    pub file_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// removed property.
    pub removed: Option<bool>,
    /// teamDrive property.
    pub team_drive: Option<TeamDrive>,
    /// teamDriveId property.
    pub team_drive_id: Option<String>,
    /// time property.
    pub time: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `About` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct About {
    /// appInstalled property.
    pub app_installed: Option<bool>,
    /// canCreateDrives property.
    pub can_create_drives: Option<bool>,
    /// canCreateTeamDrives property.
    pub can_create_team_drives: Option<bool>,
    /// driveThemes property.
    pub drive_themes: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// exportFormats property.
    pub export_formats: Option<serde_json::Value>,
    /// folderColorPalette property.
    pub folder_color_palette: Option<Vec<String>>,
    /// importFormats property.
    pub import_formats: Option<serde_json::Value>,
    /// kind property.
    pub kind: Option<String>,
    /// maxImportSizes property.
    pub max_import_sizes: Option<serde_json::Value>,
    /// maxUploadSize property.
    pub max_upload_size: Option<String>,
    /// storageQuota property.
    pub storage_quota: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// teamDriveThemes property.
    pub team_drive_themes: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// user property.
    pub user: Option<User>,
}

/// `Drive` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Drive {
    /// backgroundImageFile property.
    pub background_image_file: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// backgroundImageLink property.
    pub background_image_link: Option<String>,
    /// capabilities property.
    pub capabilities: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// colorRgb property.
    pub color_rgb: Option<String>,
    /// createdTime property.
    pub created_time: Option<String>,
    /// hidden property.
    pub hidden: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// orgUnitId property.
    pub org_unit_id: Option<String>,
    /// restrictions property.
    pub restrictions: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// themeId property.
    pub theme_id: Option<String>,
}

/// `Label` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Label {
    /// fields property.
    pub fields: Option<serde_json::Value>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// revisionId property.
    pub revision_id: Option<String>,
}

/// `PermissionList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PermissionList {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// permissions property.
    pub permissions: Option<Vec<Permission>>,
}

/// `ClientEncryptionDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClientEncryptionDetails {
    /// decryptionMetadata property.
    pub decryption_metadata: Option<DecryptionMetadata>,
    /// encryptionState property.
    pub encryption_state: Option<String>,
}

/// `User` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct User {
    /// displayName property.
    pub display_name: Option<String>,
    /// emailAddress property.
    pub email_address: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// me property.
    pub me: Option<bool>,
    /// permissionId property.
    pub permission_id: Option<String>,
    /// photoLink property.
    pub photo_link: Option<String>,
}

/// `AccessProposalRoleAndView` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessProposalRoleAndView {
    /// role property.
    pub role: Option<String>,
    /// view property.
    pub view: Option<String>,
}

/// `CommentList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommentList {
    /// comments property.
    pub comments: Option<Vec<Comment>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `RevisionList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RevisionList {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// revisions property.
    pub revisions: Option<Vec<Revision>>,
}

/// `TeamDriveList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TeamDriveList {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// teamDrives property.
    pub team_drives: Option<Vec<TeamDrive>>,
}

/// `StartPageToken` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StartPageToken {
    /// kind property.
    pub kind: Option<String>,
    /// startPageToken property.
    pub start_page_token: Option<String>,
}

/// `Channel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Channel {
    /// address property.
    pub address: Option<String>,
    /// expiration property.
    pub expiration: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// params property.
    pub params: Option<serde_json::Value>,
    /// payload property.
    pub payload: Option<bool>,
    /// resourceId property.
    pub resource_id: Option<String>,
    /// resourceUri property.
    pub resource_uri: Option<String>,
    /// token property.
    pub token: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `DriveList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DriveList {
    /// drives property.
    pub drives: Option<Vec<Drive>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ReplyList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReplyList {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// replies property.
    pub replies: Option<Vec<Reply>>,
}

/// `DownloadRestriction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DownloadRestriction {
    /// restrictedForReaders property.
    pub restricted_for_readers: Option<bool>,
    /// restrictedForWriters property.
    pub restricted_for_writers: Option<bool>,
}

/// `AppList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppList {
    /// defaultAppIds property.
    pub default_app_ids: Option<Vec<String>>,
    /// items property.
    pub items: Option<Vec<App>>,
    /// kind property.
    pub kind: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `DecryptionMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DecryptionMetadata {
    /// aes256GcmChunkSize property.
    pub aes256_gcm_chunk_size: Option<String>,
    /// encryptionResourceKeyHash property.
    pub encryption_resource_key_hash: Option<String>,
    /// jwt property.
    pub jwt: Option<String>,
    /// kaclsId property.
    pub kacls_id: Option<String>,
    /// kaclsName property.
    pub kacls_name: Option<String>,
    /// keyFormat property.
    pub key_format: Option<String>,
    /// wrappedKey property.
    pub wrapped_key: Option<String>,
}

/// `ResolveAccessProposalRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResolveAccessProposalRequest {
    /// action property.
    pub action: Option<String>,
    /// role property.
    pub role: Option<Vec<String>>,
    /// sendNotification property.
    pub send_notification: Option<bool>,
    /// view property.
    pub view: Option<String>,
}

/// `ModifyLabelsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ModifyLabelsResponse {
    /// kind property.
    pub kind: Option<String>,
    /// modifiedLabels property.
    pub modified_labels: Option<Vec<Label>>,
}

/// `ApprovalList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApprovalList {
    /// items property.
    pub items: Option<Vec<Approval>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ChangeList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChangeList {
    /// changes property.
    pub changes: Option<Vec<Change>>,
    /// kind property.
    pub kind: Option<String>,
    /// newStartPageToken property.
    pub new_start_page_token: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ReviewerResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReviewerResponse {
    /// kind property.
    pub kind: Option<String>,
    /// response property.
    pub response: Option<String>,
    /// reviewer property.
    pub reviewer: Option<User>,
}

/// `ContentRestriction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContentRestriction {
    /// ownerRestricted property.
    pub owner_restricted: Option<bool>,
    /// readOnly property.
    pub read_only: Option<bool>,
    /// reason property.
    pub reason: Option<String>,
    /// restrictingUser property.
    pub restricting_user: Option<User>,
    /// restrictionTime property.
    pub restriction_time: Option<String>,
    /// systemRestricted property.
    pub system_restricted: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
}

/// `DownloadRestrictionsMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DownloadRestrictionsMetadata {
    /// effectiveDownloadRestrictionWithContext property.
    pub effective_download_restriction_with_context: Option<DownloadRestriction>,
    /// itemDownloadRestriction property.
    pub item_download_restriction: Option<DownloadRestriction>,
}

/// `TeamDrive` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TeamDrive {
    /// backgroundImageFile property.
    pub background_image_file: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// backgroundImageLink property.
    pub background_image_link: Option<String>,
    /// capabilities property.
    pub capabilities: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// colorRgb property.
    pub color_rgb: Option<String>,
    /// createdTime property.
    pub created_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// orgUnitId property.
    pub org_unit_id: Option<String>,
    /// restrictions property.
    pub restrictions: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// themeId property.
    pub theme_id: Option<String>,
}

/// `FileList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FileList {
    /// files property.
    pub files: Option<Vec<File>>,
    /// incompleteSearch property.
    pub incomplete_search: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Comment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Comment {
    /// anchor property.
    pub anchor: Option<String>,
    /// assigneeEmailAddress property.
    pub assignee_email_address: Option<String>,
    /// author property.
    pub author: Option<User>,
    /// content property.
    pub content: Option<String>,
    /// createdTime property.
    pub created_time: Option<String>,
    /// deleted property.
    pub deleted: Option<bool>,
    /// htmlContent property.
    pub html_content: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// mentionedEmailAddresses property.
    pub mentioned_email_addresses: Option<Vec<String>>,
    /// modifiedTime property.
    pub modified_time: Option<String>,
    /// quotedFileContent property.
    pub quoted_file_content: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// replies property.
    pub replies: Option<Vec<Reply>>,
    /// resolved property.
    pub resolved: Option<bool>,
}

/// `Reply` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Reply {
    /// action property.
    pub action: Option<String>,
    /// assigneeEmailAddress property.
    pub assignee_email_address: Option<String>,
    /// author property.
    pub author: Option<User>,
    /// content property.
    pub content: Option<String>,
    /// createdTime property.
    pub created_time: Option<String>,
    /// deleted property.
    pub deleted: Option<bool>,
    /// htmlContent property.
    pub html_content: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// mentionedEmailAddresses property.
    pub mentioned_email_addresses: Option<Vec<String>>,
    /// modifiedTime property.
    pub modified_time: Option<String>,
}

/// `GenerateCseTokenResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateCseTokenResponse {
    /// currentKaclsId property.
    pub current_kacls_id: Option<String>,
    /// currentKaclsName property.
    pub current_kacls_name: Option<String>,
    /// fileId property.
    pub file_id: Option<String>,
    /// jwt property.
    pub jwt: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Revision` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Revision {
    /// exportLinks property.
    pub export_links: Option<serde_json::Value>,
    /// id property.
    pub id: Option<String>,
    /// keepForever property.
    pub keep_forever: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// lastModifyingUser property.
    pub last_modifying_user: Option<User>,
    /// md5Checksum property.
    pub md5_checksum: Option<String>,
    /// mimeType property.
    pub mime_type: Option<String>,
    /// modifiedTime property.
    pub modified_time: Option<String>,
    /// originalFilename property.
    pub original_filename: Option<String>,
    /// publishAuto property.
    pub publish_auto: Option<bool>,
    /// published property.
    pub published: Option<bool>,
    /// publishedLink property.
    pub published_link: Option<String>,
    /// publishedOutsideDomain property.
    pub published_outside_domain: Option<bool>,
    /// size property.
    pub size: Option<String>,
}

/// `Approval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Approval {
    /// approvalId property.
    pub approval_id: Option<String>,
    /// completeTime property.
    pub complete_time: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// dueTime property.
    pub due_time: Option<String>,
    /// initiator property.
    pub initiator: Option<User>,
    /// kind property.
    pub kind: Option<String>,
    /// modifyTime property.
    pub modify_time: Option<String>,
    /// reviewerResponses property.
    pub reviewer_responses: Option<Vec<ReviewerResponse>>,
    /// status property.
    pub status: Option<String>,
    /// targetFileId property.
    pub target_file_id: Option<String>,
}

/// `AccessProposal` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccessProposal {
    /// createTime property.
    pub create_time: Option<String>,
    /// fileId property.
    pub file_id: Option<String>,
    /// proposalId property.
    pub proposal_id: Option<String>,
    /// recipientEmailAddress property.
    pub recipient_email_address: Option<String>,
    /// requestMessage property.
    pub request_message: Option<String>,
    /// requesterEmailAddress property.
    pub requester_email_address: Option<String>,
    /// rolesAndViews property.
    pub roles_and_views: Option<Vec<AccessProposalRoleAndView>>,
}

/// `Permission` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Permission {
    /// allowFileDiscovery property.
    pub allow_file_discovery: Option<bool>,
    /// deleted property.
    pub deleted: Option<bool>,
    /// displayName property.
    pub display_name: Option<String>,
    /// domain property.
    pub domain: Option<String>,
    /// emailAddress property.
    pub email_address: Option<String>,
    /// expirationTime property.
    pub expiration_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// inheritedPermissionsDisabled property.
    pub inherited_permissions_disabled: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// pendingOwner property.
    pub pending_owner: Option<bool>,
    /// permissionDetails property.
    pub permission_details: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// photoLink property.
    pub photo_link: Option<String>,
    /// role property.
    pub role: Option<String>,
    /// teamDrivePermissionDetails property.
    pub team_drive_permission_details:
        Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// type property.
    pub r#type: Option<String>,
    /// view property.
    pub view: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`drive.accessproposals.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveAccessproposalsGetArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
}

/// Arguments for [`drive.accessproposals.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveAccessproposalsListArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`drive.accessproposals.resolve_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveAccessproposalsResolveArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `proposalId`.
    pub proposal_id: String,
    /// Request body.
    pub body: ResolveAccessProposalRequest,
}

/// Arguments for [`drive.approvals.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveApprovalsGetArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `approvalId`.
    pub approval_id: String,
}

/// Arguments for [`drive.approvals.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveApprovalsListArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`drive.apps.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveAppsGetArgs {
    /// Path parameter: `appId`.
    pub app_id: String,
}

/// Arguments for [`drive.apps.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveAppsListArgs {
    /// Query parameter: `appFilterExtensions`.
    pub app_filter_extensions: Option<String>,
    /// Query parameter: `appFilterMimeTypes`.
    pub app_filter_mime_types: Option<String>,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`drive.changes.getStartPageToken_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveChangesGetStartPageTokenArgs {
    /// Query parameter: `driveId`.
    pub drive_id: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `teamDriveId`.
    pub team_drive_id: Option<String>,
}

/// Arguments for [`drive.changes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveChangesListArgs {
    /// Query parameter: `driveId`.
    pub drive_id: Option<String>,
    /// Query parameter: `includeCorpusRemovals`.
    pub include_corpus_removals: Option<String>,
    /// Query parameter: `includeItemsFromAllDrives`.
    pub include_items_from_all_drives: Option<String>,
    /// Query parameter: `includeLabels`.
    pub include_labels: Option<String>,
    /// Query parameter: `includePermissionsForView`.
    pub include_permissions_for_view: Option<String>,
    /// Query parameter: `includeRemoved`.
    pub include_removed: Option<String>,
    /// Query parameter: `includeTeamDriveItems`.
    pub include_team_drive_items: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `restrictToMyDrive`.
    pub restrict_to_my_drive: Option<String>,
    /// Query parameter: `spaces`.
    pub spaces: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `teamDriveId`.
    pub team_drive_id: Option<String>,
}

/// Arguments for [`drive.changes.watch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveChangesWatchArgs {
    /// Query parameter: `driveId`.
    pub drive_id: Option<String>,
    /// Query parameter: `includeCorpusRemovals`.
    pub include_corpus_removals: Option<String>,
    /// Query parameter: `includeItemsFromAllDrives`.
    pub include_items_from_all_drives: Option<String>,
    /// Query parameter: `includeLabels`.
    pub include_labels: Option<String>,
    /// Query parameter: `includePermissionsForView`.
    pub include_permissions_for_view: Option<String>,
    /// Query parameter: `includeRemoved`.
    pub include_removed: Option<String>,
    /// Query parameter: `includeTeamDriveItems`.
    pub include_team_drive_items: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `restrictToMyDrive`.
    pub restrict_to_my_drive: Option<String>,
    /// Query parameter: `spaces`.
    pub spaces: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `teamDriveId`.
    pub team_drive_id: Option<String>,
    /// Request body.
    pub body: Channel,
}

/// Arguments for [`drive.channels.stop_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveChannelsStopArgs {
    /// Request body.
    pub body: Channel,
}

/// Arguments for [`drive.comments.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveCommentsCreateArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Request body.
    pub body: Comment,
}

/// Arguments for [`drive.comments.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveCommentsDeleteArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `commentId`.
    pub comment_id: String,
}

/// Arguments for [`drive.comments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveCommentsGetArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `commentId`.
    pub comment_id: String,
    /// Query parameter: `includeDeleted`.
    pub include_deleted: Option<String>,
}

/// Arguments for [`drive.comments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveCommentsListArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `includeDeleted`.
    pub include_deleted: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `startModifiedTime`.
    pub start_modified_time: Option<String>,
}

/// Arguments for [`drive.comments.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveCommentsUpdateArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `commentId`.
    pub comment_id: String,
    /// Request body.
    pub body: Comment,
}

/// Arguments for [`drive.drives.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveDrivesCreateArgs {
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: Drive,
}

/// Arguments for [`drive.drives.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveDrivesDeleteArgs {
    /// Path parameter: `driveId`.
    pub drive_id: String,
    /// Query parameter: `allowItemDeletion`.
    pub allow_item_deletion: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.drives.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveDrivesGetArgs {
    /// Path parameter: `driveId`.
    pub drive_id: String,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.drives.hide_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveDrivesHideArgs {
    /// Path parameter: `driveId`.
    pub drive_id: String,
}

/// Arguments for [`drive.drives.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveDrivesListArgs {
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `q`.
    pub q: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.drives.unhide_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveDrivesUnhideArgs {
    /// Path parameter: `driveId`.
    pub drive_id: String,
}

/// Arguments for [`drive.drives.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveDrivesUpdateArgs {
    /// Path parameter: `driveId`.
    pub drive_id: String,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
    /// Request body.
    pub body: Drive,
}

/// Arguments for [`drive.files.copy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesCopyArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `enforceSingleParent`.
    pub enforce_single_parent: Option<String>,
    /// Query parameter: `ignoreDefaultVisibility`.
    pub ignore_default_visibility: Option<String>,
    /// Query parameter: `includeLabels`.
    pub include_labels: Option<String>,
    /// Query parameter: `includePermissionsForView`.
    pub include_permissions_for_view: Option<String>,
    /// Query parameter: `keepRevisionForever`.
    pub keep_revision_forever: Option<String>,
    /// Query parameter: `ocrLanguage`.
    pub ocr_language: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Request body.
    pub body: File,
}

/// Arguments for [`drive.files.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesCreateArgs {
    /// Query parameter: `enforceSingleParent`.
    pub enforce_single_parent: Option<String>,
    /// Query parameter: `ignoreDefaultVisibility`.
    pub ignore_default_visibility: Option<String>,
    /// Query parameter: `includeLabels`.
    pub include_labels: Option<String>,
    /// Query parameter: `includePermissionsForView`.
    pub include_permissions_for_view: Option<String>,
    /// Query parameter: `keepRevisionForever`.
    pub keep_revision_forever: Option<String>,
    /// Query parameter: `ocrLanguage`.
    pub ocr_language: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `useContentAsIndexableText`.
    pub use_content_as_indexable_text: Option<String>,
    /// Request body.
    pub body: File,
}

/// Arguments for [`drive.files.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesDeleteArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `enforceSingleParent`.
    pub enforce_single_parent: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
}

/// Arguments for [`drive.files.download_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesDownloadArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `mimeType`.
    pub mime_type: Option<String>,
    /// Query parameter: `revisionId`.
    pub revision_id: Option<String>,
}

/// Arguments for [`drive.files.emptyTrash_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesEmptyTrashArgs {
    /// Query parameter: `driveId`.
    pub drive_id: Option<String>,
    /// Query parameter: `enforceSingleParent`.
    pub enforce_single_parent: Option<String>,
}

/// Arguments for [`drive.files.export_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesExportArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `mimeType`.
    pub mime_type: Option<String>,
}

/// Arguments for [`drive.files.generateCseToken_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesGenerateCseTokenArgs {
    /// Query parameter: `fileId`.
    pub file_id: Option<String>,
    /// Query parameter: `parent`.
    pub parent: Option<String>,
}

/// Arguments for [`drive.files.generateIds_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesGenerateIdsArgs {
    /// Query parameter: `count`.
    pub count: Option<String>,
    /// Query parameter: `space`.
    pub space: Option<String>,
    /// Query parameter: `type`.
    pub r#type: Option<String>,
}

/// Arguments for [`drive.files.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesGetArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `acknowledgeAbuse`.
    pub acknowledge_abuse: Option<String>,
    /// Query parameter: `includeLabels`.
    pub include_labels: Option<String>,
    /// Query parameter: `includePermissionsForView`.
    pub include_permissions_for_view: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
}

/// Arguments for [`drive.files.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesListArgs {
    /// Query parameter: `corpora`.
    pub corpora: Option<String>,
    /// Query parameter: `corpus`.
    pub corpus: Option<String>,
    /// Query parameter: `driveId`.
    pub drive_id: Option<String>,
    /// Query parameter: `includeItemsFromAllDrives`.
    pub include_items_from_all_drives: Option<String>,
    /// Query parameter: `includeLabels`.
    pub include_labels: Option<String>,
    /// Query parameter: `includePermissionsForView`.
    pub include_permissions_for_view: Option<String>,
    /// Query parameter: `includeTeamDriveItems`.
    pub include_team_drive_items: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `q`.
    pub q: Option<String>,
    /// Query parameter: `spaces`.
    pub spaces: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `teamDriveId`.
    pub team_drive_id: Option<String>,
}

/// Arguments for [`drive.files.listLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesListLabelsArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`drive.files.modifyLabels_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesModifyLabelsArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Request body.
    pub body: ModifyLabelsRequest,
}

/// Arguments for [`drive.files.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesUpdateArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `addParents`.
    pub add_parents: Option<String>,
    /// Query parameter: `enforceSingleParent`.
    pub enforce_single_parent: Option<String>,
    /// Query parameter: `includeLabels`.
    pub include_labels: Option<String>,
    /// Query parameter: `includePermissionsForView`.
    pub include_permissions_for_view: Option<String>,
    /// Query parameter: `keepRevisionForever`.
    pub keep_revision_forever: Option<String>,
    /// Query parameter: `ocrLanguage`.
    pub ocr_language: Option<String>,
    /// Query parameter: `removeParents`.
    pub remove_parents: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `useContentAsIndexableText`.
    pub use_content_as_indexable_text: Option<String>,
    /// Request body.
    pub body: File,
}

/// Arguments for [`drive.files.watch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveFilesWatchArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `acknowledgeAbuse`.
    pub acknowledge_abuse: Option<String>,
    /// Query parameter: `includeLabels`.
    pub include_labels: Option<String>,
    /// Query parameter: `includePermissionsForView`.
    pub include_permissions_for_view: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Request body.
    pub body: Channel,
}

/// Arguments for [`drive.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`drive.permissions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivePermissionsCreateArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `emailMessage`.
    pub email_message: Option<String>,
    /// Query parameter: `enforceExpansiveAccess`.
    pub enforce_expansive_access: Option<String>,
    /// Query parameter: `enforceSingleParent`.
    pub enforce_single_parent: Option<String>,
    /// Query parameter: `moveToNewOwnersRoot`.
    pub move_to_new_owners_root: Option<String>,
    /// Query parameter: `sendNotificationEmail`.
    pub send_notification_email: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `transferOwnership`.
    pub transfer_ownership: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
    /// Request body.
    pub body: Permission,
}

/// Arguments for [`drive.permissions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivePermissionsDeleteArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `permissionId`.
    pub permission_id: String,
    /// Query parameter: `enforceExpansiveAccess`.
    pub enforce_expansive_access: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.permissions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivePermissionsGetArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `permissionId`.
    pub permission_id: String,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.permissions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivePermissionsListArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `includePermissionsForView`.
    pub include_permissions_for_view: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.permissions.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivePermissionsUpdateArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `permissionId`.
    pub permission_id: String,
    /// Query parameter: `enforceExpansiveAccess`.
    pub enforce_expansive_access: Option<String>,
    /// Query parameter: `removeExpiration`.
    pub remove_expiration: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `transferOwnership`.
    pub transfer_ownership: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
    /// Request body.
    pub body: Permission,
}

/// Arguments for [`drive.replies.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveRepliesCreateArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `commentId`.
    pub comment_id: String,
    /// Request body.
    pub body: Reply,
}

/// Arguments for [`drive.replies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveRepliesDeleteArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `commentId`.
    pub comment_id: String,
    /// Path parameter: `replyId`.
    pub reply_id: String,
}

/// Arguments for [`drive.replies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveRepliesGetArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `commentId`.
    pub comment_id: String,
    /// Path parameter: `replyId`.
    pub reply_id: String,
    /// Query parameter: `includeDeleted`.
    pub include_deleted: Option<String>,
}

/// Arguments for [`drive.replies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveRepliesListArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `commentId`.
    pub comment_id: String,
    /// Query parameter: `includeDeleted`.
    pub include_deleted: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`drive.replies.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveRepliesUpdateArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `commentId`.
    pub comment_id: String,
    /// Path parameter: `replyId`.
    pub reply_id: String,
    /// Request body.
    pub body: Reply,
}

/// Arguments for [`drive.revisions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveRevisionsDeleteArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `revisionId`.
    pub revision_id: String,
}

/// Arguments for [`drive.revisions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveRevisionsGetArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `revisionId`.
    pub revision_id: String,
    /// Query parameter: `acknowledgeAbuse`.
    pub acknowledge_abuse: Option<String>,
}

/// Arguments for [`drive.revisions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveRevisionsListArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`drive.revisions.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveRevisionsUpdateArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `revisionId`.
    pub revision_id: String,
    /// Request body.
    pub body: Revision,
}

/// Arguments for [`drive.teamdrives.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveTeamdrivesCreateArgs {
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Request body.
    pub body: TeamDrive,
}

/// Arguments for [`drive.teamdrives.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveTeamdrivesDeleteArgs {
    /// Path parameter: `teamDriveId`.
    pub team_drive_id: String,
}

/// Arguments for [`drive.teamdrives.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveTeamdrivesGetArgs {
    /// Path parameter: `teamDriveId`.
    pub team_drive_id: String,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.teamdrives.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveTeamdrivesListArgs {
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `q`.
    pub q: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.teamdrives.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DriveTeamdrivesUpdateArgs {
    /// Path parameter: `teamDriveId`.
    pub team_drive_id: String,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
    /// Request body.
    pub body: TeamDrive,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET about
// -----------------------------------------------------------------------------

/// GET about.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_about_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_about_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveAboutGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<About>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/about",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: About =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/accessproposals/{proposalId}
// -----------------------------------------------------------------------------

/// GET files/{fileId}/accessproposals/{proposalId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_accessproposals_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_accessproposals_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveAccessproposalsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccessProposal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/accessproposals/{}",
        args.file_id, args.proposal_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccessProposal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/accessproposals
// -----------------------------------------------------------------------------

/// GET files/{fileId}/accessproposals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_accessproposals_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_accessproposals_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveAccessproposalsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAccessProposalsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/accessproposals",
        args.file_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListAccessProposalsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST files/{fileId}/accessproposals/{proposalId}:resolve
// -----------------------------------------------------------------------------

/// POST files/{fileId}/accessproposals/{proposalId}:resolve.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_accessproposals_resolve_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_accessproposals_resolve_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveAccessproposalsResolveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/accessproposals/{}:resolve",
        args.file_id, args.proposal_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/approvals/{approvalId}
// -----------------------------------------------------------------------------

/// GET files/{fileId}/approvals/{approvalId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_approvals_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_approvals_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveApprovalsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Approval>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/approvals/{}",
        args.file_id, args.approval_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Approval =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/approvals
// -----------------------------------------------------------------------------

/// GET files/{fileId}/approvals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_approvals_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_approvals_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveApprovalsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ApprovalList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/approvals",
        args.file_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ApprovalList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET apps/{appId}
// -----------------------------------------------------------------------------

/// GET apps/{appId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_apps_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_apps_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveAppsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<App>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/apps/{}", args.app_id,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: App = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET apps
// -----------------------------------------------------------------------------

/// GET apps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_apps_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_apps_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveAppsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AppList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/apps",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.app_filter_extensions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("appFilterExtensions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.app_filter_mime_types {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("appFilterMimeTypes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AppList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET changes/startPageToken
// -----------------------------------------------------------------------------

/// GET changes/startPageToken.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_changes_get_start_page_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_changes_get_start_page_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveChangesGetStartPageTokenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StartPageToken>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/changes/startPageToken",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.drive_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("driveId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.team_drive_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("teamDriveId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: StartPageToken =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET changes
// -----------------------------------------------------------------------------

/// GET changes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_changes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_changes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveChangesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ChangeList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/changes",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.drive_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("driveId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_corpus_removals {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeCorpusRemovals=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_items_from_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeItemsFromAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_labels {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeLabels=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_permissions_for_view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includePermissionsForView=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_removed {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeRemoved=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_team_drive_items {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeTeamDriveItems=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.restrict_to_my_drive {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("restrictToMyDrive=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.spaces {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("spaces=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.team_drive_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("teamDriveId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ChangeList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST changes/watch
// -----------------------------------------------------------------------------

/// POST changes/watch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_changes_watch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_changes_watch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveChangesWatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/changes/watch",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.drive_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("driveId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_corpus_removals {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeCorpusRemovals=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_items_from_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeItemsFromAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_labels {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeLabels=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_permissions_for_view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includePermissionsForView=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_removed {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeRemoved=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_team_drive_items {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeTeamDriveItems=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.restrict_to_my_drive {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("restrictToMyDrive=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.spaces {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("spaces=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.team_drive_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("teamDriveId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Channel =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST channels/stop
// -----------------------------------------------------------------------------

/// POST channels/stop.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_channels_stop_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_channels_stop_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveChannelsStopArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/channels/stop",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST files/{fileId}/comments
// -----------------------------------------------------------------------------

/// POST files/{fileId}/comments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_comments_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_comments_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveCommentsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Comment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/comments",
        args.file_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Comment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE files/{fileId}/comments/{commentId}
// -----------------------------------------------------------------------------

/// DELETE files/{fileId}/comments/{commentId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_comments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_comments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveCommentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/comments/{}",
        args.file_id, args.comment_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/comments/{commentId}
// -----------------------------------------------------------------------------

/// GET files/{fileId}/comments/{commentId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_comments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_comments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveCommentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Comment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/comments/{}",
        args.file_id, args.comment_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.include_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Comment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/comments
// -----------------------------------------------------------------------------

/// GET files/{fileId}/comments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_comments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_comments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveCommentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommentList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/comments",
        args.file_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.include_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_modified_time {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startModifiedTime=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CommentList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH files/{fileId}/comments/{commentId}
// -----------------------------------------------------------------------------

/// PATCH files/{fileId}/comments/{commentId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_comments_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_comments_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveCommentsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Comment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/comments/{}",
        args.file_id, args.comment_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Comment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST drives
// -----------------------------------------------------------------------------

/// POST drives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_drives_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_drives_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveDrivesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Drive>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/drives",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Drive =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE drives/{driveId}
// -----------------------------------------------------------------------------

/// DELETE drives/{driveId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_drives_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_drives_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveDrivesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/drives/{}",
        args.drive_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_item_deletion {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowItemDeletion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET drives/{driveId}
// -----------------------------------------------------------------------------

/// GET drives/{driveId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_drives_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_drives_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveDrivesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Drive>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/drives/{}",
        args.drive_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Drive =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST drives/{driveId}/hide
// -----------------------------------------------------------------------------

/// POST drives/{driveId}/hide.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_drives_hide_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_drives_hide_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveDrivesHideArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Drive>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/drives/{}/hide",
        args.drive_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Drive =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET drives
// -----------------------------------------------------------------------------

/// GET drives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_drives_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_drives_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveDrivesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DriveList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/drives",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.q {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("q=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DriveList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST drives/{driveId}/unhide
// -----------------------------------------------------------------------------

/// POST drives/{driveId}/unhide.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_drives_unhide_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_drives_unhide_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveDrivesUnhideArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Drive>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/drives/{}/unhide",
        args.drive_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Drive =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH drives/{driveId}
// -----------------------------------------------------------------------------

/// PATCH drives/{driveId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_drives_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_drives_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveDrivesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Drive>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/drives/{}",
        args.drive_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Drive =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST files/{fileId}/copy
// -----------------------------------------------------------------------------

/// POST files/{fileId}/copy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_copy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_copy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesCopyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<File>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/copy",
        args.file_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.enforce_single_parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enforceSingleParent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.ignore_default_visibility {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ignoreDefaultVisibility=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_labels {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeLabels=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_permissions_for_view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includePermissionsForView=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.keep_revision_forever {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("keepRevisionForever=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.ocr_language {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ocrLanguage=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: File =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST files
// -----------------------------------------------------------------------------

/// POST files.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<File>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/files",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.enforce_single_parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enforceSingleParent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.ignore_default_visibility {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ignoreDefaultVisibility=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_labels {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeLabels=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_permissions_for_view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includePermissionsForView=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.keep_revision_forever {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("keepRevisionForever=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.ocr_language {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ocrLanguage=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_content_as_indexable_text {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useContentAsIndexableText=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: File =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE files/{fileId}
// -----------------------------------------------------------------------------

/// DELETE files/{fileId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/files/{}", args.file_id,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.enforce_single_parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enforceSingleParent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST files/{fileId}/download
// -----------------------------------------------------------------------------

/// POST files/{fileId}/download.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_download_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_download_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesDownloadArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/download",
        args.file_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.mime_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mimeType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.revision_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("revisionId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE files/trash
// -----------------------------------------------------------------------------

/// DELETE files/trash.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_empty_trash_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_empty_trash_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesEmptyTrashArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/files/trash",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.drive_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("driveId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.enforce_single_parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enforceSingleParent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/export
// -----------------------------------------------------------------------------

/// GET files/{fileId}/export.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_export_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_export_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesExportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/export",
        args.file_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.mime_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mimeType=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/generateCseToken
// -----------------------------------------------------------------------------

/// GET files/generateCseToken.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_generate_cse_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_generate_cse_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesGenerateCseTokenArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateCseTokenResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/files/generateCseToken",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.file_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("fileId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("parent=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenerateCseTokenResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/generateIds
// -----------------------------------------------------------------------------

/// GET files/generateIds.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_generate_ids_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_generate_ids_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesGenerateIdsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GeneratedIds>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/files/generateIds",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.count {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("count=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.space {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("space=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.r#type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("type=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GeneratedIds =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}
// -----------------------------------------------------------------------------

/// GET files/{fileId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<File>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/files/{}", args.file_id,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.acknowledge_abuse {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("acknowledgeAbuse=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_labels {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeLabels=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_permissions_for_view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includePermissionsForView=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: File =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files
// -----------------------------------------------------------------------------

/// GET files.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FileList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/files",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.corpora {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("corpora=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.corpus {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("corpus=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.drive_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("driveId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_items_from_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeItemsFromAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_labels {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeLabels=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_permissions_for_view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includePermissionsForView=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_team_drive_items {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeTeamDriveItems=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.q {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("q=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.spaces {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("spaces=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.team_drive_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("teamDriveId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FileList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/listLabels
// -----------------------------------------------------------------------------

/// GET files/{fileId}/listLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_list_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_list_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesListLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LabelList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/listLabels",
        args.file_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LabelList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST files/{fileId}/modifyLabels
// -----------------------------------------------------------------------------

/// POST files/{fileId}/modifyLabels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_modify_labels_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_modify_labels_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesModifyLabelsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ModifyLabelsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/modifyLabels",
        args.file_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ModifyLabelsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH files/{fileId}
// -----------------------------------------------------------------------------

/// PATCH files/{fileId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<File>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/files/{}", args.file_id,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.add_parents {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("addParents=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.enforce_single_parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enforceSingleParent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_labels {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeLabels=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_permissions_for_view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includePermissionsForView=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.keep_revision_forever {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("keepRevisionForever=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.ocr_language {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ocrLanguage=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.remove_parents {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("removeParents=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_content_as_indexable_text {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useContentAsIndexableText=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: File =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST files/{fileId}/watch
// -----------------------------------------------------------------------------

/// POST files/{fileId}/watch.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_files_watch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_files_watch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveFilesWatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/watch",
        args.file_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.acknowledge_abuse {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("acknowledgeAbuse=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_labels {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeLabels=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_permissions_for_view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includePermissionsForView=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Channel =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET operations/{name}
// -----------------------------------------------------------------------------

/// GET operations/{name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/operations/{}",
        args.name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST files/{fileId}/permissions
// -----------------------------------------------------------------------------

/// POST files/{fileId}/permissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_permissions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_permissions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivePermissionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Permission>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/permissions",
        args.file_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.email_message {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("emailMessage=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.enforce_expansive_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enforceExpansiveAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.enforce_single_parent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enforceSingleParent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.move_to_new_owners_root {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("moveToNewOwnersRoot=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.send_notification_email {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sendNotificationEmail=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.transfer_ownership {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("transferOwnership=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Permission =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE files/{fileId}/permissions/{permissionId}
// -----------------------------------------------------------------------------

/// DELETE files/{fileId}/permissions/{permissionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_permissions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_permissions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivePermissionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/permissions/{}",
        args.file_id, args.permission_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.enforce_expansive_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enforceExpansiveAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/permissions/{permissionId}
// -----------------------------------------------------------------------------

/// GET files/{fileId}/permissions/{permissionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_permissions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_permissions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivePermissionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Permission>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/permissions/{}",
        args.file_id, args.permission_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Permission =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/permissions
// -----------------------------------------------------------------------------

/// GET files/{fileId}/permissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_permissions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_permissions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivePermissionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PermissionList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/permissions",
        args.file_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.include_permissions_for_view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includePermissionsForView=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PermissionList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH files/{fileId}/permissions/{permissionId}
// -----------------------------------------------------------------------------

/// PATCH files/{fileId}/permissions/{permissionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_permissions_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_permissions_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivePermissionsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Permission>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/permissions/{}",
        args.file_id, args.permission_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.enforce_expansive_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enforceExpansiveAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.remove_expiration {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("removeExpiration=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_all_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsAllDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.supports_team_drives {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("supportsTeamDrives=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.transfer_ownership {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("transferOwnership=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Permission =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST files/{fileId}/comments/{commentId}/replies
// -----------------------------------------------------------------------------

/// POST files/{fileId}/comments/{commentId}/replies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_replies_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_replies_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveRepliesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reply>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/comments/{}/replies",
        args.file_id, args.comment_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Reply =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE files/{fileId}/comments/{commentId}/replies/{replyId}
// -----------------------------------------------------------------------------

/// DELETE files/{fileId}/comments/{commentId}/replies/{replyId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_replies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_replies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveRepliesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/comments/{}/replies/{}",
        args.file_id, args.comment_id, args.reply_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/comments/{commentId}/replies/{replyId}
// -----------------------------------------------------------------------------

/// GET files/{fileId}/comments/{commentId}/replies/{replyId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_replies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_replies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveRepliesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reply>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/comments/{}/replies/{}",
        args.file_id, args.comment_id, args.reply_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.include_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Reply =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/comments/{commentId}/replies
// -----------------------------------------------------------------------------

/// GET files/{fileId}/comments/{commentId}/replies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_replies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_replies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveRepliesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReplyList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/comments/{}/replies",
        args.file_id, args.comment_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.include_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReplyList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH files/{fileId}/comments/{commentId}/replies/{replyId}
// -----------------------------------------------------------------------------

/// PATCH files/{fileId}/comments/{commentId}/replies/{replyId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_replies_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_replies_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveRepliesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Reply>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/comments/{}/replies/{}",
        args.file_id, args.comment_id, args.reply_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Reply =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE files/{fileId}/revisions/{revisionId}
// -----------------------------------------------------------------------------

/// DELETE files/{fileId}/revisions/{revisionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_revisions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_revisions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveRevisionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/revisions/{}",
        args.file_id, args.revision_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/revisions/{revisionId}
// -----------------------------------------------------------------------------

/// GET files/{fileId}/revisions/{revisionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_revisions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_revisions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveRevisionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Revision>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/revisions/{}",
        args.file_id, args.revision_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.acknowledge_abuse {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("acknowledgeAbuse=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Revision =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/revisions
// -----------------------------------------------------------------------------

/// GET files/{fileId}/revisions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_revisions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_revisions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveRevisionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RevisionList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/revisions",
        args.file_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RevisionList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH files/{fileId}/revisions/{revisionId}
// -----------------------------------------------------------------------------

/// PATCH files/{fileId}/revisions/{revisionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_revisions_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_revisions_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveRevisionsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Revision>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/revisions/{}",
        args.file_id, args.revision_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Revision =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST teamdrives
// -----------------------------------------------------------------------------

/// POST teamdrives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_teamdrives_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_teamdrives_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveTeamdrivesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TeamDrive>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/teamdrives",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TeamDrive =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE teamdrives/{teamDriveId}
// -----------------------------------------------------------------------------

/// DELETE teamdrives/{teamDriveId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_teamdrives_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_teamdrives_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveTeamdrivesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/teamdrives/{}",
        args.team_drive_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET teamdrives/{teamDriveId}
// -----------------------------------------------------------------------------

/// GET teamdrives/{teamDriveId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_teamdrives_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_teamdrives_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveTeamdrivesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TeamDrive>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/teamdrives/{}",
        args.team_drive_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TeamDrive =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET teamdrives
// -----------------------------------------------------------------------------

/// GET teamdrives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_teamdrives_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_teamdrives_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveTeamdrivesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TeamDriveList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/drive/v3/teamdrives",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.q {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("q=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TeamDriveList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH teamdrives/{teamDriveId}
// -----------------------------------------------------------------------------

/// PATCH teamdrives/{teamDriveId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_teamdrives_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_teamdrives_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DriveTeamdrivesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TeamDrive>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/teamdrives/{}",
        args.team_drive_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.use_domain_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useDomainAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TeamDrive =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
