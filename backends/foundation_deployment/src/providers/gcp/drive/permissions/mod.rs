//! Auto-generated API module for gcp/drive permissions.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_drive_permissions `

#![cfg(feature = "gcp_drive_permissions")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `PermissionList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PermissionList {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// permissions property.
    pub permissions: Option<Vec<Permission>>,
}

/// `Permission` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Permission {
    /// allowFileDiscovery property.
    pub allow_file_discovery: Option<bool>,
    /// deleted property.
    pub deleted: Option<bool>,
    /// displayName property.
    pub display_name: Option<String>,
    /// domain property.
    pub domain: Option<String>,
    /// emailAddress property.
    pub email_address: Option<String>,
    /// expirationTime property.
    pub expiration_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// inheritedPermissionsDisabled property.
    pub inherited_permissions_disabled: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// pendingOwner property.
    pub pending_owner: Option<bool>,
    /// permissionDetails property.
    pub permission_details: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// photoLink property.
    pub photo_link: Option<String>,
    /// role property.
    pub role: Option<String>,
    /// teamDrivePermissionDetails property.
    pub team_drive_permission_details:
        Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// type property.
    pub r#type: Option<String>,
    /// view property.
    pub view: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`drive.permissions.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DrivePermissionsCreateArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `emailMessage`.
    pub email_message: Option<String>,
    /// Query parameter: `enforceExpansiveAccess`.
    pub enforce_expansive_access: Option<String>,
    /// Query parameter: `enforceSingleParent`.
    pub enforce_single_parent: Option<String>,
    /// Query parameter: `moveToNewOwnersRoot`.
    pub move_to_new_owners_root: Option<String>,
    /// Query parameter: `sendNotificationEmail`.
    pub send_notification_email: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `transferOwnership`.
    pub transfer_ownership: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.permissions.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DrivePermissionsDeleteArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `permissionId`.
    pub permission_id: String,
    /// Query parameter: `enforceExpansiveAccess`.
    pub enforce_expansive_access: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.permissions.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DrivePermissionsGetArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `permissionId`.
    pub permission_id: String,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.permissions.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DrivePermissionsListArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Query parameter: `includePermissionsForView`.
    pub include_permissions_for_view: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

/// Arguments for [`drive.permissions.update_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DrivePermissionsUpdateArgs {
    /// Path parameter: `fileId`.
    pub file_id: String,
    /// Path parameter: `permissionId`.
    pub permission_id: String,
    /// Query parameter: `enforceExpansiveAccess`.
    pub enforce_expansive_access: Option<String>,
    /// Query parameter: `removeExpiration`.
    pub remove_expiration: Option<String>,
    /// Query parameter: `supportsAllDrives`.
    pub supports_all_drives: Option<String>,
    /// Query parameter: `supportsTeamDrives`.
    pub supports_team_drives: Option<String>,
    /// Query parameter: `transferOwnership`.
    pub transfer_ownership: Option<String>,
    /// Query parameter: `useDomainAdminAccess`.
    pub use_domain_admin_access: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST files/{fileId}/permissions
// -----------------------------------------------------------------------------

/// POST files/{fileId}/permissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_permissions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_permissions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivePermissionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Permission>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/permissions",
        args.file_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Permission =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE files/{fileId}/permissions/{permissionId}
// -----------------------------------------------------------------------------

/// DELETE files/{fileId}/permissions/{permissionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_permissions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_permissions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivePermissionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/permissions/{}",
        args.file_id, args.permission_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/permissions/{permissionId}
// -----------------------------------------------------------------------------

/// GET files/{fileId}/permissions/{permissionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_permissions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_permissions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivePermissionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Permission>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/permissions/{}",
        args.file_id, args.permission_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Permission =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET files/{fileId}/permissions
// -----------------------------------------------------------------------------

/// GET files/{fileId}/permissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_permissions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_permissions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivePermissionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PermissionList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/permissions",
        args.file_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PermissionList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH files/{fileId}/permissions/{permissionId}
// -----------------------------------------------------------------------------

/// PATCH files/{fileId}/permissions/{permissionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drive_permissions_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drive_permissions_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivePermissionsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Permission>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/drive/v3/files/{}/permissions/{}",
        args.file_id, args.permission_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Permission =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
