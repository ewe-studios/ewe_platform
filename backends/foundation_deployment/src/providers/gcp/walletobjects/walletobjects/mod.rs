//! Auto-generated API module for gcp/walletobjects walletobjects.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_walletobjects_walletobjects `

#![cfg(feature = "gcp_walletobjects_walletobjects")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `OfferObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OfferObject {
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// barcode property.
    pub barcode: Option<Barcode>,
    /// classId property.
    pub class_id: Option<String>,
    /// classReference property.
    pub class_reference: Option<OfferClass>,
    /// disableExpirationNotification property.
    pub disable_expiration_notification: Option<bool>,
    /// groupingInfo property.
    pub grouping_info: Option<GroupingInfo>,
    /// hasLinkedDevice property.
    pub has_linked_device: Option<bool>,
    /// hasUsers property.
    pub has_users: Option<bool>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// kind property.
    pub kind: Option<String>,
    /// linkedObjectIds property.
    pub linked_object_ids: Option<Vec<String>>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// passConstraints property.
    pub pass_constraints: Option<PassConstraints>,
    /// rotatingBarcode property.
    pub rotating_barcode: Option<RotatingBarcode>,
    /// saveRestrictions property.
    pub save_restrictions: Option<SaveRestrictions>,
    /// smartTapRedemptionValue property.
    pub smart_tap_redemption_value: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// validTimeInterval property.
    pub valid_time_interval: Option<TimeInterval>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// version property.
    pub version: Option<String>,
}

/// `Image` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Image {
    /// contentDescription property.
    pub content_description: Option<LocalizedString>,
    /// kind property.
    pub kind: Option<String>,
    /// privateImageId property.
    pub private_image_id: Option<String>,
    /// sourceUri property.
    pub source_uri: Option<ImageUri>,
}

/// `LoyaltyClassListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoyaltyClassListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<LoyaltyClass>>,
}

/// `EventTicketClassListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventTicketClassListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<EventTicketClass>>,
}

/// `TranslatedString` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TranslatedString {
    /// kind property.
    pub kind: Option<String>,
    /// language property.
    pub language: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `OfferClassListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OfferClassListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<OfferClass>>,
}

/// `RotatingBarcodeTotpDetailsTotpParameters` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RotatingBarcodeTotpDetailsTotpParameters {
    /// key property.
    pub key: Option<String>,
    /// valueLength property.
    pub value_length: Option<i64>,
}

/// `GiftCardObjectListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GiftCardObjectListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<GiftCardObject>>,
}

/// `LabelValue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LabelValue {
    /// label property.
    pub label: Option<String>,
    /// localizedLabel property.
    pub localized_label: Option<LocalizedString>,
    /// localizedValue property.
    pub localized_value: Option<LocalizedString>,
    /// value property.
    pub value: Option<String>,
}

/// `TextModuleData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TextModuleData {
    /// body property.
    pub body: Option<String>,
    /// header property.
    pub header: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// localizedBody property.
    pub localized_body: Option<LocalizedString>,
    /// localizedHeader property.
    pub localized_header: Option<LocalizedString>,
}

/// `UpcomingNotification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpcomingNotification {
    /// enableNotification property.
    pub enable_notification: Option<bool>,
}

/// `DiffChecksumsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiffChecksumsResponse {
    /// checksumsLocation property.
    pub checksums_location: Option<CompositeMedia>,
    /// chunkSizeBytes property.
    pub chunk_size_bytes: Option<String>,
    /// objectLocation property.
    pub object_location: Option<CompositeMedia>,
    /// objectSizeBytes property.
    pub object_size_bytes: Option<String>,
    /// objectVersion property.
    pub object_version: Option<String>,
}

/// `Permissions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Permissions {
    /// issuerId property.
    pub issuer_id: Option<String>,
    /// permissions property.
    pub permissions: Option<Vec<Permission>>,
}

/// `FlightClass` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlightClass {
    /// allowMultipleUsersPerObject property.
    pub allow_multiple_users_per_object: Option<bool>,
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// boardingAndSeatingPolicy property.
    pub boarding_and_seating_policy: Option<BoardingAndSeatingPolicy>,
    /// callbackOptions property.
    pub callback_options: Option<CallbackOptions>,
    /// classTemplateInfo property.
    pub class_template_info: Option<ClassTemplateInfo>,
    /// countryCode property.
    pub country_code: Option<String>,
    /// destination property.
    pub destination: Option<AirportInfo>,
    /// enableSmartTap property.
    pub enable_smart_tap: Option<bool>,
    /// flightHeader property.
    pub flight_header: Option<FlightHeader>,
    /// flightStatus property.
    pub flight_status: Option<String>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// hexBackgroundColor property.
    pub hex_background_color: Option<String>,
    /// homepageUri property.
    pub homepage_uri: Option<Uri>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// issuerName property.
    pub issuer_name: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// languageOverride property.
    pub language_override: Option<String>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// localBoardingDateTime property.
    pub local_boarding_date_time: Option<String>,
    /// localEstimatedOrActualArrivalDateTime property.
    pub local_estimated_or_actual_arrival_date_time: Option<String>,
    /// localEstimatedOrActualDepartureDateTime property.
    pub local_estimated_or_actual_departure_date_time: Option<String>,
    /// localGateClosingDateTime property.
    pub local_gate_closing_date_time: Option<String>,
    /// localScheduledArrivalDateTime property.
    pub local_scheduled_arrival_date_time: Option<String>,
    /// localScheduledDepartureDateTime property.
    pub local_scheduled_departure_date_time: Option<String>,
    /// localizedIssuerName property.
    pub localized_issuer_name: Option<LocalizedString>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// multipleDevicesAndHoldersAllowedStatus property.
    pub multiple_devices_and_holders_allowed_status: Option<String>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// origin property.
    pub origin: Option<AirportInfo>,
    /// redemptionIssuers property.
    pub redemption_issuers: Option<Vec<String>>,
    /// review property.
    pub review: Option<Review>,
    /// reviewStatus property.
    pub review_status: Option<String>,
    /// securityAnimation property.
    pub security_animation: Option<SecurityAnimation>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// version property.
    pub version: Option<String>,
    /// viewUnlockRequirement property.
    pub view_unlock_requirement: Option<String>,
    /// wordMark property.
    pub word_mark: Option<Image>,
}

/// `CardRowTemplateInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CardRowTemplateInfo {
    /// oneItem property.
    pub one_item: Option<CardRowOneItem>,
    /// threeItems property.
    pub three_items: Option<CardRowThreeItems>,
    /// twoItems property.
    pub two_items: Option<CardRowTwoItems>,
}

/// `OfferClassAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OfferClassAddMessageResponse {
    /// resource property.
    pub resource: Option<OfferClass>,
}

/// `AppLinkDataAppLinkInfoAppTarget` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppLinkDataAppLinkInfoAppTarget {
    /// packageName property.
    pub package_name: Option<String>,
    /// targetUri property.
    pub target_uri: Option<Uri>,
}

/// `LocalizedString` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalizedString {
    /// defaultValue property.
    pub default_value: Option<TranslatedString>,
    /// kind property.
    pub kind: Option<String>,
    /// translatedValues property.
    pub translated_values: Option<Vec<TranslatedString>>,
}

/// `DeviceContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeviceContext {
    /// deviceToken property.
    pub device_token: Option<String>,
}

/// `GenericObjectListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenericObjectListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<GenericObject>>,
}

/// `MerchantLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MerchantLocation {
    /// latitude property.
    pub latitude: Option<f64>,
    /// longitude property.
    pub longitude: Option<f64>,
}

/// `DiscoverableProgramMerchantSigninInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiscoverableProgramMerchantSigninInfo {
    /// signinWebsite property.
    pub signin_website: Option<Uri>,
}

/// `GiftCardClass` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GiftCardClass {
    /// allowBarcodeRedemption property.
    pub allow_barcode_redemption: Option<bool>,
    /// allowMultipleUsersPerObject property.
    pub allow_multiple_users_per_object: Option<bool>,
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// callbackOptions property.
    pub callback_options: Option<CallbackOptions>,
    /// cardNumberLabel property.
    pub card_number_label: Option<String>,
    /// classTemplateInfo property.
    pub class_template_info: Option<ClassTemplateInfo>,
    /// countryCode property.
    pub country_code: Option<String>,
    /// enableSmartTap property.
    pub enable_smart_tap: Option<bool>,
    /// eventNumberLabel property.
    pub event_number_label: Option<String>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// hexBackgroundColor property.
    pub hex_background_color: Option<String>,
    /// homepageUri property.
    pub homepage_uri: Option<Uri>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// issuerName property.
    pub issuer_name: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// localizedCardNumberLabel property.
    pub localized_card_number_label: Option<LocalizedString>,
    /// localizedEventNumberLabel property.
    pub localized_event_number_label: Option<LocalizedString>,
    /// localizedIssuerName property.
    pub localized_issuer_name: Option<LocalizedString>,
    /// localizedMerchantName property.
    pub localized_merchant_name: Option<LocalizedString>,
    /// localizedPinLabel property.
    pub localized_pin_label: Option<LocalizedString>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// merchantName property.
    pub merchant_name: Option<String>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// multipleDevicesAndHoldersAllowedStatus property.
    pub multiple_devices_and_holders_allowed_status: Option<String>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// pinLabel property.
    pub pin_label: Option<String>,
    /// programLogo property.
    pub program_logo: Option<Image>,
    /// redemptionIssuers property.
    pub redemption_issuers: Option<Vec<String>>,
    /// review property.
    pub review: Option<Review>,
    /// reviewStatus property.
    pub review_status: Option<String>,
    /// securityAnimation property.
    pub security_animation: Option<SecurityAnimation>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// version property.
    pub version: Option<String>,
    /// viewUnlockRequirement property.
    pub view_unlock_requirement: Option<String>,
    /// wideProgramLogo property.
    pub wide_program_logo: Option<Image>,
    /// wordMark property.
    pub word_mark: Option<Image>,
}

/// `LoyaltyClass` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoyaltyClass {
    /// accountIdLabel property.
    pub account_id_label: Option<String>,
    /// accountNameLabel property.
    pub account_name_label: Option<String>,
    /// allowMultipleUsersPerObject property.
    pub allow_multiple_users_per_object: Option<bool>,
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// callbackOptions property.
    pub callback_options: Option<CallbackOptions>,
    /// classTemplateInfo property.
    pub class_template_info: Option<ClassTemplateInfo>,
    /// countryCode property.
    pub country_code: Option<String>,
    /// discoverableProgram property.
    pub discoverable_program: Option<DiscoverableProgram>,
    /// enableSmartTap property.
    pub enable_smart_tap: Option<bool>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// hexBackgroundColor property.
    pub hex_background_color: Option<String>,
    /// homepageUri property.
    pub homepage_uri: Option<Uri>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// issuerName property.
    pub issuer_name: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// localizedAccountIdLabel property.
    pub localized_account_id_label: Option<LocalizedString>,
    /// localizedAccountNameLabel property.
    pub localized_account_name_label: Option<LocalizedString>,
    /// localizedIssuerName property.
    pub localized_issuer_name: Option<LocalizedString>,
    /// localizedProgramName property.
    pub localized_program_name: Option<LocalizedString>,
    /// localizedRewardsTier property.
    pub localized_rewards_tier: Option<LocalizedString>,
    /// localizedRewardsTierLabel property.
    pub localized_rewards_tier_label: Option<LocalizedString>,
    /// localizedSecondaryRewardsTier property.
    pub localized_secondary_rewards_tier: Option<LocalizedString>,
    /// localizedSecondaryRewardsTierLabel property.
    pub localized_secondary_rewards_tier_label: Option<LocalizedString>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// multipleDevicesAndHoldersAllowedStatus property.
    pub multiple_devices_and_holders_allowed_status: Option<String>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// programLogo property.
    pub program_logo: Option<Image>,
    /// programName property.
    pub program_name: Option<String>,
    /// redemptionIssuers property.
    pub redemption_issuers: Option<Vec<String>>,
    /// review property.
    pub review: Option<Review>,
    /// reviewStatus property.
    pub review_status: Option<String>,
    /// rewardsTier property.
    pub rewards_tier: Option<String>,
    /// rewardsTierLabel property.
    pub rewards_tier_label: Option<String>,
    /// secondaryRewardsTier property.
    pub secondary_rewards_tier: Option<String>,
    /// secondaryRewardsTierLabel property.
    pub secondary_rewards_tier_label: Option<String>,
    /// securityAnimation property.
    pub security_animation: Option<SecurityAnimation>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// version property.
    pub version: Option<String>,
    /// viewUnlockRequirement property.
    pub view_unlock_requirement: Option<String>,
    /// wideProgramLogo property.
    pub wide_program_logo: Option<Image>,
    /// wordMark property.
    pub word_mark: Option<Image>,
}

/// `OfferClass` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OfferClass {
    /// allowMultipleUsersPerObject property.
    pub allow_multiple_users_per_object: Option<bool>,
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// callbackOptions property.
    pub callback_options: Option<CallbackOptions>,
    /// classTemplateInfo property.
    pub class_template_info: Option<ClassTemplateInfo>,
    /// countryCode property.
    pub country_code: Option<String>,
    /// details property.
    pub details: Option<String>,
    /// enableSmartTap property.
    pub enable_smart_tap: Option<bool>,
    /// finePrint property.
    pub fine_print: Option<String>,
    /// helpUri property.
    pub help_uri: Option<Uri>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// hexBackgroundColor property.
    pub hex_background_color: Option<String>,
    /// homepageUri property.
    pub homepage_uri: Option<Uri>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// issuerName property.
    pub issuer_name: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// localizedDetails property.
    pub localized_details: Option<LocalizedString>,
    /// localizedFinePrint property.
    pub localized_fine_print: Option<LocalizedString>,
    /// localizedIssuerName property.
    pub localized_issuer_name: Option<LocalizedString>,
    /// localizedProvider property.
    pub localized_provider: Option<LocalizedString>,
    /// localizedShortTitle property.
    pub localized_short_title: Option<LocalizedString>,
    /// localizedTitle property.
    pub localized_title: Option<LocalizedString>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// multipleDevicesAndHoldersAllowedStatus property.
    pub multiple_devices_and_holders_allowed_status: Option<String>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// provider property.
    pub provider: Option<String>,
    /// redemptionChannel property.
    pub redemption_channel: Option<String>,
    /// redemptionIssuers property.
    pub redemption_issuers: Option<Vec<String>>,
    /// review property.
    pub review: Option<Review>,
    /// reviewStatus property.
    pub review_status: Option<String>,
    /// securityAnimation property.
    pub security_animation: Option<SecurityAnimation>,
    /// shortTitle property.
    pub short_title: Option<String>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// title property.
    pub title: Option<String>,
    /// titleImage property.
    pub title_image: Option<Image>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// version property.
    pub version: Option<String>,
    /// viewUnlockRequirement property.
    pub view_unlock_requirement: Option<String>,
    /// wideTitleImage property.
    pub wide_title_image: Option<Image>,
    /// wordMark property.
    pub word_mark: Option<Image>,
}

/// `Issuer` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Issuer {
    /// callbackOptions property.
    pub callback_options: Option<CallbackOptions>,
    /// contactInfo property.
    pub contact_info: Option<IssuerContactInfo>,
    /// homepageUrl property.
    pub homepage_url: Option<String>,
    /// issuerId property.
    pub issuer_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// smartTapMerchantData property.
    pub smart_tap_merchant_data: Option<SmartTapMerchantData>,
}

/// `FieldReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FieldReference {
    /// dateFormat property.
    pub date_format: Option<String>,
    /// fieldPath property.
    pub field_path: Option<String>,
}

/// `OfferObjectAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OfferObjectAddMessageResponse {
    /// resource property.
    pub resource: Option<OfferObject>,
}

/// `SetPassUpdateNoticeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetPassUpdateNoticeRequest {
    /// externalPassId property.
    pub external_pass_id: Option<String>,
    /// updateUri property.
    pub update_uri: Option<String>,
    /// updatedPassJwtSignature property.
    pub updated_pass_jwt_signature: Option<String>,
}

/// `TransitClass` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransitClass {
    /// activationOptions property.
    pub activation_options: Option<ActivationOptions>,
    /// allowMultipleUsersPerObject property.
    pub allow_multiple_users_per_object: Option<bool>,
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// callbackOptions property.
    pub callback_options: Option<CallbackOptions>,
    /// classTemplateInfo property.
    pub class_template_info: Option<ClassTemplateInfo>,
    /// countryCode property.
    pub country_code: Option<String>,
    /// customCarriageLabel property.
    pub custom_carriage_label: Option<LocalizedString>,
    /// customCoachLabel property.
    pub custom_coach_label: Option<LocalizedString>,
    /// customConcessionCategoryLabel property.
    pub custom_concession_category_label: Option<LocalizedString>,
    /// customConfirmationCodeLabel property.
    pub custom_confirmation_code_label: Option<LocalizedString>,
    /// customDiscountMessageLabel property.
    pub custom_discount_message_label: Option<LocalizedString>,
    /// customFareClassLabel property.
    pub custom_fare_class_label: Option<LocalizedString>,
    /// customFareNameLabel property.
    pub custom_fare_name_label: Option<LocalizedString>,
    /// customOtherRestrictionsLabel property.
    pub custom_other_restrictions_label: Option<LocalizedString>,
    /// customPlatformLabel property.
    pub custom_platform_label: Option<LocalizedString>,
    /// customPurchaseFaceValueLabel property.
    pub custom_purchase_face_value_label: Option<LocalizedString>,
    /// customPurchasePriceLabel property.
    pub custom_purchase_price_label: Option<LocalizedString>,
    /// customPurchaseReceiptNumberLabel property.
    pub custom_purchase_receipt_number_label: Option<LocalizedString>,
    /// customRouteRestrictionsDetailsLabel property.
    pub custom_route_restrictions_details_label: Option<LocalizedString>,
    /// customRouteRestrictionsLabel property.
    pub custom_route_restrictions_label: Option<LocalizedString>,
    /// customSeatLabel property.
    pub custom_seat_label: Option<LocalizedString>,
    /// customTicketNumberLabel property.
    pub custom_ticket_number_label: Option<LocalizedString>,
    /// customTimeRestrictionsLabel property.
    pub custom_time_restrictions_label: Option<LocalizedString>,
    /// customTransitTerminusNameLabel property.
    pub custom_transit_terminus_name_label: Option<LocalizedString>,
    /// customZoneLabel property.
    pub custom_zone_label: Option<LocalizedString>,
    /// enableSingleLegItinerary property.
    pub enable_single_leg_itinerary: Option<bool>,
    /// enableSmartTap property.
    pub enable_smart_tap: Option<bool>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// hexBackgroundColor property.
    pub hex_background_color: Option<String>,
    /// homepageUri property.
    pub homepage_uri: Option<Uri>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// issuerName property.
    pub issuer_name: Option<String>,
    /// languageOverride property.
    pub language_override: Option<String>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// localizedIssuerName property.
    pub localized_issuer_name: Option<LocalizedString>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// logo property.
    pub logo: Option<Image>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// multipleDevicesAndHoldersAllowedStatus property.
    pub multiple_devices_and_holders_allowed_status: Option<String>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// redemptionIssuers property.
    pub redemption_issuers: Option<Vec<String>>,
    /// review property.
    pub review: Option<Review>,
    /// reviewStatus property.
    pub review_status: Option<String>,
    /// securityAnimation property.
    pub security_animation: Option<SecurityAnimation>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// transitOperatorName property.
    pub transit_operator_name: Option<LocalizedString>,
    /// transitType property.
    pub transit_type: Option<String>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// version property.
    pub version: Option<String>,
    /// viewUnlockRequirement property.
    pub view_unlock_requirement: Option<String>,
    /// watermark property.
    pub watermark: Option<Image>,
    /// wideLogo property.
    pub wide_logo: Option<Image>,
    /// wordMark property.
    pub word_mark: Option<Image>,
}

/// `ObjectId` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObjectId {
    /// bucketName property.
    pub bucket_name: Option<String>,
    /// generation property.
    pub generation: Option<String>,
    /// objectName property.
    pub object_name: Option<String>,
}

/// `Media` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Media {
    /// algorithm property.
    pub algorithm: Option<String>,
    /// bigstoreObjectRef property.
    pub bigstore_object_ref: Option<String>,
    /// blobRef property.
    pub blob_ref: Option<String>,
    /// blobstore2Info property.
    pub blobstore2_info: Option<Blobstore2Info>,
    /// compositeMedia property.
    pub composite_media: Option<Vec<CompositeMedia>>,
    /// contentType property.
    pub content_type: Option<String>,
    /// contentTypeInfo property.
    pub content_type_info: Option<ContentTypeInfo>,
    /// cosmoBinaryReference property.
    pub cosmo_binary_reference: Option<String>,
    /// crc32cHash property.
    pub crc32c_hash: Option<i64>,
    /// diffChecksumsResponse property.
    pub diff_checksums_response: Option<DiffChecksumsResponse>,
    /// diffDownloadResponse property.
    pub diff_download_response: Option<DiffDownloadResponse>,
    /// diffUploadRequest property.
    pub diff_upload_request: Option<DiffUploadRequest>,
    /// diffUploadResponse property.
    pub diff_upload_response: Option<DiffUploadResponse>,
    /// diffVersionResponse property.
    pub diff_version_response: Option<DiffVersionResponse>,
    /// downloadParameters property.
    pub download_parameters: Option<DownloadParameters>,
    /// filename property.
    pub filename: Option<String>,
    /// hash property.
    pub hash: Option<String>,
    /// hashVerified property.
    pub hash_verified: Option<bool>,
    /// inline property.
    pub inline: Option<String>,
    /// isPotentialRetry property.
    pub is_potential_retry: Option<bool>,
    /// length property.
    pub length: Option<String>,
    /// md5Hash property.
    pub md5_hash: Option<String>,
    /// mediaId property.
    pub media_id: Option<String>,
    /// objectId property.
    pub object_id: Option<ObjectId>,
    /// path property.
    pub path: Option<String>,
    /// referenceType property.
    pub reference_type: Option<String>,
    /// sha1Hash property.
    pub sha1_hash: Option<String>,
    /// sha256Hash property.
    pub sha256_hash: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
    /// token property.
    pub token: Option<String>,
}

/// `JwtResource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JwtResource {
    /// jwt property.
    pub jwt: Option<String>,
}

/// `TransitObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransitObject {
    /// activationStatus property.
    pub activation_status: Option<ActivationStatus>,
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// barcode property.
    pub barcode: Option<Barcode>,
    /// classId property.
    pub class_id: Option<String>,
    /// classReference property.
    pub class_reference: Option<TransitClass>,
    /// concessionCategory property.
    pub concession_category: Option<String>,
    /// customConcessionCategory property.
    pub custom_concession_category: Option<LocalizedString>,
    /// customTicketStatus property.
    pub custom_ticket_status: Option<LocalizedString>,
    /// deviceContext property.
    pub device_context: Option<DeviceContext>,
    /// disableExpirationNotification property.
    pub disable_expiration_notification: Option<bool>,
    /// groupingInfo property.
    pub grouping_info: Option<GroupingInfo>,
    /// hasLinkedDevice property.
    pub has_linked_device: Option<bool>,
    /// hasUsers property.
    pub has_users: Option<bool>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// hexBackgroundColor property.
    pub hex_background_color: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// linkedObjectIds property.
    pub linked_object_ids: Option<Vec<String>>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// passConstraints property.
    pub pass_constraints: Option<PassConstraints>,
    /// passengerNames property.
    pub passenger_names: Option<String>,
    /// passengerType property.
    pub passenger_type: Option<String>,
    /// purchaseDetails property.
    pub purchase_details: Option<PurchaseDetails>,
    /// rotatingBarcode property.
    pub rotating_barcode: Option<RotatingBarcode>,
    /// saveRestrictions property.
    pub save_restrictions: Option<SaveRestrictions>,
    /// smartTapRedemptionValue property.
    pub smart_tap_redemption_value: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// ticketLeg property.
    pub ticket_leg: Option<TicketLeg>,
    /// ticketLegs property.
    pub ticket_legs: Option<Vec<TicketLeg>>,
    /// ticketNumber property.
    pub ticket_number: Option<String>,
    /// ticketRestrictions property.
    pub ticket_restrictions: Option<TicketRestrictions>,
    /// ticketStatus property.
    pub ticket_status: Option<String>,
    /// tripId property.
    pub trip_id: Option<String>,
    /// tripType property.
    pub trip_type: Option<String>,
    /// validTimeInterval property.
    pub valid_time_interval: Option<TimeInterval>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// version property.
    pub version: Option<String>,
}

/// `EventReservationInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventReservationInfo {
    /// confirmationCode property.
    pub confirmation_code: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `TicketRestrictions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TicketRestrictions {
    /// otherRestrictions property.
    pub other_restrictions: Option<LocalizedString>,
    /// routeRestrictions property.
    pub route_restrictions: Option<LocalizedString>,
    /// routeRestrictionsDetails property.
    pub route_restrictions_details: Option<LocalizedString>,
    /// timeRestrictions property.
    pub time_restrictions: Option<LocalizedString>,
}

/// `FieldSelector` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FieldSelector {
    /// fields property.
    pub fields: Option<Vec<FieldReference>>,
}

/// `OfferObjectListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OfferObjectListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<OfferObject>>,
}

/// `DiffDownloadResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiffDownloadResponse {
    /// objectLocation property.
    pub object_location: Option<CompositeMedia>,
}

/// `EventTicketClassAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventTicketClassAddMessageResponse {
    /// resource property.
    pub resource: Option<EventTicketClass>,
}

/// `SignUpInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SignUpInfo {
    /// classId property.
    pub class_id: Option<String>,
}

/// `TransitClassAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransitClassAddMessageResponse {
    /// resource property.
    pub resource: Option<TransitClass>,
}

/// `DownloadParameters` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DownloadParameters {
    /// allowGzipCompression property.
    pub allow_gzip_compression: Option<bool>,
    /// ignoreRange property.
    pub ignore_range: Option<bool>,
}

/// `SmartTap` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SmartTap {
    /// id property.
    pub id: Option<String>,
    /// infos property.
    pub infos: Option<Vec<IssuerToUserInfo>>,
    /// kind property.
    pub kind: Option<String>,
    /// merchantId property.
    pub merchant_id: Option<String>,
}

/// `DateTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DateTime {
    /// date property.
    pub date: Option<String>,
}

/// `AuthenticationKey` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuthenticationKey {
    /// id property.
    pub id: Option<i64>,
    /// publicKeyPem property.
    pub public_key_pem: Option<String>,
}

/// `IssuerToUserInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IssuerToUserInfo {
    /// action property.
    pub action: Option<String>,
    /// signUpInfo property.
    pub sign_up_info: Option<SignUpInfo>,
    /// url property.
    pub url: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `ActivationStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivationStatus {
    /// state property.
    pub state: Option<String>,
}

/// `SecurityAnimation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityAnimation {
    /// animationType property.
    pub animation_type: Option<String>,
}

/// `EventTicketObjectListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventTicketObjectListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<EventTicketObject>>,
}

/// `SmartTapMerchantData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SmartTapMerchantData {
    /// authenticationKeys property.
    pub authentication_keys: Option<Vec<AuthenticationKey>>,
    /// smartTapMerchantId property.
    pub smart_tap_merchant_id: Option<String>,
}

/// `Notifications` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Notifications {
    /// expiryNotification property.
    pub expiry_notification: Option<ExpiryNotification>,
    /// upcomingNotification property.
    pub upcoming_notification: Option<UpcomingNotification>,
}

/// `FlightClassListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlightClassListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<FlightClass>>,
}

/// `EventTicketClass` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventTicketClass {
    /// allowMultipleUsersPerObject property.
    pub allow_multiple_users_per_object: Option<bool>,
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// callbackOptions property.
    pub callback_options: Option<CallbackOptions>,
    /// classTemplateInfo property.
    pub class_template_info: Option<ClassTemplateInfo>,
    /// confirmationCodeLabel property.
    pub confirmation_code_label: Option<String>,
    /// countryCode property.
    pub country_code: Option<String>,
    /// customConfirmationCodeLabel property.
    pub custom_confirmation_code_label: Option<LocalizedString>,
    /// customGateLabel property.
    pub custom_gate_label: Option<LocalizedString>,
    /// customRowLabel property.
    pub custom_row_label: Option<LocalizedString>,
    /// customSeatLabel property.
    pub custom_seat_label: Option<LocalizedString>,
    /// customSectionLabel property.
    pub custom_section_label: Option<LocalizedString>,
    /// dateTime property.
    pub date_time: Option<EventDateTime>,
    /// enableSmartTap property.
    pub enable_smart_tap: Option<bool>,
    /// eventId property.
    pub event_id: Option<String>,
    /// eventName property.
    pub event_name: Option<LocalizedString>,
    /// finePrint property.
    pub fine_print: Option<LocalizedString>,
    /// gateLabel property.
    pub gate_label: Option<String>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// hexBackgroundColor property.
    pub hex_background_color: Option<String>,
    /// homepageUri property.
    pub homepage_uri: Option<Uri>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// issuerName property.
    pub issuer_name: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// localizedIssuerName property.
    pub localized_issuer_name: Option<LocalizedString>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// logo property.
    pub logo: Option<Image>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// multipleDevicesAndHoldersAllowedStatus property.
    pub multiple_devices_and_holders_allowed_status: Option<String>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// redemptionIssuers property.
    pub redemption_issuers: Option<Vec<String>>,
    /// review property.
    pub review: Option<Review>,
    /// reviewStatus property.
    pub review_status: Option<String>,
    /// rowLabel property.
    pub row_label: Option<String>,
    /// seatLabel property.
    pub seat_label: Option<String>,
    /// sectionLabel property.
    pub section_label: Option<String>,
    /// securityAnimation property.
    pub security_animation: Option<SecurityAnimation>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// venue property.
    pub venue: Option<EventVenue>,
    /// version property.
    pub version: Option<String>,
    /// viewUnlockRequirement property.
    pub view_unlock_requirement: Option<String>,
    /// wideLogo property.
    pub wide_logo: Option<Image>,
    /// wordMark property.
    pub word_mark: Option<Image>,
}

/// `GenericClassAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenericClassAddMessageResponse {
    /// resource property.
    pub resource: Option<GenericClass>,
}

/// `TicketLeg` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TicketLeg {
    /// arrivalDateTime property.
    pub arrival_date_time: Option<String>,
    /// carriage property.
    pub carriage: Option<String>,
    /// departureDateTime property.
    pub departure_date_time: Option<String>,
    /// destinationName property.
    pub destination_name: Option<LocalizedString>,
    /// destinationStationCode property.
    pub destination_station_code: Option<String>,
    /// fareName property.
    pub fare_name: Option<LocalizedString>,
    /// originName property.
    pub origin_name: Option<LocalizedString>,
    /// originStationCode property.
    pub origin_station_code: Option<String>,
    /// platform property.
    pub platform: Option<String>,
    /// ticketSeat property.
    pub ticket_seat: Option<TicketSeat>,
    /// ticketSeats property.
    pub ticket_seats: Option<Vec<TicketSeat>>,
    /// transitOperatorName property.
    pub transit_operator_name: Option<LocalizedString>,
    /// transitTerminusName property.
    pub transit_terminus_name: Option<LocalizedString>,
    /// zone property.
    pub zone: Option<String>,
}

/// `CardRowTwoItems` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CardRowTwoItems {
    /// endItem property.
    pub end_item: Option<TemplateItem>,
    /// startItem property.
    pub start_item: Option<TemplateItem>,
}

/// `GiftCardClassListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GiftCardClassListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<GiftCardClass>>,
}

/// `TransitObjectUploadRotatingBarcodeValuesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransitObjectUploadRotatingBarcodeValuesRequest {
    /// blob property.
    pub blob: Option<Media>,
    /// mediaRequestInfo property.
    pub media_request_info: Option<MediaRequestInfo>,
}

/// `EventTicketObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventTicketObject {
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// barcode property.
    pub barcode: Option<Barcode>,
    /// classId property.
    pub class_id: Option<String>,
    /// classReference property.
    pub class_reference: Option<EventTicketClass>,
    /// disableExpirationNotification property.
    pub disable_expiration_notification: Option<bool>,
    /// faceValue property.
    pub face_value: Option<Money>,
    /// groupingInfo property.
    pub grouping_info: Option<GroupingInfo>,
    /// hasLinkedDevice property.
    pub has_linked_device: Option<bool>,
    /// hasUsers property.
    pub has_users: Option<bool>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// hexBackgroundColor property.
    pub hex_background_color: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// kind property.
    pub kind: Option<String>,
    /// linkedObjectIds property.
    pub linked_object_ids: Option<Vec<String>>,
    /// linkedOfferIds property.
    pub linked_offer_ids: Option<Vec<String>>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// passConstraints property.
    pub pass_constraints: Option<PassConstraints>,
    /// reservationInfo property.
    pub reservation_info: Option<EventReservationInfo>,
    /// rotatingBarcode property.
    pub rotating_barcode: Option<RotatingBarcode>,
    /// saveRestrictions property.
    pub save_restrictions: Option<SaveRestrictions>,
    /// seatInfo property.
    pub seat_info: Option<EventSeat>,
    /// smartTapRedemptionValue property.
    pub smart_tap_redemption_value: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// ticketHolderName property.
    pub ticket_holder_name: Option<String>,
    /// ticketNumber property.
    pub ticket_number: Option<String>,
    /// ticketType property.
    pub ticket_type: Option<LocalizedString>,
    /// validTimeInterval property.
    pub valid_time_interval: Option<TimeInterval>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// version property.
    pub version: Option<String>,
}

/// `ImageModuleData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ImageModuleData {
    /// id property.
    pub id: Option<String>,
    /// mainImage property.
    pub main_image: Option<Image>,
}

/// `Permission` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Permission {
    /// emailAddress property.
    pub email_address: Option<String>,
    /// role property.
    pub role: Option<String>,
}

/// `FlightHeader` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlightHeader {
    /// carrier property.
    pub carrier: Option<FlightCarrier>,
    /// flightNumber property.
    pub flight_number: Option<String>,
    /// flightNumberDisplayOverride property.
    pub flight_number_display_override: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// operatingCarrier property.
    pub operating_carrier: Option<FlightCarrier>,
    /// operatingFlightNumber property.
    pub operating_flight_number: Option<String>,
}

/// `IssuerListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IssuerListResponse {
    /// resources property.
    pub resources: Option<Vec<Issuer>>,
}

/// `TimeInterval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeInterval {
    /// end property.
    pub end: Option<DateTime>,
    /// kind property.
    pub kind: Option<String>,
    /// start property.
    pub start: Option<DateTime>,
}

/// `PassConstraints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PassConstraints {
    /// nfcConstraint property.
    pub nfc_constraint: Option<Vec<String>>,
    /// screenshotEligibility property.
    pub screenshot_eligibility: Option<String>,
}

/// `ReservationInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReservationInfo {
    /// confirmationCode property.
    pub confirmation_code: Option<String>,
    /// eticketNumber property.
    pub eticket_number: Option<String>,
    /// frequentFlyerInfo property.
    pub frequent_flyer_info: Option<FrequentFlyerInfo>,
    /// kind property.
    pub kind: Option<String>,
}

/// `EventTicketObjectAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventTicketObjectAddMessageResponse {
    /// resource property.
    pub resource: Option<EventTicketObject>,
}

/// `FlightObjectAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlightObjectAddMessageResponse {
    /// resource property.
    pub resource: Option<FlightObject>,
}

/// `Money` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Money {
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// micros property.
    pub micros: Option<String>,
}

/// `InfoModuleData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InfoModuleData {
    /// labelValueRows property.
    pub label_value_rows: Option<Vec<LabelValueRow>>,
    /// showLastUpdateTime property.
    pub show_last_update_time: Option<bool>,
}

/// `ImageUri` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ImageUri {
    /// description property.
    pub description: Option<String>,
    /// localizedDescription property.
    pub localized_description: Option<LocalizedString>,
    /// uri property.
    pub uri: Option<String>,
}

/// `EventSeat` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventSeat {
    /// gate property.
    pub gate: Option<LocalizedString>,
    /// kind property.
    pub kind: Option<String>,
    /// row property.
    pub row: Option<LocalizedString>,
    /// seat property.
    pub seat: Option<LocalizedString>,
    /// section property.
    pub section: Option<LocalizedString>,
}

/// `FlightClassAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlightClassAddMessageResponse {
    /// resource property.
    pub resource: Option<FlightClass>,
}

/// `FlightObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlightObject {
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// barcode property.
    pub barcode: Option<Barcode>,
    /// boardingAndSeatingInfo property.
    pub boarding_and_seating_info: Option<BoardingAndSeatingInfo>,
    /// classId property.
    pub class_id: Option<String>,
    /// classReference property.
    pub class_reference: Option<FlightClass>,
    /// disableExpirationNotification property.
    pub disable_expiration_notification: Option<bool>,
    /// groupingInfo property.
    pub grouping_info: Option<GroupingInfo>,
    /// hasLinkedDevice property.
    pub has_linked_device: Option<bool>,
    /// hasUsers property.
    pub has_users: Option<bool>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// hexBackgroundColor property.
    pub hex_background_color: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// kind property.
    pub kind: Option<String>,
    /// linkedObjectIds property.
    pub linked_object_ids: Option<Vec<String>>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// passConstraints property.
    pub pass_constraints: Option<PassConstraints>,
    /// passengerName property.
    pub passenger_name: Option<String>,
    /// reservationInfo property.
    pub reservation_info: Option<ReservationInfo>,
    /// rotatingBarcode property.
    pub rotating_barcode: Option<RotatingBarcode>,
    /// saveRestrictions property.
    pub save_restrictions: Option<SaveRestrictions>,
    /// securityProgramLogo property.
    pub security_program_logo: Option<Image>,
    /// smartTapRedemptionValue property.
    pub smart_tap_redemption_value: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// validTimeInterval property.
    pub valid_time_interval: Option<TimeInterval>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// version property.
    pub version: Option<String>,
}

/// `GenericObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenericObject {
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// barcode property.
    pub barcode: Option<Barcode>,
    /// cardTitle property.
    pub card_title: Option<LocalizedString>,
    /// classId property.
    pub class_id: Option<String>,
    /// genericType property.
    pub generic_type: Option<String>,
    /// groupingInfo property.
    pub grouping_info: Option<GroupingInfo>,
    /// hasUsers property.
    pub has_users: Option<bool>,
    /// header property.
    pub header: Option<LocalizedString>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// hexBackgroundColor property.
    pub hex_background_color: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// linkedObjectIds property.
    pub linked_object_ids: Option<Vec<String>>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// logo property.
    pub logo: Option<Image>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// notifications property.
    pub notifications: Option<Notifications>,
    /// passConstraints property.
    pub pass_constraints: Option<PassConstraints>,
    /// rotatingBarcode property.
    pub rotating_barcode: Option<RotatingBarcode>,
    /// saveRestrictions property.
    pub save_restrictions: Option<SaveRestrictions>,
    /// smartTapRedemptionValue property.
    pub smart_tap_redemption_value: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// subheader property.
    pub subheader: Option<LocalizedString>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// validTimeInterval property.
    pub valid_time_interval: Option<TimeInterval>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// wideLogo property.
    pub wide_logo: Option<Image>,
}

/// `DetailsTemplateOverride` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DetailsTemplateOverride {
    /// detailsItemInfos property.
    pub details_item_infos: Option<Vec<DetailsItemInfo>>,
}

/// `EventDateTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventDateTime {
    /// customDoorsOpenLabel property.
    pub custom_doors_open_label: Option<LocalizedString>,
    /// doorsOpen property.
    pub doors_open: Option<String>,
    /// doorsOpenLabel property.
    pub doors_open_label: Option<String>,
    /// end property.
    pub end: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// start property.
    pub start: Option<String>,
}

/// `Review` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Review {
    /// comments property.
    pub comments: Option<String>,
}

/// `ModifyLinkedOfferObjectsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ModifyLinkedOfferObjectsRequest {
    /// linkedOfferObjectIds property.
    pub linked_offer_object_ids: Option<ModifyLinkedOfferObjects>,
}

/// `TransitObjectAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransitObjectAddMessageResponse {
    /// resource property.
    pub resource: Option<TransitObject>,
}

/// `Message` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Message {
    /// body property.
    pub body: Option<String>,
    /// displayInterval property.
    pub display_interval: Option<TimeInterval>,
    /// header property.
    pub header: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// localizedBody property.
    pub localized_body: Option<LocalizedString>,
    /// localizedHeader property.
    pub localized_header: Option<LocalizedString>,
    /// messageType property.
    pub message_type: Option<String>,
}

/// `Uri` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Uri {
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// localizedDescription property.
    pub localized_description: Option<LocalizedString>,
    /// uri property.
    pub uri: Option<String>,
}

/// `LoyaltyPoints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoyaltyPoints {
    /// balance property.
    pub balance: Option<LoyaltyPointsBalance>,
    /// label property.
    pub label: Option<String>,
    /// localizedLabel property.
    pub localized_label: Option<LocalizedString>,
}

/// `ListTemplateOverride` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListTemplateOverride {
    /// firstRowOption property.
    pub first_row_option: Option<FirstRowOption>,
    /// secondRowOption property.
    pub second_row_option: Option<FieldSelector>,
    /// thirdRowOption property.
    pub third_row_option: Option<FieldSelector>,
}

/// `SetPassUpdateNoticeResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetPassUpdateNoticeResponse {}

/// `CardRowOneItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CardRowOneItem {
    /// item property.
    pub item: Option<TemplateItem>,
}

/// `MediaRequestInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MediaRequestInfo {
    /// currentBytes property.
    pub current_bytes: Option<String>,
    /// customData property.
    pub custom_data: Option<String>,
    /// diffObjectVersion property.
    pub diff_object_version: Option<String>,
    /// finalStatus property.
    pub final_status: Option<i64>,
    /// notificationType property.
    pub notification_type: Option<String>,
    /// physicalHeaders property.
    pub physical_headers: Option<String>,
    /// requestId property.
    pub request_id: Option<String>,
    /// requestReceivedParamsServingInfo property.
    pub request_received_params_serving_info: Option<String>,
    /// totalBytes property.
    pub total_bytes: Option<String>,
    /// totalBytesIsEstimated property.
    pub total_bytes_is_estimated: Option<bool>,
}

/// `DiscoverableProgram` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiscoverableProgram {
    /// merchantSigninInfo property.
    pub merchant_signin_info: Option<DiscoverableProgramMerchantSigninInfo>,
    /// merchantSignupInfo property.
    pub merchant_signup_info: Option<DiscoverableProgramMerchantSignupInfo>,
    /// state property.
    pub state: Option<String>,
}

/// `RotatingBarcode` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RotatingBarcode {
    /// alternateText property.
    pub alternate_text: Option<String>,
    /// initialRotatingBarcodeValues property.
    pub initial_rotating_barcode_values: Option<RotatingBarcodeValues>,
    /// renderEncoding property.
    pub render_encoding: Option<String>,
    /// showCodeText property.
    pub show_code_text: Option<LocalizedString>,
    /// totpDetails property.
    pub totp_details: Option<RotatingBarcodeTotpDetails>,
    /// type property.
    pub r#type: Option<String>,
    /// valuePattern property.
    pub value_pattern: Option<String>,
}

/// `RotatingBarcodeValues` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RotatingBarcodeValues {
    /// periodMillis property.
    pub period_millis: Option<String>,
    /// startDateTime property.
    pub start_date_time: Option<String>,
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `PurchaseDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PurchaseDetails {
    /// accountId property.
    pub account_id: Option<String>,
    /// confirmationCode property.
    pub confirmation_code: Option<String>,
    /// purchaseDateTime property.
    pub purchase_date_time: Option<String>,
    /// purchaseReceiptNumber property.
    pub purchase_receipt_number: Option<String>,
    /// ticketCost property.
    pub ticket_cost: Option<TicketCost>,
}

/// `CardTemplateOverride` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CardTemplateOverride {
    /// cardRowTemplateInfos property.
    pub card_row_template_infos: Option<Vec<CardRowTemplateInfo>>,
}

/// `LatLongPoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LatLongPoint {
    /// kind property.
    pub kind: Option<String>,
    /// latitude property.
    pub latitude: Option<f64>,
    /// longitude property.
    pub longitude: Option<f64>,
}

/// `GroupingInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GroupingInfo {
    /// groupingId property.
    pub grouping_id: Option<String>,
    /// sortIndex property.
    pub sort_index: Option<i64>,
}

/// `LoyaltyObjectAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoyaltyObjectAddMessageResponse {
    /// resource property.
    pub resource: Option<LoyaltyObject>,
}

/// `DetailsItemInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DetailsItemInfo {
    /// item property.
    pub item: Option<TemplateItem>,
}

/// `BoardingAndSeatingPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BoardingAndSeatingPolicy {
    /// boardingPolicy property.
    pub boarding_policy: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// seatClassPolicy property.
    pub seat_class_policy: Option<String>,
}

/// `CallbackOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CallbackOptions {
    /// updateRequestUrl property.
    pub update_request_url: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `LoyaltyClassAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoyaltyClassAddMessageResponse {
    /// resource property.
    pub resource: Option<LoyaltyClass>,
}

/// `CardRowThreeItems` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CardRowThreeItems {
    /// endItem property.
    pub end_item: Option<TemplateItem>,
    /// middleItem property.
    pub middle_item: Option<TemplateItem>,
    /// startItem property.
    pub start_item: Option<TemplateItem>,
}

/// `TicketSeat` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TicketSeat {
    /// coach property.
    pub coach: Option<String>,
    /// customFareClass property.
    pub custom_fare_class: Option<LocalizedString>,
    /// fareClass property.
    pub fare_class: Option<String>,
    /// seat property.
    pub seat: Option<String>,
    /// seatAssignment property.
    pub seat_assignment: Option<LocalizedString>,
}

/// `SaveRestrictions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SaveRestrictions {
    /// restrictToEmailSha256 property.
    pub restrict_to_email_sha256: Option<String>,
}

/// `TransitClassListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransitClassListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<TransitClass>>,
}

/// `LoyaltyObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoyaltyObject {
    /// accountId property.
    pub account_id: Option<String>,
    /// accountName property.
    pub account_name: Option<String>,
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// barcode property.
    pub barcode: Option<Barcode>,
    /// classId property.
    pub class_id: Option<String>,
    /// classReference property.
    pub class_reference: Option<LoyaltyClass>,
    /// disableExpirationNotification property.
    pub disable_expiration_notification: Option<bool>,
    /// groupingInfo property.
    pub grouping_info: Option<GroupingInfo>,
    /// hasLinkedDevice property.
    pub has_linked_device: Option<bool>,
    /// hasUsers property.
    pub has_users: Option<bool>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// kind property.
    pub kind: Option<String>,
    /// linkedObjectIds property.
    pub linked_object_ids: Option<Vec<String>>,
    /// linkedOfferIds property.
    pub linked_offer_ids: Option<Vec<String>>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// loyaltyPoints property.
    pub loyalty_points: Option<LoyaltyPoints>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// passConstraints property.
    pub pass_constraints: Option<PassConstraints>,
    /// rotatingBarcode property.
    pub rotating_barcode: Option<RotatingBarcode>,
    /// saveRestrictions property.
    pub save_restrictions: Option<SaveRestrictions>,
    /// secondaryLoyaltyPoints property.
    pub secondary_loyalty_points: Option<LoyaltyPoints>,
    /// smartTapRedemptionValue property.
    pub smart_tap_redemption_value: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// validTimeInterval property.
    pub valid_time_interval: Option<TimeInterval>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// version property.
    pub version: Option<String>,
}

/// `ValueAddedModuleData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ValueAddedModuleData {
    /// body property.
    pub body: Option<LocalizedString>,
    /// header property.
    pub header: Option<LocalizedString>,
    /// image property.
    pub image: Option<Image>,
    /// sortIndex property.
    pub sort_index: Option<i64>,
    /// uri property.
    pub uri: Option<String>,
    /// viewConstraints property.
    pub view_constraints: Option<ModuleViewConstraints>,
}

/// `CardBarcodeSectionDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CardBarcodeSectionDetails {
    /// firstBottomDetail property.
    pub first_bottom_detail: Option<BarcodeSectionDetail>,
    /// firstTopDetail property.
    pub first_top_detail: Option<BarcodeSectionDetail>,
    /// secondTopDetail property.
    pub second_top_detail: Option<BarcodeSectionDetail>,
}

/// `FirstRowOption` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirstRowOption {
    /// fieldOption property.
    pub field_option: Option<FieldSelector>,
    /// transitOption property.
    pub transit_option: Option<String>,
}

/// `Resources` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Resources {
    /// eventTicketClasses property.
    pub event_ticket_classes: Option<Vec<EventTicketClass>>,
    /// eventTicketObjects property.
    pub event_ticket_objects: Option<Vec<EventTicketObject>>,
    /// flightClasses property.
    pub flight_classes: Option<Vec<FlightClass>>,
    /// flightObjects property.
    pub flight_objects: Option<Vec<FlightObject>>,
    /// genericClasses property.
    pub generic_classes: Option<Vec<GenericClass>>,
    /// genericObjects property.
    pub generic_objects: Option<Vec<GenericObject>>,
    /// giftCardClasses property.
    pub gift_card_classes: Option<Vec<GiftCardClass>>,
    /// giftCardObjects property.
    pub gift_card_objects: Option<Vec<GiftCardObject>>,
    /// loyaltyClasses property.
    pub loyalty_classes: Option<Vec<LoyaltyClass>>,
    /// loyaltyObjects property.
    pub loyalty_objects: Option<Vec<LoyaltyObject>>,
    /// offerClasses property.
    pub offer_classes: Option<Vec<OfferClass>>,
    /// offerObjects property.
    pub offer_objects: Option<Vec<OfferObject>>,
    /// transitClasses property.
    pub transit_classes: Option<Vec<TransitClass>>,
    /// transitObjects property.
    pub transit_objects: Option<Vec<TransitObject>>,
}

/// `FlightCarrier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlightCarrier {
    /// airlineAllianceLogo property.
    pub airline_alliance_logo: Option<Image>,
    /// airlineLogo property.
    pub airline_logo: Option<Image>,
    /// airlineName property.
    pub airline_name: Option<LocalizedString>,
    /// carrierIataCode property.
    pub carrier_iata_code: Option<String>,
    /// carrierIcaoCode property.
    pub carrier_icao_code: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// wideAirlineLogo property.
    pub wide_airline_logo: Option<Image>,
}

/// `DiscoverableProgramMerchantSignupInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiscoverableProgramMerchantSignupInfo {
    /// signupSharedDatas property.
    pub signup_shared_datas: Option<Vec<String>>,
    /// signupWebsite property.
    pub signup_website: Option<Uri>,
}

/// `BarcodeSectionDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BarcodeSectionDetail {
    /// fieldSelector property.
    pub field_selector: Option<FieldSelector>,
}

/// `TransitObjectListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransitObjectListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<TransitObject>>,
}

/// `GenericClass` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenericClass {
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// callbackOptions property.
    pub callback_options: Option<CallbackOptions>,
    /// classTemplateInfo property.
    pub class_template_info: Option<ClassTemplateInfo>,
    /// enableSmartTap property.
    pub enable_smart_tap: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// multipleDevicesAndHoldersAllowedStatus property.
    pub multiple_devices_and_holders_allowed_status: Option<String>,
    /// redemptionIssuers property.
    pub redemption_issuers: Option<Vec<String>>,
    /// securityAnimation property.
    pub security_animation: Option<SecurityAnimation>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// viewUnlockRequirement property.
    pub view_unlock_requirement: Option<String>,
}

/// `RotatingBarcodeTotpDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RotatingBarcodeTotpDetails {
    /// algorithm property.
    pub algorithm: Option<String>,
    /// parameters property.
    pub parameters: Option<Vec<RotatingBarcodeTotpDetailsTotpParameters>>,
    /// periodMillis property.
    pub period_millis: Option<String>,
}

/// `LabelValueRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LabelValueRow {
    /// columns property.
    pub columns: Option<Vec<LabelValue>>,
}

/// `BoardingAndSeatingInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BoardingAndSeatingInfo {
    /// boardingDoor property.
    pub boarding_door: Option<String>,
    /// boardingGroup property.
    pub boarding_group: Option<String>,
    /// boardingPosition property.
    pub boarding_position: Option<String>,
    /// boardingPrivilegeImage property.
    pub boarding_privilege_image: Option<Image>,
    /// kind property.
    pub kind: Option<String>,
    /// seatAssignment property.
    pub seat_assignment: Option<LocalizedString>,
    /// seatClass property.
    pub seat_class: Option<String>,
    /// seatNumber property.
    pub seat_number: Option<String>,
    /// sequenceNumber property.
    pub sequence_number: Option<String>,
}

/// `ModifyLinkedOfferObjects` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ModifyLinkedOfferObjects {
    /// addLinkedOfferObjectIds property.
    pub add_linked_offer_object_ids: Option<Vec<String>>,
    /// removeLinkedOfferObjectIds property.
    pub remove_linked_offer_object_ids: Option<Vec<String>>,
}

/// `GiftCardObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GiftCardObject {
    /// appLinkData property.
    pub app_link_data: Option<AppLinkData>,
    /// balance property.
    pub balance: Option<Money>,
    /// balanceUpdateTime property.
    pub balance_update_time: Option<DateTime>,
    /// barcode property.
    pub barcode: Option<Barcode>,
    /// cardNumber property.
    pub card_number: Option<String>,
    /// classId property.
    pub class_id: Option<String>,
    /// classReference property.
    pub class_reference: Option<GiftCardClass>,
    /// disableExpirationNotification property.
    pub disable_expiration_notification: Option<bool>,
    /// eventNumber property.
    pub event_number: Option<String>,
    /// groupingInfo property.
    pub grouping_info: Option<GroupingInfo>,
    /// hasLinkedDevice property.
    pub has_linked_device: Option<bool>,
    /// hasUsers property.
    pub has_users: Option<bool>,
    /// heroImage property.
    pub hero_image: Option<Image>,
    /// id property.
    pub id: Option<String>,
    /// imageModulesData property.
    pub image_modules_data: Option<Vec<ImageModuleData>>,
    /// infoModuleData property.
    pub info_module_data: Option<InfoModuleData>,
    /// kind property.
    pub kind: Option<String>,
    /// linkedObjectIds property.
    pub linked_object_ids: Option<Vec<String>>,
    /// linksModuleData property.
    pub links_module_data: Option<LinksModuleData>,
    /// locations property.
    pub locations: Option<Vec<LatLongPoint>>,
    /// merchantLocations property.
    pub merchant_locations: Option<Vec<MerchantLocation>>,
    /// messages property.
    pub messages: Option<Vec<Message>>,
    /// notifyPreference property.
    pub notify_preference: Option<String>,
    /// passConstraints property.
    pub pass_constraints: Option<PassConstraints>,
    /// pin property.
    pub pin: Option<String>,
    /// rotatingBarcode property.
    pub rotating_barcode: Option<RotatingBarcode>,
    /// saveRestrictions property.
    pub save_restrictions: Option<SaveRestrictions>,
    /// smartTapRedemptionValue property.
    pub smart_tap_redemption_value: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// textModulesData property.
    pub text_modules_data: Option<Vec<TextModuleData>>,
    /// validTimeInterval property.
    pub valid_time_interval: Option<TimeInterval>,
    /// valueAddedModuleData property.
    pub value_added_module_data: Option<Vec<ValueAddedModuleData>>,
    /// version property.
    pub version: Option<String>,
}

/// `TemplateItem` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TemplateItem {
    /// firstValue property.
    pub first_value: Option<FieldSelector>,
    /// predefinedItem property.
    pub predefined_item: Option<String>,
    /// secondValue property.
    pub second_value: Option<FieldSelector>,
}

/// `LoyaltyPointsBalance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoyaltyPointsBalance {
    /// double property.
    pub double: Option<f64>,
    /// int property.
    pub int: Option<i64>,
    /// money property.
    pub money: Option<Money>,
    /// string property.
    pub string: Option<String>,
}

/// `GiftCardClassAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GiftCardClassAddMessageResponse {
    /// resource property.
    pub resource: Option<GiftCardClass>,
}

/// `Pagination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Pagination {
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resultsPerPage property.
    pub results_per_page: Option<i64>,
}

/// `Barcode` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Barcode {
    /// alternateText property.
    pub alternate_text: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// renderEncoding property.
    pub render_encoding: Option<String>,
    /// showCodeText property.
    pub show_code_text: Option<LocalizedString>,
    /// type property.
    pub r#type: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `ExpiryNotification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExpiryNotification {
    /// enableNotification property.
    pub enable_notification: Option<bool>,
}

/// `JwtInsertResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JwtInsertResponse {
    /// resources property.
    pub resources: Option<Resources>,
    /// saveUri property.
    pub save_uri: Option<String>,
}

/// `GiftCardObjectAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GiftCardObjectAddMessageResponse {
    /// resource property.
    pub resource: Option<GiftCardObject>,
}

/// `DiffVersionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiffVersionResponse {
    /// objectSizeBytes property.
    pub object_size_bytes: Option<String>,
    /// objectVersion property.
    pub object_version: Option<String>,
}

/// `AddMessageRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AddMessageRequest {
    /// message property.
    pub message: Option<Message>,
}

/// `TicketCost` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TicketCost {
    /// discountMessage property.
    pub discount_message: Option<LocalizedString>,
    /// faceValue property.
    pub face_value: Option<Money>,
    /// purchasePrice property.
    pub purchase_price: Option<Money>,
}

/// `IssuerContactInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IssuerContactInfo {
    /// alertsEmails property.
    pub alerts_emails: Option<Vec<String>>,
    /// email property.
    pub email: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// phone property.
    pub phone: Option<String>,
}

/// `AirportInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AirportInfo {
    /// airportIataCode property.
    pub airport_iata_code: Option<String>,
    /// airportNameOverride property.
    pub airport_name_override: Option<LocalizedString>,
    /// gate property.
    pub gate: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// terminal property.
    pub terminal: Option<String>,
}

/// `TransitObjectUploadRotatingBarcodeValuesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TransitObjectUploadRotatingBarcodeValuesResponse {}

/// `AppLinkDataAppLinkInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppLinkDataAppLinkInfo {
    /// appLogoImage property.
    pub app_logo_image: Option<Image>,
    /// appTarget property.
    pub app_target: Option<AppLinkDataAppLinkInfoAppTarget>,
    /// description property.
    pub description: Option<LocalizedString>,
    /// title property.
    pub title: Option<LocalizedString>,
}

/// `FrequentFlyerInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FrequentFlyerInfo {
    /// frequentFlyerNumber property.
    pub frequent_flyer_number: Option<String>,
    /// frequentFlyerProgramName property.
    pub frequent_flyer_program_name: Option<LocalizedString>,
    /// kind property.
    pub kind: Option<String>,
}

/// `ActivationOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ActivationOptions {
    /// activationUrl property.
    pub activation_url: Option<String>,
    /// allowReactivation property.
    pub allow_reactivation: Option<bool>,
}

/// `EventVenue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventVenue {
    /// address property.
    pub address: Option<LocalizedString>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<LocalizedString>,
}

/// `LoyaltyObjectListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LoyaltyObjectListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<LoyaltyObject>>,
}

/// `CompositeMedia` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CompositeMedia {
    /// blobRef property.
    pub blob_ref: Option<String>,
    /// blobstore2Info property.
    pub blobstore2_info: Option<Blobstore2Info>,
    /// cosmoBinaryReference property.
    pub cosmo_binary_reference: Option<String>,
    /// crc32cHash property.
    pub crc32c_hash: Option<i64>,
    /// inline property.
    pub inline: Option<String>,
    /// length property.
    pub length: Option<String>,
    /// md5Hash property.
    pub md5_hash: Option<String>,
    /// objectId property.
    pub object_id: Option<ObjectId>,
    /// path property.
    pub path: Option<String>,
    /// referenceType property.
    pub reference_type: Option<String>,
    /// sha1Hash property.
    pub sha1_hash: Option<String>,
}

/// `AppLinkData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppLinkData {
    /// androidAppLinkInfo property.
    pub android_app_link_info: Option<AppLinkDataAppLinkInfo>,
    /// displayText property.
    pub display_text: Option<LocalizedString>,
    /// iosAppLinkInfo property.
    pub ios_app_link_info: Option<AppLinkDataAppLinkInfo>,
    /// webAppLinkInfo property.
    pub web_app_link_info: Option<AppLinkDataAppLinkInfo>,
}

/// `Blobstore2Info` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Blobstore2Info {
    /// blobGeneration property.
    pub blob_generation: Option<String>,
    /// blobId property.
    pub blob_id: Option<String>,
    /// downloadExternalReadToken property.
    pub download_external_read_token: Option<String>,
    /// downloadReadHandle property.
    pub download_read_handle: Option<String>,
    /// readToken property.
    pub read_token: Option<String>,
    /// uploadFragmentListCreationInfo property.
    pub upload_fragment_list_creation_info: Option<String>,
    /// uploadMetadataContainer property.
    pub upload_metadata_container: Option<String>,
}

/// `ContentTypeInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContentTypeInfo {
    /// bestGuess property.
    pub best_guess: Option<String>,
    /// fromBytes property.
    pub from_bytes: Option<String>,
    /// fromFileName property.
    pub from_file_name: Option<String>,
    /// fromHeader property.
    pub from_header: Option<String>,
    /// fromUrlPath property.
    pub from_url_path: Option<String>,
}

/// `ClassTemplateInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClassTemplateInfo {
    /// cardBarcodeSectionDetails property.
    pub card_barcode_section_details: Option<CardBarcodeSectionDetails>,
    /// cardTemplateOverride property.
    pub card_template_override: Option<CardTemplateOverride>,
    /// detailsTemplateOverride property.
    pub details_template_override: Option<DetailsTemplateOverride>,
    /// listTemplateOverride property.
    pub list_template_override: Option<ListTemplateOverride>,
}

/// `GenericClassListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenericClassListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<GenericClass>>,
}

/// `FlightObjectListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FlightObjectListResponse {
    /// pagination property.
    pub pagination: Option<Pagination>,
    /// resources property.
    pub resources: Option<Vec<FlightObject>>,
}

/// `GenericObjectAddMessageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenericObjectAddMessageResponse {
    /// resource property.
    pub resource: Option<GenericObject>,
}

/// `LinksModuleData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LinksModuleData {
    /// uris property.
    pub uris: Option<Vec<Uri>>,
}

/// `DiffUploadRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiffUploadRequest {
    /// checksumsInfo property.
    pub checksums_info: Option<CompositeMedia>,
    /// objectInfo property.
    pub object_info: Option<CompositeMedia>,
    /// objectVersion property.
    pub object_version: Option<String>,
}

/// `DiffUploadResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiffUploadResponse {
    /// objectVersion property.
    pub object_version: Option<String>,
    /// originalObject property.
    pub original_object: Option<CompositeMedia>,
}

/// `ModuleViewConstraints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ModuleViewConstraints {
    /// displayInterval property.
    pub display_interval: Option<TimeInterval>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`walletobjects.eventticketclass.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.eventticketclass.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.eventticketclass.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassInsertArgs {
    /// Request body.
    pub body: EventTicketClass,
}

/// Arguments for [`walletobjects.eventticketclass.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassListArgs {
    /// Query parameter: `issuerId`.
    pub issuer_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.eventticketclass.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: EventTicketClass,
}

/// Arguments for [`walletobjects.eventticketclass.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketclassUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: EventTicketClass,
}

/// Arguments for [`walletobjects.eventticketobject.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.eventticketobject.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.eventticketobject.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectInsertArgs {
    /// Request body.
    pub body: EventTicketObject,
}

/// Arguments for [`walletobjects.eventticketobject.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectListArgs {
    /// Query parameter: `classId`.
    pub class_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.eventticketobject.modifylinkedofferobjects_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectModifylinkedofferobjectsArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: ModifyLinkedOfferObjectsRequest,
}

/// Arguments for [`walletobjects.eventticketobject.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: EventTicketObject,
}

/// Arguments for [`walletobjects.eventticketobject.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsEventticketobjectUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: EventTicketObject,
}

/// Arguments for [`walletobjects.flightclass.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.flightclass.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.flightclass.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassInsertArgs {
    /// Request body.
    pub body: FlightClass,
}

/// Arguments for [`walletobjects.flightclass.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassListArgs {
    /// Query parameter: `issuerId`.
    pub issuer_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.flightclass.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: FlightClass,
}

/// Arguments for [`walletobjects.flightclass.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightclassUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: FlightClass,
}

/// Arguments for [`walletobjects.flightobject.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.flightobject.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.flightobject.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectInsertArgs {
    /// Request body.
    pub body: FlightObject,
}

/// Arguments for [`walletobjects.flightobject.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectListArgs {
    /// Query parameter: `classId`.
    pub class_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.flightobject.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: FlightObject,
}

/// Arguments for [`walletobjects.flightobject.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsFlightobjectUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: FlightObject,
}

/// Arguments for [`walletobjects.genericclass.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.genericclass.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.genericclass.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassInsertArgs {
    /// Request body.
    pub body: GenericClass,
}

/// Arguments for [`walletobjects.genericclass.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassListArgs {
    /// Query parameter: `issuerId`.
    pub issuer_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.genericclass.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: GenericClass,
}

/// Arguments for [`walletobjects.genericclass.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericclassUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: GenericClass,
}

/// Arguments for [`walletobjects.genericobject.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.genericobject.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.genericobject.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectInsertArgs {
    /// Request body.
    pub body: GenericObject,
}

/// Arguments for [`walletobjects.genericobject.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectListArgs {
    /// Query parameter: `classId`.
    pub class_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.genericobject.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: GenericObject,
}

/// Arguments for [`walletobjects.genericobject.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGenericobjectUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: GenericObject,
}

/// Arguments for [`walletobjects.giftcardclass.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.giftcardclass.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.giftcardclass.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassInsertArgs {
    /// Request body.
    pub body: GiftCardClass,
}

/// Arguments for [`walletobjects.giftcardclass.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassListArgs {
    /// Query parameter: `issuerId`.
    pub issuer_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.giftcardclass.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: GiftCardClass,
}

/// Arguments for [`walletobjects.giftcardclass.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardclassUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: GiftCardClass,
}

/// Arguments for [`walletobjects.giftcardobject.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.giftcardobject.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.giftcardobject.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectInsertArgs {
    /// Request body.
    pub body: GiftCardObject,
}

/// Arguments for [`walletobjects.giftcardobject.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectListArgs {
    /// Query parameter: `classId`.
    pub class_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.giftcardobject.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: GiftCardObject,
}

/// Arguments for [`walletobjects.giftcardobject.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsGiftcardobjectUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: GiftCardObject,
}

/// Arguments for [`walletobjects.issuer.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsIssuerGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.issuer.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsIssuerInsertArgs {
    /// Request body.
    pub body: Issuer,
}

/// Arguments for [`walletobjects.issuer.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsIssuerPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: Issuer,
}

/// Arguments for [`walletobjects.issuer.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsIssuerUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: Issuer,
}

/// Arguments for [`walletobjects.jwt.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsJwtInsertArgs {
    /// Request body.
    pub body: JwtResource,
}

/// Arguments for [`walletobjects.loyaltyclass.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.loyaltyclass.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.loyaltyclass.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassInsertArgs {
    /// Request body.
    pub body: LoyaltyClass,
}

/// Arguments for [`walletobjects.loyaltyclass.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassListArgs {
    /// Query parameter: `issuerId`.
    pub issuer_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.loyaltyclass.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: LoyaltyClass,
}

/// Arguments for [`walletobjects.loyaltyclass.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyclassUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: LoyaltyClass,
}

/// Arguments for [`walletobjects.loyaltyobject.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.loyaltyobject.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.loyaltyobject.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectInsertArgs {
    /// Request body.
    pub body: LoyaltyObject,
}

/// Arguments for [`walletobjects.loyaltyobject.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectListArgs {
    /// Query parameter: `classId`.
    pub class_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.loyaltyobject.modifylinkedofferobjects_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectModifylinkedofferobjectsArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: ModifyLinkedOfferObjectsRequest,
}

/// Arguments for [`walletobjects.loyaltyobject.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: LoyaltyObject,
}

/// Arguments for [`walletobjects.loyaltyobject.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsLoyaltyobjectUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: LoyaltyObject,
}

/// Arguments for [`walletobjects.media.download_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsMediaDownloadArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.media.upload_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsMediaUploadArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: TransitObjectUploadRotatingBarcodeValuesRequest,
}

/// Arguments for [`walletobjects.offerclass.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.offerclass.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.offerclass.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassInsertArgs {
    /// Request body.
    pub body: OfferClass,
}

/// Arguments for [`walletobjects.offerclass.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassListArgs {
    /// Query parameter: `issuerId`.
    pub issuer_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.offerclass.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: OfferClass,
}

/// Arguments for [`walletobjects.offerclass.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferclassUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: OfferClass,
}

/// Arguments for [`walletobjects.offerobject.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.offerobject.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.offerobject.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectInsertArgs {
    /// Request body.
    pub body: OfferObject,
}

/// Arguments for [`walletobjects.offerobject.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectListArgs {
    /// Query parameter: `classId`.
    pub class_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.offerobject.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: OfferObject,
}

/// Arguments for [`walletobjects.offerobject.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsOfferobjectUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: OfferObject,
}

/// Arguments for [`walletobjects.permissions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsPermissionsGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.permissions.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsPermissionsUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: Permissions,
}

/// Arguments for [`walletobjects.smarttap.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsSmarttapInsertArgs {
    /// Request body.
    pub body: SmartTap,
}

/// Arguments for [`walletobjects.transitclass.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.transitclass.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.transitclass.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassInsertArgs {
    /// Request body.
    pub body: TransitClass,
}

/// Arguments for [`walletobjects.transitclass.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassListArgs {
    /// Query parameter: `issuerId`.
    pub issuer_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.transitclass.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: TransitClass,
}

/// Arguments for [`walletobjects.transitclass.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitclassUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: TransitClass,
}

/// Arguments for [`walletobjects.transitobject.addmessage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectAddmessageArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: AddMessageRequest,
}

/// Arguments for [`walletobjects.transitobject.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectGetArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
}

/// Arguments for [`walletobjects.transitobject.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectInsertArgs {
    /// Request body.
    pub body: TransitObject,
}

/// Arguments for [`walletobjects.transitobject.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectListArgs {
    /// Query parameter: `classId`.
    pub class_id: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `token`.
    pub token: Option<String>,
}

/// Arguments for [`walletobjects.transitobject.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectPatchArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: TransitObject,
}

/// Arguments for [`walletobjects.transitobject.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsTransitobjectUpdateArgs {
    /// Path parameter: `resourceId`.
    pub resource_id: String,
    /// Request body.
    pub body: TransitObject,
}

/// Arguments for [`walletobjects.walletobjects.v1.privateContent.setPassUpdateNotice_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WalletobjectsWalletobjectsV1PrivateContentSetPassUpdateNoticeArgs {
    /// Request body.
    pub body: SetPassUpdateNoticeRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST walletobjects/v1/eventTicketClass/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/eventTicketClass/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketclass_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketclass_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketclassAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<EventTicketClassAddMessageResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketClassAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/eventTicketClass/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/eventTicketClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketclass_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketclass_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketclassGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTicketClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/eventTicketClass
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/eventTicketClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketclass_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketclass_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketclassInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTicketClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/eventTicketClass
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/eventTicketClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketclass_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketclass_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketclassListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTicketClassListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.issuer_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("issuerId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketClassListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/eventTicketClass/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/eventTicketClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketclass_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketclass_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketclassPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTicketClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/eventTicketClass/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/eventTicketClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketclass_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketclass_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketclassUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTicketClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/eventTicketObject/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/eventTicketObject/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketobject_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketobject_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketobjectAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<EventTicketObjectAddMessageResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketObjectAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/eventTicketObject/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/eventTicketObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketobject_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketobject_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketobjectGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTicketObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/eventTicketObject
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/eventTicketObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketobject_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketobject_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketobjectInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTicketObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/eventTicketObject
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/eventTicketObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketobject_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketobject_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketobjectListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTicketObjectListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.class_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("classId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketObjectListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/eventTicketObject/{resourceId}/modifyLinkedOfferObjects
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/eventTicketObject/{resourceId}/modifyLinkedOfferObjects.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketobject_modifylinkedofferobjects_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketobject_modifylinkedofferobjects_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketobjectModifylinkedofferobjectsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTicketObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject/{}/modifyLinkedOfferObjects",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/eventTicketObject/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/eventTicketObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketobject_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketobject_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketobjectPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTicketObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/eventTicketObject/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/eventTicketObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_eventticketobject_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_eventticketobject_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsEventticketobjectUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EventTicketObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/eventTicketObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EventTicketObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/flightClass/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/flightClass/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightclass_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightclass_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightclassAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightClassAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightClass/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightClassAddMessageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/flightClass/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/flightClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightclass_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightclass_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightclassGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/flightClass
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/flightClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightclass_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightclass_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightclassInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/flightClass",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/flightClass
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/flightClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightclass_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightclass_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightclassListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightClassListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/flightClass",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.issuer_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("issuerId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightClassListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/flightClass/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/flightClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightclass_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightclass_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightclassPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/flightClass/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/flightClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightclass_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightclass_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightclassUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/flightObject/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/flightObject/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightobject_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightobject_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightobjectAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightObjectAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightObject/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightObjectAddMessageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/flightObject/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/flightObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightobject_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightobject_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightobjectGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/flightObject
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/flightObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightobject_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightobject_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightobjectInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/flightObject",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/flightObject
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/flightObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightobject_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightobject_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightobjectListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightObjectListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/flightObject",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.class_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("classId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightObjectListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/flightObject/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/flightObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightobject_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightobject_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightobjectPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/flightObject/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/flightObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_flightobject_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_flightobject_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsFlightobjectUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlightObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/flightObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FlightObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/genericClass/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/genericClass/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericclass_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericclass_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericclassAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericClassAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericClass/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericClassAddMessageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/genericClass/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/genericClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericclass_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericclass_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericclassGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/genericClass
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/genericClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericclass_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericclass_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericclassInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/genericClass",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/genericClass
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/genericClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericclass_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericclass_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericclassListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericClassListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/genericClass",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.issuer_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("issuerId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericClassListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/genericClass/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/genericClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericclass_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericclass_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericclassPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/genericClass/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/genericClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericclass_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericclass_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericclassUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/genericObject/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/genericObject/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericobject_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericobject_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericobjectAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericObjectAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericObject/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericObjectAddMessageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/genericObject/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/genericObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericobject_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericobject_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericobjectGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/genericObject
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/genericObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericobject_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericobject_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericobjectInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/genericObject",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/genericObject
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/genericObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericobject_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericobject_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericobjectListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericObjectListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/genericObject",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.class_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("classId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericObjectListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/genericObject/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/genericObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericobject_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericobject_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericobjectPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/genericObject/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/genericObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_genericobject_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_genericobject_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGenericobjectUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenericObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/genericObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenericObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/giftCardClass/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/giftCardClass/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardclass_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardclass_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardclassAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardClassAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardClassAddMessageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/giftCardClass/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/giftCardClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardclass_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardclass_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardclassGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/giftCardClass
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/giftCardClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardclass_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardclass_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardclassInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/giftCardClass
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/giftCardClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardclass_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardclass_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardclassListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardClassListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.issuer_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("issuerId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardClassListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/giftCardClass/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/giftCardClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardclass_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardclass_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardclassPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/giftCardClass/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/giftCardClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardclass_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardclass_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardclassUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/giftCardObject/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/giftCardObject/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardobject_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardobject_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardobjectAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardObjectAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardObjectAddMessageResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/giftCardObject/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/giftCardObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardobject_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardobject_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardobjectGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/giftCardObject
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/giftCardObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardobject_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardobject_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardobjectInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/giftCardObject
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/giftCardObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardobject_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardobject_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardobjectListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardObjectListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.class_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("classId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardObjectListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/giftCardObject/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/giftCardObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardobject_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardobject_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardobjectPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/giftCardObject/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/giftCardObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_giftcardobject_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_giftcardobject_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsGiftcardobjectUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GiftCardObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/giftCardObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GiftCardObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/issuer/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/issuer/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_issuer_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_issuer_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsIssuerGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Issuer>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/issuer/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Issuer =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/issuer
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/issuer.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_issuer_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_issuer_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsIssuerInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Issuer>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://walletobjects.googleapis.com/walletobjects/v1/issuer",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Issuer =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/issuer
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/issuer.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_issuer_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_issuer_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsIssuerListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IssuerListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://walletobjects.googleapis.com/walletobjects/v1/issuer",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IssuerListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/issuer/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/issuer/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_issuer_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_issuer_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsIssuerPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Issuer>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/issuer/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Issuer =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/issuer/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/issuer/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_issuer_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_issuer_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsIssuerUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Issuer>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/issuer/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Issuer =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/jwt
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/jwt.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_jwt_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_jwt_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsJwtInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<JwtInsertResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://walletobjects.googleapis.com/walletobjects/v1/jwt",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: JwtInsertResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/loyaltyClass/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/loyaltyClass/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyclass_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyclass_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyclassAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyClassAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyClassAddMessageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/loyaltyClass/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/loyaltyClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyclass_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyclass_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyclassGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/loyaltyClass
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/loyaltyClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyclass_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyclass_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyclassInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/loyaltyClass
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/loyaltyClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyclass_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyclass_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyclassListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyClassListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.issuer_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("issuerId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyClassListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/loyaltyClass/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/loyaltyClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyclass_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyclass_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyclassPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/loyaltyClass/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/loyaltyClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyclass_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyclass_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyclassUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/loyaltyObject/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/loyaltyObject/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyobject_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyobject_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyobjectAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyObjectAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyObjectAddMessageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/loyaltyObject/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/loyaltyObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyobject_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyobject_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyobjectGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/loyaltyObject
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/loyaltyObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyobject_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyobject_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyobjectInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/loyaltyObject
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/loyaltyObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyobject_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyobject_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyobjectListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyObjectListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.class_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("classId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyObjectListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/loyaltyObject/{resourceId}/modifyLinkedOfferObjects
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/loyaltyObject/{resourceId}/modifyLinkedOfferObjects.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyobject_modifylinkedofferobjects_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyobject_modifylinkedofferobjects_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyobjectModifylinkedofferobjectsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject/{}/modifyLinkedOfferObjects",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/loyaltyObject/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/loyaltyObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyobject_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyobject_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyobjectPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/loyaltyObject/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/loyaltyObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_loyaltyobject_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_loyaltyobject_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsLoyaltyobjectUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoyaltyObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/loyaltyObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LoyaltyObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/transitObject/{resourceId}/downloadRotatingBarcodeValues
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/transitObject/{resourceId}/downloadRotatingBarcodeValues.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_media_download_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_media_download_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsMediaDownloadArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Media>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}/downloadRotatingBarcodeValues",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Media =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/transitObject/{resourceId}/uploadRotatingBarcodeValues
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/transitObject/{resourceId}/uploadRotatingBarcodeValues.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_media_upload_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_media_upload_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsMediaUploadArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<TransitObjectUploadRotatingBarcodeValuesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}/uploadRotatingBarcodeValues",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitObjectUploadRotatingBarcodeValuesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/offerClass/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/offerClass/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerclass_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerclass_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferclassAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferClassAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerClass/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferClassAddMessageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/offerClass/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/offerClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerclass_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerclass_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferclassGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/offerClass
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/offerClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerclass_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerclass_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferclassInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://walletobjects.googleapis.com/walletobjects/v1/offerClass",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/offerClass
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/offerClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerclass_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerclass_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferclassListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferClassListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://walletobjects.googleapis.com/walletobjects/v1/offerClass",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.issuer_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("issuerId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferClassListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/offerClass/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/offerClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerclass_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerclass_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferclassPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/offerClass/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/offerClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerclass_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerclass_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferclassUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/offerObject/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/offerObject/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerobject_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerobject_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferobjectAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferObjectAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerObject/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferObjectAddMessageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/offerObject/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/offerObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerobject_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerobject_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferobjectGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/offerObject
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/offerObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerobject_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerobject_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferobjectInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/offerObject",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/offerObject
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/offerObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerobject_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerobject_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferobjectListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferObjectListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/offerObject",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.class_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("classId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferObjectListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/offerObject/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/offerObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerobject_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerobject_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferobjectPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/offerObject/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/offerObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_offerobject_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_offerobject_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsOfferobjectUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OfferObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/offerObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OfferObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/permissions/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/permissions/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_permissions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_permissions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsPermissionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Permissions>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/permissions/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Permissions =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/permissions/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/permissions/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_permissions_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_permissions_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsPermissionsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Permissions>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/permissions/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Permissions =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/smartTap
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/smartTap.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_smarttap_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_smarttap_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsSmarttapInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SmartTap>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://walletobjects.googleapis.com/walletobjects/v1/smartTap",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SmartTap =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/transitClass/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/transitClass/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitclass_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitclass_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitclassAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitClassAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitClass/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitClassAddMessageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/transitClass/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/transitClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitclass_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitclass_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitclassGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/transitClass
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/transitClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitclass_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitclass_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitclassInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/transitClass",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/transitClass
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/transitClass.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitclass_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitclass_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitclassListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitClassListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/transitClass",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.issuer_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("issuerId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitClassListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/transitClass/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/transitClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitclass_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitclass_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitclassPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/transitClass/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/transitClass/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitclass_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitclass_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitclassUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitClass>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitClass/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitClass =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/transitObject/{resourceId}/addMessage
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/transitObject/{resourceId}/addMessage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitobject_addmessage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitobject_addmessage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitobjectAddmessageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitObjectAddMessageResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}/addMessage",
        args.resource_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitObjectAddMessageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/transitObject/{resourceId}
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/transitObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitobject_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitobject_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitobjectGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/transitObject
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/transitObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitobject_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitobject_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitobjectInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/transitObject",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET walletobjects/v1/transitObject
// -----------------------------------------------------------------------------

/// GET walletobjects/v1/transitObject.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitobject_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitobject_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitobjectListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitObjectListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://walletobjects.googleapis.com/walletobjects/v1/transitObject",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.class_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("classId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitObjectListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH walletobjects/v1/transitObject/{resourceId}
// -----------------------------------------------------------------------------

/// PATCH walletobjects/v1/transitObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitobject_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitobject_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitobjectPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT walletobjects/v1/transitObject/{resourceId}
// -----------------------------------------------------------------------------

/// PUT walletobjects/v1/transitObject/{resourceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_transitobject_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_transitobject_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsTransitobjectUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TransitObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/transitObject/{}",
        args.resource_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TransitObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST walletobjects/v1/privateContent/setPassUpdateNotice
// -----------------------------------------------------------------------------

/// POST walletobjects/v1/privateContent/setPassUpdateNotice.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = walletobjects_walletobjects_v1_private_content_set_pass_update_notice_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn walletobjects_walletobjects_v1_private_content_set_pass_update_notice_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WalletobjectsWalletobjectsV1PrivateContentSetPassUpdateNoticeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SetPassUpdateNoticeResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://walletobjects.googleapis.com/walletobjects/v1/privateContent/setPassUpdateNotice",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SetPassUpdateNoticeResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
