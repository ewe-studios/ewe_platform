//! Auto-generated API module for gcp/batch jobs.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_batch_jobs `

#![cfg(feature = "gcp_batch_jobs")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Accelerator` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Accelerator {
    /// count property.
    pub count: Option<String>,
    /// driverVersion property.
    pub driver_version: Option<String>,
    /// installGpuDrivers property.
    pub install_gpu_drivers: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `PlacementPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlacementPolicy {
    /// collocation property.
    pub collocation: Option<String>,
    /// maxDistance property.
    pub max_distance: Option<String>,
}

/// `Environment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Environment {
    /// encryptedVariables property.
    pub encrypted_variables: Option<KMSEnvMap>,
    /// secretVariables property.
    pub secret_variables: Option<serde_json::Value>,
    /// variables property.
    pub variables: Option<serde_json::Value>,
}

/// `Barrier` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Barrier {
    /// name property.
    pub name: Option<String>,
}

/// `ComputeResource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComputeResource {
    /// bootDiskMib property.
    pub boot_disk_mib: Option<String>,
    /// cpuMilli property.
    pub cpu_milli: Option<String>,
    /// memoryMib property.
    pub memory_mib: Option<String>,
}

/// `InstancePolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstancePolicy {
    /// accelerators property.
    pub accelerators: Option<Vec<Accelerator>>,
    /// bootDisk property.
    pub boot_disk: Option<Disk>,
    /// disks property.
    pub disks: Option<Vec<AttachedDisk>>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// minCpuPlatform property.
    pub min_cpu_platform: Option<String>,
    /// provisioningModel property.
    pub provisioning_model: Option<String>,
    /// reservation property.
    pub reservation: Option<String>,
}

/// `LifecyclePolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LifecyclePolicy {
    /// action property.
    pub action: Option<String>,
    /// actionCondition property.
    pub action_condition: Option<ActionCondition>,
}

/// `AllocationPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AllocationPolicy {
    /// instances property.
    pub instances: Option<Vec<InstancePolicyOrTemplate>>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// location property.
    pub location: Option<LocationPolicy>,
    /// network property.
    pub network: Option<NetworkPolicy>,
    /// placement property.
    pub placement: Option<PlacementPolicy>,
    /// serviceAccount property.
    pub service_account: Option<ServiceAccount>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `GCS` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GCS {
    /// remotePath property.
    pub remote_path: Option<String>,
}

/// `NetworkPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NetworkPolicy {
    /// networkInterfaces property.
    pub network_interfaces: Option<Vec<NetworkInterface>>,
}

/// `TaskExecution` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TaskExecution {
    /// exitCode property.
    pub exit_code: Option<i64>,
}

/// `Runnable` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Runnable {
    /// alwaysRun property.
    pub always_run: Option<bool>,
    /// background property.
    pub background: Option<bool>,
    /// barrier property.
    pub barrier: Option<Barrier>,
    /// container property.
    pub container: Option<Container>,
    /// displayName property.
    pub display_name: Option<String>,
    /// environment property.
    pub environment: Option<Environment>,
    /// ignoreExitStatus property.
    pub ignore_exit_status: Option<bool>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// script property.
    pub script: Option<Script>,
    /// timeout property.
    pub timeout: Option<String>,
}

/// `ActionCondition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ActionCondition {
    /// exitCodes property.
    pub exit_codes: Option<Vec<i64>>,
}

/// `NetworkInterface` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NetworkInterface {
    /// network property.
    pub network: Option<String>,
    /// noExternalIpAddress property.
    pub no_external_ip_address: Option<bool>,
    /// subnetwork property.
    pub subnetwork: Option<String>,
}

/// `Message` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Message {
    /// newJobState property.
    pub new_job_state: Option<String>,
    /// newTaskState property.
    pub new_task_state: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `JobStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct JobStatus {
    /// runDuration property.
    pub run_duration: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// statusEvents property.
    pub status_events: Option<Vec<StatusEvent>>,
    /// taskGroups property.
    pub task_groups: Option<serde_json::Value>,
}

/// `Script` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Script {
    /// path property.
    pub path: Option<String>,
    /// text property.
    pub text: Option<String>,
}

/// `TaskGroup` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TaskGroup {
    /// name property.
    pub name: Option<String>,
    /// parallelism property.
    pub parallelism: Option<String>,
    /// permissiveSsh property.
    pub permissive_ssh: Option<bool>,
    /// requireHostsFile property.
    pub require_hosts_file: Option<bool>,
    /// runAsNonRoot property.
    pub run_as_non_root: Option<bool>,
    /// schedulingPolicy property.
    pub scheduling_policy: Option<String>,
    /// taskCount property.
    pub task_count: Option<String>,
    /// taskCountPerNode property.
    pub task_count_per_node: Option<String>,
    /// taskEnvironments property.
    pub task_environments: Option<Vec<Environment>>,
    /// taskSpec property.
    pub task_spec: Option<TaskSpec>,
}

/// `LocationPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LocationPolicy {
    /// allowedLocations property.
    pub allowed_locations: Option<Vec<String>>,
}

/// `ListJobsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListJobsResponse {
    /// jobs property.
    pub jobs: Option<Vec<Job>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `ServiceAccount` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ServiceAccount {
    /// email property.
    pub email: Option<String>,
    /// scopes property.
    pub scopes: Option<Vec<String>>,
}

/// `Disk` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Disk {
    /// diskInterface property.
    pub disk_interface: Option<String>,
    /// image property.
    pub image: Option<String>,
    /// sizeGb property.
    pub size_gb: Option<String>,
    /// snapshot property.
    pub snapshot: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Volume` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Volume {
    /// deviceName property.
    pub device_name: Option<String>,
    /// gcs property.
    pub gcs: Option<GCS>,
    /// mountOptions property.
    pub mount_options: Option<Vec<String>>,
    /// mountPath property.
    pub mount_path: Option<String>,
    /// nfs property.
    pub nfs: Option<NFS>,
}

/// `Job` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Job {
    /// allocationPolicy property.
    pub allocation_policy: Option<AllocationPolicy>,
    /// createTime property.
    pub create_time: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// logsPolicy property.
    pub logs_policy: Option<LogsPolicy>,
    /// name property.
    pub name: Option<String>,
    /// notifications property.
    pub notifications: Option<Vec<JobNotification>>,
    /// priority property.
    pub priority: Option<String>,
    /// status property.
    pub status: Option<JobStatus>,
    /// taskGroups property.
    pub task_groups: Option<Vec<TaskGroup>>,
    /// uid property.
    pub uid: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `AttachedDisk` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AttachedDisk {
    /// deviceName property.
    pub device_name: Option<String>,
    /// existingDisk property.
    pub existing_disk: Option<String>,
    /// newDisk property.
    pub new_disk: Option<Disk>,
}

/// `TaskSpec` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TaskSpec {
    /// computeResource property.
    pub compute_resource: Option<ComputeResource>,
    /// environment property.
    pub environment: Option<Environment>,
    /// environments property.
    pub environments: Option<serde_json::Value>,
    /// lifecyclePolicies property.
    pub lifecycle_policies: Option<Vec<LifecyclePolicy>>,
    /// maxRetryCount property.
    pub max_retry_count: Option<i64>,
    /// maxRunDuration property.
    pub max_run_duration: Option<String>,
    /// runnables property.
    pub runnables: Option<Vec<Runnable>>,
    /// volumes property.
    pub volumes: Option<Vec<Volume>>,
}

/// `InstancePolicyOrTemplate` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstancePolicyOrTemplate {
    /// blockProjectSshKeys property.
    pub block_project_ssh_keys: Option<bool>,
    /// installGpuDrivers property.
    pub install_gpu_drivers: Option<bool>,
    /// installOpsAgent property.
    pub install_ops_agent: Option<bool>,
    /// instanceTemplate property.
    pub instance_template: Option<String>,
    /// policy property.
    pub policy: Option<InstancePolicy>,
}

/// `NFS` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NFS {
    /// remotePath property.
    pub remote_path: Option<String>,
    /// server property.
    pub server: Option<String>,
}

/// `CloudLoggingOption` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CloudLoggingOption {
    /// useGenericTaskMonitoredResource property.
    pub use_generic_task_monitored_resource: Option<bool>,
}

/// `StatusEvent` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StatusEvent {
    /// description property.
    pub description: Option<String>,
    /// eventTime property.
    pub event_time: Option<String>,
    /// taskExecution property.
    pub task_execution: Option<TaskExecution>,
    /// taskState property.
    pub task_state: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Container` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Container {
    /// blockExternalNetwork property.
    pub block_external_network: Option<bool>,
    /// commands property.
    pub commands: Option<Vec<String>>,
    /// enableImageStreaming property.
    pub enable_image_streaming: Option<bool>,
    /// entrypoint property.
    pub entrypoint: Option<String>,
    /// imageUri property.
    pub image_uri: Option<String>,
    /// options property.
    pub options: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// username property.
    pub username: Option<String>,
    /// volumes property.
    pub volumes: Option<Vec<String>>,
}

/// `KMSEnvMap` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KMSEnvMap {
    /// cipherText property.
    pub cipher_text: Option<String>,
    /// keyName property.
    pub key_name: Option<String>,
}

/// `LogsPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LogsPolicy {
    /// cloudLoggingOption property.
    pub cloud_logging_option: Option<CloudLoggingOption>,
    /// destination property.
    pub destination: Option<String>,
    /// logsPath property.
    pub logs_path: Option<String>,
}

/// `JobNotification` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct JobNotification {
    /// message property.
    pub message: Option<Message>,
    /// pubsubTopic property.
    pub pubsub_topic: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`batch.projects.locations.jobs.cancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BatchProjectsLocationsJobsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`batch.projects.locations.jobs.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BatchProjectsLocationsJobsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `jobId`.
    pub job_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`batch.projects.locations.jobs.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BatchProjectsLocationsJobsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `reason`.
    pub reason: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`batch.projects.locations.jobs.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BatchProjectsLocationsJobsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`batch.projects.locations.jobs.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BatchProjectsLocationsJobsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}:cancel
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = batch_projects_locations_jobs_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn batch_projects_locations_jobs_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://batch.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs/{{jobsId}}:cancel",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/jobs
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = batch_projects_locations_jobs_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn batch_projects_locations_jobs_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://batch.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = batch_projects_locations_jobs_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn batch_projects_locations_jobs_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://batch.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs/{{jobsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = batch_projects_locations_jobs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn batch_projects_locations_jobs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://batch.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs/{{jobsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = batch_projects_locations_jobs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn batch_projects_locations_jobs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://batch.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
