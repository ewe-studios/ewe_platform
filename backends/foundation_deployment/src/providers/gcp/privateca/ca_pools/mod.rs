//! Auto-generated API module for gcp/privateca caPools.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_privateca_ca_pools `

#![cfg(feature = "gcp_privateca_ca_pools")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;
use super::shared::Policy;
use super::shared::TestIamPermissionsResponse;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `KeyUsageOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KeyUsageOptions {
    /// certSign property.
    pub cert_sign: Option<bool>,
    /// contentCommitment property.
    pub content_commitment: Option<bool>,
    /// crlSign property.
    pub crl_sign: Option<bool>,
    /// dataEncipherment property.
    pub data_encipherment: Option<bool>,
    /// decipherOnly property.
    pub decipher_only: Option<bool>,
    /// digitalSignature property.
    pub digital_signature: Option<bool>,
    /// encipherOnly property.
    pub encipher_only: Option<bool>,
    /// keyAgreement property.
    pub key_agreement: Option<bool>,
    /// keyEncipherment property.
    pub key_encipherment: Option<bool>,
}

/// `IssuanceModes` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IssuanceModes {
    /// allowConfigBasedIssuance property.
    pub allow_config_based_issuance: Option<bool>,
    /// allowCsrBasedIssuance property.
    pub allow_csr_based_issuance: Option<bool>,
}

/// `ObjectId` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ObjectId {
    /// objectIdPath property.
    pub object_id_path: Option<Vec<i64>>,
}

/// `Binding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `CaOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CaOptions {
    /// isCa property.
    pub is_ca: Option<bool>,
    /// maxIssuerPathLength property.
    pub max_issuer_path_length: Option<i64>,
}

/// `IssuancePolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IssuancePolicy {
    /// allowedIssuanceModes property.
    pub allowed_issuance_modes: Option<IssuanceModes>,
    /// allowedKeyTypes property.
    pub allowed_key_types: Option<Vec<AllowedKeyType>>,
    /// backdateDuration property.
    pub backdate_duration: Option<String>,
    /// baselineValues property.
    pub baseline_values: Option<X509Parameters>,
    /// identityConstraints property.
    pub identity_constraints: Option<CertificateIdentityConstraints>,
    /// maximumLifetime property.
    pub maximum_lifetime: Option<String>,
    /// passthroughExtensions property.
    pub passthrough_extensions: Option<CertificateExtensionConstraints>,
}

/// `ExtendedKeyUsageOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExtendedKeyUsageOptions {
    /// clientAuth property.
    pub client_auth: Option<bool>,
    /// codeSigning property.
    pub code_signing: Option<bool>,
    /// emailProtection property.
    pub email_protection: Option<bool>,
    /// ocspSigning property.
    pub ocsp_signing: Option<bool>,
    /// serverAuth property.
    pub server_auth: Option<bool>,
    /// timeStamping property.
    pub time_stamping: Option<bool>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `FetchCaCertsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FetchCaCertsResponse {
    /// caCerts property.
    pub ca_certs: Option<Vec<CertChain>>,
}

/// `X509Extension` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct X509Extension {
    /// critical property.
    pub critical: Option<bool>,
    /// objectId property.
    pub object_id: Option<ObjectId>,
    /// value property.
    pub value: Option<String>,
}

/// `RsaKeyType` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RsaKeyType {
    /// maxModulusSize property.
    pub max_modulus_size: Option<String>,
    /// minModulusSize property.
    pub min_modulus_size: Option<String>,
}

/// `CaPool` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CaPool {
    /// encryptionSpec property.
    pub encryption_spec: Option<EncryptionSpec>,
    /// issuancePolicy property.
    pub issuance_policy: Option<IssuancePolicy>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// publishingOptions property.
    pub publishing_options: Option<PublishingOptions>,
    /// tier property.
    pub tier: Option<String>,
}

/// `ListCaPoolsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListCaPoolsResponse {
    /// caPools property.
    pub ca_pools: Option<Vec<CaPool>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `Expr` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `X509Parameters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct X509Parameters {
    /// additionalExtensions property.
    pub additional_extensions: Option<Vec<X509Extension>>,
    /// aiaOcspServers property.
    pub aia_ocsp_servers: Option<Vec<String>>,
    /// caOptions property.
    pub ca_options: Option<CaOptions>,
    /// keyUsage property.
    pub key_usage: Option<KeyUsage>,
    /// nameConstraints property.
    pub name_constraints: Option<NameConstraints>,
    /// policyIds property.
    pub policy_ids: Option<Vec<ObjectId>>,
}

/// `CertificateIdentityConstraints` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CertificateIdentityConstraints {
    /// allowSubjectAltNamesPassthrough property.
    pub allow_subject_alt_names_passthrough: Option<bool>,
    /// allowSubjectPassthrough property.
    pub allow_subject_passthrough: Option<bool>,
    /// celExpression property.
    pub cel_expression: Option<Expr>,
}

/// `EncryptionSpec` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EncryptionSpec {
    /// cloudKmsKey property.
    pub cloud_kms_key: Option<String>,
}

/// `AllowedKeyType` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AllowedKeyType {
    /// ellipticCurve property.
    pub elliptic_curve: Option<EcKeyType>,
    /// rsa property.
    pub rsa: Option<RsaKeyType>,
}

/// `KeyUsage` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KeyUsage {
    /// baseKeyUsage property.
    pub base_key_usage: Option<KeyUsageOptions>,
    /// extendedKeyUsage property.
    pub extended_key_usage: Option<ExtendedKeyUsageOptions>,
    /// unknownExtendedKeyUsages property.
    pub unknown_extended_key_usages: Option<Vec<ObjectId>>,
}

/// `EcKeyType` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EcKeyType {
    /// signatureAlgorithm property.
    pub signature_algorithm: Option<String>,
}

/// `AuditLogConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditLogConfig {
    /// exemptedMembers property.
    pub exempted_members: Option<Vec<String>>,
    /// logType property.
    pub log_type: Option<String>,
}

/// `CertificateExtensionConstraints` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CertificateExtensionConstraints {
    /// additionalExtensions property.
    pub additional_extensions: Option<Vec<ObjectId>>,
    /// knownExtensions property.
    pub known_extensions: Option<Vec<String>>,
}

/// `NameConstraints` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NameConstraints {
    /// critical property.
    pub critical: Option<bool>,
    /// excludedDnsNames property.
    pub excluded_dns_names: Option<Vec<String>>,
    /// excludedEmailAddresses property.
    pub excluded_email_addresses: Option<Vec<String>>,
    /// excludedIpRanges property.
    pub excluded_ip_ranges: Option<Vec<String>>,
    /// excludedUris property.
    pub excluded_uris: Option<Vec<String>>,
    /// permittedDnsNames property.
    pub permitted_dns_names: Option<Vec<String>>,
    /// permittedEmailAddresses property.
    pub permitted_email_addresses: Option<Vec<String>>,
    /// permittedIpRanges property.
    pub permitted_ip_ranges: Option<Vec<String>>,
    /// permittedUris property.
    pub permitted_uris: Option<Vec<String>>,
}

/// `CertChain` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CertChain {
    /// certificates property.
    pub certificates: Option<Vec<String>>,
}

/// `PublishingOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PublishingOptions {
    /// encodingFormat property.
    pub encoding_format: Option<String>,
    /// publishCaCert property.
    pub publish_ca_cert: Option<bool>,
    /// publishCrl property.
    pub publish_crl: Option<bool>,
}

/// `AuditConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditConfig {
    /// auditLogConfigs property.
    pub audit_log_configs: Option<Vec<AuditLogConfig>>,
    /// service property.
    pub service: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`privateca.projects.locations.caPools.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `caPoolId`.
    pub ca_pool_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`privateca.projects.locations.caPools.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `ignoreDependentResources`.
    pub ignore_dependent_resources: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`privateca.projects.locations.caPools.fetchCaCerts_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsFetchCaCertsArgs {
    /// Path parameter: `caPool`.
    pub ca_pool: String,
}

/// Arguments for [`privateca.projects.locations.caPools.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`privateca.projects.locations.caPools.getIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `options.requestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`privateca.projects.locations.caPools.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`privateca.projects.locations.caPools.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`privateca.projects.locations.caPools.setIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`privateca.projects.locations.caPools.testIamPermissions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/caPools
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:fetchCaCerts
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:fetchCaCerts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_fetch_ca_certs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_fetch_ca_certs_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchCaCertsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}:fetchCaCerts",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FetchCaCertsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CaPool>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CaPool =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:getIamPolicy
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}:getIamPolicy",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/caPools
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCaPoolsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCaPoolsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}:setIamPolicy",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}:testIamPermissions",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
