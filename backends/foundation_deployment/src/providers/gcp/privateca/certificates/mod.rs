//! Auto-generated API module for gcp/privateca certificates.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_privateca_certificates `

#![cfg(feature = "gcp_privateca_certificates")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `KeyUsageOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KeyUsageOptions {
    /// certSign property.
    pub cert_sign: Option<bool>,
    /// contentCommitment property.
    pub content_commitment: Option<bool>,
    /// crlSign property.
    pub crl_sign: Option<bool>,
    /// dataEncipherment property.
    pub data_encipherment: Option<bool>,
    /// decipherOnly property.
    pub decipher_only: Option<bool>,
    /// digitalSignature property.
    pub digital_signature: Option<bool>,
    /// encipherOnly property.
    pub encipher_only: Option<bool>,
    /// keyAgreement property.
    pub key_agreement: Option<bool>,
    /// keyEncipherment property.
    pub key_encipherment: Option<bool>,
}

/// `ExtendedKeyUsageOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExtendedKeyUsageOptions {
    /// clientAuth property.
    pub client_auth: Option<bool>,
    /// codeSigning property.
    pub code_signing: Option<bool>,
    /// emailProtection property.
    pub email_protection: Option<bool>,
    /// ocspSigning property.
    pub ocsp_signing: Option<bool>,
    /// serverAuth property.
    pub server_auth: Option<bool>,
    /// timeStamping property.
    pub time_stamping: Option<bool>,
}

/// `Subject` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Subject {
    /// commonName property.
    pub common_name: Option<String>,
    /// countryCode property.
    pub country_code: Option<String>,
    /// locality property.
    pub locality: Option<String>,
    /// organization property.
    pub organization: Option<String>,
    /// organizationalUnit property.
    pub organizational_unit: Option<String>,
    /// postalCode property.
    pub postal_code: Option<String>,
    /// province property.
    pub province: Option<String>,
    /// rdnSequence property.
    pub rdn_sequence: Option<Vec<RelativeDistinguishedName>>,
    /// streetAddress property.
    pub street_address: Option<String>,
}

/// `RelativeDistinguishedName` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RelativeDistinguishedName {
    /// attributes property.
    pub attributes: Option<Vec<AttributeTypeAndValue>>,
}

/// `KeyId` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KeyId {
    /// keyId property.
    pub key_id: Option<String>,
}

/// `CertificateFingerprint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CertificateFingerprint {
    /// sha256Hash property.
    pub sha256_hash: Option<String>,
}

/// `SubjectDescription` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubjectDescription {
    /// hexSerialNumber property.
    pub hex_serial_number: Option<String>,
    /// lifetime property.
    pub lifetime: Option<String>,
    /// notAfterTime property.
    pub not_after_time: Option<String>,
    /// notBeforeTime property.
    pub not_before_time: Option<String>,
    /// subject property.
    pub subject: Option<Subject>,
    /// subjectAltName property.
    pub subject_alt_name: Option<SubjectAltNames>,
}

/// `SubjectAltNames` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubjectAltNames {
    /// customSans property.
    pub custom_sans: Option<Vec<X509Extension>>,
    /// dnsNames property.
    pub dns_names: Option<Vec<String>>,
    /// emailAddresses property.
    pub email_addresses: Option<Vec<String>>,
    /// ipAddresses property.
    pub ip_addresses: Option<Vec<String>>,
    /// uris property.
    pub uris: Option<Vec<String>>,
}

/// `CertificateConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CertificateConfig {
    /// publicKey property.
    pub public_key: Option<PublicKey>,
    /// subjectConfig property.
    pub subject_config: Option<SubjectConfig>,
    /// subjectKeyId property.
    pub subject_key_id: Option<CertificateConfigKeyId>,
    /// x509Config property.
    pub x509_config: Option<X509Parameters>,
}

/// `CaOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CaOptions {
    /// isCa property.
    pub is_ca: Option<bool>,
    /// maxIssuerPathLength property.
    pub max_issuer_path_length: Option<i64>,
}

/// `ObjectId` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ObjectId {
    /// objectIdPath property.
    pub object_id_path: Option<Vec<i64>>,
}

/// `CertificateConfigKeyId` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CertificateConfigKeyId {
    /// keyId property.
    pub key_id: Option<String>,
}

/// `ListCertificatesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListCertificatesResponse {
    /// certificates property.
    pub certificates: Option<Vec<Certificate>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `AttributeTypeAndValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AttributeTypeAndValue {
    /// objectId property.
    pub object_id: Option<ObjectId>,
    /// type property.
    pub r#type: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `SubjectConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SubjectConfig {
    /// subject property.
    pub subject: Option<Subject>,
    /// subjectAltName property.
    pub subject_alt_name: Option<SubjectAltNames>,
}

/// `NameConstraints` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NameConstraints {
    /// critical property.
    pub critical: Option<bool>,
    /// excludedDnsNames property.
    pub excluded_dns_names: Option<Vec<String>>,
    /// excludedEmailAddresses property.
    pub excluded_email_addresses: Option<Vec<String>>,
    /// excludedIpRanges property.
    pub excluded_ip_ranges: Option<Vec<String>>,
    /// excludedUris property.
    pub excluded_uris: Option<Vec<String>>,
    /// permittedDnsNames property.
    pub permitted_dns_names: Option<Vec<String>>,
    /// permittedEmailAddresses property.
    pub permitted_email_addresses: Option<Vec<String>>,
    /// permittedIpRanges property.
    pub permitted_ip_ranges: Option<Vec<String>>,
    /// permittedUris property.
    pub permitted_uris: Option<Vec<String>>,
}

/// `Certificate` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Certificate {
    /// certificateDescription property.
    pub certificate_description: Option<CertificateDescription>,
    /// certificateTemplate property.
    pub certificate_template: Option<String>,
    /// config property.
    pub config: Option<CertificateConfig>,
    /// createTime property.
    pub create_time: Option<String>,
    /// issuerCertificateAuthority property.
    pub issuer_certificate_authority: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// lifetime property.
    pub lifetime: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// pemCertificate property.
    pub pem_certificate: Option<String>,
    /// pemCertificateChain property.
    pub pem_certificate_chain: Option<Vec<String>>,
    /// pemCsr property.
    pub pem_csr: Option<String>,
    /// revocationDetails property.
    pub revocation_details: Option<RevocationDetails>,
    /// subjectMode property.
    pub subject_mode: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `KeyUsage` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KeyUsage {
    /// baseKeyUsage property.
    pub base_key_usage: Option<KeyUsageOptions>,
    /// extendedKeyUsage property.
    pub extended_key_usage: Option<ExtendedKeyUsageOptions>,
    /// unknownExtendedKeyUsages property.
    pub unknown_extended_key_usages: Option<Vec<ObjectId>>,
}

/// `X509Parameters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct X509Parameters {
    /// additionalExtensions property.
    pub additional_extensions: Option<Vec<X509Extension>>,
    /// aiaOcspServers property.
    pub aia_ocsp_servers: Option<Vec<String>>,
    /// caOptions property.
    pub ca_options: Option<CaOptions>,
    /// keyUsage property.
    pub key_usage: Option<KeyUsage>,
    /// nameConstraints property.
    pub name_constraints: Option<NameConstraints>,
    /// policyIds property.
    pub policy_ids: Option<Vec<ObjectId>>,
}

/// `X509Extension` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct X509Extension {
    /// critical property.
    pub critical: Option<bool>,
    /// objectId property.
    pub object_id: Option<ObjectId>,
    /// value property.
    pub value: Option<String>,
}

/// `CertificateDescription` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CertificateDescription {
    /// aiaIssuingCertificateUrls property.
    pub aia_issuing_certificate_urls: Option<Vec<String>>,
    /// authorityKeyId property.
    pub authority_key_id: Option<KeyId>,
    /// certFingerprint property.
    pub cert_fingerprint: Option<CertificateFingerprint>,
    /// crlDistributionPoints property.
    pub crl_distribution_points: Option<Vec<String>>,
    /// publicKey property.
    pub public_key: Option<PublicKey>,
    /// subjectDescription property.
    pub subject_description: Option<SubjectDescription>,
    /// subjectKeyId property.
    pub subject_key_id: Option<KeyId>,
    /// tbsCertificateDigest property.
    pub tbs_certificate_digest: Option<String>,
    /// x509Description property.
    pub x509_description: Option<X509Parameters>,
}

/// `PublicKey` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PublicKey {
    /// format property.
    pub format: Option<String>,
    /// key property.
    pub key: Option<String>,
}

/// `RevocationDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RevocationDetails {
    /// revocationState property.
    pub revocation_state: Option<String>,
    /// revocationTime property.
    pub revocation_time: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`privateca.projects.locations.caPools.certificates.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificatesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `certificateId`.
    pub certificate_id: Option<String>,
    /// Query parameter: `issuingCertificateAuthorityId`.
    pub issuing_certificate_authority_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`privateca.projects.locations.caPools.certificates.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificatesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`privateca.projects.locations.caPools.certificates.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificatesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`privateca.projects.locations.caPools.certificates.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificatesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`privateca.projects.locations.caPools.certificates.revoke_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificatesRevokeArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_certificates_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_certificates_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Certificate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}/certificates",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Certificate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_certificates_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_certificates_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Certificate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}/certificates/{{certificatesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Certificate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_certificates_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_certificates_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCertificatesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}/certificates",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCertificatesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_certificates_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_certificates_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Certificate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}/certificates/{{certificatesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Certificate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}:revoke
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}:revoke.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = privateca_projects_locations_ca_pools_certificates_revoke_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn privateca_projects_locations_ca_pools_certificates_revoke_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Certificate>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/caPools/{{caPoolsId}}/certificates/{{certificatesId}}:revoke",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Certificate =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
