//! Auto-generated API module for gcp/contactcenteraiplatform contactCenters.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_contactcenteraiplatform_contact_centers `

#![cfg(feature = "gcp_contactcenteraiplatform_contact_centers")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `WeeklySchedule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WeeklySchedule {
    /// days property.
    pub days: Option<Vec<String>>,
    /// duration property.
    pub duration: Option<String>,
    /// endTime property.
    pub end_time: Option<TimeOfDay>,
    /// startTime property.
    pub start_time: Option<TimeOfDay>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `AdminUser` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AdminUser {
    /// familyName property.
    pub family_name: Option<String>,
    /// givenName property.
    pub given_name: Option<String>,
}

/// `PscSetting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PscSetting {
    /// allowedConsumerProjectIds property.
    pub allowed_consumer_project_ids: Option<Vec<String>>,
    /// producerProjectIds property.
    pub producer_project_ids: Option<Vec<String>>,
}

/// `SAMLParams` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SAMLParams {
    /// authenticationContexts property.
    pub authentication_contexts: Option<Vec<String>>,
    /// certificate property.
    pub certificate: Option<String>,
    /// emailMapping property.
    pub email_mapping: Option<String>,
    /// entityId property.
    pub entity_id: Option<String>,
    /// ssoUri property.
    pub sso_uri: Option<String>,
    /// userEmail property.
    pub user_email: Option<String>,
}

/// `Early` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Early {}

/// `PrivateAccess` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PrivateAccess {
    /// egressSettings property.
    pub egress_settings: Option<Vec<Component>>,
    /// ingressSettings property.
    pub ingress_settings: Option<Vec<Component>>,
    /// pscSetting property.
    pub psc_setting: Option<PscSetting>,
}

/// `TimeOfDay` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeOfDay {
    /// hours property.
    pub hours: Option<i64>,
    /// minutes property.
    pub minutes: Option<i64>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<i64>,
}

/// `ContactCenter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ContactCenter {
    /// adminUser property.
    pub admin_user: Option<AdminUser>,
    /// advancedReportingEnabled property.
    pub advanced_reporting_enabled: Option<bool>,
    /// ccaipManagedUsers property.
    pub ccaip_managed_users: Option<bool>,
    /// createTime property.
    pub create_time: Option<String>,
    /// critical property.
    pub critical: Option<Critical>,
    /// customerDomainPrefix property.
    pub customer_domain_prefix: Option<String>,
    /// deleteTime property.
    pub delete_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// early property.
    pub early: Option<Early>,
    /// expireTime property.
    pub expire_time: Option<String>,
    /// featureConfig property.
    pub feature_config: Option<FeatureConfig>,
    /// instanceConfig property.
    pub instance_config: Option<InstanceConfig>,
    /// kmsKey property.
    pub kms_key: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// normal property.
    pub normal: Option<Normal>,
    /// privateAccess property.
    pub private_access: Option<PrivateAccess>,
    /// privateComponents property.
    pub private_components: Option<Vec<String>>,
    /// purgeTime property.
    pub purge_time: Option<String>,
    /// releaseVersion property.
    pub release_version: Option<String>,
    /// samlParams property.
    pub saml_params: Option<SAMLParams>,
    /// state property.
    pub state: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// uris property.
    pub uris: Option<URIs>,
    /// userEmail property.
    pub user_email: Option<String>,
}

/// `Component` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Component {
    /// name property.
    pub name: Option<String>,
    /// serviceAttachmentNames property.
    pub service_attachment_names: Option<Vec<String>>,
}

/// `ListContactCentersResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListContactCentersResponse {
    /// contactCenters property.
    pub contact_centers: Option<Vec<ContactCenter>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `URIs` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct URIs {
    /// chatBotUri property.
    pub chat_bot_uri: Option<String>,
    /// mediaUri property.
    pub media_uri: Option<String>,
    /// rootUri property.
    pub root_uri: Option<String>,
    /// virtualAgentStreamingServiceUri property.
    pub virtual_agent_streaming_service_uri: Option<String>,
}

/// `FeatureConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FeatureConfig {
    /// agentDesktopEnabled property.
    pub agent_desktop_enabled: Option<bool>,
}

/// `Normal` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Normal {}

/// `InstanceConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstanceConfig {
    /// instanceSize property.
    pub instance_size: Option<String>,
}

/// `Critical` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Critical {
    /// peakHours property.
    pub peak_hours: Option<Vec<WeeklySchedule>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`contactcenteraiplatform.projects.locations.contactCenters.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsContactCentersCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `contactCenterId`.
    pub contact_center_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`contactcenteraiplatform.projects.locations.contactCenters.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsContactCentersDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`contactcenteraiplatform.projects.locations.contactCenters.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsContactCentersGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`contactcenteraiplatform.projects.locations.contactCenters.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsContactCentersListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`contactcenteraiplatform.projects.locations.contactCenters.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ContactcenteraiplatformProjectsLocationsContactCentersPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters
// -----------------------------------------------------------------------------

/// POST v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = contactcenteraiplatform_projects_locations_contact_centers_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn contactcenteraiplatform_projects_locations_contact_centers_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{{projectsId}}/locations/{{locationsId}}/contactCenters",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
// -----------------------------------------------------------------------------

/// DELETE v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = contactcenteraiplatform_projects_locations_contact_centers_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn contactcenteraiplatform_projects_locations_contact_centers_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{{projectsId}}/locations/{{locationsId}}/contactCenters/{{contactCentersId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
// -----------------------------------------------------------------------------

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = contactcenteraiplatform_projects_locations_contact_centers_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn contactcenteraiplatform_projects_locations_contact_centers_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ContactCenter>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{{projectsId}}/locations/{{locationsId}}/contactCenters/{{contactCentersId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ContactCenter =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters
// -----------------------------------------------------------------------------

/// GET v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = contactcenteraiplatform_projects_locations_contact_centers_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn contactcenteraiplatform_projects_locations_contact_centers_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListContactCentersResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{{projectsId}}/locations/{{locationsId}}/contactCenters",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListContactCentersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}
// -----------------------------------------------------------------------------

/// PATCH v1alpha1/projects/{projectsId}/locations/{locationsId}/contactCenters/{contactCentersId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = contactcenteraiplatform_projects_locations_contact_centers_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn contactcenteraiplatform_projects_locations_contact_centers_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://contactcenteraiplatform.googleapis.com/v1alpha1/projects/{{projectsId}}/locations/{{locationsId}}/contactCenters/{{contactCentersId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
