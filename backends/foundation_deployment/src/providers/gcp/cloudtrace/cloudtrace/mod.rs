//! Auto-generated API module for gcp/cloudtrace cloudtrace.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_cloudtrace_cloudtrace `

#![cfg(feature = "gcp_cloudtrace_cloudtrace")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `TruncatableString` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TruncatableString {
    /// truncatedByteCount property.
    pub truncated_byte_count: Option<i64>,
    /// value property.
    pub value: Option<String>,
}

/// `BatchWriteSpansRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BatchWriteSpansRequest {
    /// spans property.
    pub spans: Option<Vec<Span>>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `Links` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Links {
    /// droppedLinksCount property.
    pub dropped_links_count: Option<i64>,
    /// link property.
    pub link: Option<Vec<Link>>,
}

/// `StackFrame` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StackFrame {
    /// columnNumber property.
    pub column_number: Option<String>,
    /// fileName property.
    pub file_name: Option<TruncatableString>,
    /// functionName property.
    pub function_name: Option<TruncatableString>,
    /// lineNumber property.
    pub line_number: Option<String>,
    /// loadModule property.
    pub load_module: Option<Module>,
    /// originalFunctionName property.
    pub original_function_name: Option<TruncatableString>,
    /// sourceVersion property.
    pub source_version: Option<TruncatableString>,
}

/// `TimeEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeEvent {
    /// annotation property.
    pub annotation: Option<Annotation>,
    /// messageEvent property.
    pub message_event: Option<MessageEvent>,
    /// time property.
    pub time: Option<String>,
}

/// `Link` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Link {
    /// attributes property.
    pub attributes: Option<Attributes>,
    /// spanId property.
    pub span_id: Option<String>,
    /// traceId property.
    pub trace_id: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `MessageEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MessageEvent {
    /// compressedSizeBytes property.
    pub compressed_size_bytes: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// uncompressedSizeBytes property.
    pub uncompressed_size_bytes: Option<String>,
}

/// `StackTrace` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StackTrace {
    /// stackFrames property.
    pub stack_frames: Option<StackFrames>,
    /// stackTraceHashId property.
    pub stack_trace_hash_id: Option<String>,
}

/// `Span` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Span {
    /// attributes property.
    pub attributes: Option<Attributes>,
    /// childSpanCount property.
    pub child_span_count: Option<i64>,
    /// displayName property.
    pub display_name: Option<TruncatableString>,
    /// endTime property.
    pub end_time: Option<String>,
    /// links property.
    pub links: Option<Links>,
    /// name property.
    pub name: Option<String>,
    /// parentSpanId property.
    pub parent_span_id: Option<String>,
    /// sameProcessAsParentSpan property.
    pub same_process_as_parent_span: Option<bool>,
    /// spanId property.
    pub span_id: Option<String>,
    /// spanKind property.
    pub span_kind: Option<String>,
    /// stackTrace property.
    pub stack_trace: Option<StackTrace>,
    /// startTime property.
    pub start_time: Option<String>,
    /// status property.
    pub status: Option<Status>,
    /// timeEvents property.
    pub time_events: Option<TimeEvents>,
}

/// `TimeEvents` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeEvents {
    /// droppedAnnotationsCount property.
    pub dropped_annotations_count: Option<i64>,
    /// droppedMessageEventsCount property.
    pub dropped_message_events_count: Option<i64>,
    /// timeEvent property.
    pub time_event: Option<Vec<TimeEvent>>,
}

/// `Module` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Module {
    /// buildId property.
    pub build_id: Option<TruncatableString>,
    /// module property.
    pub module: Option<TruncatableString>,
}

/// `Annotation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Annotation {
    /// attributes property.
    pub attributes: Option<Attributes>,
    /// description property.
    pub description: Option<TruncatableString>,
}

/// `Attributes` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Attributes {
    /// attributeMap property.
    pub attribute_map: Option<serde_json::Value>,
    /// droppedAttributesCount property.
    pub dropped_attributes_count: Option<i64>,
}

/// `StackFrames` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StackFrames {
    /// droppedFramesCount property.
    pub dropped_frames_count: Option<i64>,
    /// frame property.
    pub frame: Option<Vec<StackFrame>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`cloudtrace.projects.traces.batchWrite_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudtraceProjectsTracesBatchWriteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: BatchWriteSpansRequest,
}

/// Arguments for [`cloudtrace.projects.traces.spans.createSpan_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct CloudtraceProjectsTracesSpansCreateSpanArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: Span,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v2/{+name}/traces:batchWrite
// -----------------------------------------------------------------------------

/// POST v2/{+name}/traces:batchWrite.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudtrace_projects_traces_batch_write_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudtrace_projects_traces_batch_write_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudtraceProjectsTracesBatchWriteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudtrace.googleapis.com/v2/{}/traces:batchWrite",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}
// -----------------------------------------------------------------------------

/// POST v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudtrace_projects_traces_spans_create_span_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudtrace_projects_traces_spans_create_span_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudtraceProjectsTracesSpansCreateSpanArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Span>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://cloudtrace.googleapis.com/v2/{}", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Span =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
