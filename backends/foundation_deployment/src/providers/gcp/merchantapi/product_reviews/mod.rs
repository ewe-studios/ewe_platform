//! Auto-generated API module for gcp/merchantapi productReviews.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_merchantapi_product_reviews `

#![cfg(feature = "gcp_merchantapi_product_reviews")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::ProductReview;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ReviewLink` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReviewLink {
    /// link property.
    pub link: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `CustomAttribute` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomAttribute {
    /// groupValues property.
    pub group_values: Option<Vec<Box<CustomAttribute>>>,
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `ProductReviewDestinationStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductReviewDestinationStatus {
    /// reportingContext property.
    pub reporting_context: Option<String>,
}

/// `ProductReviewItemLevelIssue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductReviewItemLevelIssue {
    /// attribute property.
    pub attribute: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// detail property.
    pub detail: Option<String>,
    /// documentation property.
    pub documentation: Option<String>,
    /// reportingContext property.
    pub reporting_context: Option<String>,
    /// resolution property.
    pub resolution: Option<String>,
    /// severity property.
    pub severity: Option<String>,
}

/// `ProductReviewAttributes` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductReviewAttributes {
    /// aggregatorName property.
    pub aggregator_name: Option<String>,
    /// asins property.
    pub asins: Option<Vec<String>>,
    /// brands property.
    pub brands: Option<Vec<String>>,
    /// collectionMethod property.
    pub collection_method: Option<String>,
    /// cons property.
    pub cons: Option<Vec<String>>,
    /// content property.
    pub content: Option<String>,
    /// gtins property.
    pub gtins: Option<Vec<String>>,
    /// isIncentivizedReview property.
    pub is_incentivized_review: Option<bool>,
    /// isSpam property.
    pub is_spam: Option<bool>,
    /// isVerifiedPurchase property.
    pub is_verified_purchase: Option<bool>,
    /// maxRating property.
    pub max_rating: Option<String>,
    /// minRating property.
    pub min_rating: Option<String>,
    /// mpns property.
    pub mpns: Option<Vec<String>>,
    /// productLinks property.
    pub product_links: Option<Vec<String>>,
    /// productNames property.
    pub product_names: Option<Vec<String>>,
    /// pros property.
    pub pros: Option<Vec<String>>,
    /// publisherFavicon property.
    pub publisher_favicon: Option<String>,
    /// publisherName property.
    pub publisher_name: Option<String>,
    /// rating property.
    pub rating: Option<f64>,
    /// reviewCountry property.
    pub review_country: Option<String>,
    /// reviewLanguage property.
    pub review_language: Option<String>,
    /// reviewLink property.
    pub review_link: Option<ReviewLink>,
    /// reviewTime property.
    pub review_time: Option<String>,
    /// reviewerId property.
    pub reviewer_id: Option<String>,
    /// reviewerImageLinks property.
    pub reviewer_image_links: Option<Vec<String>>,
    /// reviewerIsAnonymous property.
    pub reviewer_is_anonymous: Option<bool>,
    /// reviewerUsername property.
    pub reviewer_username: Option<String>,
    /// skus property.
    pub skus: Option<Vec<String>>,
    /// subclientName property.
    pub subclient_name: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// transactionId property.
    pub transaction_id: Option<String>,
}

/// `ListProductReviewsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListProductReviewsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// productReviews property.
    pub product_reviews: Option<Vec<ProductReview>>,
}

/// `ProductReviewStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductReviewStatus {
    /// createTime property.
    pub create_time: Option<String>,
    /// destinationStatuses property.
    pub destination_statuses: Option<Vec<ProductReviewDestinationStatus>>,
    /// itemLevelIssues property.
    pub item_level_issues: Option<Vec<ProductReviewItemLevelIssue>>,
    /// lastUpdateTime property.
    pub last_update_time: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`merchantapi.accounts.productReviews.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MerchantapiAccountsProductReviewsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`merchantapi.accounts.productReviews.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MerchantapiAccountsProductReviewsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`merchantapi.accounts.productReviews.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MerchantapiAccountsProductReviewsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// DELETE reviews/v1beta/accounts/{accountsId}/productReviews/{productReviewsId}
// -----------------------------------------------------------------------------

/// DELETE reviews/v1beta/accounts/{accountsId}/productReviews/{productReviewsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = merchantapi_accounts_product_reviews_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn merchantapi_accounts_product_reviews_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://merchantapi.googleapis.com/reviews/v1beta/accounts/{{accountsId}}/productReviews/{{productReviewsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET reviews/v1beta/accounts/{accountsId}/productReviews/{productReviewsId}
// -----------------------------------------------------------------------------

/// GET reviews/v1beta/accounts/{accountsId}/productReviews/{productReviewsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = merchantapi_accounts_product_reviews_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn merchantapi_accounts_product_reviews_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProductReview>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://merchantapi.googleapis.com/reviews/v1beta/accounts/{{accountsId}}/productReviews/{{productReviewsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProductReview =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET reviews/v1beta/accounts/{accountsId}/productReviews
// -----------------------------------------------------------------------------

/// GET reviews/v1beta/accounts/{accountsId}/productReviews.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = merchantapi_accounts_product_reviews_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn merchantapi_accounts_product_reviews_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProductReviewsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://merchantapi.googleapis.com/reviews/v1beta/accounts/{{accountsId}}/productReviews",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListProductReviewsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
