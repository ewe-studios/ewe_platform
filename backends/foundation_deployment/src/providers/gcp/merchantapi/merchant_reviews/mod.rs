//! Auto-generated API module for gcp/merchantapi merchantReviews.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_merchantapi_merchant_reviews `

#![cfg(feature = "gcp_merchantapi_merchant_reviews")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::MerchantReview;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `MerchantReviewDestinationStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MerchantReviewDestinationStatus {
    /// reportingContext property.
    pub reporting_context: Option<String>,
}

/// `MerchantReviewStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MerchantReviewStatus {
    /// createTime property.
    pub create_time: Option<String>,
    /// destinationStatuses property.
    pub destination_statuses: Option<Vec<MerchantReviewDestinationStatus>>,
    /// itemLevelIssues property.
    pub item_level_issues: Option<Vec<MerchantReviewItemLevelIssue>>,
    /// lastUpdateTime property.
    pub last_update_time: Option<String>,
}

/// `CustomAttribute` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomAttribute {
    /// groupValues property.
    pub group_values: Option<Vec<CustomAttribute>>,
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `ListMerchantReviewsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListMerchantReviewsResponse {
    /// merchantReviews property.
    pub merchant_reviews: Option<Vec<MerchantReview>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `MerchantReviewAttributes` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MerchantReviewAttributes {
    /// collectionMethod property.
    pub collection_method: Option<String>,
    /// content property.
    pub content: Option<String>,
    /// isAnonymous property.
    pub is_anonymous: Option<bool>,
    /// maxRating property.
    pub max_rating: Option<String>,
    /// merchantDisplayName property.
    pub merchant_display_name: Option<String>,
    /// merchantId property.
    pub merchant_id: Option<String>,
    /// merchantLink property.
    pub merchant_link: Option<String>,
    /// merchantRatingLink property.
    pub merchant_rating_link: Option<String>,
    /// minRating property.
    pub min_rating: Option<String>,
    /// rating property.
    pub rating: Option<f64>,
    /// reviewCountry property.
    pub review_country: Option<String>,
    /// reviewLanguage property.
    pub review_language: Option<String>,
    /// reviewTime property.
    pub review_time: Option<String>,
    /// reviewerId property.
    pub reviewer_id: Option<String>,
    /// reviewerUsername property.
    pub reviewer_username: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `MerchantReviewItemLevelIssue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MerchantReviewItemLevelIssue {
    /// attribute property.
    pub attribute: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// detail property.
    pub detail: Option<String>,
    /// documentation property.
    pub documentation: Option<String>,
    /// reportingContext property.
    pub reporting_context: Option<String>,
    /// resolution property.
    pub resolution: Option<String>,
    /// severity property.
    pub severity: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`merchantapi.accounts.merchantReviews.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MerchantapiAccountsMerchantReviewsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`merchantapi.accounts.merchantReviews.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MerchantapiAccountsMerchantReviewsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`merchantapi.accounts.merchantReviews.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MerchantapiAccountsMerchantReviewsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// DELETE reviews/v1beta/accounts/{accountsId}/merchantReviews/{merchantReviewsId}
// -----------------------------------------------------------------------------

/// DELETE reviews/v1beta/accounts/{accountsId}/merchantReviews/{merchantReviewsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = merchantapi_accounts_merchant_reviews_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn merchantapi_accounts_merchant_reviews_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://merchantapi.googleapis.com/reviews/v1beta/accounts/{{accountsId}}/merchantReviews/{{merchantReviewsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET reviews/v1beta/accounts/{accountsId}/merchantReviews/{merchantReviewsId}
// -----------------------------------------------------------------------------

/// GET reviews/v1beta/accounts/{accountsId}/merchantReviews/{merchantReviewsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = merchantapi_accounts_merchant_reviews_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn merchantapi_accounts_merchant_reviews_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MerchantReview>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://merchantapi.googleapis.com/reviews/v1beta/accounts/{{accountsId}}/merchantReviews/{{merchantReviewsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: MerchantReview =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET reviews/v1beta/accounts/{accountsId}/merchantReviews
// -----------------------------------------------------------------------------

/// GET reviews/v1beta/accounts/{accountsId}/merchantReviews.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = merchantapi_accounts_merchant_reviews_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn merchantapi_accounts_merchant_reviews_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMerchantReviewsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://merchantapi.googleapis.com/reviews/v1beta/accounts/{{accountsId}}/merchantReviews",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListMerchantReviewsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
