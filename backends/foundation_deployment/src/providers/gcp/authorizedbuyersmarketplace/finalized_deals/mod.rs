//! Auto-generated API module for gcp/authorizedbuyersmarketplace finalizedDeals.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_authorizedbuyersmarketplace_finalized_deals `

#![cfg(feature = "gcp_authorizedbuyersmarketplace_finalized_deals")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `InventoryTypeTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InventoryTypeTargeting {
    /// inventoryTypes property.
    pub inventory_types: Option<Vec<String>>,
}

/// `AdSize` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AdSize {
    /// height property.
    pub height: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// width property.
    pub width: Option<String>,
}

/// `FinalizedDeal` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FinalizedDeal {
    /// deal property.
    pub deal: Option<Deal>,
    /// dealPausingInfo property.
    pub deal_pausing_info: Option<DealPausingInfo>,
    /// dealServingStatus property.
    pub deal_serving_status: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// readyToServe property.
    pub ready_to_serve: Option<bool>,
    /// rtbMetrics property.
    pub rtb_metrics: Option<RtbMetrics>,
}

/// `ProgrammaticGuaranteedTerms` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProgrammaticGuaranteedTerms {
    /// fixedPrice property.
    pub fixed_price: Option<Price>,
    /// guaranteedLooks property.
    pub guaranteed_looks: Option<String>,
    /// impressionCap property.
    pub impression_cap: Option<String>,
    /// minimumDailyLooks property.
    pub minimum_daily_looks: Option<String>,
    /// percentShareOfVoice property.
    pub percent_share_of_voice: Option<String>,
    /// reservationType property.
    pub reservation_type: Option<String>,
}

/// `FirstPartyMobileApplicationTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FirstPartyMobileApplicationTargeting {
    /// excludedAppIds property.
    pub excluded_app_ids: Option<Vec<String>>,
    /// targetedAppIds property.
    pub targeted_app_ids: Option<Vec<String>>,
}

/// `FrequencyCap` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FrequencyCap {
    /// maxImpressions property.
    pub max_impressions: Option<i64>,
    /// timeUnitType property.
    pub time_unit_type: Option<String>,
    /// timeUnitsCount property.
    pub time_units_count: Option<i64>,
}

/// `DayPartTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DayPartTargeting {
    /// dayParts property.
    pub day_parts: Option<Vec<DayPart>>,
    /// timeZoneType property.
    pub time_zone_type: Option<String>,
}

/// `TimeZone` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeZone {
    /// id property.
    pub id: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `CriteriaTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CriteriaTargeting {
    /// excludedCriteriaIds property.
    pub excluded_criteria_ids: Option<Vec<String>>,
    /// targetedCriteriaIds property.
    pub targeted_criteria_ids: Option<Vec<String>>,
}

/// `MarketplaceTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MarketplaceTargeting {
    /// daypartTargeting property.
    pub daypart_targeting: Option<DayPartTargeting>,
    /// excludedSensitiveCategoryIds property.
    pub excluded_sensitive_category_ids: Option<Vec<String>>,
    /// geoTargeting property.
    pub geo_targeting: Option<CriteriaTargeting>,
    /// inventorySizeTargeting property.
    pub inventory_size_targeting: Option<InventorySizeTargeting>,
    /// inventoryTypeTargeting property.
    pub inventory_type_targeting: Option<InventoryTypeTargeting>,
    /// placementTargeting property.
    pub placement_targeting: Option<PlacementTargeting>,
    /// technologyTargeting property.
    pub technology_targeting: Option<TechnologyTargeting>,
    /// userListTargeting property.
    pub user_list_targeting: Option<CriteriaTargeting>,
    /// verticalTargeting property.
    pub vertical_targeting: Option<CriteriaTargeting>,
    /// videoTargeting property.
    pub video_targeting: Option<VideoTargeting>,
}

/// `RtbMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RtbMetrics {
    /// adImpressions7Days property.
    pub ad_impressions7_days: Option<String>,
    /// bidRate7Days property.
    pub bid_rate7_days: Option<f64>,
    /// bidRequests7Days property.
    pub bid_requests7_days: Option<String>,
    /// bids7Days property.
    pub bids7_days: Option<String>,
    /// filteredBidRate7Days property.
    pub filtered_bid_rate7_days: Option<f64>,
    /// mustBidRateCurrentMonth property.
    pub must_bid_rate_current_month: Option<f64>,
}

/// `CreativeRequirements` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreativeRequirements {
    /// creativeFormat property.
    pub creative_format: Option<String>,
    /// creativePreApprovalPolicy property.
    pub creative_pre_approval_policy: Option<String>,
    /// creativeSafeFrameCompatibility property.
    pub creative_safe_frame_compatibility: Option<String>,
    /// maxAdDurationMs property.
    pub max_ad_duration_ms: Option<String>,
    /// programmaticCreativeSource property.
    pub programmatic_creative_source: Option<String>,
    /// skippableAdType property.
    pub skippable_ad_type: Option<String>,
}

/// `PreferredDealTerms` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PreferredDealTerms {
    /// fixedPrice property.
    pub fixed_price: Option<Price>,
}

/// `DeliveryControl` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeliveryControl {
    /// companionDeliveryType property.
    pub companion_delivery_type: Option<String>,
    /// creativeRotationType property.
    pub creative_rotation_type: Option<String>,
    /// deliveryRateType property.
    pub delivery_rate_type: Option<String>,
    /// frequencyCap property.
    pub frequency_cap: Option<Vec<FrequencyCap>>,
    /// roadblockingType property.
    pub roadblocking_type: Option<String>,
}

/// `MediaPlanner` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MediaPlanner {
    /// accountId property.
    pub account_id: Option<String>,
    /// ancestorNames property.
    pub ancestor_names: Option<Vec<String>>,
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `DayPart` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DayPart {
    /// dayOfWeek property.
    pub day_of_week: Option<String>,
    /// endTime property.
    pub end_time: Option<TimeOfDay>,
    /// startTime property.
    pub start_time: Option<TimeOfDay>,
}

/// `PlacementTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlacementTargeting {
    /// mobileApplicationTargeting property.
    pub mobile_application_targeting: Option<MobileApplicationTargeting>,
    /// uriTargeting property.
    pub uri_targeting: Option<UriTargeting>,
}

/// `UriTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UriTargeting {
    /// excludedUris property.
    pub excluded_uris: Option<Vec<String>>,
    /// targetedUris property.
    pub targeted_uris: Option<Vec<String>>,
}

/// `VideoTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VideoTargeting {
    /// excludedPositionTypes property.
    pub excluded_position_types: Option<Vec<String>>,
    /// targetedPositionTypes property.
    pub targeted_position_types: Option<Vec<String>>,
}

/// `TimeOfDay` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeOfDay {
    /// hours property.
    pub hours: Option<i64>,
    /// minutes property.
    pub minutes: Option<i64>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<i64>,
}

/// `TechnologyTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TechnologyTargeting {
    /// deviceCapabilityTargeting property.
    pub device_capability_targeting: Option<CriteriaTargeting>,
    /// deviceCategoryTargeting property.
    pub device_category_targeting: Option<CriteriaTargeting>,
    /// operatingSystemTargeting property.
    pub operating_system_targeting: Option<OperatingSystemTargeting>,
}

/// `ListFinalizedDealsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListFinalizedDealsResponse {
    /// finalizedDeals property.
    pub finalized_deals: Option<Vec<FinalizedDeal>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `OperatingSystemTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OperatingSystemTargeting {
    /// operatingSystemCriteria property.
    pub operating_system_criteria: Option<CriteriaTargeting>,
    /// operatingSystemVersionCriteria property.
    pub operating_system_version_criteria: Option<CriteriaTargeting>,
}

/// `MobileApplicationTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MobileApplicationTargeting {
    /// firstPartyTargeting property.
    pub first_party_targeting: Option<FirstPartyMobileApplicationTargeting>,
}

/// `Deal` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Deal {
    /// billedBuyer property.
    pub billed_buyer: Option<String>,
    /// buyer property.
    pub buyer: Option<String>,
    /// buyerPermissionType property.
    pub buyer_permission_type: Option<String>,
    /// client property.
    pub client: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// creativeRequirements property.
    pub creative_requirements: Option<CreativeRequirements>,
    /// dealType property.
    pub deal_type: Option<String>,
    /// deliveryControl property.
    pub delivery_control: Option<DeliveryControl>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// eligibleSeatIds property.
    pub eligible_seat_ids: Option<Vec<String>>,
    /// estimatedGrossSpend property.
    pub estimated_gross_spend: Option<Money>,
    /// flightEndTime property.
    pub flight_end_time: Option<String>,
    /// flightStartTime property.
    pub flight_start_time: Option<String>,
    /// mediaPlanner property.
    pub media_planner: Option<MediaPlanner>,
    /// name property.
    pub name: Option<String>,
    /// preferredDealTerms property.
    pub preferred_deal_terms: Option<PreferredDealTerms>,
    /// privateAuctionTerms property.
    pub private_auction_terms: Option<PrivateAuctionTerms>,
    /// programmaticGuaranteedTerms property.
    pub programmatic_guaranteed_terms: Option<ProgrammaticGuaranteedTerms>,
    /// proposalRevision property.
    pub proposal_revision: Option<String>,
    /// publisherProfile property.
    pub publisher_profile: Option<String>,
    /// sellerTimeZone property.
    pub seller_time_zone: Option<TimeZone>,
    /// targeting property.
    pub targeting: Option<MarketplaceTargeting>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `Price` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Price {
    /// amount property.
    pub amount: Option<Money>,
    /// type property.
    pub r#type: Option<String>,
}

/// `PrivateAuctionTerms` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PrivateAuctionTerms {
    /// floorPrice property.
    pub floor_price: Option<Price>,
    /// openAuctionAllowed property.
    pub open_auction_allowed: Option<bool>,
}

/// `Money` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Money {
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// units property.
    pub units: Option<String>,
}

/// `DealPausingInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DealPausingInfo {
    /// pauseReason property.
    pub pause_reason: Option<String>,
    /// pauseRole property.
    pub pause_role: Option<String>,
    /// pausingConsented property.
    pub pausing_consented: Option<bool>,
}

/// `InventorySizeTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InventorySizeTargeting {
    /// excludedInventorySizes property.
    pub excluded_inventory_sizes: Option<Vec<AdSize>>,
    /// targetedInventorySizes property.
    pub targeted_inventory_sizes: Option<Vec<AdSize>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`authorizedbuyersmarketplace.bidders.finalizedDeals.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBiddersFinalizedDealsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`authorizedbuyersmarketplace.bidders.finalizedDeals.setReadyToServe_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBiddersFinalizedDealsSetReadyToServeArgs {
    /// Path parameter: `deal`.
    pub deal: String,
}

/// Arguments for [`authorizedbuyersmarketplace.buyers.finalizedDeals.addCreative_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsAddCreativeArgs {
    /// Path parameter: `deal`.
    pub deal: String,
}

/// Arguments for [`authorizedbuyersmarketplace.buyers.finalizedDeals.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`authorizedbuyersmarketplace.buyers.finalizedDeals.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`authorizedbuyersmarketplace.buyers.finalizedDeals.pause_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsPauseArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`authorizedbuyersmarketplace.buyers.finalizedDeals.resume_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsResumeArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`authorizedbuyersmarketplace.buyers.finalizedDeals.setReadyToServe_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersFinalizedDealsSetReadyToServeArgs {
    /// Path parameter: `deal`.
    pub deal: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/bidders/{biddersId}/finalizedDeals
// -----------------------------------------------------------------------------

/// GET v1/bidders/{biddersId}/finalizedDeals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = authorizedbuyersmarketplace_bidders_finalized_deals_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn authorizedbuyersmarketplace_bidders_finalized_deals_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFinalizedDealsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/bidders/{{biddersId}}/finalizedDeals",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFinalizedDealsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/bidders/{biddersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe
// -----------------------------------------------------------------------------

/// POST v1/bidders/{biddersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn authorizedbuyersmarketplace_bidders_finalized_deals_set_ready_to_serve_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/bidders/{{biddersId}}/finalizedDeals/{{finalizedDealsId}}:setReadyToServe",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FinalizedDeal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:addCreative
// -----------------------------------------------------------------------------

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:addCreative.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn authorizedbuyersmarketplace_buyers_finalized_deals_add_creative_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{{buyersId}}/finalizedDeals/{{finalizedDealsId}}:addCreative",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FinalizedDeal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}
// -----------------------------------------------------------------------------

/// GET v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = authorizedbuyersmarketplace_buyers_finalized_deals_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn authorizedbuyersmarketplace_buyers_finalized_deals_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{{buyersId}}/finalizedDeals/{{finalizedDealsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FinalizedDeal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/buyers/{buyersId}/finalizedDeals
// -----------------------------------------------------------------------------

/// GET v1/buyers/{buyersId}/finalizedDeals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = authorizedbuyersmarketplace_buyers_finalized_deals_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn authorizedbuyersmarketplace_buyers_finalized_deals_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFinalizedDealsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{{buyersId}}/finalizedDeals",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFinalizedDealsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:pause
// -----------------------------------------------------------------------------

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:pause.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = authorizedbuyersmarketplace_buyers_finalized_deals_pause_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn authorizedbuyersmarketplace_buyers_finalized_deals_pause_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{{buyersId}}/finalizedDeals/{{finalizedDealsId}}:pause",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FinalizedDeal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:resume
// -----------------------------------------------------------------------------

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:resume.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = authorizedbuyersmarketplace_buyers_finalized_deals_resume_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn authorizedbuyersmarketplace_buyers_finalized_deals_resume_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{{buyersId}}/finalizedDeals/{{finalizedDealsId}}:resume",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FinalizedDeal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe
// -----------------------------------------------------------------------------

/// POST v1/buyers/{buyersId}/finalizedDeals/{finalizedDealsId}:setReadyToServe.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn authorizedbuyersmarketplace_buyers_finalized_deals_set_ready_to_serve_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FinalizedDeal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{{buyersId}}/finalizedDeals/{{finalizedDealsId}}:setReadyToServe",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FinalizedDeal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
