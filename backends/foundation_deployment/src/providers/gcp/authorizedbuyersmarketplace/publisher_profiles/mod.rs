//! Auto-generated API module for gcp/authorizedbuyersmarketplace publisherProfiles.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_authorizedbuyersmarketplace_publisher_profiles `

#![cfg(feature = "gcp_authorizedbuyersmarketplace_publisher_profiles")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `PublisherProfile` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PublisherProfile {
    /// audienceDescription property.
    pub audience_description: Option<String>,
    /// directDealsContact property.
    pub direct_deals_contact: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// domains property.
    pub domains: Option<Vec<String>>,
    /// isParent property.
    pub is_parent: Option<bool>,
    /// logoUrl property.
    pub logo_url: Option<String>,
    /// mediaKitUrl property.
    pub media_kit_url: Option<String>,
    /// mobileApps property.
    pub mobile_apps: Option<Vec<PublisherProfileMobileApplication>>,
    /// name property.
    pub name: Option<String>,
    /// overview property.
    pub overview: Option<String>,
    /// pitchStatement property.
    pub pitch_statement: Option<String>,
    /// programmaticDealsContact property.
    pub programmatic_deals_contact: Option<String>,
    /// publisherCode property.
    pub publisher_code: Option<String>,
    /// samplePageUrl property.
    pub sample_page_url: Option<String>,
    /// topHeadlines property.
    pub top_headlines: Option<Vec<String>>,
}

/// `PublisherProfileMobileApplication` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PublisherProfileMobileApplication {
    /// appStore property.
    pub app_store: Option<String>,
    /// externalAppId property.
    pub external_app_id: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `ListPublisherProfilesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListPublisherProfilesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// publisherProfiles property.
    pub publisher_profiles: Option<Vec<PublisherProfile>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`authorizedbuyersmarketplace.buyers.publisherProfiles.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersPublisherProfilesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`authorizedbuyersmarketplace.buyers.publisherProfiles.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AuthorizedbuyersmarketplaceBuyersPublisherProfilesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/buyers/{buyersId}/publisherProfiles/{publisherProfilesId}
// -----------------------------------------------------------------------------

/// GET v1/buyers/{buyersId}/publisherProfiles/{publisherProfilesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = authorizedbuyersmarketplace_buyers_publisher_profiles_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublisherProfile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{{buyersId}}/publisherProfiles/{{publisherProfilesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PublisherProfile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/buyers/{buyersId}/publisherProfiles
// -----------------------------------------------------------------------------

/// GET v1/buyers/{buyersId}/publisherProfiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = authorizedbuyersmarketplace_buyers_publisher_profiles_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn authorizedbuyersmarketplace_buyers_publisher_profiles_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPublisherProfilesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://authorizedbuyersmarketplace.googleapis.com/v1/buyers/{{buyersId}}/publisherProfiles",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPublisherProfilesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
