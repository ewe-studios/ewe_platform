//! Auto-generated API module for gcp/chromemanagement chromemanagement.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_chromemanagement_chromemanagement `

#![cfg(feature = "gcp_chromemanagement_chromemanagement")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleChromeManagementV1CountActiveDevicesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountActiveDevicesResponse {
    /// sevenDaysCount property.
    pub seven_days_count: Option<String>,
    /// thirtyDaysCount property.
    pub thirty_days_count: Option<String>,
}

/// `GoogleChromeManagementV1DisplayDevice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1DisplayDevice {
    /// displayHeightMm property.
    pub display_height_mm: Option<i64>,
    /// displayName property.
    pub display_name: Option<String>,
    /// displayWidthMm property.
    pub display_width_mm: Option<i64>,
    /// edidVersion property.
    pub edid_version: Option<String>,
    /// internal property.
    pub internal: Option<bool>,
    /// manufactureYear property.
    pub manufacture_year: Option<i64>,
    /// manufacturerId property.
    pub manufacturer_id: Option<String>,
    /// modelId property.
    pub model_id: Option<i64>,
    /// serialNumber property.
    pub serial_number: Option<i64>,
}

/// `GoogleChromeManagementV1DiskInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1DiskInfo {
    /// bytesReadThisSession property.
    pub bytes_read_this_session: Option<String>,
    /// bytesWrittenThisSession property.
    pub bytes_written_this_session: Option<String>,
    /// discardTimeThisSession property.
    pub discard_time_this_session: Option<String>,
    /// health property.
    pub health: Option<String>,
    /// ioTimeThisSession property.
    pub io_time_this_session: Option<String>,
    /// manufacturer property.
    pub manufacturer: Option<String>,
    /// model property.
    pub model: Option<String>,
    /// readTimeThisSession property.
    pub read_time_this_session: Option<String>,
    /// serialNumber property.
    pub serial_number: Option<String>,
    /// sizeBytes property.
    pub size_bytes: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// volumeIds property.
    pub volume_ids: Option<Vec<String>>,
    /// writeTimeThisSession property.
    pub write_time_this_session: Option<String>,
}

/// `GoogleChromeManagementVersionsV1ListChromeBrowserProfilesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ListChromeBrowserProfilesResponse {
    /// chromeBrowserProfiles property.
    pub chrome_browser_profiles: Option<Vec<GoogleChromeManagementVersionsV1ChromeBrowserProfile>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalSize property.
    pub total_size: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryAppLaunchEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryAppLaunchEvent {
    /// appId property.
    pub app_id: Option<String>,
    /// appLaunchSource property.
    pub app_launch_source: Option<String>,
    /// appType property.
    pub app_type: Option<String>,
}

/// `GoogleChromeManagementVersionsV1ThirdPartyProfileUser` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ThirdPartyProfileUser {
    /// name property.
    pub name: Option<String>,
    /// orgUnitId property.
    pub org_unit_id: Option<String>,
}

/// `GoogleChromeManagementV1BatterySampleReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1BatterySampleReport {
    /// chargeRate property.
    pub charge_rate: Option<i64>,
    /// current property.
    pub current: Option<String>,
    /// dischargeRate property.
    pub discharge_rate: Option<i64>,
    /// remainingCapacity property.
    pub remaining_capacity: Option<String>,
    /// reportTime property.
    pub report_time: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// temperature property.
    pub temperature: Option<i64>,
    /// voltage property.
    pub voltage: Option<String>,
}

/// `GoogleChromeManagementVersionsV1ChromeBrowserProfileCommandCommandResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ChromeBrowserProfileCommandCommandResult {
    /// clientExecutionTime property.
    pub client_execution_time: Option<String>,
    /// resultCode property.
    pub result_code: Option<String>,
    /// resultType property.
    pub result_type: Option<String>,
}

/// `GoogleChromeManagementV1MemoryInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1MemoryInfo {
    /// availableRamBytes property.
    pub available_ram_bytes: Option<String>,
    /// totalMemoryEncryption property.
    pub total_memory_encryption: Option<GoogleChromeManagementV1TotalMemoryEncryptionInfo>,
    /// totalRamBytes property.
    pub total_ram_bytes: Option<String>,
}

/// `GoogleChromeManagementV1RiskAssessmentEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1RiskAssessmentEntry {
    /// provider property.
    pub provider: Option<String>,
    /// riskAssessment property.
    pub risk_assessment: Option<GoogleChromeManagementV1RiskAssessment>,
    /// riskLevel property.
    pub risk_level: Option<String>,
}

/// `GoogleChromeManagementV1ChromeAppRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1ChromeAppRequest {
    /// appDetails property.
    pub app_details: Option<String>,
    /// appId property.
    pub app_id: Option<String>,
    /// detailUri property.
    pub detail_uri: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// iconUri property.
    pub icon_uri: Option<String>,
    /// latestRequestTime property.
    pub latest_request_time: Option<String>,
    /// requestCount property.
    pub request_count: Option<String>,
}

/// `GoogleChromeManagementVersionsV1ReportingData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ReportingData {
    /// browserExecutablePath property.
    pub browser_executable_path: Option<String>,
    /// extensionData property.
    pub extension_data: Option<Vec<GoogleChromeManagementVersionsV1ReportingDataExtensionData>>,
    /// extensionPolicyData property.
    pub extension_policy_data:
        Option<Vec<GoogleChromeManagementVersionsV1ReportingDataExtensionPolicyData>>,
    /// installedBrowserVersion property.
    pub installed_browser_version: Option<String>,
    /// policyData property.
    pub policy_data: Option<Vec<GoogleChromeManagementVersionsV1ReportingDataPolicyData>>,
    /// profilePath property.
    pub profile_path: Option<String>,
}

/// `GoogleChromeManagementV1UserPrintReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1UserPrintReport {
    /// deviceCount property.
    pub device_count: Option<String>,
    /// jobCount property.
    pub job_count: Option<String>,
    /// printerCount property.
    pub printer_count: Option<String>,
    /// userEmail property.
    pub user_email: Option<String>,
    /// userId property.
    pub user_id: Option<String>,
}

/// `GoogleChromeManagementV1StorageInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1StorageInfo {
    /// availableDiskBytes property.
    pub available_disk_bytes: Option<String>,
    /// totalDiskBytes property.
    pub total_disk_bytes: Option<String>,
    /// volume property.
    pub volume: Option<Vec<GoogleChromeManagementV1StorageInfoDiskVolume>>,
}

/// `GoogleChromeManagementV1AppReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1AppReport {
    /// reportTime property.
    pub report_time: Option<String>,
    /// usageData property.
    pub usage_data: Option<Vec<GoogleChromeManagementV1AppUsageData>>,
}

/// `GoogleChromeManagementV1ListTelemetryEventsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1ListTelemetryEventsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// telemetryEvents property.
    pub telemetry_events: Option<Vec<GoogleChromeManagementV1TelemetryEvent>>,
}

/// `GoogleChromeManagementV1PeripheralsReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1PeripheralsReport {
    /// reportTime property.
    pub report_time: Option<String>,
    /// usbPeripheralReport property.
    pub usb_peripheral_report: Option<Vec<GoogleChromeManagementV1UsbPeripheralReport>>,
}

/// `GoogleChromeManagementV1AndroidAppPermission` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1AndroidAppPermission {
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleChromeManagementV1PrintJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1PrintJob {
    /// colorMode property.
    pub color_mode: Option<String>,
    /// completeTime property.
    pub complete_time: Option<String>,
    /// copyCount property.
    pub copy_count: Option<i64>,
    /// createTime property.
    pub create_time: Option<String>,
    /// documentPageCount property.
    pub document_page_count: Option<i64>,
    /// duplexMode property.
    pub duplex_mode: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// printer property.
    pub printer: Option<String>,
    /// printerId property.
    pub printer_id: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// userEmail property.
    pub user_email: Option<String>,
    /// userId property.
    pub user_id: Option<String>,
}

/// `GoogleChromeManagementVersionsV1UploadCertificateRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1UploadCertificateRequest {
    /// certificatePem property.
    pub certificate_pem: Option<String>,
}

/// `GoogleRpcStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleRpcStatus {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `GoogleChromeManagementV1CountChromeAppRequestsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountChromeAppRequestsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// requestedApps property.
    pub requested_apps: Option<Vec<GoogleChromeManagementV1ChromeAppRequest>>,
    /// totalSize property.
    pub total_size: Option<i64>,
}

/// `GoogleChromeManagementV1TelemetryEventNotificationFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryEventNotificationFilter {
    /// eventTypes property.
    pub event_types: Option<Vec<String>>,
}

/// `GoogleLongrunningListOperationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleLongrunningListOperationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<GoogleLongrunningOperation>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `GoogleChromeManagementV1TouchScreenInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TouchScreenInfo {
    /// devices property.
    pub devices: Option<Vec<GoogleChromeManagementV1TouchScreenDevice>>,
    /// touchpadLibrary property.
    pub touchpad_library: Option<String>,
}

/// `GoogleChromeManagementV1MemoryStatusReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1MemoryStatusReport {
    /// pageFaults property.
    pub page_faults: Option<i64>,
    /// reportTime property.
    pub report_time: Option<String>,
    /// sampleFrequency property.
    pub sample_frequency: Option<String>,
    /// systemRamFreeBytes property.
    pub system_ram_free_bytes: Option<String>,
}

/// `GoogleChromeManagementVersionsV1UploadCertificateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1UploadCertificateResponse {}

/// `GoogleChromeManagementVersionsV1ScepProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ScepProfile {
    /// certificateTemplateName property.
    pub certificate_template_name: Option<String>,
    /// country property.
    pub country: Option<String>,
    /// keyUsages property.
    pub key_usages: Option<Vec<String>>,
    /// locality property.
    pub locality: Option<String>,
    /// organization property.
    pub organization: Option<String>,
    /// organizationalUnits property.
    pub organizational_units: Option<Vec<String>>,
    /// state property.
    pub state: Option<String>,
    /// subjectAltNames property.
    pub subject_alt_names: Option<Vec<GoogleChromeManagementVersionsV1SubjectAltName>>,
    /// subjectCommonName property.
    pub subject_common_name: Option<String>,
}

/// `GoogleChromeManagementV1TouchScreenDevice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TouchScreenDevice {
    /// displayName property.
    pub display_name: Option<String>,
    /// stylusCapable property.
    pub stylus_capable: Option<bool>,
    /// touchPointCount property.
    pub touch_point_count: Option<i64>,
}

/// `GoogleChromeManagementV1ChromeAppSiteAccess` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1ChromeAppSiteAccess {
    /// hostMatch property.
    pub host_match: Option<String>,
}

/// `GoogleChromeManagementV1NetworkDiagnosticsReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1NetworkDiagnosticsReport {
    /// httpsLatencyData property.
    pub https_latency_data: Option<GoogleChromeManagementV1HttpsLatencyRoutineData>,
    /// reportTime property.
    pub report_time: Option<String>,
}

/// `GoogleChromeManagementVersionsV1SetFailureRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1SetFailureRequest {
    /// errorMessage property.
    pub error_message: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryUsbPeripheralsEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryUsbPeripheralsEvent {
    /// usbPeripheralReport property.
    pub usb_peripheral_report: Option<Vec<GoogleChromeManagementV1UsbPeripheralReport>>,
}

/// `GoogleChromeManagementVersionsV1ReportingDataExtensionPolicyData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ReportingDataExtensionPolicyData {
    /// extensionId property.
    pub extension_id: Option<String>,
    /// extensionName property.
    pub extension_name: Option<String>,
    /// policyData property.
    pub policy_data: Option<Vec<GoogleChromeManagementVersionsV1ReportingDataPolicyData>>,
}

/// `GoogleChromeManagementV1BootPerformanceReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1BootPerformanceReport {
    /// bootUpDuration property.
    pub boot_up_duration: Option<String>,
    /// bootUpTime property.
    pub boot_up_time: Option<String>,
    /// reportTime property.
    pub report_time: Option<String>,
    /// shutdownDuration property.
    pub shutdown_duration: Option<String>,
    /// shutdownReason property.
    pub shutdown_reason: Option<String>,
    /// shutdownTime property.
    pub shutdown_time: Option<String>,
}

/// `GoogleChromeManagementV1ThunderboltInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1ThunderboltInfo {
    /// securityLevel property.
    pub security_level: Option<String>,
}

/// `GoogleChromeManagementVersionsV1ReportingDataPolicyData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ReportingDataPolicyData {
    /// conflicts property.
    pub conflicts: Option<Vec<GoogleChromeManagementVersionsV1ReportingDataConflictingPolicyData>>,
    /// error property.
    pub error: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// source property.
    pub source: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `GoogleChromeManagementV1HeartbeatStatusReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1HeartbeatStatusReport {
    /// reportTime property.
    pub report_time: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `GoogleChromeManagementVersionsV1AttestationCredential` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1AttestationCredential {
    /// keyRotationTime property.
    pub key_rotation_time: Option<String>,
    /// keyTrustLevel property.
    pub key_trust_level: Option<String>,
    /// keyType property.
    pub key_type: Option<String>,
    /// publicKey property.
    pub public_key: Option<String>,
}

/// `GoogleTypeDate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeDate {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `GoogleChromeManagementV1TelemetryDevice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryDevice {
    /// appReport property.
    pub app_report: Option<Vec<GoogleChromeManagementV1AppReport>>,
    /// audioStatusReport property.
    pub audio_status_report: Option<Vec<GoogleChromeManagementV1AudioStatusReport>>,
    /// batteryInfo property.
    pub battery_info: Option<Vec<GoogleChromeManagementV1BatteryInfo>>,
    /// batteryStatusReport property.
    pub battery_status_report: Option<Vec<GoogleChromeManagementV1BatteryStatusReport>>,
    /// bootPerformanceReport property.
    pub boot_performance_report: Option<Vec<GoogleChromeManagementV1BootPerformanceReport>>,
    /// cpuInfo property.
    pub cpu_info: Option<Vec<GoogleChromeManagementV1CpuInfo>>,
    /// cpuStatusReport property.
    pub cpu_status_report: Option<Vec<GoogleChromeManagementV1CpuStatusReport>>,
    /// customer property.
    pub customer: Option<String>,
    /// deviceId property.
    pub device_id: Option<String>,
    /// graphicsInfo property.
    pub graphics_info: Option<GoogleChromeManagementV1GraphicsInfo>,
    /// graphicsStatusReport property.
    pub graphics_status_report: Option<Vec<GoogleChromeManagementV1GraphicsStatusReport>>,
    /// heartbeatStatusReport property.
    pub heartbeat_status_report: Option<Vec<GoogleChromeManagementV1HeartbeatStatusReport>>,
    /// kioskAppStatusReport property.
    pub kiosk_app_status_report: Option<Vec<GoogleChromeManagementV1KioskAppStatusReport>>,
    /// memoryInfo property.
    pub memory_info: Option<GoogleChromeManagementV1MemoryInfo>,
    /// memoryStatusReport property.
    pub memory_status_report: Option<Vec<GoogleChromeManagementV1MemoryStatusReport>>,
    /// name property.
    pub name: Option<String>,
    /// networkBandwidthReport property.
    pub network_bandwidth_report: Option<Vec<GoogleChromeManagementV1NetworkBandwidthReport>>,
    /// networkDiagnosticsReport property.
    pub network_diagnostics_report: Option<Vec<GoogleChromeManagementV1NetworkDiagnosticsReport>>,
    /// networkInfo property.
    pub network_info: Option<GoogleChromeManagementV1NetworkInfo>,
    /// networkStatusReport property.
    pub network_status_report: Option<Vec<GoogleChromeManagementV1NetworkStatusReport>>,
    /// orgUnitId property.
    pub org_unit_id: Option<String>,
    /// osUpdateStatus property.
    pub os_update_status: Option<Vec<GoogleChromeManagementV1OsUpdateStatus>>,
    /// peripheralsReport property.
    pub peripherals_report: Option<Vec<GoogleChromeManagementV1PeripheralsReport>>,
    /// runtimeCountersReport property.
    pub runtime_counters_report: Option<Vec<GoogleChromeManagementV1RuntimeCountersReport>>,
    /// serialNumber property.
    pub serial_number: Option<String>,
    /// storageInfo property.
    pub storage_info: Option<GoogleChromeManagementV1StorageInfo>,
    /// storageStatusReport property.
    pub storage_status_report: Option<Vec<GoogleChromeManagementV1StorageStatusReport>>,
    /// thunderboltInfo property.
    pub thunderbolt_info: Option<Vec<GoogleChromeManagementV1ThunderboltInfo>>,
}

/// `GoogleChromeManagementV1ListTelemetryNotificationConfigsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1ListTelemetryNotificationConfigsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// telemetryNotificationConfigs property.
    pub telemetry_notification_configs:
        Option<Vec<GoogleChromeManagementV1TelemetryNotificationConfig>>,
}

/// `GoogleChromeManagementVersionsV1GenericProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1GenericProfile {
    /// profileAdapterConfigReference property.
    pub profile_adapter_config_reference: Option<String>,
}

/// `GoogleChromeManagementV1AudioStatusReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1AudioStatusReport {
    /// inputDevice property.
    pub input_device: Option<String>,
    /// inputGain property.
    pub input_gain: Option<i64>,
    /// inputMute property.
    pub input_mute: Option<bool>,
    /// outputDevice property.
    pub output_device: Option<String>,
    /// outputMute property.
    pub output_mute: Option<bool>,
    /// outputVolume property.
    pub output_volume: Option<i64>,
    /// reportTime property.
    pub report_time: Option<String>,
}

/// `GoogleChromeManagementVersionsV1ListChromeBrowserProfileCommandsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ListChromeBrowserProfileCommandsResponse {
    /// chromeBrowserProfileCommands property.
    pub chrome_browser_profile_commands:
        Option<Vec<GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalSize property.
    pub total_size: Option<String>,
}

/// `GoogleChromeManagementV1NetworkBandwidthReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1NetworkBandwidthReport {
    /// downloadSpeedKbps property.
    pub download_speed_kbps: Option<String>,
    /// reportTime property.
    pub report_time: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryUserInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryUserInfo {
    /// email property.
    pub email: Option<String>,
    /// orgUnitId property.
    pub org_unit_id: Option<String>,
}

/// `GoogleChromeManagementV1InstalledApp` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1InstalledApp {
    /// appId property.
    pub app_id: Option<String>,
    /// appInstallType property.
    pub app_install_type: Option<String>,
    /// appSource property.
    pub app_source: Option<String>,
    /// appType property.
    pub app_type: Option<String>,
    /// browserDeviceCount property.
    pub browser_device_count: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// displayName property.
    pub display_name: Option<String>,
    /// homepageUri property.
    pub homepage_uri: Option<String>,
    /// osUserCount property.
    pub os_user_count: Option<String>,
    /// permissions property.
    pub permissions: Option<Vec<String>>,
    /// riskAssessment property.
    pub risk_assessment: Option<GoogleChromeManagementV1RiskAssessmentData>,
}

/// `GoogleChromeManagementVersionsV1ReportingDataConflictingPolicyData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ReportingDataConflictingPolicyData {
    /// source property.
    pub source: Option<String>,
}

/// `GoogleChromeManagementV1DeviceHardwareCountReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1DeviceHardwareCountReport {
    /// bucket property.
    pub bucket: Option<String>,
    /// count property.
    pub count: Option<String>,
}

/// `GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserResponse {
    /// thirdPartyProfileUser property.
    pub third_party_profile_user: Option<GoogleChromeManagementVersionsV1ThirdPartyProfileUser>,
}

/// `GoogleChromeManagementV1AndroidAppInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1AndroidAppInfo {
    /// permissions property.
    pub permissions: Option<Vec<GoogleChromeManagementV1AndroidAppPermission>>,
}

/// `GoogleChromeManagementV1ChromeAppPermission` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1ChromeAppPermission {
    /// accessUserData property.
    pub access_user_data: Option<bool>,
    /// documentationUri property.
    pub documentation_uri: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryExternalDisplayEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryExternalDisplayEvent {
    /// externalDisplayData property.
    pub external_display_data: Option<Vec<GoogleChromeManagementV1TelemetryExternalDisplayData>>,
}

/// `GoogleChromeManagementV1DisplayInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1DisplayInfo {
    /// deviceId property.
    pub device_id: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// edidVersion property.
    pub edid_version: Option<String>,
    /// isInternal property.
    pub is_internal: Option<bool>,
    /// refreshRate property.
    pub refresh_rate: Option<i64>,
    /// resolutionHeight property.
    pub resolution_height: Option<i64>,
    /// resolutionWidth property.
    pub resolution_width: Option<i64>,
    /// serialNumber property.
    pub serial_number: Option<i64>,
}

/// `GoogleChromeManagementV1CpuTemperatureInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CpuTemperatureInfo {
    /// label property.
    pub label: Option<String>,
    /// temperatureCelsius property.
    pub temperature_celsius: Option<i64>,
}

/// `GoogleChromeManagementV1CountInstalledAppsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountInstalledAppsResponse {
    /// installedApps property.
    pub installed_apps: Option<Vec<GoogleChromeManagementV1InstalledApp>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalSize property.
    pub total_size: Option<i64>,
}

/// `GoogleChromeManagementV1OsUpdateStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1OsUpdateStatus {
    /// lastRebootTime property.
    pub last_reboot_time: Option<String>,
    /// lastUpdateCheckTime property.
    pub last_update_check_time: Option<String>,
    /// lastUpdateTime property.
    pub last_update_time: Option<String>,
    /// newPlatformVersion property.
    pub new_platform_version: Option<String>,
    /// newRequestedPlatformVersion property.
    pub new_requested_platform_version: Option<String>,
    /// updateState property.
    pub update_state: Option<String>,
}

/// `GoogleChromeManagementV1ListTelemetryUsersResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1ListTelemetryUsersResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// telemetryUsers property.
    pub telemetry_users: Option<Vec<GoogleChromeManagementV1TelemetryUser>>,
}

/// `GoogleChromeManagementV1TelemetryAppInstallEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryAppInstallEvent {
    /// appId property.
    pub app_id: Option<String>,
    /// appInstallReason property.
    pub app_install_reason: Option<String>,
    /// appInstallSource property.
    pub app_install_source: Option<String>,
    /// appInstallTime property.
    pub app_install_time: Option<String>,
    /// appType property.
    pub app_type: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryExternalDisplayData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryExternalDisplayData {
    /// displayName property.
    pub display_name: Option<String>,
    /// edidVersion property.
    pub edid_version: Option<String>,
    /// refreshRate property.
    pub refresh_rate: Option<String>,
    /// resolutionHorizontal property.
    pub resolution_horizontal: Option<i64>,
    /// resolutionVertical property.
    pub resolution_vertical: Option<i64>,
    /// serialNumber property.
    pub serial_number: Option<i64>,
}

/// `GoogleChromeManagementV1FetchUsersRequestingExtensionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1FetchUsersRequestingExtensionResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalSize property.
    pub total_size: Option<i64>,
    /// userDetails property.
    pub user_details: Option<Vec<GoogleChromeManagementV1UserRequestingExtensionDetails>>,
}

/// `GoogleChromeManagementV1CpuInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CpuInfo {
    /// architecture property.
    pub architecture: Option<String>,
    /// keylockerConfigured property.
    pub keylocker_configured: Option<bool>,
    /// keylockerSupported property.
    pub keylocker_supported: Option<bool>,
    /// maxClockSpeed property.
    pub max_clock_speed: Option<i64>,
    /// model property.
    pub model: Option<String>,
}

/// `GoogleChromeManagementVersionsV1SetFailureResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1SetFailureResponse {}

/// `GoogleChromeManagementV1NetworkStatusReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1NetworkStatusReport {
    /// connectionState property.
    pub connection_state: Option<String>,
    /// connectionType property.
    pub connection_type: Option<String>,
    /// encryptionOn property.
    pub encryption_on: Option<bool>,
    /// gatewayIpAddress property.
    pub gateway_ip_address: Option<String>,
    /// gatewayIpv6Address property.
    pub gateway_ipv6_address: Option<String>,
    /// guid property.
    pub guid: Option<String>,
    /// ipv6Address property.
    pub ipv6_address: Option<Vec<String>>,
    /// lanIpAddress property.
    pub lan_ip_address: Option<String>,
    /// linkDownSpeedKbps property.
    pub link_down_speed_kbps: Option<String>,
    /// metered property.
    pub metered: Option<bool>,
    /// receivingBitRateMbps property.
    pub receiving_bit_rate_mbps: Option<String>,
    /// reportTime property.
    pub report_time: Option<String>,
    /// sampleFrequency property.
    pub sample_frequency: Option<String>,
    /// signalStrengthDbm property.
    pub signal_strength_dbm: Option<i64>,
    /// transmissionBitRateMbps property.
    pub transmission_bit_rate_mbps: Option<String>,
    /// transmissionPowerDbm property.
    pub transmission_power_dbm: Option<i64>,
    /// wifiLinkQuality property.
    pub wifi_link_quality: Option<String>,
    /// wifiPowerManagementEnabled property.
    pub wifi_power_management_enabled: Option<bool>,
}

/// `GoogleChromeManagementVersionsV1ScepCaConnection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ScepCaConnection {
    /// caConnectionAdapterConfigReference property.
    pub ca_connection_adapter_config_reference: Option<String>,
}

/// `GoogleChromeManagementV1PrinterReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1PrinterReport {
    /// deviceCount property.
    pub device_count: Option<String>,
    /// jobCount property.
    pub job_count: Option<String>,
    /// printer property.
    pub printer: Option<String>,
    /// printerId property.
    pub printer_id: Option<String>,
    /// printerModel property.
    pub printer_model: Option<String>,
    /// userCount property.
    pub user_count: Option<String>,
}

/// `GoogleChromeManagementV1UserRequestingExtensionDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1UserRequestingExtensionDetails {
    /// email property.
    pub email: Option<String>,
    /// justification property.
    pub justification: Option<String>,
}

/// `GoogleChromeManagementV1RiskAssessmentData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1RiskAssessmentData {
    /// entries property.
    pub entries: Option<Vec<GoogleChromeManagementV1RiskAssessmentEntry>>,
    /// overallRiskLevel property.
    pub overall_risk_level: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryUserDevice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryUserDevice {
    /// appReport property.
    pub app_report: Option<Vec<GoogleChromeManagementV1AppReport>>,
    /// audioStatusReport property.
    pub audio_status_report: Option<Vec<GoogleChromeManagementV1AudioStatusReport>>,
    /// deviceActivityReport property.
    pub device_activity_report: Option<Vec<GoogleChromeManagementV1DeviceActivityReport>>,
    /// deviceId property.
    pub device_id: Option<String>,
    /// networkBandwidthReport property.
    pub network_bandwidth_report: Option<Vec<GoogleChromeManagementV1NetworkBandwidthReport>>,
    /// peripheralsReport property.
    pub peripherals_report: Option<Vec<GoogleChromeManagementV1PeripheralsReport>>,
}

/// `GoogleChromeManagementV1HttpsLatencyRoutineData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1HttpsLatencyRoutineData {
    /// latency property.
    pub latency: Option<String>,
    /// problem property.
    pub problem: Option<String>,
}

/// `GoogleChromeManagementV1ChromeAppInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1ChromeAppInfo {
    /// googleOwned property.
    pub google_owned: Option<bool>,
    /// isCwsHosted property.
    pub is_cws_hosted: Option<bool>,
    /// isExtensionPolicySupported property.
    pub is_extension_policy_supported: Option<bool>,
    /// isKioskOnly property.
    pub is_kiosk_only: Option<bool>,
    /// isTheme property.
    pub is_theme: Option<bool>,
    /// kioskEnabled property.
    pub kiosk_enabled: Option<bool>,
    /// manifestVersion property.
    pub manifest_version: Option<String>,
    /// minUserCount property.
    pub min_user_count: Option<i64>,
    /// permissions property.
    pub permissions: Option<Vec<GoogleChromeManagementV1ChromeAppPermission>>,
    /// siteAccess property.
    pub site_access: Option<Vec<GoogleChromeManagementV1ChromeAppSiteAccess>>,
    /// supportEnabled property.
    pub support_enabled: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserRequest {
    /// destinationOrgUnit property.
    pub destination_org_unit: Option<String>,
}

/// `GoogleChromeManagementV1FetchDevicesRequestingExtensionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1FetchDevicesRequestingExtensionResponse {
    /// deviceDetails property.
    pub device_details: Option<Vec<GoogleChromeManagementV1DeviceRequestingExtensionDetails>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalSize property.
    pub total_size: Option<i64>,
}

/// `GoogleChromeManagementVersionsV1ChromeOsUserSession` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ChromeOsUserSession {
    /// chromeOsDevice property.
    pub chrome_os_device: Option<GoogleChromeManagementVersionsV1ChromeOsDevice>,
    /// userDirectoryApiId property.
    pub user_directory_api_id: Option<String>,
    /// userPrimaryEmail property.
    pub user_primary_email: Option<String>,
}

/// `GoogleChromeManagementV1CountDevicesPerBootTypeResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountDevicesPerBootTypeResponse {
    /// devBootTypeCount property.
    pub dev_boot_type_count: Option<String>,
    /// unreportedBootTypeCount property.
    pub unreported_boot_type_count: Option<String>,
    /// verifiedBootTypeCount property.
    pub verified_boot_type_count: Option<String>,
}

/// `GoogleChromeManagementV1RuntimeCountersReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1RuntimeCountersReport {
    /// enterHibernationCount property.
    pub enter_hibernation_count: Option<String>,
    /// enterPoweroffCount property.
    pub enter_poweroff_count: Option<String>,
    /// enterSleepCount property.
    pub enter_sleep_count: Option<String>,
    /// reportTime property.
    pub report_time: Option<String>,
    /// uptimeRuntimeDuration property.
    pub uptime_runtime_duration: Option<String>,
}

/// `GoogleChromeManagementV1CountPrintJobsByUserResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountPrintJobsByUserResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalSize property.
    pub total_size: Option<String>,
    /// userPrintReports property.
    pub user_print_reports: Option<Vec<GoogleChromeManagementV1UserPrintReport>>,
}

/// `GoogleChromeManagementV1BatteryStatusReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1BatteryStatusReport {
    /// batteryHealth property.
    pub battery_health: Option<String>,
    /// cycleCount property.
    pub cycle_count: Option<i64>,
    /// fullChargeCapacity property.
    pub full_charge_capacity: Option<String>,
    /// reportTime property.
    pub report_time: Option<String>,
    /// sample property.
    pub sample: Option<Vec<GoogleChromeManagementV1BatterySampleReport>>,
    /// serialNumber property.
    pub serial_number: Option<String>,
}

/// `GoogleChromeManagementV1AppDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1AppDetails {
    /// androidAppInfo property.
    pub android_app_info: Option<GoogleChromeManagementV1AndroidAppInfo>,
    /// appId property.
    pub app_id: Option<String>,
    /// chromeAppInfo property.
    pub chrome_app_info: Option<GoogleChromeManagementV1ChromeAppInfo>,
    /// description property.
    pub description: Option<String>,
    /// detailUri property.
    pub detail_uri: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// firstPublishTime property.
    pub first_publish_time: Option<String>,
    /// homepageUri property.
    pub homepage_uri: Option<String>,
    /// iconUri property.
    pub icon_uri: Option<String>,
    /// isPaidApp property.
    pub is_paid_app: Option<bool>,
    /// latestPublishTime property.
    pub latest_publish_time: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// privacyPolicyUri property.
    pub privacy_policy_uri: Option<String>,
    /// publisher property.
    pub publisher: Option<String>,
    /// reviewNumber property.
    pub review_number: Option<String>,
    /// reviewRating property.
    pub review_rating: Option<f64>,
    /// revisionId property.
    pub revision_id: Option<String>,
    /// serviceError property.
    pub service_error: Option<GoogleRpcStatus>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryNotificationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryNotificationConfig {
    /// customer property.
    pub customer: Option<String>,
    /// filter property.
    pub filter: Option<GoogleChromeManagementV1TelemetryNotificationFilter>,
    /// googleCloudPubsubTopic property.
    pub google_cloud_pubsub_topic: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryDeviceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryDeviceInfo {
    /// deviceId property.
    pub device_id: Option<String>,
    /// orgUnitId property.
    pub org_unit_id: Option<String>,
}

/// `GoogleChromeManagementV1AppUsageData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1AppUsageData {
    /// appId property.
    pub app_id: Option<String>,
    /// appInstanceId property.
    pub app_instance_id: Option<String>,
    /// appType property.
    pub app_type: Option<String>,
    /// runningDuration property.
    pub running_duration: Option<String>,
}

/// `GoogleChromeManagementV1Device` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1Device {
    /// deviceId property.
    pub device_id: Option<String>,
    /// machine property.
    pub machine: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryNetworkConnectionStateChangeEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryNetworkConnectionStateChangeEvent {
    /// connectionState property.
    pub connection_state: Option<String>,
    /// guid property.
    pub guid: Option<String>,
}

/// `GoogleChromeManagementVersionsV1ReportingDataExtensionData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ReportingDataExtensionData {
    /// description property.
    pub description: Option<String>,
    /// extensionId property.
    pub extension_id: Option<String>,
    /// extensionType property.
    pub extension_type: Option<String>,
    /// homepageUri property.
    pub homepage_uri: Option<String>,
    /// installationType property.
    pub installation_type: Option<String>,
    /// isDisabled property.
    pub is_disabled: Option<bool>,
    /// isWebstoreExtension property.
    pub is_webstore_extension: Option<bool>,
    /// manifestVersion property.
    pub manifest_version: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// permissions property.
    pub permissions: Option<Vec<String>>,
    /// version property.
    pub version: Option<String>,
}

/// `GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessRequest {
    /// callerInstanceId property.
    pub caller_instance_id: Option<String>,
}

/// `GoogleChromeManagementV1CountPrintJobsByPrinterResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountPrintJobsByPrinterResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// printerReports property.
    pub printer_reports: Option<Vec<GoogleChromeManagementV1PrinterReport>>,
    /// totalSize property.
    pub total_size: Option<String>,
}

/// `GoogleChromeManagementV1CountChromeCrashEventsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountChromeCrashEventsResponse {
    /// crashEventCounts property.
    pub crash_event_counts:
        Option<Vec<GoogleChromeManagementV1CountChromeCrashEventsResponseCrashEventCount>>,
}

/// `GoogleChromeManagementV1TelemetryAudioSevereUnderrunEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryAudioSevereUnderrunEvent {}

/// `GoogleChromeManagementV1NetworkDevice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1NetworkDevice {
    /// iccid property.
    pub iccid: Option<String>,
    /// imei property.
    pub imei: Option<String>,
    /// macAddress property.
    pub mac_address: Option<String>,
    /// mdn property.
    pub mdn: Option<String>,
    /// meid property.
    pub meid: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleChromeManagementV1CountChromeBrowsersNeedingAttentionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountChromeBrowsersNeedingAttentionResponse {
    /// noRecentActivityCount property.
    pub no_recent_activity_count: Option<String>,
    /// pendingBrowserUpdateCount property.
    pub pending_browser_update_count: Option<String>,
    /// recentlyEnrolledCount property.
    pub recently_enrolled_count: Option<String>,
}

/// `GoogleChromeManagementV1CountChromeVersionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountChromeVersionsResponse {
    /// browserVersions property.
    pub browser_versions: Option<Vec<GoogleChromeManagementV1BrowserVersion>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalSize property.
    pub total_size: Option<i64>,
}

/// `GoogleChromeManagementV1GraphicsAdapterInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1GraphicsAdapterInfo {
    /// adapter property.
    pub adapter: Option<String>,
    /// deviceId property.
    pub device_id: Option<String>,
    /// driverVersion property.
    pub driver_version: Option<String>,
}

/// `GoogleChromeManagementV1DeviceAueCountReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1DeviceAueCountReport {
    /// aueMonth property.
    pub aue_month: Option<String>,
    /// aueYear property.
    pub aue_year: Option<String>,
    /// count property.
    pub count: Option<String>,
    /// expired property.
    pub expired: Option<bool>,
    /// model property.
    pub model: Option<String>,
}

/// `GoogleChromeManagementV1BatteryInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1BatteryInfo {
    /// designCapacity property.
    pub design_capacity: Option<String>,
    /// designMinVoltage property.
    pub design_min_voltage: Option<i64>,
    /// manufactureDate property.
    pub manufacture_date: Option<GoogleTypeDate>,
    /// manufacturer property.
    pub manufacturer: Option<String>,
    /// serialNumber property.
    pub serial_number: Option<String>,
    /// technology property.
    pub technology: Option<String>,
}

/// `GoogleChromeManagementVersionsV1CertificateProvisioningProcess` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1CertificateProvisioningProcess {
    /// chromeOsDevice property.
    pub chrome_os_device: Option<GoogleChromeManagementVersionsV1ChromeOsDevice>,
    /// chromeOsUserSession property.
    pub chrome_os_user_session: Option<GoogleChromeManagementVersionsV1ChromeOsUserSession>,
    /// failureMessage property.
    pub failure_message: Option<String>,
    /// genericCaConnection property.
    pub generic_ca_connection: Option<GoogleChromeManagementVersionsV1GenericCaConnection>,
    /// genericProfile property.
    pub generic_profile: Option<GoogleChromeManagementVersionsV1GenericProfile>,
    /// issuedCertificate property.
    pub issued_certificate: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// provisioningProfileId property.
    pub provisioning_profile_id: Option<String>,
    /// scepCaConnection property.
    pub scep_ca_connection: Option<GoogleChromeManagementVersionsV1ScepCaConnection>,
    /// scepProfile property.
    pub scep_profile: Option<GoogleChromeManagementVersionsV1ScepProfile>,
    /// signData property.
    pub sign_data: Option<String>,
    /// signature property.
    pub signature: Option<String>,
    /// signatureAlgorithm property.
    pub signature_algorithm: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// subjectPublicKeyInfo property.
    pub subject_public_key_info: Option<String>,
}

/// `GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessResponse {}

/// `GoogleChromeManagementV1TelemetryNotificationFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryNotificationFilter {
    /// deviceId property.
    pub device_id: Option<String>,
    /// deviceOrgUnitId property.
    pub device_org_unit_id: Option<String>,
    /// telemetryEventNotificationFilter property.
    pub telemetry_event_notification_filter:
        Option<GoogleChromeManagementV1TelemetryEventNotificationFilter>,
    /// userEmail property.
    pub user_email: Option<String>,
    /// userOrgUnitId property.
    pub user_org_unit_id: Option<String>,
}

/// `GoogleChromeManagementV1UsbPeripheralReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1UsbPeripheralReport {
    /// categories property.
    pub categories: Option<Vec<String>>,
    /// classId property.
    pub class_id: Option<i64>,
    /// firmwareVersion property.
    pub firmware_version: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// pid property.
    pub pid: Option<i64>,
    /// subclassId property.
    pub subclass_id: Option<i64>,
    /// vendor property.
    pub vendor: Option<String>,
    /// vid property.
    pub vid: Option<i64>,
}

/// `GoogleChromeManagementVersionsV1SubjectAltName` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1SubjectAltName {
    /// type property.
    pub r#type: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `GoogleChromeManagementV1DeviceActivityReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1DeviceActivityReport {
    /// deviceActivityState property.
    pub device_activity_state: Option<String>,
    /// reportTime property.
    pub report_time: Option<String>,
}

/// `GoogleChromeManagementV1GraphicsStatusReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1GraphicsStatusReport {
    /// displays property.
    pub displays: Option<Vec<GoogleChromeManagementV1DisplayInfo>>,
    /// reportTime property.
    pub report_time: Option<String>,
}

/// `GoogleChromeManagementV1NetworkInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1NetworkInfo {
    /// networkDevices property.
    pub network_devices: Option<Vec<GoogleChromeManagementV1NetworkDevice>>,
}

/// `GoogleChromeManagementV1CountDevicesPerReleaseChannelResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountDevicesPerReleaseChannelResponse {
    /// betaChannelCount property.
    pub beta_channel_count: Option<String>,
    /// canaryChannelCount property.
    pub canary_channel_count: Option<String>,
    /// devChannelCount property.
    pub dev_channel_count: Option<String>,
    /// ltcChannelCount property.
    pub ltc_channel_count: Option<String>,
    /// ltsChannelCount property.
    pub lts_channel_count: Option<String>,
    /// stableChannelCount property.
    pub stable_channel_count: Option<String>,
    /// unreportedChannelCount property.
    pub unreported_channel_count: Option<String>,
    /// unsupportedChannelCount property.
    pub unsupported_channel_count: Option<String>,
}

/// `GoogleChromeManagementV1EnumeratePrintJobsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1EnumeratePrintJobsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// printJobs property.
    pub print_jobs: Option<Vec<GoogleChromeManagementV1PrintJob>>,
    /// totalSize property.
    pub total_size: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryAppUninstallEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryAppUninstallEvent {
    /// appId property.
    pub app_id: Option<String>,
    /// appType property.
    pub app_type: Option<String>,
    /// appUninstallSource property.
    pub app_uninstall_source: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryOsCrashEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryOsCrashEvent {
    /// crashId property.
    pub crash_id: Option<String>,
    /// crashType property.
    pub crash_type: Option<String>,
    /// sessionType property.
    pub session_type: Option<String>,
}

/// `GoogleChromeManagementV1TotalMemoryEncryptionInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TotalMemoryEncryptionInfo {
    /// encryptionAlgorithm property.
    pub encryption_algorithm: Option<String>,
    /// encryptionState property.
    pub encryption_state: Option<String>,
    /// keyLength property.
    pub key_length: Option<String>,
    /// maxKeys property.
    pub max_keys: Option<String>,
}

/// `GoogleChromeManagementV1RiskAssessment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1RiskAssessment {
    /// assessment property.
    pub assessment: Option<String>,
    /// detailsUrl property.
    pub details_url: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `GoogleChromeManagementV1ListTelemetryDevicesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1ListTelemetryDevicesResponse {
    /// devices property.
    pub devices: Option<Vec<GoogleChromeManagementV1TelemetryDevice>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand {
    /// commandResult property.
    pub command_result:
        Option<GoogleChromeManagementVersionsV1ChromeBrowserProfileCommandCommandResult>,
    /// commandState property.
    pub command_state: Option<String>,
    /// commandType property.
    pub command_type: Option<String>,
    /// issueTime property.
    pub issue_time: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// payload property.
    pub payload: Option<serde_json::Value>,
    /// validDuration property.
    pub valid_duration: Option<String>,
}

/// `GoogleChromeManagementV1CountChromeCrashEventsResponseCrashEventCount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountChromeCrashEventsResponseCrashEventCount {
    /// browserVersion property.
    pub browser_version: Option<String>,
    /// count property.
    pub count: Option<String>,
    /// date property.
    pub date: Option<GoogleTypeDate>,
}

/// `GoogleChromeManagementVersionsV1ChromeBrowserProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ChromeBrowserProfile {
    /// affiliationState property.
    pub affiliation_state: Option<String>,
    /// annotatedLocation property.
    pub annotated_location: Option<String>,
    /// annotatedUser property.
    pub annotated_user: Option<String>,
    /// attestationCredential property.
    pub attestation_credential: Option<GoogleChromeManagementVersionsV1AttestationCredential>,
    /// browserChannel property.
    pub browser_channel: Option<String>,
    /// browserVersion property.
    pub browser_version: Option<String>,
    /// deviceInfo property.
    pub device_info: Option<GoogleChromeManagementVersionsV1DeviceInfo>,
    /// displayName property.
    pub display_name: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// extensionCount property.
    pub extension_count: Option<String>,
    /// firstEnrollmentTime property.
    pub first_enrollment_time: Option<String>,
    /// identityProvider property.
    pub identity_provider: Option<String>,
    /// lastActivityTime property.
    pub last_activity_time: Option<String>,
    /// lastPolicyFetchTime property.
    pub last_policy_fetch_time: Option<String>,
    /// lastPolicySyncTime property.
    pub last_policy_sync_time: Option<String>,
    /// lastStatusReportTime property.
    pub last_status_report_time: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// osPlatformType property.
    pub os_platform_type: Option<String>,
    /// osPlatformVersion property.
    pub os_platform_version: Option<String>,
    /// osVersion property.
    pub os_version: Option<String>,
    /// policyCount property.
    pub policy_count: Option<String>,
    /// profileId property.
    pub profile_id: Option<String>,
    /// profilePermanentId property.
    pub profile_permanent_id: Option<String>,
    /// reportingData property.
    pub reporting_data: Option<GoogleChromeManagementVersionsV1ReportingData>,
    /// supportsFcmNotifications property.
    pub supports_fcm_notifications: Option<bool>,
    /// userEmail property.
    pub user_email: Option<String>,
    /// userId property.
    pub user_id: Option<String>,
}

/// `GoogleChromeManagementVersionsV1ChromeOsDevice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1ChromeOsDevice {
    /// deviceDirectoryApiId property.
    pub device_directory_api_id: Option<String>,
    /// serialNumber property.
    pub serial_number: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryNetworkSignalStrengthEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryNetworkSignalStrengthEvent {
    /// guid property.
    pub guid: Option<String>,
    /// signalStrengthDbm property.
    pub signal_strength_dbm: Option<i64>,
}

/// `GoogleChromeManagementV1StorageStatusReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1StorageStatusReport {
    /// disk property.
    pub disk: Option<Vec<GoogleChromeManagementV1DiskInfo>>,
    /// reportTime property.
    pub report_time: Option<String>,
}

/// `GoogleLongrunningOperation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleLongrunningOperation {
    /// done property.
    pub done: Option<bool>,
    /// error property.
    pub error: Option<GoogleRpcStatus>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// response property.
    pub response: Option<serde_json::Value>,
}

/// `GoogleChromeManagementV1CountChromeDevicesThatNeedAttentionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountChromeDevicesThatNeedAttentionResponse {
    /// noRecentPolicySyncCount property.
    pub no_recent_policy_sync_count: Option<String>,
    /// noRecentUserActivityCount property.
    pub no_recent_user_activity_count: Option<String>,
    /// osVersionNotCompliantCount property.
    pub os_version_not_compliant_count: Option<String>,
    /// pendingUpdate property.
    pub pending_update: Option<String>,
    /// unsupportedPolicyCount property.
    pub unsupported_policy_count: Option<String>,
}

/// `GoogleChromeManagementVersionsV1SignDataRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1SignDataRequest {
    /// signData property.
    pub sign_data: Option<String>,
    /// signatureAlgorithm property.
    pub signature_algorithm: Option<String>,
}

/// `GoogleChromeManagementV1BrowserVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1BrowserVersion {
    /// channel property.
    pub channel: Option<String>,
    /// count property.
    pub count: Option<String>,
    /// deviceOsVersion property.
    pub device_os_version: Option<String>,
    /// system property.
    pub system: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `GoogleChromeManagementV1FindInstalledAppDevicesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1FindInstalledAppDevicesResponse {
    /// devices property.
    pub devices: Option<Vec<GoogleChromeManagementV1Device>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalSize property.
    pub total_size: Option<i64>,
}

/// `GoogleChromeManagementV1CpuStatusReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CpuStatusReport {
    /// cpuTemperatureInfo property.
    pub cpu_temperature_info: Option<Vec<GoogleChromeManagementV1CpuTemperatureInfo>>,
    /// cpuUtilizationPct property.
    pub cpu_utilization_pct: Option<i64>,
    /// reportTime property.
    pub report_time: Option<String>,
    /// sampleFrequency property.
    pub sample_frequency: Option<String>,
}

/// `GoogleChromeManagementV1StorageInfoDiskVolume` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1StorageInfoDiskVolume {
    /// storageFreeBytes property.
    pub storage_free_bytes: Option<String>,
    /// storageTotalBytes property.
    pub storage_total_bytes: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `GoogleChromeManagementVersionsV1DeviceInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1DeviceInfo {
    /// affiliatedDeviceId property.
    pub affiliated_device_id: Option<String>,
    /// deviceType property.
    pub device_type: Option<String>,
    /// hostname property.
    pub hostname: Option<String>,
    /// machine property.
    pub machine: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryUser` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryUser {
    /// customer property.
    pub customer: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// orgUnitId property.
    pub org_unit_id: Option<String>,
    /// userDevice property.
    pub user_device: Option<Vec<GoogleChromeManagementV1TelemetryUserDevice>>,
    /// userEmail property.
    pub user_email: Option<String>,
    /// userId property.
    pub user_id: Option<String>,
}

/// `GoogleChromeManagementV1CountChromeDevicesReachingAutoExpirationDateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountChromeDevicesReachingAutoExpirationDateResponse {
    /// deviceAueCountReports property.
    pub device_aue_count_reports: Option<Vec<GoogleChromeManagementV1DeviceAueCountReport>>,
}

/// `GoogleChromeManagementV1GraphicsInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1GraphicsInfo {
    /// adapterInfo property.
    pub adapter_info: Option<GoogleChromeManagementV1GraphicsAdapterInfo>,
    /// displayDevices property.
    pub display_devices: Option<Vec<GoogleChromeManagementV1DisplayDevice>>,
    /// eprivacySupported property.
    pub eprivacy_supported: Option<bool>,
    /// touchScreenInfo property.
    pub touch_screen_info: Option<GoogleChromeManagementV1TouchScreenInfo>,
}

/// `GoogleChromeManagementV1DeviceRequestingExtensionDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1DeviceRequestingExtensionDetails {
    /// deviceName property.
    pub device_name: Option<String>,
    /// justification property.
    pub justification: Option<String>,
}

/// `GoogleProtobufEmpty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleProtobufEmpty {}

/// `GoogleChromeManagementV1CountChromeHardwareFleetDevicesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1CountChromeHardwareFleetDevicesResponse {
    /// cpuReports property.
    pub cpu_reports: Option<Vec<GoogleChromeManagementV1DeviceHardwareCountReport>>,
    /// memoryReports property.
    pub memory_reports: Option<Vec<GoogleChromeManagementV1DeviceHardwareCountReport>>,
    /// modelReports property.
    pub model_reports: Option<Vec<GoogleChromeManagementV1DeviceHardwareCountReport>>,
    /// storageReports property.
    pub storage_reports: Option<Vec<GoogleChromeManagementV1DeviceHardwareCountReport>>,
}

/// `GoogleChromeManagementV1TelemetryEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryEvent {
    /// appInstallEvent property.
    pub app_install_event: Option<GoogleChromeManagementV1TelemetryAppInstallEvent>,
    /// appLaunchEvent property.
    pub app_launch_event: Option<GoogleChromeManagementV1TelemetryAppLaunchEvent>,
    /// appUninstallEvent property.
    pub app_uninstall_event: Option<GoogleChromeManagementV1TelemetryAppUninstallEvent>,
    /// audioSevereUnderrunEvent property.
    pub audio_severe_underrun_event:
        Option<GoogleChromeManagementV1TelemetryAudioSevereUnderrunEvent>,
    /// device property.
    pub device: Option<GoogleChromeManagementV1TelemetryDeviceInfo>,
    /// eventType property.
    pub event_type: Option<String>,
    /// externalDisplaysEvent property.
    pub external_displays_event: Option<GoogleChromeManagementV1TelemetryExternalDisplayEvent>,
    /// httpsLatencyChangeEvent property.
    pub https_latency_change_event:
        Option<GoogleChromeManagementV1TelemetryHttpsLatencyChangeEvent>,
    /// name property.
    pub name: Option<String>,
    /// networkStateChangeEvent property.
    pub network_state_change_event:
        Option<GoogleChromeManagementV1TelemetryNetworkConnectionStateChangeEvent>,
    /// osCrashEvent property.
    pub os_crash_event: Option<GoogleChromeManagementV1TelemetryOsCrashEvent>,
    /// reportTime property.
    pub report_time: Option<String>,
    /// usbPeripheralsEvent property.
    pub usb_peripherals_event: Option<GoogleChromeManagementV1TelemetryUsbPeripheralsEvent>,
    /// user property.
    pub user: Option<GoogleChromeManagementV1TelemetryUserInfo>,
    /// vpnConnectionStateChangeEvent property.
    pub vpn_connection_state_change_event:
        Option<GoogleChromeManagementV1TelemetryNetworkConnectionStateChangeEvent>,
    /// wifiSignalStrengthEvent property.
    pub wifi_signal_strength_event:
        Option<GoogleChromeManagementV1TelemetryNetworkSignalStrengthEvent>,
}

/// `GoogleLongrunningCancelOperationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleLongrunningCancelOperationRequest {}

/// `GoogleChromeManagementV1KioskAppStatusReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1KioskAppStatusReport {
    /// appId property.
    pub app_id: Option<String>,
    /// appVersion property.
    pub app_version: Option<String>,
    /// reportTime property.
    pub report_time: Option<String>,
}

/// `GoogleChromeManagementV1TelemetryHttpsLatencyChangeEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementV1TelemetryHttpsLatencyChangeEvent {
    /// httpsLatencyRoutineData property.
    pub https_latency_routine_data: Option<GoogleChromeManagementV1HttpsLatencyRoutineData>,
    /// httpsLatencyState property.
    pub https_latency_state: Option<String>,
}

/// `GoogleChromeManagementVersionsV1GenericCaConnection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleChromeManagementVersionsV1GenericCaConnection {
    /// caConnectionAdapterConfigReference property.
    pub ca_connection_adapter_config_reference: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`chromemanagement.customers.apps.countChromeAppRequests_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersAppsCountChromeAppRequestsArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `orgUnitId`.
    pub org_unit_id: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`chromemanagement.customers.apps.fetchDevicesRequestingExtension_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersAppsFetchDevicesRequestingExtensionArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `extensionId`.
    pub extension_id: Option<String>,
    /// Query parameter: `orgUnitId`.
    pub org_unit_id: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`chromemanagement.customers.apps.fetchUsersRequestingExtension_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersAppsFetchUsersRequestingExtensionArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `extensionId`.
    pub extension_id: Option<String>,
    /// Query parameter: `orgUnitId`.
    pub org_unit_id: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`chromemanagement.customers.apps.android.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersAppsAndroidGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`chromemanagement.customers.apps.chrome.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersAppsChromeGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`chromemanagement.customers.apps.web.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersAppsWebGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`chromemanagement.customers.certificateProvisioningProcesses.claim_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersCertificateProvisioningProcessesClaimArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessRequest,
}

/// Arguments for [`chromemanagement.customers.certificateProvisioningProcesses.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersCertificateProvisioningProcessesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`chromemanagement.customers.certificateProvisioningProcesses.setFailure_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersCertificateProvisioningProcessesSetFailureArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleChromeManagementVersionsV1SetFailureRequest,
}

/// Arguments for [`chromemanagement.customers.certificateProvisioningProcesses.signData_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersCertificateProvisioningProcessesSignDataArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleChromeManagementVersionsV1SignDataRequest,
}

/// Arguments for [`chromemanagement.customers.certificateProvisioningProcesses.uploadCertificate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersCertificateProvisioningProcessesUploadCertificateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleChromeManagementVersionsV1UploadCertificateRequest,
}

/// Arguments for [`chromemanagement.customers.certificateProvisioningProcesses.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersCertificateProvisioningProcessesOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`chromemanagement.customers.profiles.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersProfilesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`chromemanagement.customers.profiles.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersProfilesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`chromemanagement.customers.profiles.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersProfilesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`chromemanagement.customers.profiles.commands.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersProfilesCommandsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand,
}

/// Arguments for [`chromemanagement.customers.profiles.commands.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersProfilesCommandsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`chromemanagement.customers.profiles.commands.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersProfilesCommandsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countActiveDevices_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountActiveDevicesArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `date.day`.
    pub date_day: Option<String>,
    /// Query parameter: `date.month`.
    pub date_month: Option<String>,
    /// Query parameter: `date.year`.
    pub date_year: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countChromeBrowsersNeedingAttention_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeBrowsersNeedingAttentionArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `orgUnitId`.
    pub org_unit_id: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countChromeCrashEvents_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeCrashEventsArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `orgUnitId`.
    pub org_unit_id: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countChromeDevicesReachingAutoExpirationDate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeDevicesReachingAutoExpirationDateArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `maxAueDate`.
    pub max_aue_date: Option<String>,
    /// Query parameter: `minAueDate`.
    pub min_aue_date: Option<String>,
    /// Query parameter: `orgUnitId`.
    pub org_unit_id: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countChromeDevicesThatNeedAttention_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeDevicesThatNeedAttentionArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `orgUnitId`.
    pub org_unit_id: Option<String>,
    /// Query parameter: `readMask`.
    pub read_mask: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countChromeHardwareFleetDevices_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeHardwareFleetDevicesArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `orgUnitId`.
    pub org_unit_id: Option<String>,
    /// Query parameter: `readMask`.
    pub read_mask: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countChromeVersions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountChromeVersionsArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orgUnitId`.
    pub org_unit_id: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countDevicesPerBootType_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountDevicesPerBootTypeArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `date.day`.
    pub date_day: Option<String>,
    /// Query parameter: `date.month`.
    pub date_month: Option<String>,
    /// Query parameter: `date.year`.
    pub date_year: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countDevicesPerReleaseChannel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountDevicesPerReleaseChannelArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `date.day`.
    pub date_day: Option<String>,
    /// Query parameter: `date.month`.
    pub date_month: Option<String>,
    /// Query parameter: `date.year`.
    pub date_year: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countInstalledApps_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountInstalledAppsArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `orgUnitId`.
    pub org_unit_id: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countPrintJobsByPrinter_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountPrintJobsByPrinterArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `printerOrgUnitId`.
    pub printer_org_unit_id: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.countPrintJobsByUser_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsCountPrintJobsByUserArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `printerOrgUnitId`.
    pub printer_org_unit_id: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.enumeratePrintJobs_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsEnumeratePrintJobsArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `printerOrgUnitId`.
    pub printer_org_unit_id: Option<String>,
}

/// Arguments for [`chromemanagement.customers.reports.findInstalledAppDevices_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersReportsFindInstalledAppDevicesArgs {
    /// Path parameter: `customer`.
    pub customer: String,
    /// Query parameter: `appId`.
    pub app_id: Option<String>,
    /// Query parameter: `appType`.
    pub app_type: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `orgUnitId`.
    pub org_unit_id: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`chromemanagement.customers.telemetry.devices.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryDevicesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `readMask`.
    pub read_mask: Option<String>,
}

/// Arguments for [`chromemanagement.customers.telemetry.devices.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryDevicesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `readMask`.
    pub read_mask: Option<String>,
}

/// Arguments for [`chromemanagement.customers.telemetry.events.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryEventsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `readMask`.
    pub read_mask: Option<String>,
}

/// Arguments for [`chromemanagement.customers.telemetry.notificationConfigs.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryNotificationConfigsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleChromeManagementV1TelemetryNotificationConfig,
}

/// Arguments for [`chromemanagement.customers.telemetry.notificationConfigs.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryNotificationConfigsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`chromemanagement.customers.telemetry.notificationConfigs.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryNotificationConfigsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`chromemanagement.customers.telemetry.users.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryUsersGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `readMask`.
    pub read_mask: Option<String>,
}

/// Arguments for [`chromemanagement.customers.telemetry.users.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersTelemetryUsersListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `readMask`.
    pub read_mask: Option<String>,
}

/// Arguments for [`chromemanagement.customers.thirdPartyProfileUsers.move_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementCustomersThirdPartyProfileUsersMoveArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserRequest,
}

/// Arguments for [`chromemanagement.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementOperationsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleLongrunningCancelOperationRequest,
}

/// Arguments for [`chromemanagement.operations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementOperationsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`chromemanagement.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct ChromemanagementOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/{+customer}/apps:countChromeAppRequests
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/apps:countChromeAppRequests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_apps_count_chrome_app_requests_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_apps_count_chrome_app_requests_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersAppsCountChromeAppRequestsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeAppRequestsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/apps:countChromeAppRequests",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeAppRequestsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/apps:fetchDevicesRequestingExtension
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/apps:fetchDevicesRequestingExtension.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_apps_fetch_devices_requesting_extension_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_apps_fetch_devices_requesting_extension_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersAppsFetchDevicesRequestingExtensionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1FetchDevicesRequestingExtensionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/apps:fetchDevicesRequestingExtension",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.extension_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("extensionId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1FetchDevicesRequestingExtensionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/apps:fetchUsersRequestingExtension
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/apps:fetchUsersRequestingExtension.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_apps_fetch_users_requesting_extension_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_apps_fetch_users_requesting_extension_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersAppsFetchUsersRequestingExtensionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1FetchUsersRequestingExtensionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/apps:fetchUsersRequestingExtension",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.extension_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("extensionId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1FetchUsersRequestingExtensionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_apps_android_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_apps_android_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersAppsAndroidGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1AppDetails>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1AppDetails = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_apps_chrome_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_apps_chrome_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersAppsChromeGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1AppDetails>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1AppDetails = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_apps_web_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_apps_web_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersAppsWebGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1AppDetails>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1AppDetails = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:claim
// -----------------------------------------------------------------------------

/// POST v1/{+name}:claim.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_certificate_provisioning_processes_claim_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_certificate_provisioning_processes_claim_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersCertificateProvisioningProcessesClaimArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessResponse,
                >,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}:claim",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(
        builder
            .build_send_request()
            .map_err(|e: foundation_core::wire::simple_http::HttpClientError| super::shared::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                super::shared::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                    let parsed: GoogleChromeManagementVersionsV1ClaimCertificateProvisioningProcessResponse = serde_json::from_str(&body).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                super::shared::RequestIntro::Failed(e) => Err(super::shared::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| super::shared::ApiPending::Sending)
    )
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_certificate_provisioning_processes_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_certificate_provisioning_processes_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersCertificateProvisioningProcessesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementVersionsV1CertificateProvisioningProcess>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1CertificateProvisioningProcess =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:setFailure
// -----------------------------------------------------------------------------

/// POST v1/{+name}:setFailure.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_certificate_provisioning_processes_set_failure_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_certificate_provisioning_processes_set_failure_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersCertificateProvisioningProcessesSetFailureArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementVersionsV1SetFailureResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}:setFailure",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1SetFailureResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:signData
// -----------------------------------------------------------------------------

/// POST v1/{+name}:signData.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_certificate_provisioning_processes_sign_data_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_certificate_provisioning_processes_sign_data_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersCertificateProvisioningProcessesSignDataArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}:signData",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:uploadCertificate
// -----------------------------------------------------------------------------

/// POST v1/{+name}:uploadCertificate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_certificate_provisioning_processes_upload_certificate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_certificate_provisioning_processes_upload_certificate_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersCertificateProvisioningProcessesUploadCertificateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementVersionsV1UploadCertificateResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}:uploadCertificate",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1UploadCertificateResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_certificate_provisioning_processes_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_certificate_provisioning_processes_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersCertificateProvisioningProcessesOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_profiles_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_profiles_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersProfilesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_profiles_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_profiles_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersProfilesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ChromeBrowserProfile>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1ChromeBrowserProfile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/profiles
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/profiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_profiles_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_profiles_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersProfilesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ListChromeBrowserProfilesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/profiles",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1ListChromeBrowserProfilesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/commands
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/commands.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_profiles_commands_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_profiles_commands_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersProfilesCommandsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/commands",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_profiles_commands_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_profiles_commands_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersProfilesCommandsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1ChromeBrowserProfileCommand =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/commands
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/commands.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_profiles_commands_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_profiles_commands_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersProfilesCommandsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleChromeManagementVersionsV1ListChromeBrowserProfileCommandsResponse,
                >,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/commands",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(
        builder
            .build_send_request()
            .map_err(|e: foundation_core::wire::simple_http::HttpClientError| super::shared::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                super::shared::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                    let parsed: GoogleChromeManagementVersionsV1ListChromeBrowserProfileCommandsResponse = serde_json::from_str(&body).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                super::shared::RequestIntro::Failed(e) => Err(super::shared::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| super::shared::ApiPending::Sending)
    )
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countActiveDevices
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countActiveDevices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_active_devices_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_active_devices_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountActiveDevicesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountActiveDevicesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countActiveDevices",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("date_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("date_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("date_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountActiveDevicesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countChromeBrowsersNeedingAttention
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countChromeBrowsersNeedingAttention.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_chrome_browsers_needing_attention_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_chrome_browsers_needing_attention_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountChromeBrowsersNeedingAttentionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeBrowsersNeedingAttentionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countChromeBrowsersNeedingAttention",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeBrowsersNeedingAttentionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countChromeCrashEvents
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countChromeCrashEvents.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_chrome_crash_events_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_chrome_crash_events_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountChromeCrashEventsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeCrashEventsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countChromeCrashEvents",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeCrashEventsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countChromeDevicesReachingAutoExpirationDate
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countChromeDevicesReachingAutoExpirationDate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_chrome_devices_reaching_auto_expiration_date_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountChromeDevicesReachingAutoExpirationDateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleChromeManagementV1CountChromeDevicesReachingAutoExpirationDateResponse,
                >,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countChromeDevicesReachingAutoExpirationDate",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_aue_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxAueDate=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.min_aue_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minAueDate=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(
        builder
            .build_send_request()
            .map_err(|e: foundation_core::wire::simple_http::HttpClientError| super::shared::ApiError::RequestBuildFailed(e.to_string()))?
            .map_ready(|intro| match intro {
                super::shared::RequestIntro::Success { stream, intro, headers, .. } => {
                    let status: usize = intro.0.into();
                    if status < 200 || status >= 300 {
                        return Err(super::shared::ApiError::HttpStatus { code: status as u16, headers: headers.clone(), body: None });
                    }
                    let body = foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                    let parsed: GoogleChromeManagementV1CountChromeDevicesReachingAutoExpirationDateResponse = serde_json::from_str(&body).map_err(|e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()))?;
                    Ok(ApiResponse { status: status as u16, headers: headers.clone(), body: parsed })
                }
                super::shared::RequestIntro::Failed(e) => Err(super::shared::ApiError::RequestSendFailed(e.to_string())),
            })
            .map_pending(|_| super::shared::ApiPending::Sending)
    )
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countChromeDevicesThatNeedAttention
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countChromeDevicesThatNeedAttention.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_chrome_devices_that_need_attention_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_chrome_devices_that_need_attention_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountChromeDevicesThatNeedAttentionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeDevicesThatNeedAttentionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countChromeDevicesThatNeedAttention",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.read_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("readMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeDevicesThatNeedAttentionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countChromeHardwareFleetDevices
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countChromeHardwareFleetDevices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_chrome_hardware_fleet_devices_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountChromeHardwareFleetDevicesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeHardwareFleetDevicesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countChromeHardwareFleetDevices",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.read_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("readMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeHardwareFleetDevicesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countChromeVersions
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countChromeVersions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_chrome_versions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_chrome_versions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountChromeVersionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountChromeVersionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countChromeVersions",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountChromeVersionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countDevicesPerBootType
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countDevicesPerBootType.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_devices_per_boot_type_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_devices_per_boot_type_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountDevicesPerBootTypeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerBootTypeResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countDevicesPerBootType",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("date_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("date_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("date_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountDevicesPerBootTypeResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countDevicesPerReleaseChannel
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countDevicesPerReleaseChannel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_devices_per_release_channel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_devices_per_release_channel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountDevicesPerReleaseChannelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountDevicesPerReleaseChannelResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countDevicesPerReleaseChannel",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("date_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("date_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("date_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountDevicesPerReleaseChannelResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countInstalledApps
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countInstalledApps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_installed_apps_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_installed_apps_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountInstalledAppsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountInstalledAppsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countInstalledApps",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountInstalledAppsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countPrintJobsByPrinter
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countPrintJobsByPrinter.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_print_jobs_by_printer_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_print_jobs_by_printer_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountPrintJobsByPrinterArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountPrintJobsByPrinterResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countPrintJobsByPrinter",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.printer_org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("printerOrgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountPrintJobsByPrinterResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:countPrintJobsByUser
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:countPrintJobsByUser.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_count_print_jobs_by_user_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_count_print_jobs_by_user_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsCountPrintJobsByUserArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1CountPrintJobsByUserResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:countPrintJobsByUser",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.printer_org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("printerOrgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1CountPrintJobsByUserResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:enumeratePrintJobs
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:enumeratePrintJobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_enumerate_print_jobs_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_enumerate_print_jobs_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsEnumeratePrintJobsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1EnumeratePrintJobsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:enumeratePrintJobs",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.printer_org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("printerOrgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1EnumeratePrintJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+customer}/reports:findInstalledAppDevices
// -----------------------------------------------------------------------------

/// GET v1/{+customer}/reports:findInstalledAppDevices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_reports_find_installed_app_devices_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_reports_find_installed_app_devices_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersReportsFindInstalledAppDevicesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1FindInstalledAppDevicesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/reports:findInstalledAppDevices",
        args.customer,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.app_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("appId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.app_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("appType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.org_unit_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orgUnitId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1FindInstalledAppDevicesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_telemetry_devices_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_telemetry_devices_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersTelemetryDevicesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1TelemetryDevice>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.read_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("readMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1TelemetryDevice = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/telemetry/devices
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/telemetry/devices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_telemetry_devices_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_telemetry_devices_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersTelemetryDevicesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1ListTelemetryDevicesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/telemetry/devices",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.read_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("readMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1ListTelemetryDevicesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/telemetry/events
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/telemetry/events.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_telemetry_events_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_telemetry_events_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersTelemetryEventsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1ListTelemetryEventsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/telemetry/events",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.read_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("readMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1ListTelemetryEventsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/telemetry/notificationConfigs
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/telemetry/notificationConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_telemetry_notification_configs_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_telemetry_notification_configs_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersTelemetryNotificationConfigsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1TelemetryNotificationConfig>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/telemetry/notificationConfigs",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1TelemetryNotificationConfig =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_telemetry_notification_configs_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_telemetry_notification_configs_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersTelemetryNotificationConfigsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/telemetry/notificationConfigs
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/telemetry/notificationConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_telemetry_notification_configs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_telemetry_notification_configs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersTelemetryNotificationConfigsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1ListTelemetryNotificationConfigsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/telemetry/notificationConfigs",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1ListTelemetryNotificationConfigsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_telemetry_users_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_telemetry_users_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersTelemetryUsersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1TelemetryUser>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.read_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("readMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1TelemetryUser = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/telemetry/users
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/telemetry/users.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_telemetry_users_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_telemetry_users_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersTelemetryUsersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementV1ListTelemetryUsersResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}/telemetry/users",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.read_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("readMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementV1ListTelemetryUsersResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:move
// -----------------------------------------------------------------------------

/// POST v1/{+name}:move.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_customers_third_party_profile_users_move_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_customers_third_party_profile_users_move_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementCustomersThirdPartyProfileUsersMoveArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}:move",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleChromeManagementVersionsV1MoveThirdPartyProfileUserResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://chromemanagement.googleapis.com/v1/{}:cancel",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_operations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_operations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementOperationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = chromemanagement_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn chromemanagement_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ChromemanagementOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleLongrunningListOperationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://chromemanagement.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
