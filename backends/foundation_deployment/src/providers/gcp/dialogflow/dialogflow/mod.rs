//! Auto-generated API module for gcp/dialogflow dialogflow.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dialogflow_dialogflow `

#![cfg(feature = "gcp_dialogflow_dialogflow")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleCloudDialogflowCxV3ResponseMessageConversationSuccess` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResponseMessageConversationSuccess {
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
}

/// `GoogleCloudDialogflowCxV3LoadVersionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3LoadVersionRequest {
    /// allowOverrideAgentResources property.
    pub allow_override_agent_resources: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3CodeBlock` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3CodeBlock {
    /// code property.
    pub code: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListVersionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListVersionsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// versions property.
    pub versions: Option<Vec<GoogleCloudDialogflowCxV3Version>>,
}

/// `GoogleCloudDialogflowCxV3Flow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Flow {
    /// advancedSettings property.
    pub advanced_settings: Option<GoogleCloudDialogflowCxV3AdvancedSettings>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// eventHandlers property.
    pub event_handlers: Option<Vec<GoogleCloudDialogflowCxV3EventHandler>>,
    /// inputParameterDefinitions property.
    pub input_parameter_definitions: Option<Vec<GoogleCloudDialogflowCxV3ParameterDefinition>>,
    /// knowledgeConnectorSettings property.
    pub knowledge_connector_settings: Option<GoogleCloudDialogflowCxV3KnowledgeConnectorSettings>,
    /// locked property.
    pub locked: Option<bool>,
    /// multiLanguageSettings property.
    pub multi_language_settings: Option<GoogleCloudDialogflowCxV3FlowMultiLanguageSettings>,
    /// name property.
    pub name: Option<String>,
    /// nluSettings property.
    pub nlu_settings: Option<GoogleCloudDialogflowCxV3NluSettings>,
    /// outputParameterDefinitions property.
    pub output_parameter_definitions: Option<Vec<GoogleCloudDialogflowCxV3ParameterDefinition>>,
    /// transitionRouteGroups property.
    pub transition_route_groups: Option<Vec<String>>,
    /// transitionRoutes property.
    pub transition_routes: Option<Vec<GoogleCloudDialogflowCxV3TransitionRoute>>,
}

/// `GoogleCloudDialogflowCxV3ListAgentsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListAgentsResponse {
    /// agents property.
    pub agents: Option<Vec<GoogleCloudDialogflowCxV3Agent>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ImportPlaybookRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ImportPlaybookRequest {
    /// importStrategy property.
    pub import_strategy: Option<GoogleCloudDialogflowCxV3PlaybookImportStrategy>,
    /// playbookContent property.
    pub playbook_content: Option<String>,
    /// playbookUri property.
    pub playbook_uri: Option<String>,
}

/// `GoogleCloudDialogflowCxV3TestCase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TestCase {
    /// creationTime property.
    pub creation_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// lastTestResult property.
    pub last_test_result: Option<GoogleCloudDialogflowCxV3TestCaseResult>,
    /// name property.
    pub name: Option<String>,
    /// notes property.
    pub notes: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
    /// testCaseConversationTurns property.
    pub test_case_conversation_turns: Option<Vec<GoogleCloudDialogflowCxV3ConversationTurn>>,
    /// testConfig property.
    pub test_config: Option<GoogleCloudDialogflowCxV3TestConfig>,
}

/// `GoogleCloudDialogflowCxV3ToolVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolVersion {
    /// createTime property.
    pub create_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// tool property.
    pub tool: Option<GoogleCloudDialogflowCxV3Tool>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ToolAuthentication` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolAuthentication {
    /// apiKeyConfig property.
    pub api_key_config: Option<GoogleCloudDialogflowCxV3ToolAuthenticationApiKeyConfig>,
    /// bearerTokenConfig property.
    pub bearer_token_config: Option<GoogleCloudDialogflowCxV3ToolAuthenticationBearerTokenConfig>,
    /// oauthConfig property.
    pub oauth_config: Option<GoogleCloudDialogflowCxV3ToolAuthenticationOAuthConfig>,
    /// serviceAccountAuthConfig property.
    pub service_account_auth_config:
        Option<GoogleCloudDialogflowCxV3ToolAuthenticationServiceAccountAuthConfig>,
    /// serviceAgentAuthConfig property.
    pub service_agent_auth_config:
        Option<GoogleCloudDialogflowCxV3ToolAuthenticationServiceAgentAuthConfig>,
}

/// `GoogleCloudDialogflowCxV3ToolAuthenticationServiceAccountAuthConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolAuthenticationServiceAccountAuthConfig {
    /// serviceAccount property.
    pub service_account: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ToolCall` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolCall {
    /// action property.
    pub action: Option<String>,
    /// inputParameters property.
    pub input_parameters: Option<serde_json::Value>,
    /// tool property.
    pub tool: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ConversationTurnVirtualAgentOutput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ConversationTurnVirtualAgentOutput {
    /// currentPage property.
    pub current_page: Option<GoogleCloudDialogflowCxV3Page>,
    /// diagnosticInfo property.
    pub diagnostic_info: Option<serde_json::Value>,
    /// differences property.
    pub differences: Option<Vec<GoogleCloudDialogflowCxV3TestRunDifference>>,
    /// sessionParameters property.
    pub session_parameters: Option<serde_json::Value>,
    /// status property.
    pub status: Option<GoogleRpcStatus>,
    /// textResponses property.
    pub text_responses: Option<Vec<GoogleCloudDialogflowCxV3ResponseMessageText>>,
    /// triggeredIntent property.
    pub triggered_intent: Option<GoogleCloudDialogflowCxV3Intent>,
}

/// `GoogleCloudDialogflowCxV3WebhookGenericWebServiceServiceAccountAuthConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3WebhookGenericWebServiceServiceAccountAuthConfig {
    /// serviceAccount property.
    pub service_account: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ToolAuthenticationServiceAgentAuthConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolAuthenticationServiceAgentAuthConfig {
    /// serviceAgentAuth property.
    pub service_agent_auth: Option<String>,
}

/// `GoogleCloudDialogflowCxV3GenerativeSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3GenerativeSettings {
    /// fallbackSettings property.
    pub fallback_settings: Option<GoogleCloudDialogflowCxV3GenerativeSettingsFallbackSettings>,
    /// generativeSafetySettings property.
    pub generative_safety_settings: Option<GoogleCloudDialogflowCxV3SafetySettings>,
    /// knowledgeConnectorSettings property.
    pub knowledge_connector_settings:
        Option<GoogleCloudDialogflowCxV3GenerativeSettingsKnowledgeConnectorSettings>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// llmModelSettings property.
    pub llm_model_settings: Option<GoogleCloudDialogflowCxV3LlmModelSettings>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleCloudDialogflowCxV3DetectIntentRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DetectIntentRequest {
    /// outputAudioConfig property.
    pub output_audio_config: Option<GoogleCloudDialogflowCxV3OutputAudioConfig>,
    /// queryInput property.
    pub query_input: Option<GoogleCloudDialogflowCxV3QueryInput>,
    /// queryParams property.
    pub query_params: Option<GoogleCloudDialogflowCxV3QueryParameters>,
    /// responseView property.
    pub response_view: Option<String>,
}

/// `GoogleCloudDialogflowCxV3FulfillIntentResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FulfillIntentResponse {
    /// outputAudio property.
    pub output_audio: Option<String>,
    /// outputAudioConfig property.
    pub output_audio_config: Option<GoogleCloudDialogflowCxV3OutputAudioConfig>,
    /// queryResult property.
    pub query_result: Option<GoogleCloudDialogflowCxV3QueryResult>,
    /// responseId property.
    pub response_id: Option<String>,
}

/// `GoogleCloudDialogflowCxV3FormParameterFillBehavior` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FormParameterFillBehavior {
    /// initialPromptFulfillment property.
    pub initial_prompt_fulfillment: Option<GoogleCloudDialogflowCxV3Fulfillment>,
    /// repromptEventHandlers property.
    pub reprompt_event_handlers: Option<Vec<GoogleCloudDialogflowCxV3EventHandler>>,
}

/// `GoogleCloudDialogflowCxV3TextToSpeechSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TextToSpeechSettings {
    /// synthesizeSpeechConfigs property.
    pub synthesize_speech_configs: Option<serde_json::Value>,
}

/// `GoogleCloudDialogflowCxV3ListIntentsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListIntentsResponse {
    /// intents property.
    pub intents: Option<Vec<GoogleCloudDialogflowCxV3Intent>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3IntentParameter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3IntentParameter {
    /// entityType property.
    pub entity_type: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// isList property.
    pub is_list: Option<bool>,
    /// redact property.
    pub redact: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3RestorePlaybookVersionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3RestorePlaybookVersionRequest {}

/// `GoogleCloudDialogflowCxV3SecuritySettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SecuritySettings {
    /// audioExportSettings property.
    pub audio_export_settings: Option<GoogleCloudDialogflowCxV3SecuritySettingsAudioExportSettings>,
    /// deidentifyTemplate property.
    pub deidentify_template: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// insightsExportSettings property.
    pub insights_export_settings:
        Option<GoogleCloudDialogflowCxV3SecuritySettingsInsightsExportSettings>,
    /// inspectTemplate property.
    pub inspect_template: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// purgeDataTypes property.
    pub purge_data_types: Option<Vec<String>>,
    /// redactionScope property.
    pub redaction_scope: Option<String>,
    /// redactionStrategy property.
    pub redaction_strategy: Option<String>,
    /// retentionStrategy property.
    pub retention_strategy: Option<String>,
    /// retentionWindowDays property.
    pub retention_window_days: Option<i64>,
}

/// `GoogleCloudDialogflowCxV3ResponseMessageEndInteraction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResponseMessageEndInteraction {}

/// `GoogleCloudDialogflowCxV3RestoreAgentRequestGitSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3RestoreAgentRequestGitSource {
    /// trackingBranch property.
    pub tracking_branch: Option<String>,
}

/// `GoogleCloudDialogflowCxV3EnvironmentTestCasesConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3EnvironmentTestCasesConfig {
    /// enableContinuousRun property.
    pub enable_continuous_run: Option<bool>,
    /// enablePredeploymentRun property.
    pub enable_predeployment_run: Option<bool>,
    /// testCases property.
    pub test_cases: Option<Vec<String>>,
}

/// `GoogleCloudDialogflowCxV3WebhookGenericWebService` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3WebhookGenericWebService {
    /// allowedCaCerts property.
    pub allowed_ca_certs: Option<Vec<String>>,
    /// httpMethod property.
    pub http_method: Option<String>,
    /// oauthConfig property.
    pub oauth_config: Option<GoogleCloudDialogflowCxV3WebhookGenericWebServiceOAuthConfig>,
    /// parameterMapping property.
    pub parameter_mapping: Option<serde_json::Value>,
    /// password property.
    pub password: Option<String>,
    /// requestBody property.
    pub request_body: Option<String>,
    /// requestHeaders property.
    pub request_headers: Option<serde_json::Value>,
    /// secretVersionForUsernamePassword property.
    pub secret_version_for_username_password: Option<String>,
    /// secretVersionsForRequestHeaders property.
    pub secret_versions_for_request_headers: Option<serde_json::Value>,
    /// serviceAccountAuthConfig property.
    pub service_account_auth_config:
        Option<GoogleCloudDialogflowCxV3WebhookGenericWebServiceServiceAccountAuthConfig>,
    /// serviceAgentAuth property.
    pub service_agent_auth: Option<String>,
    /// uri property.
    pub uri: Option<String>,
    /// username property.
    pub username: Option<String>,
    /// webhookType property.
    pub webhook_type: Option<String>,
}

/// `GoogleCloudDialogflowCxV3DeployFlowRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DeployFlowRequest {
    /// flowVersion property.
    pub flow_version: Option<String>,
}

/// `GoogleCloudDialogflowCxV3DataStoreConnectionSignals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DataStoreConnectionSignals {
    /// answer property.
    pub answer: Option<String>,
    /// answerGenerationModelCallSignals property.
    pub answer_generation_model_call_signals:
        Option<GoogleCloudDialogflowCxV3DataStoreConnectionSignalsAnswerGenerationModelCallSignals>,
    /// answerParts property.
    pub answer_parts: Option<Vec<GoogleCloudDialogflowCxV3DataStoreConnectionSignalsAnswerPart>>,
    /// citedSnippets property.
    pub cited_snippets:
        Option<Vec<GoogleCloudDialogflowCxV3DataStoreConnectionSignalsCitedSnippet>>,
    /// groundingSignals property.
    pub grounding_signals:
        Option<GoogleCloudDialogflowCxV3DataStoreConnectionSignalsGroundingSignals>,
    /// rewriterModelCallSignals property.
    pub rewriter_model_call_signals:
        Option<GoogleCloudDialogflowCxV3DataStoreConnectionSignalsRewriterModelCallSignals>,
    /// rewrittenQuery property.
    pub rewritten_query: Option<String>,
    /// safetySignals property.
    pub safety_signals: Option<GoogleCloudDialogflowCxV3DataStoreConnectionSignalsSafetySignals>,
    /// searchSnippets property.
    pub search_snippets:
        Option<Vec<GoogleCloudDialogflowCxV3DataStoreConnectionSignalsSearchSnippet>>,
}

/// `GoogleCloudDialogflowCxV3BoostSpecConditionBoostSpecBoostControlSpecControlPoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3BoostSpecConditionBoostSpecBoostControlSpecControlPoint {
    /// attributeValue property.
    pub attribute_value: Option<String>,
    /// boostAmount property.
    pub boost_amount: Option<f64>,
}

/// `GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse {
    /// environments property.
    pub environments: Option<Vec<GoogleCloudDialogflowCxV3Environment>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ContinuousTestResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ContinuousTestResult {
    /// name property.
    pub name: Option<String>,
    /// result property.
    pub result: Option<String>,
    /// runTime property.
    pub run_time: Option<String>,
    /// testCaseResults property.
    pub test_case_results: Option<Vec<String>>,
}

/// `GoogleCloudDialogflowCxV3DataStoreConnectionSignalsCitedSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DataStoreConnectionSignalsCitedSnippet {
    /// searchSnippet property.
    pub search_snippet: Option<GoogleCloudDialogflowCxV3DataStoreConnectionSignalsSearchSnippet>,
    /// snippetIndex property.
    pub snippet_index: Option<i64>,
}

/// `GoogleCloudDialogflowCxV3SafetySettingsRaiSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SafetySettingsRaiSettings {
    /// categoryFilters property.
    pub category_filters:
        Option<Vec<GoogleCloudDialogflowCxV3SafetySettingsRaiSettingsCategoryFilter>>,
}

/// `GoogleCloudDialogflowCxV3VersionVariantsVariant` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3VersionVariantsVariant {
    /// isControlGroup property.
    pub is_control_group: Option<bool>,
    /// trafficAllocation property.
    pub traffic_allocation: Option<f64>,
    /// version property.
    pub version: Option<String>,
}

/// `GoogleCloudDialogflowCxV3MatchIntentResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3MatchIntentResponse {
    /// currentPage property.
    pub current_page: Option<GoogleCloudDialogflowCxV3Page>,
    /// matches property.
    pub matches: Option<Vec<GoogleCloudDialogflowCxV3Match>>,
    /// text property.
    pub text: Option<String>,
    /// transcript property.
    pub transcript: Option<String>,
    /// triggerEvent property.
    pub trigger_event: Option<String>,
    /// triggerIntent property.
    pub trigger_intent: Option<String>,
}

/// `GoogleProtobufEmpty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleProtobufEmpty {}

/// `GoogleCloudDialogflowCxV3BoostSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3BoostSpec {
    /// conditionBoostSpecs property.
    pub condition_boost_specs: Option<Vec<GoogleCloudDialogflowCxV3BoostSpecConditionBoostSpec>>,
}

/// `GoogleCloudDialogflowCxV3TransitionCoverage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TransitionCoverage {
    /// coverageScore property.
    pub coverage_score: Option<f64>,
    /// transitions property.
    pub transitions: Option<Vec<GoogleCloudDialogflowCxV3TransitionCoverageTransition>>,
}

/// `GoogleCloudDialogflowCxV3ParameterDefinition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ParameterDefinition {
    /// description property.
    pub description: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// typeSchema property.
    pub type_schema: Option<Box<GoogleCloudDialogflowCxV3TypeSchema>>,
}

/// `GoogleCloudDialogflowCxV3TextInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TextInput {
    /// text property.
    pub text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3AgentGitIntegrationSettingsGithubSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AgentGitIntegrationSettingsGithubSettings {
    /// accessToken property.
    pub access_token: Option<String>,
    /// branches property.
    pub branches: Option<Vec<String>>,
    /// displayName property.
    pub display_name: Option<String>,
    /// repositoryUri property.
    pub repository_uri: Option<String>,
    /// trackingBranch property.
    pub tracking_branch: Option<String>,
}

/// `GoogleCloudDialogflowCxV3Intent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Intent {
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// dtmfPattern property.
    pub dtmf_pattern: Option<String>,
    /// isFallback property.
    pub is_fallback: Option<bool>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// parameters property.
    pub parameters: Option<Vec<GoogleCloudDialogflowCxV3IntentParameter>>,
    /// priority property.
    pub priority: Option<i64>,
    /// trainingPhrases property.
    pub training_phrases: Option<Vec<GoogleCloudDialogflowCxV3IntentTrainingPhrase>>,
}

/// `GoogleCloudDialogflowCxV3FlowMultiLanguageSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FlowMultiLanguageSettings {
    /// enableMultiLanguageDetection property.
    pub enable_multi_language_detection: Option<bool>,
    /// supportedResponseLanguageCodes property.
    pub supported_response_language_codes: Option<Vec<String>>,
}

/// `GoogleCloudDialogflowCxV3ToolTLSConfigCACert` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolTLSConfigCACert {
    /// cert property.
    pub cert: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListPagesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListPagesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// pages property.
    pub pages: Option<Vec<GoogleCloudDialogflowCxV3Page>>,
}

/// `GoogleCloudDialogflowCxV3Agent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Agent {
    /// advancedSettings property.
    pub advanced_settings: Option<GoogleCloudDialogflowCxV3AdvancedSettings>,
    /// answerFeedbackSettings property.
    pub answer_feedback_settings: Option<GoogleCloudDialogflowCxV3AgentAnswerFeedbackSettings>,
    /// avatarUri property.
    pub avatar_uri: Option<String>,
    /// clientCertificateSettings property.
    pub client_certificate_settings:
        Option<GoogleCloudDialogflowCxV3AgentClientCertificateSettings>,
    /// defaultLanguageCode property.
    pub default_language_code: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// enableMultiLanguageTraining property.
    pub enable_multi_language_training: Option<bool>,
    /// enableSpellCorrection property.
    pub enable_spell_correction: Option<bool>,
    /// enableStackdriverLogging property.
    pub enable_stackdriver_logging: Option<bool>,
    /// genAppBuilderSettings property.
    pub gen_app_builder_settings: Option<GoogleCloudDialogflowCxV3AgentGenAppBuilderSettings>,
    /// gitIntegrationSettings property.
    pub git_integration_settings: Option<GoogleCloudDialogflowCxV3AgentGitIntegrationSettings>,
    /// locked property.
    pub locked: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// personalizationSettings property.
    pub personalization_settings: Option<GoogleCloudDialogflowCxV3AgentPersonalizationSettings>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// securitySettings property.
    pub security_settings: Option<String>,
    /// speechToTextSettings property.
    pub speech_to_text_settings: Option<GoogleCloudDialogflowCxV3SpeechToTextSettings>,
    /// startFlow property.
    pub start_flow: Option<String>,
    /// startPlaybook property.
    pub start_playbook: Option<String>,
    /// supportedLanguageCodes property.
    pub supported_language_codes: Option<Vec<String>>,
    /// textToSpeechSettings property.
    pub text_to_speech_settings: Option<GoogleCloudDialogflowCxV3TextToSpeechSettings>,
    /// timeZone property.
    pub time_zone: Option<String>,
}

/// `GoogleCloudDialogflowCxV3GcsDestination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3GcsDestination {
    /// uri property.
    pub uri: Option<String>,
}

/// `GoogleCloudDialogflowCxV3IntentInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3IntentInput {
    /// intent property.
    pub intent: Option<String>,
}

/// `GoogleCloudDialogflowCxV3EntityTypeExcludedPhrase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3EntityTypeExcludedPhrase {
    /// value property.
    pub value: Option<String>,
}

/// `GoogleCloudDialogflowCxV3AgentGenAppBuilderSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AgentGenAppBuilderSettings {
    /// engine property.
    pub engine: Option<String>,
}

/// `GoogleCloudDialogflowCxV3GeneratorModelParameter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3GeneratorModelParameter {
    /// maxDecodeSteps property.
    pub max_decode_steps: Option<i64>,
    /// temperature property.
    pub temperature: Option<f64>,
    /// topK property.
    pub top_k: Option<i64>,
    /// topP property.
    pub top_p: Option<f64>,
}

/// `GoogleCloudDialogflowCxV3ToolAuthenticationBearerTokenConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolAuthenticationBearerTokenConfig {
    /// secretVersionForToken property.
    pub secret_version_for_token: Option<String>,
    /// token property.
    pub token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3FilterSpecs` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FilterSpecs {
    /// dataStores property.
    pub data_stores: Option<Vec<String>>,
    /// filter property.
    pub filter: Option<String>,
}

/// `GoogleCloudDialogflowCxV3Version` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Version {
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nluSettings property.
    pub nlu_settings: Option<GoogleCloudDialogflowCxV3NluSettings>,
    /// state property.
    pub state: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ConversationTurn` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ConversationTurn {
    /// userInput property.
    pub user_input: Option<GoogleCloudDialogflowCxV3ConversationTurnUserInput>,
    /// virtualAgentOutput property.
    pub virtual_agent_output: Option<GoogleCloudDialogflowCxV3ConversationTurnVirtualAgentOutput>,
}

/// `GoogleLongrunningOperation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleLongrunningOperation {
    /// done property.
    pub done: Option<bool>,
    /// error property.
    pub error: Option<GoogleRpcStatus>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// response property.
    pub response: Option<serde_json::Value>,
}

/// `GoogleRpcStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleRpcStatus {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `GoogleCloudDialogflowCxV3LlmModelSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3LlmModelSettings {
    /// model property.
    pub model: Option<String>,
    /// promptText property.
    pub prompt_text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3SearchConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SearchConfig {
    /// boostSpecs property.
    pub boost_specs: Option<Vec<GoogleCloudDialogflowCxV3BoostSpecs>>,
    /// filterSpecs property.
    pub filter_specs: Option<Vec<GoogleCloudDialogflowCxV3FilterSpecs>>,
}

/// `GoogleCloudDialogflowCxV3Example` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Example {
    /// actions property.
    pub actions: Option<Vec<GoogleCloudDialogflowCxV3Action>>,
    /// conversationState property.
    pub conversation_state: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// playbookInput property.
    pub playbook_input: Option<GoogleCloudDialogflowCxV3PlaybookInput>,
    /// playbookOutput property.
    pub playbook_output: Option<GoogleCloudDialogflowCxV3PlaybookOutput>,
    /// tokenCount property.
    pub token_count: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ResponseMessagePlayAudio` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResponseMessagePlayAudio {
    /// allowPlaybackInterruption property.
    pub allow_playback_interruption: Option<bool>,
    /// audioUri property.
    pub audio_uri: Option<String>,
}

/// `GoogleCloudDialogflowCxV3TransitionRouteGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TransitionRouteGroup {
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// transitionRoutes property.
    pub transition_routes: Option<Vec<GoogleCloudDialogflowCxV3TransitionRoute>>,
}

/// `GoogleCloudDialogflowCxV3FulfillmentConditionalCasesCaseCaseContent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FulfillmentConditionalCasesCaseCaseContent {
    /// additionalCases property.
    pub additional_cases: Option<Box<GoogleCloudDialogflowCxV3FulfillmentConditionalCases>>,
    /// message property.
    pub message: Option<GoogleCloudDialogflowCxV3ResponseMessage>,
}

/// `GoogleCloudDialogflowCxV3AgentValidationResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AgentValidationResult {
    /// flowValidationResults property.
    pub flow_validation_results: Option<Vec<GoogleCloudDialogflowCxV3FlowValidationResult>>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListTestCaseResultsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListTestCaseResultsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// testCaseResults property.
    pub test_case_results: Option<Vec<GoogleCloudDialogflowCxV3TestCaseResult>>,
}

/// `GoogleCloudDialogflowCxV3ExperimentResultConfidenceInterval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExperimentResultConfidenceInterval {
    /// confidenceLevel property.
    pub confidence_level: Option<f64>,
    /// lowerBound property.
    pub lower_bound: Option<f64>,
    /// ratio property.
    pub ratio: Option<f64>,
    /// upperBound property.
    pub upper_bound: Option<f64>,
}

/// `GoogleCloudDialogflowCxV3ExportAgentRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExportAgentRequest {
    /// agentUri property.
    pub agent_uri: Option<String>,
    /// dataFormat property.
    pub data_format: Option<String>,
    /// environment property.
    pub environment: Option<String>,
    /// gitDestination property.
    pub git_destination: Option<GoogleCloudDialogflowCxV3ExportAgentRequestGitDestination>,
    /// includeBigqueryExportSettings property.
    pub include_bigquery_export_settings: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3WebhookGenericWebServiceOAuthConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3WebhookGenericWebServiceOAuthConfig {
    /// clientId property.
    pub client_id: Option<String>,
    /// clientSecret property.
    pub client_secret: Option<String>,
    /// scopes property.
    pub scopes: Option<Vec<String>>,
    /// secretVersionForClientSecret property.
    pub secret_version_for_client_secret: Option<String>,
    /// tokenEndpoint property.
    pub token_endpoint: Option<String>,
}

/// `GoogleCloudDialogflowCxV3AnswerFeedbackRatingReason` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AnswerFeedbackRatingReason {
    /// feedback property.
    pub feedback: Option<String>,
    /// reasonLabels property.
    pub reason_labels: Option<Vec<String>>,
}

/// `GoogleCloudDialogflowCxV3AgentClientCertificateSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AgentClientCertificateSettings {
    /// passphrase property.
    pub passphrase: Option<String>,
    /// privateKey property.
    pub private_key: Option<String>,
    /// sslCertificate property.
    pub ssl_certificate: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListExperimentsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListExperimentsResponse {
    /// experiments property.
    pub experiments: Option<Vec<GoogleCloudDialogflowCxV3Experiment>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ExportTestCasesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExportTestCasesRequest {
    /// dataFormat property.
    pub data_format: Option<String>,
    /// filter property.
    pub filter: Option<String>,
    /// gcsUri property.
    pub gcs_uri: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListWebhooksResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListWebhooksResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// webhooks property.
    pub webhooks: Option<Vec<GoogleCloudDialogflowCxV3Webhook>>,
}

/// `GoogleCloudDialogflowCxV3Environment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Environment {
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// testCasesConfig property.
    pub test_cases_config: Option<GoogleCloudDialogflowCxV3EnvironmentTestCasesConfig>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// versionConfigs property.
    pub version_configs: Option<Vec<GoogleCloudDialogflowCxV3EnvironmentVersionConfig>>,
    /// webhookConfig property.
    pub webhook_config: Option<GoogleCloudDialogflowCxV3EnvironmentWebhookConfig>,
}

/// `GoogleCloudDialogflowCxV3CompareVersionsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3CompareVersionsRequest {
    /// languageCode property.
    pub language_code: Option<String>,
    /// targetVersion property.
    pub target_version: Option<String>,
}

/// `GoogleCloudDialogflowCxV3InlineSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3InlineSource {
    /// content property.
    pub content: Option<String>,
}

/// `GoogleCloudDialogflowCxV3Page` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Page {
    /// advancedSettings property.
    pub advanced_settings: Option<GoogleCloudDialogflowCxV3AdvancedSettings>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// entryFulfillment property.
    pub entry_fulfillment: Option<GoogleCloudDialogflowCxV3Fulfillment>,
    /// eventHandlers property.
    pub event_handlers: Option<Vec<GoogleCloudDialogflowCxV3EventHandler>>,
    /// form property.
    pub form: Option<GoogleCloudDialogflowCxV3Form>,
    /// knowledgeConnectorSettings property.
    pub knowledge_connector_settings: Option<GoogleCloudDialogflowCxV3KnowledgeConnectorSettings>,
    /// name property.
    pub name: Option<String>,
    /// transitionRouteGroups property.
    pub transition_route_groups: Option<Vec<String>>,
    /// transitionRoutes property.
    pub transition_routes: Option<Vec<GoogleCloudDialogflowCxV3TransitionRoute>>,
}

/// `GoogleCloudDialogflowCxV3ListSecuritySettingsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListSecuritySettingsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// securitySettings property.
    pub security_settings: Option<Vec<GoogleCloudDialogflowCxV3SecuritySettings>>,
}

/// `GoogleCloudDialogflowCxV3ResponseMessageKnowledgeInfoCard` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResponseMessageKnowledgeInfoCard {}

/// `GoogleCloudDialogflowCxV3EntityTypeEntity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3EntityTypeEntity {
    /// synonyms property.
    pub synonyms: Option<Vec<String>>,
    /// value property.
    pub value: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ResponseMessageText` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResponseMessageText {
    /// allowPlaybackInterruption property.
    pub allow_playback_interruption: Option<bool>,
    /// text property.
    pub text: Option<Vec<String>>,
}

/// `GoogleCloudDialogflowCxV3AdvancedSettingsDtmfSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AdvancedSettingsDtmfSettings {
    /// enabled property.
    pub enabled: Option<bool>,
    /// endpointingTimeoutDuration property.
    pub endpointing_timeout_duration: Option<String>,
    /// finishDigit property.
    pub finish_digit: Option<String>,
    /// interdigitTimeoutDuration property.
    pub interdigit_timeout_duration: Option<String>,
    /// maxDigits property.
    pub max_digits: Option<i64>,
}

/// `GoogleCloudDialogflowCxV3ConversationTurnUserInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ConversationTurnUserInput {
    /// enableSentimentAnalysis property.
    pub enable_sentiment_analysis: Option<bool>,
    /// injectedParameters property.
    pub injected_parameters: Option<serde_json::Value>,
    /// input property.
    pub input: Option<GoogleCloudDialogflowCxV3QueryInput>,
    /// isWebhookEnabled property.
    pub is_webhook_enabled: Option<bool>,
}

/// `GoogleLongrunningListOperationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleLongrunningListOperationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<GoogleLongrunningOperation>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `GoogleCloudDialogflowCxV3SafetySettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SafetySettings {
    /// bannedPhrases property.
    pub banned_phrases: Option<Vec<GoogleCloudDialogflowCxV3SafetySettingsPhrase>>,
    /// defaultBannedPhraseMatchStrategy property.
    pub default_banned_phrase_match_strategy: Option<String>,
    /// defaultRaiSettings property.
    pub default_rai_settings: Option<GoogleCloudDialogflowCxV3SafetySettingsRaiSettings>,
    /// promptSecuritySettings property.
    pub prompt_security_settings:
        Option<GoogleCloudDialogflowCxV3SafetySettingsPromptSecuritySettings>,
    /// raiSettings property.
    pub rai_settings: Option<GoogleCloudDialogflowCxV3SafetySettingsRaiSettings>,
}

/// `GoogleCloudDialogflowCxV3ExperimentDefinition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExperimentDefinition {
    /// condition property.
    pub condition: Option<String>,
    /// versionVariants property.
    pub version_variants: Option<GoogleCloudDialogflowCxV3VersionVariants>,
}

/// `GoogleCloudLocationListLocationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudLocationListLocationsResponse {
    /// locations property.
    pub locations: Option<Vec<GoogleCloudLocationLocation>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3InputAudioConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3InputAudioConfig {
    /// audioEncoding property.
    pub audio_encoding: Option<String>,
    /// bargeInConfig property.
    pub barge_in_config: Option<GoogleCloudDialogflowCxV3BargeInConfig>,
    /// enableWordInfo property.
    pub enable_word_info: Option<bool>,
    /// model property.
    pub model: Option<String>,
    /// modelVariant property.
    pub model_variant: Option<String>,
    /// optOutConformerModelMigration property.
    pub opt_out_conformer_model_migration: Option<bool>,
    /// phraseHints property.
    pub phrase_hints: Option<Vec<String>>,
    /// sampleRateHertz property.
    pub sample_rate_hertz: Option<i64>,
    /// singleUtterance property.
    pub single_utterance: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3ImportFlowRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ImportFlowRequest {
    /// flowContent property.
    pub flow_content: Option<String>,
    /// flowImportStrategy property.
    pub flow_import_strategy: Option<GoogleCloudDialogflowCxV3FlowImportStrategy>,
    /// flowUri property.
    pub flow_uri: Option<String>,
    /// importOption property.
    pub import_option: Option<String>,
}

/// `GoogleCloudLocationLocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudLocationLocation {
    /// displayName property.
    pub display_name: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// locationId property.
    pub location_id: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListExamplesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListExamplesResponse {
    /// examples property.
    pub examples: Option<Vec<GoogleCloudDialogflowCxV3Example>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3WebhookServiceDirectoryConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3WebhookServiceDirectoryConfig {
    /// genericWebService property.
    pub generic_web_service: Option<GoogleCloudDialogflowCxV3WebhookGenericWebService>,
    /// service property.
    pub service: Option<String>,
}

/// `GoogleCloudDialogflowCxV3DataStoreConnectionSignalsGroundingSignals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DataStoreConnectionSignalsGroundingSignals {
    /// decision property.
    pub decision: Option<String>,
    /// score property.
    pub score: Option<String>,
}

/// `GoogleCloudDialogflowCxV3RolloutConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3RolloutConfig {
    /// failureCondition property.
    pub failure_condition: Option<String>,
    /// rolloutCondition property.
    pub rollout_condition: Option<String>,
    /// rolloutSteps property.
    pub rollout_steps: Option<Vec<GoogleCloudDialogflowCxV3RolloutConfigRolloutStep>>,
}

/// `GoogleCloudDialogflowCxV3SessionEntityType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SessionEntityType {
    /// entities property.
    pub entities: Option<Vec<GoogleCloudDialogflowCxV3EntityTypeEntity>>,
    /// entityOverrideMode property.
    pub entity_override_mode: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleTypeLatLng` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeLatLng {
    /// latitude property.
    pub latitude: Option<f64>,
    /// longitude property.
    pub longitude: Option<f64>,
}

/// `GoogleCloudDialogflowCxV3ListPlaybooksResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListPlaybooksResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// playbooks property.
    pub playbooks: Option<Vec<GoogleCloudDialogflowCxV3Playbook>>,
}

/// `GoogleCloudDialogflowCxV3TransitionRouteGroupCoverage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TransitionRouteGroupCoverage {
    /// coverageScore property.
    pub coverage_score: Option<f64>,
    /// coverages property.
    pub coverages: Option<Vec<GoogleCloudDialogflowCxV3TransitionRouteGroupCoverageCoverage>>,
}

/// `GoogleCloudDialogflowCxV3ExperimentResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExperimentResult {
    /// lastUpdateTime property.
    pub last_update_time: Option<String>,
    /// versionMetrics property.
    pub version_metrics: Option<Vec<GoogleCloudDialogflowCxV3ExperimentResultVersionMetrics>>,
}

/// `GoogleCloudDialogflowCxV3CalculateCoverageResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3CalculateCoverageResponse {
    /// agent property.
    pub agent: Option<String>,
    /// intentCoverage property.
    pub intent_coverage: Option<GoogleCloudDialogflowCxV3IntentCoverage>,
    /// routeGroupCoverage property.
    pub route_group_coverage: Option<GoogleCloudDialogflowCxV3TransitionRouteGroupCoverage>,
    /// transitionCoverage property.
    pub transition_coverage: Option<GoogleCloudDialogflowCxV3TransitionCoverage>,
}

/// `GoogleCloudDialogflowCxV3ExperimentResultMetric` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExperimentResultMetric {
    /// confidenceInterval property.
    pub confidence_interval: Option<GoogleCloudDialogflowCxV3ExperimentResultConfidenceInterval>,
    /// count property.
    pub count: Option<f64>,
    /// countType property.
    pub count_type: Option<String>,
    /// ratio property.
    pub ratio: Option<f64>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleCloudDialogflowCxV3DetectIntentResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DetectIntentResponse {
    /// allowCancellation property.
    pub allow_cancellation: Option<bool>,
    /// outputAudio property.
    pub output_audio: Option<String>,
    /// outputAudioConfig property.
    pub output_audio_config: Option<GoogleCloudDialogflowCxV3OutputAudioConfig>,
    /// queryResult property.
    pub query_result: Option<GoogleCloudDialogflowCxV3QueryResult>,
    /// responseId property.
    pub response_id: Option<String>,
    /// responseType property.
    pub response_type: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListEnvironmentsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListEnvironmentsResponse {
    /// environments property.
    pub environments: Option<Vec<GoogleCloudDialogflowCxV3Environment>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3Phrase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Phrase {
    /// text property.
    pub text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3DataStoreConnection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DataStoreConnection {
    /// dataStore property.
    pub data_store: Option<String>,
    /// dataStoreType property.
    pub data_store_type: Option<String>,
    /// documentProcessingMode property.
    pub document_processing_mode: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ToolTLSConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolTLSConfig {
    /// caCerts property.
    pub ca_certs: Option<Vec<GoogleCloudDialogflowCxV3ToolTLSConfigCACert>>,
}

/// `GoogleCloudDialogflowCxV3ToolCallResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolCallResult {
    /// action property.
    pub action: Option<String>,
    /// error property.
    pub error: Option<GoogleCloudDialogflowCxV3ToolCallResultError>,
    /// outputParameters property.
    pub output_parameters: Option<serde_json::Value>,
    /// tool property.
    pub tool: Option<String>,
}

/// `GoogleCloudDialogflowCxV3QueryResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3QueryResult {
    /// advancedSettings property.
    pub advanced_settings: Option<GoogleCloudDialogflowCxV3AdvancedSettings>,
    /// allowAnswerFeedback property.
    pub allow_answer_feedback: Option<bool>,
    /// currentFlow property.
    pub current_flow: Option<GoogleCloudDialogflowCxV3Flow>,
    /// currentPage property.
    pub current_page: Option<GoogleCloudDialogflowCxV3Page>,
    /// dataStoreConnectionSignals property.
    pub data_store_connection_signals: Option<GoogleCloudDialogflowCxV3DataStoreConnectionSignals>,
    /// diagnosticInfo property.
    pub diagnostic_info: Option<serde_json::Value>,
    /// dtmf property.
    pub dtmf: Option<GoogleCloudDialogflowCxV3DtmfInput>,
    /// intent property.
    pub intent: Option<GoogleCloudDialogflowCxV3Intent>,
    /// intentDetectionConfidence property.
    pub intent_detection_confidence: Option<f64>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// match property.
    pub r#match: Option<GoogleCloudDialogflowCxV3Match>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
    /// responseMessages property.
    pub response_messages: Option<Vec<GoogleCloudDialogflowCxV3ResponseMessage>>,
    /// sentimentAnalysisResult property.
    pub sentiment_analysis_result: Option<GoogleCloudDialogflowCxV3SentimentAnalysisResult>,
    /// text property.
    pub text: Option<String>,
    /// traceBlocks property.
    pub trace_blocks: Option<Vec<GoogleCloudDialogflowCxV3TraceBlock>>,
    /// transcript property.
    pub transcript: Option<String>,
    /// triggerEvent property.
    pub trigger_event: Option<String>,
    /// triggerIntent property.
    pub trigger_intent: Option<String>,
    /// webhookPayloads property.
    pub webhook_payloads: Option<Vec<serde_json::Value>>,
    /// webhookStatuses property.
    pub webhook_statuses: Option<Vec<GoogleRpcStatus>>,
}

/// `GoogleCloudDialogflowCxV3DataStoreConnectionSignalsSearchSnippet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DataStoreConnectionSignalsSearchSnippet {
    /// documentTitle property.
    pub document_title: Option<String>,
    /// documentUri property.
    pub document_uri: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// text property.
    pub text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3SynthesizeSpeechConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SynthesizeSpeechConfig {
    /// effectsProfileId property.
    pub effects_profile_id: Option<Vec<String>>,
    /// pitch property.
    pub pitch: Option<f64>,
    /// speakingRate property.
    pub speaking_rate: Option<f64>,
    /// voice property.
    pub voice: Option<GoogleCloudDialogflowCxV3VoiceSelectionParams>,
    /// volumeGainDb property.
    pub volume_gain_db: Option<f64>,
}

/// `GoogleCloudDialogflowCxV3ExportIntentsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExportIntentsRequest {
    /// dataFormat property.
    pub data_format: Option<String>,
    /// intents property.
    pub intents: Option<Vec<String>>,
    /// intentsContentInline property.
    pub intents_content_inline: Option<bool>,
    /// intentsUri property.
    pub intents_uri: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ImportEntityTypesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ImportEntityTypesRequest {
    /// entityTypesContent property.
    pub entity_types_content: Option<GoogleCloudDialogflowCxV3InlineSource>,
    /// entityTypesUri property.
    pub entity_types_uri: Option<String>,
    /// mergeOption property.
    pub merge_option: Option<String>,
    /// targetEntityType property.
    pub target_entity_type: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ToolOpenApiTool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolOpenApiTool {
    /// authentication property.
    pub authentication: Option<GoogleCloudDialogflowCxV3ToolAuthentication>,
    /// serviceDirectoryConfig property.
    pub service_directory_config: Option<GoogleCloudDialogflowCxV3ToolServiceDirectoryConfig>,
    /// textSchema property.
    pub text_schema: Option<String>,
    /// tlsConfig property.
    pub tls_config: Option<GoogleCloudDialogflowCxV3ToolTLSConfig>,
}

/// `GoogleCloudDialogflowCxV3EntityType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3EntityType {
    /// autoExpansionMode property.
    pub auto_expansion_mode: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// enableFuzzyExtraction property.
    pub enable_fuzzy_extraction: Option<bool>,
    /// entities property.
    pub entities: Option<Vec<GoogleCloudDialogflowCxV3EntityTypeEntity>>,
    /// excludedPhrases property.
    pub excluded_phrases: Option<Vec<GoogleCloudDialogflowCxV3EntityTypeExcludedPhrase>>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// redact property.
    pub redact: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3ListToolsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListToolsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// tools property.
    pub tools: Option<Vec<GoogleCloudDialogflowCxV3Tool>>,
}

/// `GoogleCloudDialogflowCxV3IntentCoverageIntent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3IntentCoverageIntent {
    /// covered property.
    pub covered: Option<bool>,
    /// intent property.
    pub intent: Option<String>,
}

/// `GoogleCloudDialogflowCxV3PlaybookInstruction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3PlaybookInstruction {
    /// guidelines property.
    pub guidelines: Option<String>,
    /// steps property.
    pub steps: Option<Vec<Box<GoogleCloudDialogflowCxV3PlaybookStep>>>,
}

/// `GoogleCloudDialogflowCxV3MatchIntentRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3MatchIntentRequest {
    /// persistParameterChanges property.
    pub persist_parameter_changes: Option<bool>,
    /// queryInput property.
    pub query_input: Option<GoogleCloudDialogflowCxV3QueryInput>,
    /// queryParams property.
    pub query_params: Option<GoogleCloudDialogflowCxV3QueryParameters>,
}

/// `GoogleCloudDialogflowCxV3ToolUse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolUse {
    /// action property.
    pub action: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// inputActionParameters property.
    pub input_action_parameters: Option<serde_json::Value>,
    /// outputActionParameters property.
    pub output_action_parameters: Option<serde_json::Value>,
    /// tool property.
    pub tool: Option<String>,
}

/// `GoogleCloudDialogflowCxV3AgentUtterance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AgentUtterance {
    /// text property.
    pub text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ExportFlowRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExportFlowRequest {
    /// flowUri property.
    pub flow_uri: Option<String>,
    /// includeReferencedFlows property.
    pub include_referenced_flows: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3HandlerLifecycleHandler` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3HandlerLifecycleHandler {
    /// condition property.
    pub condition: Option<String>,
    /// fulfillment property.
    pub fulfillment: Option<GoogleCloudDialogflowCxV3Fulfillment>,
    /// lifecycleStage property.
    pub lifecycle_stage: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse {
    /// continuousTestResults property.
    pub continuous_test_results: Option<Vec<GoogleCloudDialogflowCxV3ContinuousTestResult>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ResponseMessageLiveAgentHandoff` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResponseMessageLiveAgentHandoff {
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
}

/// `GoogleCloudDialogflowCxV3TestConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TestConfig {
    /// flow property.
    pub flow: Option<String>,
    /// page property.
    pub page: Option<String>,
    /// trackingParameters property.
    pub tracking_parameters: Option<Vec<String>>,
}

/// `GoogleCloudDialogflowCxV3ListFlowsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListFlowsResponse {
    /// flows property.
    pub flows: Option<Vec<GoogleCloudDialogflowCxV3Flow>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ResponseMessage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResponseMessage {
    /// channel property.
    pub channel: Option<String>,
    /// conversationSuccess property.
    pub conversation_success: Option<GoogleCloudDialogflowCxV3ResponseMessageConversationSuccess>,
    /// endInteraction property.
    pub end_interaction: Option<GoogleCloudDialogflowCxV3ResponseMessageEndInteraction>,
    /// knowledgeInfoCard property.
    pub knowledge_info_card: Option<GoogleCloudDialogflowCxV3ResponseMessageKnowledgeInfoCard>,
    /// liveAgentHandoff property.
    pub live_agent_handoff: Option<GoogleCloudDialogflowCxV3ResponseMessageLiveAgentHandoff>,
    /// mixedAudio property.
    pub mixed_audio: Option<GoogleCloudDialogflowCxV3ResponseMessageMixedAudio>,
    /// outputAudioText property.
    pub output_audio_text: Option<GoogleCloudDialogflowCxV3ResponseMessageOutputAudioText>,
    /// payload property.
    pub payload: Option<serde_json::Value>,
    /// playAudio property.
    pub play_audio: Option<GoogleCloudDialogflowCxV3ResponseMessagePlayAudio>,
    /// responseType property.
    pub response_type: Option<String>,
    /// telephonyTransferCall property.
    pub telephony_transfer_call:
        Option<GoogleCloudDialogflowCxV3ResponseMessageTelephonyTransferCall>,
    /// text property.
    pub text: Option<GoogleCloudDialogflowCxV3ResponseMessageText>,
    /// toolCall property.
    pub tool_call: Option<GoogleCloudDialogflowCxV3ToolCall>,
}

/// `GoogleCloudDialogflowCxV3TraceBlock` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TraceBlock {
    /// actions property.
    pub actions: Option<Vec<GoogleCloudDialogflowCxV3Action>>,
    /// completeTime property.
    pub complete_time: Option<String>,
    /// endState property.
    pub end_state: Option<String>,
    /// flowTraceMetadata property.
    pub flow_trace_metadata: Option<GoogleCloudDialogflowCxV3FlowTraceMetadata>,
    /// inputParameters property.
    pub input_parameters: Option<serde_json::Value>,
    /// outputParameters property.
    pub output_parameters: Option<serde_json::Value>,
    /// playbookTraceMetadata property.
    pub playbook_trace_metadata: Option<GoogleCloudDialogflowCxV3PlaybookTraceMetadata>,
    /// speechProcessingMetadata property.
    pub speech_processing_metadata: Option<GoogleCloudDialogflowCxV3SpeechProcessingMetadata>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `GoogleCloudDialogflowCxV3FulfillIntentRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FulfillIntentRequest {
    /// match property.
    pub r#match: Option<GoogleCloudDialogflowCxV3Match>,
    /// matchIntentRequest property.
    pub match_intent_request: Option<GoogleCloudDialogflowCxV3MatchIntentRequest>,
    /// outputAudioConfig property.
    pub output_audio_config: Option<GoogleCloudDialogflowCxV3OutputAudioConfig>,
}

/// `GoogleCloudDialogflowCxV3AgentPersonalizationSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AgentPersonalizationSettings {
    /// defaultEndUserMetadata property.
    pub default_end_user_metadata: Option<serde_json::Value>,
}

/// `GoogleCloudDialogflowCxV3Deployment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Deployment {
    /// endTime property.
    pub end_time: Option<String>,
    /// flowVersion property.
    pub flow_version: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// result property.
    pub result: Option<GoogleCloudDialogflowCxV3DeploymentResult>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `GoogleCloudDialogflowCxV3KnowledgeConnectorSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3KnowledgeConnectorSettings {
    /// dataStoreConnections property.
    pub data_store_connections: Option<Vec<GoogleCloudDialogflowCxV3DataStoreConnection>>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// targetFlow property.
    pub target_flow: Option<String>,
    /// targetPage property.
    pub target_page: Option<String>,
    /// triggerFulfillment property.
    pub trigger_fulfillment: Option<GoogleCloudDialogflowCxV3Fulfillment>,
}

/// `GoogleCloudDialogflowCxV3ValidateAgentRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ValidateAgentRequest {
    /// languageCode property.
    pub language_code: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ResponseMessageMixedAudioSegment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResponseMessageMixedAudioSegment {
    /// allowPlaybackInterruption property.
    pub allow_playback_interruption: Option<bool>,
    /// audio property.
    pub audio: Option<String>,
    /// uri property.
    pub uri: Option<String>,
}

/// `GoogleCloudDialogflowCxV3SafetySettingsPhrase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SafetySettingsPhrase {
    /// languageCode property.
    pub language_code: Option<String>,
    /// text property.
    pub text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3AudioInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AudioInput {
    /// audio property.
    pub audio: Option<String>,
    /// config property.
    pub config: Option<GoogleCloudDialogflowCxV3InputAudioConfig>,
}

/// `GoogleCloudDialogflowCxV3RolloutConfigRolloutStep` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3RolloutConfigRolloutStep {
    /// displayName property.
    pub display_name: Option<String>,
    /// minDuration property.
    pub min_duration: Option<String>,
    /// trafficPercent property.
    pub traffic_percent: Option<i64>,
}

/// `GoogleCloudDialogflowCxV3StartExperimentRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3StartExperimentRequest {}

/// `GoogleCloudDialogflowCxV3IntentTrainingPhrase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3IntentTrainingPhrase {
    /// id property.
    pub id: Option<String>,
    /// parts property.
    pub parts: Option<Vec<GoogleCloudDialogflowCxV3IntentTrainingPhrasePart>>,
    /// repeatCount property.
    pub repeat_count: Option<i64>,
}

/// `GoogleCloudDialogflowCxV3AnswerFeedback` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AnswerFeedback {
    /// customRating property.
    pub custom_rating: Option<String>,
    /// rating property.
    pub rating: Option<String>,
    /// ratingReason property.
    pub rating_reason: Option<GoogleCloudDialogflowCxV3AnswerFeedbackRatingReason>,
}

/// `GoogleCloudDialogflowCxV3Playbook` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Playbook {
    /// codeBlock property.
    pub code_block: Option<GoogleCloudDialogflowCxV3CodeBlock>,
    /// createTime property.
    pub create_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// goal property.
    pub goal: Option<String>,
    /// handlers property.
    pub handlers: Option<Vec<GoogleCloudDialogflowCxV3Handler>>,
    /// inlineActions property.
    pub inline_actions: Option<Vec<String>>,
    /// inputParameterDefinitions property.
    pub input_parameter_definitions: Option<Vec<GoogleCloudDialogflowCxV3ParameterDefinition>>,
    /// instruction property.
    pub instruction: Option<GoogleCloudDialogflowCxV3PlaybookInstruction>,
    /// llmModelSettings property.
    pub llm_model_settings: Option<GoogleCloudDialogflowCxV3LlmModelSettings>,
    /// name property.
    pub name: Option<String>,
    /// outputParameterDefinitions property.
    pub output_parameter_definitions: Option<Vec<GoogleCloudDialogflowCxV3ParameterDefinition>>,
    /// playbookType property.
    pub playbook_type: Option<String>,
    /// referencedFlows property.
    pub referenced_flows: Option<Vec<String>>,
    /// referencedPlaybooks property.
    pub referenced_playbooks: Option<Vec<String>>,
    /// referencedTools property.
    pub referenced_tools: Option<Vec<String>>,
    /// tokenCount property.
    pub token_count: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `GoogleCloudDialogflowCxV3FulfillmentGeneratorSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FulfillmentGeneratorSettings {
    /// generator property.
    pub generator: Option<String>,
    /// inputParameters property.
    pub input_parameters: Option<serde_json::Value>,
    /// outputParameter property.
    pub output_parameter: Option<String>,
}

/// `GoogleCloudDialogflowCxV3TransitionRouteGroupCoverageCoverageTransition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TransitionRouteGroupCoverageCoverageTransition {
    /// covered property.
    pub covered: Option<bool>,
    /// transitionRoute property.
    pub transition_route: Option<GoogleCloudDialogflowCxV3TransitionRoute>,
}

/// `GoogleCloudDialogflowCxV3BoostSpecs` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3BoostSpecs {
    /// dataStores property.
    pub data_stores: Option<Vec<String>>,
    /// spec property.
    pub spec: Option<Vec<GoogleCloudDialogflowCxV3BoostSpec>>,
}

/// `GoogleCloudDialogflowCxV3BatchRunTestCasesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3BatchRunTestCasesRequest {
    /// environment property.
    pub environment: Option<String>,
    /// testCases property.
    pub test_cases: Option<Vec<String>>,
}

/// `GoogleCloudDialogflowCxV3EventInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3EventInput {
    /// event property.
    pub event: Option<String>,
}

/// `GoogleCloudDialogflowCxV3EventHandler` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3EventHandler {
    /// event property.
    pub event: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// targetFlow property.
    pub target_flow: Option<String>,
    /// targetPage property.
    pub target_page: Option<String>,
    /// targetPlaybook property.
    pub target_playbook: Option<String>,
    /// triggerFulfillment property.
    pub trigger_fulfillment: Option<GoogleCloudDialogflowCxV3Fulfillment>,
}

/// `GoogleCloudDialogflowCxV3ListDeploymentsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListDeploymentsResponse {
    /// deployments property.
    pub deployments: Option<Vec<GoogleCloudDialogflowCxV3Deployment>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3AdvancedSettingsLoggingSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AdvancedSettingsLoggingSettings {
    /// enableConsentBasedRedaction property.
    pub enable_consent_based_redaction: Option<bool>,
    /// enableInteractionLogging property.
    pub enable_interaction_logging: Option<bool>,
    /// enableStackdriverLogging property.
    pub enable_stackdriver_logging: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3DtmfInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DtmfInput {
    /// digits property.
    pub digits: Option<String>,
    /// finishDigit property.
    pub finish_digit: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ToolServiceDirectoryConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolServiceDirectoryConfig {
    /// service property.
    pub service: Option<String>,
}

/// `GoogleCloudDialogflowCxV3DataStoreConnectionSignalsRewriterModelCallSignals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DataStoreConnectionSignalsRewriterModelCallSignals {
    /// model property.
    pub model: Option<String>,
    /// modelOutput property.
    pub model_output: Option<String>,
    /// renderedPrompt property.
    pub rendered_prompt: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListGeneratorsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListGeneratorsResponse {
    /// generators property.
    pub generators: Option<Vec<GoogleCloudDialogflowCxV3Generator>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListChangelogsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListChangelogsResponse {
    /// changelogs property.
    pub changelogs: Option<Vec<GoogleCloudDialogflowCxV3Changelog>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3SecuritySettingsAudioExportSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SecuritySettingsAudioExportSettings {
    /// audioExportPattern property.
    pub audio_export_pattern: Option<String>,
    /// audioFormat property.
    pub audio_format: Option<String>,
    /// enableAudioRedaction property.
    pub enable_audio_redaction: Option<bool>,
    /// gcsBucket property.
    pub gcs_bucket: Option<String>,
    /// storeTtsAudio property.
    pub store_tts_audio: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3VersionVariants` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3VersionVariants {
    /// variants property.
    pub variants: Option<Vec<GoogleCloudDialogflowCxV3VersionVariantsVariant>>,
}

/// `GoogleCloudDialogflowCxV3RestoreAgentRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3RestoreAgentRequest {
    /// agentContent property.
    pub agent_content: Option<String>,
    /// agentUri property.
    pub agent_uri: Option<String>,
    /// gitSource property.
    pub git_source: Option<GoogleCloudDialogflowCxV3RestoreAgentRequestGitSource>,
    /// restoreOption property.
    pub restore_option: Option<String>,
}

/// `GoogleCloudDialogflowCxV3TestCaseResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TestCaseResult {
    /// conversationTurns property.
    pub conversation_turns: Option<Vec<GoogleCloudDialogflowCxV3ConversationTurn>>,
    /// environment property.
    pub environment: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// testResult property.
    pub test_result: Option<String>,
    /// testTime property.
    pub test_time: Option<String>,
}

/// `GoogleCloudDialogflowCxV3RestoreToolVersionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3RestoreToolVersionRequest {}

/// `GoogleCloudDialogflowCxV3NluSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3NluSettings {
    /// classificationThreshold property.
    pub classification_threshold: Option<f64>,
    /// modelTrainingMode property.
    pub model_training_mode: Option<String>,
    /// modelType property.
    pub model_type: Option<String>,
}

/// `GoogleCloudDialogflowCxV3PlaybookVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3PlaybookVersion {
    /// description property.
    pub description: Option<String>,
    /// examples property.
    pub examples: Option<Vec<GoogleCloudDialogflowCxV3Example>>,
    /// name property.
    pub name: Option<String>,
    /// playbook property.
    pub playbook: Option<GoogleCloudDialogflowCxV3Playbook>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `GoogleCloudDialogflowCxV3Form` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Form {
    /// parameters property.
    pub parameters: Option<Vec<GoogleCloudDialogflowCxV3FormParameter>>,
}

/// `GoogleCloudDialogflowCxV3ResponseMessageTelephonyTransferCall` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResponseMessageTelephonyTransferCall {
    /// phoneNumber property.
    pub phone_number: Option<String>,
}

/// `GoogleCloudDialogflowCxV3Handler` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Handler {
    /// eventHandler property.
    pub event_handler: Option<GoogleCloudDialogflowCxV3HandlerEventHandler>,
    /// lifecycleHandler property.
    pub lifecycle_handler: Option<GoogleCloudDialogflowCxV3HandlerLifecycleHandler>,
}

/// `GoogleCloudDialogflowCxV3RunContinuousTestRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3RunContinuousTestRequest {}

/// `GoogleCloudDialogflowCxV3BargeInConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3BargeInConfig {
    /// noBargeInDuration property.
    pub no_barge_in_duration: Option<String>,
    /// totalDuration property.
    pub total_duration: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ToolDataStoreTool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolDataStoreTool {
    /// dataStoreConnections property.
    pub data_store_connections: Option<Vec<GoogleCloudDialogflowCxV3DataStoreConnection>>,
    /// fallbackPrompt property.
    pub fallback_prompt: Option<GoogleCloudDialogflowCxV3ToolDataStoreToolFallbackPrompt>,
}

/// `GoogleCloudDialogflowCxV3PlaybookStep` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3PlaybookStep {
    /// steps property.
    pub steps: Option<Vec<Box<GoogleCloudDialogflowCxV3PlaybookStep>>>,
    /// text property.
    pub text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3BoostSpecConditionBoostSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3BoostSpecConditionBoostSpec {
    /// boost property.
    pub boost: Option<f64>,
    /// boostControlSpec property.
    pub boost_control_spec:
        Option<GoogleCloudDialogflowCxV3BoostSpecConditionBoostSpecBoostControlSpec>,
    /// condition property.
    pub condition: Option<String>,
}

/// `GoogleCloudDialogflowCxV3SentimentAnalysisResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SentimentAnalysisResult {
    /// magnitude property.
    pub magnitude: Option<f64>,
    /// score property.
    pub score: Option<f64>,
}

/// `GoogleCloudDialogflowCxV3RestoreToolVersionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3RestoreToolVersionResponse {
    /// tool property.
    pub tool: Option<GoogleCloudDialogflowCxV3Tool>,
}

/// `GoogleCloudDialogflowCxV3DataStoreConnectionSignalsSafetySignals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DataStoreConnectionSignalsSafetySignals {
    /// bannedPhraseMatch property.
    pub banned_phrase_match: Option<String>,
    /// decision property.
    pub decision: Option<String>,
    /// matchedBannedPhrase property.
    pub matched_banned_phrase: Option<String>,
}

/// `GoogleCloudDialogflowCxV3FlowValidationResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FlowValidationResult {
    /// name property.
    pub name: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// validationMessages property.
    pub validation_messages: Option<Vec<GoogleCloudDialogflowCxV3ValidationMessage>>,
}

/// `GoogleCloudDialogflowCxV3HandlerEventHandler` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3HandlerEventHandler {
    /// condition property.
    pub condition: Option<String>,
    /// event property.
    pub event: Option<String>,
    /// fulfillment property.
    pub fulfillment: Option<GoogleCloudDialogflowCxV3Fulfillment>,
}

/// `GoogleCloudDialogflowCxV3EnvironmentWebhookConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3EnvironmentWebhookConfig {
    /// webhookOverrides property.
    pub webhook_overrides: Option<Vec<GoogleCloudDialogflowCxV3Webhook>>,
}

/// `GoogleCloudDialogflowCxV3VariantsHistory` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3VariantsHistory {
    /// updateTime property.
    pub update_time: Option<String>,
    /// versionVariants property.
    pub version_variants: Option<GoogleCloudDialogflowCxV3VersionVariants>,
}

/// `GoogleCloudDialogflowCxV3GenerativeSettingsFallbackSettingsPromptTemplate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3GenerativeSettingsFallbackSettingsPromptTemplate {
    /// displayName property.
    pub display_name: Option<String>,
    /// frozen property.
    pub frozen: Option<bool>,
    /// promptText property.
    pub prompt_text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3GenerativeSettingsKnowledgeConnectorSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3GenerativeSettingsKnowledgeConnectorSettings {
    /// agent property.
    pub agent: Option<String>,
    /// agentIdentity property.
    pub agent_identity: Option<String>,
    /// agentScope property.
    pub agent_scope: Option<String>,
    /// business property.
    pub business: Option<String>,
    /// businessDescription property.
    pub business_description: Option<String>,
    /// disableDataStoreFallback property.
    pub disable_data_store_fallback: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3ExperimentResultVersionMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExperimentResultVersionMetrics {
    /// metrics property.
    pub metrics: Option<Vec<GoogleCloudDialogflowCxV3ExperimentResultMetric>>,
    /// sessionCount property.
    pub session_count: Option<i64>,
    /// version property.
    pub version: Option<String>,
}

/// `GoogleCloudDialogflowCxV3SecuritySettingsInsightsExportSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SecuritySettingsInsightsExportSettings {
    /// enableInsightsExport property.
    pub enable_insights_export: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3ListEntityTypesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListEntityTypesResponse {
    /// entityTypes property.
    pub entity_types: Option<Vec<GoogleCloudDialogflowCxV3EntityType>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleCloudDialogflowCxV3BatchDeleteTestCasesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3BatchDeleteTestCasesRequest {
    /// names property.
    pub names: Option<Vec<String>>,
}

/// `GoogleCloudDialogflowCxV3TypeSchema` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TypeSchema {
    /// inlineSchema property.
    pub inline_schema: Option<Box<GoogleCloudDialogflowCxV3InlineSchema>>,
    /// schemaReference property.
    pub schema_reference: Option<GoogleCloudDialogflowCxV3TypeSchemaSchemaReference>,
}

/// `GoogleCloudDialogflowCxV3FlowInvocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FlowInvocation {
    /// displayName property.
    pub display_name: Option<String>,
    /// flow property.
    pub flow: Option<String>,
    /// flowState property.
    pub flow_state: Option<String>,
}

/// `GoogleCloudDialogflowCxV3TransitionRoute` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TransitionRoute {
    /// condition property.
    pub condition: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// intent property.
    pub intent: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// targetFlow property.
    pub target_flow: Option<String>,
    /// targetPage property.
    pub target_page: Option<String>,
    /// triggerFulfillment property.
    pub trigger_fulfillment: Option<GoogleCloudDialogflowCxV3Fulfillment>,
}

/// `GoogleCloudDialogflowCxV3DataStoreConnectionSignalsAnswerPart` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DataStoreConnectionSignalsAnswerPart {
    /// supportingIndices property.
    pub supporting_indices: Option<Vec<i64>>,
    /// text property.
    pub text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3TypeSchemaSchemaReference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TypeSchemaSchemaReference {
    /// schema property.
    pub schema: Option<String>,
    /// tool property.
    pub tool: Option<String>,
}

/// `GoogleCloudDialogflowCxV3UserUtterance` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3UserUtterance {
    /// text property.
    pub text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3FlowTraceMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FlowTraceMetadata {
    /// displayName property.
    pub display_name: Option<String>,
    /// flow property.
    pub flow: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ToolFunctionTool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolFunctionTool {
    /// inputSchema property.
    pub input_schema: Option<serde_json::Value>,
    /// outputSchema property.
    pub output_schema: Option<serde_json::Value>,
}

/// `GoogleCloudDialogflowCxV3DeploymentResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DeploymentResult {
    /// deploymentTestResults property.
    pub deployment_test_results: Option<Vec<String>>,
    /// experiment property.
    pub experiment: Option<String>,
}

/// `GoogleCloudDialogflowCxV3FulfillmentConditionalCasesCase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FulfillmentConditionalCasesCase {
    /// caseContent property.
    pub case_content:
        Option<Vec<Box<GoogleCloudDialogflowCxV3FulfillmentConditionalCasesCaseCaseContent>>>,
    /// condition property.
    pub condition: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ResourceName` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResourceName {
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleCloudDialogflowCxV3SpeechToTextSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SpeechToTextSettings {
    /// enableSpeechAdaptation property.
    pub enable_speech_adaptation: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3SubmitAnswerFeedbackRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SubmitAnswerFeedbackRequest {
    /// answerFeedback property.
    pub answer_feedback: Option<GoogleCloudDialogflowCxV3AnswerFeedback>,
    /// responseId property.
    pub response_id: Option<String>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `GoogleCloudDialogflowCxV3TestRunDifference` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TestRunDifference {
    /// description property.
    pub description: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleCloudDialogflowCxV3Changelog` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Changelog {
    /// action property.
    pub action: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// resource property.
    pub resource: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// userEmail property.
    pub user_email: Option<String>,
}

/// `GoogleCloudDialogflowCxV3TransitionRouteGroupCoverageCoverage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TransitionRouteGroupCoverageCoverage {
    /// coverageScore property.
    pub coverage_score: Option<f64>,
    /// routeGroup property.
    pub route_group: Option<GoogleCloudDialogflowCxV3TransitionRouteGroup>,
    /// transitions property.
    pub transitions:
        Option<Vec<GoogleCloudDialogflowCxV3TransitionRouteGroupCoverageCoverageTransition>>,
}

/// `GoogleCloudDialogflowCxV3Action` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Action {
    /// agentUtterance property.
    pub agent_utterance: Option<GoogleCloudDialogflowCxV3AgentUtterance>,
    /// flowInvocation property.
    pub flow_invocation: Option<GoogleCloudDialogflowCxV3FlowInvocation>,
    /// flowTransition property.
    pub flow_transition: Option<GoogleCloudDialogflowCxV3FlowTransition>,
    /// playbookInvocation property.
    pub playbook_invocation: Option<GoogleCloudDialogflowCxV3PlaybookInvocation>,
    /// playbookTransition property.
    pub playbook_transition: Option<GoogleCloudDialogflowCxV3PlaybookTransition>,
    /// toolUse property.
    pub tool_use: Option<GoogleCloudDialogflowCxV3ToolUse>,
    /// userUtterance property.
    pub user_utterance: Option<GoogleCloudDialogflowCxV3UserUtterance>,
}

/// `GoogleCloudDialogflowCxV3PlaybookTransition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3PlaybookTransition {
    /// displayName property.
    pub display_name: Option<String>,
    /// playbook property.
    pub playbook: Option<String>,
}

/// `GoogleCloudDialogflowCxV3IntentTrainingPhrasePart` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3IntentTrainingPhrasePart {
    /// parameterId property.
    pub parameter_id: Option<String>,
    /// text property.
    pub text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3TransitionCoverageTransitionNode` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TransitionCoverageTransitionNode {
    /// flow property.
    pub flow: Option<GoogleCloudDialogflowCxV3Flow>,
    /// page property.
    pub page: Option<GoogleCloudDialogflowCxV3Page>,
}

/// `GoogleCloudDialogflowCxV3ToolCallResultError` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolCallResultError {
    /// message property.
    pub message: Option<String>,
}

/// `GoogleCloudDialogflowCxV3TransitionCoverageTransition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TransitionCoverageTransition {
    /// covered property.
    pub covered: Option<bool>,
    /// eventHandler property.
    pub event_handler: Option<GoogleCloudDialogflowCxV3EventHandler>,
    /// index property.
    pub index: Option<i64>,
    /// source property.
    pub source: Option<GoogleCloudDialogflowCxV3TransitionCoverageTransitionNode>,
    /// target property.
    pub target: Option<GoogleCloudDialogflowCxV3TransitionCoverageTransitionNode>,
    /// transitionRoute property.
    pub transition_route: Option<GoogleCloudDialogflowCxV3TransitionRoute>,
}

/// `GoogleCloudDialogflowCxV3SafetySettingsPromptSecuritySettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SafetySettingsPromptSecuritySettings {
    /// enablePromptSecurity property.
    pub enable_prompt_security: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3Experiment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Experiment {
    /// createTime property.
    pub create_time: Option<String>,
    /// definition property.
    pub definition: Option<GoogleCloudDialogflowCxV3ExperimentDefinition>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// experimentLength property.
    pub experiment_length: Option<String>,
    /// lastUpdateTime property.
    pub last_update_time: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// result property.
    pub result: Option<GoogleCloudDialogflowCxV3ExperimentResult>,
    /// rolloutConfig property.
    pub rollout_config: Option<GoogleCloudDialogflowCxV3RolloutConfig>,
    /// rolloutFailureReason property.
    pub rollout_failure_reason: Option<String>,
    /// rolloutState property.
    pub rollout_state: Option<GoogleCloudDialogflowCxV3RolloutState>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// variantsHistory property.
    pub variants_history: Option<Vec<GoogleCloudDialogflowCxV3VariantsHistory>>,
}

/// `GoogleCloudDialogflowCxV3GenerativeSettingsFallbackSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3GenerativeSettingsFallbackSettings {
    /// promptTemplates property.
    pub prompt_templates:
        Option<Vec<GoogleCloudDialogflowCxV3GenerativeSettingsFallbackSettingsPromptTemplate>>,
    /// selectedPrompt property.
    pub selected_prompt: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ToolAuthenticationApiKeyConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolAuthenticationApiKeyConfig {
    /// apiKey property.
    pub api_key: Option<String>,
    /// keyName property.
    pub key_name: Option<String>,
    /// requestLocation property.
    pub request_location: Option<String>,
    /// secretVersionForApiKey property.
    pub secret_version_for_api_key: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ResponseMessageMixedAudio` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResponseMessageMixedAudio {
    /// segments property.
    pub segments: Option<Vec<GoogleCloudDialogflowCxV3ResponseMessageMixedAudioSegment>>,
}

/// `GoogleCloudDialogflowCxV3FulfillmentSetParameterAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FulfillmentSetParameterAction {
    /// parameter property.
    pub parameter: Option<String>,
    /// value property.
    pub value: Option<serde_json::Value>,
}

/// `GoogleCloudDialogflowCxV3ListTestCasesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListTestCasesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// testCases property.
    pub test_cases: Option<Vec<GoogleCloudDialogflowCxV3TestCase>>,
}

/// `GoogleCloudDialogflowCxV3Fulfillment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Fulfillment {
    /// advancedSettings property.
    pub advanced_settings: Option<GoogleCloudDialogflowCxV3AdvancedSettings>,
    /// conditionalCases property.
    pub conditional_cases: Option<Vec<Box<GoogleCloudDialogflowCxV3FulfillmentConditionalCases>>>,
    /// enableGenerativeFallback property.
    pub enable_generative_fallback: Option<bool>,
    /// generators property.
    pub generators: Option<Vec<GoogleCloudDialogflowCxV3FulfillmentGeneratorSettings>>,
    /// messages property.
    pub messages: Option<Vec<GoogleCloudDialogflowCxV3ResponseMessage>>,
    /// returnPartialResponses property.
    pub return_partial_responses: Option<bool>,
    /// setParameterActions property.
    pub set_parameter_actions: Option<Vec<GoogleCloudDialogflowCxV3FulfillmentSetParameterAction>>,
    /// tag property.
    pub tag: Option<String>,
    /// webhook property.
    pub webhook: Option<String>,
}

/// `GoogleCloudDialogflowCxV3Webhook` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Webhook {
    /// disabled property.
    pub disabled: Option<bool>,
    /// displayName property.
    pub display_name: Option<String>,
    /// genericWebService property.
    pub generic_web_service: Option<GoogleCloudDialogflowCxV3WebhookGenericWebService>,
    /// name property.
    pub name: Option<String>,
    /// serviceDirectory property.
    pub service_directory: Option<GoogleCloudDialogflowCxV3WebhookServiceDirectoryConfig>,
    /// timeout property.
    pub timeout: Option<String>,
}

/// `GoogleCloudDialogflowCxV3Match` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Match {
    /// confidence property.
    pub confidence: Option<f64>,
    /// event property.
    pub event: Option<String>,
    /// intent property.
    pub intent: Option<GoogleCloudDialogflowCxV3Intent>,
    /// matchType property.
    pub match_type: Option<String>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
    /// resolvedInput property.
    pub resolved_input: Option<String>,
}

/// `GoogleCloudDialogflowCxV3PlaybookOutput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3PlaybookOutput {
    /// executionSummary property.
    pub execution_summary: Option<String>,
}

/// `GoogleCloudDialogflowCxV3Generator` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Generator {
    /// displayName property.
    pub display_name: Option<String>,
    /// llmModelSettings property.
    pub llm_model_settings: Option<GoogleCloudDialogflowCxV3LlmModelSettings>,
    /// modelParameter property.
    pub model_parameter: Option<GoogleCloudDialogflowCxV3GeneratorModelParameter>,
    /// name property.
    pub name: Option<String>,
    /// placeholders property.
    pub placeholders: Option<Vec<GoogleCloudDialogflowCxV3GeneratorPlaceholder>>,
    /// promptText property.
    pub prompt_text: Option<GoogleCloudDialogflowCxV3Phrase>,
}

/// `GoogleCloudDialogflowCxV3IntentCoverage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3IntentCoverage {
    /// coverageScore property.
    pub coverage_score: Option<f64>,
    /// intents property.
    pub intents: Option<Vec<GoogleCloudDialogflowCxV3IntentCoverageIntent>>,
}

/// `GoogleCloudDialogflowCxV3ValidateFlowRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ValidateFlowRequest {
    /// languageCode property.
    pub language_code: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ToolDataStoreToolFallbackPrompt` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolDataStoreToolFallbackPrompt {}

/// `GoogleCloudDialogflowCxV3CompareVersionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3CompareVersionsResponse {
    /// baseVersionContentJson property.
    pub base_version_content_json: Option<String>,
    /// compareTime property.
    pub compare_time: Option<String>,
    /// targetVersionContentJson property.
    pub target_version_content_json: Option<String>,
}

/// `GoogleCloudDialogflowCxV3QueryInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3QueryInput {
    /// audio property.
    pub audio: Option<GoogleCloudDialogflowCxV3AudioInput>,
    /// dtmf property.
    pub dtmf: Option<GoogleCloudDialogflowCxV3DtmfInput>,
    /// event property.
    pub event: Option<GoogleCloudDialogflowCxV3EventInput>,
    /// intent property.
    pub intent: Option<GoogleCloudDialogflowCxV3IntentInput>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// text property.
    pub text: Option<GoogleCloudDialogflowCxV3TextInput>,
    /// toolCallResult property.
    pub tool_call_result: Option<GoogleCloudDialogflowCxV3ToolCallResult>,
}

/// `GoogleCloudDialogflowCxV3PlaybookInput` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3PlaybookInput {
    /// precedingConversationSummary property.
    pub preceding_conversation_summary: Option<String>,
}

/// `GoogleCloudDialogflowCxV3RunTestCaseRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3RunTestCaseRequest {
    /// environment property.
    pub environment: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// sessionEntityTypes property.
    pub session_entity_types: Option<Vec<GoogleCloudDialogflowCxV3SessionEntityType>>,
}

/// `GoogleCloudDialogflowCxV3PlaybookInvocation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3PlaybookInvocation {
    /// displayName property.
    pub display_name: Option<String>,
    /// playbook property.
    pub playbook: Option<String>,
    /// playbookInput property.
    pub playbook_input: Option<GoogleCloudDialogflowCxV3PlaybookInput>,
    /// playbookOutput property.
    pub playbook_output: Option<GoogleCloudDialogflowCxV3PlaybookOutput>,
    /// playbookState property.
    pub playbook_state: Option<String>,
}

/// `GoogleCloudDialogflowCxV3EnvironmentVersionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3EnvironmentVersionConfig {
    /// version property.
    pub version: Option<String>,
}

/// `GoogleCloudDialogflowCxV3FormParameter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FormParameter {
    /// advancedSettings property.
    pub advanced_settings: Option<GoogleCloudDialogflowCxV3AdvancedSettings>,
    /// defaultValue property.
    pub default_value: Option<serde_json::Value>,
    /// displayName property.
    pub display_name: Option<String>,
    /// entityType property.
    pub entity_type: Option<String>,
    /// fillBehavior property.
    pub fill_behavior: Option<GoogleCloudDialogflowCxV3FormParameterFillBehavior>,
    /// isList property.
    pub is_list: Option<bool>,
    /// redact property.
    pub redact: Option<bool>,
    /// required property.
    pub required: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3StopExperimentRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3StopExperimentRequest {}

/// `GoogleCloudDialogflowCxV3VoiceSelectionParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3VoiceSelectionParams {
    /// name property.
    pub name: Option<String>,
    /// ssmlGender property.
    pub ssml_gender: Option<String>,
}

/// `GoogleCloudDialogflowCxV3QueryParameters` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3QueryParameters {
    /// analyzeQueryTextSentiment property.
    pub analyze_query_text_sentiment: Option<bool>,
    /// channel property.
    pub channel: Option<String>,
    /// currentPage property.
    pub current_page: Option<String>,
    /// currentPlaybook property.
    pub current_playbook: Option<String>,
    /// disableWebhook property.
    pub disable_webhook: Option<bool>,
    /// endUserMetadata property.
    pub end_user_metadata: Option<serde_json::Value>,
    /// flowVersions property.
    pub flow_versions: Option<Vec<String>>,
    /// geoLocation property.
    pub geo_location: Option<GoogleTypeLatLng>,
    /// llmModelSettings property.
    pub llm_model_settings: Option<GoogleCloudDialogflowCxV3LlmModelSettings>,
    /// parameterScope property.
    pub parameter_scope: Option<String>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
    /// payload property.
    pub payload: Option<serde_json::Value>,
    /// populateDataStoreConnectionSignals property.
    pub populate_data_store_connection_signals: Option<bool>,
    /// searchConfig property.
    pub search_config: Option<GoogleCloudDialogflowCxV3SearchConfig>,
    /// sessionEntityTypes property.
    pub session_entity_types: Option<Vec<GoogleCloudDialogflowCxV3SessionEntityType>>,
    /// sessionTtl property.
    pub session_ttl: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// webhookHeaders property.
    pub webhook_headers: Option<serde_json::Value>,
}

/// `GoogleCloudDialogflowCxV3FulfillmentConditionalCases` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FulfillmentConditionalCases {
    /// cases property.
    pub cases: Option<Vec<Box<GoogleCloudDialogflowCxV3FulfillmentConditionalCasesCase>>>,
}

/// `GoogleCloudDialogflowCxV3TrainFlowRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3TrainFlowRequest {}

/// `GoogleCloudDialogflowCxV3OutputAudioConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3OutputAudioConfig {
    /// audioEncoding property.
    pub audio_encoding: Option<String>,
    /// sampleRateHertz property.
    pub sample_rate_hertz: Option<i64>,
    /// synthesizeSpeechConfig property.
    pub synthesize_speech_config: Option<GoogleCloudDialogflowCxV3SynthesizeSpeechConfig>,
}

/// `GoogleCloudDialogflowCxV3ImportIntentsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ImportIntentsRequest {
    /// intentsContent property.
    pub intents_content: Option<GoogleCloudDialogflowCxV3InlineSource>,
    /// intentsUri property.
    pub intents_uri: Option<String>,
    /// mergeOption property.
    pub merge_option: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ExportEntityTypesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExportEntityTypesRequest {
    /// dataFormat property.
    pub data_format: Option<String>,
    /// entityTypes property.
    pub entity_types: Option<Vec<String>>,
    /// entityTypesContentInline property.
    pub entity_types_content_inline: Option<bool>,
    /// entityTypesUri property.
    pub entity_types_uri: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
}

/// `GoogleCloudDialogflowCxV3SafetySettingsRaiSettingsCategoryFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SafetySettingsRaiSettingsCategoryFilter {
    /// category property.
    pub category: Option<String>,
    /// filterLevel property.
    pub filter_level: Option<String>,
}

/// `GoogleCloudDialogflowCxV3AgentAnswerFeedbackSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AgentAnswerFeedbackSettings {
    /// enableAnswerFeedback property.
    pub enable_answer_feedback: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// transitionRouteGroups property.
    pub transition_route_groups: Option<Vec<GoogleCloudDialogflowCxV3TransitionRouteGroup>>,
}

/// `GoogleCloudDialogflowCxV3DataStoreConnectionSignalsAnswerGenerationModelCallSignals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3DataStoreConnectionSignalsAnswerGenerationModelCallSignals {
    /// model property.
    pub model: Option<String>,
    /// modelOutput property.
    pub model_output: Option<String>,
    /// renderedPrompt property.
    pub rendered_prompt: Option<String>,
}

/// `GoogleCloudDialogflowCxV3PlaybookImportStrategy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3PlaybookImportStrategy {
    /// mainPlaybookImportStrategy property.
    pub main_playbook_import_strategy: Option<String>,
    /// nestedResourceImportStrategy property.
    pub nested_resource_import_strategy: Option<String>,
    /// toolImportStrategy property.
    pub tool_import_strategy: Option<String>,
}

/// `GoogleCloudDialogflowCxV3InlineSchema` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3InlineSchema {
    /// items property.
    pub items: Option<Box<GoogleCloudDialogflowCxV3TypeSchema>>,
    /// type property.
    pub r#type: Option<String>,
}

/// `GoogleCloudDialogflowCxV3AdvancedSettingsSpeechSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AdvancedSettingsSpeechSettings {
    /// endpointerSensitivity property.
    pub endpointer_sensitivity: Option<i64>,
    /// models property.
    pub models: Option<serde_json::Value>,
    /// noSpeechTimeout property.
    pub no_speech_timeout: Option<String>,
    /// useTimeoutBasedEndpointing property.
    pub use_timeout_based_endpointing: Option<bool>,
}

/// `GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse {
    /// playbook property.
    pub playbook: Option<GoogleCloudDialogflowCxV3Playbook>,
}

/// `GoogleCloudDialogflowCxV3ExportPlaybookRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExportPlaybookRequest {
    /// dataFormat property.
    pub data_format: Option<String>,
    /// playbookUri property.
    pub playbook_uri: Option<String>,
}

/// `GoogleCloudDialogflowCxV3AgentGitIntegrationSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AgentGitIntegrationSettings {
    /// githubSettings property.
    pub github_settings: Option<GoogleCloudDialogflowCxV3AgentGitIntegrationSettingsGithubSettings>,
}

/// `GoogleCloudDialogflowCxV3RolloutState` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3RolloutState {
    /// startTime property.
    pub start_time: Option<String>,
    /// step property.
    pub step: Option<String>,
    /// stepIndex property.
    pub step_index: Option<i64>,
}

/// `GoogleCloudDialogflowCxV3ValidationMessage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ValidationMessage {
    /// detail property.
    pub detail: Option<String>,
    /// resourceNames property.
    pub resource_names: Option<Vec<GoogleCloudDialogflowCxV3ResourceName>>,
    /// resourceType property.
    pub resource_type: Option<String>,
    /// resources property.
    pub resources: Option<Vec<String>>,
    /// severity property.
    pub severity: Option<String>,
}

/// `GoogleCloudDialogflowCxV3GeneratorPlaceholder` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3GeneratorPlaceholder {
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ExportAgentRequestGitDestination` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ExportAgentRequestGitDestination {
    /// commitMessage property.
    pub commit_message: Option<String>,
    /// trackingBranch property.
    pub tracking_branch: Option<String>,
}

/// `GoogleCloudDialogflowCxV3BoostSpecConditionBoostSpecBoostControlSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3BoostSpecConditionBoostSpecBoostControlSpec {
    /// attributeType property.
    pub attribute_type: Option<String>,
    /// controlPoints property.
    pub control_points: Option<
        Vec<GoogleCloudDialogflowCxV3BoostSpecConditionBoostSpecBoostControlSpecControlPoint>,
    >,
    /// fieldName property.
    pub field_name: Option<String>,
    /// interpolationType property.
    pub interpolation_type: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ImportTestCasesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ImportTestCasesRequest {
    /// content property.
    pub content: Option<String>,
    /// gcsUri property.
    pub gcs_uri: Option<String>,
}

/// `GoogleCloudDialogflowCxV3PlaybookTraceMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3PlaybookTraceMetadata {
    /// displayName property.
    pub display_name: Option<String>,
    /// playbook property.
    pub playbook: Option<String>,
}

/// `GoogleCloudDialogflowCxV3FlowImportStrategy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FlowImportStrategy {
    /// globalImportStrategy property.
    pub global_import_strategy: Option<String>,
}

/// `GoogleCloudDialogflowCxV3FlowTransition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3FlowTransition {
    /// displayName property.
    pub display_name: Option<String>,
    /// flow property.
    pub flow: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListToolVersionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListToolVersionsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// toolVersions property.
    pub tool_versions: Option<Vec<GoogleCloudDialogflowCxV3ToolVersion>>,
}

/// `GoogleCloudDialogflowCxV3ResponseMessageOutputAudioText` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ResponseMessageOutputAudioText {
    /// allowPlaybackInterruption property.
    pub allow_playback_interruption: Option<bool>,
    /// ssml property.
    pub ssml: Option<String>,
    /// text property.
    pub text: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// playbookVersions property.
    pub playbook_versions: Option<Vec<GoogleCloudDialogflowCxV3PlaybookVersion>>,
}

/// `GoogleCloudDialogflowCxV3AdvancedSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3AdvancedSettings {
    /// audioExportGcsDestination property.
    pub audio_export_gcs_destination: Option<GoogleCloudDialogflowCxV3GcsDestination>,
    /// dtmfSettings property.
    pub dtmf_settings: Option<GoogleCloudDialogflowCxV3AdvancedSettingsDtmfSettings>,
    /// loggingSettings property.
    pub logging_settings: Option<GoogleCloudDialogflowCxV3AdvancedSettingsLoggingSettings>,
    /// speechSettings property.
    pub speech_settings: Option<GoogleCloudDialogflowCxV3AdvancedSettingsSpeechSettings>,
}

/// `GoogleCloudDialogflowCxV3SpeechProcessingMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3SpeechProcessingMetadata {
    /// displayName property.
    pub display_name: Option<String>,
}

/// `GoogleCloudDialogflowCxV3Tool` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3Tool {
    /// dataStoreSpec property.
    pub data_store_spec: Option<GoogleCloudDialogflowCxV3ToolDataStoreTool>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// functionSpec property.
    pub function_spec: Option<GoogleCloudDialogflowCxV3ToolFunctionTool>,
    /// name property.
    pub name: Option<String>,
    /// openApiSpec property.
    pub open_api_spec: Option<GoogleCloudDialogflowCxV3ToolOpenApiTool>,
    /// toolType property.
    pub tool_type: Option<String>,
}

/// `GoogleCloudDialogflowCxV3ToolAuthenticationOAuthConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudDialogflowCxV3ToolAuthenticationOAuthConfig {
    /// clientId property.
    pub client_id: Option<String>,
    /// clientSecret property.
    pub client_secret: Option<String>,
    /// oauthGrantType property.
    pub oauth_grant_type: Option<String>,
    /// scopes property.
    pub scopes: Option<Vec<String>>,
    /// secretVersionForClientSecret property.
    pub secret_version_for_client_secret: Option<String>,
    /// tokenEndpoint property.
    pub token_endpoint: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dialogflow.projects.locations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `extraLocationTypes`.
    pub extra_location_types: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Agent,
}

/// Arguments for [`dialogflow.projects.locations.agents.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.export_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsExportArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ExportAgentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.getGenerativeSettings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGetGenerativeSettingsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.getValidationResult_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGetValidationResultArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Agent,
}

/// Arguments for [`dialogflow.projects.locations.agents.restore_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsRestoreArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3RestoreAgentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.updateGenerativeSettings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsUpdateGenerativeSettingsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3GenerativeSettings,
}

/// Arguments for [`dialogflow.projects.locations.agents.validate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsValidateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ValidateAgentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.changelogs.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsChangelogsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.changelogs.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsChangelogsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.entityTypes.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3EntityType,
}

/// Arguments for [`dialogflow.projects.locations.agents.entityTypes.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.entityTypes.export_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesExportArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ExportEntityTypesRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.entityTypes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.entityTypes.import_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesImportArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ImportEntityTypesRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.entityTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.entityTypes.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3EntityType,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Environment,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.deployFlow_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsDeployFlowArgs {
    /// Path parameter: `environment`.
    pub environment: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3DeployFlowRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.lookupEnvironmentHistory_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsLookupEnvironmentHistoryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Environment,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.runContinuousTest_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsRunContinuousTestArgs {
    /// Path parameter: `environment`.
    pub environment: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3RunContinuousTestRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.continuousTestResults.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsContinuousTestResultsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.deployments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.deployments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.experiments.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Experiment,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.experiments.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.experiments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.experiments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.experiments.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Experiment,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.experiments.start_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStartArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3StartExperimentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.experiments.stop_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStopArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3StopExperimentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.sessions.detectIntent_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsDetectIntentArgs {
    /// Path parameter: `session`.
    pub session: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3DetectIntentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.sessions.fulfillIntent_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsFulfillIntentArgs {
    /// Path parameter: `session`.
    pub session: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3FulfillIntentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.sessions.matchIntent_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsMatchIntentArgs {
    /// Path parameter: `session`.
    pub session: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3MatchIntentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.sessions.serverStreamingDetectIntent_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsServerStreamingDetectIntentArgs {
    /// Path parameter: `session`.
    pub session: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3DetectIntentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.sessions.entityTypes.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3SessionEntityType,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.sessions.entityTypes.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.sessions.entityTypes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.sessions.entityTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.environments.sessions.entityTypes.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3SessionEntityType,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Flow,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.export_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsExportArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ExportFlowRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.getValidationResult_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsGetValidationResultArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.import_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsImportArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ImportFlowRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Flow,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.train_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTrainArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3TrainFlowRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.validate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsValidateArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ValidateFlowRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.pages.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPagesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Page,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.pages.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPagesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.pages.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPagesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.pages.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPagesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.pages.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPagesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Page,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.transitionRouteGroups.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3TransitionRouteGroup,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.transitionRouteGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.transitionRouteGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.transitionRouteGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.transitionRouteGroups.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3TransitionRouteGroup,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.versions.compareVersions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsCompareVersionsArgs {
    /// Path parameter: `baseVersion`.
    pub base_version: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3CompareVersionsRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.versions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Version,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.versions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.versions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.versions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.versions.load_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsLoadArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3LoadVersionRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.flows.versions.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Version,
}

/// Arguments for [`dialogflow.projects.locations.agents.generators.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGeneratorsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Generator,
}

/// Arguments for [`dialogflow.projects.locations.agents.generators.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGeneratorsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.generators.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGeneratorsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.generators.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGeneratorsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.generators.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGeneratorsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Generator,
}

/// Arguments for [`dialogflow.projects.locations.agents.intents.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Intent,
}

/// Arguments for [`dialogflow.projects.locations.agents.intents.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.intents.export_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsExportArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ExportIntentsRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.intents.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.intents.import_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsImportArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ImportIntentsRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.intents.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `intentView`.
    pub intent_view: Option<String>,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.intents.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Intent,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Playbook,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.export_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExportArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ExportPlaybookRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.import_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksImportArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ImportPlaybookRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Playbook,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.examples.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExamplesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Example,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.examples.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExamplesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.examples.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExamplesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.examples.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExamplesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.examples.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExamplesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Example,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.versions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksVersionsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3PlaybookVersion,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.versions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksVersionsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.versions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksVersionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.versions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksVersionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.playbooks.versions.restore_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksVersionsRestoreArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3RestorePlaybookVersionRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.sessions.detectIntent_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsDetectIntentArgs {
    /// Path parameter: `session`.
    pub session: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3DetectIntentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.sessions.fulfillIntent_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsFulfillIntentArgs {
    /// Path parameter: `session`.
    pub session: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3FulfillIntentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.sessions.matchIntent_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsMatchIntentArgs {
    /// Path parameter: `session`.
    pub session: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3MatchIntentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.sessions.serverStreamingDetectIntent_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsServerStreamingDetectIntentArgs {
    /// Path parameter: `session`.
    pub session: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3DetectIntentRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.sessions.submitAnswerFeedback_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsSubmitAnswerFeedbackArgs {
    /// Path parameter: `session`.
    pub session: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3SubmitAnswerFeedbackRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.sessions.entityTypes.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsEntityTypesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3SessionEntityType,
}

/// Arguments for [`dialogflow.projects.locations.agents.sessions.entityTypes.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsEntityTypesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.sessions.entityTypes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsEntityTypesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.sessions.entityTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsEntityTypesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.sessions.entityTypes.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsEntityTypesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3SessionEntityType,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.batchDelete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesBatchDeleteArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3BatchDeleteTestCasesRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.batchRun_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesBatchRunArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3BatchRunTestCasesRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.calculateCoverage_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesCalculateCoverageArgs {
    /// Path parameter: `agent`.
    pub agent: String,
    /// Query parameter: `type`.
    pub r#type: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3TestCase,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.export_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesExportArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ExportTestCasesRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.import_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesImportArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ImportTestCasesRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3TestCase,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.run_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesRunArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3RunTestCaseRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.results.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesResultsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.testCases.results.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesResultsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.tools.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Tool,
}

/// Arguments for [`dialogflow.projects.locations.agents.tools.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.tools.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.tools.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.tools.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Tool,
}

/// Arguments for [`dialogflow.projects.locations.agents.tools.versions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsVersionsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3ToolVersion,
}

/// Arguments for [`dialogflow.projects.locations.agents.tools.versions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsVersionsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.tools.versions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsVersionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.tools.versions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsVersionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.tools.versions.restore_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsVersionsRestoreArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3RestoreToolVersionRequest,
}

/// Arguments for [`dialogflow.projects.locations.agents.transitionRouteGroups.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTransitionRouteGroupsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3TransitionRouteGroup,
}

/// Arguments for [`dialogflow.projects.locations.agents.transitionRouteGroups.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTransitionRouteGroupsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.transitionRouteGroups.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTransitionRouteGroupsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.transitionRouteGroups.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTransitionRouteGroupsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.transitionRouteGroups.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTransitionRouteGroupsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3TransitionRouteGroup,
}

/// Arguments for [`dialogflow.projects.locations.agents.webhooks.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsWebhooksCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Webhook,
}

/// Arguments for [`dialogflow.projects.locations.agents.webhooks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsWebhooksDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.webhooks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsWebhooksGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.agents.webhooks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsWebhooksListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.agents.webhooks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsWebhooksPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3Webhook,
}

/// Arguments for [`dialogflow.projects.locations.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsOperationsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.securitySettings.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsSecuritySettingsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3SecuritySettings,
}

/// Arguments for [`dialogflow.projects.locations.securitySettings.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsSecuritySettingsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.securitySettings.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsSecuritySettingsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.locations.securitySettings.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsSecuritySettingsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dialogflow.projects.locations.securitySettings.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsSecuritySettingsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: GoogleCloudDialogflowCxV3SecuritySettings,
}

/// Arguments for [`dialogflow.projects.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsOperationsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dialogflow.projects.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DialogflowProjectsOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudLocationLocation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudLocationLocation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}/locations
// -----------------------------------------------------------------------------

/// GET v3/{+name}/locations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudLocationListLocationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/locations",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.extra_location_types {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("extraLocationTypes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudLocationListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/agents
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/agents.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/agents",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Agent =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:export
// -----------------------------------------------------------------------------

/// POST v3/{+name}:export.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_export_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_export_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsExportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:export", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Agent =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_get_generative_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_get_generative_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsGetGenerativeSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3GenerativeSettings>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3GenerativeSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_get_validation_result_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_get_validation_result_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsGetValidationResultArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3AgentValidationResult>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3AgentValidationResult =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/agents
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/agents.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListAgentsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/agents",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListAgentsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Agent =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:restore
// -----------------------------------------------------------------------------

/// POST v3/{+name}:restore.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_restore_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_restore_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsRestoreArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:restore", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_update_generative_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_update_generative_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsUpdateGenerativeSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3GenerativeSettings>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3GenerativeSettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:validate
// -----------------------------------------------------------------------------

/// POST v3/{+name}:validate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_validate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_validate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsValidateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3AgentValidationResult>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:validate",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3AgentValidationResult =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_changelogs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_changelogs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsChangelogsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3Changelog>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Changelog = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/changelogs
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/changelogs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_changelogs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_changelogs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsChangelogsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListChangelogsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/changelogs",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListChangelogsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/entityTypes
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/entityTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_entity_types_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_entity_types_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEntityTypesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3EntityType>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/entityTypes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3EntityType = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_entity_types_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_entity_types_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEntityTypesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/entityTypes:export
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/entityTypes:export.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_entity_types_export_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_entity_types_export_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEntityTypesExportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/entityTypes:export",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_entity_types_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_entity_types_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEntityTypesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3EntityType>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3EntityType = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/entityTypes:import
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/entityTypes:import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_entity_types_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_entity_types_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEntityTypesImportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/entityTypes:import",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/entityTypes
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/entityTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_entity_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_entity_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEntityTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListEntityTypesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/entityTypes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListEntityTypesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_entity_types_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_entity_types_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEntityTypesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3EntityType>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3EntityType = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/environments
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/environments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/environments",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+environment}:deployFlow
// -----------------------------------------------------------------------------

/// POST v3/{+environment}:deployFlow.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_deploy_flow_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_deploy_flow_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsDeployFlowArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:deployFlow",
        args.environment,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3Environment>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Environment = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/environments
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/environments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListEnvironmentsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/environments",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListEnvironmentsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}:lookupEnvironmentHistory
// -----------------------------------------------------------------------------

/// GET v3/{+name}:lookupEnvironmentHistory.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_lookup_environment_history_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_lookup_environment_history_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsLookupEnvironmentHistoryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:lookupEnvironmentHistory",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+environment}:runContinuousTest
// -----------------------------------------------------------------------------

/// POST v3/{+environment}:runContinuousTest.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_run_continuous_test_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_run_continuous_test_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsRunContinuousTestArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:runContinuousTest",
        args.environment,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/continuousTestResults
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/continuousTestResults.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_continuous_test_results_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_continuous_test_results_list_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsContinuousTestResultsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/continuousTestResults",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_deployments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_deployments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3Deployment>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Deployment = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/deployments
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/deployments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_deployments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_deployments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListDeploymentsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/deployments",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListDeploymentsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/experiments
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/experiments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_experiments_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_experiments_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3Experiment>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/experiments",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Experiment = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_experiments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_experiments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_experiments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_experiments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3Experiment>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Experiment = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/experiments
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/experiments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_experiments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_experiments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListExperimentsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/experiments",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListExperimentsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_experiments_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_experiments_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3Experiment>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Experiment = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:start
// -----------------------------------------------------------------------------

/// POST v3/{+name}:start.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_experiments_start_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_experiments_start_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStartArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3Experiment>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:start", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Experiment = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:stop
// -----------------------------------------------------------------------------

/// POST v3/{+name}:stop.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_experiments_stop_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_experiments_stop_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStopArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3Experiment>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:stop", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Experiment = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+session}:detectIntent
// -----------------------------------------------------------------------------

/// POST v3/{+session}:detectIntent.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_sessions_detect_intent_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_sessions_detect_intent_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsDetectIntentArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:detectIntent",
        args.session,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3DetectIntentResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+session}:fulfillIntent
// -----------------------------------------------------------------------------

/// POST v3/{+session}:fulfillIntent.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsFulfillIntentArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3FulfillIntentResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:fulfillIntent",
        args.session,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3FulfillIntentResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+session}:matchIntent
// -----------------------------------------------------------------------------

/// POST v3/{+session}:matchIntent.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_sessions_match_intent_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_sessions_match_intent_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsMatchIntentArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3MatchIntentResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:matchIntent",
        args.session,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3MatchIntentResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+session}:serverStreamingDetectIntent
// -----------------------------------------------------------------------------

/// POST v3/{+session}:serverStreamingDetectIntent.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsServerStreamingDetectIntentArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:serverStreamingDetectIntent",
        args.session,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3DetectIntentResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/entityTypes
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/entityTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_sessions_entity_types_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_create_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/entityTypes",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_sessions_entity_types_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/entityTypes
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/entityTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_sessions_entity_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/entityTypes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/flows
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/flows.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}/flows", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Flow =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:export
// -----------------------------------------------------------------------------

/// POST v3/{+name}:export.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_export_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_export_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsExportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:export", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Flow =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_get_validation_result_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_get_validation_result_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsGetValidationResultArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3FlowValidationResult>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3FlowValidationResult =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/flows:import
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/flows:import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsImportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/flows:import",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/flows
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/flows.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListFlowsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}/flows", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListFlowsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Flow =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:train
// -----------------------------------------------------------------------------

/// POST v3/{+name}:train.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_train_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_train_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsTrainArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:train", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:validate
// -----------------------------------------------------------------------------

/// POST v3/{+name}:validate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_validate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_validate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsValidateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3FlowValidationResult>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:validate",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3FlowValidationResult =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/pages
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/pages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_pages_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_pages_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsPagesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}/pages", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Page =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_pages_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_pages_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsPagesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_pages_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_pages_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsPagesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Page =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/pages
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/pages.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_pages_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_pages_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsPagesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListPagesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}/pages", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListPagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_pages_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_pages_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsPagesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Page =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/transitionRouteGroups
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/transitionRouteGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_transition_route_groups_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/transitionRouteGroups",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_transition_route_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_transition_route_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/transitionRouteGroups
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/transitionRouteGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_transition_route_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/transitionRouteGroups",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_transition_route_groups_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+baseVersion}:compareVersions
// -----------------------------------------------------------------------------

/// POST v3/{+baseVersion}:compareVersions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_versions_compare_versions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_versions_compare_versions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsCompareVersionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3CompareVersionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:compareVersions",
        args.base_version,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3CompareVersionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/versions
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/versions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_versions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_versions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/versions",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_versions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_versions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_versions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_versions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Version>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Version = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/versions
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/versions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_versions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_versions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListVersionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/versions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListVersionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:load
// -----------------------------------------------------------------------------

/// POST v3/{+name}:load.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_versions_load_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_versions_load_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsLoadArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:load", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_flows_versions_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_flows_versions_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Version>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Version = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/generators
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/generators.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_generators_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_generators_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsGeneratorsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3Generator>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/generators",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Generator = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_generators_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_generators_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsGeneratorsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_generators_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_generators_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsGeneratorsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3Generator>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Generator = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/generators
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/generators.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_generators_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_generators_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsGeneratorsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListGeneratorsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/generators",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListGeneratorsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_generators_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_generators_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsGeneratorsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3Generator>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Generator = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/intents
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/intents.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_intents_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_intents_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsIntentsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/intents",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Intent =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_intents_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_intents_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsIntentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/intents:export
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/intents:export.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_intents_export_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_intents_export_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsIntentsExportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/intents:export",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_intents_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_intents_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsIntentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Intent =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/intents:import
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/intents:import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_intents_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_intents_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsIntentsImportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/intents:import",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/intents
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/intents.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_intents_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_intents_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsIntentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListIntentsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/intents",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.intent_view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("intentView=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListIntentsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_intents_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_intents_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsIntentsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Intent =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/playbooks
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/playbooks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/playbooks",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Playbook = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:export
// -----------------------------------------------------------------------------

/// POST v3/{+name}:export.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_export_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_export_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:export", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Playbook = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/playbooks:import
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/playbooks:import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksImportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/playbooks:import",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/playbooks
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/playbooks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListPlaybooksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/playbooks",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListPlaybooksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Playbook = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/examples
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/examples.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_examples_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_examples_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExamplesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/examples",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Example = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_examples_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_examples_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExamplesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_examples_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_examples_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExamplesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Example = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/examples
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/examples.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_examples_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_examples_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExamplesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListExamplesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/examples",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListExamplesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_examples_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_examples_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExamplesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Example = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/versions
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/versions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_versions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_versions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksVersionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3PlaybookVersion>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/versions",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3PlaybookVersion = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_versions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_versions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksVersionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_versions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_versions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksVersionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3PlaybookVersion>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3PlaybookVersion = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/versions
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/versions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_versions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_versions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksVersionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/versions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:restore
// -----------------------------------------------------------------------------

/// POST v3/{+name}:restore.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_playbooks_versions_restore_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_playbooks_versions_restore_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsPlaybooksVersionsRestoreArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:restore", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+session}:detectIntent
// -----------------------------------------------------------------------------

/// POST v3/{+session}:detectIntent.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_sessions_detect_intent_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_sessions_detect_intent_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsSessionsDetectIntentArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:detectIntent",
        args.session,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3DetectIntentResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+session}:fulfillIntent
// -----------------------------------------------------------------------------

/// POST v3/{+session}:fulfillIntent.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_sessions_fulfill_intent_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_sessions_fulfill_intent_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsSessionsFulfillIntentArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3FulfillIntentResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:fulfillIntent",
        args.session,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3FulfillIntentResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+session}:matchIntent
// -----------------------------------------------------------------------------

/// POST v3/{+session}:matchIntent.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_sessions_match_intent_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_sessions_match_intent_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsSessionsMatchIntentArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3MatchIntentResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:matchIntent",
        args.session,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3MatchIntentResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+session}:serverStreamingDetectIntent
// -----------------------------------------------------------------------------

/// POST v3/{+session}:serverStreamingDetectIntent.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsSessionsServerStreamingDetectIntentArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:serverStreamingDetectIntent",
        args.session,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3DetectIntentResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+session}:submitAnswerFeedback
// -----------------------------------------------------------------------------

/// POST v3/{+session}:submitAnswerFeedback.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_sessions_submit_answer_feedback_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_sessions_submit_answer_feedback_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsSessionsSubmitAnswerFeedbackArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3AnswerFeedback>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}:submitAnswerFeedback",
        args.session,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3AnswerFeedback = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/entityTypes
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/entityTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_sessions_entity_types_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_sessions_entity_types_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsSessionsEntityTypesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/entityTypes",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_sessions_entity_types_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_sessions_entity_types_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsSessionsEntityTypesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_sessions_entity_types_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_sessions_entity_types_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsSessionsEntityTypesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/entityTypes
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/entityTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_sessions_entity_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_sessions_entity_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsSessionsEntityTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/entityTypes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_sessions_entity_types_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_sessions_entity_types_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsSessionsEntityTypesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/testCases:batchDelete
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/testCases:batchDelete.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_batch_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_batch_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesBatchDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/testCases:batchDelete",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/testCases:batchRun
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/testCases:batchRun.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_batch_run_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_batch_run_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesBatchRunArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/testCases:batchRun",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+agent}/testCases:calculateCoverage
// -----------------------------------------------------------------------------

/// GET v3/{+agent}/testCases:calculateCoverage.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_calculate_coverage_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_calculate_coverage_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesCalculateCoverageArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3CalculateCoverageResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/testCases:calculateCoverage",
        args.agent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.r#type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("type=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3CalculateCoverageResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/testCases
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/testCases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/testCases",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TestCase = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/testCases:export
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/testCases:export.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_export_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_export_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesExportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/testCases:export",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TestCase = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/testCases:import
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/testCases:import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesImportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/testCases:import",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/testCases
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/testCases.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListTestCasesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/testCases",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListTestCasesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TestCase = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:run
// -----------------------------------------------------------------------------

/// POST v3/{+name}:run.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_run_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_run_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesRunArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:run", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_results_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_results_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesResultsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3TestCaseResult>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TestCaseResult = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/results
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/results.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_test_cases_results_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_test_cases_results_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTestCasesResultsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListTestCaseResultsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/results",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListTestCaseResultsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/tools
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/tools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_tools_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_tools_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsToolsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}/tools", args.parent,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Tool =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_tools_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_tools_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsToolsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_tools_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_tools_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsToolsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Tool =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/tools
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/tools.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_tools_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_tools_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsToolsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListToolsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}/tools", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListToolsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_tools_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_tools_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsToolsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Tool =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/versions
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/versions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_tools_versions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_tools_versions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsToolsVersionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ToolVersion>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/versions",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ToolVersion = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_tools_versions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_tools_versions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsToolsVersionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_tools_versions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_tools_versions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsToolsVersionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ToolVersion>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ToolVersion = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/versions
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/versions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_tools_versions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_tools_versions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsToolsVersionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListToolVersionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/versions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListToolVersionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:restore
// -----------------------------------------------------------------------------

/// POST v3/{+name}:restore.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_tools_versions_restore_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_tools_versions_restore_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsToolsVersionsRestoreArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3RestoreToolVersionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:restore", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3RestoreToolVersionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/transitionRouteGroups
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/transitionRouteGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_transition_route_groups_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_transition_route_groups_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/transitionRouteGroups",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_transition_route_groups_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_transition_route_groups_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_transition_route_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_transition_route_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/transitionRouteGroups
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/transitionRouteGroups.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_transition_route_groups_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_transition_route_groups_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/transitionRouteGroups",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_transition_route_groups_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_transition_route_groups_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/webhooks
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/webhooks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_webhooks_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_webhooks_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsWebhooksCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/webhooks",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Webhook = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_webhooks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_webhooks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsWebhooksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_webhooks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_webhooks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsWebhooksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Webhook = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/webhooks
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/webhooks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_webhooks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_webhooks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsWebhooksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListWebhooksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/webhooks",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListWebhooksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_agents_webhooks_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_agents_webhooks_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsAgentsWebhooksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Webhook = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v3/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:cancel", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}/operations
// -----------------------------------------------------------------------------

/// GET v3/{+name}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleLongrunningListOperationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/operations",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+parent}/securitySettings
// -----------------------------------------------------------------------------

/// POST v3/{+parent}/securitySettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_security_settings_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_security_settings_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsSecuritySettingsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/securitySettings",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SecuritySettings = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{+name}
// -----------------------------------------------------------------------------

/// DELETE v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_security_settings_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_security_settings_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsSecuritySettingsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_security_settings_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_security_settings_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsSecuritySettingsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SecuritySettings = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+parent}/securitySettings
// -----------------------------------------------------------------------------

/// GET v3/{+parent}/securitySettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_security_settings_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_security_settings_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsSecuritySettingsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSecuritySettingsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/securitySettings",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListSecuritySettingsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{+name}
// -----------------------------------------------------------------------------

/// PATCH v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_locations_security_settings_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_locations_security_settings_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsLocationsSecuritySettingsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SecuritySettings = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v3/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}:cancel", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}
// -----------------------------------------------------------------------------

/// GET v3/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://dialogflow.googleapis.com/v3/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{+name}/operations
// -----------------------------------------------------------------------------

/// GET v3/{+name}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dialogflow_projects_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dialogflow_projects_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DialogflowProjectsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleLongrunningListOperationsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/{}/operations",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
