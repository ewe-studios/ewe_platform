//! Auto-generated API module for gcp/firebasedynamiclinks installAttribution.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_firebasedynamiclinks_install_attribution `

#![cfg(feature = "gcp_firebasedynamiclinks_install_attribution")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GetIosPostInstallAttributionResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GetIosPostInstallAttributionResponse {
    /// appMinimumVersion property.
    pub app_minimum_version: Option<String>,
    /// attributionConfidence property.
    pub attribution_confidence: Option<String>,
    /// deepLink property.
    pub deep_link: Option<String>,
    /// externalBrowserDestinationLink property.
    pub external_browser_destination_link: Option<String>,
    /// fallbackLink property.
    pub fallback_link: Option<String>,
    /// invitationId property.
    pub invitation_id: Option<String>,
    /// isStrongMatchExecutable property.
    pub is_strong_match_executable: Option<bool>,
    /// matchMessage property.
    pub match_message: Option<String>,
    /// requestIpVersion property.
    pub request_ip_version: Option<String>,
    /// requestedLink property.
    pub requested_link: Option<String>,
    /// resolvedLink property.
    pub resolved_link: Option<String>,
    /// utmCampaign property.
    pub utm_campaign: Option<String>,
    /// utmContent property.
    pub utm_content: Option<String>,
    /// utmMedium property.
    pub utm_medium: Option<String>,
    /// utmSource property.
    pub utm_source: Option<String>,
    /// utmTerm property.
    pub utm_term: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/installAttribution
// -----------------------------------------------------------------------------

/// POST v1/installAttribution.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = firebasedynamiclinks_install_attribution_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn firebasedynamiclinks_install_attribution_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GetIosPostInstallAttributionResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://firebasedynamiclinks.googleapis.com/v1/installAttribution",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetIosPostInstallAttributionResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
