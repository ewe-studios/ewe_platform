//! Auto-generated API module for gcp/sasportal devices:createSigned.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_sasportal_devices_create_signed `

#![cfg(feature = "gcp_sasportal_devices_create_signed")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::SasPortalDevice;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SasPortalDeviceMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SasPortalDeviceMetadata {
    /// antennaModel property.
    pub antenna_model: Option<String>,
    /// commonChannelGroup property.
    pub common_channel_group: Option<String>,
    /// interferenceCoordinationGroup property.
    pub interference_coordination_group: Option<String>,
    /// nrqzValidated property.
    pub nrqz_validated: Option<bool>,
    /// nrqzValidation property.
    pub nrqz_validation: Option<SasPortalNrqzValidation>,
}

/// `SasPortalFrequencyRange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SasPortalFrequencyRange {
    /// highFrequencyMhz property.
    pub high_frequency_mhz: Option<f64>,
    /// lowFrequencyMhz property.
    pub low_frequency_mhz: Option<f64>,
}

/// `SasPortalDeviceGrant` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SasPortalDeviceGrant {
    /// channelType property.
    pub channel_type: Option<String>,
    /// expireTime property.
    pub expire_time: Option<String>,
    /// frequencyRange property.
    pub frequency_range: Option<SasPortalFrequencyRange>,
    /// grantId property.
    pub grant_id: Option<String>,
    /// lastHeartbeatTransmitExpireTime property.
    pub last_heartbeat_transmit_expire_time: Option<String>,
    /// maxEirp property.
    pub max_eirp: Option<f64>,
    /// moveList property.
    pub move_list: Option<Vec<SasPortalDpaMoveList>>,
    /// state property.
    pub state: Option<String>,
    /// suspensionReason property.
    pub suspension_reason: Option<Vec<String>>,
}

/// `SasPortalNrqzValidation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SasPortalNrqzValidation {
    /// caseId property.
    pub case_id: Option<String>,
    /// cpiId property.
    pub cpi_id: Option<String>,
    /// latitude property.
    pub latitude: Option<f64>,
    /// longitude property.
    pub longitude: Option<f64>,
    /// state property.
    pub state: Option<String>,
}

/// `SasPortalDeviceModel` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SasPortalDeviceModel {
    /// firmwareVersion property.
    pub firmware_version: Option<String>,
    /// hardwareVersion property.
    pub hardware_version: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// softwareVersion property.
    pub software_version: Option<String>,
    /// vendor property.
    pub vendor: Option<String>,
}

/// `SasPortalDpaMoveList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SasPortalDpaMoveList {
    /// dpaId property.
    pub dpa_id: Option<String>,
    /// frequencyRange property.
    pub frequency_range: Option<SasPortalFrequencyRange>,
}

/// `SasPortalDeviceAirInterface` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SasPortalDeviceAirInterface {
    /// radioTechnology property.
    pub radio_technology: Option<String>,
    /// supportedSpec property.
    pub supported_spec: Option<String>,
}

/// `SasPortalDeviceConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SasPortalDeviceConfig {
    /// airInterface property.
    pub air_interface: Option<SasPortalDeviceAirInterface>,
    /// callSign property.
    pub call_sign: Option<String>,
    /// category property.
    pub category: Option<String>,
    /// installationParams property.
    pub installation_params: Option<SasPortalInstallationParams>,
    /// isSigned property.
    pub is_signed: Option<bool>,
    /// measurementCapabilities property.
    pub measurement_capabilities: Option<Vec<String>>,
    /// model property.
    pub model: Option<SasPortalDeviceModel>,
    /// state property.
    pub state: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// userId property.
    pub user_id: Option<String>,
}

/// `SasPortalInstallationParams` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SasPortalInstallationParams {
    /// antennaAzimuth property.
    pub antenna_azimuth: Option<i64>,
    /// antennaBeamwidth property.
    pub antenna_beamwidth: Option<i64>,
    /// antennaDowntilt property.
    pub antenna_downtilt: Option<i64>,
    /// antennaGain property.
    pub antenna_gain: Option<f64>,
    /// antennaModel property.
    pub antenna_model: Option<String>,
    /// cpeCbsdIndication property.
    pub cpe_cbsd_indication: Option<bool>,
    /// eirpCapability property.
    pub eirp_capability: Option<i64>,
    /// height property.
    pub height: Option<f64>,
    /// heightType property.
    pub height_type: Option<String>,
    /// horizontalAccuracy property.
    pub horizontal_accuracy: Option<f64>,
    /// indoorDeployment property.
    pub indoor_deployment: Option<bool>,
    /// latitude property.
    pub latitude: Option<f64>,
    /// longitude property.
    pub longitude: Option<f64>,
    /// verticalAccuracy property.
    pub vertical_accuracy: Option<f64>,
}

/// `SasPortalChannelWithScore` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SasPortalChannelWithScore {
    /// frequencyRange property.
    pub frequency_range: Option<SasPortalFrequencyRange>,
    /// score property.
    pub score: Option<f64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`sasportal.customers.deployments.devices.createSigned_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDeploymentsDevicesCreateSignedArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`sasportal.customers.devices.createSigned_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersDevicesCreateSignedArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`sasportal.customers.nodes.devices.createSigned_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalCustomersNodesDevicesCreateSignedArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`sasportal.nodes.deployments.devices.createSigned_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDeploymentsDevicesCreateSignedArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`sasportal.nodes.devices.createSigned_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesDevicesCreateSignedArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`sasportal.nodes.nodes.devices.createSigned_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SasportalNodesNodesDevicesCreateSignedArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned
// -----------------------------------------------------------------------------

/// POST v1alpha1/customers/{customersId}/deployments/{deploymentsId}/devices:createSigned.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sasportal_customers_deployments_devices_create_signed_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sasportal_customers_deployments_devices_create_signed_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{{customersId}}/deployments/{{deploymentsId}}/devices:createSigned",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SasPortalDevice =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1alpha1/customers/{customersId}/devices:createSigned
// -----------------------------------------------------------------------------

/// POST v1alpha1/customers/{customersId}/devices:createSigned.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sasportal_customers_devices_create_signed_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sasportal_customers_devices_create_signed_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{{customersId}}/devices:createSigned",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SasPortalDevice =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned
// -----------------------------------------------------------------------------

/// POST v1alpha1/customers/{customersId}/nodes/{nodesId}/devices:createSigned.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sasportal_customers_nodes_devices_create_signed_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sasportal_customers_nodes_devices_create_signed_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sasportal.googleapis.com/v1alpha1/customers/{{customersId}}/nodes/{{nodesId}}/devices:createSigned",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SasPortalDevice =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned
// -----------------------------------------------------------------------------

/// POST v1alpha1/nodes/{nodesId}/deployments/{deploymentsId}/devices:createSigned.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sasportal_nodes_deployments_devices_create_signed_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sasportal_nodes_deployments_devices_create_signed_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{{nodesId}}/deployments/{{deploymentsId}}/devices:createSigned",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SasPortalDevice =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1alpha1/nodes/{nodesId}/devices:createSigned
// -----------------------------------------------------------------------------

/// POST v1alpha1/nodes/{nodesId}/devices:createSigned.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sasportal_nodes_devices_create_signed_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sasportal_nodes_devices_create_signed_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{{nodesId}}/devices:createSigned",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SasPortalDevice =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned
// -----------------------------------------------------------------------------

/// POST v1alpha1/nodes/{nodesId}/nodes/{nodesId1}/devices:createSigned.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = sasportal_nodes_nodes_devices_create_signed_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn sasportal_nodes_nodes_devices_create_signed_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SasPortalDevice>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://sasportal.googleapis.com/v1alpha1/nodes/{{nodesId}}/nodes/{{nodesId1}}/devices:createSigned",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SasPortalDevice =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
