//! Auto-generated API module for gcp/businessprofileperformance locations.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_businessprofileperformance_locations `

#![cfg(feature = "gcp_businessprofileperformance_locations")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `FetchMultiDailyMetricsTimeSeriesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FetchMultiDailyMetricsTimeSeriesResponse {
    /// multiDailyMetricTimeSeries property.
    pub multi_daily_metric_time_series: Option<Vec<MultiDailyMetricTimeSeries>>,
}

/// `DailyMetricTimeSeries` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DailyMetricTimeSeries {
    /// dailyMetric property.
    pub daily_metric: Option<String>,
    /// dailySubEntityType property.
    pub daily_sub_entity_type: Option<DailySubEntityType>,
    /// timeSeries property.
    pub time_series: Option<TimeSeries>,
}

/// `DatedValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatedValue {
    /// date property.
    pub date: Option<Date>,
    /// value property.
    pub value: Option<String>,
}

/// `GetDailyMetricsTimeSeriesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GetDailyMetricsTimeSeriesResponse {
    /// timeSeries property.
    pub time_series: Option<TimeSeries>,
}

/// `TimeOfDay` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeOfDay {
    /// hours property.
    pub hours: Option<i64>,
    /// minutes property.
    pub minutes: Option<i64>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// seconds property.
    pub seconds: Option<i64>,
}

/// `TimeSeries` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeSeries {
    /// datedValues property.
    pub dated_values: Option<Vec<DatedValue>>,
}

/// `DailySubEntityType` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DailySubEntityType {
    /// dayOfWeek property.
    pub day_of_week: Option<String>,
    /// timeOfDay property.
    pub time_of_day: Option<TimeOfDay>,
}

/// `Date` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `MultiDailyMetricTimeSeries` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MultiDailyMetricTimeSeries {
    /// dailyMetricTimeSeries property.
    pub daily_metric_time_series: Option<Vec<DailyMetricTimeSeries>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`businessprofileperformance.locations.fetchMultiDailyMetricsTimeSeries_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BusinessprofileperformanceLocationsFetchMultiDailyMetricsTimeSeriesArgs {
    /// Path parameter: `location`.
    pub location: String,
    /// Query parameter: `dailyMetrics`.
    pub daily_metrics: Option<String>,
    /// Query parameter: `dailyRange.endDate.day`.
    pub daily_range_end_date_day: Option<String>,
    /// Query parameter: `dailyRange.endDate.month`.
    pub daily_range_end_date_month: Option<String>,
    /// Query parameter: `dailyRange.endDate.year`.
    pub daily_range_end_date_year: Option<String>,
    /// Query parameter: `dailyRange.startDate.day`.
    pub daily_range_start_date_day: Option<String>,
    /// Query parameter: `dailyRange.startDate.month`.
    pub daily_range_start_date_month: Option<String>,
    /// Query parameter: `dailyRange.startDate.year`.
    pub daily_range_start_date_year: Option<String>,
}

/// Arguments for [`businessprofileperformance.locations.getDailyMetricsTimeSeries_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BusinessprofileperformanceLocationsGetDailyMetricsTimeSeriesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `dailyMetric`.
    pub daily_metric: Option<String>,
    /// Query parameter: `dailyRange.endDate.day`.
    pub daily_range_end_date_day: Option<String>,
    /// Query parameter: `dailyRange.endDate.month`.
    pub daily_range_end_date_month: Option<String>,
    /// Query parameter: `dailyRange.endDate.year`.
    pub daily_range_end_date_year: Option<String>,
    /// Query parameter: `dailyRange.startDate.day`.
    pub daily_range_start_date_day: Option<String>,
    /// Query parameter: `dailyRange.startDate.month`.
    pub daily_range_start_date_month: Option<String>,
    /// Query parameter: `dailyRange.startDate.year`.
    pub daily_range_start_date_year: Option<String>,
    /// Query parameter: `dailySubEntityType.dayOfWeek`.
    pub daily_sub_entity_type_day_of_week: Option<String>,
    /// Query parameter: `dailySubEntityType.timeOfDay.hours`.
    pub daily_sub_entity_type_time_of_day_hours: Option<String>,
    /// Query parameter: `dailySubEntityType.timeOfDay.minutes`.
    pub daily_sub_entity_type_time_of_day_minutes: Option<String>,
    /// Query parameter: `dailySubEntityType.timeOfDay.nanos`.
    pub daily_sub_entity_type_time_of_day_nanos: Option<String>,
    /// Query parameter: `dailySubEntityType.timeOfDay.seconds`.
    pub daily_sub_entity_type_time_of_day_seconds: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/locations/{locationsId}:fetchMultiDailyMetricsTimeSeries
// -----------------------------------------------------------------------------

/// GET v1/locations/{locationsId}:fetchMultiDailyMetricsTimeSeries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn businessprofileperformance_locations_fetch_multi_daily_metrics_time_series_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<FetchMultiDailyMetricsTimeSeriesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://businessprofileperformance.googleapis.com/v1/locations/{{locationsId}}:fetchMultiDailyMetricsTimeSeries",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FetchMultiDailyMetricsTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/locations/{locationsId}:getDailyMetricsTimeSeries
// -----------------------------------------------------------------------------

/// GET v1/locations/{locationsId}:getDailyMetricsTimeSeries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = businessprofileperformance_locations_get_daily_metrics_time_series_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn businessprofileperformance_locations_get_daily_metrics_time_series_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetDailyMetricsTimeSeriesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://businessprofileperformance.googleapis.com/v1/locations/{{locationsId}}:getDailyMetricsTimeSeries",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GetDailyMetricsTimeSeriesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
