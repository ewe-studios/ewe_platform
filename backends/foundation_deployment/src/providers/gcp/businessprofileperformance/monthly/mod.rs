//! Auto-generated API module for gcp/businessprofileperformance monthly.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_businessprofileperformance_monthly `

#![cfg(feature = "gcp_businessprofileperformance_monthly")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ListSearchKeywordImpressionsMonthlyResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListSearchKeywordImpressionsMonthlyResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// searchKeywordsCounts property.
    pub search_keywords_counts: Option<Vec<SearchKeywordCount>>,
}

/// `SearchKeywordCount` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchKeywordCount {
    /// insightsValue property.
    pub insights_value: Option<InsightsValue>,
    /// searchKeyword property.
    pub search_keyword: Option<String>,
}

/// `InsightsValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InsightsValue {
    /// threshold property.
    pub threshold: Option<String>,
    /// value property.
    pub value: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`businessprofileperformance.locations.searchkeywords.impressions.monthly.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BusinessprofileperformanceLocationsSearchkeywordsImpressionsMonthlyListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `monthlyRange.endMonth.day`.
    pub monthly_range_end_month_day: Option<String>,
    /// Query parameter: `monthlyRange.endMonth.month`.
    pub monthly_range_end_month_month: Option<String>,
    /// Query parameter: `monthlyRange.endMonth.year`.
    pub monthly_range_end_month_year: Option<String>,
    /// Query parameter: `monthlyRange.startMonth.day`.
    pub monthly_range_start_month_day: Option<String>,
    /// Query parameter: `monthlyRange.startMonth.month`.
    pub monthly_range_start_month_month: Option<String>,
    /// Query parameter: `monthlyRange.startMonth.year`.
    pub monthly_range_start_month_year: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/locations/{locationsId}/searchkeywords/impressions/monthly
// -----------------------------------------------------------------------------

/// GET v1/locations/{locationsId}/searchkeywords/impressions/monthly.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = businessprofileperformance_locations_searchkeywords_impressions_monthly_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn businessprofileperformance_locations_searchkeywords_impressions_monthly_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListSearchKeywordImpressionsMonthlyResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://businessprofileperformance.googleapis.com/v1/locations/{{locationsId}}/searchkeywords/impressions/monthly",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListSearchKeywordImpressionsMonthlyResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
