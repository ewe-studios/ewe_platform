//! Auto-generated API module for gcp/apphub discoveredWorkloads:lookup.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_apphub_discovered_workloads_lookup `

#![cfg(feature = "gcp_apphub_discovered_workloads_lookup")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `FunctionalType` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FunctionalType {
    /// type property.
    pub r#type: Option<String>,
}

/// `LookupDiscoveredWorkloadResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LookupDiscoveredWorkloadResponse {
    /// discoveredWorkload property.
    pub discovered_workload: Option<DiscoveredWorkload>,
}

/// `DiscoveredWorkload` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DiscoveredWorkload {
    /// name property.
    pub name: Option<String>,
    /// workloadProperties property.
    pub workload_properties: Option<WorkloadProperties>,
    /// workloadReference property.
    pub workload_reference: Option<WorkloadReference>,
}

/// `WorkloadProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WorkloadProperties {
    /// extendedMetadata property.
    pub extended_metadata: Option<serde_json::Value>,
    /// functionalType property.
    pub functional_type: Option<FunctionalType>,
    /// gcpProject property.
    pub gcp_project: Option<String>,
    /// identity property.
    pub identity: Option<Identity>,
    /// location property.
    pub location: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `Identity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Identity {
    /// principal property.
    pub principal: Option<String>,
}

/// `WorkloadReference` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WorkloadReference {
    /// uri property.
    pub uri: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`apphub.projects.locations.discoveredWorkloads.lookup_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApphubProjectsLocationsDiscoveredWorkloadsLookupArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `uri`.
    pub uri: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/discoveredWorkloads:lookup
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveredWorkloads:lookup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = apphub_projects_locations_discovered_workloads_lookup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn apphub_projects_locations_discovered_workloads_lookup_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupDiscoveredWorkloadResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://apphub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/discoveredWorkloads:lookup",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LookupDiscoveredWorkloadResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
