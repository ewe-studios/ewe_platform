//! Auto-generated API module for gcp/apphub discoveredServices:lookup.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_apphub_discovered_services_lookup `

#![cfg(feature = "gcp_apphub_discovered_services_lookup")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ServiceReference` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ServiceReference {
    /// uri property.
    pub uri: Option<String>,
}

/// `FunctionalType` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FunctionalType {
    /// type property.
    pub r#type: Option<String>,
}

/// `Identity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Identity {
    /// principal property.
    pub principal: Option<String>,
}

/// `LookupDiscoveredServiceResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LookupDiscoveredServiceResponse {
    /// discoveredService property.
    pub discovered_service: Option<DiscoveredService>,
}

/// `RegistrationType` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RegistrationType {
    /// type property.
    pub r#type: Option<String>,
}

/// `ServiceProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ServiceProperties {
    /// extendedMetadata property.
    pub extended_metadata: Option<serde_json::Value>,
    /// functionalType property.
    pub functional_type: Option<FunctionalType>,
    /// gcpProject property.
    pub gcp_project: Option<String>,
    /// identity property.
    pub identity: Option<Identity>,
    /// location property.
    pub location: Option<String>,
    /// registrationType property.
    pub registration_type: Option<RegistrationType>,
    /// zone property.
    pub zone: Option<String>,
}

/// `DiscoveredService` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DiscoveredService {
    /// name property.
    pub name: Option<String>,
    /// serviceProperties property.
    pub service_properties: Option<ServiceProperties>,
    /// serviceReference property.
    pub service_reference: Option<ServiceReference>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`apphub.projects.locations.discoveredServices.lookup_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ApphubProjectsLocationsDiscoveredServicesLookupArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `uri`.
    pub uri: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/discoveredServices:lookup
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/discoveredServices:lookup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = apphub_projects_locations_discovered_services_lookup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn apphub_projects_locations_discovered_services_lookup_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupDiscoveredServiceResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://apphub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/discoveredServices:lookup",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LookupDiscoveredServiceResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
