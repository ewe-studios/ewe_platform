//! Auto-generated API module for gcp/books layers.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_books_layers `

#![cfg(feature = "gcp_books_layers")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Volumeannotations` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Volumeannotations {
    /// items property.
    pub items: Option<Vec<Volumeannotation>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalItems property.
    pub total_items: Option<i64>,
    /// version property.
    pub version: Option<String>,
}

/// `Volumeannotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Volumeannotation {
    /// annotationDataId property.
    pub annotation_data_id: Option<String>,
    /// annotationDataLink property.
    pub annotation_data_link: Option<String>,
    /// annotationType property.
    pub annotation_type: Option<String>,
    /// contentRanges property.
    pub content_ranges: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// data property.
    pub data: Option<String>,
    /// deleted property.
    pub deleted: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// layerId property.
    pub layer_id: Option<String>,
    /// pageIds property.
    pub page_ids: Option<Vec<String>>,
    /// selectedText property.
    pub selected_text: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `BooksAnnotationsRange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BooksAnnotationsRange {
    /// endOffset property.
    pub end_offset: Option<String>,
    /// endPosition property.
    pub end_position: Option<String>,
    /// startOffset property.
    pub start_offset: Option<String>,
    /// startPosition property.
    pub start_position: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`books.layers.volumeAnnotations.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksLayersVolumeAnnotationsListArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Path parameter: `layerId`.
    pub layer_id: String,
    /// Query parameter: `contentVersion`.
    pub content_version: Option<String>,
    /// Query parameter: `endOffset`.
    pub end_offset: Option<String>,
    /// Query parameter: `endPosition`.
    pub end_position: Option<String>,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `showDeleted`.
    pub show_deleted: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `startOffset`.
    pub start_offset: Option<String>,
    /// Query parameter: `startPosition`.
    pub start_position: Option<String>,
    /// Query parameter: `updatedMax`.
    pub updated_max: Option<String>,
    /// Query parameter: `updatedMin`.
    pub updated_min: Option<String>,
    /// Query parameter: `volumeAnnotationsVersion`.
    pub volume_annotations_version: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET books/v1/volumes/{volumeId}/layers/{layerId}
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/{volumeId}/layers/{layerId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_layers_volume_annotations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_layers_volume_annotations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksLayersVolumeAnnotationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumeannotations>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layers/{}",
        args.volume_id, args.layer_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumeannotations =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
