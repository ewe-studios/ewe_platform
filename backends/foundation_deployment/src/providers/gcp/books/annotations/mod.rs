//! Auto-generated API module for gcp/books annotations.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_books_annotations `

#![cfg(feature = "gcp_books_annotations")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `BooksAnnotationsRange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BooksAnnotationsRange {
    /// endOffset property.
    pub end_offset: Option<String>,
    /// endPosition property.
    pub end_position: Option<String>,
    /// startOffset property.
    pub start_offset: Option<String>,
    /// startPosition property.
    pub start_position: Option<String>,
}

/// `Annotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Annotation {
    /// afterSelectedText property.
    pub after_selected_text: Option<String>,
    /// beforeSelectedText property.
    pub before_selected_text: Option<String>,
    /// clientVersionRanges property.
    pub client_version_ranges: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// created property.
    pub created: Option<String>,
    /// currentVersionRanges property.
    pub current_version_ranges: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// data property.
    pub data: Option<String>,
    /// deleted property.
    pub deleted: Option<bool>,
    /// highlightStyle property.
    pub highlight_style: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// layerId property.
    pub layer_id: Option<String>,
    /// layerSummary property.
    pub layer_summary: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// pageIds property.
    pub page_ids: Option<Vec<String>>,
    /// selectedText property.
    pub selected_text: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `Annotations` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Annotations {
    /// items property.
    pub items: Option<Vec<Annotation>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalItems property.
    pub total_items: Option<i64>,
}

/// `Volumeannotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Volumeannotation {
    /// annotationDataId property.
    pub annotation_data_id: Option<String>,
    /// annotationDataLink property.
    pub annotation_data_link: Option<String>,
    /// annotationType property.
    pub annotation_type: Option<String>,
    /// contentRanges property.
    pub content_ranges: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// data property.
    pub data: Option<String>,
    /// deleted property.
    pub deleted: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// layerId property.
    pub layer_id: Option<String>,
    /// pageIds property.
    pub page_ids: Option<Vec<String>>,
    /// selectedText property.
    pub selected_text: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`books.layers.volumeAnnotations.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksLayersVolumeAnnotationsGetArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Path parameter: `layerId`.
    pub layer_id: String,
    /// Path parameter: `annotationId`.
    pub annotation_id: String,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.mylibrary.annotations.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsDeleteArgs {
    /// Path parameter: `annotationId`.
    pub annotation_id: String,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.mylibrary.annotations.insert_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsInsertArgs {
    /// Query parameter: `annotationId`.
    pub annotation_id: Option<String>,
    /// Query parameter: `country`.
    pub country: Option<String>,
    /// Query parameter: `showOnlySummaryInResponse`.
    pub show_only_summary_in_response: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.mylibrary.annotations.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsListArgs {
    /// Query parameter: `contentVersion`.
    pub content_version: Option<String>,
    /// Query parameter: `layerId`.
    pub layer_id: Option<String>,
    /// Query parameter: `layerIds`.
    pub layer_ids: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `showDeleted`.
    pub show_deleted: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `updatedMax`.
    pub updated_max: Option<String>,
    /// Query parameter: `updatedMin`.
    pub updated_min: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`books.mylibrary.annotations.update_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsUpdateArgs {
    /// Path parameter: `annotationId`.
    pub annotation_id: String,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET books/v1/volumes/{volumeId}/layers/{layerId}/annotations/{annotationId}
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/annotations/{annotationId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_layers_volume_annotations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_layers_volume_annotations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksLayersVolumeAnnotationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumeannotation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layers/{}/annotations/{}",
        args.volume_id, args.layer_id, args.annotation_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumeannotation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE books/v1/mylibrary/annotations/{annotationId}
// -----------------------------------------------------------------------------

/// DELETE books/v1/mylibrary/annotations/{annotationId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_annotations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_annotations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryAnnotationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/annotations/{}",
        args.annotation_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/mylibrary/annotations
// -----------------------------------------------------------------------------

/// POST books/v1/mylibrary/annotations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_annotations_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_annotations_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Annotation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/mylibrary/annotations",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Annotation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/mylibrary/annotations
// -----------------------------------------------------------------------------

/// GET books/v1/mylibrary/annotations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_annotations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_annotations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Annotations>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/mylibrary/annotations",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Annotations =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT books/v1/mylibrary/annotations/{annotationId}
// -----------------------------------------------------------------------------

/// PUT books/v1/mylibrary/annotations/{annotationId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_annotations_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_annotations_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryAnnotationsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Annotation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/annotations/{}",
        args.annotation_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Annotation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
