//! Auto-generated API module for gcp/books get.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_books_get `

#![cfg(feature = "gcp_books_get")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Series` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Series {
    /// kind property.
    pub kind: Option<String>,
    /// series property.
    pub series: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

/// `Seriesmembership` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Seriesmembership {
    /// kind property.
    pub kind: Option<String>,
    /// member property.
    pub member: Option<Vec<Volume>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `DownloadAccessRestriction` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DownloadAccessRestriction {
    /// deviceAllowed property.
    pub device_allowed: Option<bool>,
    /// downloadsAcquired property.
    pub downloads_acquired: Option<i64>,
    /// justAcquired property.
    pub just_acquired: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// maxDownloadDevices property.
    pub max_download_devices: Option<i64>,
    /// message property.
    pub message: Option<String>,
    /// nonce property.
    pub nonce: Option<String>,
    /// reasonCode property.
    pub reason_code: Option<String>,
    /// restricted property.
    pub restricted: Option<bool>,
    /// signature property.
    pub signature: Option<String>,
    /// source property.
    pub source: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `Offers` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Offers {
    /// items property.
    pub items: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Notification` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Notification {
    /// body property.
    pub body: Option<String>,
    /// crmExperimentIds property.
    pub crm_experiment_ids: Option<Vec<String>>,
    /// doc_id property.
    pub doc_id: Option<String>,
    /// doc_type property.
    pub doc_type: Option<String>,
    /// dont_show_notification property.
    pub dont_show_notification: Option<bool>,
    /// iconUrl property.
    pub icon_url: Option<String>,
    /// is_document_mature property.
    pub is_document_mature: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// notificationGroup property.
    pub notification_group: Option<String>,
    /// notification_type property.
    pub notification_type: Option<String>,
    /// pcampaign_id property.
    pub pcampaign_id: Option<String>,
    /// reason property.
    pub reason: Option<String>,
    /// show_notification_settings_action property.
    pub show_notification_settings_action: Option<bool>,
    /// targetUrl property.
    pub target_url: Option<String>,
    /// timeToExpireMs property.
    pub time_to_expire_ms: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `ReadingPosition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReadingPosition {
    /// epubCfiPosition property.
    pub epub_cfi_position: Option<String>,
    /// gbImagePosition property.
    pub gb_image_position: Option<String>,
    /// gbTextPosition property.
    pub gb_text_position: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// pdfPosition property.
    pub pdf_position: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `Discoveryclusters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Discoveryclusters {
    /// clusters property.
    pub clusters: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// kind property.
    pub kind: Option<String>,
    /// totalClusters property.
    pub total_clusters: Option<i64>,
}

/// `Volume` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Volume {
    /// accessInfo property.
    pub access_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// layerInfo property.
    pub layer_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// recommendedInfo property.
    pub recommended_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// saleInfo property.
    pub sale_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// searchInfo property.
    pub search_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// userInfo property.
    pub user_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// volumeInfo property.
    pub volume_info: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Review` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Review {
    /// author property.
    pub author: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// content property.
    pub content: Option<String>,
    /// date property.
    pub date: Option<String>,
    /// fullTextUrl property.
    pub full_text_url: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// rating property.
    pub rating: Option<String>,
    /// source property.
    pub source: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// title property.
    pub title: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `Volumeseriesinfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Volumeseriesinfo {
    /// bookDisplayNumber property.
    pub book_display_number: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// shortSeriesBookTitle property.
    pub short_series_book_title: Option<String>,
    /// volumeSeries property.
    pub volume_series: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`books.notification.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksNotificationGetArgs {
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `notification_id`.
    pub notification_id: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.personalizedstream.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksPersonalizedstreamGetArgs {
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `maxAllowedMaturityRating`.
    pub max_allowed_maturity_rating: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.promooffer.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksPromoofferGetArgs {
    /// Query parameter: `androidId`.
    pub android_id: Option<String>,
    /// Query parameter: `device`.
    pub device: Option<String>,
    /// Query parameter: `manufacturer`.
    pub manufacturer: Option<String>,
    /// Query parameter: `model`.
    pub model: Option<String>,
    /// Query parameter: `product`.
    pub product: Option<String>,
    /// Query parameter: `serial`.
    pub serial: Option<String>,
}

/// Arguments for [`books.series.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksSeriesGetArgs {
    /// Query parameter: `series_id`.
    pub series_id: Option<String>,
}

/// Arguments for [`books.series.membership.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BooksSeriesMembershipGetArgs {
    /// Query parameter: `page_size`.
    pub page_size: Option<String>,
    /// Query parameter: `page_token`.
    pub page_token: Option<String>,
    /// Query parameter: `series_id`.
    pub series_id: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET books/v1/notification/get
// -----------------------------------------------------------------------------

/// GET books/v1/notification/get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_notification_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_notification_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Notification>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/notification/get",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Notification =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/personalizedstream/get
// -----------------------------------------------------------------------------

/// GET books/v1/personalizedstream/get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_personalizedstream_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_personalizedstream_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Discoveryclusters>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/personalizedstream/get",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Discoveryclusters =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/promooffer/get
// -----------------------------------------------------------------------------

/// GET books/v1/promooffer/get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_promooffer_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_promooffer_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Offers>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/promooffer/get",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Offers =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/series/get
// -----------------------------------------------------------------------------

/// GET books/v1/series/get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_series_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_series_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Series>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/series/get",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Series =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/series/membership/get
// -----------------------------------------------------------------------------

/// GET books/v1/series/membership/get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_series_membership_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_series_membership_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Seriesmembership>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/series/membership/get",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Seriesmembership =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
