//! Auto-generated API module for gcp/books books.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_books_books `

#![cfg(feature = "gcp_books_books")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Notification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Notification {
    /// body property.
    pub body: Option<String>,
    /// crmExperimentIds property.
    pub crm_experiment_ids: Option<Vec<String>>,
    /// doc_id property.
    pub doc_id: Option<String>,
    /// doc_type property.
    pub doc_type: Option<String>,
    /// dont_show_notification property.
    pub dont_show_notification: Option<bool>,
    /// iconUrl property.
    pub icon_url: Option<String>,
    /// is_document_mature property.
    pub is_document_mature: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// notificationGroup property.
    pub notification_group: Option<String>,
    /// notification_type property.
    pub notification_type: Option<String>,
    /// pcampaign_id property.
    pub pcampaign_id: Option<String>,
    /// reason property.
    pub reason: Option<String>,
    /// show_notification_settings_action property.
    pub show_notification_settings_action: Option<bool>,
    /// targetUrl property.
    pub target_url: Option<String>,
    /// timeToExpireMs property.
    pub time_to_expire_ms: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `DownloadAccesses` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DownloadAccesses {
    /// downloadAccessList property.
    pub download_access_list: Option<Vec<DownloadAccessRestriction>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Bookshelf` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Bookshelf {
    /// access property.
    pub access: Option<String>,
    /// created property.
    pub created: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// volumeCount property.
    pub volume_count: Option<i64>,
    /// volumesLastUpdated property.
    pub volumes_last_updated: Option<String>,
}

/// `BooksAnnotationsRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BooksAnnotationsRange {
    /// endOffset property.
    pub end_offset: Option<String>,
    /// endPosition property.
    pub end_position: Option<String>,
    /// startOffset property.
    pub start_offset: Option<String>,
    /// startPosition property.
    pub start_position: Option<String>,
}

/// `BooksVolumesRecommendedRateResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BooksVolumesRecommendedRateResponse {
    /// consistency_token property.
    pub consistency_token: Option<String>,
}

/// `Annotations` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Annotations {
    /// items property.
    pub items: Option<Vec<Annotation>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalItems property.
    pub total_items: Option<i64>,
}

/// `Layersummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Layersummary {
    /// annotationCount property.
    pub annotation_count: Option<i64>,
    /// annotationTypes property.
    pub annotation_types: Option<Vec<String>>,
    /// annotationsDataLink property.
    pub annotations_data_link: Option<String>,
    /// annotationsLink property.
    pub annotations_link: Option<String>,
    /// contentVersion property.
    pub content_version: Option<String>,
    /// dataCount property.
    pub data_count: Option<i64>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// layerId property.
    pub layer_id: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// volumeAnnotationsVersion property.
    pub volume_annotations_version: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `Volumeannotations` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Volumeannotations {
    /// items property.
    pub items: Option<Vec<Volumeannotation>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalItems property.
    pub total_items: Option<i64>,
    /// version property.
    pub version: Option<String>,
}

/// `FamilyInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FamilyInfo {
    /// kind property.
    pub kind: Option<String>,
    /// membership property.
    pub membership: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Metadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metadata {
    /// items property.
    pub items: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Annotation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Annotation {
    /// afterSelectedText property.
    pub after_selected_text: Option<String>,
    /// beforeSelectedText property.
    pub before_selected_text: Option<String>,
    /// clientVersionRanges property.
    pub client_version_ranges: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// created property.
    pub created: Option<String>,
    /// currentVersionRanges property.
    pub current_version_ranges: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// data property.
    pub data: Option<String>,
    /// deleted property.
    pub deleted: Option<bool>,
    /// highlightStyle property.
    pub highlight_style: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// layerId property.
    pub layer_id: Option<String>,
    /// layerSummary property.
    pub layer_summary: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// pageIds property.
    pub page_ids: Option<Vec<String>>,
    /// selectedText property.
    pub selected_text: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `RequestAccessData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RequestAccessData {
    /// concurrentAccess property.
    pub concurrent_access: Option<ConcurrentAccessRestriction>,
    /// downloadAccess property.
    pub download_access: Option<DownloadAccessRestriction>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Volume2` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Volume2 {
    /// items property.
    pub items: Option<Vec<Volume>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Seriesmembership` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Seriesmembership {
    /// kind property.
    pub kind: Option<String>,
    /// member property.
    pub member: Option<Vec<Volume>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Dictlayerdata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Dictlayerdata {
    /// common property.
    pub common: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// dict property.
    pub dict: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Geolayerdata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Geolayerdata {
    /// common property.
    pub common: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// geo property.
    pub geo: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Layersummaries` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Layersummaries {
    /// items property.
    pub items: Option<Vec<Layersummary>>,
    /// kind property.
    pub kind: Option<String>,
    /// totalItems property.
    pub total_items: Option<i64>,
}

/// `Offers` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Offers {
    /// items property.
    pub items: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Usersettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Usersettings {
    /// kind property.
    pub kind: Option<String>,
    /// notesExport property.
    pub notes_export: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// notification property.
    pub notification: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Bookshelves` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Bookshelves {
    /// items property.
    pub items: Option<Vec<Bookshelf>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `Annotationsdata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Annotationsdata {
    /// items property.
    pub items: Option<Vec<GeoAnnotationdata>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// totalItems property.
    pub total_items: Option<i64>,
}

/// `Volumeannotation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Volumeannotation {
    /// annotationDataId property.
    pub annotation_data_id: Option<String>,
    /// annotationDataLink property.
    pub annotation_data_link: Option<String>,
    /// annotationType property.
    pub annotation_type: Option<String>,
    /// contentRanges property.
    pub content_ranges: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// data property.
    pub data: Option<String>,
    /// deleted property.
    pub deleted: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// layerId property.
    pub layer_id: Option<String>,
    /// pageIds property.
    pub page_ids: Option<Vec<String>>,
    /// selectedText property.
    pub selected_text: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `AnnotationsSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnnotationsSummary {
    /// kind property.
    pub kind: Option<String>,
    /// layers property.
    pub layers: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

/// `ReadingPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReadingPosition {
    /// epubCfiPosition property.
    pub epub_cfi_position: Option<String>,
    /// gbImagePosition property.
    pub gb_image_position: Option<String>,
    /// gbTextPosition property.
    pub gb_text_position: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// pdfPosition property.
    pub pdf_position: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `Review` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Review {
    /// author property.
    pub author: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// content property.
    pub content: Option<String>,
    /// date property.
    pub date: Option<String>,
    /// fullTextUrl property.
    pub full_text_url: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// rating property.
    pub rating: Option<String>,
    /// source property.
    pub source: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// title property.
    pub title: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `Series` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Series {
    /// kind property.
    pub kind: Option<String>,
    /// series property.
    pub series: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

/// `Discoveryclusters` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Discoveryclusters {
    /// clusters property.
    pub clusters: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// kind property.
    pub kind: Option<String>,
    /// totalClusters property.
    pub total_clusters: Option<i64>,
}

/// `DownloadAccessRestriction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DownloadAccessRestriction {
    /// deviceAllowed property.
    pub device_allowed: Option<bool>,
    /// downloadsAcquired property.
    pub downloads_acquired: Option<i64>,
    /// justAcquired property.
    pub just_acquired: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// maxDownloadDevices property.
    pub max_download_devices: Option<i64>,
    /// message property.
    pub message: Option<String>,
    /// nonce property.
    pub nonce: Option<String>,
    /// reasonCode property.
    pub reason_code: Option<String>,
    /// restricted property.
    pub restricted: Option<bool>,
    /// signature property.
    pub signature: Option<String>,
    /// source property.
    pub source: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `Category` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Category {
    /// items property.
    pub items: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `DictionaryAnnotationdata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DictionaryAnnotationdata {
    /// annotationType property.
    pub annotation_type: Option<String>,
    /// data property.
    pub data: Option<Dictlayerdata>,
    /// encodedData property.
    pub encoded_data: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// layerId property.
    pub layer_id: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `ConcurrentAccessRestriction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConcurrentAccessRestriction {
    /// deviceAllowed property.
    pub device_allowed: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// maxConcurrentDevices property.
    pub max_concurrent_devices: Option<i64>,
    /// message property.
    pub message: Option<String>,
    /// nonce property.
    pub nonce: Option<String>,
    /// reasonCode property.
    pub reason_code: Option<String>,
    /// restricted property.
    pub restricted: Option<bool>,
    /// signature property.
    pub signature: Option<String>,
    /// source property.
    pub source: Option<String>,
    /// timeWindowSeconds property.
    pub time_window_seconds: Option<i64>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `BooksCloudloadingResource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BooksCloudloadingResource {
    /// author property.
    pub author: Option<String>,
    /// processingState property.
    pub processing_state: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

/// `Volume` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Volume {
    /// accessInfo property.
    pub access_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// layerInfo property.
    pub layer_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// recommendedInfo property.
    pub recommended_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// saleInfo property.
    pub sale_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// searchInfo property.
    pub search_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// userInfo property.
    pub user_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// volumeInfo property.
    pub volume_info: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Volumeseriesinfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Volumeseriesinfo {
    /// bookDisplayNumber property.
    pub book_display_number: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// shortSeriesBookTitle property.
    pub short_series_book_title: Option<String>,
    /// volumeSeries property.
    pub volume_series: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

/// `Volumes` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Volumes {
    /// items property.
    pub items: Option<Vec<Volume>>,
    /// kind property.
    pub kind: Option<String>,
    /// totalItems property.
    pub total_items: Option<i64>,
}

/// `GeoAnnotationdata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GeoAnnotationdata {
    /// annotationType property.
    pub annotation_type: Option<String>,
    /// data property.
    pub data: Option<Geolayerdata>,
    /// encodedData property.
    pub encoded_data: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// layerId property.
    pub layer_id: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// volumeId property.
    pub volume_id: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`books.bookshelves.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksBookshelvesGetArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `shelf`.
    pub shelf: String,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.bookshelves.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksBookshelvesListArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.bookshelves.volumes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksBookshelvesVolumesListArgs {
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `shelf`.
    pub shelf: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `showPreorders`.
    pub show_preorders: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `startIndex`.
    pub start_index: Option<String>,
}

/// Arguments for [`books.cloudloading.addBook_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksCloudloadingAddBookArgs {
    /// Query parameter: `drive_document_id`.
    pub drive_document_id: Option<String>,
    /// Query parameter: `mime_type`.
    pub mime_type: Option<String>,
    /// Query parameter: `name`.
    pub name: Option<String>,
    /// Query parameter: `upload_client_token`.
    pub upload_client_token: Option<String>,
}

/// Arguments for [`books.cloudloading.deleteBook_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksCloudloadingDeleteBookArgs {
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`books.cloudloading.updateBook_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksCloudloadingUpdateBookArgs {
    /// Request body.
    pub body: BooksCloudloadingResource,
}

/// Arguments for [`books.dictionary.listOfflineMetadata_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksDictionaryListOfflineMetadataArgs {
    /// Query parameter: `cpksver`.
    pub cpksver: Option<String>,
}

/// Arguments for [`books.familysharing.getFamilyInfo_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksFamilysharingGetFamilyInfoArgs {
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.familysharing.share_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksFamilysharingShareArgs {
    /// Query parameter: `docId`.
    pub doc_id: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`books.familysharing.unshare_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksFamilysharingUnshareArgs {
    /// Query parameter: `docId`.
    pub doc_id: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`books.layers.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksLayersGetArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Path parameter: `summaryId`.
    pub summary_id: String,
    /// Query parameter: `contentVersion`.
    pub content_version: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.layers.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksLayersListArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Query parameter: `contentVersion`.
    pub content_version: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.layers.annotationData.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksLayersAnnotationDataGetArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Path parameter: `layerId`.
    pub layer_id: String,
    /// Path parameter: `annotationDataId`.
    pub annotation_data_id: String,
    /// Query parameter: `allowWebDefinitions`.
    pub allow_web_definitions: Option<String>,
    /// Query parameter: `contentVersion`.
    pub content_version: Option<String>,
    /// Query parameter: `h`.
    pub h: Option<String>,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `scale`.
    pub scale: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `w`.
    pub w: Option<String>,
}

/// Arguments for [`books.layers.annotationData.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksLayersAnnotationDataListArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Path parameter: `layerId`.
    pub layer_id: String,
    /// Query parameter: `annotationDataId`.
    pub annotation_data_id: Option<String>,
    /// Query parameter: `contentVersion`.
    pub content_version: Option<String>,
    /// Query parameter: `h`.
    pub h: Option<String>,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `scale`.
    pub scale: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `updatedMax`.
    pub updated_max: Option<String>,
    /// Query parameter: `updatedMin`.
    pub updated_min: Option<String>,
    /// Query parameter: `w`.
    pub w: Option<String>,
}

/// Arguments for [`books.layers.volumeAnnotations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksLayersVolumeAnnotationsGetArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Path parameter: `layerId`.
    pub layer_id: String,
    /// Path parameter: `annotationId`.
    pub annotation_id: String,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.layers.volumeAnnotations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksLayersVolumeAnnotationsListArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Path parameter: `layerId`.
    pub layer_id: String,
    /// Query parameter: `contentVersion`.
    pub content_version: Option<String>,
    /// Query parameter: `endOffset`.
    pub end_offset: Option<String>,
    /// Query parameter: `endPosition`.
    pub end_position: Option<String>,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `showDeleted`.
    pub show_deleted: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `startOffset`.
    pub start_offset: Option<String>,
    /// Query parameter: `startPosition`.
    pub start_position: Option<String>,
    /// Query parameter: `updatedMax`.
    pub updated_max: Option<String>,
    /// Query parameter: `updatedMin`.
    pub updated_min: Option<String>,
    /// Query parameter: `volumeAnnotationsVersion`.
    pub volume_annotations_version: Option<String>,
}

/// Arguments for [`books.myconfig.getUserSettings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMyconfigGetUserSettingsArgs {
    /// Query parameter: `country`.
    pub country: Option<String>,
}

/// Arguments for [`books.myconfig.releaseDownloadAccess_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMyconfigReleaseDownloadAccessArgs {
    /// Query parameter: `cpksver`.
    pub cpksver: Option<String>,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `volumeIds`.
    pub volume_ids: Option<String>,
}

/// Arguments for [`books.myconfig.requestAccess_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMyconfigRequestAccessArgs {
    /// Query parameter: `cpksver`.
    pub cpksver: Option<String>,
    /// Query parameter: `licenseTypes`.
    pub license_types: Option<String>,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `nonce`.
    pub nonce: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`books.myconfig.syncVolumeLicenses_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMyconfigSyncVolumeLicensesArgs {
    /// Query parameter: `cpksver`.
    pub cpksver: Option<String>,
    /// Query parameter: `features`.
    pub features: Option<String>,
    /// Query parameter: `includeNonComicsSeries`.
    pub include_non_comics_series: Option<String>,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `nonce`.
    pub nonce: Option<String>,
    /// Query parameter: `showPreorders`.
    pub show_preorders: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `volumeIds`.
    pub volume_ids: Option<String>,
}

/// Arguments for [`books.myconfig.updateUserSettings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMyconfigUpdateUserSettingsArgs {
    /// Request body.
    pub body: Usersettings,
}

/// Arguments for [`books.mylibrary.annotations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsDeleteArgs {
    /// Path parameter: `annotationId`.
    pub annotation_id: String,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.mylibrary.annotations.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsInsertArgs {
    /// Query parameter: `annotationId`.
    pub annotation_id: Option<String>,
    /// Query parameter: `country`.
    pub country: Option<String>,
    /// Query parameter: `showOnlySummaryInResponse`.
    pub show_only_summary_in_response: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Request body.
    pub body: Annotation,
}

/// Arguments for [`books.mylibrary.annotations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsListArgs {
    /// Query parameter: `contentVersion`.
    pub content_version: Option<String>,
    /// Query parameter: `layerId`.
    pub layer_id: Option<String>,
    /// Query parameter: `layerIds`.
    pub layer_ids: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `showDeleted`.
    pub show_deleted: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `updatedMax`.
    pub updated_max: Option<String>,
    /// Query parameter: `updatedMin`.
    pub updated_min: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`books.mylibrary.annotations.summary_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsSummaryArgs {
    /// Query parameter: `layerIds`.
    pub layer_ids: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`books.mylibrary.annotations.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryAnnotationsUpdateArgs {
    /// Path parameter: `annotationId`.
    pub annotation_id: String,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Request body.
    pub body: Annotation,
}

/// Arguments for [`books.mylibrary.bookshelves.addVolume_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesAddVolumeArgs {
    /// Path parameter: `shelf`.
    pub shelf: String,
    /// Query parameter: `reason`.
    pub reason: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`books.mylibrary.bookshelves.clearVolumes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesClearVolumesArgs {
    /// Path parameter: `shelf`.
    pub shelf: String,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.mylibrary.bookshelves.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesGetArgs {
    /// Path parameter: `shelf`.
    pub shelf: String,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.mylibrary.bookshelves.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesListArgs {
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.mylibrary.bookshelves.moveVolume_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesMoveVolumeArgs {
    /// Path parameter: `shelf`.
    pub shelf: String,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
    /// Query parameter: `volumePosition`.
    pub volume_position: Option<String>,
}

/// Arguments for [`books.mylibrary.bookshelves.removeVolume_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesRemoveVolumeArgs {
    /// Path parameter: `shelf`.
    pub shelf: String,
    /// Query parameter: `reason`.
    pub reason: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`books.mylibrary.bookshelves.volumes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryBookshelvesVolumesListArgs {
    /// Path parameter: `shelf`.
    pub shelf: String,
    /// Query parameter: `country`.
    pub country: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `q`.
    pub q: Option<String>,
    /// Query parameter: `showPreorders`.
    pub show_preorders: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `startIndex`.
    pub start_index: Option<String>,
}

/// Arguments for [`books.mylibrary.readingpositions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryReadingpositionsGetArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Query parameter: `contentVersion`.
    pub content_version: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.mylibrary.readingpositions.setPosition_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksMylibraryReadingpositionsSetPositionArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Query parameter: `action`.
    pub action: Option<String>,
    /// Query parameter: `contentVersion`.
    pub content_version: Option<String>,
    /// Query parameter: `deviceCookie`.
    pub device_cookie: Option<String>,
    /// Query parameter: `position`.
    pub position: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `timestamp`.
    pub timestamp: Option<String>,
}

/// Arguments for [`books.notification.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksNotificationGetArgs {
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `notification_id`.
    pub notification_id: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.onboarding.listCategories_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksOnboardingListCategoriesArgs {
    /// Query parameter: `locale`.
    pub locale: Option<String>,
}

/// Arguments for [`books.onboarding.listCategoryVolumes_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksOnboardingListCategoryVolumesArgs {
    /// Query parameter: `categoryId`.
    pub category_id: Option<String>,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `maxAllowedMaturityRating`.
    pub max_allowed_maturity_rating: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`books.personalizedstream.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksPersonalizedstreamGetArgs {
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `maxAllowedMaturityRating`.
    pub max_allowed_maturity_rating: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.promooffer.accept_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksPromoofferAcceptArgs {
    /// Query parameter: `androidId`.
    pub android_id: Option<String>,
    /// Query parameter: `device`.
    pub device: Option<String>,
    /// Query parameter: `manufacturer`.
    pub manufacturer: Option<String>,
    /// Query parameter: `model`.
    pub model: Option<String>,
    /// Query parameter: `offerId`.
    pub offer_id: Option<String>,
    /// Query parameter: `product`.
    pub product: Option<String>,
    /// Query parameter: `serial`.
    pub serial: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`books.promooffer.dismiss_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksPromoofferDismissArgs {
    /// Query parameter: `androidId`.
    pub android_id: Option<String>,
    /// Query parameter: `device`.
    pub device: Option<String>,
    /// Query parameter: `manufacturer`.
    pub manufacturer: Option<String>,
    /// Query parameter: `model`.
    pub model: Option<String>,
    /// Query parameter: `offerId`.
    pub offer_id: Option<String>,
    /// Query parameter: `product`.
    pub product: Option<String>,
    /// Query parameter: `serial`.
    pub serial: Option<String>,
}

/// Arguments for [`books.promooffer.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksPromoofferGetArgs {
    /// Query parameter: `androidId`.
    pub android_id: Option<String>,
    /// Query parameter: `device`.
    pub device: Option<String>,
    /// Query parameter: `manufacturer`.
    pub manufacturer: Option<String>,
    /// Query parameter: `model`.
    pub model: Option<String>,
    /// Query parameter: `product`.
    pub product: Option<String>,
    /// Query parameter: `serial`.
    pub serial: Option<String>,
}

/// Arguments for [`books.series.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksSeriesGetArgs {
    /// Query parameter: `series_id`.
    pub series_id: Option<String>,
}

/// Arguments for [`books.series.membership.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksSeriesMembershipGetArgs {
    /// Query parameter: `page_size`.
    pub page_size: Option<String>,
    /// Query parameter: `page_token`.
    pub page_token: Option<String>,
    /// Query parameter: `series_id`.
    pub series_id: Option<String>,
}

/// Arguments for [`books.volumes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksVolumesGetArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Query parameter: `country`.
    pub country: Option<String>,
    /// Query parameter: `includeNonComicsSeries`.
    pub include_non_comics_series: Option<String>,
    /// Query parameter: `partner`.
    pub partner: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `user_library_consistent_read`.
    pub user_library_consistent_read: Option<String>,
}

/// Arguments for [`books.volumes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksVolumesListArgs {
    /// Query parameter: `download`.
    pub download: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `langRestrict`.
    pub lang_restrict: Option<String>,
    /// Query parameter: `libraryRestrict`.
    pub library_restrict: Option<String>,
    /// Query parameter: `maxAllowedMaturityRating`.
    pub max_allowed_maturity_rating: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `partner`.
    pub partner: Option<String>,
    /// Query parameter: `printType`.
    pub print_type: Option<String>,
    /// Query parameter: `projection`.
    pub projection: Option<String>,
    /// Query parameter: `q`.
    pub q: Option<String>,
    /// Query parameter: `showPreorders`.
    pub show_preorders: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `startIndex`.
    pub start_index: Option<String>,
}

/// Arguments for [`books.volumes.associated.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksVolumesAssociatedListArgs {
    /// Path parameter: `volumeId`.
    pub volume_id: String,
    /// Query parameter: `association`.
    pub association: Option<String>,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `maxAllowedMaturityRating`.
    pub max_allowed_maturity_rating: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.volumes.mybooks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksVolumesMybooksListArgs {
    /// Query parameter: `acquireMethod`.
    pub acquire_method: Option<String>,
    /// Query parameter: `country`.
    pub country: Option<String>,
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `processingState`.
    pub processing_state: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `startIndex`.
    pub start_index: Option<String>,
}

/// Arguments for [`books.volumes.recommended.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksVolumesRecommendedListArgs {
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `maxAllowedMaturityRating`.
    pub max_allowed_maturity_rating: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
}

/// Arguments for [`books.volumes.recommended.rate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksVolumesRecommendedRateArgs {
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `rating`.
    pub rating: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

/// Arguments for [`books.volumes.useruploaded.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct BooksVolumesUseruploadedListArgs {
    /// Query parameter: `locale`.
    pub locale: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `processingState`.
    pub processing_state: Option<String>,
    /// Query parameter: `source`.
    pub source: Option<String>,
    /// Query parameter: `startIndex`.
    pub start_index: Option<String>,
    /// Query parameter: `volumeId`.
    pub volume_id: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET books/v1/users/{userId}/bookshelves/{shelf}
// -----------------------------------------------------------------------------

/// GET books/v1/users/{userId}/bookshelves/{shelf}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_bookshelves_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_bookshelves_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksBookshelvesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bookshelf>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/users/{}/bookshelves/{}",
        args.user_id, args.shelf,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Bookshelf =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/users/{userId}/bookshelves
// -----------------------------------------------------------------------------

/// GET books/v1/users/{userId}/bookshelves.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_bookshelves_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_bookshelves_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksBookshelvesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bookshelves>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/users/{}/bookshelves",
        args.user_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Bookshelves =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/users/{userId}/bookshelves/{shelf}/volumes
// -----------------------------------------------------------------------------

/// GET books/v1/users/{userId}/bookshelves/{shelf}/volumes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_bookshelves_volumes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_bookshelves_volumes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksBookshelvesVolumesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/users/{}/bookshelves/{}/volumes",
        args.user_id, args.shelf,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.show_preorders {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("showPreorders=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startIndex=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumes =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/cloudloading/addBook
// -----------------------------------------------------------------------------

/// POST books/v1/cloudloading/addBook.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_cloudloading_add_book_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_cloudloading_add_book_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksCloudloadingAddBookArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BooksCloudloadingResource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/cloudloading/addBook",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.drive_document_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("drive_document_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.mime_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("mime_type=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.upload_client_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("upload_client_token=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BooksCloudloadingResource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/cloudloading/deleteBook
// -----------------------------------------------------------------------------

/// POST books/v1/cloudloading/deleteBook.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_cloudloading_delete_book_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_cloudloading_delete_book_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksCloudloadingDeleteBookArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/cloudloading/deleteBook",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/cloudloading/updateBook
// -----------------------------------------------------------------------------

/// POST books/v1/cloudloading/updateBook.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_cloudloading_update_book_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_cloudloading_update_book_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksCloudloadingUpdateBookArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BooksCloudloadingResource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/cloudloading/updateBook",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BooksCloudloadingResource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/dictionary/listOfflineMetadata
// -----------------------------------------------------------------------------

/// GET books/v1/dictionary/listOfflineMetadata.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_dictionary_list_offline_metadata_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_dictionary_list_offline_metadata_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksDictionaryListOfflineMetadataArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Metadata>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/dictionary/listOfflineMetadata",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cpksver {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("cpksver=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Metadata =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/familysharing/getFamilyInfo
// -----------------------------------------------------------------------------

/// GET books/v1/familysharing/getFamilyInfo.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_familysharing_get_family_info_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_familysharing_get_family_info_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksFamilysharingGetFamilyInfoArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FamilyInfo>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/familysharing/getFamilyInfo",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FamilyInfo =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/familysharing/share
// -----------------------------------------------------------------------------

/// POST books/v1/familysharing/share.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_familysharing_share_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_familysharing_share_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksFamilysharingShareArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/familysharing/share",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.doc_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("docId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/familysharing/unshare
// -----------------------------------------------------------------------------

/// POST books/v1/familysharing/unshare.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_familysharing_unshare_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_familysharing_unshare_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksFamilysharingUnshareArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/familysharing/unshare",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.doc_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("docId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes/{volumeId}/layersummary/{summaryId}
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/{volumeId}/layersummary/{summaryId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_layers_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_layers_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksLayersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Layersummary>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layersummary/{}",
        args.volume_id, args.summary_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.content_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contentVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Layersummary =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes/{volumeId}/layersummary
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/{volumeId}/layersummary.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_layers_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_layers_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksLayersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Layersummaries>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layersummary",
        args.volume_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.content_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contentVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Layersummaries =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes/{volumeId}/layers/{layerId}/data/{annotationDataId}
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data/{annotationDataId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_layers_annotation_data_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_layers_annotation_data_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksLayersAnnotationDataGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DictionaryAnnotationdata>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layers/{}/data/{}",
        args.volume_id, args.layer_id, args.annotation_data_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.allow_web_definitions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowWebDefinitions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.content_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contentVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.h {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("h=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.scale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("scale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.w {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("w=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DictionaryAnnotationdata =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes/{volumeId}/layers/{layerId}/data
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/data.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_layers_annotation_data_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_layers_annotation_data_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksLayersAnnotationDataListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Annotationsdata>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layers/{}/data",
        args.volume_id, args.layer_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.annotation_data_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("annotationDataId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.content_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contentVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.h {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("h=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.scale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("scale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.updated_max {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updatedMax=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.updated_min {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updatedMin=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.w {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("w=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Annotationsdata =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes/{volumeId}/layers/{layerId}/annotations/{annotationId}
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/{volumeId}/layers/{layerId}/annotations/{annotationId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_layers_volume_annotations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_layers_volume_annotations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksLayersVolumeAnnotationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumeannotation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layers/{}/annotations/{}",
        args.volume_id, args.layer_id, args.annotation_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumeannotation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes/{volumeId}/layers/{layerId}
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/{volumeId}/layers/{layerId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_layers_volume_annotations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_layers_volume_annotations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksLayersVolumeAnnotationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumeannotations>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/layers/{}",
        args.volume_id, args.layer_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.content_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contentVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_position {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endPosition=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.show_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("showDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_offset {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startOffset=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_position {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startPosition=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.updated_max {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updatedMax=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.updated_min {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updatedMin=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_annotations_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeAnnotationsVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumeannotations =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/myconfig/getUserSettings
// -----------------------------------------------------------------------------

/// GET books/v1/myconfig/getUserSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_myconfig_get_user_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_myconfig_get_user_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMyconfigGetUserSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Usersettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/myconfig/getUserSettings",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("country=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Usersettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/myconfig/releaseDownloadAccess
// -----------------------------------------------------------------------------

/// POST books/v1/myconfig/releaseDownloadAccess.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_myconfig_release_download_access_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_myconfig_release_download_access_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMyconfigReleaseDownloadAccessArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DownloadAccesses>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/myconfig/releaseDownloadAccess",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cpksver {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("cpksver=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeIds=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DownloadAccesses =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/myconfig/requestAccess
// -----------------------------------------------------------------------------

/// POST books/v1/myconfig/requestAccess.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_myconfig_request_access_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_myconfig_request_access_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMyconfigRequestAccessArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RequestAccessData>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/myconfig/requestAccess",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cpksver {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("cpksver=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.license_types {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("licenseTypes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.nonce {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("nonce=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RequestAccessData =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/myconfig/syncVolumeLicenses
// -----------------------------------------------------------------------------

/// POST books/v1/myconfig/syncVolumeLicenses.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_myconfig_sync_volume_licenses_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_myconfig_sync_volume_licenses_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMyconfigSyncVolumeLicensesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/myconfig/syncVolumeLicenses",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.cpksver {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("cpksver=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.features {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("features=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_non_comics_series {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeNonComicsSeries=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.nonce {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("nonce=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.show_preorders {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("showPreorders=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeIds=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumes =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/myconfig/updateUserSettings
// -----------------------------------------------------------------------------

/// POST books/v1/myconfig/updateUserSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_myconfig_update_user_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_myconfig_update_user_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMyconfigUpdateUserSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Usersettings>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/myconfig/updateUserSettings",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Usersettings =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE books/v1/mylibrary/annotations/{annotationId}
// -----------------------------------------------------------------------------

/// DELETE books/v1/mylibrary/annotations/{annotationId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_annotations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_annotations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryAnnotationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/annotations/{}",
        args.annotation_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/mylibrary/annotations
// -----------------------------------------------------------------------------

/// POST books/v1/mylibrary/annotations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_annotations_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_annotations_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryAnnotationsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Annotation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/mylibrary/annotations",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.annotation_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("annotationId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("country=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.show_only_summary_in_response {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("showOnlySummaryInResponse=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Annotation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/mylibrary/annotations
// -----------------------------------------------------------------------------

/// GET books/v1/mylibrary/annotations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_annotations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_annotations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryAnnotationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Annotations>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/mylibrary/annotations",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.content_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contentVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.layer_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("layerId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.layer_ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("layerIds=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.show_deleted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("showDeleted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.updated_max {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updatedMax=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.updated_min {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updatedMin=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Annotations =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/mylibrary/annotations/summary
// -----------------------------------------------------------------------------

/// POST books/v1/mylibrary/annotations/summary.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_annotations_summary_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_annotations_summary_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryAnnotationsSummaryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnnotationsSummary>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/mylibrary/annotations/summary",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.layer_ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("layerIds=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AnnotationsSummary =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT books/v1/mylibrary/annotations/{annotationId}
// -----------------------------------------------------------------------------

/// PUT books/v1/mylibrary/annotations/{annotationId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_annotations_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_annotations_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryAnnotationsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Annotation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/annotations/{}",
        args.annotation_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Annotation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/mylibrary/bookshelves/{shelf}/addVolume
// -----------------------------------------------------------------------------

/// POST books/v1/mylibrary/bookshelves/{shelf}/addVolume.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_bookshelves_add_volume_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_bookshelves_add_volume_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryBookshelvesAddVolumeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}/addVolume",
        args.shelf,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.reason {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("reason=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/mylibrary/bookshelves/{shelf}/clearVolumes
// -----------------------------------------------------------------------------

/// POST books/v1/mylibrary/bookshelves/{shelf}/clearVolumes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_bookshelves_clear_volumes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_bookshelves_clear_volumes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryBookshelvesClearVolumesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}/clearVolumes",
        args.shelf,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/mylibrary/bookshelves/{shelf}
// -----------------------------------------------------------------------------

/// GET books/v1/mylibrary/bookshelves/{shelf}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_bookshelves_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_bookshelves_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryBookshelvesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bookshelf>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}",
        args.shelf,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Bookshelf =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/mylibrary/bookshelves
// -----------------------------------------------------------------------------

/// GET books/v1/mylibrary/bookshelves.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_bookshelves_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_bookshelves_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryBookshelvesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Bookshelves>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/mylibrary/bookshelves",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Bookshelves =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/mylibrary/bookshelves/{shelf}/moveVolume
// -----------------------------------------------------------------------------

/// POST books/v1/mylibrary/bookshelves/{shelf}/moveVolume.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_bookshelves_move_volume_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_bookshelves_move_volume_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryBookshelvesMoveVolumeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}/moveVolume",
        args.shelf,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_position {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumePosition=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/mylibrary/bookshelves/{shelf}/removeVolume
// -----------------------------------------------------------------------------

/// POST books/v1/mylibrary/bookshelves/{shelf}/removeVolume.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_bookshelves_remove_volume_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_bookshelves_remove_volume_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryBookshelvesRemoveVolumeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}/removeVolume",
        args.shelf,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.reason {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("reason=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/mylibrary/bookshelves/{shelf}/volumes
// -----------------------------------------------------------------------------

/// GET books/v1/mylibrary/bookshelves/{shelf}/volumes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_bookshelves_volumes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_bookshelves_volumes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryBookshelvesVolumesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/bookshelves/{}/volumes",
        args.shelf,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("country=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.q {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("q=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.show_preorders {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("showPreorders=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startIndex=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumes =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/mylibrary/readingpositions/{volumeId}
// -----------------------------------------------------------------------------

/// GET books/v1/mylibrary/readingpositions/{volumeId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_readingpositions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_readingpositions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryReadingpositionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReadingPosition>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/readingpositions/{}",
        args.volume_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.content_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contentVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReadingPosition =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/mylibrary/readingpositions/{volumeId}/setPosition
// -----------------------------------------------------------------------------

/// POST books/v1/mylibrary/readingpositions/{volumeId}/setPosition.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_mylibrary_readingpositions_set_position_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_mylibrary_readingpositions_set_position_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksMylibraryReadingpositionsSetPositionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/mylibrary/readingpositions/{}/setPosition",
        args.volume_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.action {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("action=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.content_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("contentVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.device_cookie {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deviceCookie=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.position {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("position=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.timestamp {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("timestamp=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/notification/get
// -----------------------------------------------------------------------------

/// GET books/v1/notification/get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_notification_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_notification_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksNotificationGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Notification>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/notification/get",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.notification_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("notification_id=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Notification =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/onboarding/listCategories
// -----------------------------------------------------------------------------

/// GET books/v1/onboarding/listCategories.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_onboarding_list_categories_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_onboarding_list_categories_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksOnboardingListCategoriesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Category>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/onboarding/listCategories",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Category =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/onboarding/listCategoryVolumes
// -----------------------------------------------------------------------------

/// GET books/v1/onboarding/listCategoryVolumes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_onboarding_list_category_volumes_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_onboarding_list_category_volumes_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksOnboardingListCategoryVolumesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volume2>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://books.googleapis.com/books/v1/onboarding/listCategoryVolumes",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.category_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("categoryId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_allowed_maturity_rating {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxAllowedMaturityRating=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volume2 =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/personalizedstream/get
// -----------------------------------------------------------------------------

/// GET books/v1/personalizedstream/get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_personalizedstream_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_personalizedstream_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksPersonalizedstreamGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Discoveryclusters>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/personalizedstream/get",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_allowed_maturity_rating {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxAllowedMaturityRating=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Discoveryclusters =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/promooffer/accept
// -----------------------------------------------------------------------------

/// POST books/v1/promooffer/accept.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_promooffer_accept_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_promooffer_accept_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksPromoofferAcceptArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/promooffer/accept",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.android_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("androidId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.device {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("device=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.manufacturer {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("manufacturer=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.model {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("model=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.offer_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("offerId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.product {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("product=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.serial {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serial=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/promooffer/dismiss
// -----------------------------------------------------------------------------

/// POST books/v1/promooffer/dismiss.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_promooffer_dismiss_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_promooffer_dismiss_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksPromoofferDismissArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/promooffer/dismiss",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.android_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("androidId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.device {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("device=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.manufacturer {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("manufacturer=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.model {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("model=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.offer_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("offerId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.product {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("product=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.serial {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serial=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/promooffer/get
// -----------------------------------------------------------------------------

/// GET books/v1/promooffer/get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_promooffer_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_promooffer_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksPromoofferGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Offers>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/promooffer/get",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.android_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("androidId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.device {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("device=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.manufacturer {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("manufacturer=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.model {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("model=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.product {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("product=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.serial {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("serial=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Offers =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/series/get
// -----------------------------------------------------------------------------

/// GET books/v1/series/get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_series_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_series_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksSeriesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Series>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/series/get",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.series_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("series_id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Series =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/series/membership/get
// -----------------------------------------------------------------------------

/// GET books/v1/series/membership/get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_series_membership_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_series_membership_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksSeriesMembershipGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Seriesmembership>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/series/membership/get",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page_size=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("page_token=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.series_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("series_id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Seriesmembership =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes/{volumeId}
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/{volumeId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_volumes_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_volumes_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksVolumesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volume>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}",
        args.volume_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("country=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_non_comics_series {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeNonComicsSeries=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.partner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("partner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.user_library_consistent_read {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("user_library_consistent_read=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volume =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes
// -----------------------------------------------------------------------------

/// GET books/v1/volumes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_volumes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_volumes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksVolumesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/volumes",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.download {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("download=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.lang_restrict {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("langRestrict=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.library_restrict {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("libraryRestrict=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_allowed_maturity_rating {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxAllowedMaturityRating=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.partner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("partner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.print_type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("printType=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.projection {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projection=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.q {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("q=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.show_preorders {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("showPreorders=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startIndex=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumes =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes/{volumeId}/associated
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/{volumeId}/associated.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_volumes_associated_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_volumes_associated_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksVolumesAssociatedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://books.googleapis.com/books/v1/volumes/{}/associated",
        args.volume_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.association {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("association=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_allowed_maturity_rating {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxAllowedMaturityRating=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumes =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes/mybooks
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/mybooks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_volumes_mybooks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_volumes_mybooks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksVolumesMybooksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/volumes/mybooks",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.acquire_method {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("acquireMethod=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.country {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("country=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.processing_state {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("processingState=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startIndex=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumes =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes/recommended
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/recommended.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_volumes_recommended_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_volumes_recommended_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksVolumesRecommendedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/volumes/recommended",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_allowed_maturity_rating {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxAllowedMaturityRating=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumes =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST books/v1/volumes/recommended/rate
// -----------------------------------------------------------------------------

/// POST books/v1/volumes/recommended/rate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_volumes_recommended_rate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_volumes_recommended_rate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksVolumesRecommendedRateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<BooksVolumesRecommendedRateResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/volumes/recommended/rate",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.rating {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("rating=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BooksVolumesRecommendedRateResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET books/v1/volumes/useruploaded
// -----------------------------------------------------------------------------

/// GET books/v1/volumes/useruploaded.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = books_volumes_useruploaded_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn books_volumes_useruploaded_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &BooksVolumesUseruploadedListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volumes>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://books.googleapis.com/books/v1/volumes/useruploaded",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.locale {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("locale=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.processing_state {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("processingState=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.source {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("source=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startIndex=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.volume_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("volumeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Volumes =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
