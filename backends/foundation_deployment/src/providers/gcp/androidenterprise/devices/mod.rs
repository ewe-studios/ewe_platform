//! Auto-generated API module for gcp/androidenterprise devices.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_androidenterprise_devices `

#![cfg(feature = "gcp_androidenterprise_devices")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `AutoInstallConstraint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AutoInstallConstraint {
    /// chargingStateConstraint property.
    pub charging_state_constraint: Option<String>,
    /// deviceIdleStateConstraint property.
    pub device_idle_state_constraint: Option<String>,
    /// networkTypeConstraint property.
    pub network_type_constraint: Option<String>,
}

/// `ManagedPropertyBundle` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedPropertyBundle {
    /// managedProperty property.
    pub managed_property: Option<Vec<ManagedProperty>>,
}

/// `DeviceReport` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeviceReport {
    /// appState property.
    pub app_state: Option<Vec<AppState>>,
    /// lastUpdatedTimestampMillis property.
    pub last_updated_timestamp_millis: Option<String>,
}

/// `AppState` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AppState {
    /// keyedAppState property.
    pub keyed_app_state: Option<Vec<KeyedAppState>>,
    /// packageName property.
    pub package_name: Option<String>,
}

/// `MaintenanceWindow` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MaintenanceWindow {
    /// durationMs property.
    pub duration_ms: Option<String>,
    /// startTimeAfterMidnightMs property.
    pub start_time_after_midnight_ms: Option<String>,
}

/// `ManagedProperty` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ManagedProperty {
    /// key property.
    pub key: Option<String>,
    /// valueBool property.
    pub value_bool: Option<bool>,
    /// valueBundle property.
    pub value_bundle: Option<ManagedPropertyBundle>,
    /// valueBundleArray property.
    pub value_bundle_array: Option<Vec<ManagedPropertyBundle>>,
    /// valueInteger property.
    pub value_integer: Option<i64>,
    /// valueString property.
    pub value_string: Option<String>,
    /// valueStringArray property.
    pub value_string_array: Option<Vec<String>>,
}

/// `AutoInstallPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AutoInstallPolicy {
    /// autoInstallConstraint property.
    pub auto_install_constraint: Option<Vec<AutoInstallConstraint>>,
    /// autoInstallMode property.
    pub auto_install_mode: Option<String>,
    /// autoInstallPriority property.
    pub auto_install_priority: Option<i64>,
    /// minimumVersionCode property.
    pub minimum_version_code: Option<i64>,
}

/// `KeyedAppState` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KeyedAppState {
    /// data property.
    pub data: Option<String>,
    /// key property.
    pub key: Option<String>,
    /// message property.
    pub message: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// stateTimestampMillis property.
    pub state_timestamp_millis: Option<String>,
}

/// `ProductPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductPolicy {
    /// autoInstallPolicy property.
    pub auto_install_policy: Option<AutoInstallPolicy>,
    /// autoUpdateMode property.
    pub auto_update_mode: Option<String>,
    /// enterpriseAuthenticationAppLinkConfigs property.
    pub enterprise_authentication_app_link_configs:
        Option<Vec<EnterpriseAuthenticationAppLinkConfig>>,
    /// managedConfiguration property.
    pub managed_configuration: Option<ManagedConfiguration>,
    /// productId property.
    pub product_id: Option<String>,
    /// trackIds property.
    pub track_ids: Option<Vec<String>>,
    /// tracks property.
    pub tracks: Option<Vec<String>>,
}

/// `Policy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Policy {
    /// autoUpdatePolicy property.
    pub auto_update_policy: Option<String>,
    /// deviceReportPolicy property.
    pub device_report_policy: Option<String>,
    /// maintenanceWindow property.
    pub maintenance_window: Option<MaintenanceWindow>,
    /// policyId property.
    pub policy_id: Option<String>,
    /// productAvailabilityPolicy property.
    pub product_availability_policy: Option<String>,
    /// productPolicy property.
    pub product_policy: Option<Vec<ProductPolicy>>,
}

/// `VariableSet` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VariableSet {
    /// placeholder property.
    pub placeholder: Option<String>,
    /// userValue property.
    pub user_value: Option<String>,
}

/// `ConfigurationVariables` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConfigurationVariables {
    /// mcmId property.
    pub mcm_id: Option<String>,
    /// variableSet property.
    pub variable_set: Option<Vec<VariableSet>>,
}

/// `DevicesListResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DevicesListResponse {
    /// device property.
    pub device: Option<Vec<Device>>,
}

/// `EnterpriseAuthenticationAppLinkConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EnterpriseAuthenticationAppLinkConfig {
    /// uri property.
    pub uri: Option<String>,
}

/// `Device` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Device {
    /// androidId property.
    pub android_id: Option<String>,
    /// device property.
    pub device: Option<String>,
    /// latestBuildFingerprint property.
    pub latest_build_fingerprint: Option<String>,
    /// maker property.
    pub maker: Option<String>,
    /// managementType property.
    pub management_type: Option<String>,
    /// model property.
    pub model: Option<String>,
    /// policy property.
    pub policy: Option<Policy>,
    /// product property.
    pub product: Option<String>,
    /// report property.
    pub report: Option<DeviceReport>,
    /// retailBrand property.
    pub retail_brand: Option<String>,
    /// sdkVersion property.
    pub sdk_version: Option<i64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`androidenterprise.devices.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseDevicesGetArgs {
    /// Path parameter: `enterpriseId`.
    pub enterprise_id: String,
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `deviceId`.
    pub device_id: String,
}

/// Arguments for [`androidenterprise.devices.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseDevicesListArgs {
    /// Path parameter: `enterpriseId`.
    pub enterprise_id: String,
    /// Path parameter: `userId`.
    pub user_id: String,
}

/// Arguments for [`androidenterprise.devices.update_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AndroidenterpriseDevicesUpdateArgs {
    /// Path parameter: `enterpriseId`.
    pub enterprise_id: String,
    /// Path parameter: `userId`.
    pub user_id: String,
    /// Path parameter: `deviceId`.
    pub device_id: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
// -----------------------------------------------------------------------------

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidenterprise_devices_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidenterprise_devices_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidenterpriseDevicesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Device>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}",
        args.enterprise_id,
        args.user_id,
        args.device_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Device =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices
// -----------------------------------------------------------------------------

/// GET androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidenterprise_devices_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidenterprise_devices_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidenterpriseDevicesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DevicesListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices",
        args.enterprise_id,
        args.user_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DevicesListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}
// -----------------------------------------------------------------------------

/// PUT androidenterprise/v1/enterprises/{enterpriseId}/users/{userId}/devices/{deviceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidenterprise_devices_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidenterprise_devices_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidenterpriseDevicesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Device>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidenterprise.googleapis.com/androidenterprise/v1/enterprises/{}/users/{}/devices/{}",
        args.enterprise_id,
        args.user_id,
        args.device_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Device =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
