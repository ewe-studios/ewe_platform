//! Auto-generated API module for gcp/script deployments.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_script_deployments `

#![cfg(feature = "gcp_script_deployments")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `DeploymentConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeploymentConfig {
    /// description property.
    pub description: Option<String>,
    /// manifestFileName property.
    pub manifest_file_name: Option<String>,
    /// scriptId property.
    pub script_id: Option<String>,
    /// versionNumber property.
    pub version_number: Option<i64>,
}

/// `Empty` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Empty {}

/// `ListDeploymentsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListDeploymentsResponse {
    /// deployments property.
    pub deployments: Option<Vec<Deployment>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleAppsScriptTypeExecutionApiEntryPoint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsScriptTypeExecutionApiEntryPoint {
    /// entryPointConfig property.
    pub entry_point_config: Option<GoogleAppsScriptTypeExecutionApiConfig>,
}

/// `Deployment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Deployment {
    /// deploymentConfig property.
    pub deployment_config: Option<DeploymentConfig>,
    /// deploymentId property.
    pub deployment_id: Option<String>,
    /// entryPoints property.
    pub entry_points: Option<Vec<EntryPoint>>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `GoogleAppsScriptTypeWebAppConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsScriptTypeWebAppConfig {
    /// access property.
    pub access: Option<String>,
    /// executeAs property.
    pub execute_as: Option<String>,
}

/// `GoogleAppsScriptTypeExecutionApiConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsScriptTypeExecutionApiConfig {
    /// access property.
    pub access: Option<String>,
}

/// `GoogleAppsScriptTypeAddOnEntryPoint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsScriptTypeAddOnEntryPoint {
    /// addOnType property.
    pub add_on_type: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// helpUrl property.
    pub help_url: Option<String>,
    /// postInstallTipUrl property.
    pub post_install_tip_url: Option<String>,
    /// reportIssueUrl property.
    pub report_issue_url: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `EntryPoint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntryPoint {
    /// addOn property.
    pub add_on: Option<GoogleAppsScriptTypeAddOnEntryPoint>,
    /// entryPointType property.
    pub entry_point_type: Option<String>,
    /// executionApi property.
    pub execution_api: Option<GoogleAppsScriptTypeExecutionApiEntryPoint>,
    /// webApp property.
    pub web_app: Option<GoogleAppsScriptTypeWebAppEntryPoint>,
}

/// `GoogleAppsScriptTypeWebAppEntryPoint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsScriptTypeWebAppEntryPoint {
    /// entryPointConfig property.
    pub entry_point_config: Option<GoogleAppsScriptTypeWebAppConfig>,
    /// url property.
    pub url: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`script.projects.deployments.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsDeploymentsCreateArgs {
    /// Path parameter: `scriptId`.
    pub script_id: String,
}

/// Arguments for [`script.projects.deployments.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsDeploymentsDeleteArgs {
    /// Path parameter: `scriptId`.
    pub script_id: String,
    /// Path parameter: `deploymentId`.
    pub deployment_id: String,
}

/// Arguments for [`script.projects.deployments.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsDeploymentsGetArgs {
    /// Path parameter: `scriptId`.
    pub script_id: String,
    /// Path parameter: `deploymentId`.
    pub deployment_id: String,
}

/// Arguments for [`script.projects.deployments.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsDeploymentsListArgs {
    /// Path parameter: `scriptId`.
    pub script_id: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`script.projects.deployments.update_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ScriptProjectsDeploymentsUpdateArgs {
    /// Path parameter: `scriptId`.
    pub script_id: String,
    /// Path parameter: `deploymentId`.
    pub deployment_id: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{scriptId}/deployments
// -----------------------------------------------------------------------------

/// POST v1/projects/{scriptId}/deployments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = script_projects_deployments_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn script_projects_deployments_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ScriptProjectsDeploymentsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Deployment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://script.googleapis.com/v1/projects/{}/deployments",
        args.script_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Deployment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{scriptId}/deployments/{deploymentId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{scriptId}/deployments/{deploymentId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = script_projects_deployments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn script_projects_deployments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ScriptProjectsDeploymentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://script.googleapis.com/v1/projects/{}/deployments/{}",
        args.script_id, args.deployment_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{scriptId}/deployments/{deploymentId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{scriptId}/deployments/{deploymentId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = script_projects_deployments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn script_projects_deployments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ScriptProjectsDeploymentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Deployment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://script.googleapis.com/v1/projects/{}/deployments/{}",
        args.script_id, args.deployment_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Deployment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{scriptId}/deployments
// -----------------------------------------------------------------------------

/// GET v1/projects/{scriptId}/deployments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = script_projects_deployments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn script_projects_deployments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ScriptProjectsDeploymentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDeploymentsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://script.googleapis.com/v1/projects/{}/deployments",
        args.script_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDeploymentsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT v1/projects/{scriptId}/deployments/{deploymentId}
// -----------------------------------------------------------------------------

/// PUT v1/projects/{scriptId}/deployments/{deploymentId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = script_projects_deployments_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn script_projects_deployments_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ScriptProjectsDeploymentsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Deployment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://script.googleapis.com/v1/projects/{}/deployments/{}",
        args.script_id, args.deployment_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Deployment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
