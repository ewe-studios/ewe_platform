//! Auto-generated API module for gcp/analyticshub dataExchanges.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_analyticshub_data_exchanges `

#![cfg(feature = "gcp_analyticshub_data_exchanges")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::ListSharedResourceSubscriptionsResponse;
use super::shared::Operation;
use super::shared::Policy;
use super::shared::TestIamPermissionsResponse;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Subscription` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Subscription {
    /// commercialInfo property.
    pub commercial_info: Option<GoogleCloudBigqueryAnalyticshubV1SubscriptionCommercialInfo>,
    /// creationTime property.
    pub creation_time: Option<String>,
    /// dataExchange property.
    pub data_exchange: Option<String>,
    /// destinationDataset property.
    pub destination_dataset: Option<DestinationDataset>,
    /// lastModifyTime property.
    pub last_modify_time: Option<String>,
    /// linkedDatasetMap property.
    pub linked_dataset_map: Option<serde_json::Value>,
    /// linkedResources property.
    pub linked_resources: Option<Vec<LinkedResource>>,
    /// listing property.
    pub listing: Option<String>,
    /// logLinkedDatasetQueryUserEmail property.
    pub log_linked_dataset_query_user_email: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// organizationDisplayName property.
    pub organization_display_name: Option<String>,
    /// organizationId property.
    pub organization_id: Option<String>,
    /// resourceType property.
    pub resource_type: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// subscriberContact property.
    pub subscriber_contact: Option<String>,
}

/// `LinkedResource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LinkedResource {
    /// linkedDataset property.
    pub linked_dataset: Option<String>,
    /// linkedPubsubSubscription property.
    pub linked_pubsub_subscription: Option<String>,
    /// listing property.
    pub listing: Option<String>,
}

/// `GoogleCloudBigqueryAnalyticshubV1SubscriptionCommercialInfoGoogleCloudMarketplaceInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudBigqueryAnalyticshubV1SubscriptionCommercialInfoGoogleCloudMarketplaceInfo {
    /// order property.
    pub order: Option<String>,
}

/// `Binding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `DestinationDataset` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DestinationDataset {
    /// datasetReference property.
    pub dataset_reference: Option<DestinationDatasetReference>,
    /// description property.
    pub description: Option<String>,
    /// friendlyName property.
    pub friendly_name: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// location property.
    pub location: Option<String>,
    /// replicaLocations property.
    pub replica_locations: Option<Vec<String>>,
}

/// `AuditLogConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditLogConfig {
    /// exemptedMembers property.
    pub exempted_members: Option<Vec<String>>,
    /// logType property.
    pub log_type: Option<String>,
}

/// `DefaultExchangeConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DefaultExchangeConfig {}

/// `Expr` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `GoogleCloudBigqueryAnalyticshubV1SubscriptionCommercialInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudBigqueryAnalyticshubV1SubscriptionCommercialInfo {
    /// cloudMarketplace property.
    pub cloud_marketplace: Option<
        GoogleCloudBigqueryAnalyticshubV1SubscriptionCommercialInfoGoogleCloudMarketplaceInfo,
    >,
}

/// `DataExchange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DataExchange {
    /// description property.
    pub description: Option<String>,
    /// discoveryType property.
    pub discovery_type: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// documentation property.
    pub documentation: Option<String>,
    /// icon property.
    pub icon: Option<String>,
    /// listingCount property.
    pub listing_count: Option<i64>,
    /// logLinkedDatasetQueryUserEmail property.
    pub log_linked_dataset_query_user_email: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// primaryContact property.
    pub primary_contact: Option<String>,
    /// sharingEnvironmentConfig property.
    pub sharing_environment_config: Option<SharingEnvironmentConfig>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `ListDataExchangesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListDataExchangesResponse {
    /// dataExchanges property.
    pub data_exchanges: Option<Vec<DataExchange>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `DestinationDatasetReference` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DestinationDatasetReference {
    /// datasetId property.
    pub dataset_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
}

/// `SharingEnvironmentConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SharingEnvironmentConfig {
    /// dcrExchangeConfig property.
    pub dcr_exchange_config: Option<DcrExchangeConfig>,
    /// defaultExchangeConfig property.
    pub default_exchange_config: Option<DefaultExchangeConfig>,
}

/// `ListOrgDataExchangesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListOrgDataExchangesResponse {
    /// dataExchanges property.
    pub data_exchanges: Option<Vec<DataExchange>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `DcrExchangeConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DcrExchangeConfig {
    /// singleLinkedDatasetPerCleanroom property.
    pub single_linked_dataset_per_cleanroom: Option<bool>,
    /// singleSelectedResourceSharingRestriction property.
    pub single_selected_resource_sharing_restriction: Option<bool>,
}

/// `AuditConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditConfig {
    /// auditLogConfigs property.
    pub audit_log_configs: Option<Vec<AuditLogConfig>>,
    /// service property.
    pub service: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`analyticshub.organizations.locations.dataExchanges.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticshubOrganizationsLocationsDataExchangesListArgs {
    /// Path parameter: `organization`.
    pub organization: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`analyticshub.projects.locations.dataExchanges.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticshubProjectsLocationsDataExchangesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `dataExchangeId`.
    pub data_exchange_id: Option<String>,
}

/// Arguments for [`analyticshub.projects.locations.dataExchanges.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticshubProjectsLocationsDataExchangesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`analyticshub.projects.locations.dataExchanges.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticshubProjectsLocationsDataExchangesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`analyticshub.projects.locations.dataExchanges.getIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticshubProjectsLocationsDataExchangesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`analyticshub.projects.locations.dataExchanges.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticshubProjectsLocationsDataExchangesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`analyticshub.projects.locations.dataExchanges.listSubscriptions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticshubProjectsLocationsDataExchangesListSubscriptionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `includeDeletedSubscriptions`.
    pub include_deleted_subscriptions: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`analyticshub.projects.locations.dataExchanges.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticshubProjectsLocationsDataExchangesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`analyticshub.projects.locations.dataExchanges.setIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticshubProjectsLocationsDataExchangesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`analyticshub.projects.locations.dataExchanges.subscribe_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticshubProjectsLocationsDataExchangesSubscribeArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`analyticshub.projects.locations.dataExchanges.testIamPermissions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticshubProjectsLocationsDataExchangesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/organizations/{organizationsId}/locations/{locationsId}/dataExchanges
// -----------------------------------------------------------------------------

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/dataExchanges.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticshub_organizations_locations_data_exchanges_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticshub_organizations_locations_data_exchanges_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOrgDataExchangesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticshub.googleapis.com/v1/organizations/{{organizationsId}}/locations/{{locationsId}}/dataExchanges",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOrgDataExchangesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/dataExchanges
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/dataExchanges.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticshub_projects_locations_data_exchanges_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticshub_projects_locations_data_exchanges_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataExchange>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticshub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dataExchanges",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DataExchange =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticshub_projects_locations_data_exchanges_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticshub_projects_locations_data_exchanges_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticshub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dataExchanges/{{dataExchangesId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticshub_projects_locations_data_exchanges_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticshub_projects_locations_data_exchanges_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataExchange>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticshub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dataExchanges/{{dataExchangesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DataExchange =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticshub_projects_locations_data_exchanges_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticshub_projects_locations_data_exchanges_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticshub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dataExchanges/{{dataExchangesId}}:getIamPolicy",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/dataExchanges
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataExchanges.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticshub_projects_locations_data_exchanges_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticshub_projects_locations_data_exchanges_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataExchangesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticshub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dataExchanges",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDataExchangesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}:listSubscriptions
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}:listSubscriptions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticshub_projects_locations_data_exchanges_list_subscriptions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticshub_projects_locations_data_exchanges_list_subscriptions_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListSharedResourceSubscriptionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticshub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dataExchanges/{{dataExchangesId}}:listSubscriptions",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListSharedResourceSubscriptionsResponse = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticshub_projects_locations_data_exchanges_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticshub_projects_locations_data_exchanges_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataExchange>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticshub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dataExchanges/{{dataExchangesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DataExchange =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticshub_projects_locations_data_exchanges_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticshub_projects_locations_data_exchanges_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticshub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dataExchanges/{{dataExchangesId}}:setIamPolicy",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}:subscribe
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}:subscribe.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticshub_projects_locations_data_exchanges_subscribe_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticshub_projects_locations_data_exchanges_subscribe_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticshub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dataExchanges/{{dataExchangesId}}:subscribe",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/dataExchanges/{dataExchangesId}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analyticshub_projects_locations_data_exchanges_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analyticshub_projects_locations_data_exchanges_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://analyticshub.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dataExchanges/{{dataExchangesId}}:testIamPermissions",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
