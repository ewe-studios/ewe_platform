//! Auto-generated API module for gcp/drivelabels drivelabels.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_drivelabels_drivelabels `

#![cfg(feature = "gcp_drivelabels_drivelabels")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleAppsDriveLabelsV2BadgeConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2BadgeConfig {
    /// color property.
    pub color: Option<GoogleTypeColor>,
    /// priorityOverride property.
    pub priority_override: Option<String>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDisableFieldResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDisableFieldResponse {}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateFieldPropertiesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateFieldPropertiesRequest {
    /// id property.
    pub id: Option<String>,
    /// properties property.
    pub properties: Option<GoogleAppsDriveLabelsV2FieldProperties>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `GoogleAppsDriveLabelsV2FieldSelectionOptionsChoice` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldSelectionOptionsChoice {
    /// appliedCapabilities property.
    pub applied_capabilities:
        Option<GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceAppliedCapabilities>,
    /// createTime property.
    pub create_time: Option<String>,
    /// creator property.
    pub creator: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// disableTime property.
    pub disable_time: Option<String>,
    /// disabler property.
    pub disabler: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// displayHints property.
    pub display_hints: Option<GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceDisplayHints>,
    /// id property.
    pub id: Option<String>,
    /// lifecycle property.
    pub lifecycle: Option<GoogleAppsDriveLabelsV2Lifecycle>,
    /// lockStatus property.
    pub lock_status: Option<GoogleAppsDriveLabelsV2LockStatus>,
    /// properties property.
    pub properties: Option<GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceProperties>,
    /// publishTime property.
    pub publish_time: Option<String>,
    /// publisher property.
    pub publisher: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// schemaCapabilities property.
    pub schema_capabilities:
        Option<GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceSchemaCapabilities>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// updater property.
    pub updater: Option<GoogleAppsDriveLabelsV2UserInfo>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDisableFieldRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDisableFieldRequest {
    /// disabledPolicy property.
    pub disabled_policy: Option<GoogleAppsDriveLabelsV2LifecycleDisabledPolicy>,
    /// id property.
    pub id: Option<String>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `GoogleAppsDriveLabelsV2BatchDeleteLabelPermissionsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2BatchDeleteLabelPermissionsRequest {
    /// requests property.
    pub requests: Option<Vec<GoogleAppsDriveLabelsV2DeleteLabelPermissionRequest>>,
    /// useAdminAccess property.
    pub use_admin_access: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2ListLabelPermissionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2ListLabelPermissionsResponse {
    /// labelPermissions property.
    pub label_permissions: Option<Vec<GoogleAppsDriveLabelsV2LabelPermission>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleTypeDate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeDate {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `GoogleAppsDriveLabelsV2FieldUserOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldUserOptions {
    /// listOptions property.
    pub list_options: Option<GoogleAppsDriveLabelsV2FieldListOptions>,
}

/// `GoogleAppsDriveLabelsV2TextLimits` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2TextLimits {
    /// maxLength property.
    pub max_length: Option<i64>,
    /// minLength property.
    pub min_length: Option<i64>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDisableSelectionChoiceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDisableSelectionChoiceRequest {
    /// disabledPolicy property.
    pub disabled_policy: Option<GoogleAppsDriveLabelsV2LifecycleDisabledPolicy>,
    /// fieldId property.
    pub field_id: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `GoogleAppsDriveLabelsV2LabelLock` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LabelLock {
    /// capabilities property.
    pub capabilities: Option<GoogleAppsDriveLabelsV2LabelLockCapabilities>,
    /// choiceId property.
    pub choice_id: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// creator property.
    pub creator: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// deleteTime property.
    pub delete_time: Option<String>,
    /// fieldId property.
    pub field_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `GoogleAppsDriveLabelsV2LabelLimits` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LabelLimits {
    /// fieldLimits property.
    pub field_limits: Option<GoogleAppsDriveLabelsV2FieldLimits>,
    /// maxDeletedFields property.
    pub max_deleted_fields: Option<i64>,
    /// maxDescriptionLength property.
    pub max_description_length: Option<i64>,
    /// maxDraftRevisions property.
    pub max_draft_revisions: Option<i64>,
    /// maxFields property.
    pub max_fields: Option<i64>,
    /// maxTitleLength property.
    pub max_title_length: Option<i64>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleAppsDriveLabelsV2LabelSchemaCapabilities` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LabelSchemaCapabilities {
    /// canDelete property.
    pub can_delete: Option<bool>,
    /// canDisable property.
    pub can_disable: Option<bool>,
    /// canEnable property.
    pub can_enable: Option<bool>,
    /// canUpdate property.
    pub can_update: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDisableSelectionChoiceResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDisableSelectionChoiceResponse {}

/// `GoogleAppsDriveLabelsV2FieldSelectionOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldSelectionOptions {
    /// choices property.
    pub choices: Option<Vec<GoogleAppsDriveLabelsV2FieldSelectionOptionsChoice>>,
    /// listOptions property.
    pub list_options: Option<GoogleAppsDriveLabelsV2FieldListOptions>,
}

/// `GoogleAppsDriveLabelsV2LabelProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LabelProperties {
    /// description property.
    pub description: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `GoogleAppsDriveLabelsV2FieldDateOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldDateOptions {
    /// dateFormat property.
    pub date_format: Option<String>,
    /// dateFormatType property.
    pub date_format_type: Option<String>,
    /// maxValue property.
    pub max_value: Option<GoogleTypeDate>,
    /// minValue property.
    pub min_value: Option<GoogleTypeDate>,
}

/// `GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceDisplayHints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceDisplayHints {
    /// badgeColors property.
    pub badge_colors: Option<GoogleAppsDriveLabelsV2BadgeColors>,
    /// badgePriority property.
    pub badge_priority: Option<String>,
    /// darkBadgeColors property.
    pub dark_badge_colors: Option<GoogleAppsDriveLabelsV2BadgeColors>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// hiddenInSearch property.
    pub hidden_in_search: Option<bool>,
    /// shownInApply property.
    pub shown_in_apply: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2LabelAppliedCapabilities` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LabelAppliedCapabilities {
    /// canApply property.
    pub can_apply: Option<bool>,
    /// canRead property.
    pub can_read: Option<bool>,
    /// canRemove property.
    pub can_remove: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2ListLimits` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2ListLimits {
    /// maxEntries property.
    pub max_entries: Option<i64>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestRequest {
    /// createField property.
    pub create_field: Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestCreateFieldRequest>,
    /// createSelectionChoice property.
    pub create_selection_choice:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestCreateSelectionChoiceRequest>,
    /// deleteField property.
    pub delete_field: Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDeleteFieldRequest>,
    /// deleteSelectionChoice property.
    pub delete_selection_choice:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDeleteSelectionChoiceRequest>,
    /// disableField property.
    pub disable_field: Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDisableFieldRequest>,
    /// disableSelectionChoice property.
    pub disable_selection_choice:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDisableSelectionChoiceRequest>,
    /// enableField property.
    pub enable_field: Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestEnableFieldRequest>,
    /// enableSelectionChoice property.
    pub enable_selection_choice:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestEnableSelectionChoiceRequest>,
    /// updateField property.
    pub update_field:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateFieldPropertiesRequest>,
    /// updateFieldType property.
    pub update_field_type:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateFieldTypeRequest>,
    /// updateLabel property.
    pub update_label:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateLabelPropertiesRequest>,
    /// updateSelectionChoiceProperties property.
    pub update_selection_choice_properties: Option<
        GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateSelectionChoicePropertiesRequest,
    >,
}

/// `GoogleAppsDriveLabelsV2DeleteLabelPermissionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeleteLabelPermissionRequest {
    /// name property.
    pub name: Option<String>,
    /// useAdminAccess property.
    pub use_admin_access: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateLabelPropertiesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateLabelPropertiesResponse {}

/// `GoogleAppsDriveLabelsV2FieldListOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldListOptions {
    /// maxEntries property.
    pub max_entries: Option<i64>,
}

/// `GoogleAppsDriveLabelsV2UserLimits` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2UserLimits {
    /// listLimits property.
    pub list_limits: Option<GoogleAppsDriveLabelsV2ListLimits>,
}

/// `GoogleAppsDriveLabelsV2LabelDisplayHints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LabelDisplayHints {
    /// disabled property.
    pub disabled: Option<bool>,
    /// hiddenInSearch property.
    pub hidden_in_search: Option<bool>,
    /// priority property.
    pub priority: Option<String>,
    /// shownInApply property.
    pub shown_in_apply: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseResponse {
    /// createField property.
    pub create_field: Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseCreateFieldResponse>,
    /// createSelectionChoice property.
    pub create_selection_choice:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseCreateSelectionChoiceResponse>,
    /// deleteField property.
    pub delete_field: Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDeleteFieldResponse>,
    /// deleteSelectionChoice property.
    pub delete_selection_choice:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDeleteSelectionChoiceResponse>,
    /// disableField property.
    pub disable_field: Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDisableFieldResponse>,
    /// disableSelectionChoice property.
    pub disable_selection_choice:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDisableSelectionChoiceResponse>,
    /// enableField property.
    pub enable_field: Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseEnableFieldResponse>,
    /// enableSelectionChoice property.
    pub enable_selection_choice:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseEnableSelectionChoiceResponse>,
    /// updateField property.
    pub update_field:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateFieldPropertiesResponse>,
    /// updateFieldType property.
    pub update_field_type:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateFieldTypeResponse>,
    /// updateLabel property.
    pub update_label:
        Option<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateLabelPropertiesResponse>,
    /// updateSelectionChoiceProperties property.
    pub update_selection_choice_properties: Option<
        GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateSelectionChoicePropertiesResponse,
    >,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseEnableSelectionChoiceResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseEnableSelectionChoiceResponse {}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestCreateSelectionChoiceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestCreateSelectionChoiceRequest {
    /// choice property.
    pub choice: Option<GoogleAppsDriveLabelsV2FieldSelectionOptionsChoice>,
    /// fieldId property.
    pub field_id: Option<String>,
}

/// `GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceAppliedCapabilities` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceAppliedCapabilities {
    /// canRead property.
    pub can_read: Option<bool>,
    /// canSearch property.
    pub can_search: Option<bool>,
    /// canSelect property.
    pub can_select: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2UpdateLabelEnabledAppSettingsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2UpdateLabelEnabledAppSettingsRequest {
    /// enabledAppSettings property.
    pub enabled_app_settings: Option<GoogleAppsDriveLabelsV2LabelEnabledAppSettings>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// useAdminAccess property.
    pub use_admin_access: Option<bool>,
    /// view property.
    pub view: Option<String>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestCreateFieldRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestCreateFieldRequest {
    /// field property.
    pub field: Option<GoogleAppsDriveLabelsV2Field>,
}

/// `GoogleAppsDriveLabelsV2FieldAppliedCapabilities` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldAppliedCapabilities {
    /// canRead property.
    pub can_read: Option<bool>,
    /// canSearch property.
    pub can_search: Option<bool>,
    /// canWrite property.
    pub can_write: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse {
    /// permissions property.
    pub permissions: Option<Vec<GoogleAppsDriveLabelsV2LabelPermission>>,
}

/// `GoogleAppsDriveLabelsV2FieldIntegerOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldIntegerOptions {
    /// maxValue property.
    pub max_value: Option<String>,
    /// minValue property.
    pub min_value: Option<String>,
}

/// `GoogleAppsDriveLabelsV2FieldTextOptions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldTextOptions {
    /// maxLength property.
    pub max_length: Option<i64>,
    /// minLength property.
    pub min_length: Option<i64>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseCreateFieldResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseCreateFieldResponse {
    /// id property.
    pub id: Option<String>,
    /// priority property.
    pub priority: Option<i64>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestEnableFieldRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestEnableFieldRequest {
    /// id property.
    pub id: Option<String>,
}

/// `GoogleAppsDriveLabelsV2FieldProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldProperties {
    /// displayName property.
    pub display_name: Option<String>,
    /// insertBeforeField property.
    pub insert_before_field: Option<String>,
    /// required property.
    pub required: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceProperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceProperties {
    /// badgeConfig property.
    pub badge_config: Option<GoogleAppsDriveLabelsV2BadgeConfig>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// insertBeforeChoice property.
    pub insert_before_choice: Option<String>,
}

/// `GoogleProtobufEmpty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleProtobufEmpty {}

/// `GoogleAppsDriveLabelsV2LabelEnabledAppSettingsEnabledApp` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LabelEnabledAppSettingsEnabledApp {
    /// app property.
    pub app: Option<String>,
}

/// `GoogleAppsDriveLabelsV2FieldDisplayHints` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldDisplayHints {
    /// disabled property.
    pub disabled: Option<bool>,
    /// hiddenInSearch property.
    pub hidden_in_search: Option<bool>,
    /// required property.
    pub required: Option<bool>,
    /// shownInApply property.
    pub shown_in_apply: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2LabelEnabledAppSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LabelEnabledAppSettings {
    /// enabledApps property.
    pub enabled_apps: Option<Vec<GoogleAppsDriveLabelsV2LabelEnabledAppSettingsEnabledApp>>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDeleteFieldResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDeleteFieldResponse {}

/// `GoogleAppsDriveLabelsV2FieldLimits` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldLimits {
    /// dateLimits property.
    pub date_limits: Option<GoogleAppsDriveLabelsV2DateLimits>,
    /// integerLimits property.
    pub integer_limits: Option<GoogleAppsDriveLabelsV2IntegerLimits>,
    /// longTextLimits property.
    pub long_text_limits: Option<GoogleAppsDriveLabelsV2LongTextLimits>,
    /// maxDescriptionLength property.
    pub max_description_length: Option<i64>,
    /// maxDisplayNameLength property.
    pub max_display_name_length: Option<i64>,
    /// maxIdLength property.
    pub max_id_length: Option<i64>,
    /// selectionLimits property.
    pub selection_limits: Option<GoogleAppsDriveLabelsV2SelectionLimits>,
    /// textLimits property.
    pub text_limits: Option<GoogleAppsDriveLabelsV2TextLimits>,
    /// userLimits property.
    pub user_limits: Option<GoogleAppsDriveLabelsV2UserLimits>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateFieldPropertiesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateFieldPropertiesResponse {
    /// priority property.
    pub priority: Option<i64>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDeleteSelectionChoiceResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseDeleteSelectionChoiceResponse {}

/// `GoogleAppsDriveLabelsV2LockStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LockStatus {
    /// locked property.
    pub locked: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2FieldSchemaCapabilities` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldSchemaCapabilities {
    /// canDelete property.
    pub can_delete: Option<bool>,
    /// canDisable property.
    pub can_disable: Option<bool>,
    /// canEnable property.
    pub can_enable: Option<bool>,
    /// canUpdate property.
    pub can_update: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDeleteFieldRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDeleteFieldRequest {
    /// id property.
    pub id: Option<String>,
}

/// `GoogleAppsDriveLabelsV2LabelLockCapabilities` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LabelLockCapabilities {
    /// canViewPolicy property.
    pub can_view_policy: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequest {
    /// languageCode property.
    pub language_code: Option<String>,
    /// requests property.
    pub requests: Option<Vec<GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestRequest>>,
    /// useAdminAccess property.
    pub use_admin_access: Option<bool>,
    /// view property.
    pub view: Option<String>,
    /// writeControl property.
    pub write_control: Option<GoogleAppsDriveLabelsV2WriteControl>,
}

/// `GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsRequest {
    /// requests property.
    pub requests: Option<Vec<GoogleAppsDriveLabelsV2UpdateLabelPermissionRequest>>,
    /// useAdminAccess property.
    pub use_admin_access: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2BadgeColors` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2BadgeColors {
    /// backgroundColor property.
    pub background_color: Option<GoogleTypeColor>,
    /// foregroundColor property.
    pub foreground_color: Option<GoogleTypeColor>,
    /// soloColor property.
    pub solo_color: Option<GoogleTypeColor>,
}

/// `GoogleAppsDriveLabelsV2LongTextLimits` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LongTextLimits {
    /// maxLength property.
    pub max_length: Option<i64>,
    /// minLength property.
    pub min_length: Option<i64>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateFieldTypeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateFieldTypeRequest {
    /// dateOptions property.
    pub date_options: Option<GoogleAppsDriveLabelsV2FieldDateOptions>,
    /// id property.
    pub id: Option<String>,
    /// integerOptions property.
    pub integer_options: Option<GoogleAppsDriveLabelsV2FieldIntegerOptions>,
    /// selectionOptions property.
    pub selection_options: Option<GoogleAppsDriveLabelsV2FieldSelectionOptions>,
    /// textOptions property.
    pub text_options: Option<GoogleAppsDriveLabelsV2FieldTextOptions>,
    /// updateMask property.
    pub update_mask: Option<String>,
    /// userOptions property.
    pub user_options: Option<GoogleAppsDriveLabelsV2FieldUserOptions>,
}

/// `GoogleAppsDriveLabelsV2DisableLabelRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DisableLabelRequest {
    /// disabledPolicy property.
    pub disabled_policy: Option<GoogleAppsDriveLabelsV2LifecycleDisabledPolicy>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// updateMask property.
    pub update_mask: Option<String>,
    /// useAdminAccess property.
    pub use_admin_access: Option<bool>,
    /// writeControl property.
    pub write_control: Option<GoogleAppsDriveLabelsV2WriteControl>,
}

/// `GoogleAppsDriveLabelsV2EnableLabelRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2EnableLabelRequest {
    /// languageCode property.
    pub language_code: Option<String>,
    /// useAdminAccess property.
    pub use_admin_access: Option<bool>,
    /// writeControl property.
    pub write_control: Option<GoogleAppsDriveLabelsV2WriteControl>,
}

/// `GoogleAppsDriveLabelsV2LabelAppliedLabelPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LabelAppliedLabelPolicy {
    /// copyMode property.
    pub copy_mode: Option<String>,
}

/// `GoogleAppsDriveLabelsV2ListLabelsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2ListLabelsResponse {
    /// labels property.
    pub labels: Option<Vec<GoogleAppsDriveLabelsV2Label>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceSchemaCapabilities` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceSchemaCapabilities {
    /// canDelete property.
    pub can_delete: Option<bool>,
    /// canDisable property.
    pub can_disable: Option<bool>,
    /// canEnable property.
    pub can_enable: Option<bool>,
    /// canUpdate property.
    pub can_update: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2SelectionLimits` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2SelectionLimits {
    /// listLimits property.
    pub list_limits: Option<GoogleAppsDriveLabelsV2ListLimits>,
    /// maxChoices property.
    pub max_choices: Option<i64>,
    /// maxDeletedChoices property.
    pub max_deleted_choices: Option<i64>,
    /// maxDisplayNameLength property.
    pub max_display_name_length: Option<i64>,
    /// maxIdLength property.
    pub max_id_length: Option<i64>,
}

/// `GoogleAppsDriveLabelsV2UpdateLabelPermissionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2UpdateLabelPermissionRequest {
    /// labelPermission property.
    pub label_permission: Option<GoogleAppsDriveLabelsV2LabelPermission>,
    /// parent property.
    pub parent: Option<String>,
    /// useAdminAccess property.
    pub use_admin_access: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateSelectionChoicePropertiesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateSelectionChoicePropertiesRequest {
    /// fieldId property.
    pub field_id: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// properties property.
    pub properties: Option<GoogleAppsDriveLabelsV2FieldSelectionOptionsChoiceProperties>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `GoogleAppsDriveLabelsV2DateLimits` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DateLimits {
    /// maxValue property.
    pub max_value: Option<GoogleTypeDate>,
    /// minValue property.
    pub min_value: Option<GoogleTypeDate>,
}

/// `GoogleTypeColor` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeColor {
    /// alpha property.
    pub alpha: Option<f64>,
    /// blue property.
    pub blue: Option<f64>,
    /// green property.
    pub green: Option<f64>,
    /// red property.
    pub red: Option<f64>,
}

/// `GoogleAppsDriveLabelsV2LabelPermission` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LabelPermission {
    /// audience property.
    pub audience: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// group property.
    pub group: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// person property.
    pub person: Option<String>,
    /// role property.
    pub role: Option<String>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDeleteSelectionChoiceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestDeleteSelectionChoiceRequest {
    /// fieldId property.
    pub field_id: Option<String>,
    /// id property.
    pub id: Option<String>,
}

/// `GoogleAppsDriveLabelsV2UpdateLabelCopyModeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2UpdateLabelCopyModeRequest {
    /// copyMode property.
    pub copy_mode: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// useAdminAccess property.
    pub use_admin_access: Option<bool>,
    /// view property.
    pub view: Option<String>,
}

/// `GoogleAppsDriveLabelsV2ListLabelLocksResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2ListLabelLocksResponse {
    /// labelLocks property.
    pub label_locks: Option<Vec<GoogleAppsDriveLabelsV2LabelLock>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleAppsDriveLabelsV2UserCapabilities` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2UserCapabilities {
    /// canAccessLabelManager property.
    pub can_access_label_manager: Option<bool>,
    /// canAdministrateLabels property.
    pub can_administrate_labels: Option<bool>,
    /// canCreateAdminLabels property.
    pub can_create_admin_labels: Option<bool>,
    /// canCreateSharedLabels property.
    pub can_create_shared_labels: Option<bool>,
    /// name property.
    pub name: Option<String>,
}

/// `GoogleAppsDriveLabelsV2Field` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2Field {
    /// appliedCapabilities property.
    pub applied_capabilities: Option<GoogleAppsDriveLabelsV2FieldAppliedCapabilities>,
    /// createTime property.
    pub create_time: Option<String>,
    /// creator property.
    pub creator: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// dateOptions property.
    pub date_options: Option<GoogleAppsDriveLabelsV2FieldDateOptions>,
    /// disableTime property.
    pub disable_time: Option<String>,
    /// disabler property.
    pub disabler: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// displayHints property.
    pub display_hints: Option<GoogleAppsDriveLabelsV2FieldDisplayHints>,
    /// id property.
    pub id: Option<String>,
    /// integerOptions property.
    pub integer_options: Option<GoogleAppsDriveLabelsV2FieldIntegerOptions>,
    /// lifecycle property.
    pub lifecycle: Option<GoogleAppsDriveLabelsV2Lifecycle>,
    /// lockStatus property.
    pub lock_status: Option<GoogleAppsDriveLabelsV2LockStatus>,
    /// properties property.
    pub properties: Option<GoogleAppsDriveLabelsV2FieldProperties>,
    /// publisher property.
    pub publisher: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// queryKey property.
    pub query_key: Option<String>,
    /// schemaCapabilities property.
    pub schema_capabilities: Option<GoogleAppsDriveLabelsV2FieldSchemaCapabilities>,
    /// selectionOptions property.
    pub selection_options: Option<GoogleAppsDriveLabelsV2FieldSelectionOptions>,
    /// textOptions property.
    pub text_options: Option<GoogleAppsDriveLabelsV2FieldTextOptions>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// updater property.
    pub updater: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// userOptions property.
    pub user_options: Option<GoogleAppsDriveLabelsV2FieldUserOptions>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseCreateSelectionChoiceResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseCreateSelectionChoiceResponse {
    /// fieldId property.
    pub field_id: Option<String>,
    /// id property.
    pub id: Option<String>,
}

/// `GoogleAppsDriveLabelsV2WriteControl` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2WriteControl {
    /// requiredRevisionId property.
    pub required_revision_id: Option<String>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateLabelPropertiesRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestUpdateLabelPropertiesRequest {
    /// properties property.
    pub properties: Option<GoogleAppsDriveLabelsV2LabelProperties>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponse {
    /// responses property.
    pub responses: Option<Vec<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseResponse>>,
    /// updatedLabel property.
    pub updated_label: Option<GoogleAppsDriveLabelsV2Label>,
}

/// `GoogleAppsDriveLabelsV2IntegerLimits` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2IntegerLimits {
    /// maxValue property.
    pub max_value: Option<String>,
    /// minValue property.
    pub min_value: Option<String>,
}

/// `GoogleAppsDriveLabelsV2Lifecycle` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2Lifecycle {
    /// disabledPolicy property.
    pub disabled_policy: Option<GoogleAppsDriveLabelsV2LifecycleDisabledPolicy>,
    /// hasUnpublishedChanges property.
    pub has_unpublished_changes: Option<bool>,
    /// state property.
    pub state: Option<String>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestEnableSelectionChoiceRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelRequestEnableSelectionChoiceRequest {
    /// fieldId property.
    pub field_id: Option<String>,
    /// id property.
    pub id: Option<String>,
}

/// `GoogleAppsDriveLabelsV2UserInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2UserInfo {
    /// person property.
    pub person: Option<String>,
}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseEnableFieldResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseEnableFieldResponse {}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateFieldTypeResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateFieldTypeResponse {}

/// `GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateSelectionChoicePropertiesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2DeltaUpdateLabelResponseUpdateSelectionChoicePropertiesResponse {
    /// priority property.
    pub priority: Option<i64>,
}

/// `GoogleAppsDriveLabelsV2PublishLabelRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2PublishLabelRequest {
    /// languageCode property.
    pub language_code: Option<String>,
    /// useAdminAccess property.
    pub use_admin_access: Option<bool>,
    /// writeControl property.
    pub write_control: Option<GoogleAppsDriveLabelsV2WriteControl>,
}

/// `GoogleAppsDriveLabelsV2LifecycleDisabledPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2LifecycleDisabledPolicy {
    /// hideInSearch property.
    pub hide_in_search: Option<bool>,
    /// showInApply property.
    pub show_in_apply: Option<bool>,
}

/// `GoogleAppsDriveLabelsV2Label` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAppsDriveLabelsV2Label {
    /// appliedCapabilities property.
    pub applied_capabilities: Option<GoogleAppsDriveLabelsV2LabelAppliedCapabilities>,
    /// appliedLabelPolicy property.
    pub applied_label_policy: Option<GoogleAppsDriveLabelsV2LabelAppliedLabelPolicy>,
    /// createTime property.
    pub create_time: Option<String>,
    /// creator property.
    pub creator: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// customer property.
    pub customer: Option<String>,
    /// disableTime property.
    pub disable_time: Option<String>,
    /// disabler property.
    pub disabler: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// displayHints property.
    pub display_hints: Option<GoogleAppsDriveLabelsV2LabelDisplayHints>,
    /// enabledAppSettings property.
    pub enabled_app_settings: Option<GoogleAppsDriveLabelsV2LabelEnabledAppSettings>,
    /// fields property.
    pub fields: Option<Vec<GoogleAppsDriveLabelsV2Field>>,
    /// id property.
    pub id: Option<String>,
    /// labelType property.
    pub label_type: Option<String>,
    /// learnMoreUri property.
    pub learn_more_uri: Option<String>,
    /// lifecycle property.
    pub lifecycle: Option<GoogleAppsDriveLabelsV2Lifecycle>,
    /// lockStatus property.
    pub lock_status: Option<GoogleAppsDriveLabelsV2LockStatus>,
    /// name property.
    pub name: Option<String>,
    /// properties property.
    pub properties: Option<GoogleAppsDriveLabelsV2LabelProperties>,
    /// publishTime property.
    pub publish_time: Option<String>,
    /// publisher property.
    pub publisher: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// revisionCreateTime property.
    pub revision_create_time: Option<String>,
    /// revisionCreator property.
    pub revision_creator: Option<GoogleAppsDriveLabelsV2UserInfo>,
    /// revisionId property.
    pub revision_id: Option<String>,
    /// schemaCapabilities property.
    pub schema_capabilities: Option<GoogleAppsDriveLabelsV2LabelSchemaCapabilities>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`drivelabels.labels.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsCreateArgs {
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2Label,
}

/// Arguments for [`drivelabels.labels.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
    /// Query parameter: `writeControl.requiredRevisionId`.
    pub write_control_required_revision_id: Option<String>,
}

/// Arguments for [`drivelabels.labels.delta_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsDeltaArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2DeltaUpdateLabelRequest,
}

/// Arguments for [`drivelabels.labels.disable_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsDisableArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2DisableLabelRequest,
}

/// Arguments for [`drivelabels.labels.enable_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsEnableArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2EnableLabelRequest,
}

/// Arguments for [`drivelabels.labels.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`drivelabels.labels.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsListArgs {
    /// Query parameter: `customer`.
    pub customer: Option<String>,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
    /// Query parameter: `minimumRole`.
    pub minimum_role: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `publishedOnly`.
    pub published_only: Option<String>,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`drivelabels.labels.publish_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsPublishArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2PublishLabelRequest,
}

/// Arguments for [`drivelabels.labels.updateLabelCopyMode_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsUpdateLabelCopyModeArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2UpdateLabelCopyModeRequest,
}

/// Arguments for [`drivelabels.labels.updateLabelEnabledAppSettings_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsUpdateLabelEnabledAppSettingsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2UpdateLabelEnabledAppSettingsRequest,
}

/// Arguments for [`drivelabels.labels.updatePermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsUpdatePermissionsArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2LabelPermission,
}

/// Arguments for [`drivelabels.labels.locks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsLocksListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`drivelabels.labels.permissions.batchDelete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsPermissionsBatchDeleteArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2BatchDeleteLabelPermissionsRequest,
}

/// Arguments for [`drivelabels.labels.permissions.batchUpdate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsPermissionsBatchUpdateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsRequest,
}

/// Arguments for [`drivelabels.labels.permissions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsPermissionsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2LabelPermission,
}

/// Arguments for [`drivelabels.labels.permissions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsPermissionsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
}

/// Arguments for [`drivelabels.labels.permissions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsPermissionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
}

/// Arguments for [`drivelabels.labels.revisions.updatePermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsRevisionsUpdatePermissionsArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2LabelPermission,
}

/// Arguments for [`drivelabels.labels.revisions.locks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsRevisionsLocksListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`drivelabels.labels.revisions.permissions.batchDelete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsRevisionsPermissionsBatchDeleteArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2BatchDeleteLabelPermissionsRequest,
}

/// Arguments for [`drivelabels.labels.revisions.permissions.batchUpdate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsRevisionsPermissionsBatchUpdateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsRequest,
}

/// Arguments for [`drivelabels.labels.revisions.permissions.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsRevisionsPermissionsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
    /// Request body.
    pub body: GoogleAppsDriveLabelsV2LabelPermission,
}

/// Arguments for [`drivelabels.labels.revisions.permissions.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsRevisionsPermissionsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
}

/// Arguments for [`drivelabels.labels.revisions.permissions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLabelsRevisionsPermissionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `useAdminAccess`.
    pub use_admin_access: Option<String>,
}

/// Arguments for [`drivelabels.limits.getLabel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsLimitsGetLabelArgs {
    /// Query parameter: `name`.
    pub name: Option<String>,
}

/// Arguments for [`drivelabels.users.getCapabilities_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DrivelabelsUsersGetCapabilitiesArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `customer`.
    pub customer: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v2/labels
// -----------------------------------------------------------------------------

/// POST v2/labels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://drivelabels.googleapis.com/v2/labels",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2/{+name}
// -----------------------------------------------------------------------------

/// DELETE v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://drivelabels.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.write_control_required_revision_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("writeControl_requiredRevisionId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:delta
// -----------------------------------------------------------------------------

/// POST v2/{+name}:delta.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_delta_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_delta_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsDeltaArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://drivelabels.googleapis.com/v2/{}:delta", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2DeltaUpdateLabelResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:disable
// -----------------------------------------------------------------------------

/// POST v2/{+name}:disable.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_disable_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_disable_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsDisableArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}:disable",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:enable
// -----------------------------------------------------------------------------

/// POST v2/{+name}:enable.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_enable_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_enable_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsEnableArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://drivelabels.googleapis.com/v2/{}:enable", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://drivelabels.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/labels
// -----------------------------------------------------------------------------

/// GET v2/labels.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2ListLabelsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://drivelabels.googleapis.com/v2/labels",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.customer {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("customer=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.minimum_role {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("minimumRole=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.published_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("publishedOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2ListLabelsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:publish
// -----------------------------------------------------------------------------

/// POST v2/{+name}:publish.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_publish_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_publish_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsPublishArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}:publish",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:updateLabelCopyMode
// -----------------------------------------------------------------------------

/// POST v2/{+name}:updateLabelCopyMode.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_update_label_copy_mode_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_update_label_copy_mode_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsUpdateLabelCopyModeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}:updateLabelCopyMode",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+name}:updateLabelEnabledAppSettings
// -----------------------------------------------------------------------------

/// POST v2/{+name}:updateLabelEnabledAppSettings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_update_label_enabled_app_settings_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_update_label_enabled_app_settings_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsUpdateLabelEnabledAppSettingsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}:updateLabelEnabledAppSettings",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+parent}/permissions
// -----------------------------------------------------------------------------

/// PATCH v2/{+parent}/permissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_update_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_update_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsUpdatePermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/permissions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2LabelPermission = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/locks
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/locks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_locks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_locks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsLocksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2ListLabelLocksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/locks",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2ListLabelLocksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/permissions:batchDelete
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/permissions:batchDelete.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_permissions_batch_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_permissions_batch_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsPermissionsBatchDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/permissions:batchDelete",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/permissions:batchUpdate
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/permissions:batchUpdate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_permissions_batch_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_permissions_batch_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsPermissionsBatchUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/permissions:batchUpdate",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/permissions
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/permissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_permissions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_permissions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsPermissionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/permissions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2LabelPermission = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2/{+name}
// -----------------------------------------------------------------------------

/// DELETE v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_permissions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_permissions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsPermissionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://drivelabels.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/permissions
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/permissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_permissions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_permissions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsPermissionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2ListLabelPermissionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/permissions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2ListLabelPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v2/{+parent}/permissions
// -----------------------------------------------------------------------------

/// PATCH v2/{+parent}/permissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_revisions_update_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_revisions_update_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsRevisionsUpdatePermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/permissions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2LabelPermission = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/locks
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/locks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_revisions_locks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_revisions_locks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsRevisionsLocksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2ListLabelLocksResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/locks",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2ListLabelLocksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/permissions:batchDelete
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/permissions:batchDelete.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_revisions_permissions_batch_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_revisions_permissions_batch_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsRevisionsPermissionsBatchDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/permissions:batchDelete",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/permissions:batchUpdate
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/permissions:batchUpdate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_revisions_permissions_batch_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_revisions_permissions_batch_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsRevisionsPermissionsBatchUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/permissions:batchUpdate",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/{+parent}/permissions
// -----------------------------------------------------------------------------

/// POST v2/{+parent}/permissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_revisions_permissions_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_revisions_permissions_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsRevisionsPermissionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/permissions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2LabelPermission = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2/{+name}
// -----------------------------------------------------------------------------

/// DELETE v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_revisions_permissions_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_revisions_permissions_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsRevisionsPermissionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://drivelabels.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+parent}/permissions
// -----------------------------------------------------------------------------

/// GET v2/{+parent}/permissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_labels_revisions_permissions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_labels_revisions_permissions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLabelsRevisionsPermissionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2ListLabelPermissionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://drivelabels.googleapis.com/v2/{}/permissions",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.use_admin_access {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("useAdminAccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2ListLabelPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/limits/label
// -----------------------------------------------------------------------------

/// GET v2/limits/label.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_limits_get_label_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_limits_get_label_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsLimitsGetLabelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2LabelLimits>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://drivelabels.googleapis.com/v2/limits/label",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("name=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2LabelLimits = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/{+name}
// -----------------------------------------------------------------------------

/// GET v2/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = drivelabels_users_get_capabilities_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn drivelabels_users_get_capabilities_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DrivelabelsUsersGetCapabilitiesArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAppsDriveLabelsV2UserCapabilities>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://drivelabels.googleapis.com/v2/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.customer {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("customer=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2UserCapabilities = serde_json::from_str(&body)
                    .map_err(
                    |e: serde_json::Error| super::shared::ApiError::ParseFailed(e.to_string()),
                )?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
