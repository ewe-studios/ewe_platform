//! Auto-generated API module for gcp/androidmanagement androidmanagement.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_androidmanagement_androidmanagement `

#![cfg(feature = "gcp_androidmanagement_androidmanagement")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ScreenTimeoutSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ScreenTimeoutSettings {
    /// screenTimeout property.
    pub screen_timeout: Option<String>,
    /// screenTimeoutMode property.
    pub screen_timeout_mode: Option<String>,
}

/// `Role` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Role {
    /// roleType property.
    pub role_type: Option<String>,
}

/// `RemovePolicyApplicationsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemovePolicyApplicationsRequest {
    /// packageNames property.
    pub package_names: Option<Vec<String>>,
}

/// `DpcMigrationInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DpcMigrationInfo {
    /// additionalData property.
    pub additional_data: Option<String>,
    /// previousDpc property.
    pub previous_dpc: Option<String>,
}

/// `PreferentialNetworkServiceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreferentialNetworkServiceConfig {
    /// fallbackToDefaultConnection property.
    pub fallback_to_default_connection: Option<String>,
    /// nonMatchingNetworks property.
    pub non_matching_networks: Option<String>,
    /// preferentialNetworkId property.
    pub preferential_network_id: Option<String>,
}

/// `ApnSetting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApnSetting {
    /// alwaysOnSetting property.
    pub always_on_setting: Option<String>,
    /// apn property.
    pub apn: Option<String>,
    /// apnTypes property.
    pub apn_types: Option<Vec<String>>,
    /// authType property.
    pub auth_type: Option<String>,
    /// carrierId property.
    pub carrier_id: Option<i64>,
    /// displayName property.
    pub display_name: Option<String>,
    /// mmsProxyAddress property.
    pub mms_proxy_address: Option<String>,
    /// mmsProxyPort property.
    pub mms_proxy_port: Option<i64>,
    /// mmsc property.
    pub mmsc: Option<String>,
    /// mtuV4 property.
    pub mtu_v4: Option<i64>,
    /// mtuV6 property.
    pub mtu_v6: Option<i64>,
    /// mvnoType property.
    pub mvno_type: Option<String>,
    /// networkTypes property.
    pub network_types: Option<Vec<String>>,
    /// numericOperatorId property.
    pub numeric_operator_id: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// protocol property.
    pub protocol: Option<String>,
    /// proxyAddress property.
    pub proxy_address: Option<String>,
    /// proxyPort property.
    pub proxy_port: Option<i64>,
    /// roamingProtocol property.
    pub roaming_protocol: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `GenerateEnterpriseUpgradeUrlResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateEnterpriseUpgradeUrlResponse {
    /// url property.
    pub url: Option<String>,
}

/// `ManagedProperty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedProperty {
    /// defaultValue property.
    pub default_value: Option<serde_json::Value>,
    /// description property.
    pub description: Option<String>,
    /// entries property.
    pub entries: Option<Vec<ManagedPropertyEntry>>,
    /// key property.
    pub key: Option<String>,
    /// nestedProperties property.
    pub nested_properties: Option<Vec<Box<ManagedProperty>>>,
    /// title property.
    pub title: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `EnrollmentToken` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EnrollmentToken {
    /// additionalData property.
    pub additional_data: Option<String>,
    /// allowPersonalUsage property.
    pub allow_personal_usage: Option<String>,
    /// duration property.
    pub duration: Option<String>,
    /// expirationTimestamp property.
    pub expiration_timestamp: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// oneTimeOnly property.
    pub one_time_only: Option<bool>,
    /// policyName property.
    pub policy_name: Option<String>,
    /// qrCode property.
    pub qr_code: Option<String>,
    /// user property.
    pub user: Option<User>,
    /// value property.
    pub value: Option<String>,
}

/// `SignupUrl` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SignupUrl {
    /// name property.
    pub name: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `Display` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Display {
    /// density property.
    pub density: Option<i64>,
    /// displayId property.
    pub display_id: Option<i64>,
    /// height property.
    pub height: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// refreshRate property.
    pub refresh_rate: Option<i64>,
    /// state property.
    pub state: Option<String>,
    /// width property.
    pub width: Option<i64>,
}

/// `SystemUpdateInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SystemUpdateInfo {
    /// updateReceivedTime property.
    pub update_received_time: Option<String>,
    /// updateStatus property.
    pub update_status: Option<String>,
}

/// `WebAppIcon` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WebAppIcon {
    /// imageData property.
    pub image_data: Option<String>,
}

/// `CrossProfilePolicies` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CrossProfilePolicies {
    /// crossProfileAppFunctions property.
    pub cross_profile_app_functions: Option<String>,
    /// crossProfileCopyPaste property.
    pub cross_profile_copy_paste: Option<String>,
    /// crossProfileDataSharing property.
    pub cross_profile_data_sharing: Option<String>,
    /// exemptionsToShowWorkContactsInPersonalProfile property.
    pub exemptions_to_show_work_contacts_in_personal_profile: Option<PackageNameList>,
    /// showWorkContactsInPersonalProfile property.
    pub show_work_contacts_in_personal_profile: Option<String>,
    /// workProfileWidgetsDefault property.
    pub work_profile_widgets_default: Option<String>,
}

/// `SetupAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetupAction {
    /// description property.
    pub description: Option<UserFacingMessage>,
    /// launchApp property.
    pub launch_app: Option<LaunchAppAction>,
    /// title property.
    pub title: Option<UserFacingMessage>,
}

/// `RequestDeviceInfoParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RequestDeviceInfoParams {
    /// deviceInfo property.
    pub device_info: Option<String>,
}

/// `DeviceConnectivityManagement` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeviceConnectivityManagement {
    /// apnPolicy property.
    pub apn_policy: Option<ApnPolicy>,
    /// bluetoothSharing property.
    pub bluetooth_sharing: Option<String>,
    /// configureWifi property.
    pub configure_wifi: Option<String>,
    /// preferentialNetworkServiceSettings property.
    pub preferential_network_service_settings: Option<PreferentialNetworkServiceSettings>,
    /// privateDnsSettings property.
    pub private_dns_settings: Option<PrivateDnsSettings>,
    /// tetheringSettings property.
    pub tethering_settings: Option<String>,
    /// usbDataAccess property.
    pub usb_data_access: Option<String>,
    /// wifiDirectSettings property.
    pub wifi_direct_settings: Option<String>,
    /// wifiRoamingPolicy property.
    pub wifi_roaming_policy: Option<WifiRoamingPolicy>,
    /// wifiSsidPolicy property.
    pub wifi_ssid_policy: Option<WifiSsidPolicy>,
}

/// `SecurityPosture` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SecurityPosture {
    /// devicePosture property.
    pub device_posture: Option<String>,
    /// postureDetails property.
    pub posture_details: Option<Vec<PostureDetail>>,
}

/// `RemovePolicyApplicationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemovePolicyApplicationsResponse {
    /// policy property.
    pub policy: Option<Policy>,
}

/// `WifiSsidPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WifiSsidPolicy {
    /// wifiSsidPolicyType property.
    pub wifi_ssid_policy_type: Option<String>,
    /// wifiSsids property.
    pub wifi_ssids: Option<Vec<WifiSsid>>,
}

/// `ProvisioningInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProvisioningInfo {
    /// apiLevel property.
    pub api_level: Option<i64>,
    /// authenticatedUserEmail property.
    pub authenticated_user_email: Option<String>,
    /// brand property.
    pub brand: Option<String>,
    /// enterprise property.
    pub enterprise: Option<String>,
    /// imei property.
    pub imei: Option<String>,
    /// managementMode property.
    pub management_mode: Option<String>,
    /// meid property.
    pub meid: Option<String>,
    /// model property.
    pub model: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// ownership property.
    pub ownership: Option<String>,
    /// serialNumber property.
    pub serial_number: Option<String>,
}

/// `ListPoliciesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListPoliciesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// policies property.
    pub policies: Option<Vec<Policy>>,
}

/// `ListDevicesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListDevicesResponse {
    /// devices property.
    pub devices: Option<Vec<Device>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleAuthenticationSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAuthenticationSettings {
    /// googleAuthenticationRequired property.
    pub google_authentication_required: Option<String>,
}

/// `StartLostModeParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StartLostModeParams {
    /// lostEmailAddress property.
    pub lost_email_address: Option<String>,
    /// lostMessage property.
    pub lost_message: Option<UserFacingMessage>,
    /// lostOrganization property.
    pub lost_organization: Option<UserFacingMessage>,
    /// lostPhoneNumber property.
    pub lost_phone_number: Option<UserFacingMessage>,
    /// lostStreetAddress property.
    pub lost_street_address: Option<UserFacingMessage>,
}

/// `EidInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EidInfo {
    /// eids property.
    pub eids: Option<Vec<Eid>>,
}

/// `Application` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Application {
    /// appPricing property.
    pub app_pricing: Option<String>,
    /// appTracks property.
    pub app_tracks: Option<Vec<AppTrackInfo>>,
    /// appVersions property.
    pub app_versions: Option<Vec<AppVersion>>,
    /// author property.
    pub author: Option<String>,
    /// availableCountries property.
    pub available_countries: Option<Vec<String>>,
    /// category property.
    pub category: Option<String>,
    /// contentRating property.
    pub content_rating: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// distributionChannel property.
    pub distribution_channel: Option<String>,
    /// features property.
    pub features: Option<Vec<String>>,
    /// fullDescription property.
    pub full_description: Option<String>,
    /// iconUrl property.
    pub icon_url: Option<String>,
    /// managedProperties property.
    pub managed_properties: Option<Vec<Box<ManagedProperty>>>,
    /// minAndroidSdkVersion property.
    pub min_android_sdk_version: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// permissions property.
    pub permissions: Option<Vec<ApplicationPermission>>,
    /// playStoreUrl property.
    pub play_store_url: Option<String>,
    /// recentChanges property.
    pub recent_changes: Option<String>,
    /// screenshotUrls property.
    pub screenshot_urls: Option<Vec<String>>,
    /// smallIconUrl property.
    pub small_icon_url: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `EsimInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EsimInfo {
    /// iccId property.
    pub icc_id: Option<String>,
}

/// `ApplicationEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApplicationEvent {
    /// createTime property.
    pub create_time: Option<String>,
    /// eventType property.
    pub event_type: Option<String>,
}

/// `EsimCommandStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EsimCommandStatus {
    /// esimInfo property.
    pub esim_info: Option<EsimInfo>,
    /// internalErrorDetails property.
    pub internal_error_details: Option<InternalErrorDetails>,
    /// status property.
    pub status: Option<String>,
}

/// `InstallConstraint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstallConstraint {
    /// chargingConstraint property.
    pub charging_constraint: Option<String>,
    /// deviceIdleConstraint property.
    pub device_idle_constraint: Option<String>,
    /// networkTypeConstraint property.
    pub network_type_constraint: Option<String>,
}

/// `Enterprise` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Enterprise {
    /// appAutoApprovalEnabled property.
    pub app_auto_approval_enabled: Option<bool>,
    /// contactInfo property.
    pub contact_info: Option<ContactInfo>,
    /// enabledNotificationTypes property.
    pub enabled_notification_types: Option<Vec<String>>,
    /// enterpriseDisplayName property.
    pub enterprise_display_name: Option<String>,
    /// enterpriseType property.
    pub enterprise_type: Option<String>,
    /// googleAuthenticationSettings property.
    pub google_authentication_settings: Option<GoogleAuthenticationSettings>,
    /// logo property.
    pub logo: Option<ExternalData>,
    /// managedGoogleDomainType property.
    pub managed_google_domain_type: Option<String>,
    /// managedGooglePlayAccountsEnterpriseType property.
    pub managed_google_play_accounts_enterprise_type: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// primaryColor property.
    pub primary_color: Option<i64>,
    /// pubsubTopic property.
    pub pubsub_topic: Option<String>,
    /// signinDetails property.
    pub signin_details: Option<Vec<SigninDetail>>,
    /// termsAndConditions property.
    pub terms_and_conditions: Option<Vec<TermsAndConditions>>,
}

/// `DefaultApplicationContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DefaultApplicationContext {
    /// defaultApplicationScope property.
    pub default_application_scope: Option<String>,
}

/// `User` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct User {
    /// accountIdentifier property.
    pub account_identifier: Option<String>,
}

/// `WebToken` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WebToken {
    /// enabledFeatures property.
    pub enabled_features: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// parentFrameUrl property.
    pub parent_frame_url: Option<String>,
    /// permissions property.
    pub permissions: Option<Vec<String>>,
    /// value property.
    pub value: Option<String>,
}

/// `ListWebAppsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListWebAppsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// webApps property.
    pub web_apps: Option<Vec<WebApp>>,
}

/// `Eid` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Eid {
    /// eid property.
    pub eid: Option<String>,
}

/// `PasswordRequirements` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PasswordRequirements {
    /// maximumFailedPasswordsForWipe property.
    pub maximum_failed_passwords_for_wipe: Option<i64>,
    /// passwordExpirationTimeout property.
    pub password_expiration_timeout: Option<String>,
    /// passwordHistoryLength property.
    pub password_history_length: Option<i64>,
    /// passwordMinimumLength property.
    pub password_minimum_length: Option<i64>,
    /// passwordMinimumLetters property.
    pub password_minimum_letters: Option<i64>,
    /// passwordMinimumLowerCase property.
    pub password_minimum_lower_case: Option<i64>,
    /// passwordMinimumNonLetter property.
    pub password_minimum_non_letter: Option<i64>,
    /// passwordMinimumNumeric property.
    pub password_minimum_numeric: Option<i64>,
    /// passwordMinimumSymbols property.
    pub password_minimum_symbols: Option<i64>,
    /// passwordMinimumUpperCase property.
    pub password_minimum_upper_case: Option<i64>,
    /// passwordQuality property.
    pub password_quality: Option<String>,
    /// passwordScope property.
    pub password_scope: Option<String>,
    /// requirePasswordUnlock property.
    pub require_password_unlock: Option<String>,
    /// unifiedLockSettings property.
    pub unified_lock_settings: Option<String>,
}

/// `AppTrackInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppTrackInfo {
    /// trackAlias property.
    pub track_alias: Option<String>,
    /// trackId property.
    pub track_id: Option<String>,
}

/// `DefaultApplicationSettingAttempt` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DefaultApplicationSettingAttempt {
    /// attemptOutcome property.
    pub attempt_outcome: Option<String>,
    /// packageName property.
    pub package_name: Option<String>,
}

/// `LaunchAppAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LaunchAppAction {
    /// packageName property.
    pub package_name: Option<String>,
}

/// `ApnPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApnPolicy {
    /// apnSettings property.
    pub apn_settings: Option<Vec<ApnSetting>>,
    /// overrideApns property.
    pub override_apns: Option<String>,
}

/// `WifiRoamingSetting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WifiRoamingSetting {
    /// wifiRoamingMode property.
    pub wifi_roaming_mode: Option<String>,
    /// wifiSsid property.
    pub wifi_ssid: Option<String>,
}

/// `DeviceRadioState` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeviceRadioState {
    /// airplaneModeState property.
    pub airplane_mode_state: Option<String>,
    /// cellularTwoGState property.
    pub cellular_two_g_state: Option<String>,
    /// minimumWifiSecurityLevel property.
    pub minimum_wifi_security_level: Option<String>,
    /// ultraWidebandState property.
    pub ultra_wideband_state: Option<String>,
    /// userInitiatedAddEsimSettings property.
    pub user_initiated_add_esim_settings: Option<String>,
    /// wifiState property.
    pub wifi_state: Option<String>,
}

/// `OncCertificateProvider` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OncCertificateProvider {
    /// certificateReferences property.
    pub certificate_references: Option<Vec<String>>,
    /// contentProviderEndpoint property.
    pub content_provider_endpoint: Option<ContentProviderEndpoint>,
}

/// `PolicyEnforcementRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PolicyEnforcementRule {
    /// blockAction property.
    pub block_action: Option<BlockAction>,
    /// settingName property.
    pub setting_name: Option<String>,
    /// wipeAction property.
    pub wipe_action: Option<WipeAction>,
}

/// `ExtensionConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExtensionConfig {
    /// notificationReceiver property.
    pub notification_receiver: Option<String>,
    /// signingKeyFingerprintsSha256 property.
    pub signing_key_fingerprints_sha256: Option<Vec<String>>,
}

/// `DeviceSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeviceSettings {
    /// adbEnabled property.
    pub adb_enabled: Option<bool>,
    /// developmentSettingsEnabled property.
    pub development_settings_enabled: Option<bool>,
    /// encryptionStatus property.
    pub encryption_status: Option<String>,
    /// isDeviceSecure property.
    pub is_device_secure: Option<bool>,
    /// isEncrypted property.
    pub is_encrypted: Option<bool>,
    /// unknownSourcesEnabled property.
    pub unknown_sources_enabled: Option<bool>,
    /// verifyAppsEnabled property.
    pub verify_apps_enabled: Option<bool>,
}

/// `UsageLog` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UsageLog {
    /// enabledLogTypes property.
    pub enabled_log_types: Option<Vec<String>>,
    /// uploadOnCellularAllowed property.
    pub upload_on_cellular_allowed: Option<Vec<String>>,
}

/// `TelephonyInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TelephonyInfo {
    /// activationState property.
    pub activation_state: Option<String>,
    /// carrierName property.
    pub carrier_name: Option<String>,
    /// configMode property.
    pub config_mode: Option<String>,
    /// iccId property.
    pub icc_id: Option<String>,
    /// phoneNumber property.
    pub phone_number: Option<String>,
}

/// `DefaultApplicationInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DefaultApplicationInfo {
    /// defaultApplicationSettingAttempts property.
    pub default_application_setting_attempts: Option<Vec<DefaultApplicationSettingAttempt>>,
    /// defaultApplicationType property.
    pub default_application_type: Option<String>,
    /// packageName property.
    pub package_name: Option<String>,
}

/// `AppVersion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppVersion {
    /// production property.
    pub production: Option<bool>,
    /// trackIds property.
    pub track_ids: Option<Vec<String>>,
    /// versionCode property.
    pub version_code: Option<i64>,
    /// versionString property.
    pub version_string: Option<String>,
}

/// `SpecificNonComplianceContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpecificNonComplianceContext {
    /// defaultApplicationContext property.
    pub default_application_context: Option<DefaultApplicationContext>,
    /// oncWifiContext property.
    pub onc_wifi_context: Option<OncWifiContext>,
    /// passwordPoliciesContext property.
    pub password_policies_context: Option<PasswordPoliciesContext>,
}

/// `ScreenBrightnessSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ScreenBrightnessSettings {
    /// screenBrightness property.
    pub screen_brightness: Option<i64>,
    /// screenBrightnessMode property.
    pub screen_brightness_mode: Option<String>,
}

/// `Date` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `DefaultApplication` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DefaultApplication {
    /// packageName property.
    pub package_name: Option<String>,
}

/// `ApplicationPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApplicationPolicy {
    /// accessibleTrackIds property.
    pub accessible_track_ids: Option<Vec<String>>,
    /// alwaysOnVpnLockdownExemption property.
    pub always_on_vpn_lockdown_exemption: Option<String>,
    /// autoUpdateMode property.
    pub auto_update_mode: Option<String>,
    /// connectedWorkAndPersonalApp property.
    pub connected_work_and_personal_app: Option<String>,
    /// credentialProviderPolicy property.
    pub credential_provider_policy: Option<String>,
    /// customAppConfig property.
    pub custom_app_config: Option<CustomAppConfig>,
    /// defaultPermissionPolicy property.
    pub default_permission_policy: Option<String>,
    /// delegatedScopes property.
    pub delegated_scopes: Option<Vec<String>>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// extensionConfig property.
    pub extension_config: Option<ExtensionConfig>,
    /// installConstraint property.
    pub install_constraint: Option<Vec<InstallConstraint>>,
    /// installPriority property.
    pub install_priority: Option<i64>,
    /// installType property.
    pub install_type: Option<String>,
    /// lockTaskAllowed property.
    pub lock_task_allowed: Option<bool>,
    /// managedConfiguration property.
    pub managed_configuration: Option<serde_json::Value>,
    /// managedConfigurationTemplate property.
    pub managed_configuration_template: Option<ManagedConfigurationTemplate>,
    /// minimumVersionCode property.
    pub minimum_version_code: Option<i64>,
    /// packageName property.
    pub package_name: Option<String>,
    /// permissionGrants property.
    pub permission_grants: Option<Vec<PermissionGrant>>,
    /// preferentialNetworkId property.
    pub preferential_network_id: Option<String>,
    /// roles property.
    pub roles: Option<Vec<Role>>,
    /// signingKeyCerts property.
    pub signing_key_certs: Option<Vec<ApplicationSigningKeyCert>>,
    /// userControlSettings property.
    pub user_control_settings: Option<String>,
    /// workProfileWidgets property.
    pub work_profile_widgets: Option<String>,
}

/// `InternalErrorDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InternalErrorDetails {
    /// errorCode property.
    pub error_code: Option<String>,
    /// errorCodeDetail property.
    pub error_code_detail: Option<String>,
    /// operationCode property.
    pub operation_code: Option<String>,
    /// operationCodeDetail property.
    pub operation_code_detail: Option<String>,
}

/// `TermsAndConditions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TermsAndConditions {
    /// content property.
    pub content: Option<UserFacingMessage>,
    /// header property.
    pub header: Option<UserFacingMessage>,
}

/// `AlwaysOnVpnPackage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AlwaysOnVpnPackage {
    /// lockdownEnabled property.
    pub lockdown_enabled: Option<bool>,
    /// packageName property.
    pub package_name: Option<String>,
}

/// `StatusReportingSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StatusReportingSettings {
    /// applicationReportingSettings property.
    pub application_reporting_settings: Option<ApplicationReportingSettings>,
    /// applicationReportsEnabled property.
    pub application_reports_enabled: Option<bool>,
    /// commonCriteriaModeEnabled property.
    pub common_criteria_mode_enabled: Option<bool>,
    /// defaultApplicationInfoReportingEnabled property.
    pub default_application_info_reporting_enabled: Option<bool>,
    /// deviceSettingsEnabled property.
    pub device_settings_enabled: Option<bool>,
    /// displayInfoEnabled property.
    pub display_info_enabled: Option<bool>,
    /// hardwareStatusEnabled property.
    pub hardware_status_enabled: Option<bool>,
    /// memoryInfoEnabled property.
    pub memory_info_enabled: Option<bool>,
    /// networkInfoEnabled property.
    pub network_info_enabled: Option<bool>,
    /// powerManagementEventsEnabled property.
    pub power_management_events_enabled: Option<bool>,
    /// softwareInfoEnabled property.
    pub software_info_enabled: Option<bool>,
    /// systemPropertiesEnabled property.
    pub system_properties_enabled: Option<bool>,
}

/// `ClearAppsDataStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClearAppsDataStatus {
    /// results property.
    pub results: Option<serde_json::Value>,
}

/// `PreferentialNetworkServiceSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PreferentialNetworkServiceSettings {
    /// defaultPreferentialNetworkId property.
    pub default_preferential_network_id: Option<String>,
    /// preferentialNetworkServiceConfigs property.
    pub preferential_network_service_configs: Option<Vec<PreferentialNetworkServiceConfig>>,
}

/// `StopLostModeStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StopLostModeStatus {
    /// status property.
    pub status: Option<String>,
}

/// `GenerateEnterpriseUpgradeUrlRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateEnterpriseUpgradeUrlRequest {
    /// adminEmail property.
    pub admin_email: Option<String>,
    /// allowedDomains property.
    pub allowed_domains: Option<Vec<String>>,
}

/// `FreezePeriod` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FreezePeriod {
    /// endDate property.
    pub end_date: Option<Date>,
    /// startDate property.
    pub start_date: Option<Date>,
}

/// `PersonalUsagePolicies` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PersonalUsagePolicies {
    /// accountTypesWithManagementDisabled property.
    pub account_types_with_management_disabled: Option<Vec<String>>,
    /// bluetoothSharing property.
    pub bluetooth_sharing: Option<String>,
    /// cameraDisabled property.
    pub camera_disabled: Option<bool>,
    /// maxDaysWithWorkOff property.
    pub max_days_with_work_off: Option<i64>,
    /// personalApplications property.
    pub personal_applications: Option<Vec<PersonalApplicationPolicy>>,
    /// personalPlayStoreMode property.
    pub personal_play_store_mode: Option<String>,
    /// privateSpacePolicy property.
    pub private_space_policy: Option<String>,
    /// screenCaptureDisabled property.
    pub screen_capture_disabled: Option<bool>,
}

/// `BlockAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BlockAction {
    /// blockAfterDays property.
    pub block_after_days: Option<i64>,
    /// blockScope property.
    pub block_scope: Option<String>,
}

/// `ContactInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContactInfo {
    /// contactEmail property.
    pub contact_email: Option<String>,
    /// dataProtectionOfficerEmail property.
    pub data_protection_officer_email: Option<String>,
    /// dataProtectionOfficerName property.
    pub data_protection_officer_name: Option<String>,
    /// dataProtectionOfficerPhone property.
    pub data_protection_officer_phone: Option<String>,
    /// euRepresentativeEmail property.
    pub eu_representative_email: Option<String>,
    /// euRepresentativeName property.
    pub eu_representative_name: Option<String>,
    /// euRepresentativePhone property.
    pub eu_representative_phone: Option<String>,
}

/// `DefaultApplicationSetting` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DefaultApplicationSetting {
    /// defaultApplicationScopes property.
    pub default_application_scopes: Option<Vec<String>>,
    /// defaultApplicationType property.
    pub default_application_type: Option<String>,
    /// defaultApplications property.
    pub default_applications: Option<Vec<DefaultApplication>>,
}

/// `PackageNameList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PackageNameList {
    /// packageNames property.
    pub package_names: Option<Vec<String>>,
}

/// `ApplicationSigningKeyCert` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApplicationSigningKeyCert {
    /// signingKeyCertFingerprintSha256 property.
    pub signing_key_cert_fingerprint_sha256: Option<String>,
}

/// `ExternalData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ExternalData {
    /// sha256Hash property.
    pub sha256_hash: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `MigrationToken` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MigrationToken {
    /// additionalData property.
    pub additional_data: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// device property.
    pub device: Option<String>,
    /// deviceId property.
    pub device_id: Option<String>,
    /// expireTime property.
    pub expire_time: Option<String>,
    /// managementMode property.
    pub management_mode: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// policy property.
    pub policy: Option<String>,
    /// ttl property.
    pub ttl: Option<String>,
    /// userId property.
    pub user_id: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `ModifyPolicyApplicationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ModifyPolicyApplicationsResponse {
    /// policy property.
    pub policy: Option<Policy>,
}

/// `AddEsimParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AddEsimParams {
    /// activationCode property.
    pub activation_code: Option<String>,
    /// activationState property.
    pub activation_state: Option<String>,
}

/// `ApplicationReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApplicationReport {
    /// applicationSource property.
    pub application_source: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// events property.
    pub events: Option<Vec<ApplicationEvent>>,
    /// installerPackageName property.
    pub installer_package_name: Option<String>,
    /// keyedAppStates property.
    pub keyed_app_states: Option<Vec<KeyedAppState>>,
    /// packageName property.
    pub package_name: Option<String>,
    /// packageSha256Hash property.
    pub package_sha256_hash: Option<String>,
    /// signingKeyCertFingerprints property.
    pub signing_key_cert_fingerprints: Option<Vec<String>>,
    /// state property.
    pub state: Option<String>,
    /// userFacingType property.
    pub user_facing_type: Option<String>,
    /// versionCode property.
    pub version_code: Option<i64>,
    /// versionName property.
    pub version_name: Option<String>,
}

/// `CustomAppConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomAppConfig {
    /// userUninstallSettings property.
    pub user_uninstall_settings: Option<String>,
}

/// `HardwareStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HardwareStatus {
    /// batteryTemperatures property.
    pub battery_temperatures: Option<Vec<f64>>,
    /// cpuTemperatures property.
    pub cpu_temperatures: Option<Vec<f64>>,
    /// cpuUsages property.
    pub cpu_usages: Option<Vec<f64>>,
    /// createTime property.
    pub create_time: Option<String>,
    /// fanSpeeds property.
    pub fan_speeds: Option<Vec<f64>>,
    /// gpuTemperatures property.
    pub gpu_temperatures: Option<Vec<f64>>,
    /// skinTemperatures property.
    pub skin_temperatures: Option<Vec<f64>>,
}

/// `ListEnrollmentTokensResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListEnrollmentTokensResponse {
    /// enrollmentTokens property.
    pub enrollment_tokens: Option<Vec<EnrollmentToken>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ApplicationPermission` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApplicationPermission {
    /// description property.
    pub description: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// permissionId property.
    pub permission_id: Option<String>,
}

/// `Device` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Device {
    /// apiLevel property.
    pub api_level: Option<i64>,
    /// applicationReports property.
    pub application_reports: Option<Vec<ApplicationReport>>,
    /// appliedPasswordPolicies property.
    pub applied_password_policies: Option<Vec<PasswordRequirements>>,
    /// appliedPolicyName property.
    pub applied_policy_name: Option<String>,
    /// appliedPolicyVersion property.
    pub applied_policy_version: Option<String>,
    /// appliedState property.
    pub applied_state: Option<String>,
    /// commonCriteriaModeInfo property.
    pub common_criteria_mode_info: Option<CommonCriteriaModeInfo>,
    /// defaultApplicationInfo property.
    pub default_application_info: Option<Vec<DefaultApplicationInfo>>,
    /// deviceSettings property.
    pub device_settings: Option<DeviceSettings>,
    /// disabledReason property.
    pub disabled_reason: Option<UserFacingMessage>,
    /// displays property.
    pub displays: Option<Vec<Display>>,
    /// dpcMigrationInfo property.
    pub dpc_migration_info: Option<DpcMigrationInfo>,
    /// enrollmentTime property.
    pub enrollment_time: Option<String>,
    /// enrollmentTokenData property.
    pub enrollment_token_data: Option<String>,
    /// enrollmentTokenName property.
    pub enrollment_token_name: Option<String>,
    /// hardwareInfo property.
    pub hardware_info: Option<HardwareInfo>,
    /// hardwareStatusSamples property.
    pub hardware_status_samples: Option<Vec<HardwareStatus>>,
    /// lastPolicyComplianceReportTime property.
    pub last_policy_compliance_report_time: Option<String>,
    /// lastPolicySyncTime property.
    pub last_policy_sync_time: Option<String>,
    /// lastStatusReportTime property.
    pub last_status_report_time: Option<String>,
    /// managementMode property.
    pub management_mode: Option<String>,
    /// memoryEvents property.
    pub memory_events: Option<Vec<MemoryEvent>>,
    /// memoryInfo property.
    pub memory_info: Option<MemoryInfo>,
    /// name property.
    pub name: Option<String>,
    /// networkInfo property.
    pub network_info: Option<NetworkInfo>,
    /// nonComplianceDetails property.
    pub non_compliance_details: Option<Vec<NonComplianceDetail>>,
    /// ownership property.
    pub ownership: Option<String>,
    /// policyCompliant property.
    pub policy_compliant: Option<bool>,
    /// policyName property.
    pub policy_name: Option<String>,
    /// powerManagementEvents property.
    pub power_management_events: Option<Vec<PowerManagementEvent>>,
    /// previousDeviceNames property.
    pub previous_device_names: Option<Vec<String>>,
    /// securityPosture property.
    pub security_posture: Option<SecurityPosture>,
    /// softwareInfo property.
    pub software_info: Option<SoftwareInfo>,
    /// state property.
    pub state: Option<String>,
    /// systemProperties property.
    pub system_properties: Option<serde_json::Value>,
    /// user property.
    pub user: Option<User>,
    /// userName property.
    pub user_name: Option<String>,
}

/// `MemoryInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MemoryInfo {
    /// totalInternalStorage property.
    pub total_internal_storage: Option<String>,
    /// totalRam property.
    pub total_ram: Option<String>,
}

/// `ManagedPropertyEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedPropertyEntry {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `OncWifiContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OncWifiContext {
    /// wifiGuid property.
    pub wifi_guid: Option<String>,
}

/// `EuiccChipInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EuiccChipInfo {
    /// eid property.
    pub eid: Option<String>,
}

/// `ContentProviderEndpoint` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ContentProviderEndpoint {
    /// packageName property.
    pub package_name: Option<String>,
    /// signingCertsSha256 property.
    pub signing_certs_sha256: Option<Vec<String>>,
    /// uri property.
    pub uri: Option<String>,
}

/// `PostureDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostureDetail {
    /// advice property.
    pub advice: Option<Vec<UserFacingMessage>>,
    /// securityRisk property.
    pub security_risk: Option<String>,
}

/// `KioskCustomization` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct KioskCustomization {
    /// deviceSettings property.
    pub device_settings: Option<String>,
    /// powerButtonActions property.
    pub power_button_actions: Option<String>,
    /// statusBar property.
    pub status_bar: Option<String>,
    /// systemErrorWarnings property.
    pub system_error_warnings: Option<String>,
    /// systemNavigation property.
    pub system_navigation: Option<String>,
}

/// `WipeParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WipeParams {
    /// wipeDataFlags property.
    pub wipe_data_flags: Option<Vec<String>>,
    /// wipeReason property.
    pub wipe_reason: Option<UserFacingMessage>,
}

/// `SigninDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SigninDetail {
    /// allowPersonalUsage property.
    pub allow_personal_usage: Option<String>,
    /// defaultStatus property.
    pub default_status: Option<String>,
    /// qrCode property.
    pub qr_code: Option<String>,
    /// signinEnrollmentToken property.
    pub signin_enrollment_token: Option<String>,
    /// signinUrl property.
    pub signin_url: Option<String>,
    /// tokenTag property.
    pub token_tag: Option<String>,
}

/// `ListMigrationTokensResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListMigrationTokensResponse {
    /// migrationTokens property.
    pub migration_tokens: Option<Vec<MigrationToken>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `AdvancedSecurityOverrides` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdvancedSecurityOverrides {
    /// commonCriteriaMode property.
    pub common_criteria_mode: Option<String>,
    /// contentProtectionPolicy property.
    pub content_protection_policy: Option<String>,
    /// developerSettings property.
    pub developer_settings: Option<String>,
    /// googlePlayProtectVerifyApps property.
    pub google_play_protect_verify_apps: Option<String>,
    /// mtePolicy property.
    pub mte_policy: Option<String>,
    /// personalAppsThatCanReadWorkNotifications property.
    pub personal_apps_that_can_read_work_notifications: Option<Vec<String>>,
    /// untrustedAppsPolicy property.
    pub untrusted_apps_policy: Option<String>,
}

/// `PersistentPreferredActivity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PersistentPreferredActivity {
    /// actions property.
    pub actions: Option<Vec<String>>,
    /// categories property.
    pub categories: Option<Vec<String>>,
    /// receiverActivity property.
    pub receiver_activity: Option<String>,
}

/// `ApplicationReportingSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApplicationReportingSettings {
    /// includeRemovedApps property.
    pub include_removed_apps: Option<bool>,
}

/// `RequestDeviceInfoStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RequestDeviceInfoStatus {
    /// eidInfo property.
    pub eid_info: Option<EidInfo>,
    /// status property.
    pub status: Option<String>,
}

/// `ManagedConfigurationTemplate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManagedConfigurationTemplate {
    /// configurationVariables property.
    pub configuration_variables: Option<serde_json::Value>,
    /// templateId property.
    pub template_id: Option<String>,
}

/// `ComplianceRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ComplianceRule {
    /// apiLevelCondition property.
    pub api_level_condition: Option<ApiLevelCondition>,
    /// disableApps property.
    pub disable_apps: Option<bool>,
    /// nonComplianceDetailCondition property.
    pub non_compliance_detail_condition: Option<NonComplianceDetailCondition>,
    /// packageNamesToDisable property.
    pub package_names_to_disable: Option<Vec<String>>,
}

/// `WorkAccountSetupConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WorkAccountSetupConfig {
    /// authenticationType property.
    pub authentication_type: Option<String>,
    /// requiredAccountEmail property.
    pub required_account_email: Option<String>,
}

/// `NetworkInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkInfo {
    /// imei property.
    pub imei: Option<String>,
    /// meid property.
    pub meid: Option<String>,
    /// networkOperatorName property.
    pub network_operator_name: Option<String>,
    /// telephonyInfos property.
    pub telephony_infos: Option<Vec<TelephonyInfo>>,
    /// wifiMacAddress property.
    pub wifi_mac_address: Option<String>,
}

/// `WebApp` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WebApp {
    /// displayMode property.
    pub display_mode: Option<String>,
    /// icons property.
    pub icons: Option<Vec<WebAppIcon>>,
    /// name property.
    pub name: Option<String>,
    /// startUrl property.
    pub start_url: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// versionCode property.
    pub version_code: Option<String>,
}

/// `ApiLevelCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApiLevelCondition {
    /// minApiLevel property.
    pub min_api_level: Option<i64>,
}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `StartLostModeStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StartLostModeStatus {
    /// status property.
    pub status: Option<String>,
}

/// `SoftwareInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SoftwareInfo {
    /// androidBuildNumber property.
    pub android_build_number: Option<String>,
    /// androidBuildTime property.
    pub android_build_time: Option<String>,
    /// androidDevicePolicyVersionCode property.
    pub android_device_policy_version_code: Option<i64>,
    /// androidDevicePolicyVersionName property.
    pub android_device_policy_version_name: Option<String>,
    /// androidVersion property.
    pub android_version: Option<String>,
    /// bootloaderVersion property.
    pub bootloader_version: Option<String>,
    /// deviceBuildSignature property.
    pub device_build_signature: Option<String>,
    /// deviceKernelVersion property.
    pub device_kernel_version: Option<String>,
    /// primaryLanguageCode property.
    pub primary_language_code: Option<String>,
    /// securityPatchLevel property.
    pub security_patch_level: Option<String>,
    /// systemUpdateInfo property.
    pub system_update_info: Option<SystemUpdateInfo>,
}

/// `PowerManagementEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PowerManagementEvent {
    /// batteryLevel property.
    pub battery_level: Option<f64>,
    /// createTime property.
    pub create_time: Option<String>,
    /// eventType property.
    pub event_type: Option<String>,
}

/// `ProxyInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProxyInfo {
    /// excludedHosts property.
    pub excluded_hosts: Option<Vec<String>>,
    /// host property.
    pub host: Option<String>,
    /// pacUri property.
    pub pac_uri: Option<String>,
    /// port property.
    pub port: Option<i64>,
}

/// `Command` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Command {
    /// addEsimParams property.
    pub add_esim_params: Option<AddEsimParams>,
    /// clearAppsDataParams property.
    pub clear_apps_data_params: Option<ClearAppsDataParams>,
    /// clearAppsDataStatus property.
    pub clear_apps_data_status: Option<ClearAppsDataStatus>,
    /// createTime property.
    pub create_time: Option<String>,
    /// duration property.
    pub duration: Option<String>,
    /// errorCode property.
    pub error_code: Option<String>,
    /// esimStatus property.
    pub esim_status: Option<EsimCommandStatus>,
    /// newPassword property.
    pub new_password: Option<String>,
    /// removeEsimParams property.
    pub remove_esim_params: Option<RemoveEsimParams>,
    /// requestDeviceInfoParams property.
    pub request_device_info_params: Option<RequestDeviceInfoParams>,
    /// requestDeviceInfoStatus property.
    pub request_device_info_status: Option<RequestDeviceInfoStatus>,
    /// resetPasswordFlags property.
    pub reset_password_flags: Option<Vec<String>>,
    /// startLostModeParams property.
    pub start_lost_mode_params: Option<StartLostModeParams>,
    /// startLostModeStatus property.
    pub start_lost_mode_status: Option<StartLostModeStatus>,
    /// stopLostModeParams property.
    pub stop_lost_mode_params: Option<StopLostModeParams>,
    /// stopLostModeStatus property.
    pub stop_lost_mode_status: Option<StopLostModeStatus>,
    /// type property.
    pub r#type: Option<String>,
    /// userName property.
    pub user_name: Option<String>,
    /// wipeParams property.
    pub wipe_params: Option<WipeParams>,
}

/// `PersonalApplicationPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PersonalApplicationPolicy {
    /// installType property.
    pub install_type: Option<String>,
    /// packageName property.
    pub package_name: Option<String>,
}

/// `CommonCriteriaModeInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CommonCriteriaModeInfo {
    /// commonCriteriaModeStatus property.
    pub common_criteria_mode_status: Option<String>,
    /// policySignatureVerificationStatus property.
    pub policy_signature_verification_status: Option<String>,
}

/// `HardwareInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HardwareInfo {
    /// batteryShutdownTemperatures property.
    pub battery_shutdown_temperatures: Option<Vec<f64>>,
    /// batteryThrottlingTemperatures property.
    pub battery_throttling_temperatures: Option<Vec<f64>>,
    /// brand property.
    pub brand: Option<String>,
    /// cpuShutdownTemperatures property.
    pub cpu_shutdown_temperatures: Option<Vec<f64>>,
    /// cpuThrottlingTemperatures property.
    pub cpu_throttling_temperatures: Option<Vec<f64>>,
    /// deviceBasebandVersion property.
    pub device_baseband_version: Option<String>,
    /// enterpriseSpecificId property.
    pub enterprise_specific_id: Option<String>,
    /// euiccChipInfo property.
    pub euicc_chip_info: Option<Vec<EuiccChipInfo>>,
    /// gpuShutdownTemperatures property.
    pub gpu_shutdown_temperatures: Option<Vec<f64>>,
    /// gpuThrottlingTemperatures property.
    pub gpu_throttling_temperatures: Option<Vec<f64>>,
    /// hardware property.
    pub hardware: Option<String>,
    /// manufacturer property.
    pub manufacturer: Option<String>,
    /// model property.
    pub model: Option<String>,
    /// serialNumber property.
    pub serial_number: Option<String>,
    /// skinShutdownTemperatures property.
    pub skin_shutdown_temperatures: Option<Vec<f64>>,
    /// skinThrottlingTemperatures property.
    pub skin_throttling_temperatures: Option<Vec<f64>>,
}

/// `ListEnterprisesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListEnterprisesResponse {
    /// enterprises property.
    pub enterprises: Option<Vec<Enterprise>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ListOperationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListOperationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<Operation>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `Policy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Policy {
    /// accountTypesWithManagementDisabled property.
    pub account_types_with_management_disabled: Option<Vec<String>>,
    /// addUserDisabled property.
    pub add_user_disabled: Option<bool>,
    /// adjustVolumeDisabled property.
    pub adjust_volume_disabled: Option<bool>,
    /// advancedSecurityOverrides property.
    pub advanced_security_overrides: Option<AdvancedSecurityOverrides>,
    /// alwaysOnVpnPackage property.
    pub always_on_vpn_package: Option<AlwaysOnVpnPackage>,
    /// androidDevicePolicyTracks property.
    pub android_device_policy_tracks: Option<Vec<String>>,
    /// appAutoUpdatePolicy property.
    pub app_auto_update_policy: Option<String>,
    /// appFunctions property.
    pub app_functions: Option<String>,
    /// applications property.
    pub applications: Option<Vec<ApplicationPolicy>>,
    /// assistContentPolicy property.
    pub assist_content_policy: Option<String>,
    /// autoDateAndTimeZone property.
    pub auto_date_and_time_zone: Option<String>,
    /// autoTimeRequired property.
    pub auto_time_required: Option<bool>,
    /// blockApplicationsEnabled property.
    pub block_applications_enabled: Option<bool>,
    /// bluetoothConfigDisabled property.
    pub bluetooth_config_disabled: Option<bool>,
    /// bluetoothContactSharingDisabled property.
    pub bluetooth_contact_sharing_disabled: Option<bool>,
    /// bluetoothDisabled property.
    pub bluetooth_disabled: Option<bool>,
    /// cameraAccess property.
    pub camera_access: Option<String>,
    /// cameraDisabled property.
    pub camera_disabled: Option<bool>,
    /// cellBroadcastsConfigDisabled property.
    pub cell_broadcasts_config_disabled: Option<bool>,
    /// choosePrivateKeyRules property.
    pub choose_private_key_rules: Option<Vec<ChoosePrivateKeyRule>>,
    /// complianceRules property.
    pub compliance_rules: Option<Vec<ComplianceRule>>,
    /// createWindowsDisabled property.
    pub create_windows_disabled: Option<bool>,
    /// credentialProviderPolicyDefault property.
    pub credential_provider_policy_default: Option<String>,
    /// credentialsConfigDisabled property.
    pub credentials_config_disabled: Option<bool>,
    /// crossProfilePolicies property.
    pub cross_profile_policies: Option<CrossProfilePolicies>,
    /// dataRoamingDisabled property.
    pub data_roaming_disabled: Option<bool>,
    /// debuggingFeaturesAllowed property.
    pub debugging_features_allowed: Option<bool>,
    /// defaultApplicationSettings property.
    pub default_application_settings: Option<Vec<DefaultApplicationSetting>>,
    /// defaultPermissionPolicy property.
    pub default_permission_policy: Option<String>,
    /// deviceConnectivityManagement property.
    pub device_connectivity_management: Option<DeviceConnectivityManagement>,
    /// deviceOwnerLockScreenInfo property.
    pub device_owner_lock_screen_info: Option<UserFacingMessage>,
    /// deviceRadioState property.
    pub device_radio_state: Option<DeviceRadioState>,
    /// displaySettings property.
    pub display_settings: Option<DisplaySettings>,
    /// encryptionPolicy property.
    pub encryption_policy: Option<String>,
    /// ensureVerifyAppsEnabled property.
    pub ensure_verify_apps_enabled: Option<bool>,
    /// enterpriseDisplayNameVisibility property.
    pub enterprise_display_name_visibility: Option<String>,
    /// factoryResetDisabled property.
    pub factory_reset_disabled: Option<bool>,
    /// frpAdminEmails property.
    pub frp_admin_emails: Option<Vec<String>>,
    /// funDisabled property.
    pub fun_disabled: Option<bool>,
    /// installAppsDisabled property.
    pub install_apps_disabled: Option<bool>,
    /// installUnknownSourcesAllowed property.
    pub install_unknown_sources_allowed: Option<bool>,
    /// keyguardDisabled property.
    pub keyguard_disabled: Option<bool>,
    /// keyguardDisabledFeatures property.
    pub keyguard_disabled_features: Option<Vec<String>>,
    /// kioskCustomLauncherEnabled property.
    pub kiosk_custom_launcher_enabled: Option<bool>,
    /// kioskCustomization property.
    pub kiosk_customization: Option<KioskCustomization>,
    /// locationMode property.
    pub location_mode: Option<String>,
    /// longSupportMessage property.
    pub long_support_message: Option<UserFacingMessage>,
    /// maximumTimeToLock property.
    pub maximum_time_to_lock: Option<String>,
    /// microphoneAccess property.
    pub microphone_access: Option<String>,
    /// minimumApiLevel property.
    pub minimum_api_level: Option<i64>,
    /// mobileNetworksConfigDisabled property.
    pub mobile_networks_config_disabled: Option<bool>,
    /// modifyAccountsDisabled property.
    pub modify_accounts_disabled: Option<bool>,
    /// mountPhysicalMediaDisabled property.
    pub mount_physical_media_disabled: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// networkEscapeHatchEnabled property.
    pub network_escape_hatch_enabled: Option<bool>,
    /// networkResetDisabled property.
    pub network_reset_disabled: Option<bool>,
    /// oncCertificateProviders property.
    pub onc_certificate_providers: Option<Vec<OncCertificateProvider>>,
    /// openNetworkConfiguration property.
    pub open_network_configuration: Option<serde_json::Value>,
    /// outgoingBeamDisabled property.
    pub outgoing_beam_disabled: Option<bool>,
    /// outgoingCallsDisabled property.
    pub outgoing_calls_disabled: Option<bool>,
    /// passwordPolicies property.
    pub password_policies: Option<Vec<PasswordRequirements>>,
    /// passwordRequirements property.
    pub password_requirements: Option<PasswordRequirements>,
    /// permissionGrants property.
    pub permission_grants: Option<Vec<PermissionGrant>>,
    /// permittedAccessibilityServices property.
    pub permitted_accessibility_services: Option<PackageNameList>,
    /// permittedInputMethods property.
    pub permitted_input_methods: Option<PackageNameList>,
    /// persistentPreferredActivities property.
    pub persistent_preferred_activities: Option<Vec<PersistentPreferredActivity>>,
    /// personalUsagePolicies property.
    pub personal_usage_policies: Option<PersonalUsagePolicies>,
    /// playStoreMode property.
    pub play_store_mode: Option<String>,
    /// policyEnforcementRules property.
    pub policy_enforcement_rules: Option<Vec<PolicyEnforcementRule>>,
    /// preferentialNetworkService property.
    pub preferential_network_service: Option<String>,
    /// printingPolicy property.
    pub printing_policy: Option<String>,
    /// privateKeySelectionEnabled property.
    pub private_key_selection_enabled: Option<bool>,
    /// recommendedGlobalProxy property.
    pub recommended_global_proxy: Option<ProxyInfo>,
    /// removeUserDisabled property.
    pub remove_user_disabled: Option<bool>,
    /// safeBootDisabled property.
    pub safe_boot_disabled: Option<bool>,
    /// screenCaptureDisabled property.
    pub screen_capture_disabled: Option<bool>,
    /// setUserIconDisabled property.
    pub set_user_icon_disabled: Option<bool>,
    /// setWallpaperDisabled property.
    pub set_wallpaper_disabled: Option<bool>,
    /// setupActions property.
    pub setup_actions: Option<Vec<SetupAction>>,
    /// shareLocationDisabled property.
    pub share_location_disabled: Option<bool>,
    /// shortSupportMessage property.
    pub short_support_message: Option<UserFacingMessage>,
    /// skipFirstUseHintsEnabled property.
    pub skip_first_use_hints_enabled: Option<bool>,
    /// smsDisabled property.
    pub sms_disabled: Option<bool>,
    /// statusBarDisabled property.
    pub status_bar_disabled: Option<bool>,
    /// statusReportingSettings property.
    pub status_reporting_settings: Option<StatusReportingSettings>,
    /// stayOnPluggedModes property.
    pub stay_on_plugged_modes: Option<Vec<String>>,
    /// systemUpdate property.
    pub system_update: Option<SystemUpdate>,
    /// tetheringConfigDisabled property.
    pub tethering_config_disabled: Option<bool>,
    /// uninstallAppsDisabled property.
    pub uninstall_apps_disabled: Option<bool>,
    /// unmuteMicrophoneDisabled property.
    pub unmute_microphone_disabled: Option<bool>,
    /// usageLog property.
    pub usage_log: Option<UsageLog>,
    /// usbFileTransferDisabled property.
    pub usb_file_transfer_disabled: Option<bool>,
    /// usbMassStorageEnabled property.
    pub usb_mass_storage_enabled: Option<bool>,
    /// version property.
    pub version: Option<String>,
    /// vpnConfigDisabled property.
    pub vpn_config_disabled: Option<bool>,
    /// wifiConfigDisabled property.
    pub wifi_config_disabled: Option<bool>,
    /// wifiConfigsLockdownEnabled property.
    pub wifi_configs_lockdown_enabled: Option<bool>,
    /// wipeDataFlags property.
    pub wipe_data_flags: Option<Vec<String>>,
    /// workAccountSetupConfig property.
    pub work_account_setup_config: Option<WorkAccountSetupConfig>,
}

/// `UserFacingMessage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserFacingMessage {
    /// defaultMessage property.
    pub default_message: Option<String>,
    /// localizedMessages property.
    pub localized_messages: Option<serde_json::Value>,
}

/// `PrivateDnsSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PrivateDnsSettings {
    /// privateDnsHost property.
    pub private_dns_host: Option<String>,
    /// privateDnsMode property.
    pub private_dns_mode: Option<String>,
}

/// `SystemUpdate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SystemUpdate {
    /// endMinutes property.
    pub end_minutes: Option<i64>,
    /// freezePeriods property.
    pub freeze_periods: Option<Vec<FreezePeriod>>,
    /// startMinutes property.
    pub start_minutes: Option<i64>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ChoosePrivateKeyRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ChoosePrivateKeyRule {
    /// packageNames property.
    pub package_names: Option<Vec<String>>,
    /// privateKeyAlias property.
    pub private_key_alias: Option<String>,
    /// urlPattern property.
    pub url_pattern: Option<String>,
}

/// `PermissionGrant` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PermissionGrant {
    /// permission property.
    pub permission: Option<String>,
    /// policy property.
    pub policy: Option<String>,
}

/// `NonComplianceDetailCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NonComplianceDetailCondition {
    /// nonComplianceReason property.
    pub non_compliance_reason: Option<String>,
    /// packageName property.
    pub package_name: Option<String>,
    /// settingName property.
    pub setting_name: Option<String>,
}

/// `StopLostModeParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StopLostModeParams {}

/// `NonComplianceDetail` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NonComplianceDetail {
    /// currentValue property.
    pub current_value: Option<serde_json::Value>,
    /// fieldPath property.
    pub field_path: Option<String>,
    /// installationFailureReason property.
    pub installation_failure_reason: Option<String>,
    /// nonComplianceReason property.
    pub non_compliance_reason: Option<String>,
    /// packageName property.
    pub package_name: Option<String>,
    /// settingName property.
    pub setting_name: Option<String>,
    /// specificNonComplianceContext property.
    pub specific_non_compliance_context: Option<SpecificNonComplianceContext>,
    /// specificNonComplianceReason property.
    pub specific_non_compliance_reason: Option<String>,
}

/// `WifiSsid` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WifiSsid {
    /// wifiSsid property.
    pub wifi_ssid: Option<String>,
}

/// `DisplaySettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DisplaySettings {
    /// screenBrightnessSettings property.
    pub screen_brightness_settings: Option<ScreenBrightnessSettings>,
    /// screenTimeoutSettings property.
    pub screen_timeout_settings: Option<ScreenTimeoutSettings>,
}

/// `RemoveEsimParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemoveEsimParams {
    /// iccId property.
    pub icc_id: Option<String>,
}

/// `MemoryEvent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MemoryEvent {
    /// byteCount property.
    pub byte_count: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// eventType property.
    pub event_type: Option<String>,
}

/// `ApplicationPolicyChange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApplicationPolicyChange {
    /// application property.
    pub application: Option<ApplicationPolicy>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `ClearAppsDataParams` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ClearAppsDataParams {
    /// packageNames property.
    pub package_names: Option<Vec<String>>,
}

/// `WifiRoamingPolicy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WifiRoamingPolicy {
    /// wifiRoamingSettings property.
    pub wifi_roaming_settings: Option<Vec<WifiRoamingSetting>>,
}

/// `KeyedAppState` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct KeyedAppState {
    /// createTime property.
    pub create_time: Option<String>,
    /// data property.
    pub data: Option<String>,
    /// key property.
    pub key: Option<String>,
    /// lastUpdateTime property.
    pub last_update_time: Option<String>,
    /// message property.
    pub message: Option<String>,
    /// severity property.
    pub severity: Option<String>,
}

/// `PasswordPoliciesContext` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PasswordPoliciesContext {
    /// passwordPolicyScope property.
    pub password_policy_scope: Option<String>,
}

/// `ModifyPolicyApplicationsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ModifyPolicyApplicationsRequest {
    /// changes property.
    pub changes: Option<Vec<ApplicationPolicyChange>>,
}

/// `WipeAction` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WipeAction {
    /// preserveFrp property.
    pub preserve_frp: Option<bool>,
    /// wipeAfterDays property.
    pub wipe_after_days: Option<i64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`androidmanagement.enterprises.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesCreateArgs {
    /// Query parameter: `agreementAccepted`.
    pub agreement_accepted: Option<String>,
    /// Query parameter: `enterpriseToken`.
    pub enterprise_token: Option<String>,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
    /// Query parameter: `signupUrlName`.
    pub signup_url_name: Option<String>,
    /// Request body.
    pub body: Enterprise,
}

/// Arguments for [`androidmanagement.enterprises.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.generateEnterpriseUpgradeUrl_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesGenerateEnterpriseUpgradeUrlArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: GenerateEnterpriseUpgradeUrlRequest,
}

/// Arguments for [`androidmanagement.enterprises.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesListArgs {
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`androidmanagement.enterprises.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Enterprise,
}

/// Arguments for [`androidmanagement.enterprises.applications.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesApplicationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `languageCode`.
    pub language_code: Option<String>,
}

/// Arguments for [`androidmanagement.enterprises.devices.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesDevicesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `wipeDataFlags`.
    pub wipe_data_flags: Option<String>,
    /// Query parameter: `wipeReasonMessage`.
    pub wipe_reason_message: Option<String>,
}

/// Arguments for [`androidmanagement.enterprises.devices.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesDevicesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.devices.issueCommand_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesDevicesIssueCommandArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: Command,
}

/// Arguments for [`androidmanagement.enterprises.devices.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesDevicesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`androidmanagement.enterprises.devices.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesDevicesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Device,
}

/// Arguments for [`androidmanagement.enterprises.devices.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesDevicesOperationsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.devices.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesDevicesOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.devices.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesDevicesOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`androidmanagement.enterprises.enrollmentTokens.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesEnrollmentTokensCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: EnrollmentToken,
}

/// Arguments for [`androidmanagement.enterprises.enrollmentTokens.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesEnrollmentTokensDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.enrollmentTokens.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesEnrollmentTokensGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.enrollmentTokens.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesEnrollmentTokensListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`androidmanagement.enterprises.migrationTokens.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesMigrationTokensCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: MigrationToken,
}

/// Arguments for [`androidmanagement.enterprises.migrationTokens.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesMigrationTokensGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.migrationTokens.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesMigrationTokensListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`androidmanagement.enterprises.policies.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesPoliciesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.policies.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesPoliciesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.policies.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesPoliciesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`androidmanagement.enterprises.policies.modifyPolicyApplications_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesPoliciesModifyPolicyApplicationsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: ModifyPolicyApplicationsRequest,
}

/// Arguments for [`androidmanagement.enterprises.policies.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesPoliciesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: Policy,
}

/// Arguments for [`androidmanagement.enterprises.policies.removePolicyApplications_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesPoliciesRemovePolicyApplicationsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: RemovePolicyApplicationsRequest,
}

/// Arguments for [`androidmanagement.enterprises.webApps.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesWebAppsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: WebApp,
}

/// Arguments for [`androidmanagement.enterprises.webApps.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesWebAppsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.webApps.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesWebAppsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.enterprises.webApps.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesWebAppsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`androidmanagement.enterprises.webApps.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesWebAppsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Request body.
    pub body: WebApp,
}

/// Arguments for [`androidmanagement.enterprises.webTokens.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementEnterprisesWebTokensCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: WebToken,
}

/// Arguments for [`androidmanagement.provisioningInfo.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementProvisioningInfoGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`androidmanagement.signupUrls.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AndroidmanagementSignupUrlsCreateArgs {
    /// Query parameter: `adminEmail`.
    pub admin_email: Option<String>,
    /// Query parameter: `allowedDomains`.
    pub allowed_domains: Option<String>,
    /// Query parameter: `callbackUrl`.
    pub callback_url: Option<String>,
    /// Query parameter: `projectId`.
    pub project_id: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/enterprises
// -----------------------------------------------------------------------------

/// POST v1/enterprises.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Enterprise>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/enterprises",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.agreement_accepted {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("agreementAccepted=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.enterprise_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("enterpriseToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.signup_url_name {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("signupUrlName=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Enterprise =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:generateEnterpriseUpgradeUrl
// -----------------------------------------------------------------------------

/// POST v1/{+name}:generateEnterpriseUpgradeUrl.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_generate_enterprise_upgrade_url_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_generate_enterprise_upgrade_url_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesGenerateEnterpriseUpgradeUrlArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GenerateEnterpriseUpgradeUrlResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}:generateEnterpriseUpgradeUrl",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenerateEnterpriseUpgradeUrlResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Enterprise>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Enterprise =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/enterprises
// -----------------------------------------------------------------------------

/// GET v1/enterprises.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListEnterprisesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/enterprises",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListEnterprisesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Enterprise>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Enterprise =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_applications_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_applications_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesApplicationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Application>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.language_code {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languageCode=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Application =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_devices_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_devices_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesDevicesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.wipe_data_flags {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("wipeDataFlags=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.wipe_reason_message {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("wipeReasonMessage=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_devices_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_devices_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesDevicesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Device>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Device =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:issueCommand
// -----------------------------------------------------------------------------

/// POST v1/{+name}:issueCommand.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_devices_issue_command_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_devices_issue_command_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesDevicesIssueCommandArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}:issueCommand",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/devices
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/devices.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_devices_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_devices_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesDevicesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDevicesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}/devices",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDevicesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_devices_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_devices_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesDevicesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Device>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Device =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_devices_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_devices_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesDevicesOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}:cancel",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_devices_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_devices_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesDevicesOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_devices_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_devices_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesDevicesOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOperationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/enrollmentTokens
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/enrollmentTokens.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_enrollment_tokens_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_enrollment_tokens_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesEnrollmentTokensCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EnrollmentToken>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}/enrollmentTokens",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EnrollmentToken =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_enrollment_tokens_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_enrollment_tokens_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesEnrollmentTokensDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_enrollment_tokens_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_enrollment_tokens_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesEnrollmentTokensGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EnrollmentToken>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EnrollmentToken =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/enrollmentTokens
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/enrollmentTokens.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_enrollment_tokens_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_enrollment_tokens_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesEnrollmentTokensListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListEnrollmentTokensResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}/enrollmentTokens",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListEnrollmentTokensResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/migrationTokens
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/migrationTokens.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_migration_tokens_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_migration_tokens_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesMigrationTokensCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MigrationToken>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}/migrationTokens",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: MigrationToken =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_migration_tokens_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_migration_tokens_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesMigrationTokensGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MigrationToken>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: MigrationToken =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/migrationTokens
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/migrationTokens.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_migration_tokens_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_migration_tokens_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesMigrationTokensListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMigrationTokensResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}/migrationTokens",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListMigrationTokensResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_policies_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_policies_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesPoliciesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_policies_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_policies_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesPoliciesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/policies
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/policies.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_policies_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_policies_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesPoliciesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPoliciesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}/policies",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPoliciesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:modifyPolicyApplications
// -----------------------------------------------------------------------------

/// POST v1/{+name}:modifyPolicyApplications.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_policies_modify_policy_applications_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_policies_modify_policy_applications_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesPoliciesModifyPolicyApplicationsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ModifyPolicyApplicationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}:modifyPolicyApplications",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ModifyPolicyApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_policies_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_policies_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesPoliciesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:removePolicyApplications
// -----------------------------------------------------------------------------

/// POST v1/{+name}:removePolicyApplications.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_policies_remove_policy_applications_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_policies_remove_policy_applications_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesPoliciesRemovePolicyApplicationsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemovePolicyApplicationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}:removePolicyApplications",
        args.name,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RemovePolicyApplicationsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/webApps
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/webApps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_web_apps_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_web_apps_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesWebAppsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebApp>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}/webApps",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WebApp =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_web_apps_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_web_apps_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesWebAppsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_web_apps_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_web_apps_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesWebAppsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebApp>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WebApp =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/webApps
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/webApps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_web_apps_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_web_apps_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesWebAppsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListWebAppsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}/webApps",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListWebAppsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_web_apps_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_web_apps_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesWebAppsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebApp>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WebApp =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/webTokens
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/webTokens.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_enterprises_web_tokens_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_enterprises_web_tokens_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementEnterprisesWebTokensCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WebToken>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://androidmanagement.googleapis.com/v1/{}/webTokens",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WebToken =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_provisioning_info_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_provisioning_info_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementProvisioningInfoGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProvisioningInfo>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProvisioningInfo =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/signupUrls
// -----------------------------------------------------------------------------

/// POST v1/signupUrls.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = androidmanagement_signup_urls_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn androidmanagement_signup_urls_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AndroidmanagementSignupUrlsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SignupUrl>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://androidmanagement.googleapis.com/v1/signupUrls",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.admin_email {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("adminEmail=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.allowed_domains {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("allowedDomains=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.callback_url {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("callbackUrl=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.project_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("projectId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SignupUrl =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
