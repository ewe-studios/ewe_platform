//! Auto-generated API module for gcp/analytics analytics.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_analytics_analytics `

#![cfg(feature = "gcp_analytics_analytics")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `HashClientIdRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HashClientIdRequest {
    /// clientId property.
    pub client_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
}

/// `GaData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GaData {
    /// columnHeaders property.
    pub column_headers: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// containsSampledData property.
    pub contains_sampled_data: Option<bool>,
    /// dataLastRefreshed property.
    pub data_last_refreshed: Option<String>,
    /// dataTable property.
    pub data_table: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// id property.
    pub id: Option<String>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// profileInfo property.
    pub profile_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// query property.
    pub query: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// rows property.
    pub rows: Option<Vec<Vec<String>>>,
    /// sampleSize property.
    pub sample_size: Option<String>,
    /// sampleSpace property.
    pub sample_space: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// totalsForAllResults property.
    pub totals_for_all_results: Option<serde_json::Value>,
}

/// `CustomDataSource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomDataSource {
    /// accountId property.
    pub account_id: Option<String>,
    /// childLink property.
    pub child_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// created property.
    pub created: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// importBehavior property.
    pub import_behavior: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// parentLink property.
    pub parent_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// profilesLinked property.
    pub profiles_linked: Option<Vec<String>>,
    /// schema property.
    pub schema: Option<Vec<String>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// uploadType property.
    pub upload_type: Option<String>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
}

/// `Goal` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Goal {
    /// accountId property.
    pub account_id: Option<String>,
    /// active property.
    pub active: Option<bool>,
    /// created property.
    pub created: Option<String>,
    /// eventDetails property.
    pub event_details: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// id property.
    pub id: Option<String>,
    /// internalWebPropertyId property.
    pub internal_web_property_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// parentLink property.
    pub parent_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// profileId property.
    pub profile_id: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// urlDestinationDetails property.
    pub url_destination_details: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// value property.
    pub value: Option<f64>,
    /// visitNumPagesDetails property.
    pub visit_num_pages_details: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// visitTimeOnSiteDetails property.
    pub visit_time_on_site_details: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
}

/// `RemarketingAudiences` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemarketingAudiences {
    /// items property.
    pub items: Option<Vec<RemarketingAudience>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `UserDeletionRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserDeletionRequest {
    /// deletionRequestTime property.
    pub deletion_request_time: Option<String>,
    /// firebaseProjectId property.
    pub firebase_project_id: Option<String>,
    /// id property.
    pub id: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// kind property.
    pub kind: Option<String>,
    /// propertyId property.
    pub property_id: Option<String>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
}

/// `RealtimeData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RealtimeData {
    /// columnHeaders property.
    pub column_headers: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// profileInfo property.
    pub profile_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// query property.
    pub query: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// rows property.
    pub rows: Option<Vec<Vec<String>>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// totalsForAllResults property.
    pub totals_for_all_results: Option<serde_json::Value>,
}

/// `CustomMetric` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomMetric {
    /// accountId property.
    pub account_id: Option<String>,
    /// active property.
    pub active: Option<bool>,
    /// created property.
    pub created: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// index property.
    pub index: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// max_value property.
    pub max_value: Option<String>,
    /// min_value property.
    pub min_value: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// parentLink property.
    pub parent_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// scope property.
    pub scope: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
}

/// `UserRef` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserRef {
    /// email property.
    pub email: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `UnsampledReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UnsampledReport {
    /// accountId property.
    pub account_id: Option<String>,
    /// cloudStorageDownloadDetails property.
    pub cloud_storage_download_details:
        Option<std::collections::HashMap<String, serde_json::Value>>,
    /// created property.
    pub created: Option<String>,
    /// dimensions property.
    pub dimensions: Option<String>,
    /// downloadType property.
    pub download_type: Option<String>,
    /// driveDownloadDetails property.
    pub drive_download_details: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// end-date property.
    pub end_date: Option<String>,
    /// filters property.
    pub filters: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// metrics property.
    pub metrics: Option<String>,
    /// profileId property.
    pub profile_id: Option<String>,
    /// segment property.
    pub segment: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// start-date property.
    pub start_date: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// title property.
    pub title: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
}

/// `Goals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Goals {
    /// items property.
    pub items: Option<Vec<Goal>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `AccountSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountSummary {
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// starred property.
    pub starred: Option<bool>,
    /// webProperties property.
    pub web_properties: Option<Vec<WebPropertySummary>>,
}

/// `EntityAdWordsLinks` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EntityAdWordsLinks {
    /// items property.
    pub items: Option<Vec<EntityAdWordsLink>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
}

/// `AnalyticsDataimportDeleteUploadDataRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AnalyticsDataimportDeleteUploadDataRequest {
    /// customDataImportUids property.
    pub custom_data_import_uids: Option<Vec<String>>,
}

/// `Upload` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Upload {
    /// accountId property.
    pub account_id: Option<String>,
    /// customDataSourceId property.
    pub custom_data_source_id: Option<String>,
    /// errors property.
    pub errors: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// uploadTime property.
    pub upload_time: Option<String>,
}

/// `Uploads` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Uploads {
    /// items property.
    pub items: Option<Vec<Upload>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
}

/// `HashClientIdResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HashClientIdResponse {
    /// clientId property.
    pub client_id: Option<String>,
    /// hashedClientId property.
    pub hashed_client_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
}

/// `EntityAdWordsLink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EntityAdWordsLink {
    /// adWordsAccounts property.
    pub ad_words_accounts: Option<Vec<AdWordsAccount>>,
    /// entity property.
    pub entity: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// profileIds property.
    pub profile_ids: Option<Vec<String>>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `WebPropertySummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WebPropertySummary {
    /// id property.
    pub id: Option<String>,
    /// internalWebPropertyId property.
    pub internal_web_property_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// level property.
    pub level: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// profiles property.
    pub profiles: Option<Vec<ProfileSummary>>,
    /// starred property.
    pub starred: Option<bool>,
    /// websiteUrl property.
    pub website_url: Option<String>,
}

/// `ProfileSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProfileSummary {
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// starred property.
    pub starred: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
}

/// `EntityUserLinks` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EntityUserLinks {
    /// items property.
    pub items: Option<Vec<EntityUserLink>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
}

/// `IncludeConditions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IncludeConditions {
    /// daysToLookBack property.
    pub days_to_look_back: Option<i64>,
    /// isSmartList property.
    pub is_smart_list: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// membershipDurationDays property.
    pub membership_duration_days: Option<i64>,
    /// segment property.
    pub segment: Option<String>,
}

/// `RemarketingAudience` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RemarketingAudience {
    /// accountId property.
    pub account_id: Option<String>,
    /// audienceDefinition property.
    pub audience_definition: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// audienceType property.
    pub audience_type: Option<String>,
    /// created property.
    pub created: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// internalWebPropertyId property.
    pub internal_web_property_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// linkedAdAccounts property.
    pub linked_ad_accounts: Option<Vec<LinkedForeignAccount>>,
    /// linkedViews property.
    pub linked_views: Option<Vec<String>>,
    /// name property.
    pub name: Option<String>,
    /// stateBasedAudienceDefinition property.
    pub state_based_audience_definition:
        Option<std::collections::HashMap<String, serde_json::Value>>,
    /// updated property.
    pub updated: Option<String>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
}

/// `CustomDataSources` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomDataSources {
    /// items property.
    pub items: Option<Vec<CustomDataSource>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `ProfileFilterLinks` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProfileFilterLinks {
    /// items property.
    pub items: Option<Vec<ProfileFilterLink>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `Filters` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Filters {
    /// items property.
    pub items: Option<Vec<Filter>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `AccountRef` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountRef {
    /// href property.
    pub href: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `FilterExpression` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FilterExpression {
    /// caseSensitive property.
    pub case_sensitive: Option<bool>,
    /// expressionValue property.
    pub expression_value: Option<String>,
    /// field property.
    pub field: Option<String>,
    /// fieldIndex property.
    pub field_index: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// matchType property.
    pub match_type: Option<String>,
}

/// `Columns` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Columns {
    /// attributeNames property.
    pub attribute_names: Option<Vec<String>>,
    /// etag property.
    pub etag: Option<String>,
    /// items property.
    pub items: Option<Vec<Column>>,
    /// kind property.
    pub kind: Option<String>,
    /// totalResults property.
    pub total_results: Option<i64>,
}

/// `FilterRef` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FilterRef {
    /// accountId property.
    pub account_id: Option<String>,
    /// href property.
    pub href: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `Experiments` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Experiments {
    /// items property.
    pub items: Option<Vec<Experiment>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `Filter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Filter {
    /// accountId property.
    pub account_id: Option<String>,
    /// advancedDetails property.
    pub advanced_details: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// created property.
    pub created: Option<String>,
    /// excludeDetails property.
    pub exclude_details: Option<FilterExpression>,
    /// id property.
    pub id: Option<String>,
    /// includeDetails property.
    pub include_details: Option<FilterExpression>,
    /// kind property.
    pub kind: Option<String>,
    /// lowercaseDetails property.
    pub lowercase_details: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// name property.
    pub name: Option<String>,
    /// parentLink property.
    pub parent_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// searchAndReplaceDetails property.
    pub search_and_replace_details: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// uppercaseDetails property.
    pub uppercase_details: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `Webproperties` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Webproperties {
    /// items property.
    pub items: Option<Vec<Webproperty>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `Segments` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Segments {
    /// items property.
    pub items: Option<Vec<Segment>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `ProfileFilterLink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProfileFilterLink {
    /// filterRef property.
    pub filter_ref: Option<FilterRef>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// profileRef property.
    pub profile_ref: Option<ProfileRef>,
    /// rank property.
    pub rank: Option<i64>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `AdWordsAccount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdWordsAccount {
    /// autoTaggingEnabled property.
    pub auto_tagging_enabled: Option<bool>,
    /// customerId property.
    pub customer_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `UnsampledReports` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UnsampledReports {
    /// items property.
    pub items: Option<Vec<UnsampledReport>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `ProfileRef` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ProfileRef {
    /// accountId property.
    pub account_id: Option<String>,
    /// href property.
    pub href: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// internalWebPropertyId property.
    pub internal_web_property_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
}

/// `Profile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Profile {
    /// accountId property.
    pub account_id: Option<String>,
    /// botFilteringEnabled property.
    pub bot_filtering_enabled: Option<bool>,
    /// childLink property.
    pub child_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// created property.
    pub created: Option<String>,
    /// currency property.
    pub currency: Option<String>,
    /// defaultPage property.
    pub default_page: Option<String>,
    /// eCommerceTracking property.
    pub e_commerce_tracking: Option<bool>,
    /// enhancedECommerceTracking property.
    pub enhanced_e_commerce_tracking: Option<bool>,
    /// excludeQueryParameters property.
    pub exclude_query_parameters: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// internalWebPropertyId property.
    pub internal_web_property_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// parentLink property.
    pub parent_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// permissions property.
    pub permissions: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// siteSearchCategoryParameters property.
    pub site_search_category_parameters: Option<String>,
    /// siteSearchQueryParameters property.
    pub site_search_query_parameters: Option<String>,
    /// starred property.
    pub starred: Option<bool>,
    /// stripSiteSearchCategoryParameters property.
    pub strip_site_search_category_parameters: Option<bool>,
    /// stripSiteSearchQueryParameters property.
    pub strip_site_search_query_parameters: Option<bool>,
    /// timezone property.
    pub timezone: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
    /// websiteUrl property.
    pub website_url: Option<String>,
}

/// `Profiles` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Profiles {
    /// items property.
    pub items: Option<Vec<Profile>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `CustomDimension` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomDimension {
    /// accountId property.
    pub account_id: Option<String>,
    /// active property.
    pub active: Option<bool>,
    /// created property.
    pub created: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// index property.
    pub index: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// parentLink property.
    pub parent_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// scope property.
    pub scope: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// updated property.
    pub updated: Option<String>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
}

/// `Accounts` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Accounts {
    /// items property.
    pub items: Option<Vec<Account>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `Webproperty` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Webproperty {
    /// accountId property.
    pub account_id: Option<String>,
    /// childLink property.
    pub child_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// created property.
    pub created: Option<String>,
    /// dataRetentionResetOnNewActivity property.
    pub data_retention_reset_on_new_activity: Option<bool>,
    /// dataRetentionTtl property.
    pub data_retention_ttl: Option<String>,
    /// defaultProfileId property.
    pub default_profile_id: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// industryVertical property.
    pub industry_vertical: Option<String>,
    /// internalWebPropertyId property.
    pub internal_web_property_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// level property.
    pub level: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// parentLink property.
    pub parent_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// permissions property.
    pub permissions: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// profileCount property.
    pub profile_count: Option<i64>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// starred property.
    pub starred: Option<bool>,
    /// updated property.
    pub updated: Option<String>,
    /// websiteUrl property.
    pub website_url: Option<String>,
}

/// `McfData` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct McfData {
    /// columnHeaders property.
    pub column_headers: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// containsSampledData property.
    pub contains_sampled_data: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// profileInfo property.
    pub profile_info: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// query property.
    pub query: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// rows property.
    pub rows: Option<Vec<Vec<std::collections::HashMap<String, serde_json::Value>>>>,
    /// sampleSize property.
    pub sample_size: Option<String>,
    /// sampleSpace property.
    pub sample_space: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// totalsForAllResults property.
    pub totals_for_all_results: Option<serde_json::Value>,
}

/// `EntityUserLink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EntityUserLink {
    /// entity property.
    pub entity: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// permissions property.
    pub permissions: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// userRef property.
    pub user_ref: Option<UserRef>,
}

/// `AccountTicket` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountTicket {
    /// account property.
    pub account: Option<Account>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// profile property.
    pub profile: Option<Profile>,
    /// redirectUri property.
    pub redirect_uri: Option<String>,
    /// webproperty property.
    pub webproperty: Option<Webproperty>,
}

/// `Column` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Column {
    /// attributes property.
    pub attributes: Option<serde_json::Value>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `WebPropertyRef` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WebPropertyRef {
    /// accountId property.
    pub account_id: Option<String>,
    /// href property.
    pub href: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// internalWebPropertyId property.
    pub internal_web_property_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `LinkedForeignAccount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LinkedForeignAccount {
    /// accountId property.
    pub account_id: Option<String>,
    /// eligibleForSearch property.
    pub eligible_for_search: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// internalWebPropertyId property.
    pub internal_web_property_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// linkedAccountId property.
    pub linked_account_id: Option<String>,
    /// remarketingAudienceId property.
    pub remarketing_audience_id: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
}

/// `Account` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Account {
    /// childLink property.
    pub child_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// created property.
    pub created: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// permissions property.
    pub permissions: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// starred property.
    pub starred: Option<bool>,
    /// updated property.
    pub updated: Option<String>,
}

/// `CustomDimensions` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomDimensions {
    /// items property.
    pub items: Option<Vec<CustomDimension>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `CustomMetrics` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomMetrics {
    /// items property.
    pub items: Option<Vec<CustomMetric>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `Segment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Segment {
    /// created property.
    pub created: Option<String>,
    /// definition property.
    pub definition: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// segmentId property.
    pub segment_id: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// updated property.
    pub updated: Option<String>,
}

/// `AccountTreeResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountTreeResponse {
    /// account property.
    pub account: Option<Account>,
    /// kind property.
    pub kind: Option<String>,
    /// profile property.
    pub profile: Option<Profile>,
    /// webproperty property.
    pub webproperty: Option<Webproperty>,
}

/// `AccountSummaries` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountSummaries {
    /// items property.
    pub items: Option<Vec<AccountSummary>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `AccountTreeRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AccountTreeRequest {
    /// accountName property.
    pub account_name: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// profileName property.
    pub profile_name: Option<String>,
    /// timezone property.
    pub timezone: Option<String>,
    /// webpropertyName property.
    pub webproperty_name: Option<String>,
    /// websiteUrl property.
    pub website_url: Option<String>,
}

/// `Experiment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Experiment {
    /// accountId property.
    pub account_id: Option<String>,
    /// created property.
    pub created: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// editableInGaUi property.
    pub editable_in_ga_ui: Option<bool>,
    /// endTime property.
    pub end_time: Option<String>,
    /// equalWeighting property.
    pub equal_weighting: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// internalWebPropertyId property.
    pub internal_web_property_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// minimumExperimentLengthInDays property.
    pub minimum_experiment_length_in_days: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// objectiveMetric property.
    pub objective_metric: Option<String>,
    /// optimizationType property.
    pub optimization_type: Option<String>,
    /// parentLink property.
    pub parent_link: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// profileId property.
    pub profile_id: Option<String>,
    /// reasonExperimentEnded property.
    pub reason_experiment_ended: Option<String>,
    /// rewriteVariationUrlsAsOriginal property.
    pub rewrite_variation_urls_as_original: Option<bool>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// servingFramework property.
    pub serving_framework: Option<String>,
    /// snippet property.
    pub snippet: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// trafficCoverage property.
    pub traffic_coverage: Option<f64>,
    /// updated property.
    pub updated: Option<String>,
    /// variations property.
    pub variations: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// webPropertyId property.
    pub web_property_id: Option<String>,
    /// winnerConfidenceLevel property.
    pub winner_confidence_level: Option<f64>,
    /// winnerFound property.
    pub winner_found: Option<bool>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`analytics.data.ga.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsDataGaGetArgs {
    /// Query parameter: `dimensions`.
    pub dimensions: Option<String>,
    /// Query parameter: `end-date`.
    pub end_date: Option<String>,
    /// Query parameter: `filters`.
    pub filters: Option<String>,
    /// Query parameter: `ids`.
    pub ids: Option<String>,
    /// Query parameter: `include-empty-rows`.
    pub include_empty_rows: Option<String>,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `metrics`.
    pub metrics: Option<String>,
    /// Query parameter: `output`.
    pub output: Option<String>,
    /// Query parameter: `samplingLevel`.
    pub sampling_level: Option<String>,
    /// Query parameter: `segment`.
    pub segment: Option<String>,
    /// Query parameter: `sort`.
    pub sort: Option<String>,
    /// Query parameter: `start-date`.
    pub start_date: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.data.mcf.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsDataMcfGetArgs {
    /// Query parameter: `dimensions`.
    pub dimensions: Option<String>,
    /// Query parameter: `end-date`.
    pub end_date: Option<String>,
    /// Query parameter: `filters`.
    pub filters: Option<String>,
    /// Query parameter: `ids`.
    pub ids: Option<String>,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `metrics`.
    pub metrics: Option<String>,
    /// Query parameter: `samplingLevel`.
    pub sampling_level: Option<String>,
    /// Query parameter: `sort`.
    pub sort: Option<String>,
    /// Query parameter: `start-date`.
    pub start_date: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.data.realtime.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsDataRealtimeGetArgs {
    /// Query parameter: `dimensions`.
    pub dimensions: Option<String>,
    /// Query parameter: `filters`.
    pub filters: Option<String>,
    /// Query parameter: `ids`.
    pub ids: Option<String>,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `metrics`.
    pub metrics: Option<String>,
    /// Query parameter: `sort`.
    pub sort: Option<String>,
}

/// Arguments for [`analytics.management.accountSummaries.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementAccountSummariesListArgs {
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.accountUserLinks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementAccountUserLinksDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `linkId`.
    pub link_id: String,
}

/// Arguments for [`analytics.management.accountUserLinks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementAccountUserLinksInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: EntityUserLink,
}

/// Arguments for [`analytics.management.accountUserLinks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementAccountUserLinksListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.accountUserLinks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementAccountUserLinksUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `linkId`.
    pub link_id: String,
    /// Request body.
    pub body: EntityUserLink,
}

/// Arguments for [`analytics.management.accounts.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementAccountsListArgs {
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.clientId.hashClientId_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementClientIdHashClientIdArgs {
    /// Request body.
    pub body: HashClientIdRequest,
}

/// Arguments for [`analytics.management.customDataSources.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementCustomDataSourcesListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.customDimensions.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementCustomDimensionsGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `customDimensionId`.
    pub custom_dimension_id: String,
}

/// Arguments for [`analytics.management.customDimensions.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementCustomDimensionsInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Request body.
    pub body: CustomDimension,
}

/// Arguments for [`analytics.management.customDimensions.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementCustomDimensionsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.customDimensions.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementCustomDimensionsPatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `customDimensionId`.
    pub custom_dimension_id: String,
    /// Query parameter: `ignoreCustomDataSourceLinks`.
    pub ignore_custom_data_source_links: Option<String>,
    /// Request body.
    pub body: CustomDimension,
}

/// Arguments for [`analytics.management.customDimensions.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementCustomDimensionsUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `customDimensionId`.
    pub custom_dimension_id: String,
    /// Query parameter: `ignoreCustomDataSourceLinks`.
    pub ignore_custom_data_source_links: Option<String>,
    /// Request body.
    pub body: CustomDimension,
}

/// Arguments for [`analytics.management.customMetrics.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementCustomMetricsGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `customMetricId`.
    pub custom_metric_id: String,
}

/// Arguments for [`analytics.management.customMetrics.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementCustomMetricsInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Request body.
    pub body: CustomMetric,
}

/// Arguments for [`analytics.management.customMetrics.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementCustomMetricsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.customMetrics.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementCustomMetricsPatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `customMetricId`.
    pub custom_metric_id: String,
    /// Query parameter: `ignoreCustomDataSourceLinks`.
    pub ignore_custom_data_source_links: Option<String>,
    /// Request body.
    pub body: CustomMetric,
}

/// Arguments for [`analytics.management.customMetrics.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementCustomMetricsUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `customMetricId`.
    pub custom_metric_id: String,
    /// Query parameter: `ignoreCustomDataSourceLinks`.
    pub ignore_custom_data_source_links: Option<String>,
    /// Request body.
    pub body: CustomMetric,
}

/// Arguments for [`analytics.management.experiments.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementExperimentsDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `experimentId`.
    pub experiment_id: String,
}

/// Arguments for [`analytics.management.experiments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementExperimentsGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `experimentId`.
    pub experiment_id: String,
}

/// Arguments for [`analytics.management.experiments.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementExperimentsInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Request body.
    pub body: Experiment,
}

/// Arguments for [`analytics.management.experiments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementExperimentsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.experiments.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementExperimentsPatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `experimentId`.
    pub experiment_id: String,
    /// Request body.
    pub body: Experiment,
}

/// Arguments for [`analytics.management.experiments.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementExperimentsUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `experimentId`.
    pub experiment_id: String,
    /// Request body.
    pub body: Experiment,
}

/// Arguments for [`analytics.management.filters.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementFiltersDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `filterId`.
    pub filter_id: String,
}

/// Arguments for [`analytics.management.filters.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementFiltersGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `filterId`.
    pub filter_id: String,
}

/// Arguments for [`analytics.management.filters.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementFiltersInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: Filter,
}

/// Arguments for [`analytics.management.filters.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementFiltersListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.filters.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementFiltersPatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `filterId`.
    pub filter_id: String,
    /// Request body.
    pub body: Filter,
}

/// Arguments for [`analytics.management.filters.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementFiltersUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `filterId`.
    pub filter_id: String,
    /// Request body.
    pub body: Filter,
}

/// Arguments for [`analytics.management.goals.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementGoalsGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `goalId`.
    pub goal_id: String,
}

/// Arguments for [`analytics.management.goals.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementGoalsInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Request body.
    pub body: Goal,
}

/// Arguments for [`analytics.management.goals.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementGoalsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.goals.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementGoalsPatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `goalId`.
    pub goal_id: String,
    /// Request body.
    pub body: Goal,
}

/// Arguments for [`analytics.management.goals.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementGoalsUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `goalId`.
    pub goal_id: String,
    /// Request body.
    pub body: Goal,
}

/// Arguments for [`analytics.management.profileFilterLinks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfileFilterLinksDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `linkId`.
    pub link_id: String,
}

/// Arguments for [`analytics.management.profileFilterLinks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfileFilterLinksGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `linkId`.
    pub link_id: String,
}

/// Arguments for [`analytics.management.profileFilterLinks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfileFilterLinksInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Request body.
    pub body: ProfileFilterLink,
}

/// Arguments for [`analytics.management.profileFilterLinks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfileFilterLinksListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.profileFilterLinks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfileFilterLinksPatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `linkId`.
    pub link_id: String,
    /// Request body.
    pub body: ProfileFilterLink,
}

/// Arguments for [`analytics.management.profileFilterLinks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfileFilterLinksUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `linkId`.
    pub link_id: String,
    /// Request body.
    pub body: ProfileFilterLink,
}

/// Arguments for [`analytics.management.profileUserLinks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfileUserLinksDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `linkId`.
    pub link_id: String,
}

/// Arguments for [`analytics.management.profileUserLinks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfileUserLinksInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Request body.
    pub body: EntityUserLink,
}

/// Arguments for [`analytics.management.profileUserLinks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfileUserLinksListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.profileUserLinks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfileUserLinksUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `linkId`.
    pub link_id: String,
    /// Request body.
    pub body: EntityUserLink,
}

/// Arguments for [`analytics.management.profiles.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfilesDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
}

/// Arguments for [`analytics.management.profiles.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfilesGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
}

/// Arguments for [`analytics.management.profiles.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfilesInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Request body.
    pub body: Profile,
}

/// Arguments for [`analytics.management.profiles.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfilesListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.profiles.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfilesPatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Request body.
    pub body: Profile,
}

/// Arguments for [`analytics.management.profiles.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementProfilesUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Request body.
    pub body: Profile,
}

/// Arguments for [`analytics.management.remarketingAudience.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementRemarketingAudienceDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `remarketingAudienceId`.
    pub remarketing_audience_id: String,
}

/// Arguments for [`analytics.management.remarketingAudience.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementRemarketingAudienceGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `remarketingAudienceId`.
    pub remarketing_audience_id: String,
}

/// Arguments for [`analytics.management.remarketingAudience.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementRemarketingAudienceInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Request body.
    pub body: RemarketingAudience,
}

/// Arguments for [`analytics.management.remarketingAudience.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementRemarketingAudienceListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
    /// Query parameter: `type`.
    pub r#type: Option<String>,
}

/// Arguments for [`analytics.management.remarketingAudience.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementRemarketingAudiencePatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `remarketingAudienceId`.
    pub remarketing_audience_id: String,
    /// Request body.
    pub body: RemarketingAudience,
}

/// Arguments for [`analytics.management.remarketingAudience.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementRemarketingAudienceUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `remarketingAudienceId`.
    pub remarketing_audience_id: String,
    /// Request body.
    pub body: RemarketingAudience,
}

/// Arguments for [`analytics.management.segments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementSegmentsListArgs {
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.unsampledReports.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementUnsampledReportsDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `unsampledReportId`.
    pub unsampled_report_id: String,
}

/// Arguments for [`analytics.management.unsampledReports.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementUnsampledReportsGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `unsampledReportId`.
    pub unsampled_report_id: String,
}

/// Arguments for [`analytics.management.unsampledReports.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementUnsampledReportsInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Request body.
    pub body: UnsampledReport,
}

/// Arguments for [`analytics.management.unsampledReports.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementUnsampledReportsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.uploads.deleteUploadData_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementUploadsDeleteUploadDataArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `customDataSourceId`.
    pub custom_data_source_id: String,
    /// Request body.
    pub body: AnalyticsDataimportDeleteUploadDataRequest,
}

/// Arguments for [`analytics.management.uploads.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementUploadsGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `customDataSourceId`.
    pub custom_data_source_id: String,
    /// Path parameter: `uploadId`.
    pub upload_id: String,
}

/// Arguments for [`analytics.management.uploads.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementUploadsListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `customDataSourceId`.
    pub custom_data_source_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.uploads.uploadData_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementUploadsUploadDataArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `customDataSourceId`.
    pub custom_data_source_id: String,
}

/// Arguments for [`analytics.management.webPropertyAdWordsLinks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebPropertyAdWordsLinksDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `webPropertyAdWordsLinkId`.
    pub web_property_ad_words_link_id: String,
}

/// Arguments for [`analytics.management.webPropertyAdWordsLinks.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebPropertyAdWordsLinksGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `webPropertyAdWordsLinkId`.
    pub web_property_ad_words_link_id: String,
}

/// Arguments for [`analytics.management.webPropertyAdWordsLinks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebPropertyAdWordsLinksInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Request body.
    pub body: EntityAdWordsLink,
}

/// Arguments for [`analytics.management.webPropertyAdWordsLinks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebPropertyAdWordsLinksListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.webPropertyAdWordsLinks.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebPropertyAdWordsLinksPatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `webPropertyAdWordsLinkId`.
    pub web_property_ad_words_link_id: String,
    /// Request body.
    pub body: EntityAdWordsLink,
}

/// Arguments for [`analytics.management.webPropertyAdWordsLinks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebPropertyAdWordsLinksUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `webPropertyAdWordsLinkId`.
    pub web_property_ad_words_link_id: String,
    /// Request body.
    pub body: EntityAdWordsLink,
}

/// Arguments for [`analytics.management.webproperties.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebpropertiesGetArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
}

/// Arguments for [`analytics.management.webproperties.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebpropertiesInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Request body.
    pub body: Webproperty,
}

/// Arguments for [`analytics.management.webproperties.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebpropertiesListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.webproperties.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebpropertiesPatchArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Request body.
    pub body: Webproperty,
}

/// Arguments for [`analytics.management.webproperties.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebpropertiesUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Request body.
    pub body: Webproperty,
}

/// Arguments for [`analytics.management.webpropertyUserLinks.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebpropertyUserLinksDeleteArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `linkId`.
    pub link_id: String,
}

/// Arguments for [`analytics.management.webpropertyUserLinks.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebpropertyUserLinksInsertArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Request body.
    pub body: EntityUserLink,
}

/// Arguments for [`analytics.management.webpropertyUserLinks.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebpropertyUserLinksListArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

/// Arguments for [`analytics.management.webpropertyUserLinks.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsManagementWebpropertyUserLinksUpdateArgs {
    /// Path parameter: `accountId`.
    pub account_id: String,
    /// Path parameter: `webPropertyId`.
    pub web_property_id: String,
    /// Path parameter: `linkId`.
    pub link_id: String,
    /// Request body.
    pub body: EntityUserLink,
}

/// Arguments for [`analytics.metadata.columns.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsMetadataColumnsListArgs {
    /// Path parameter: `reportType`.
    pub report_type: String,
}

/// Arguments for [`analytics.provisioning.createAccountTicket_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsProvisioningCreateAccountTicketArgs {
    /// Request body.
    pub body: AccountTicket,
}

/// Arguments for [`analytics.provisioning.createAccountTree_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsProvisioningCreateAccountTreeArgs {
    /// Request body.
    pub body: AccountTreeRequest,
}

/// Arguments for [`analytics.userDeletion.userDeletionRequest.upsert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AnalyticsUserDeletionUserDeletionRequestUpsertArgs {
    /// Request body.
    pub body: UserDeletionRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET data/ga
// -----------------------------------------------------------------------------

/// GET data/ga.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_data_ga_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_data_ga_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsDataGaGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GaData>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/analytics/v3/data/ga",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.dimensions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dimensions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("end_date=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filters {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filters=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ids=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.include_empty_rows {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("include_empty_rows=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.metrics {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("metrics=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.output {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("output=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sampling_level {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("samplingLevel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.segment {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("segment=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sort=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_date=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GaData =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET data/mcf
// -----------------------------------------------------------------------------

/// GET data/mcf.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_data_mcf_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_data_mcf_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsDataMcfGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<McfData>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/analytics/v3/data/mcf",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.dimensions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dimensions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("end_date=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filters {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filters=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ids=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.metrics {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("metrics=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sampling_level {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("samplingLevel=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sort=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_date=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: McfData =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET data/realtime
// -----------------------------------------------------------------------------

/// GET data/realtime.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_data_realtime_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_data_realtime_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsDataRealtimeGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RealtimeData>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/analytics/v3/data/realtime",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.dimensions {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("dimensions=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filters {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filters=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ids=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.metrics {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("metrics=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.sort {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sort=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RealtimeData =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accountSummaries
// -----------------------------------------------------------------------------

/// GET management/accountSummaries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_account_summaries_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_account_summaries_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementAccountSummariesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountSummaries>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/analytics/v3/management/accountSummaries",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountSummaries =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE management/accounts/{accountId}/entityUserLinks/{linkId}
// -----------------------------------------------------------------------------

/// DELETE management/accounts/{accountId}/entityUserLinks/{linkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_account_user_links_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_account_user_links_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementAccountUserLinksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/entityUserLinks/{}",
        args.account_id, args.link_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/entityUserLinks
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/entityUserLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_account_user_links_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_account_user_links_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementAccountUserLinksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityUserLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/entityUserLinks",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityUserLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/entityUserLinks
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/entityUserLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_account_user_links_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_account_user_links_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementAccountUserLinksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityUserLinks>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/entityUserLinks",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityUserLinks =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/entityUserLinks/{linkId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/entityUserLinks/{linkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_account_user_links_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_account_user_links_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementAccountUserLinksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityUserLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/entityUserLinks/{}",
        args.account_id, args.link_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityUserLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts
// -----------------------------------------------------------------------------

/// GET management/accounts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_accounts_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_accounts_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementAccountsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Accounts>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/analytics/v3/management/accounts",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Accounts =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/clientId:hashClientId
// -----------------------------------------------------------------------------

/// POST management/clientId:hashClientId.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_client_id_hash_client_id_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_client_id_hash_client_id_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementClientIdHashClientIdArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HashClientIdResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/analytics/v3/management/clientId:hashClientId",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: HashClientIdResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_custom_data_sources_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_custom_data_sources_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementCustomDataSourcesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomDataSources>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customDataSources",
        args.account_id,
        args.web_property_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomDataSources =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_custom_dimensions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_custom_dimensions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementCustomDimensionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomDimension>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customDimensions/{}",
        args.account_id,
        args.web_property_id,
        args.custom_dimension_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomDimension =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_custom_dimensions_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_custom_dimensions_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementCustomDimensionsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomDimension>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customDimensions",
        args.account_id,
        args.web_property_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomDimension =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_custom_dimensions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_custom_dimensions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementCustomDimensionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomDimensions>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customDimensions",
        args.account_id,
        args.web_property_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomDimensions =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
// -----------------------------------------------------------------------------

/// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_custom_dimensions_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_custom_dimensions_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementCustomDimensionsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomDimension>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customDimensions/{}",
        args.account_id,
        args.web_property_id,
        args.custom_dimension_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.ignore_custom_data_source_links {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ignoreCustomDataSourceLinks=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomDimension =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/customDimensions/{customDimensionId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_custom_dimensions_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_custom_dimensions_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementCustomDimensionsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomDimension>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customDimensions/{}",
        args.account_id,
        args.web_property_id,
        args.custom_dimension_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.ignore_custom_data_source_links {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ignoreCustomDataSourceLinks=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomDimension =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_custom_metrics_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_custom_metrics_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementCustomMetricsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomMetric>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customMetrics/{}",
        args.account_id,
        args.web_property_id,
        args.custom_metric_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomMetric =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_custom_metrics_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_custom_metrics_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementCustomMetricsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomMetric>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customMetrics",
        args.account_id,
        args.web_property_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomMetric =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_custom_metrics_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_custom_metrics_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementCustomMetricsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomMetrics>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customMetrics",
        args.account_id,
        args.web_property_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomMetrics =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
// -----------------------------------------------------------------------------

/// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_custom_metrics_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_custom_metrics_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementCustomMetricsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomMetric>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customMetrics/{}",
        args.account_id,
        args.web_property_id,
        args.custom_metric_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.ignore_custom_data_source_links {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ignoreCustomDataSourceLinks=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomMetric =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/customMetrics/{customMetricId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_custom_metrics_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_custom_metrics_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementCustomMetricsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomMetric>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customMetrics/{}",
        args.account_id,
        args.web_property_id,
        args.custom_metric_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.ignore_custom_data_source_links {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ignoreCustomDataSourceLinks=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CustomMetric =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
// -----------------------------------------------------------------------------

/// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_experiments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_experiments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementExperimentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/experiments/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.experiment_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_experiments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_experiments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementExperimentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Experiment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/experiments/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.experiment_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Experiment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_experiments_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_experiments_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementExperimentsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Experiment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/experiments",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Experiment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_experiments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_experiments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementExperimentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Experiments>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/experiments",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Experiments =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
// -----------------------------------------------------------------------------

/// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_experiments_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_experiments_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementExperimentsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Experiment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/experiments/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.experiment_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Experiment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/experiments/{experimentId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_experiments_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_experiments_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementExperimentsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Experiment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/experiments/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.experiment_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Experiment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE management/accounts/{accountId}/filters/{filterId}
// -----------------------------------------------------------------------------

/// DELETE management/accounts/{accountId}/filters/{filterId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_filters_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_filters_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementFiltersDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Filter>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/filters/{}",
        args.account_id, args.filter_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Filter =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/filters/{filterId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/filters/{filterId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_filters_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_filters_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementFiltersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Filter>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/filters/{}",
        args.account_id, args.filter_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Filter =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/filters
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/filters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_filters_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_filters_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementFiltersInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Filter>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/filters",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Filter =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/filters
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/filters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_filters_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_filters_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementFiltersListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Filters>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/filters",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Filters =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH management/accounts/{accountId}/filters/{filterId}
// -----------------------------------------------------------------------------

/// PATCH management/accounts/{accountId}/filters/{filterId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_filters_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_filters_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementFiltersPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Filter>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/filters/{}",
        args.account_id, args.filter_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Filter =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/filters/{filterId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/filters/{filterId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_filters_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_filters_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementFiltersUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Filter>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/filters/{}",
        args.account_id, args.filter_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Filter =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_goals_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_goals_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementGoalsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Goal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/goals/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.goal_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Goal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_goals_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_goals_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementGoalsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Goal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/goals",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Goal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_goals_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_goals_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementGoalsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Goals>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/goals",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Goals =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
// -----------------------------------------------------------------------------

/// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_goals_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_goals_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementGoalsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Goal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/goals/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.goal_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Goal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/goals/{goalId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_goals_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_goals_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementGoalsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Goal>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/goals/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.goal_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Goal =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
// -----------------------------------------------------------------------------

/// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profile_filter_links_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profile_filter_links_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfileFilterLinksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.link_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profile_filter_links_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profile_filter_links_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfileFilterLinksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProfileFilterLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.link_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProfileFilterLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profile_filter_links_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profile_filter_links_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfileFilterLinksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProfileFilterLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProfileFilterLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profile_filter_links_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profile_filter_links_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfileFilterLinksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProfileFilterLinks>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProfileFilterLinks =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
// -----------------------------------------------------------------------------

/// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profile_filter_links_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profile_filter_links_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfileFilterLinksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProfileFilterLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.link_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProfileFilterLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/profileFilterLinks/{linkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profile_filter_links_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profile_filter_links_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfileFilterLinksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProfileFilterLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/profileFilterLinks/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.link_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ProfileFilterLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks/{linkId}
// -----------------------------------------------------------------------------

/// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks/{linkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profile_user_links_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profile_user_links_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfileUserLinksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/entityUserLinks/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.link_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profile_user_links_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profile_user_links_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfileUserLinksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityUserLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/entityUserLinks",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityUserLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profile_user_links_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profile_user_links_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfileUserLinksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityUserLinks>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/entityUserLinks",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityUserLinks =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks/{linkId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/entityUserLinks/{linkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profile_user_links_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profile_user_links_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfileUserLinksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityUserLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/entityUserLinks/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.link_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityUserLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
// -----------------------------------------------------------------------------

/// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profiles_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profiles_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfilesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profiles_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profiles_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfilesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Profile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profiles_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profiles_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfilesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles",
        args.account_id, args.web_property_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Profile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profiles_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profiles_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfilesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profiles>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles",
        args.account_id, args.web_property_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Profiles =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
// -----------------------------------------------------------------------------

/// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profiles_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profiles_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfilesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Profile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_profiles_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_profiles_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementProfilesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Profile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Profile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
// -----------------------------------------------------------------------------

/// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_remarketing_audience_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_remarketing_audience_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementRemarketingAudienceDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/remarketingAudiences/{}",
        args.account_id,
        args.web_property_id,
        args.remarketing_audience_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_remarketing_audience_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_remarketing_audience_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementRemarketingAudienceGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingAudience>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/remarketingAudiences/{}",
        args.account_id,
        args.web_property_id,
        args.remarketing_audience_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RemarketingAudience =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_remarketing_audience_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_remarketing_audience_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementRemarketingAudienceInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingAudience>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/remarketingAudiences",
        args.account_id,
        args.web_property_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RemarketingAudience =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_remarketing_audience_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_remarketing_audience_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementRemarketingAudienceListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingAudiences>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/remarketingAudiences",
        args.account_id,
        args.web_property_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.r#type {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("type=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RemarketingAudiences =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
// -----------------------------------------------------------------------------

/// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_remarketing_audience_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_remarketing_audience_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementRemarketingAudiencePatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingAudience>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/remarketingAudiences/{}",
        args.account_id,
        args.web_property_id,
        args.remarketing_audience_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RemarketingAudience =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/remarketingAudiences/{remarketingAudienceId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_remarketing_audience_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_remarketing_audience_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementRemarketingAudienceUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemarketingAudience>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/remarketingAudiences/{}",
        args.account_id,
        args.web_property_id,
        args.remarketing_audience_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RemarketingAudience =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/segments
// -----------------------------------------------------------------------------

/// GET management/segments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_segments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_segments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementSegmentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Segments>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://www.googleapis.com/analytics/v3/management/segments",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Segments =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports/{unsampledReportId}
// -----------------------------------------------------------------------------

/// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports/{unsampledReportId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_unsampled_reports_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_unsampled_reports_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementUnsampledReportsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/unsampledReports/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.unsampled_report_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports/{unsampledReportId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports/{unsampledReportId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_unsampled_reports_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_unsampled_reports_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementUnsampledReportsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UnsampledReport>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/unsampledReports/{}",
        args.account_id,
        args.web_property_id,
        args.profile_id,
        args.unsampled_report_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UnsampledReport =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_unsampled_reports_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_unsampled_reports_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementUnsampledReportsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UnsampledReport>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/unsampledReports",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UnsampledReport =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/profiles/{profileId}/unsampledReports.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_unsampled_reports_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_unsampled_reports_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementUnsampledReportsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UnsampledReports>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/profiles/{}/unsampledReports",
        args.account_id,
        args.web_property_id,
        args.profile_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UnsampledReports =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/deleteUploadData
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/deleteUploadData.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_uploads_delete_upload_data_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_uploads_delete_upload_data_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementUploadsDeleteUploadDataArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customDataSources/{}/deleteUploadData",
        args.account_id,
        args.web_property_id,
        args.custom_data_source_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads/{uploadId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads/{uploadId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_uploads_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_uploads_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementUploadsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Upload>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customDataSources/{}/uploads/{}",
        args.account_id,
        args.web_property_id,
        args.custom_data_source_id,
        args.upload_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Upload =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_uploads_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_uploads_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementUploadsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Uploads>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customDataSources/{}/uploads",
        args.account_id,
        args.web_property_id,
        args.custom_data_source_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Uploads =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/customDataSources/{customDataSourceId}/uploads.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_uploads_upload_data_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_uploads_upload_data_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementUploadsUploadDataArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Upload>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/customDataSources/{}/uploads",
        args.account_id,
        args.web_property_id,
        args.custom_data_source_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Upload =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
// -----------------------------------------------------------------------------

/// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_web_property_ad_words_links_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_web_property_ad_words_links_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebPropertyAdWordsLinksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/entityAdWordsLinks/{}",
        args.account_id,
        args.web_property_id,
        args.web_property_ad_words_link_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_web_property_ad_words_links_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_web_property_ad_words_links_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebPropertyAdWordsLinksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityAdWordsLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/entityAdWordsLinks/{}",
        args.account_id,
        args.web_property_id,
        args.web_property_ad_words_link_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityAdWordsLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_web_property_ad_words_links_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_web_property_ad_words_links_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebPropertyAdWordsLinksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityAdWordsLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/entityAdWordsLinks",
        args.account_id,
        args.web_property_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityAdWordsLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_web_property_ad_words_links_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_web_property_ad_words_links_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebPropertyAdWordsLinksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityAdWordsLinks>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/entityAdWordsLinks",
        args.account_id,
        args.web_property_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityAdWordsLinks =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
// -----------------------------------------------------------------------------

/// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_web_property_ad_words_links_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_web_property_ad_words_links_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebPropertyAdWordsLinksPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityAdWordsLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/entityAdWordsLinks/{}",
        args.account_id,
        args.web_property_id,
        args.web_property_ad_words_link_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityAdWordsLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/entityAdWordsLinks/{webPropertyAdWordsLinkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_web_property_ad_words_links_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_web_property_ad_words_links_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebPropertyAdWordsLinksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityAdWordsLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/entityAdWordsLinks/{}",
        args.account_id,
        args.web_property_id,
        args.web_property_ad_words_link_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityAdWordsLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_webproperties_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_webproperties_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebpropertiesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Webproperty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}",
        args.account_id, args.web_property_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Webproperty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_webproperties_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_webproperties_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebpropertiesInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Webproperty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties",
        args.account_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Webproperty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_webproperties_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_webproperties_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebpropertiesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Webproperties>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties",
        args.account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Webproperties =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}
// -----------------------------------------------------------------------------

/// PATCH management/accounts/{accountId}/webproperties/{webPropertyId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_webproperties_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_webproperties_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebpropertiesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Webproperty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}",
        args.account_id, args.web_property_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Webproperty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/webproperties/{webPropertyId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/webproperties/{webPropertyId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_webproperties_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_webproperties_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebpropertiesUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Webproperty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}",
        args.account_id, args.web_property_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Webproperty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks/{linkId}
// -----------------------------------------------------------------------------

/// DELETE management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks/{linkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_webproperty_user_links_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_webproperty_user_links_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebpropertyUserLinksDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/entityUserLinks/{}",
        args.account_id,
        args.web_property_id,
        args.link_id,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks
// -----------------------------------------------------------------------------

/// POST management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_webproperty_user_links_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_webproperty_user_links_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebpropertyUserLinksInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityUserLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/entityUserLinks",
        args.account_id,
        args.web_property_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityUserLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks
// -----------------------------------------------------------------------------

/// GET management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_webproperty_user_links_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_webproperty_user_links_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebpropertyUserLinksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityUserLinks>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/entityUserLinks",
        args.account_id,
        args.web_property_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("max_results=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("start_index=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityUserLinks =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks/{linkId}
// -----------------------------------------------------------------------------

/// PUT management/accounts/{accountId}/webproperties/{webPropertyId}/entityUserLinks/{linkId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_webproperty_user_links_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_webproperty_user_links_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsManagementWebpropertyUserLinksUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EntityUserLink>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/management/accounts/{}/webproperties/{}/entityUserLinks/{}",
        args.account_id,
        args.web_property_id,
        args.link_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: EntityUserLink =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET metadata/{reportType}/columns
// -----------------------------------------------------------------------------

/// GET metadata/{reportType}/columns.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_metadata_columns_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_metadata_columns_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsMetadataColumnsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Columns>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/metadata/{}/columns",
        args.report_type,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Columns =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST provisioning/createAccountTicket
// -----------------------------------------------------------------------------

/// POST provisioning/createAccountTicket.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_provisioning_create_account_ticket_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_provisioning_create_account_ticket_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsProvisioningCreateAccountTicketArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountTicket>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/analytics/v3/provisioning/createAccountTicket",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountTicket =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST provisioning/createAccountTree
// -----------------------------------------------------------------------------

/// POST provisioning/createAccountTree.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_provisioning_create_account_tree_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_provisioning_create_account_tree_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsProvisioningCreateAccountTreeArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountTreeResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/analytics/v3/provisioning/createAccountTree",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountTreeResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST userDeletion/userDeletionRequests:upsert
// -----------------------------------------------------------------------------

/// POST userDeletion/userDeletionRequests:upsert.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_user_deletion_user_deletion_request_upsert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_user_deletion_user_deletion_request_upsert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AnalyticsUserDeletionUserDeletionRequestUpsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserDeletionRequest>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/analytics/v3/userDeletion/userDeletionRequests:upsert",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UserDeletionRequest =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
