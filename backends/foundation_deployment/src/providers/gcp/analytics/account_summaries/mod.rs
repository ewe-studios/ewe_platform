//! Auto-generated API module for gcp/analytics accountSummaries.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_analytics_account_summaries `

#![cfg(feature = "gcp_analytics_account_summaries")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `AccountSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccountSummary {
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// starred property.
    pub starred: Option<bool>,
    /// webProperties property.
    pub web_properties: Option<Vec<WebPropertySummary>>,
}

/// `ProfileSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProfileSummary {
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// starred property.
    pub starred: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
}

/// `AccountSummaries` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccountSummaries {
    /// items property.
    pub items: Option<Vec<AccountSummary>>,
    /// itemsPerPage property.
    pub items_per_page: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// nextLink property.
    pub next_link: Option<String>,
    /// previousLink property.
    pub previous_link: Option<String>,
    /// startIndex property.
    pub start_index: Option<i64>,
    /// totalResults property.
    pub total_results: Option<i64>,
    /// username property.
    pub username: Option<String>,
}

/// `WebPropertySummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WebPropertySummary {
    /// id property.
    pub id: Option<String>,
    /// internalWebPropertyId property.
    pub internal_web_property_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// level property.
    pub level: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// profiles property.
    pub profiles: Option<Vec<ProfileSummary>>,
    /// starred property.
    pub starred: Option<bool>,
    /// websiteUrl property.
    pub website_url: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`analytics.management.accountSummaries.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AnalyticsManagementAccountSummariesListArgs {
    /// Query parameter: `max-results`.
    pub max_results: Option<String>,
    /// Query parameter: `start-index`.
    pub start_index: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET management/accountSummaries
// -----------------------------------------------------------------------------

/// GET management/accountSummaries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = analytics_management_account_summaries_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn analytics_management_account_summaries_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountSummaries>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://www.googleapis.com/analytics/v3/management/accountSummaries",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountSummaries =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
