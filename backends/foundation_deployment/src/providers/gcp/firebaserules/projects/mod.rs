//! Auto-generated API module for gcp/firebaserules projects.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_firebaserules_projects `

#![cfg(feature = "gcp_firebaserules_projects")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ValueCount` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ValueCount {
    /// count property.
    pub count: Option<i64>,
    /// value property.
    pub value: Option<serde_json::Value>,
}

/// `VisitedExpression` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VisitedExpression {
    /// sourcePosition property.
    pub source_position: Option<SourcePosition>,
    /// value property.
    pub value: Option<serde_json::Value>,
}

/// `TestRulesetResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TestRulesetResponse {
    /// issues property.
    pub issues: Option<Vec<Issue>>,
    /// testResults property.
    pub test_results: Option<Vec<TestResult>>,
}

/// `FunctionCall` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FunctionCall {
    /// args property.
    pub args: Option<Vec<serde_json::Value>>,
    /// function property.
    pub function: Option<String>,
}

/// `Issue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Issue {
    /// description property.
    pub description: Option<String>,
    /// severity property.
    pub severity: Option<String>,
    /// sourcePosition property.
    pub source_position: Option<SourcePosition>,
}

/// `TestResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TestResult {
    /// debugMessages property.
    pub debug_messages: Option<Vec<String>>,
    /// errorPosition property.
    pub error_position: Option<SourcePosition>,
    /// expressionReports property.
    pub expression_reports: Option<Vec<ExpressionReport>>,
    /// functionCalls property.
    pub function_calls: Option<Vec<FunctionCall>>,
    /// state property.
    pub state: Option<String>,
    /// visitedExpressions property.
    pub visited_expressions: Option<Vec<VisitedExpression>>,
}

/// `ExpressionReport` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExpressionReport {
    /// children property.
    pub children: Option<Vec<ExpressionReport>>,
    /// sourcePosition property.
    pub source_position: Option<SourcePosition>,
    /// values property.
    pub values: Option<Vec<ValueCount>>,
}

/// `SourcePosition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SourcePosition {
    /// column property.
    pub column: Option<i64>,
    /// currentOffset property.
    pub current_offset: Option<i64>,
    /// endOffset property.
    pub end_offset: Option<i64>,
    /// fileName property.
    pub file_name: Option<String>,
    /// line property.
    pub line: Option<i64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`firebaserules.projects.test_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FirebaserulesProjectsTestArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}:test
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}:test.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = firebaserules_projects_test_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn firebaserules_projects_test_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestRulesetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://firebaserules.googleapis.com/v1/projects/{{projectsId}}:test",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestRulesetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
