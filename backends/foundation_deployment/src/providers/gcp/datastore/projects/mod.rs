//! Auto-generated API module for gcp/datastore projects.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_datastore_projects `

#![cfg(feature = "gcp_datastore_projects")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::GoogleLongrunningOperation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ReserveIdsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReserveIdsResponse {}

/// `Projection` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Projection {
    /// property property.
    pub property: Option<PropertyReference>,
}

/// `ExecutionStats` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExecutionStats {
    /// debugStats property.
    pub debug_stats: Option<serde_json::Value>,
    /// executionDuration property.
    pub execution_duration: Option<String>,
    /// readOperations property.
    pub read_operations: Option<String>,
    /// resultsReturned property.
    pub results_returned: Option<String>,
}

/// `QueryResultBatch` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct QueryResultBatch {
    /// endCursor property.
    pub end_cursor: Option<String>,
    /// entityResultType property.
    pub entity_result_type: Option<String>,
    /// entityResults property.
    pub entity_results: Option<Vec<EntityResult>>,
    /// moreResults property.
    pub more_results: Option<String>,
    /// readTime property.
    pub read_time: Option<String>,
    /// skippedCursor property.
    pub skipped_cursor: Option<String>,
    /// skippedResults property.
    pub skipped_results: Option<i64>,
    /// snapshotVersion property.
    pub snapshot_version: Option<String>,
}

/// `EntityResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntityResult {
    /// createTime property.
    pub create_time: Option<String>,
    /// cursor property.
    pub cursor: Option<String>,
    /// entity property.
    pub entity: Option<Entity>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `FindNearest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FindNearest {
    /// distanceMeasure property.
    pub distance_measure: Option<String>,
    /// distanceResultProperty property.
    pub distance_result_property: Option<String>,
    /// distanceThreshold property.
    pub distance_threshold: Option<f64>,
    /// limit property.
    pub limit: Option<i64>,
    /// queryVector property.
    pub query_vector: Option<Box<Value>>,
    /// vectorProperty property.
    pub vector_property: Option<PropertyReference>,
}

/// `Entity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Entity {
    /// key property.
    pub key: Option<Key>,
    /// properties property.
    pub properties: Option<serde_json::Value>,
}

/// `PropertyFilter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PropertyFilter {
    /// op property.
    pub op: Option<String>,
    /// property property.
    pub property: Option<PropertyReference>,
    /// value property.
    pub value: Option<Box<Value>>,
}

/// `PropertyOrder` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PropertyOrder {
    /// direction property.
    pub direction: Option<String>,
    /// property property.
    pub property: Option<PropertyReference>,
}

/// `Count` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Count {
    /// upTo property.
    pub up_to: Option<String>,
}

/// `MutationResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MutationResult {
    /// conflictDetected property.
    pub conflict_detected: Option<bool>,
    /// createTime property.
    pub create_time: Option<String>,
    /// key property.
    pub key: Option<Key>,
    /// transformResults property.
    pub transform_results: Option<Vec<Box<Value>>>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `Value` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Value {
    /// arrayValue property.
    pub array_value: Option<Box<ArrayValue>>,
    /// blobValue property.
    pub blob_value: Option<String>,
    /// booleanValue property.
    pub boolean_value: Option<bool>,
    /// doubleValue property.
    pub double_value: Option<f64>,
    /// entityValue property.
    pub entity_value: Option<Entity>,
    /// excludeFromIndexes property.
    pub exclude_from_indexes: Option<bool>,
    /// geoPointValue property.
    pub geo_point_value: Option<LatLng>,
    /// integerValue property.
    pub integer_value: Option<String>,
    /// keyValue property.
    pub key_value: Option<Key>,
    /// meaning property.
    pub meaning: Option<i64>,
    /// nullValue property.
    pub null_value: Option<String>,
    /// stringValue property.
    pub string_value: Option<String>,
    /// timestampValue property.
    pub timestamp_value: Option<String>,
}

/// `RunAggregationQueryResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RunAggregationQueryResponse {
    /// batch property.
    pub batch: Option<AggregationResultBatch>,
    /// explainMetrics property.
    pub explain_metrics: Option<ExplainMetrics>,
    /// query property.
    pub query: Option<AggregationQuery>,
    /// transaction property.
    pub transaction: Option<String>,
}

/// `RollbackResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RollbackResponse {}

/// `LatLng` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LatLng {
    /// latitude property.
    pub latitude: Option<f64>,
    /// longitude property.
    pub longitude: Option<f64>,
}

/// `Query` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Query {
    /// distinctOn property.
    pub distinct_on: Option<Vec<PropertyReference>>,
    /// endCursor property.
    pub end_cursor: Option<String>,
    /// filter property.
    pub filter: Option<Box<Filter>>,
    /// findNearest property.
    pub find_nearest: Option<FindNearest>,
    /// kind property.
    pub kind: Option<Vec<KindExpression>>,
    /// limit property.
    pub limit: Option<i64>,
    /// offset property.
    pub offset: Option<i64>,
    /// order property.
    pub order: Option<Vec<PropertyOrder>>,
    /// projection property.
    pub projection: Option<Vec<Projection>>,
    /// startCursor property.
    pub start_cursor: Option<String>,
}

/// `PlanSummary` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlanSummary {
    /// indexesUsed property.
    pub indexes_used: Option<Vec<serde_json::Value>>,
}

/// `Key` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Key {
    /// partitionId property.
    pub partition_id: Option<PartitionId>,
    /// path property.
    pub path: Option<Vec<PathElement>>,
}

/// `Avg` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Avg {
    /// property property.
    pub property: Option<PropertyReference>,
}

/// `LookupResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LookupResponse {
    /// deferred property.
    pub deferred: Option<Vec<Key>>,
    /// found property.
    pub found: Option<Vec<EntityResult>>,
    /// missing property.
    pub missing: Option<Vec<EntityResult>>,
    /// readTime property.
    pub read_time: Option<String>,
    /// transaction property.
    pub transaction: Option<String>,
}

/// `PathElement` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PathElement {
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `CommitResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CommitResponse {
    /// commitTime property.
    pub commit_time: Option<String>,
    /// indexUpdates property.
    pub index_updates: Option<i64>,
    /// mutationResults property.
    pub mutation_results: Option<Vec<MutationResult>>,
}

/// `AllocateIdsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AllocateIdsResponse {
    /// keys property.
    pub keys: Option<Vec<Key>>,
}

/// `AggregationResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AggregationResult {
    /// aggregateProperties property.
    pub aggregate_properties: Option<serde_json::Value>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `ArrayValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ArrayValue {
    /// values property.
    pub values: Option<Vec<Box<Value>>>,
}

/// `CompositeFilter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CompositeFilter {
    /// filters property.
    pub filters: Option<Vec<Box<Filter>>>,
    /// op property.
    pub op: Option<String>,
}

/// `AggregationResultBatch` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AggregationResultBatch {
    /// aggregationResults property.
    pub aggregation_results: Option<Vec<AggregationResult>>,
    /// moreResults property.
    pub more_results: Option<String>,
    /// readTime property.
    pub read_time: Option<String>,
}

/// `AggregationQuery` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AggregationQuery {
    /// aggregations property.
    pub aggregations: Option<Vec<Aggregation>>,
    /// nestedQuery property.
    pub nested_query: Option<Query>,
}

/// `Aggregation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Aggregation {
    /// alias property.
    pub alias: Option<String>,
    /// avg property.
    pub avg: Option<Avg>,
    /// count property.
    pub count: Option<Count>,
    /// sum property.
    pub sum: Option<Sum>,
}

/// `Sum` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Sum {
    /// property property.
    pub property: Option<PropertyReference>,
}

/// `PartitionId` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PartitionId {
    /// databaseId property.
    pub database_id: Option<String>,
    /// namespaceId property.
    pub namespace_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
}

/// `BeginTransactionResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BeginTransactionResponse {
    /// transaction property.
    pub transaction: Option<String>,
}

/// `RunQueryResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RunQueryResponse {
    /// batch property.
    pub batch: Option<QueryResultBatch>,
    /// explainMetrics property.
    pub explain_metrics: Option<ExplainMetrics>,
    /// query property.
    pub query: Option<Query>,
    /// transaction property.
    pub transaction: Option<String>,
}

/// `ExplainMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExplainMetrics {
    /// executionStats property.
    pub execution_stats: Option<ExecutionStats>,
    /// planSummary property.
    pub plan_summary: Option<PlanSummary>,
}

/// `PropertyReference` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PropertyReference {
    /// name property.
    pub name: Option<String>,
}

/// `KindExpression` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KindExpression {
    /// name property.
    pub name: Option<String>,
}

/// `Filter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Filter {
    /// compositeFilter property.
    pub composite_filter: Option<Box<CompositeFilter>>,
    /// propertyFilter property.
    pub property_filter: Option<PropertyFilter>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`datastore.projects.allocateIds_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsAllocateIdsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

/// Arguments for [`datastore.projects.beginTransaction_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsBeginTransactionArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

/// Arguments for [`datastore.projects.commit_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsCommitArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

/// Arguments for [`datastore.projects.export_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsExportArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

/// Arguments for [`datastore.projects.import_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsImportArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

/// Arguments for [`datastore.projects.lookup_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsLookupArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

/// Arguments for [`datastore.projects.reserveIds_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsReserveIdsArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

/// Arguments for [`datastore.projects.rollback_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsRollbackArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

/// Arguments for [`datastore.projects.runAggregationQuery_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsRunAggregationQueryArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

/// Arguments for [`datastore.projects.runQuery_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastoreProjectsRunQueryArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}:allocateIds
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}:allocateIds.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastore_projects_allocate_ids_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastore_projects_allocate_ids_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastoreProjectsAllocateIdsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AllocateIdsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:allocateIds",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AllocateIdsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}:beginTransaction
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}:beginTransaction.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastore_projects_begin_transaction_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastore_projects_begin_transaction_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastoreProjectsBeginTransactionArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BeginTransactionResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:beginTransaction",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BeginTransactionResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}:commit
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}:commit.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastore_projects_commit_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastore_projects_commit_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastoreProjectsCommitArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CommitResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:commit",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CommitResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}:export
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}:export.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastore_projects_export_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastore_projects_export_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastoreProjectsExportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:export",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}:import
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}:import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastore_projects_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastore_projects_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastoreProjectsImportArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:import",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}:lookup
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}:lookup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastore_projects_lookup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastore_projects_lookup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastoreProjectsLookupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:lookup",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LookupResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}:reserveIds
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}:reserveIds.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastore_projects_reserve_ids_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastore_projects_reserve_ids_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastoreProjectsReserveIdsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReserveIdsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:reserveIds",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReserveIdsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}:rollback
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}:rollback.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastore_projects_rollback_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastore_projects_rollback_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastoreProjectsRollbackArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RollbackResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:rollback",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RollbackResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}:runAggregationQuery
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}:runAggregationQuery.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastore_projects_run_aggregation_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastore_projects_run_aggregation_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastoreProjectsRunAggregationQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RunAggregationQueryResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:runAggregationQuery",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RunAggregationQueryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectId}:runQuery
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectId}:runQuery.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastore_projects_run_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastore_projects_run_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastoreProjectsRunQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RunQueryResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastore.googleapis.com/v1/projects/{}:runQuery",
        args.project_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RunQueryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
