//! Auto-generated API module for gcp/airquality airquality.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_airquality_airquality `

#![cfg(feature = "gcp_airquality_airquality")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `LookupHistoryRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LookupHistoryRequest {
    /// customLocalAqis property.
    pub custom_local_aqis: Option<Vec<CustomLocalAqi>>,
    /// dateTime property.
    pub date_time: Option<String>,
    /// extraComputations property.
    pub extra_computations: Option<Vec<String>>,
    /// hours property.
    pub hours: Option<i64>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// location property.
    pub location: Option<LatLng>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// period property.
    pub period: Option<Interval>,
    /// uaqiColorPalette property.
    pub uaqi_color_palette: Option<String>,
    /// universalAqi property.
    pub universal_aqi: Option<bool>,
}

/// `Color` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Color {
    /// alpha property.
    pub alpha: Option<f64>,
    /// blue property.
    pub blue: Option<f64>,
    /// green property.
    pub green: Option<f64>,
    /// red property.
    pub red: Option<f64>,
}

/// `LookupForecastRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LookupForecastRequest {
    /// customLocalAqis property.
    pub custom_local_aqis: Option<Vec<CustomLocalAqi>>,
    /// dateTime property.
    pub date_time: Option<String>,
    /// extraComputations property.
    pub extra_computations: Option<Vec<String>>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// location property.
    pub location: Option<LatLng>,
    /// pageSize property.
    pub page_size: Option<i64>,
    /// pageToken property.
    pub page_token: Option<String>,
    /// period property.
    pub period: Option<Interval>,
    /// uaqiColorPalette property.
    pub uaqi_color_palette: Option<String>,
    /// universalAqi property.
    pub universal_aqi: Option<bool>,
}

/// `Interval` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Interval {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `HttpBody` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpBody {
    /// contentType property.
    pub content_type: Option<String>,
    /// data property.
    pub data: Option<String>,
    /// extensions property.
    pub extensions: Option<Vec<serde_json::Value>>,
}

/// `LookupCurrentConditionsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LookupCurrentConditionsRequest {
    /// customLocalAqis property.
    pub custom_local_aqis: Option<Vec<CustomLocalAqi>>,
    /// extraComputations property.
    pub extra_computations: Option<Vec<String>>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// location property.
    pub location: Option<LatLng>,
    /// uaqiColorPalette property.
    pub uaqi_color_palette: Option<String>,
    /// universalAqi property.
    pub universal_aqi: Option<bool>,
}

/// `CustomLocalAqi` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomLocalAqi {
    /// aqi property.
    pub aqi: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `LookupCurrentConditionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LookupCurrentConditionsResponse {
    /// dateTime property.
    pub date_time: Option<String>,
    /// healthRecommendations property.
    pub health_recommendations: Option<HealthRecommendations>,
    /// indexes property.
    pub indexes: Option<Vec<AirQualityIndex>>,
    /// pollutants property.
    pub pollutants: Option<Vec<Pollutant>>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `HourInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HourInfo {
    /// dateTime property.
    pub date_time: Option<String>,
    /// healthRecommendations property.
    pub health_recommendations: Option<HealthRecommendations>,
    /// indexes property.
    pub indexes: Option<Vec<AirQualityIndex>>,
    /// pollutants property.
    pub pollutants: Option<Vec<Pollutant>>,
}

/// `AirQualityIndex` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AirQualityIndex {
    /// aqi property.
    pub aqi: Option<i64>,
    /// aqiDisplay property.
    pub aqi_display: Option<String>,
    /// category property.
    pub category: Option<String>,
    /// code property.
    pub code: Option<String>,
    /// color property.
    pub color: Option<Color>,
    /// displayName property.
    pub display_name: Option<String>,
    /// dominantPollutant property.
    pub dominant_pollutant: Option<String>,
}

/// `HealthRecommendations` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HealthRecommendations {
    /// athletes property.
    pub athletes: Option<String>,
    /// children property.
    pub children: Option<String>,
    /// elderly property.
    pub elderly: Option<String>,
    /// generalPopulation property.
    pub general_population: Option<String>,
    /// heartDiseasePopulation property.
    pub heart_disease_population: Option<String>,
    /// lungDiseasePopulation property.
    pub lung_disease_population: Option<String>,
    /// pregnantWomen property.
    pub pregnant_women: Option<String>,
}

/// `AdditionalInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdditionalInfo {
    /// effects property.
    pub effects: Option<String>,
    /// sources property.
    pub sources: Option<String>,
}

/// `HourlyForecast` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HourlyForecast {
    /// dateTime property.
    pub date_time: Option<String>,
    /// healthRecommendations property.
    pub health_recommendations: Option<HealthRecommendations>,
    /// indexes property.
    pub indexes: Option<Vec<AirQualityIndex>>,
    /// pollutants property.
    pub pollutants: Option<Vec<Pollutant>>,
}

/// `LookupForecastResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LookupForecastResponse {
    /// hourlyForecasts property.
    pub hourly_forecasts: Option<Vec<HourlyForecast>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `Pollutant` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Pollutant {
    /// additionalInfo property.
    pub additional_info: Option<AdditionalInfo>,
    /// code property.
    pub code: Option<String>,
    /// concentration property.
    pub concentration: Option<Concentration>,
    /// displayName property.
    pub display_name: Option<String>,
    /// fullName property.
    pub full_name: Option<String>,
}

/// `LookupHistoryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LookupHistoryResponse {
    /// hoursInfo property.
    pub hours_info: Option<Vec<HourInfo>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `LatLng` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LatLng {
    /// latitude property.
    pub latitude: Option<f64>,
    /// longitude property.
    pub longitude: Option<f64>,
}

/// `Concentration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Concentration {
    /// units property.
    pub units: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`airquality.currentConditions.lookup_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AirqualityCurrentConditionsLookupArgs {
    /// Request body.
    pub body: LookupCurrentConditionsRequest,
}

/// Arguments for [`airquality.forecast.lookup_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AirqualityForecastLookupArgs {
    /// Request body.
    pub body: LookupForecastRequest,
}

/// Arguments for [`airquality.history.lookup_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AirqualityHistoryLookupArgs {
    /// Request body.
    pub body: LookupHistoryRequest,
}

/// Arguments for [`airquality.mapTypes.heatmapTiles.lookupHeatmapTile_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AirqualityMapTypesHeatmapTilesLookupHeatmapTileArgs {
    /// Path parameter: `mapType`.
    pub map_type: String,
    /// Path parameter: `zoom`.
    pub zoom: String,
    /// Path parameter: `x`.
    pub x: String,
    /// Path parameter: `y`.
    pub y: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/currentConditions:lookup
// -----------------------------------------------------------------------------

/// POST v1/currentConditions:lookup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = airquality_current_conditions_lookup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn airquality_current_conditions_lookup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AirqualityCurrentConditionsLookupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupCurrentConditionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://airquality.googleapis.com/v1/currentConditions:lookup",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LookupCurrentConditionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/forecast:lookup
// -----------------------------------------------------------------------------

/// POST v1/forecast:lookup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = airquality_forecast_lookup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn airquality_forecast_lookup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AirqualityForecastLookupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupForecastResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://airquality.googleapis.com/v1/forecast:lookup",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LookupForecastResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/history:lookup
// -----------------------------------------------------------------------------

/// POST v1/history:lookup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = airquality_history_lookup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn airquality_history_lookup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AirqualityHistoryLookupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupHistoryResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://airquality.googleapis.com/v1/history:lookup",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: LookupHistoryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}
// -----------------------------------------------------------------------------

/// GET v1/mapTypes/{mapType}/heatmapTiles/{zoom}/{x}/{y}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = airquality_map_types_heatmap_tiles_lookup_heatmap_tile_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn airquality_map_types_heatmap_tiles_lookup_heatmap_tile_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AirqualityMapTypesHeatmapTilesLookupHeatmapTileArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://airquality.googleapis.com/v1/mapTypes/{}/heatmapTiles/{}/{}/{}",
        args.map_type, args.zoom, args.x, args.y,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: HttpBody =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
